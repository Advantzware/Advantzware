/* jobmatl.p - generated 12/02/2005 - 16:52:39 by NoSweat */

/* this program was generated by auditgn1.p and should not be modified */
/* modifications should be made to the auto generation program         */

DEFINE INPUT PARAMETER action AS CHARACTER NO-UNDO.

DEFINE VARIABLE i AS INTEGER NO-UNDO.

{methods/defines/audit.i}
FIND FIRST ttbl_header.

DEFINE WORK-TABLE ttbl_jobmatl1 NO-UNDO LIKE jobmatl.
DEFINE WORK-TABLE ttbl_jobmatl2 NO-UNDO LIKE jobmatl.

CREATE ttbl_jobmatl1.
IMPORT STREAM s_audit_file ttbl_jobmatl1.
CREATE ttbl_jobmatl2.
IF action = "UPDATE" THEN
IMPORT STREAM s_audit_file ttbl_jobmatl2.

PUT STREAM s-audit-rpt UNFORMATTED
  "Action    User ID/(User Name)            DB.Table/(Database)  Date       Time" SKIP
  "--------- ------------------------------ -------------------- ---------- --------" SKIP
  ttbl_header.v_action AT 1
  ttbl_header.v_userid AT 11
  ttbl_header.v_file AT 42
  ttbl_header.v_date FORMAT "99/99/9999" AT 63
  STRING(ttbl_header.v_time,"HH:MM:SS") AT 74.
FIND users WHERE users.user_id = v_userid NO-LOCK NO-ERROR.
IF AVAILABLE users THEN
PUT STREAM s-audit-rpt UNFORMATTED "(" AT 10 users.user_name ")".
ELSE
PUT STREAM s-audit-rpt UNFORMATTED "(user record does not exist)" AT 10.
PUT STREAM s-audit-rpt UNFORMATTED
  "(" AT 41 ttbl_header.v_db ")" SKIP(1)
  "Label: Value (database field-name)" AT 3.
IF action = "UPDATE" THEN
PUT STREAM s-audit-rpt UNFORMATTED " ( * - denotes field value change )".
PUT STREAM s-audit-rpt UNFORMATTED FILL("-",75) AT 3.
IF NOT v_field_changed OR ttbl_jobmatl1.basis_weight NE ttbl_jobmatl2.basis_weight OR action NE "UPDATE" THEN
DO:
  PUT STREAM s-audit-rpt UNFORMATTED "Basis Weight: " AT 3 ttbl_jobmatl1.basis_weight FORMAT ">>9.99" " (basis_weight)".
  IF action = "UPDATE" THEN
  DO:
    IF ttbl_jobmatl1.basis_weight NE ttbl_jobmatl2.basis_weight THEN
    PUT STREAM s-audit-rpt UNFORMATTED "*" AT 1.
    PUT STREAM s-audit-rpt UNFORMATTED "Basis Weight: " AT 3 ttbl_jobmatl2.basis_weight FORMAT ">>9.99".
  END.
END.
IF NOT v_field_changed OR ttbl_jobmatl1.blank_number NE ttbl_jobmatl2.blank_number OR action NE "UPDATE" THEN
DO:
  PUT STREAM s-audit-rpt UNFORMATTED "Blank Number: " AT 3 ttbl_jobmatl1.blank_number FORMAT ">9" " (blank_number)".
  IF action = "UPDATE" THEN
  DO:
    IF ttbl_jobmatl1.blank_number NE ttbl_jobmatl2.blank_number THEN
    PUT STREAM s-audit-rpt UNFORMATTED "*" AT 1.
    PUT STREAM s-audit-rpt UNFORMATTED "Blank Number: " AT 3 ttbl_jobmatl2.blank_number FORMAT ">9".
  END.
END.
IF NOT v_field_changed OR ttbl_jobmatl1.form_number NE ttbl_jobmatl2.form_number OR action NE "UPDATE" THEN
DO:
  PUT STREAM s-audit-rpt UNFORMATTED "Form Number: " AT 3 ttbl_jobmatl1.form_number FORMAT ">>9" " (form_number)".
  IF action = "UPDATE" THEN
  DO:
    IF ttbl_jobmatl1.form_number NE ttbl_jobmatl2.form_number THEN
    PUT STREAM s-audit-rpt UNFORMATTED "*" AT 1.
    PUT STREAM s-audit-rpt UNFORMATTED "Form Number: " AT 3 ttbl_jobmatl2.form_number FORMAT ">>9".
  END.
END.
IF NOT v_field_changed OR ttbl_jobmatl1.item_description NE ttbl_jobmatl2.item_description OR action NE "UPDATE" THEN
DO:
  PUT STREAM s-audit-rpt UNFORMATTED "Item Description: " AT 3 ttbl_jobmatl1.item_description " (item_description)".
  IF action = "UPDATE" THEN
  DO:
    IF ttbl_jobmatl1.item_description NE ttbl_jobmatl2.item_description THEN
    PUT STREAM s-audit-rpt UNFORMATTED "*" AT 1.
    PUT STREAM s-audit-rpt UNFORMATTED "Item Description: " AT 3 ttbl_jobmatl2.item_description.
  END.
END.
IF NOT v_field_changed OR ttbl_jobmatl1.item_length NE ttbl_jobmatl2.item_length OR action NE "UPDATE" THEN
DO:
  PUT STREAM s-audit-rpt UNFORMATTED "Length: " AT 3 ttbl_jobmatl1.item_length FORMAT ">>>>9.9<<<<" " (item_length)".
  IF action = "UPDATE" THEN
  DO:
    IF ttbl_jobmatl1.item_length NE ttbl_jobmatl2.item_length THEN
    PUT STREAM s-audit-rpt UNFORMATTED "*" AT 1.
    PUT STREAM s-audit-rpt UNFORMATTED "Length: " AT 3 ttbl_jobmatl2.item_length FORMAT ">>>>9.9<<<<".
  END.
END.
IF NOT v_field_changed OR ttbl_jobmatl1.item_number NE ttbl_jobmatl2.item_number OR action NE "UPDATE" THEN
DO:
  PUT STREAM s-audit-rpt UNFORMATTED "Item Number: " AT 3 ttbl_jobmatl1.item_number " (item_number)".
  IF action = "UPDATE" THEN
  DO:
    IF ttbl_jobmatl1.item_number NE ttbl_jobmatl2.item_number THEN
    PUT STREAM s-audit-rpt UNFORMATTED "*" AT 1.
    PUT STREAM s-audit-rpt UNFORMATTED "Item Number: " AT 3 ttbl_jobmatl2.item_number.
  END.
END.
IF NOT v_field_changed OR ttbl_jobmatl1.item_width NE ttbl_jobmatl2.item_width OR action NE "UPDATE" THEN
DO:
  PUT STREAM s-audit-rpt UNFORMATTED "Width: " AT 3 ttbl_jobmatl1.item_width FORMAT ">>>>9.9<<<<" " (item_width)".
  IF action = "UPDATE" THEN
  DO:
    IF ttbl_jobmatl1.item_width NE ttbl_jobmatl2.item_width THEN
    PUT STREAM s-audit-rpt UNFORMATTED "*" AT 1.
    PUT STREAM s-audit-rpt UNFORMATTED "Width: " AT 3 ttbl_jobmatl2.item_width FORMAT ">>>>9.9<<<<".
  END.
END.
IF NOT v_field_changed OR ttbl_jobmatl1.job NE ttbl_jobmatl2.job OR action NE "UPDATE" THEN
DO:
  PUT STREAM s-audit-rpt UNFORMATTED "Job Number: " AT 3 ttbl_jobmatl1.job " (job)".
  IF action = "UPDATE" THEN
  DO:
    IF ttbl_jobmatl1.job NE ttbl_jobmatl2.job THEN
    PUT STREAM s-audit-rpt UNFORMATTED "*" AT 1.
    PUT STREAM s-audit-rpt UNFORMATTED "Job Number: " AT 3 ttbl_jobmatl2.job.
  END.
END.
IF NOT v_field_changed OR ttbl_jobmatl1.material_type NE ttbl_jobmatl2.material_type OR action NE "UPDATE" THEN
DO:
  PUT STREAM s-audit-rpt UNFORMATTED "Material Type: " AT 3 ttbl_jobmatl1.material_type " (material_type)".
  IF action = "UPDATE" THEN
  DO:
    IF ttbl_jobmatl1.material_type NE ttbl_jobmatl2.material_type THEN
    PUT STREAM s-audit-rpt UNFORMATTED "*" AT 1.
    PUT STREAM s-audit-rpt UNFORMATTED "Material Type: " AT 3 ttbl_jobmatl2.material_type.
  END.
END.
IF NOT v_field_changed OR ttbl_jobmatl1.number_up NE ttbl_jobmatl2.number_up OR action NE "UPDATE" THEN
DO:
  PUT STREAM s-audit-rpt UNFORMATTED "Number Up: " AT 3 ttbl_jobmatl1.number_up FORMAT ">>9" " (number_up)".
  IF action = "UPDATE" THEN
  DO:
    IF ttbl_jobmatl1.number_up NE ttbl_jobmatl2.number_up THEN
    PUT STREAM s-audit-rpt UNFORMATTED "*" AT 1.
    PUT STREAM s-audit-rpt UNFORMATTED "Number Up: " AT 3 ttbl_jobmatl2.number_up FORMAT ">>9".
  END.
END.
IF NOT v_field_changed OR ttbl_jobmatl1.qty NE ttbl_jobmatl2.qty OR action NE "UPDATE" THEN
DO:
  PUT STREAM s-audit-rpt UNFORMATTED "Quantity: " AT 3 ttbl_jobmatl1.qty FORMAT ">>,>>>,>>9.99" " (qty)".
  IF action = "UPDATE" THEN
  DO:
    IF ttbl_jobmatl1.qty NE ttbl_jobmatl2.qty THEN
    PUT STREAM s-audit-rpt UNFORMATTED "*" AT 1.
    PUT STREAM s-audit-rpt UNFORMATTED "Quantity: " AT 3 ttbl_jobmatl2.qty FORMAT ">>,>>>,>>9.99".
  END.
END.
IF NOT v_field_changed OR ttbl_jobmatl1.qty_uom NE ttbl_jobmatl2.qty_uom OR action NE "UPDATE" THEN
DO:
  PUT STREAM s-audit-rpt UNFORMATTED "Quantity UOM: " AT 3 ttbl_jobmatl1.qty_uom " (qty_uom)".
  IF action = "UPDATE" THEN
  DO:
    IF ttbl_jobmatl1.qty_uom NE ttbl_jobmatl2.qty_uom THEN
    PUT STREAM s-audit-rpt UNFORMATTED "*" AT 1.
    PUT STREAM s-audit-rpt UNFORMATTED "Quantity UOM: " AT 3 ttbl_jobmatl2.qty_uom.
  END.
END.
IF v_show_index THEN
DO:
  PUT STREAM s-audit-rpt UNFORMATTED "Index Fields - Label: Value (database field-name)" AT 5.
  PUT STREAM s-audit-rpt UNFORMATTED "Job Number: " AT 7.
  IF action = "UPDATE" THEN
  PUT STREAM s-audit-rpt UNFORMATTED ttbl_jobmatl2.job " (job)".
  ELSE
  PUT STREAM s-audit-rpt UNFORMATTED ttbl_jobmatl1.job " (job)".
  PUT STREAM s-audit-rpt UNFORMATTED "Form Number: " AT 7.
  IF action = "UPDATE" THEN
  PUT STREAM s-audit-rpt UNFORMATTED ttbl_jobmatl2.form_number FORMAT ">>9" " (form_number)".
  ELSE
  PUT STREAM s-audit-rpt UNFORMATTED ttbl_jobmatl1.form_number FORMAT ">>9" " (form_number)".
  PUT STREAM s-audit-rpt UNFORMATTED "Blank Number: " AT 7.
  IF action = "UPDATE" THEN
  PUT STREAM s-audit-rpt UNFORMATTED ttbl_jobmatl2.blank_number FORMAT ">9" " (blank_number)".
  ELSE
  PUT STREAM s-audit-rpt UNFORMATTED ttbl_jobmatl1.blank_number FORMAT ">9" " (blank_number)".
  PUT STREAM s-audit-rpt UNFORMATTED "Item Number: " AT 7.
  IF action = "UPDATE" THEN
  PUT STREAM s-audit-rpt UNFORMATTED ttbl_jobmatl2.item_number " (item_number)".
  ELSE
  PUT STREAM s-audit-rpt UNFORMATTED ttbl_jobmatl1.item_number " (item_number)".
END.
PUT STREAM s-audit-rpt UNFORMATTED SKIP(1).
