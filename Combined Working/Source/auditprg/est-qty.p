/* est-qty.p - generated 12/09/2005 - 15:42:44 by NoSweat */

/* this program was generated by auditgn1.p and should not be modified */
/* modifications should be made to the auto generation program         */

DEFINE INPUT PARAMETER action AS CHARACTER NO-UNDO.

DEFINE VARIABLE i AS INTEGER NO-UNDO.

{methods/defines/audit.i}
FIND FIRST ttbl_header.

DEFINE WORK-TABLE ttbl_est-qty1 NO-UNDO LIKE est-qty.
DEFINE WORK-TABLE ttbl_est-qty2 NO-UNDO LIKE est-qty.

CREATE ttbl_est-qty1.
IMPORT STREAM s_audit_file ttbl_est-qty1.
CREATE ttbl_est-qty2.
IF action = "UPDATE" THEN
IMPORT STREAM s_audit_file ttbl_est-qty2.

PUT STREAM s-audit-rpt UNFORMATTED
  "Action    User ID/(User Name)            DB.Table/(Database)  Date       Time" SKIP
  "--------- ------------------------------ -------------------- ---------- --------" SKIP
  ttbl_header.v_action AT 1
  ttbl_header.v_userid AT 11
  ttbl_header.v_file AT 42
  ttbl_header.v_date FORMAT "99/99/9999" AT 63
  STRING(ttbl_header.v_time,"HH:MM:SS") AT 74.
FIND users WHERE users.user_id = v_userid NO-LOCK NO-ERROR.
IF AVAILABLE users THEN
PUT STREAM s-audit-rpt UNFORMATTED "(" AT 10 users.user_name ")".
ELSE
PUT STREAM s-audit-rpt UNFORMATTED "(user record does not exist)" AT 10.
PUT STREAM s-audit-rpt UNFORMATTED
  "(" AT 41 ttbl_header.v_db ")" SKIP(1)
  "Label: Value (database field-name)" AT 3.
IF action = "UPDATE" THEN
PUT STREAM s-audit-rpt UNFORMATTED " ( * - denotes field value change )".
PUT STREAM s-audit-rpt UNFORMATTED FILL("-",75) AT 3.
IF NOT v_field_changed OR ttbl_est-qty1.company NE ttbl_est-qty2.company OR action NE "UPDATE" THEN
DO:
  PUT STREAM s-audit-rpt UNFORMATTED "Company: " AT 3 ttbl_est-qty1.company " (company)".
  IF action = "UPDATE" THEN
  DO:
    IF ttbl_est-qty1.company NE ttbl_est-qty2.company THEN
    PUT STREAM s-audit-rpt UNFORMATTED "*" AT 1.
    PUT STREAM s-audit-rpt UNFORMATTED "Company: " AT 3 ttbl_est-qty2.company.
  END.
END.
IF NOT v_field_changed OR ttbl_est-qty1.e-num NE ttbl_est-qty2.e-num OR action NE "UPDATE" THEN
DO:
  PUT STREAM s-audit-rpt UNFORMATTED "Est.#: " AT 3 ttbl_est-qty1.e-num FORMAT ">>>>>9" " (e-num)".
  IF action = "UPDATE" THEN
  DO:
    IF ttbl_est-qty1.e-num NE ttbl_est-qty2.e-num THEN
    PUT STREAM s-audit-rpt UNFORMATTED "*" AT 1.
    PUT STREAM s-audit-rpt UNFORMATTED "Est.#: " AT 3 ttbl_est-qty2.e-num FORMAT ">>>>>9".
  END.
END.
IF NOT v_field_changed OR ttbl_est-qty1.eqty NE ttbl_est-qty2.eqty OR action NE "UPDATE" THEN
DO:
  PUT STREAM s-audit-rpt UNFORMATTED "Qty: " AT 3 ttbl_est-qty1.eqty FORMAT "->>,>>9.99" " (eqty)".
  IF action = "UPDATE" THEN
  DO:
    IF ttbl_est-qty1.eqty NE ttbl_est-qty2.eqty THEN
    PUT STREAM s-audit-rpt UNFORMATTED "*" AT 1.
    PUT STREAM s-audit-rpt UNFORMATTED "Qty: " AT 3 ttbl_est-qty2.eqty FORMAT "->>,>>9.99".
  END.
END.
IF NOT v_field_changed OR ttbl_est-qty1.est-no NE ttbl_est-qty2.est-no OR action NE "UPDATE" THEN
DO:
  PUT STREAM s-audit-rpt UNFORMATTED "Estimate #: " AT 3 ttbl_est-qty1.est-no " (est-no)".
  IF action = "UPDATE" THEN
  DO:
    IF ttbl_est-qty1.est-no NE ttbl_est-qty2.est-no THEN
    PUT STREAM s-audit-rpt UNFORMATTED "*" AT 1.
    PUT STREAM s-audit-rpt UNFORMATTED "Estimate #: " AT 3 ttbl_est-qty2.est-no.
  END.
END.
DO i = 1 TO 99:
IF NOT v_field_changed OR ttbl_est-qty1.qty[i] NE ttbl_est-qty2.qty[i] OR action NE "UPDATE" THEN
DO:
  PUT STREAM s-audit-rpt UNFORMATTED "Qty[" AT 3 i "]: " ttbl_est-qty1.qty[i] FORMAT "->>,>>9.99" " (qty[" i "])".
  IF action = "UPDATE" THEN
  DO:
    IF ttbl_est-qty1.qty[i] NE ttbl_est-qty2.qty[i] THEN
    PUT STREAM s-audit-rpt UNFORMATTED "*" AT 1.
    PUT STREAM s-audit-rpt UNFORMATTED "Qty[" AT 3 i "]: " ttbl_est-qty2.qty[i] FORMAT "->>,>>9.99".
  END.
END.
END.
DO i = 1 TO 99:
IF NOT v_field_changed OR ttbl_est-qty1.qty-date[i] NE ttbl_est-qty2.qty-date[i] OR action NE "UPDATE" THEN
DO:
  PUT STREAM s-audit-rpt UNFORMATTED "Quote Date[" AT 3 i "]: " ttbl_est-qty1.qty-date[i] FORMAT "99/99/9999" " (qty-date[" i "])".
  IF action = "UPDATE" THEN
  DO:
    IF ttbl_est-qty1.qty-date[i] NE ttbl_est-qty2.qty-date[i] THEN
    PUT STREAM s-audit-rpt UNFORMATTED "*" AT 1.
    PUT STREAM s-audit-rpt UNFORMATTED "Quote Date[" AT 3 i "]: " ttbl_est-qty2.qty-date[i] FORMAT "99/99/9999".
  END.
END.
END.
IF NOT v_field_changed OR ttbl_est-qty1.qty-del NE ttbl_est-qty2.qty-del OR action NE "UPDATE" THEN
DO:
  PUT STREAM s-audit-rpt UNFORMATTED "# of Deliveries: " AT 3 ttbl_est-qty1.qty-del FORMAT ">>9" " (qty-del)".
  IF action = "UPDATE" THEN
  DO:
    IF ttbl_est-qty1.qty-del NE ttbl_est-qty2.qty-del THEN
    PUT STREAM s-audit-rpt UNFORMATTED "*" AT 1.
    PUT STREAM s-audit-rpt UNFORMATTED "# of Deliveries: " AT 3 ttbl_est-qty2.qty-del FORMAT ">>9".
  END.
END.
DO i = 1 TO 99:
IF NOT v_field_changed OR ttbl_est-qty1.qty-price[i] NE ttbl_est-qty2.qty-price[i] OR action NE "UPDATE" THEN
DO:
  PUT STREAM s-audit-rpt UNFORMATTED "Price[" AT 3 i "]: " ttbl_est-qty1.qty-price[i] FORMAT "->>,>>9.99" " (qty-price[" i "])".
  IF action = "UPDATE" THEN
  DO:
    IF ttbl_est-qty1.qty-price[i] NE ttbl_est-qty2.qty-price[i] THEN
    PUT STREAM s-audit-rpt UNFORMATTED "*" AT 1.
    PUT STREAM s-audit-rpt UNFORMATTED "Price[" AT 3 i "]: " ttbl_est-qty2.qty-price[i] FORMAT "->>,>>9.99".
  END.
END.
END.
DO i = 1 TO 99:
IF NOT v_field_changed OR ttbl_est-qty1.qty-uom[i] NE ttbl_est-qty2.qty-uom[i] OR action NE "UPDATE" THEN
DO:
  PUT STREAM s-audit-rpt UNFORMATTED "UOM[" AT 3 i "]: " ttbl_est-qty1.qty-uom[i] " (qty-uom[" i "])".
  IF action = "UPDATE" THEN
  DO:
    IF ttbl_est-qty1.qty-uom[i] NE ttbl_est-qty2.qty-uom[i] THEN
    PUT STREAM s-audit-rpt UNFORMATTED "*" AT 1.
    PUT STREAM s-audit-rpt UNFORMATTED "UOM[" AT 3 i "]: " ttbl_est-qty2.qty-uom[i].
  END.
END.
END.
IF v_show_index THEN
DO:
  PUT STREAM s-audit-rpt UNFORMATTED "Index Fields - Label: Value (database field-name)" AT 5.
  PUT STREAM s-audit-rpt UNFORMATTED "Company: " AT 7.
  IF action = "UPDATE" THEN
  PUT STREAM s-audit-rpt UNFORMATTED ttbl_est-qty2.company " (company)".
  ELSE
  PUT STREAM s-audit-rpt UNFORMATTED ttbl_est-qty1.company " (company)".
  PUT STREAM s-audit-rpt UNFORMATTED "Estimate #: " AT 7.
  IF action = "UPDATE" THEN
  PUT STREAM s-audit-rpt UNFORMATTED ttbl_est-qty2.est-no " (est-no)".
  ELSE
  PUT STREAM s-audit-rpt UNFORMATTED ttbl_est-qty1.est-no " (est-no)".
  PUT STREAM s-audit-rpt UNFORMATTED "Qty: " AT 7.
  IF action = "UPDATE" THEN
  PUT STREAM s-audit-rpt UNFORMATTED ttbl_est-qty2.eqty FORMAT "->>,>>9.99" " (eqty)".
  ELSE
  PUT STREAM s-audit-rpt UNFORMATTED ttbl_est-qty1.eqty FORMAT "->>,>>9.99" " (eqty)".
END.
PUT STREAM s-audit-rpt UNFORMATTED SKIP(1).
