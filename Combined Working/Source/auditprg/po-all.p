/* po-all.p - generated 12/09/2005 - 15:42:51 by NoSweat */

/* this program was generated by auditgn1.p and should not be modified */
/* modifications should be made to the auto generation program         */

DEFINE INPUT PARAMETER action AS CHARACTER NO-UNDO.

DEFINE VARIABLE i AS INTEGER NO-UNDO.

{methods/defines/audit.i}
FIND FIRST ttbl_header.

DEFINE WORK-TABLE ttbl_po-all1 NO-UNDO LIKE po-all.
DEFINE WORK-TABLE ttbl_po-all2 NO-UNDO LIKE po-all.

CREATE ttbl_po-all1.
IMPORT STREAM s_audit_file ttbl_po-all1.
CREATE ttbl_po-all2.
IF action = "UPDATE" THEN
IMPORT STREAM s_audit_file ttbl_po-all2.

PUT STREAM s-audit-rpt UNFORMATTED
  "Action    User ID/(User Name)            DB.Table/(Database)  Date       Time" SKIP
  "--------- ------------------------------ -------------------- ---------- --------" SKIP
  ttbl_header.v_action AT 1
  ttbl_header.v_userid AT 11
  ttbl_header.v_file AT 42
  ttbl_header.v_date FORMAT "99/99/9999" AT 63
  STRING(ttbl_header.v_time,"HH:MM:SS") AT 74.
FIND users WHERE users.user_id = v_userid NO-LOCK NO-ERROR.
IF AVAILABLE users THEN
PUT STREAM s-audit-rpt UNFORMATTED "(" AT 10 users.user_name ")".
ELSE
PUT STREAM s-audit-rpt UNFORMATTED "(user record does not exist)" AT 10.
PUT STREAM s-audit-rpt UNFORMATTED
  "(" AT 41 ttbl_header.v_db ")" SKIP(1)
  "Label: Value (database field-name)" AT 3.
IF action = "UPDATE" THEN
PUT STREAM s-audit-rpt UNFORMATTED " ( * - denotes field value change )".
PUT STREAM s-audit-rpt UNFORMATTED FILL("-",75) AT 3.
IF NOT v_field_changed OR ttbl_po-all1.blank-no NE ttbl_po-all2.blank-no OR action NE "UPDATE" THEN
DO:
  PUT STREAM s-audit-rpt UNFORMATTED "BLANK: " AT 3 ttbl_po-all1.blank-no FORMAT ">9" " (blank-no)".
  IF action = "UPDATE" THEN
  DO:
    IF ttbl_po-all1.blank-no NE ttbl_po-all2.blank-no THEN
    PUT STREAM s-audit-rpt UNFORMATTED "*" AT 1.
    PUT STREAM s-audit-rpt UNFORMATTED "BLANK: " AT 3 ttbl_po-all2.blank-no FORMAT ">9".
  END.
END.
IF NOT v_field_changed OR ttbl_po-all1.company NE ttbl_po-all2.company OR action NE "UPDATE" THEN
DO:
  PUT STREAM s-audit-rpt UNFORMATTED "Company: " AT 3 ttbl_po-all1.company " (company)".
  IF action = "UPDATE" THEN
  DO:
    IF ttbl_po-all1.company NE ttbl_po-all2.company THEN
    PUT STREAM s-audit-rpt UNFORMATTED "*" AT 1.
    PUT STREAM s-audit-rpt UNFORMATTED "Company: " AT 3 ttbl_po-all2.company.
  END.
END.
IF NOT v_field_changed OR ttbl_po-all1.form-no NE ttbl_po-all2.form-no OR action NE "UPDATE" THEN
DO:
  PUT STREAM s-audit-rpt UNFORMATTED "FORM: " AT 3 ttbl_po-all1.form-no FORMAT ">9" " (form-no)".
  IF action = "UPDATE" THEN
  DO:
    IF ttbl_po-all1.form-no NE ttbl_po-all2.form-no THEN
    PUT STREAM s-audit-rpt UNFORMATTED "*" AT 1.
    PUT STREAM s-audit-rpt UNFORMATTED "FORM: " AT 3 ttbl_po-all2.form-no FORMAT ">9".
  END.
END.
IF NOT v_field_changed OR ttbl_po-all1.i-no NE ttbl_po-all2.i-no OR action NE "UPDATE" THEN
DO:
  PUT STREAM s-audit-rpt UNFORMATTED "Item#: " AT 3 ttbl_po-all1.i-no " (i-no)".
  IF action = "UPDATE" THEN
  DO:
    IF ttbl_po-all1.i-no NE ttbl_po-all2.i-no THEN
    PUT STREAM s-audit-rpt UNFORMATTED "*" AT 1.
    PUT STREAM s-audit-rpt UNFORMATTED "Item#: " AT 3 ttbl_po-all2.i-no.
  END.
END.
IF NOT v_field_changed OR ttbl_po-all1.job NE ttbl_po-all2.job OR action NE "UPDATE" THEN
DO:
  PUT STREAM s-audit-rpt UNFORMATTED "Unique Internial Job Number: " AT 3 ttbl_po-all1.job FORMAT ">>>>>>9" " (job)".
  IF action = "UPDATE" THEN
  DO:
    IF ttbl_po-all1.job NE ttbl_po-all2.job THEN
    PUT STREAM s-audit-rpt UNFORMATTED "*" AT 1.
    PUT STREAM s-audit-rpt UNFORMATTED "Unique Internial Job Number: " AT 3 ttbl_po-all2.job FORMAT ">>>>>>9".
  END.
END.
IF NOT v_field_changed OR ttbl_po-all1.job-no NE ttbl_po-all2.job-no OR action NE "UPDATE" THEN
DO:
  PUT STREAM s-audit-rpt UNFORMATTED "Job Number: " AT 3 ttbl_po-all1.job-no " (job-no)".
  IF action = "UPDATE" THEN
  DO:
    IF ttbl_po-all1.job-no NE ttbl_po-all2.job-no THEN
    PUT STREAM s-audit-rpt UNFORMATTED "*" AT 1.
    PUT STREAM s-audit-rpt UNFORMATTED "Job Number: " AT 3 ttbl_po-all2.job-no.
  END.
END.
IF NOT v_field_changed OR ttbl_po-all1.job-no2 NE ttbl_po-all2.job-no2 OR action NE "UPDATE" THEN
DO:
  PUT STREAM s-audit-rpt UNFORMATTED "Run #: " AT 3 ttbl_po-all1.job-no2 FORMAT ">9" " (job-no2)".
  IF action = "UPDATE" THEN
  DO:
    IF ttbl_po-all1.job-no2 NE ttbl_po-all2.job-no2 THEN
    PUT STREAM s-audit-rpt UNFORMATTED "*" AT 1.
    PUT STREAM s-audit-rpt UNFORMATTED "Run #: " AT 3 ttbl_po-all2.job-no2 FORMAT ">9".
  END.
END.
IF NOT v_field_changed OR ttbl_po-all1.line NE ttbl_po-all2.line OR action NE "UPDATE" THEN
DO:
  PUT STREAM s-audit-rpt UNFORMATTED "Line: " AT 3 ttbl_po-all1.line FORMAT "99" " (line)".
  IF action = "UPDATE" THEN
  DO:
    IF ttbl_po-all1.line NE ttbl_po-all2.line THEN
    PUT STREAM s-audit-rpt UNFORMATTED "*" AT 1.
    PUT STREAM s-audit-rpt UNFORMATTED "Line: " AT 3 ttbl_po-all2.line FORMAT "99".
  END.
END.
IF NOT v_field_changed OR ttbl_po-all1.po-no NE ttbl_po-all2.po-no OR action NE "UPDATE" THEN
DO:
  PUT STREAM s-audit-rpt UNFORMATTED "Purchase Order#: " AT 3 ttbl_po-all1.po-no FORMAT ">>>>>9" " (po-no)".
  IF action = "UPDATE" THEN
  DO:
    IF ttbl_po-all1.po-no NE ttbl_po-all2.po-no THEN
    PUT STREAM s-audit-rpt UNFORMATTED "*" AT 1.
    PUT STREAM s-audit-rpt UNFORMATTED "Purchase Order#: " AT 3 ttbl_po-all2.po-no FORMAT ">>>>>9".
  END.
END.
IF NOT v_field_changed OR ttbl_po-all1.qty-all NE ttbl_po-all2.qty-all OR action NE "UPDATE" THEN
DO:
  PUT STREAM s-audit-rpt UNFORMATTED "Qty Allocated: " AT 3 ttbl_po-all1.qty-all FORMAT ">>>,>>9.99<" " (qty-all)".
  IF action = "UPDATE" THEN
  DO:
    IF ttbl_po-all1.qty-all NE ttbl_po-all2.qty-all THEN
    PUT STREAM s-audit-rpt UNFORMATTED "*" AT 1.
    PUT STREAM s-audit-rpt UNFORMATTED "Qty Allocated: " AT 3 ttbl_po-all2.qty-all FORMAT ">>>,>>9.99<".
  END.
END.
IF v_show_index THEN
DO:
  PUT STREAM s-audit-rpt UNFORMATTED "Index Fields - Label: Value (database field-name)" AT 5.
  PUT STREAM s-audit-rpt UNFORMATTED "Company: " AT 7.
  IF action = "UPDATE" THEN
  PUT STREAM s-audit-rpt UNFORMATTED ttbl_po-all2.company " (company)".
  ELSE
  PUT STREAM s-audit-rpt UNFORMATTED ttbl_po-all1.company " (company)".
  PUT STREAM s-audit-rpt UNFORMATTED "Purchase Order#: " AT 7.
  IF action = "UPDATE" THEN
  PUT STREAM s-audit-rpt UNFORMATTED ttbl_po-all2.po-no FORMAT ">>>>>9" " (po-no)".
  ELSE
  PUT STREAM s-audit-rpt UNFORMATTED ttbl_po-all1.po-no FORMAT ">>>>>9" " (po-no)".
  PUT STREAM s-audit-rpt UNFORMATTED "Line: " AT 7.
  IF action = "UPDATE" THEN
  PUT STREAM s-audit-rpt UNFORMATTED ttbl_po-all2.line FORMAT "99" " (line)".
  ELSE
  PUT STREAM s-audit-rpt UNFORMATTED ttbl_po-all1.line FORMAT "99" " (line)".
  PUT STREAM s-audit-rpt UNFORMATTED "Job Number: " AT 7.
  IF action = "UPDATE" THEN
  PUT STREAM s-audit-rpt UNFORMATTED ttbl_po-all2.job-no " (job-no)".
  ELSE
  PUT STREAM s-audit-rpt UNFORMATTED ttbl_po-all1.job-no " (job-no)".
  PUT STREAM s-audit-rpt UNFORMATTED "Run #: " AT 7.
  IF action = "UPDATE" THEN
  PUT STREAM s-audit-rpt UNFORMATTED ttbl_po-all2.job-no2 FORMAT ">9" " (job-no2)".
  ELSE
  PUT STREAM s-audit-rpt UNFORMATTED ttbl_po-all1.job-no2 FORMAT ">9" " (job-no2)".
  PUT STREAM s-audit-rpt UNFORMATTED "FORM: " AT 7.
  IF action = "UPDATE" THEN
  PUT STREAM s-audit-rpt UNFORMATTED ttbl_po-all2.form-no FORMAT ">9" " (form-no)".
  ELSE
  PUT STREAM s-audit-rpt UNFORMATTED ttbl_po-all1.form-no FORMAT ">9" " (form-no)".
END.
PUT STREAM s-audit-rpt UNFORMATTED SKIP(1).
