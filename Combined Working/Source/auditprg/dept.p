/* dept.p - generated 12/09/2005 - 15:42:41 by NoSweat */

/* this program was generated by auditgn1.p and should not be modified */
/* modifications should be made to the auto generation program         */

DEFINE INPUT PARAMETER action AS CHARACTER NO-UNDO.

DEFINE VARIABLE i AS INTEGER NO-UNDO.

{methods/defines/audit.i}
FIND FIRST ttbl_header.

DEFINE WORK-TABLE ttbl_dept1 NO-UNDO LIKE dept.
DEFINE WORK-TABLE ttbl_dept2 NO-UNDO LIKE dept.

CREATE ttbl_dept1.
IMPORT STREAM s_audit_file ttbl_dept1.
CREATE ttbl_dept2.
IF action = "UPDATE" THEN
IMPORT STREAM s_audit_file ttbl_dept2.

PUT STREAM s-audit-rpt UNFORMATTED
  "Action    User ID/(User Name)            DB.Table/(Database)  Date       Time" SKIP
  "--------- ------------------------------ -------------------- ---------- --------" SKIP
  ttbl_header.v_action AT 1
  ttbl_header.v_userid AT 11
  ttbl_header.v_file AT 42
  ttbl_header.v_date FORMAT "99/99/9999" AT 63
  STRING(ttbl_header.v_time,"HH:MM:SS") AT 74.
FIND users WHERE users.user_id = v_userid NO-LOCK NO-ERROR.
IF AVAILABLE users THEN
PUT STREAM s-audit-rpt UNFORMATTED "(" AT 10 users.user_name ")".
ELSE
PUT STREAM s-audit-rpt UNFORMATTED "(user record does not exist)" AT 10.
PUT STREAM s-audit-rpt UNFORMATTED
  "(" AT 41 ttbl_header.v_db ")" SKIP(1)
  "Label: Value (database field-name)" AT 3.
IF action = "UPDATE" THEN
PUT STREAM s-audit-rpt UNFORMATTED " ( * - denotes field value change )".
PUT STREAM s-audit-rpt UNFORMATTED FILL("-",75) AT 3.
IF NOT v_field_changed OR ttbl_dept1.code NE ttbl_dept2.code OR action NE "UPDATE" THEN
DO:
  PUT STREAM s-audit-rpt UNFORMATTED "Code: " AT 3 ttbl_dept1.code " (code)".
  IF action = "UPDATE" THEN
  DO:
    IF ttbl_dept1.code NE ttbl_dept2.code THEN
    PUT STREAM s-audit-rpt UNFORMATTED "*" AT 1.
    PUT STREAM s-audit-rpt UNFORMATTED "Code: " AT 3 ttbl_dept2.code.
  END.
END.
IF NOT v_field_changed OR ttbl_dept1.company NE ttbl_dept2.company OR action NE "UPDATE" THEN
DO:
  PUT STREAM s-audit-rpt UNFORMATTED "Company: " AT 3 ttbl_dept1.company " (company)".
  IF action = "UPDATE" THEN
  DO:
    IF ttbl_dept1.company NE ttbl_dept2.company THEN
    PUT STREAM s-audit-rpt UNFORMATTED "*" AT 1.
    PUT STREAM s-audit-rpt UNFORMATTED "Company: " AT 3 ttbl_dept2.company.
  END.
END.
IF NOT v_field_changed OR ttbl_dept1.corr NE ttbl_dept2.corr OR action NE "UPDATE" THEN
DO:
  PUT STREAM s-audit-rpt UNFORMATTED "Corr: " AT 3 ttbl_dept1.corr FORMAT ">9" " (corr)".
  IF action = "UPDATE" THEN
  DO:
    IF ttbl_dept1.corr NE ttbl_dept2.corr THEN
    PUT STREAM s-audit-rpt UNFORMATTED "*" AT 1.
    PUT STREAM s-audit-rpt UNFORMATTED "Corr: " AT 3 ttbl_dept2.corr FORMAT ">9".
  END.
END.
IF NOT v_field_changed OR ttbl_dept1.dscr NE ttbl_dept2.dscr OR action NE "UPDATE" THEN
DO:
  PUT STREAM s-audit-rpt UNFORMATTED "Description: " AT 3 ttbl_dept1.dscr " (dscr)".
  IF action = "UPDATE" THEN
  DO:
    IF ttbl_dept1.dscr NE ttbl_dept2.dscr THEN
    PUT STREAM s-audit-rpt UNFORMATTED "*" AT 1.
    PUT STREAM s-audit-rpt UNFORMATTED "Description: " AT 3 ttbl_dept2.dscr.
  END.
END.
IF NOT v_field_changed OR ttbl_dept1.fc NE ttbl_dept2.fc OR action NE "UPDATE" THEN
DO:
  PUT STREAM s-audit-rpt UNFORMATTED "Fold: " AT 3 ttbl_dept1.fc FORMAT ">9" " (fc)".
  IF action = "UPDATE" THEN
  DO:
    IF ttbl_dept1.fc NE ttbl_dept2.fc THEN
    PUT STREAM s-audit-rpt UNFORMATTED "*" AT 1.
    PUT STREAM s-audit-rpt UNFORMATTED "Fold: " AT 3 ttbl_dept2.fc FORMAT ">9".
  END.
END.
IF NOT v_field_changed OR ttbl_dept1.industry NE ttbl_dept2.industry OR action NE "UPDATE" THEN
DO:
  PUT STREAM s-audit-rpt UNFORMATTED "Industry: " AT 3 ttbl_dept1.industry " (industry)".
  IF action = "UPDATE" THEN
  DO:
    IF ttbl_dept1.industry NE ttbl_dept2.industry THEN
    PUT STREAM s-audit-rpt UNFORMATTED "*" AT 1.
    PUT STREAM s-audit-rpt UNFORMATTED "Industry: " AT 3 ttbl_dept2.industry.
  END.
END.
IF NOT v_field_changed OR ttbl_dept1.seq NE ttbl_dept2.seq OR action NE "UPDATE" THEN
DO:
  PUT STREAM s-audit-rpt UNFORMATTED "Sequence: " AT 3 ttbl_dept1.seq FORMAT ">9" " (seq)".
  IF action = "UPDATE" THEN
  DO:
    IF ttbl_dept1.seq NE ttbl_dept2.seq THEN
    PUT STREAM s-audit-rpt UNFORMATTED "*" AT 1.
    PUT STREAM s-audit-rpt UNFORMATTED "Sequence: " AT 3 ttbl_dept2.seq FORMAT ">9".
  END.
END.
IF NOT v_field_changed OR ttbl_dept1.setup NE ttbl_dept2.setup OR action NE "UPDATE" THEN
DO:
  PUT STREAM s-audit-rpt UNFORMATTED "Setup: " AT 3 ttbl_dept1.setup FORMAT ">9" " (setup)".
  IF action = "UPDATE" THEN
  DO:
    IF ttbl_dept1.setup NE ttbl_dept2.setup THEN
    PUT STREAM s-audit-rpt UNFORMATTED "*" AT 1.
    PUT STREAM s-audit-rpt UNFORMATTED "Setup: " AT 3 ttbl_dept2.setup FORMAT ">9".
  END.
END.
IF NOT v_field_changed OR ttbl_dept1.therm NE ttbl_dept2.therm OR action NE "UPDATE" THEN
DO:
  PUT STREAM s-audit-rpt UNFORMATTED "Therm: " AT 3 ttbl_dept1.therm FORMAT ">9" " (therm)".
  IF action = "UPDATE" THEN
  DO:
    IF ttbl_dept1.therm NE ttbl_dept2.therm THEN
    PUT STREAM s-audit-rpt UNFORMATTED "*" AT 1.
    PUT STREAM s-audit-rpt UNFORMATTED "Therm: " AT 3 ttbl_dept2.therm FORMAT ">9".
  END.
END.
IF v_show_index THEN
DO:
  PUT STREAM s-audit-rpt UNFORMATTED "Index Fields - Label: Value (database field-name)" AT 5.
  PUT STREAM s-audit-rpt UNFORMATTED "Code: " AT 7.
  IF action = "UPDATE" THEN
  PUT STREAM s-audit-rpt UNFORMATTED ttbl_dept2.code " (code)".
  ELSE
  PUT STREAM s-audit-rpt UNFORMATTED ttbl_dept1.code " (code)".
END.
PUT STREAM s-audit-rpt UNFORMATTED SKIP(1).
