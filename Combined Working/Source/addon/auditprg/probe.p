/* probe.p - generated 01/10/2001 - 13:00:08 by nosweat */

/* this program was generated by auditgn1.p and should not be modified */
/* modifications should be made to the auto generation program         */

DEFINE INPUT PARAMETER action AS CHARACTER NO-UNDO.

DEFINE VARIABLE i AS INTEGER NO-UNDO.

{methods/defines/audit.i}
FIND FIRST ttbl_header.

DEFINE WORK-TABLE ttbl_probe1 NO-UNDO LIKE probe.
DEFINE WORK-TABLE ttbl_probe2 NO-UNDO LIKE probe.

CREATE ttbl_probe1.
IMPORT STREAM s_audit_file ttbl_probe1.
CREATE ttbl_probe2.
IF action = "UPDATE" THEN
IMPORT STREAM s_audit_file ttbl_probe2.

PUT STREAM s-audit-rpt UNFORMATTED
  "Action    User ID/(User Name)            DB.Table/(Database)  Date       Time" SKIP
  "--------- ------------------------------ -------------------- ---------- --------" SKIP
  ttbl_header.v_action AT 1
  ttbl_header.v_userid AT 11
  ttbl_header.v_file AT 42
  ttbl_header.v_date FORMAT "99/99/9999" AT 63
  STRING(ttbl_header.v_time,"HH:MM:SS") AT 74.
FIND users WHERE users.user_id = v_userid NO-LOCK NO-ERROR.
IF AVAILABLE users THEN
PUT STREAM s-audit-rpt UNFORMATTED "(" AT 10 users.user_name ")".
ELSE
PUT STREAM s-audit-rpt UNFORMATTED "(user record does not exist)" AT 10.
PUT STREAM s-audit-rpt UNFORMATTED
  "(" AT 41 ttbl_header.v_db ")" SKIP(1)
  "Label: Value (database field-name)" AT 3.
IF action = "UPDATE" THEN
PUT STREAM s-audit-rpt UNFORMATTED " ( * - denotes field value change )".
PUT STREAM s-audit-rpt UNFORMATTED FILL("-",75) AT 3.
IF NOT v_field_changed OR ttbl_probe1.do-quote NE ttbl_probe2.do-quote OR action NE "UPDATE" THEN
DO:
  PUT STREAM s-audit-rpt UNFORMATTED "Y/N: " AT 3 ttbl_probe1.do-quote FORMAT "Y/N" " (do-quote)".
  IF action = "UPDATE" THEN
  DO:
    IF ttbl_probe1.do-quote NE ttbl_probe2.do-quote THEN
    PUT STREAM s-audit-rpt UNFORMATTED "*" AT 1.
    PUT STREAM s-audit-rpt UNFORMATTED "Y/N: " AT 3 ttbl_probe2.do-quote FORMAT "Y/N".
  END.
END.
IF NOT v_field_changed OR ttbl_probe1.dscr NE ttbl_probe2.dscr OR action NE "UPDATE" THEN
DO:
  PUT STREAM s-audit-rpt UNFORMATTED "Description: " AT 3 ttbl_probe1.dscr " (dscr)".
  IF action = "UPDATE" THEN
  DO:
    IF ttbl_probe1.dscr NE ttbl_probe2.dscr THEN
    PUT STREAM s-audit-rpt UNFORMATTED "*" AT 1.
    PUT STREAM s-audit-rpt UNFORMATTED "Description: " AT 3 ttbl_probe2.dscr.
  END.
END.
IF NOT v_field_changed OR ttbl_probe1.e-num NE ttbl_probe2.e-num OR action NE "UPDATE" THEN
DO:
  PUT STREAM s-audit-rpt UNFORMATTED "Est.#: " AT 3 ttbl_probe1.e-num FORMAT ">>>>>9" " (e-num)".
  IF action = "UPDATE" THEN
  DO:
    IF ttbl_probe1.e-num NE ttbl_probe2.e-num THEN
    PUT STREAM s-audit-rpt UNFORMATTED "*" AT 1.
    PUT STREAM s-audit-rpt UNFORMATTED "Est.#: " AT 3 ttbl_probe2.e-num FORMAT ">>>>>9".
  END.
END.
IF NOT v_field_changed OR ttbl_probe1.est-qty NE ttbl_probe2.est-qty OR action NE "UPDATE" THEN
DO:
  PUT STREAM s-audit-rpt UNFORMATTED "Qty: " AT 3 ttbl_probe1.est-qty FORMAT ">>>>>>>9" " (est-qty)".
  IF action = "UPDATE" THEN
  DO:
    IF ttbl_probe1.est-qty NE ttbl_probe2.est-qty THEN
    PUT STREAM s-audit-rpt UNFORMATTED "*" AT 1.
    PUT STREAM s-audit-rpt UNFORMATTED "Qty: " AT 3 ttbl_probe2.est-qty FORMAT ">>>>>>>9".
  END.
END.
IF NOT v_field_changed OR ttbl_probe1.fact-cost NE ttbl_probe2.fact-cost OR action NE "UPDATE" THEN
DO:
  PUT STREAM s-audit-rpt UNFORMATTED "Factory cost: " AT 3 ttbl_probe1.fact-cost FORMAT ">,>>9.99" " (fact-cost)".
  IF action = "UPDATE" THEN
  DO:
    IF ttbl_probe1.fact-cost NE ttbl_probe2.fact-cost THEN
    PUT STREAM s-audit-rpt UNFORMATTED "*" AT 1.
    PUT STREAM s-audit-rpt UNFORMATTED "Factory cost: " AT 3 ttbl_probe2.fact-cost FORMAT ">,>>9.99".
  END.
END.
IF NOT v_field_changed OR ttbl_probe1.FREIGHT NE ttbl_probe2.FREIGHT OR action NE "UPDATE" THEN
DO:
  PUT STREAM s-audit-rpt UNFORMATTED "Freight: " AT 3 ttbl_probe1.FREIGHT FORMAT "->>,>>9.99" " (FREIGHT)".
  IF action = "UPDATE" THEN
  DO:
    IF ttbl_probe1.FREIGHT NE ttbl_probe2.FREIGHT THEN
    PUT STREAM s-audit-rpt UNFORMATTED "*" AT 1.
    PUT STREAM s-audit-rpt UNFORMATTED "Freight: " AT 3 ttbl_probe2.FREIGHT FORMAT "->>,>>9.99".
  END.
END.
IF NOT v_field_changed OR ttbl_probe1.full-cost NE ttbl_probe2.full-cost OR action NE "UPDATE" THEN
DO:
  PUT STREAM s-audit-rpt UNFORMATTED "Full Cost: " AT 3 ttbl_probe1.full-cost FORMAT ">,>>9.99" " (full-cost)".
  IF action = "UPDATE" THEN
  DO:
    IF ttbl_probe1.full-cost NE ttbl_probe2.full-cost THEN
    PUT STREAM s-audit-rpt UNFORMATTED "*" AT 1.
    PUT STREAM s-audit-rpt UNFORMATTED "Full Cost: " AT 3 ttbl_probe2.full-cost FORMAT ">,>>9.99".
  END.
END.
IF NOT v_field_changed OR ttbl_probe1.gross-profit NE ttbl_probe2.gross-profit OR action NE "UPDATE" THEN
DO:
  PUT STREAM s-audit-rpt UNFORMATTED "Gross profit: " AT 3 ttbl_probe1.gross-profit FORMAT ">>9.99" " (gross-profit)".
  IF action = "UPDATE" THEN
  DO:
    IF ttbl_probe1.gross-profit NE ttbl_probe2.gross-profit THEN
    PUT STREAM s-audit-rpt UNFORMATTED "*" AT 1.
    PUT STREAM s-audit-rpt UNFORMATTED "Gross profit: " AT 3 ttbl_probe2.gross-profit FORMAT ">>9.99".
  END.
END.
IF NOT v_field_changed OR ttbl_probe1.gsh-qty NE ttbl_probe2.gsh-qty OR action NE "UPDATE" THEN
DO:
  PUT STREAM s-audit-rpt UNFORMATTED "Sheet Qty: " AT 3 ttbl_probe1.gsh-qty FORMAT ">>>,>>9" " (gsh-qty)".
  IF action = "UPDATE" THEN
  DO:
    IF ttbl_probe1.gsh-qty NE ttbl_probe2.gsh-qty THEN
    PUT STREAM s-audit-rpt UNFORMATTED "*" AT 1.
    PUT STREAM s-audit-rpt UNFORMATTED "Sheet Qty: " AT 3 ttbl_probe2.gsh-qty FORMAT ">>>,>>9".
  END.
END.
IF NOT v_field_changed OR ttbl_probe1.id NE ttbl_probe2.id OR action NE "UPDATE" THEN
DO:
  PUT STREAM s-audit-rpt UNFORMATTED "?: " AT 3 ttbl_probe1.id " (id)".
  IF action = "UPDATE" THEN
  DO:
    IF ttbl_probe1.id NE ttbl_probe2.id THEN
    PUT STREAM s-audit-rpt UNFORMATTED "*" AT 1.
    PUT STREAM s-audit-rpt UNFORMATTED "?: " AT 3 ttbl_probe2.id.
  END.
END.
IF NOT v_field_changed OR ttbl_probe1.line NE ttbl_probe2.line OR action NE "UPDATE" THEN
DO:
  PUT STREAM s-audit-rpt UNFORMATTED "Line #: " AT 3 ttbl_probe1.line FORMAT ">9" " (line)".
  IF action = "UPDATE" THEN
  DO:
    IF ttbl_probe1.line NE ttbl_probe2.line THEN
    PUT STREAM s-audit-rpt UNFORMATTED "*" AT 1.
    PUT STREAM s-audit-rpt UNFORMATTED "Line #: " AT 3 ttbl_probe2.line FORMAT ">9".
  END.
END.
IF NOT v_field_changed OR ttbl_probe1.mku_com NE ttbl_probe2.mku_com OR action NE "UPDATE" THEN
DO:
  PUT STREAM s-audit-rpt UNFORMATTED "?: " AT 3 ttbl_probe1.mku_com FORMAT ">>9.99" " (mku_com)".
  IF action = "UPDATE" THEN
  DO:
    IF ttbl_probe1.mku_com NE ttbl_probe2.mku_com THEN
    PUT STREAM s-audit-rpt UNFORMATTED "*" AT 1.
    PUT STREAM s-audit-rpt UNFORMATTED "?: " AT 3 ttbl_probe2.mku_com FORMAT ">>9.99".
  END.
END.
IF NOT v_field_changed OR ttbl_probe1.mku_gsa-l NE ttbl_probe2.mku_gsa-l OR action NE "UPDATE" THEN
DO:
  PUT STREAM s-audit-rpt UNFORMATTED "?: " AT 3 ttbl_probe1.mku_gsa-l FORMAT ">>9.99" " (mku_gsa-l)".
  IF action = "UPDATE" THEN
  DO:
    IF ttbl_probe1.mku_gsa-l NE ttbl_probe2.mku_gsa-l THEN
    PUT STREAM s-audit-rpt UNFORMATTED "*" AT 1.
    PUT STREAM s-audit-rpt UNFORMATTED "?: " AT 3 ttbl_probe2.mku_gsa-l FORMAT ">>9.99".
  END.
END.
IF NOT v_field_changed OR ttbl_probe1.mku_gsa-m NE ttbl_probe2.mku_gsa-m OR action NE "UPDATE" THEN
DO:
  PUT STREAM s-audit-rpt UNFORMATTED "?: " AT 3 ttbl_probe1.mku_gsa-m FORMAT ">>9.99" " (mku_gsa-m)".
  IF action = "UPDATE" THEN
  DO:
    IF ttbl_probe1.mku_gsa-m NE ttbl_probe2.mku_gsa-m THEN
    PUT STREAM s-audit-rpt UNFORMATTED "*" AT 1.
    PUT STREAM s-audit-rpt UNFORMATTED "?: " AT 3 ttbl_probe2.mku_gsa-m FORMAT ">>9.99".
  END.
END.
IF NOT v_field_changed OR ttbl_probe1.mku_whs NE ttbl_probe2.mku_whs OR action NE "UPDATE" THEN
DO:
  PUT STREAM s-audit-rpt UNFORMATTED "?: " AT 3 ttbl_probe1.mku_whs FORMAT ">>9.99" " (mku_whs)".
  IF action = "UPDATE" THEN
  DO:
    IF ttbl_probe1.mku_whs NE ttbl_probe2.mku_whs THEN
    PUT STREAM s-audit-rpt UNFORMATTED "*" AT 1.
    PUT STREAM s-audit-rpt UNFORMATTED "?: " AT 3 ttbl_probe2.mku_whs FORMAT ">>9.99".
  END.
END.
IF NOT v_field_changed OR ttbl_probe1.net-profit NE ttbl_probe2.net-profit OR action NE "UPDATE" THEN
DO:
  PUT STREAM s-audit-rpt UNFORMATTED "Net Profit: " AT 3 ttbl_probe1.net-profit FORMAT ">>9.99" " (net-profit)".
  IF action = "UPDATE" THEN
  DO:
    IF ttbl_probe1.net-profit NE ttbl_probe2.net-profit THEN
    PUT STREAM s-audit-rpt UNFORMATTED "*" AT 1.
    PUT STREAM s-audit-rpt UNFORMATTED "Net Profit: " AT 3 ttbl_probe2.net-profit FORMAT ">>9.99".
  END.
END.
IF NOT v_field_changed OR ttbl_probe1.prof-on NE ttbl_probe2.prof-on OR action NE "UPDATE" THEN
DO:
  PUT STREAM s-audit-rpt UNFORMATTED "Profit on: " AT 3 ttbl_probe1.prof-on " (prof-on)".
  IF action = "UPDATE" THEN
  DO:
    IF ttbl_probe1.prof-on NE ttbl_probe2.prof-on THEN
    PUT STREAM s-audit-rpt UNFORMATTED "*" AT 1.
    PUT STREAM s-audit-rpt UNFORMATTED "Profit on: " AT 3 ttbl_probe2.prof-on.
  END.
END.
IF NOT v_field_changed OR ttbl_probe1.sell-price NE ttbl_probe2.sell-price OR action NE "UPDATE" THEN
DO:
  PUT STREAM s-audit-rpt UNFORMATTED "Selling Price: " AT 3 ttbl_probe1.sell-price FORMAT ">,>>9.99" " (sell-price)".
  IF action = "UPDATE" THEN
  DO:
    IF ttbl_probe1.sell-price NE ttbl_probe2.sell-price THEN
    PUT STREAM s-audit-rpt UNFORMATTED "*" AT 1.
    PUT STREAM s-audit-rpt UNFORMATTED "Selling Price: " AT 3 ttbl_probe2.sell-price FORMAT ">,>>9.99".
  END.
END.
IF NOT v_field_changed OR ttbl_probe1.tot-lbs NE ttbl_probe2.tot-lbs OR action NE "UPDATE" THEN
DO:
  PUT STREAM s-audit-rpt UNFORMATTED "Total Pounds: " AT 3 ttbl_probe1.tot-lbs FORMAT ">>>,>>9" " (tot-lbs)".
  IF action = "UPDATE" THEN
  DO:
    IF ttbl_probe1.tot-lbs NE ttbl_probe2.tot-lbs THEN
    PUT STREAM s-audit-rpt UNFORMATTED "*" AT 1.
    PUT STREAM s-audit-rpt UNFORMATTED "Total Pounds: " AT 3 ttbl_probe2.tot-lbs FORMAT ">>>,>>9".
  END.
END.
IF v_show_index THEN
DO:
  PUT STREAM s-audit-rpt UNFORMATTED "Index Fields - Label: Value (database field-name)" AT 5.
  PUT STREAM s-audit-rpt UNFORMATTED "Est.#: " AT 7.
  IF action = "UPDATE" THEN
  PUT STREAM s-audit-rpt UNFORMATTED ttbl_probe2.e-num FORMAT ">>>>>9" " (e-num)".
  ELSE
  PUT STREAM s-audit-rpt UNFORMATTED ttbl_probe1.e-num FORMAT ">>>>>9" " (e-num)".
  PUT STREAM s-audit-rpt UNFORMATTED "Line #: " AT 7.
  IF action = "UPDATE" THEN
  PUT STREAM s-audit-rpt UNFORMATTED ttbl_probe2.line FORMAT ">9" " (line)".
  ELSE
  PUT STREAM s-audit-rpt UNFORMATTED ttbl_probe1.line FORMAT ">9" " (line)".
END.
PUT STREAM s-audit-rpt UNFORMATTED SKIP(1).
