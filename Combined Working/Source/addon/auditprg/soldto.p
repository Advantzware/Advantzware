/* soldto.p - generated 01/10/2001 - 13:00:09 by nosweat */

/* this program was generated by auditgn1.p and should not be modified */
/* modifications should be made to the auto generation program         */

DEFINE INPUT PARAMETER action AS CHARACTER NO-UNDO.

DEFINE VARIABLE i AS INTEGER NO-UNDO.

{methods/defines/audit.i}
FIND FIRST ttbl_header.

DEFINE WORK-TABLE ttbl_soldto1 NO-UNDO LIKE soldto.
DEFINE WORK-TABLE ttbl_soldto2 NO-UNDO LIKE soldto.

CREATE ttbl_soldto1.
IMPORT STREAM s_audit_file ttbl_soldto1.
CREATE ttbl_soldto2.
IF action = "UPDATE" THEN
IMPORT STREAM s_audit_file ttbl_soldto2.

PUT STREAM s-audit-rpt UNFORMATTED
  "Action    User ID/(User Name)            DB.Table/(Database)  Date       Time" SKIP
  "--------- ------------------------------ -------------------- ---------- --------" SKIP
  ttbl_header.v_action AT 1
  ttbl_header.v_userid AT 11
  ttbl_header.v_file AT 42
  ttbl_header.v_date FORMAT "99/99/9999" AT 63
  STRING(ttbl_header.v_time,"HH:MM:SS") AT 74.
FIND users WHERE users.user_id = v_userid NO-LOCK NO-ERROR.
IF AVAILABLE users THEN
PUT STREAM s-audit-rpt UNFORMATTED "(" AT 10 users.user_name ")".
ELSE
PUT STREAM s-audit-rpt UNFORMATTED "(user record does not exist)" AT 10.
PUT STREAM s-audit-rpt UNFORMATTED
  "(" AT 41 ttbl_header.v_db ")" SKIP(1)
  "Label: Value (database field-name)" AT 3.
IF action = "UPDATE" THEN
PUT STREAM s-audit-rpt UNFORMATTED " ( * - denotes field value change )".
PUT STREAM s-audit-rpt UNFORMATTED FILL("-",75) AT 3.
IF NOT v_field_changed OR ttbl_soldto1.company NE ttbl_soldto2.company OR action NE "UPDATE" THEN
DO:
  PUT STREAM s-audit-rpt UNFORMATTED "Company: " AT 3 ttbl_soldto1.company " (company)".
  IF action = "UPDATE" THEN
  DO:
    IF ttbl_soldto1.company NE ttbl_soldto2.company THEN
    PUT STREAM s-audit-rpt UNFORMATTED "*" AT 1.
    PUT STREAM s-audit-rpt UNFORMATTED "Company: " AT 3 ttbl_soldto2.company.
  END.
END.
IF NOT v_field_changed OR ttbl_soldto1.country NE ttbl_soldto2.country OR action NE "UPDATE" THEN
DO:
  PUT STREAM s-audit-rpt UNFORMATTED "Country: " AT 3 ttbl_soldto1.country " (country)".
  IF action = "UPDATE" THEN
  DO:
    IF ttbl_soldto1.country NE ttbl_soldto2.country THEN
    PUT STREAM s-audit-rpt UNFORMATTED "*" AT 1.
    PUT STREAM s-audit-rpt UNFORMATTED "Country: " AT 3 ttbl_soldto2.country.
  END.
END.
IF NOT v_field_changed OR ttbl_soldto1.cust-no NE ttbl_soldto2.cust-no OR action NE "UPDATE" THEN
DO:
  PUT STREAM s-audit-rpt UNFORMATTED "Customer Number: " AT 3 ttbl_soldto1.cust-no " (cust-no)".
  IF action = "UPDATE" THEN
  DO:
    IF ttbl_soldto1.cust-no NE ttbl_soldto2.cust-no THEN
    PUT STREAM s-audit-rpt UNFORMATTED "*" AT 1.
    PUT STREAM s-audit-rpt UNFORMATTED "Customer Number: " AT 3 ttbl_soldto2.cust-no.
  END.
END.
IF NOT v_field_changed OR ttbl_soldto1.Postal NE ttbl_soldto2.Postal OR action NE "UPDATE" THEN
DO:
  PUT STREAM s-audit-rpt UNFORMATTED "Postal Code: " AT 3 ttbl_soldto1.Postal " (Postal)".
  IF action = "UPDATE" THEN
  DO:
    IF ttbl_soldto1.Postal NE ttbl_soldto2.Postal THEN
    PUT STREAM s-audit-rpt UNFORMATTED "*" AT 1.
    PUT STREAM s-audit-rpt UNFORMATTED "Postal Code: " AT 3 ttbl_soldto2.Postal.
  END.
END.
DO i = 1 TO 2:
IF NOT v_field_changed OR ttbl_soldto1.sold-addr[i] NE ttbl_soldto2.sold-addr[i] OR action NE "UPDATE" THEN
DO:
  PUT STREAM s-audit-rpt UNFORMATTED "[" AT 3 i "]: " ttbl_soldto1.sold-addr[i] " (sold-addr[" i "])".
  IF action = "UPDATE" THEN
  DO:
    IF ttbl_soldto1.sold-addr[i] NE ttbl_soldto2.sold-addr[i] THEN
    PUT STREAM s-audit-rpt UNFORMATTED "*" AT 1.
    PUT STREAM s-audit-rpt UNFORMATTED "[" AT 3 i "]: " ttbl_soldto2.sold-addr[i].
  END.
END.
END.
IF NOT v_field_changed OR ttbl_soldto1.sold-city NE ttbl_soldto2.sold-city OR action NE "UPDATE" THEN
DO:
  PUT STREAM s-audit-rpt UNFORMATTED "City: " AT 3 ttbl_soldto1.sold-city " (sold-city)".
  IF action = "UPDATE" THEN
  DO:
    IF ttbl_soldto1.sold-city NE ttbl_soldto2.sold-city THEN
    PUT STREAM s-audit-rpt UNFORMATTED "*" AT 1.
    PUT STREAM s-audit-rpt UNFORMATTED "City: " AT 3 ttbl_soldto2.sold-city.
  END.
END.
IF NOT v_field_changed OR ttbl_soldto1.sold-id NE ttbl_soldto2.sold-id OR action NE "UPDATE" THEN
DO:
  PUT STREAM s-audit-rpt UNFORMATTED "Sold To ID: " AT 3 ttbl_soldto1.sold-id " (sold-id)".
  IF action = "UPDATE" THEN
  DO:
    IF ttbl_soldto1.sold-id NE ttbl_soldto2.sold-id THEN
    PUT STREAM s-audit-rpt UNFORMATTED "*" AT 1.
    PUT STREAM s-audit-rpt UNFORMATTED "Sold To ID: " AT 3 ttbl_soldto2.sold-id.
  END.
END.
IF NOT v_field_changed OR ttbl_soldto1.sold-name NE ttbl_soldto2.sold-name OR action NE "UPDATE" THEN
DO:
  PUT STREAM s-audit-rpt UNFORMATTED "Name: " AT 3 ttbl_soldto1.sold-name " (sold-name)".
  IF action = "UPDATE" THEN
  DO:
    IF ttbl_soldto1.sold-name NE ttbl_soldto2.sold-name THEN
    PUT STREAM s-audit-rpt UNFORMATTED "*" AT 1.
    PUT STREAM s-audit-rpt UNFORMATTED "Name: " AT 3 ttbl_soldto2.sold-name.
  END.
END.
IF NOT v_field_changed OR ttbl_soldto1.sold-no NE ttbl_soldto2.sold-no OR action NE "UPDATE" THEN
DO:
  PUT STREAM s-audit-rpt UNFORMATTED "Sold To #: " AT 3 ttbl_soldto1.sold-no FORMAT ">>9" " (sold-no)".
  IF action = "UPDATE" THEN
  DO:
    IF ttbl_soldto1.sold-no NE ttbl_soldto2.sold-no THEN
    PUT STREAM s-audit-rpt UNFORMATTED "*" AT 1.
    PUT STREAM s-audit-rpt UNFORMATTED "Sold To #: " AT 3 ttbl_soldto2.sold-no FORMAT ">>9".
  END.
END.
IF NOT v_field_changed OR ttbl_soldto1.sold-state NE ttbl_soldto2.sold-state OR action NE "UPDATE" THEN
DO:
  PUT STREAM s-audit-rpt UNFORMATTED "State: " AT 3 ttbl_soldto1.sold-state " (sold-state)".
  IF action = "UPDATE" THEN
  DO:
    IF ttbl_soldto1.sold-state NE ttbl_soldto2.sold-state THEN
    PUT STREAM s-audit-rpt UNFORMATTED "*" AT 1.
    PUT STREAM s-audit-rpt UNFORMATTED "State: " AT 3 ttbl_soldto2.sold-state.
  END.
END.
IF NOT v_field_changed OR ttbl_soldto1.sold-zip NE ttbl_soldto2.sold-zip OR action NE "UPDATE" THEN
DO:
  PUT STREAM s-audit-rpt UNFORMATTED "Zip: " AT 3 ttbl_soldto1.sold-zip " (sold-zip)".
  IF action = "UPDATE" THEN
  DO:
    IF ttbl_soldto1.sold-zip NE ttbl_soldto2.sold-zip THEN
    PUT STREAM s-audit-rpt UNFORMATTED "*" AT 1.
    PUT STREAM s-audit-rpt UNFORMATTED "Zip: " AT 3 ttbl_soldto2.sold-zip.
  END.
END.
IF v_show_index THEN
DO:
  PUT STREAM s-audit-rpt UNFORMATTED "Index Fields - Label: Value (database field-name)" AT 5.
  PUT STREAM s-audit-rpt UNFORMATTED "Company: " AT 7.
  IF action = "UPDATE" THEN
  PUT STREAM s-audit-rpt UNFORMATTED ttbl_soldto2.company " (company)".
  ELSE
  PUT STREAM s-audit-rpt UNFORMATTED ttbl_soldto1.company " (company)".
  PUT STREAM s-audit-rpt UNFORMATTED "Customer Number: " AT 7.
  IF action = "UPDATE" THEN
  PUT STREAM s-audit-rpt UNFORMATTED ttbl_soldto2.cust-no " (cust-no)".
  ELSE
  PUT STREAM s-audit-rpt UNFORMATTED ttbl_soldto1.cust-no " (cust-no)".
  PUT STREAM s-audit-rpt UNFORMATTED "Sold To #: " AT 7.
  IF action = "UPDATE" THEN
  PUT STREAM s-audit-rpt UNFORMATTED ttbl_soldto2.sold-no FORMAT ">>9" " (sold-no)".
  ELSE
  PUT STREAM s-audit-rpt UNFORMATTED ttbl_soldto1.sold-no FORMAT ">>9" " (sold-no)".
END.
PUT STREAM s-audit-rpt UNFORMATTED SKIP(1).
