/* routing-mtx.p - generated 01/10/2001 - 13:00:09 by nosweat */

/* this program was generated by auditgn1.p and should not be modified */
/* modifications should be made to the auto generation program         */

DEFINE INPUT PARAMETER action AS CHARACTER NO-UNDO.

DEFINE VARIABLE i AS INTEGER NO-UNDO.

{methods/defines/audit.i}
FIND FIRST ttbl_header.

DEFINE WORK-TABLE ttbl_routing-mtx1 NO-UNDO LIKE routing-mtx.
DEFINE WORK-TABLE ttbl_routing-mtx2 NO-UNDO LIKE routing-mtx.

CREATE ttbl_routing-mtx1.
IMPORT STREAM s_audit_file ttbl_routing-mtx1.
CREATE ttbl_routing-mtx2.
IF action = "UPDATE" THEN
IMPORT STREAM s_audit_file ttbl_routing-mtx2.

PUT STREAM s-audit-rpt UNFORMATTED
  "Action    User ID/(User Name)            DB.Table/(Database)  Date       Time" SKIP
  "--------- ------------------------------ -------------------- ---------- --------" SKIP
  ttbl_header.v_action AT 1
  ttbl_header.v_userid AT 11
  ttbl_header.v_file AT 42
  ttbl_header.v_date FORMAT "99/99/9999" AT 63
  STRING(ttbl_header.v_time,"HH:MM:SS") AT 74.
FIND users WHERE users.user_id = v_userid NO-LOCK NO-ERROR.
IF AVAILABLE users THEN
PUT STREAM s-audit-rpt UNFORMATTED "(" AT 10 users.user_name ")".
ELSE
PUT STREAM s-audit-rpt UNFORMATTED "(user record does not exist)" AT 10.
PUT STREAM s-audit-rpt UNFORMATTED
  "(" AT 41 ttbl_header.v_db ")" SKIP(1)
  "Label: Value (database field-name)" AT 3.
IF action = "UPDATE" THEN
PUT STREAM s-audit-rpt UNFORMATTED " ( * - denotes field value change )".
PUT STREAM s-audit-rpt UNFORMATTED FILL("-",75) AT 3.
DO i = 1 TO 10:
IF NOT v_field_changed OR ttbl_routing-mtx1.bl-len[i] NE ttbl_routing-mtx2.bl-len[i] OR action NE "UPDATE" THEN
DO:
  PUT STREAM s-audit-rpt UNFORMATTED "Blank Length[" AT 3 i "]: " ttbl_routing-mtx1.bl-len[i] FORMAT ">>9.99" " (bl-len[" i "])".
  IF action = "UPDATE" THEN
  DO:
    IF ttbl_routing-mtx1.bl-len[i] NE ttbl_routing-mtx2.bl-len[i] THEN
    PUT STREAM s-audit-rpt UNFORMATTED "*" AT 1.
    PUT STREAM s-audit-rpt UNFORMATTED "Blank Length[" AT 3 i "]: " ttbl_routing-mtx2.bl-len[i] FORMAT ">>9.99".
  END.
END.
END.
DO i = 1 TO 10:
IF NOT v_field_changed OR ttbl_routing-mtx1.bl-wid[i] NE ttbl_routing-mtx2.bl-wid[i] OR action NE "UPDATE" THEN
DO:
  PUT STREAM s-audit-rpt UNFORMATTED "Blank Width[" AT 3 i "]: " ttbl_routing-mtx1.bl-wid[i] FORMAT ">>9.99" " (bl-wid[" i "])".
  IF action = "UPDATE" THEN
  DO:
    IF ttbl_routing-mtx1.bl-wid[i] NE ttbl_routing-mtx2.bl-wid[i] THEN
    PUT STREAM s-audit-rpt UNFORMATTED "*" AT 1.
    PUT STREAM s-audit-rpt UNFORMATTED "Blank Width[" AT 3 i "]: " ttbl_routing-mtx2.bl-wid[i] FORMAT ">>9.99".
  END.
END.
END.
IF NOT v_field_changed OR ttbl_routing-mtx1.company NE ttbl_routing-mtx2.company OR action NE "UPDATE" THEN
DO:
  PUT STREAM s-audit-rpt UNFORMATTED "Company: " AT 3 ttbl_routing-mtx1.company " (company)".
  IF action = "UPDATE" THEN
  DO:
    IF ttbl_routing-mtx1.company NE ttbl_routing-mtx2.company THEN
    PUT STREAM s-audit-rpt UNFORMATTED "*" AT 1.
    PUT STREAM s-audit-rpt UNFORMATTED "Company: " AT 3 ttbl_routing-mtx2.company.
  END.
END.
IF NOT v_field_changed OR ttbl_routing-mtx1.dim-type NE ttbl_routing-mtx2.dim-type OR action NE "UPDATE" THEN
DO:
  PUT STREAM s-audit-rpt UNFORMATTED "?: " AT 3 ttbl_routing-mtx1.dim-type " (dim-type)".
  IF action = "UPDATE" THEN
  DO:
    IF ttbl_routing-mtx1.dim-type NE ttbl_routing-mtx2.dim-type THEN
    PUT STREAM s-audit-rpt UNFORMATTED "*" AT 1.
    PUT STREAM s-audit-rpt UNFORMATTED "?: " AT 3 ttbl_routing-mtx2.dim-type.
  END.
END.
IF NOT v_field_changed OR ttbl_routing-mtx1.loc NE ttbl_routing-mtx2.loc OR action NE "UPDATE" THEN
DO:
  PUT STREAM s-audit-rpt UNFORMATTED "Location: " AT 3 ttbl_routing-mtx1.loc " (loc)".
  IF action = "UPDATE" THEN
  DO:
    IF ttbl_routing-mtx1.loc NE ttbl_routing-mtx2.loc THEN
    PUT STREAM s-audit-rpt UNFORMATTED "*" AT 1.
    PUT STREAM s-audit-rpt UNFORMATTED "Location: " AT 3 ttbl_routing-mtx2.loc.
  END.
END.
IF NOT v_field_changed OR ttbl_routing-mtx1.msf NE ttbl_routing-mtx2.msf OR action NE "UPDATE" THEN
DO:
  PUT STREAM s-audit-rpt UNFORMATTED "Up to MSF: " AT 3 ttbl_routing-mtx1.msf FORMAT ">,>>9" " (msf)".
  IF action = "UPDATE" THEN
  DO:
    IF ttbl_routing-mtx1.msf NE ttbl_routing-mtx2.msf THEN
    PUT STREAM s-audit-rpt UNFORMATTED "*" AT 1.
    PUT STREAM s-audit-rpt UNFORMATTED "Up to MSF: " AT 3 ttbl_routing-mtx2.msf FORMAT ">,>>9".
  END.
END.
DO i = 1 TO 100:
IF NOT v_field_changed OR ttbl_routing-mtx1.r-code[i] NE ttbl_routing-mtx2.r-code[i] OR action NE "UPDATE" THEN
DO:
  PUT STREAM s-audit-rpt UNFORMATTED "Routing Code[" AT 3 i "]: " ttbl_routing-mtx1.r-code[i] " (r-code[" i "])".
  IF action = "UPDATE" THEN
  DO:
    IF ttbl_routing-mtx1.r-code[i] NE ttbl_routing-mtx2.r-code[i] THEN
    PUT STREAM s-audit-rpt UNFORMATTED "*" AT 1.
    PUT STREAM s-audit-rpt UNFORMATTED "Routing Code[" AT 3 i "]: " ttbl_routing-mtx2.r-code[i].
  END.
END.
END.
IF NOT v_field_changed OR ttbl_routing-mtx1.style NE ttbl_routing-mtx2.style OR action NE "UPDATE" THEN
DO:
  PUT STREAM s-audit-rpt UNFORMATTED "Style: " AT 3 ttbl_routing-mtx1.style " (style)".
  IF action = "UPDATE" THEN
  DO:
    IF ttbl_routing-mtx1.style NE ttbl_routing-mtx2.style THEN
    PUT STREAM s-audit-rpt UNFORMATTED "*" AT 1.
    PUT STREAM s-audit-rpt UNFORMATTED "Style: " AT 3 ttbl_routing-mtx2.style.
  END.
END.
IF v_show_index THEN
DO:
  PUT STREAM s-audit-rpt UNFORMATTED "Index Fields - Label: Value (database field-name)" AT 5.
  PUT STREAM s-audit-rpt UNFORMATTED "Company: " AT 7.
  IF action = "UPDATE" THEN
  PUT STREAM s-audit-rpt UNFORMATTED ttbl_routing-mtx2.company " (company)".
  ELSE
  PUT STREAM s-audit-rpt UNFORMATTED ttbl_routing-mtx1.company " (company)".
  PUT STREAM s-audit-rpt UNFORMATTED "Location: " AT 7.
  IF action = "UPDATE" THEN
  PUT STREAM s-audit-rpt UNFORMATTED ttbl_routing-mtx2.loc " (loc)".
  ELSE
  PUT STREAM s-audit-rpt UNFORMATTED ttbl_routing-mtx1.loc " (loc)".
  PUT STREAM s-audit-rpt UNFORMATTED "Style: " AT 7.
  IF action = "UPDATE" THEN
  PUT STREAM s-audit-rpt UNFORMATTED ttbl_routing-mtx2.style " (style)".
  ELSE
  PUT STREAM s-audit-rpt UNFORMATTED ttbl_routing-mtx1.style " (style)".
  PUT STREAM s-audit-rpt UNFORMATTED "Up to MSF: " AT 7.
  IF action = "UPDATE" THEN
  PUT STREAM s-audit-rpt UNFORMATTED ttbl_routing-mtx2.msf FORMAT ">,>>9" " (msf)".
  ELSE
  PUT STREAM s-audit-rpt UNFORMATTED ttbl_routing-mtx1.msf FORMAT ">,>>9" " (msf)".
END.
PUT STREAM s-audit-rpt UNFORMATTED SKIP(1).
