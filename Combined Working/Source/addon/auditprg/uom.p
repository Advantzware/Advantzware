/* uom.p - generated 01/10/2001 - 13:00:10 by nosweat */

/* this program was generated by auditgn1.p and should not be modified */
/* modifications should be made to the auto generation program         */

DEFINE INPUT PARAMETER action AS CHARACTER NO-UNDO.

DEFINE VARIABLE i AS INTEGER NO-UNDO.

{methods/defines/audit.i}
FIND FIRST ttbl_header.

DEFINE WORK-TABLE ttbl_uom1 NO-UNDO LIKE uom.
DEFINE WORK-TABLE ttbl_uom2 NO-UNDO LIKE uom.

CREATE ttbl_uom1.
IMPORT STREAM s_audit_file ttbl_uom1.
CREATE ttbl_uom2.
IF action = "UPDATE" THEN
IMPORT STREAM s_audit_file ttbl_uom2.

PUT STREAM s-audit-rpt UNFORMATTED
  "Action    User ID/(User Name)            DB.Table/(Database)  Date       Time" SKIP
  "--------- ------------------------------ -------------------- ---------- --------" SKIP
  ttbl_header.v_action AT 1
  ttbl_header.v_userid AT 11
  ttbl_header.v_file AT 42
  ttbl_header.v_date FORMAT "99/99/9999" AT 63
  STRING(ttbl_header.v_time,"HH:MM:SS") AT 74.
FIND users WHERE users.user_id = v_userid NO-LOCK NO-ERROR.
IF AVAILABLE users THEN
PUT STREAM s-audit-rpt UNFORMATTED "(" AT 10 users.user_name ")".
ELSE
PUT STREAM s-audit-rpt UNFORMATTED "(user record does not exist)" AT 10.
PUT STREAM s-audit-rpt UNFORMATTED
  "(" AT 41 ttbl_header.v_db ")" SKIP(1)
  "Label: Value (database field-name)" AT 3.
IF action = "UPDATE" THEN
PUT STREAM s-audit-rpt UNFORMATTED " ( * - denotes field value change )".
PUT STREAM s-audit-rpt UNFORMATTED FILL("-",75) AT 3.
IF NOT v_field_changed OR ttbl_uom1.dscr NE ttbl_uom2.dscr OR action NE "UPDATE" THEN
DO:
  PUT STREAM s-audit-rpt UNFORMATTED "Description: " AT 3 ttbl_uom1.dscr " (dscr)".
  IF action = "UPDATE" THEN
  DO:
    IF ttbl_uom1.dscr NE ttbl_uom2.dscr THEN
    PUT STREAM s-audit-rpt UNFORMATTED "*" AT 1.
    PUT STREAM s-audit-rpt UNFORMATTED "Description: " AT 3 ttbl_uom2.dscr.
  END.
END.
IF NOT v_field_changed OR ttbl_uom1.mult NE ttbl_uom2.mult OR action NE "UPDATE" THEN
DO:
  PUT STREAM s-audit-rpt UNFORMATTED "Multiplier: " AT 3 ttbl_uom1.mult FORMAT ">>>,>>>,>>9.9999" " (mult)".
  IF action = "UPDATE" THEN
  DO:
    IF ttbl_uom1.mult NE ttbl_uom2.mult THEN
    PUT STREAM s-audit-rpt UNFORMATTED "*" AT 1.
    PUT STREAM s-audit-rpt UNFORMATTED "Multiplier: " AT 3 ttbl_uom2.mult FORMAT ">>>,>>>,>>9.9999".
  END.
END.
IF NOT v_field_changed OR ttbl_uom1.Other NE ttbl_uom2.Other OR action NE "UPDATE" THEN
DO:
  PUT STREAM s-audit-rpt UNFORMATTED "Other UOM: " AT 3 ttbl_uom1.Other " (Other)".
  IF action = "UPDATE" THEN
  DO:
    IF ttbl_uom1.Other NE ttbl_uom2.Other THEN
    PUT STREAM s-audit-rpt UNFORMATTED "*" AT 1.
    PUT STREAM s-audit-rpt UNFORMATTED "Other UOM: " AT 3 ttbl_uom2.Other.
  END.
END.
IF NOT v_field_changed OR ttbl_uom1.uom NE ttbl_uom2.uom OR action NE "UPDATE" THEN
DO:
  PUT STREAM s-audit-rpt UNFORMATTED "UOM: " AT 3 ttbl_uom1.uom " (uom)".
  IF action = "UPDATE" THEN
  DO:
    IF ttbl_uom1.uom NE ttbl_uom2.uom THEN
    PUT STREAM s-audit-rpt UNFORMATTED "*" AT 1.
    PUT STREAM s-audit-rpt UNFORMATTED "UOM: " AT 3 ttbl_uom2.uom.
  END.
END.
IF v_show_index THEN
DO:
  PUT STREAM s-audit-rpt UNFORMATTED "Index Fields - Label: Value (database field-name)" AT 5.
  PUT STREAM s-audit-rpt UNFORMATTED "Description: " AT 7.
  IF action = "UPDATE" THEN
  PUT STREAM s-audit-rpt UNFORMATTED ttbl_uom2.dscr " (dscr)".
  ELSE
  PUT STREAM s-audit-rpt UNFORMATTED ttbl_uom1.dscr " (dscr)".
  PUT STREAM s-audit-rpt UNFORMATTED "UOM: " AT 7.
  IF action = "UPDATE" THEN
  PUT STREAM s-audit-rpt UNFORMATTED ttbl_uom2.uom " (uom)".
  ELSE
  PUT STREAM s-audit-rpt UNFORMATTED ttbl_uom1.uom " (uom)".
END.
PUT STREAM s-audit-rpt UNFORMATTED SKIP(1).
