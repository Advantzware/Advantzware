PROCEDURE fixTables:
  RUN fixACCOUNT.
  RUN fixAP-BUY.
  RUN fixAP-CHK.
  RUN fixAP-CTRL.
  RUN fixAP-DIS.
  RUN fixAP-DISL.
  RUN fixAP-INV.
  RUN fixAP-INVL.
  RUN fixAP-INVLR.
  RUN fixAP-LEDGER.
  RUN fixAP-PAY.
  RUN fixAP-PAYL.
  RUN fixAP-SEL.
  RUN fixAPHIST.
  RUN fixAR-CASH.
  RUN fixAR-CASHL.
  RUN fixAR-CTRL.
  RUN fixAR-INV.
  RUN fixAR-INVL.
  RUN fixAR-INVM.
  RUN fixAR-LEDGER.
  RUN fixAR-MCASH.
  RUN fixASINOTES.
  RUN fixBANK.
  RUN fixBOX-DESIGN-HDR.
  RUN fixBOX-DESIGN-LINE.
  RUN fixBUYER.
  RUN fixCARR-MTX.
  RUN fixCARRIER.
  RUN fixCE-CTRL.
  RUN fixCOMPANY.
  RUN fixCOSTTYPE.
  RUN fixCREDIT-HOLD-TYPE.
  RUN fixCREW.
  RUN fixCURRENCY.
  RUN fixCUST.
  RUN fixCUST-MARKUP.
  RUN fixCUST-PART.
  RUN fixCUST-PROD-SALES.
  RUN fixCUSTYPE.
  RUN fixDB-CTRL.
  RUN fixDEPT.
  RUN fixDOWN-TYPE.
  RUN fixE-ITEM.
  RUN fixE-ITEM-CUST.
  RUN fixE-ITEM-VEND.
  RUN fixE-ITEMFG.
  RUN fixE-ITEMFG-VEND.
  RUN fixEB.
  RUN fixEDAPCHECK.
  RUN fixEDCAT.
  RUN fixEDCATLINE.
  RUN fixEDCATPRICE.
  RUN fixEDCO.
  RUN fixEDCODE.
  RUN fixEDDOC.
  RUN fixEDICXREF.
  RUN fixEDIVADDON.
  RUN fixEDIVLINE.
  RUN fixEDIVTRAN.
  RUN fixEDMAST.
  RUN fixEDPD.
  RUN fixEDPOADDON.
  RUN fixEDPOLINE.
  RUN fixEDPOTRAN.
  RUN fixEDSHIPTO.
  RUN fixEDSHIPVIA.
  RUN fixEDSHLINE.
  RUN fixEDSHORD.
  RUN fixEDSHPACK.
  RUN fixEDSHTARE.
  RUN fixEDSHTRAN.
  RUN fixEF.
  RUN fixEF-NSH.
  RUN fixEMAILCOD.
  RUN fixEMAILDTL.
  RUN fixEMP.
  RUN fixEST.
  RUN fixEST-FLM.
  RUN fixEST-INST.
  RUN fixEST-OP.
  RUN fixEST-PREP.
  RUN fixEST-QTY.
  RUN fixEST-SUMM.
  RUN fixEXPIRATION.
  RUN fixFG-ACT.
  RUN fixFG-BIN.
  RUN fixFG-CTRL.
  RUN fixFG-HIST.
  RUN fixFG-RCPTH.
  RUN fixFG-RCPTS.
  RUN fixFG-RCTD.
  RUN fixFG-RDTL.
  RUN fixFG-RDTLH.
  RUN fixFG-SET.
  RUN fixFGCAT.
  RUN fixFILE.
  RUN fixFLUTE.
  RUN fixFRACTION.
  RUN fixGL-CTRL.
  RUN fixGL-FREQ.
  RUN fixGL-JRN.
  RUN fixGL-JRNL.
  RUN fixGL-RPT.
  RUN fixGL-RPTD.
  RUN fixGLHIST.
  RUN fixGLTRANS.
  RUN fixINV-HEAD.
  RUN fixINV-LINE.
  RUN fixINV-MISC.
  RUN fixITEM.
  RUN fixITEM-BOM.
  RUN fixITEM-SPEC.
  RUN fixITEMFG.
  RUN fixITEMFG-BOM.
  RUN fixITEMFG-INK.
  RUN fixITEMFG-LOC.
  RUN fixITEMFGDTL.
  RUN fixJC-CTRL.
  RUN fixJOB.
  RUN fixJOB-ALL.
  RUN fixJOB-BRD.
  RUN fixJOB-CAT.
  RUN fixJOB-CODE.
  RUN fixJOB-HDR.
  RUN fixJOB-MAT.
  RUN fixJOB-MCH.
  RUN fixJOB-PREP.
  RUN fixJOB-SCH.
  RUN fixLOADTAG.
  RUN fixLOC.
  RUN fixMACH.
  RUN fixMACH-ADDER.
  RUN fixMACH-CALENDAR.
  RUN fixMAT.
  RUN fixMAT-ACT.
  RUN fixMATPREP.
  RUN fixMCH-ACT.
  RUN fixMCH-SRT.
  RUN fixMISC-ACT.
  RUN fixMMTX.
  RUN fixMMTX2.
  RUN fixMMTY.
  RUN fixMNU-ITEM.
  RUN fixMONTHLY.
  RUN fixMSTD.
  RUN fixNRF-COLOR.
  RUN fixNRF-SIZE.
  RUN fixOE-BOLH.
  RUN fixOE-BOLL.
  RUN fixOE-BOLL-QTY.
  RUN fixOE-CTRL.
  RUN fixOE-ORD.
  RUN fixOE-ORDL.
  RUN fixOE-ORDM.
  RUN fixOE-PRMTX.
  RUN fixOE-REL.
  RUN fixOE-RELH.
  RUN fixOE-RELL.
  RUN fixOE-RETH.
  RUN fixOE-RETL.
  RUN fixOE-SHIP.
  RUN fixPC-MISC.
  RUN fixPC-PRDD.
  RUN fixPC-PRDH.
  RUN fixPDD.
  RUN fixPDH.
  RUN fixPERIOD.
  RUN fixPERM.
  RUN fixPERMG.
  RUN fixPERMX.
  RUN fixPO-ALL.
  RUN fixPO-CTRL.
  RUN fixPO-ORD.
  RUN fixPO-ORDL.
  RUN fixPO-RCPTS.
  RUN fixPREP.
  RUN fixPRINTER.
  RUN fixPROBE.
  RUN fixPROBEIT.
  RUN fixPROBEIT-PRICE.
  RUN fixPROCAT.
  RUN fixPROD.
  RUN fixPRODL.
  RUN fixQUOTE.
  RUN fixQUOTECHG.
  RUN fixQUOTEHD.
  RUN fixQUOTEIT.
  RUN fixQUOTEITM.
  RUN fixQUOTEQTY.
  RUN fixRCERR.
  RUN fixREFTABLE.
  RUN fixREJCT-CD.
  RUN fixREPORT.
  RUN fixRM-BIN.
  RUN fixRM-CTRL.
  RUN fixRM-RCPT.
  RUN fixRM-RCPTH.
  RUN fixRM-RCTD.
  RUN fixRM-RCTH.
  RUN fixRM-RDTL.
  RUN fixRM-RDTLH.
  RUN fixRM-RECEIPTS.
  RUN fixROUTING.
  RUN fixROUTING-MTX.
  RUN fixSBSTATUS.
  RUN fixSHIFT.
  RUN fixSHIPTO.
  RUN fixSMAN.
  RUN fixSMAN-MTX.
  RUN fixSOLDTO.
  RUN fixSTATE.
  RUN fixSTAX.
  RUN fixSTAX-GROUP.
  RUN fixSTD-CODE.
  RUN fixSTYLE.
  RUN fixSURCHARGE.
  RUN fixSYS-CTRL.
  RUN fixSYS-CTRL-SHIPTO.
  RUN fixTERMS.
  RUN fixTERR.
  RUN fixTEST-RED.
  RUN fixUOM.
  RUN fixUSER-PRINT.
  RUN fixUSERCOMP.
  RUN fixUSR.
  RUN fixUSR-GRP.
  RUN fixUSRX.
  RUN fixVEND.
  RUN fixVENTYPE.
  RUN fixWASTE-TYPE.
END PROCEDURE.
 
PROCEDURE fixACCOUNT:
  DEFINE VARIABLE i AS INTEGER NO-UNDO.

  DISABLE TRIGGERS FOR LOAD OF account.
 
  IF NOT CAN-FIND(FIRST account WHERE account.rec_key EQ '') THEN RETURN.
  RUN msg ('Processing account ... ').
  FOR EACH account EXCLUSIVE-LOCK WHERE account.rec_key EQ '':
    ASSIGN
      account.rec_key = nextRecKey()
      i = i + 1.
    RUN createRecKey (account.rec_key,'account').
  END.
  RUN msg ('Fixed ' + STRING(i) + ' Records' + CHR(10)).
END PROCEDURE.
 
PROCEDURE fixAP-BUY:
  DEFINE VARIABLE i AS INTEGER NO-UNDO.

  DISABLE TRIGGERS FOR LOAD OF ap-buy.
 
  IF NOT CAN-FIND(FIRST ap-buy WHERE ap-buy.rec_key EQ '') THEN RETURN.
  RUN msg ('Processing ap-buy ... ').
  FOR EACH ap-buy EXCLUSIVE-LOCK WHERE ap-buy.rec_key EQ '':
    ASSIGN
      ap-buy.rec_key = nextRecKey()
      i = i + 1.
    RUN createRecKey (ap-buy.rec_key,'ap-buy').
  END.
  RUN msg ('Fixed ' + STRING(i) + ' Records' + CHR(10)).
END PROCEDURE.
 
PROCEDURE fixAP-CHK:
  DEFINE VARIABLE i AS INTEGER NO-UNDO.

  DISABLE TRIGGERS FOR LOAD OF ap-chk.
 
  IF NOT CAN-FIND(FIRST ap-chk WHERE ap-chk.rec_key EQ '') THEN RETURN.
  RUN msg ('Processing ap-chk ... ').
  FOR EACH ap-chk EXCLUSIVE-LOCK WHERE ap-chk.rec_key EQ '':
    ASSIGN
      ap-chk.rec_key = nextRecKey()
      i = i + 1.
    RUN createRecKey (ap-chk.rec_key,'ap-chk').
  END.
  RUN msg ('Fixed ' + STRING(i) + ' Records' + CHR(10)).
END PROCEDURE.
 
PROCEDURE fixAP-CTRL:
  DEFINE VARIABLE i AS INTEGER NO-UNDO.

  DISABLE TRIGGERS FOR LOAD OF ap-ctrl.
 
  IF NOT CAN-FIND(FIRST ap-ctrl WHERE ap-ctrl.rec_key EQ '') THEN RETURN.
  RUN msg ('Processing ap-ctrl ... ').
  FOR EACH ap-ctrl EXCLUSIVE-LOCK WHERE ap-ctrl.rec_key EQ '':
    ASSIGN
      ap-ctrl.rec_key = nextRecKey()
      i = i + 1.
    RUN createRecKey (ap-ctrl.rec_key,'ap-ctrl').
  END.
  RUN msg ('Fixed ' + STRING(i) + ' Records' + CHR(10)).
END PROCEDURE.
 
PROCEDURE fixAP-DIS:
  DEFINE VARIABLE i AS INTEGER NO-UNDO.

  DISABLE TRIGGERS FOR LOAD OF ap-dis.
 
  IF NOT CAN-FIND(FIRST ap-dis WHERE ap-dis.rec_key EQ '') THEN RETURN.
  RUN msg ('Processing ap-dis ... ').
  FOR EACH ap-dis EXCLUSIVE-LOCK WHERE ap-dis.rec_key EQ '':
    ASSIGN
      ap-dis.rec_key = nextRecKey()
      i = i + 1.
    RUN createRecKey (ap-dis.rec_key,'ap-dis').
  END.
  RUN msg ('Fixed ' + STRING(i) + ' Records' + CHR(10)).
END PROCEDURE.
 
PROCEDURE fixAP-DISL:
  DEFINE VARIABLE i AS INTEGER NO-UNDO.

  DISABLE TRIGGERS FOR LOAD OF ap-disl.
 
  IF NOT CAN-FIND(FIRST ap-disl WHERE ap-disl.rec_key EQ '') THEN RETURN.
  RUN msg ('Processing ap-disl ... ').
  FOR EACH ap-disl EXCLUSIVE-LOCK WHERE ap-disl.rec_key EQ '':
    ASSIGN
      ap-disl.rec_key = nextRecKey()
      i = i + 1.
    RUN createRecKey (ap-disl.rec_key,'ap-disl').
  END.
  RUN msg ('Fixed ' + STRING(i) + ' Records' + CHR(10)).
END PROCEDURE.
 
PROCEDURE fixAP-INV:
  DEFINE VARIABLE i AS INTEGER NO-UNDO.

  DISABLE TRIGGERS FOR LOAD OF ap-inv.
 
  IF NOT CAN-FIND(FIRST ap-inv WHERE ap-inv.rec_key EQ '') THEN RETURN.
  RUN msg ('Processing ap-inv ... ').
  FOR EACH ap-inv EXCLUSIVE-LOCK WHERE ap-inv.rec_key EQ '':
    ASSIGN
      ap-inv.rec_key = nextRecKey()
      i = i + 1.
    RUN createRecKey (ap-inv.rec_key,'ap-inv').
  END.
  RUN msg ('Fixed ' + STRING(i) + ' Records' + CHR(10)).
END PROCEDURE.
 
PROCEDURE fixAP-INVL:
  DEFINE VARIABLE i AS INTEGER NO-UNDO.

  DISABLE TRIGGERS FOR LOAD OF ap-invl.
 
  IF NOT CAN-FIND(FIRST ap-invl WHERE ap-invl.rec_key EQ '') THEN RETURN.
  RUN msg ('Processing ap-invl ... ').
  FOR EACH ap-invl EXCLUSIVE-LOCK WHERE ap-invl.rec_key EQ '':
    ASSIGN
      ap-invl.rec_key = nextRecKey()
      i = i + 1.
    RUN createRecKey (ap-invl.rec_key,'ap-invl').
  END.
  RUN msg ('Fixed ' + STRING(i) + ' Records' + CHR(10)).
END PROCEDURE.
 
PROCEDURE fixAP-INVLR:
  DEFINE VARIABLE i AS INTEGER NO-UNDO.

  DISABLE TRIGGERS FOR LOAD OF ap-invlr.
 
  IF NOT CAN-FIND(FIRST ap-invlr WHERE ap-invlr.rec_key EQ '') THEN RETURN.
  RUN msg ('Processing ap-invlr ... ').
  FOR EACH ap-invlr EXCLUSIVE-LOCK WHERE ap-invlr.rec_key EQ '':
    ASSIGN
      ap-invlr.rec_key = nextRecKey()
      i = i + 1.
    RUN createRecKey (ap-invlr.rec_key,'ap-invlr').
  END.
  RUN msg ('Fixed ' + STRING(i) + ' Records' + CHR(10)).
END PROCEDURE.
 
PROCEDURE fixAP-LEDGER:
  DEFINE VARIABLE i AS INTEGER NO-UNDO.

  DISABLE TRIGGERS FOR LOAD OF ap-ledger.
 
  IF NOT CAN-FIND(FIRST ap-ledger WHERE ap-ledger.rec_key EQ '') THEN RETURN.
  RUN msg ('Processing ap-ledger ... ').
  FOR EACH ap-ledger EXCLUSIVE-LOCK WHERE ap-ledger.rec_key EQ '':
    ASSIGN
      ap-ledger.rec_key = nextRecKey()
      i = i + 1.
    RUN createRecKey (ap-ledger.rec_key,'ap-ledger').
  END.
  RUN msg ('Fixed ' + STRING(i) + ' Records' + CHR(10)).
END PROCEDURE.
 
PROCEDURE fixAP-PAY:
  DEFINE VARIABLE i AS INTEGER NO-UNDO.

  DISABLE TRIGGERS FOR LOAD OF ap-pay.
 
  IF NOT CAN-FIND(FIRST ap-pay WHERE ap-pay.rec_key EQ '') THEN RETURN.
  RUN msg ('Processing ap-pay ... ').
  FOR EACH ap-pay EXCLUSIVE-LOCK WHERE ap-pay.rec_key EQ '':
    ASSIGN
      ap-pay.rec_key = nextRecKey()
      i = i + 1.
    RUN createRecKey (ap-pay.rec_key,'ap-pay').
  END.
  RUN msg ('Fixed ' + STRING(i) + ' Records' + CHR(10)).
END PROCEDURE.
 
PROCEDURE fixAP-PAYL:
  DEFINE VARIABLE i AS INTEGER NO-UNDO.

  DISABLE TRIGGERS FOR LOAD OF ap-payl.
 
  IF NOT CAN-FIND(FIRST ap-payl WHERE ap-payl.rec_key EQ '') THEN RETURN.
  RUN msg ('Processing ap-payl ... ').
  FOR EACH ap-payl EXCLUSIVE-LOCK WHERE ap-payl.rec_key EQ '':
    ASSIGN
      ap-payl.rec_key = nextRecKey()
      i = i + 1.
    RUN createRecKey (ap-payl.rec_key,'ap-payl').
  END.
  RUN msg ('Fixed ' + STRING(i) + ' Records' + CHR(10)).
END PROCEDURE.
 
PROCEDURE fixAP-SEL:
  DEFINE VARIABLE i AS INTEGER NO-UNDO.

  DISABLE TRIGGERS FOR LOAD OF ap-sel.
 
  IF NOT CAN-FIND(FIRST ap-sel WHERE ap-sel.rec_key EQ '') THEN RETURN.
  RUN msg ('Processing ap-sel ... ').
  FOR EACH ap-sel EXCLUSIVE-LOCK WHERE ap-sel.rec_key EQ '':
    ASSIGN
      ap-sel.rec_key = nextRecKey()
      i = i + 1.
    RUN createRecKey (ap-sel.rec_key,'ap-sel').
  END.
  RUN msg ('Fixed ' + STRING(i) + ' Records' + CHR(10)).
END PROCEDURE.
 
PROCEDURE fixAPHIST:
  DEFINE VARIABLE i AS INTEGER NO-UNDO.

  DISABLE TRIGGERS FOR LOAD OF aphist.
 
  IF NOT CAN-FIND(FIRST aphist WHERE aphist.rec_key EQ '') THEN RETURN.
  RUN msg ('Processing aphist ... ').
  FOR EACH aphist EXCLUSIVE-LOCK WHERE aphist.rec_key EQ '':
    ASSIGN
      aphist.rec_key = nextRecKey()
      i = i + 1.
    RUN createRecKey (aphist.rec_key,'aphist').
  END.
  RUN msg ('Fixed ' + STRING(i) + ' Records' + CHR(10)).
END PROCEDURE.
 
PROCEDURE fixAR-CASH:
  DEFINE VARIABLE i AS INTEGER NO-UNDO.

  DISABLE TRIGGERS FOR LOAD OF ar-cash.
 
  IF NOT CAN-FIND(FIRST ar-cash WHERE ar-cash.rec_key EQ '') THEN RETURN.
  RUN msg ('Processing ar-cash ... ').
  FOR EACH ar-cash EXCLUSIVE-LOCK WHERE ar-cash.rec_key EQ '':
    ASSIGN
      ar-cash.rec_key = nextRecKey()
      i = i + 1.
    RUN createRecKey (ar-cash.rec_key,'ar-cash').
  END.
  RUN msg ('Fixed ' + STRING(i) + ' Records' + CHR(10)).
END PROCEDURE.
 
PROCEDURE fixAR-CASHL:
  DEFINE VARIABLE i AS INTEGER NO-UNDO.

  DISABLE TRIGGERS FOR LOAD OF ar-cashl.
 
  IF NOT CAN-FIND(FIRST ar-cashl WHERE ar-cashl.rec_key EQ '') THEN RETURN.
  RUN msg ('Processing ar-cashl ... ').
  FOR EACH ar-cashl EXCLUSIVE-LOCK WHERE ar-cashl.rec_key EQ '':
    ASSIGN
      ar-cashl.rec_key = nextRecKey()
      i = i + 1.
    RUN createRecKey (ar-cashl.rec_key,'ar-cashl').
  END.
  RUN msg ('Fixed ' + STRING(i) + ' Records' + CHR(10)).
END PROCEDURE.
 
PROCEDURE fixAR-CTRL:
  DEFINE VARIABLE i AS INTEGER NO-UNDO.

  DISABLE TRIGGERS FOR LOAD OF ar-ctrl.
 
  IF NOT CAN-FIND(FIRST ar-ctrl WHERE ar-ctrl.rec_key EQ '') THEN RETURN.
  RUN msg ('Processing ar-ctrl ... ').
  FOR EACH ar-ctrl EXCLUSIVE-LOCK WHERE ar-ctrl.rec_key EQ '':
    ASSIGN
      ar-ctrl.rec_key = nextRecKey()
      i = i + 1.
    RUN createRecKey (ar-ctrl.rec_key,'ar-ctrl').
  END.
  RUN msg ('Fixed ' + STRING(i) + ' Records' + CHR(10)).
END PROCEDURE.
 
PROCEDURE fixAR-INV:
  DEFINE VARIABLE i AS INTEGER NO-UNDO.

  DISABLE TRIGGERS FOR LOAD OF ar-inv.
 
  IF NOT CAN-FIND(FIRST ar-inv WHERE ar-inv.rec_key EQ '') THEN RETURN.
  RUN msg ('Processing ar-inv ... ').
  FOR EACH ar-inv EXCLUSIVE-LOCK WHERE ar-inv.rec_key EQ '':
    ASSIGN
      ar-inv.rec_key = nextRecKey()
      i = i + 1.
    RUN createRecKey (ar-inv.rec_key,'ar-inv').
  END.
  RUN msg ('Fixed ' + STRING(i) + ' Records' + CHR(10)).
END PROCEDURE.
 
PROCEDURE fixAR-INVL:
  DEFINE VARIABLE i AS INTEGER NO-UNDO.

  DISABLE TRIGGERS FOR LOAD OF ar-invl.
 
  IF NOT CAN-FIND(FIRST ar-invl WHERE ar-invl.rec_key EQ '') THEN RETURN.
  RUN msg ('Processing ar-invl ... ').
  FOR EACH ar-invl EXCLUSIVE-LOCK WHERE ar-invl.rec_key EQ '':
    ASSIGN
      ar-invl.rec_key = nextRecKey()
      i = i + 1.
    RUN createRecKey (ar-invl.rec_key,'ar-invl').
  END.
  RUN msg ('Fixed ' + STRING(i) + ' Records' + CHR(10)).
END PROCEDURE.
 
PROCEDURE fixAR-INVM:
  DEFINE VARIABLE i AS INTEGER NO-UNDO.

  DISABLE TRIGGERS FOR LOAD OF ar-invm.
 
  IF NOT CAN-FIND(FIRST ar-invm WHERE ar-invm.rec_key EQ '') THEN RETURN.
  RUN msg ('Processing ar-invm ... ').
  FOR EACH ar-invm EXCLUSIVE-LOCK WHERE ar-invm.rec_key EQ '':
    ASSIGN
      ar-invm.rec_key = nextRecKey()
      i = i + 1.
    RUN createRecKey (ar-invm.rec_key,'ar-invm').
  END.
  RUN msg ('Fixed ' + STRING(i) + ' Records' + CHR(10)).
END PROCEDURE.
 
PROCEDURE fixAR-LEDGER:
  DEFINE VARIABLE i AS INTEGER NO-UNDO.

  DISABLE TRIGGERS FOR LOAD OF ar-ledger.
 
  IF NOT CAN-FIND(FIRST ar-ledger WHERE ar-ledger.rec_key EQ '') THEN RETURN.
  RUN msg ('Processing ar-ledger ... ').
  FOR EACH ar-ledger EXCLUSIVE-LOCK WHERE ar-ledger.rec_key EQ '':
    ASSIGN
      ar-ledger.rec_key = nextRecKey()
      i = i + 1.
    RUN createRecKey (ar-ledger.rec_key,'ar-ledger').
  END.
  RUN msg ('Fixed ' + STRING(i) + ' Records' + CHR(10)).
END PROCEDURE.
 
PROCEDURE fixAR-MCASH:
  DEFINE VARIABLE i AS INTEGER NO-UNDO.

  DISABLE TRIGGERS FOR LOAD OF ar-mcash.
 
  IF NOT CAN-FIND(FIRST ar-mcash WHERE ar-mcash.rec_key EQ '') THEN RETURN.
  RUN msg ('Processing ar-mcash ... ').
  FOR EACH ar-mcash EXCLUSIVE-LOCK WHERE ar-mcash.rec_key EQ '':
    ASSIGN
      ar-mcash.rec_key = nextRecKey()
      i = i + 1.
    RUN createRecKey (ar-mcash.rec_key,'ar-mcash').
  END.
  RUN msg ('Fixed ' + STRING(i) + ' Records' + CHR(10)).
END PROCEDURE.
 
PROCEDURE fixASINOTES:
  DEFINE VARIABLE i AS INTEGER NO-UNDO.

  DISABLE TRIGGERS FOR LOAD OF asinotes.
 
  IF NOT CAN-FIND(FIRST asinotes WHERE asinotes.rec_key EQ '') THEN RETURN.
  RUN msg ('Processing asinotes ... ').
  FOR EACH asinotes EXCLUSIVE-LOCK WHERE asinotes.rec_key EQ '':
    ASSIGN
      asinotes.rec_key = nextRecKey()
      i = i + 1.
    RUN createRecKey (asinotes.rec_key,'asinotes').
  END.
  RUN msg ('Fixed ' + STRING(i) + ' Records' + CHR(10)).
END PROCEDURE.
 
PROCEDURE fixBANK:
  DEFINE VARIABLE i AS INTEGER NO-UNDO.

  DISABLE TRIGGERS FOR LOAD OF bank.
 
  IF NOT CAN-FIND(FIRST bank WHERE bank.rec_key EQ '') THEN RETURN.
  RUN msg ('Processing bank ... ').
  FOR EACH bank EXCLUSIVE-LOCK WHERE bank.rec_key EQ '':
    ASSIGN
      bank.rec_key = nextRecKey()
      i = i + 1.
    RUN createRecKey (bank.rec_key,'bank').
  END.
  RUN msg ('Fixed ' + STRING(i) + ' Records' + CHR(10)).
END PROCEDURE.
 
PROCEDURE fixBOX-DESIGN-HDR:
  DEFINE VARIABLE i AS INTEGER NO-UNDO.

  DISABLE TRIGGERS FOR LOAD OF box-design-hdr.
 
  IF NOT CAN-FIND(FIRST box-design-hdr WHERE box-design-hdr.rec_key EQ '') THEN RETURN.
  RUN msg ('Processing box-design-hdr ... ').
  FOR EACH box-design-hdr EXCLUSIVE-LOCK WHERE box-design-hdr.rec_key EQ '':
    ASSIGN
      box-design-hdr.rec_key = nextRecKey()
      i = i + 1.
    RUN createRecKey (box-design-hdr.rec_key,'box-design-hdr').
  END.
  RUN msg ('Fixed ' + STRING(i) + ' Records' + CHR(10)).
END PROCEDURE.
 
PROCEDURE fixBOX-DESIGN-LINE:
  DEFINE VARIABLE i AS INTEGER NO-UNDO.

  DISABLE TRIGGERS FOR LOAD OF box-design-line.
 
  IF NOT CAN-FIND(FIRST box-design-line WHERE box-design-line.rec_key EQ '') THEN RETURN.
  RUN msg ('Processing box-design-line ... ').
  FOR EACH box-design-line EXCLUSIVE-LOCK WHERE box-design-line.rec_key EQ '':
    ASSIGN
      box-design-line.rec_key = nextRecKey()
      i = i + 1.
    RUN createRecKey (box-design-line.rec_key,'box-design-line').
  END.
  RUN msg ('Fixed ' + STRING(i) + ' Records' + CHR(10)).
END PROCEDURE.
 
PROCEDURE fixBUYER:
  DEFINE VARIABLE i AS INTEGER NO-UNDO.

  DISABLE TRIGGERS FOR LOAD OF buyer.
 
  IF NOT CAN-FIND(FIRST buyer WHERE buyer.rec_key EQ '') THEN RETURN.
  RUN msg ('Processing buyer ... ').
  FOR EACH buyer EXCLUSIVE-LOCK WHERE buyer.rec_key EQ '':
    ASSIGN
      buyer.rec_key = nextRecKey()
      i = i + 1.
    RUN createRecKey (buyer.rec_key,'buyer').
  END.
  RUN msg ('Fixed ' + STRING(i) + ' Records' + CHR(10)).
END PROCEDURE.
 
PROCEDURE fixCARR-MTX:
  DEFINE VARIABLE i AS INTEGER NO-UNDO.

  DISABLE TRIGGERS FOR LOAD OF carr-mtx.
 
  IF NOT CAN-FIND(FIRST carr-mtx WHERE carr-mtx.rec_key EQ '') THEN RETURN.
  RUN msg ('Processing carr-mtx ... ').
  FOR EACH carr-mtx EXCLUSIVE-LOCK WHERE carr-mtx.rec_key EQ '':
    ASSIGN
      carr-mtx.rec_key = nextRecKey()
      i = i + 1.
    RUN createRecKey (carr-mtx.rec_key,'carr-mtx').
  END.
  RUN msg ('Fixed ' + STRING(i) + ' Records' + CHR(10)).
END PROCEDURE.
 
PROCEDURE fixCARRIER:
  DEFINE VARIABLE i AS INTEGER NO-UNDO.

  DISABLE TRIGGERS FOR LOAD OF carrier.
 
  IF NOT CAN-FIND(FIRST carrier WHERE carrier.rec_key EQ '') THEN RETURN.
  RUN msg ('Processing carrier ... ').
  FOR EACH carrier EXCLUSIVE-LOCK WHERE carrier.rec_key EQ '':
    ASSIGN
      carrier.rec_key = nextRecKey()
      i = i + 1.
    RUN createRecKey (carrier.rec_key,'carrier').
  END.
  RUN msg ('Fixed ' + STRING(i) + ' Records' + CHR(10)).
END PROCEDURE.
 
PROCEDURE fixCE-CTRL:
  DEFINE VARIABLE i AS INTEGER NO-UNDO.

  DISABLE TRIGGERS FOR LOAD OF ce-ctrl.
 
  IF NOT CAN-FIND(FIRST ce-ctrl WHERE ce-ctrl.rec_key EQ '') THEN RETURN.
  RUN msg ('Processing ce-ctrl ... ').
  FOR EACH ce-ctrl EXCLUSIVE-LOCK WHERE ce-ctrl.rec_key EQ '':
    ASSIGN
      ce-ctrl.rec_key = nextRecKey()
      i = i + 1.
    RUN createRecKey (ce-ctrl.rec_key,'ce-ctrl').
  END.
  RUN msg ('Fixed ' + STRING(i) + ' Records' + CHR(10)).
END PROCEDURE.
 
PROCEDURE fixCOMPANY:
  DEFINE VARIABLE i AS INTEGER NO-UNDO.

  DISABLE TRIGGERS FOR LOAD OF company.
 
  IF NOT CAN-FIND(FIRST company WHERE company.rec_key EQ '') THEN RETURN.
  RUN msg ('Processing company ... ').
  FOR EACH company EXCLUSIVE-LOCK WHERE company.rec_key EQ '':
    ASSIGN
      company.rec_key = nextRecKey()
      i = i + 1.
    RUN createRecKey (company.rec_key,'company').
  END.
  RUN msg ('Fixed ' + STRING(i) + ' Records' + CHR(10)).
END PROCEDURE.
 
PROCEDURE fixCOSTTYPE:
  DEFINE VARIABLE i AS INTEGER NO-UNDO.

  DISABLE TRIGGERS FOR LOAD OF costtype.
 
  IF NOT CAN-FIND(FIRST costtype WHERE costtype.rec_key EQ '') THEN RETURN.
  RUN msg ('Processing costtype ... ').
  FOR EACH costtype EXCLUSIVE-LOCK WHERE costtype.rec_key EQ '':
    ASSIGN
      costtype.rec_key = nextRecKey()
      i = i + 1.
    RUN createRecKey (costtype.rec_key,'costtype').
  END.
  RUN msg ('Fixed ' + STRING(i) + ' Records' + CHR(10)).
END PROCEDURE.
 
PROCEDURE fixCREDIT-HOLD-TYPE:
  DEFINE VARIABLE i AS INTEGER NO-UNDO.

  DISABLE TRIGGERS FOR LOAD OF credit-hold-type.
 
  IF NOT CAN-FIND(FIRST credit-hold-type WHERE credit-hold-type.rec_key EQ '') THEN RETURN.
  RUN msg ('Processing credit-hold-type ... ').
  FOR EACH credit-hold-type EXCLUSIVE-LOCK WHERE credit-hold-type.rec_key EQ '':
    ASSIGN
      credit-hold-type.rec_key = nextRecKey()
      i = i + 1.
    RUN createRecKey (credit-hold-type.rec_key,'credit-hold-type').
  END.
  RUN msg ('Fixed ' + STRING(i) + ' Records' + CHR(10)).
END PROCEDURE.
 
PROCEDURE fixCREW:
  DEFINE VARIABLE i AS INTEGER NO-UNDO.

  DISABLE TRIGGERS FOR LOAD OF crew.
 
  IF NOT CAN-FIND(FIRST crew WHERE crew.rec_key EQ '') THEN RETURN.
  RUN msg ('Processing crew ... ').
  FOR EACH crew EXCLUSIVE-LOCK WHERE crew.rec_key EQ '':
    ASSIGN
      crew.rec_key = nextRecKey()
      i = i + 1.
    RUN createRecKey (crew.rec_key,'crew').
  END.
  RUN msg ('Fixed ' + STRING(i) + ' Records' + CHR(10)).
END PROCEDURE.
 
PROCEDURE fixCURRENCY:
  DEFINE VARIABLE i AS INTEGER NO-UNDO.

  DISABLE TRIGGERS FOR LOAD OF currency.
 
  IF NOT CAN-FIND(FIRST currency WHERE currency.rec_key EQ '') THEN RETURN.
  RUN msg ('Processing currency ... ').
  FOR EACH currency EXCLUSIVE-LOCK WHERE currency.rec_key EQ '':
    ASSIGN
      currency.rec_key = nextRecKey()
      i = i + 1.
    RUN createRecKey (currency.rec_key,'currency').
  END.
  RUN msg ('Fixed ' + STRING(i) + ' Records' + CHR(10)).
END PROCEDURE.
 
PROCEDURE fixCUST:
  DEFINE VARIABLE i AS INTEGER NO-UNDO.

  DISABLE TRIGGERS FOR LOAD OF cust.
 
  IF NOT CAN-FIND(FIRST cust WHERE cust.rec_key EQ '') THEN RETURN.
  RUN msg ('Processing cust ... ').
  FOR EACH cust EXCLUSIVE-LOCK WHERE cust.rec_key EQ '':
    ASSIGN
      cust.rec_key = nextRecKey()
      i = i + 1.
    RUN createRecKey (cust.rec_key,'cust').
  END.
  RUN msg ('Fixed ' + STRING(i) + ' Records' + CHR(10)).
END PROCEDURE.
 
PROCEDURE fixCUST-MARKUP:
  DEFINE VARIABLE i AS INTEGER NO-UNDO.

  DISABLE TRIGGERS FOR LOAD OF cust-markup.
 
  IF NOT CAN-FIND(FIRST cust-markup WHERE cust-markup.rec_key EQ '') THEN RETURN.
  RUN msg ('Processing cust-markup ... ').
  FOR EACH cust-markup EXCLUSIVE-LOCK WHERE cust-markup.rec_key EQ '':
    ASSIGN
      cust-markup.rec_key = nextRecKey()
      i = i + 1.
    RUN createRecKey (cust-markup.rec_key,'cust-markup').
  END.
  RUN msg ('Fixed ' + STRING(i) + ' Records' + CHR(10)).
END PROCEDURE.
 
PROCEDURE fixCUST-PART:
  DEFINE VARIABLE i AS INTEGER NO-UNDO.

  DISABLE TRIGGERS FOR LOAD OF cust-part.
 
  IF NOT CAN-FIND(FIRST cust-part WHERE cust-part.rec_key EQ '') THEN RETURN.
  RUN msg ('Processing cust-part ... ').
  FOR EACH cust-part EXCLUSIVE-LOCK WHERE cust-part.rec_key EQ '':
    ASSIGN
      cust-part.rec_key = nextRecKey()
      i = i + 1.
    RUN createRecKey (cust-part.rec_key,'cust-part').
  END.
  RUN msg ('Fixed ' + STRING(i) + ' Records' + CHR(10)).
END PROCEDURE.
 
PROCEDURE fixCUST-PROD-SALES:
  DEFINE VARIABLE i AS INTEGER NO-UNDO.

  DISABLE TRIGGERS FOR LOAD OF cust-prod-sales.
 
  IF NOT CAN-FIND(FIRST cust-prod-sales WHERE cust-prod-sales.rec_key EQ '') THEN RETURN.
  RUN msg ('Processing cust-prod-sales ... ').
  FOR EACH cust-prod-sales EXCLUSIVE-LOCK WHERE cust-prod-sales.rec_key EQ '':
    ASSIGN
      cust-prod-sales.rec_key = nextRecKey()
      i = i + 1.
    RUN createRecKey (cust-prod-sales.rec_key,'cust-prod-sales').
  END.
  RUN msg ('Fixed ' + STRING(i) + ' Records' + CHR(10)).
END PROCEDURE.
 
PROCEDURE fixCUSTYPE:
  DEFINE VARIABLE i AS INTEGER NO-UNDO.

  DISABLE TRIGGERS FOR LOAD OF custype.
 
  IF NOT CAN-FIND(FIRST custype WHERE custype.rec_key EQ '') THEN RETURN.
  RUN msg ('Processing custype ... ').
  FOR EACH custype EXCLUSIVE-LOCK WHERE custype.rec_key EQ '':
    ASSIGN
      custype.rec_key = nextRecKey()
      i = i + 1.
    RUN createRecKey (custype.rec_key,'custype').
  END.
  RUN msg ('Fixed ' + STRING(i) + ' Records' + CHR(10)).
END PROCEDURE.
 
PROCEDURE fixDB-CTRL:
  DEFINE VARIABLE i AS INTEGER NO-UNDO.

  DISABLE TRIGGERS FOR LOAD OF db-ctrl.
 
  IF NOT CAN-FIND(FIRST db-ctrl WHERE db-ctrl.rec_key EQ '') THEN RETURN.
  RUN msg ('Processing db-ctrl ... ').
  FOR EACH db-ctrl EXCLUSIVE-LOCK WHERE db-ctrl.rec_key EQ '':
    ASSIGN
      db-ctrl.rec_key = nextRecKey()
      i = i + 1.
    RUN createRecKey (db-ctrl.rec_key,'db-ctrl').
  END.
  RUN msg ('Fixed ' + STRING(i) + ' Records' + CHR(10)).
END PROCEDURE.
 
PROCEDURE fixDEPT:
  DEFINE VARIABLE i AS INTEGER NO-UNDO.

  DISABLE TRIGGERS FOR LOAD OF dept.
 
  IF NOT CAN-FIND(FIRST dept WHERE dept.rec_key EQ '') THEN RETURN.
  RUN msg ('Processing dept ... ').
  FOR EACH dept EXCLUSIVE-LOCK WHERE dept.rec_key EQ '':
    ASSIGN
      dept.rec_key = nextRecKey()
      i = i + 1.
    RUN createRecKey (dept.rec_key,'dept').
  END.
  RUN msg ('Fixed ' + STRING(i) + ' Records' + CHR(10)).
END PROCEDURE.
 
PROCEDURE fixDOWN-TYPE:
  DEFINE VARIABLE i AS INTEGER NO-UNDO.

  DISABLE TRIGGERS FOR LOAD OF down-type.
 
  IF NOT CAN-FIND(FIRST down-type WHERE down-type.rec_key EQ '') THEN RETURN.
  RUN msg ('Processing down-type ... ').
  FOR EACH down-type EXCLUSIVE-LOCK WHERE down-type.rec_key EQ '':
    ASSIGN
      down-type.rec_key = nextRecKey()
      i = i + 1.
    RUN createRecKey (down-type.rec_key,'down-type').
  END.
  RUN msg ('Fixed ' + STRING(i) + ' Records' + CHR(10)).
END PROCEDURE.
 
PROCEDURE fixE-ITEM:
  DEFINE VARIABLE i AS INTEGER NO-UNDO.

  DISABLE TRIGGERS FOR LOAD OF e-item.
 
  IF NOT CAN-FIND(FIRST e-item WHERE e-item.rec_key EQ '') THEN RETURN.
  RUN msg ('Processing e-item ... ').
  FOR EACH e-item EXCLUSIVE-LOCK WHERE e-item.rec_key EQ '':
    ASSIGN
      e-item.rec_key = nextRecKey()
      i = i + 1.
    RUN createRecKey (e-item.rec_key,'e-item').
  END.
  RUN msg ('Fixed ' + STRING(i) + ' Records' + CHR(10)).
END PROCEDURE.
 
PROCEDURE fixE-ITEM-CUST:
  DEFINE VARIABLE i AS INTEGER NO-UNDO.

  DISABLE TRIGGERS FOR LOAD OF e-item-cust.
 
  IF NOT CAN-FIND(FIRST e-item-cust WHERE e-item-cust.rec_key EQ '') THEN RETURN.
  RUN msg ('Processing e-item-cust ... ').
  FOR EACH e-item-cust EXCLUSIVE-LOCK WHERE e-item-cust.rec_key EQ '':
    ASSIGN
      e-item-cust.rec_key = nextRecKey()
      i = i + 1.
    RUN createRecKey (e-item-cust.rec_key,'e-item-cust').
  END.
  RUN msg ('Fixed ' + STRING(i) + ' Records' + CHR(10)).
END PROCEDURE.
 
PROCEDURE fixE-ITEM-VEND:
  DEFINE VARIABLE i AS INTEGER NO-UNDO.

  DISABLE TRIGGERS FOR LOAD OF e-item-vend.
 
  IF NOT CAN-FIND(FIRST e-item-vend WHERE e-item-vend.rec_key EQ '') THEN RETURN.
  RUN msg ('Processing e-item-vend ... ').
  FOR EACH e-item-vend EXCLUSIVE-LOCK WHERE e-item-vend.rec_key EQ '':
    ASSIGN
      e-item-vend.rec_key = nextRecKey()
      i = i + 1.
    RUN createRecKey (e-item-vend.rec_key,'e-item-vend').
  END.
  RUN msg ('Fixed ' + STRING(i) + ' Records' + CHR(10)).
END PROCEDURE.
 
PROCEDURE fixE-ITEMFG:
  DEFINE VARIABLE i AS INTEGER NO-UNDO.

  DISABLE TRIGGERS FOR LOAD OF e-itemfg.
 
  IF NOT CAN-FIND(FIRST e-itemfg WHERE e-itemfg.rec_key EQ '') THEN RETURN.
  RUN msg ('Processing e-itemfg ... ').
  FOR EACH e-itemfg EXCLUSIVE-LOCK WHERE e-itemfg.rec_key EQ '':
    ASSIGN
      e-itemfg.rec_key = nextRecKey()
      i = i + 1.
    RUN createRecKey (e-itemfg.rec_key,'e-itemfg').
  END.
  RUN msg ('Fixed ' + STRING(i) + ' Records' + CHR(10)).
END PROCEDURE.
 
PROCEDURE fixE-ITEMFG-VEND:
  DEFINE VARIABLE i AS INTEGER NO-UNDO.

  DISABLE TRIGGERS FOR LOAD OF e-itemfg-vend.
 
  IF NOT CAN-FIND(FIRST e-itemfg-vend WHERE e-itemfg-vend.rec_key EQ '') THEN RETURN.
  RUN msg ('Processing e-itemfg-vend ... ').
  FOR EACH e-itemfg-vend EXCLUSIVE-LOCK WHERE e-itemfg-vend.rec_key EQ '':
    ASSIGN
      e-itemfg-vend.rec_key = nextRecKey()
      i = i + 1.
    RUN createRecKey (e-itemfg-vend.rec_key,'e-itemfg-vend').
  END.
  RUN msg ('Fixed ' + STRING(i) + ' Records' + CHR(10)).
END PROCEDURE.
 
PROCEDURE fixEB:
  DEFINE VARIABLE i AS INTEGER NO-UNDO.

  DISABLE TRIGGERS FOR LOAD OF eb.
 
  IF NOT CAN-FIND(FIRST eb WHERE eb.rec_key EQ '') THEN RETURN.
  RUN msg ('Processing eb ... ').
  FOR EACH eb EXCLUSIVE-LOCK WHERE eb.rec_key EQ '':
    ASSIGN
      eb.rec_key = nextRecKey()
      i = i + 1.
    RUN createRecKey (eb.rec_key,'eb').
  END.
  RUN msg ('Fixed ' + STRING(i) + ' Records' + CHR(10)).
END PROCEDURE.
 
PROCEDURE fixEDAPCHECK:
  DEFINE VARIABLE i AS INTEGER NO-UNDO.

  DISABLE TRIGGERS FOR LOAD OF edapcheck.
 
  IF NOT CAN-FIND(FIRST EDAPCheck WHERE EDAPCheck.rec_key EQ '') THEN RETURN.
  RUN msg ('Processing EDAPCheck ... ').
  FOR EACH EDAPCheck EXCLUSIVE-LOCK WHERE EDAPCheck.rec_key EQ '':
    ASSIGN
      EDAPCheck.rec_key = nextRecKey()
      i = i + 1.
    RUN createRecKey (EDAPCheck.rec_key,'EDAPCheck').
  END.
  RUN msg ('Fixed ' + STRING(i) + ' Records' + CHR(10)).
END PROCEDURE.
 
PROCEDURE fixEDCAT:
  DEFINE VARIABLE i AS INTEGER NO-UNDO.

  DISABLE TRIGGERS FOR LOAD OF edcat.
 
  IF NOT CAN-FIND(FIRST EDCat WHERE EDCat.rec_key EQ '') THEN RETURN.
  RUN msg ('Processing EDCat ... ').
  FOR EACH EDCat EXCLUSIVE-LOCK WHERE EDCat.rec_key EQ '':
    ASSIGN
      EDCat.rec_key = nextRecKey()
      i = i + 1.
    RUN createRecKey (EDCat.rec_key,'EDCat').
  END.
  RUN msg ('Fixed ' + STRING(i) + ' Records' + CHR(10)).
END PROCEDURE.
 
PROCEDURE fixEDCATLINE:
  DEFINE VARIABLE i AS INTEGER NO-UNDO.

  DISABLE TRIGGERS FOR LOAD OF edcatline.
 
  IF NOT CAN-FIND(FIRST EDCatline WHERE EDCatline.rec_key EQ '') THEN RETURN.
  RUN msg ('Processing EDCatline ... ').
  FOR EACH EDCatline EXCLUSIVE-LOCK WHERE EDCatline.rec_key EQ '':
    ASSIGN
      EDCatline.rec_key = nextRecKey()
      i = i + 1.
    RUN createRecKey (EDCatline.rec_key,'EDCatline').
  END.
  RUN msg ('Fixed ' + STRING(i) + ' Records' + CHR(10)).
END PROCEDURE.
 
PROCEDURE fixEDCATPRICE:
  DEFINE VARIABLE i AS INTEGER NO-UNDO.

  DISABLE TRIGGERS FOR LOAD OF edcatprice.
 
  IF NOT CAN-FIND(FIRST EDCatPrice WHERE EDCatPrice.rec_key EQ '') THEN RETURN.
  RUN msg ('Processing EDCatPrice ... ').
  FOR EACH EDCatPrice EXCLUSIVE-LOCK WHERE EDCatPrice.rec_key EQ '':
    ASSIGN
      EDCatPrice.rec_key = nextRecKey()
      i = i + 1.
    RUN createRecKey (EDCatPrice.rec_key,'EDCatPrice').
  END.
  RUN msg ('Fixed ' + STRING(i) + ' Records' + CHR(10)).
END PROCEDURE.
 
PROCEDURE fixEDCO:
  DEFINE VARIABLE i AS INTEGER NO-UNDO.

  DISABLE TRIGGERS FOR LOAD OF edco.
 
  IF NOT CAN-FIND(FIRST EDCo WHERE EDCo.rec_key EQ '') THEN RETURN.
  RUN msg ('Processing EDCo ... ').
  FOR EACH EDCo EXCLUSIVE-LOCK WHERE EDCo.rec_key EQ '':
    ASSIGN
      EDCo.rec_key = nextRecKey()
      i = i + 1.
    RUN createRecKey (EDCo.rec_key,'EDCo').
  END.
  RUN msg ('Fixed ' + STRING(i) + ' Records' + CHR(10)).
END PROCEDURE.
 
PROCEDURE fixEDCODE:
  DEFINE VARIABLE i AS INTEGER NO-UNDO.

  DISABLE TRIGGERS FOR LOAD OF edcode.
 
  IF NOT CAN-FIND(FIRST EDCode WHERE EDCode.rec_key EQ '') THEN RETURN.
  RUN msg ('Processing EDCode ... ').
  FOR EACH EDCode EXCLUSIVE-LOCK WHERE EDCode.rec_key EQ '':
    ASSIGN
      EDCode.rec_key = nextRecKey()
      i = i + 1.
    RUN createRecKey (EDCode.rec_key,'EDCode').
  END.
  RUN msg ('Fixed ' + STRING(i) + ' Records' + CHR(10)).
END PROCEDURE.
 
PROCEDURE fixEDDOC:
  DEFINE VARIABLE i AS INTEGER NO-UNDO.

  DISABLE TRIGGERS FOR LOAD OF eddoc.
 
  IF NOT CAN-FIND(FIRST EDDoc WHERE EDDoc.rec_key EQ '') THEN RETURN.
  RUN msg ('Processing EDDoc ... ').
  FOR EACH EDDoc EXCLUSIVE-LOCK WHERE EDDoc.rec_key EQ '':
    ASSIGN
      EDDoc.rec_key = nextRecKey()
      i = i + 1.
    RUN createRecKey (EDDoc.rec_key,'EDDoc').
  END.
  RUN msg ('Fixed ' + STRING(i) + ' Records' + CHR(10)).
END PROCEDURE.
 
PROCEDURE fixEDICXREF:
  DEFINE VARIABLE i AS INTEGER NO-UNDO.

  DISABLE TRIGGERS FOR LOAD OF edicxref.
 
  IF NOT CAN-FIND(FIRST EDICXref WHERE EDICXref.rec_key EQ '') THEN RETURN.
  RUN msg ('Processing EDICXref ... ').
  FOR EACH EDICXref EXCLUSIVE-LOCK WHERE EDICXref.rec_key EQ '':
    ASSIGN
      EDICXref.rec_key = nextRecKey()
      i = i + 1.
    RUN createRecKey (EDICXref.rec_key,'EDICXref').
  END.
  RUN msg ('Fixed ' + STRING(i) + ' Records' + CHR(10)).
END PROCEDURE.
 
PROCEDURE fixEDIVADDON:
  DEFINE VARIABLE i AS INTEGER NO-UNDO.

  DISABLE TRIGGERS FOR LOAD OF edivaddon.
 
  IF NOT CAN-FIND(FIRST EDIVAddon WHERE EDIVAddon.rec_key EQ '') THEN RETURN.
  RUN msg ('Processing EDIVAddon ... ').
  FOR EACH EDIVAddon EXCLUSIVE-LOCK WHERE EDIVAddon.rec_key EQ '':
    ASSIGN
      EDIVAddon.rec_key = nextRecKey()
      i = i + 1.
    RUN createRecKey (EDIVAddon.rec_key,'EDIVAddon').
  END.
  RUN msg ('Fixed ' + STRING(i) + ' Records' + CHR(10)).
END PROCEDURE.
 
PROCEDURE fixEDIVLINE:
  DEFINE VARIABLE i AS INTEGER NO-UNDO.

  DISABLE TRIGGERS FOR LOAD OF edivline.
 
  IF NOT CAN-FIND(FIRST EDIVLine WHERE EDIVLine.rec_key EQ '') THEN RETURN.
  RUN msg ('Processing EDIVLine ... ').
  FOR EACH EDIVLine EXCLUSIVE-LOCK WHERE EDIVLine.rec_key EQ '':
    ASSIGN
      EDIVLine.rec_key = nextRecKey()
      i = i + 1.
    RUN createRecKey (EDIVLine.rec_key,'EDIVLine').
  END.
  RUN msg ('Fixed ' + STRING(i) + ' Records' + CHR(10)).
END PROCEDURE.
 
PROCEDURE fixEDIVTRAN:
  DEFINE VARIABLE i AS INTEGER NO-UNDO.

  DISABLE TRIGGERS FOR LOAD OF edivtran.
 
  IF NOT CAN-FIND(FIRST EDIVTran WHERE EDIVTran.rec_key EQ '') THEN RETURN.
  RUN msg ('Processing EDIVTran ... ').
  FOR EACH EDIVTran EXCLUSIVE-LOCK WHERE EDIVTran.rec_key EQ '':
    ASSIGN
      EDIVTran.rec_key = nextRecKey()
      i = i + 1.
    RUN createRecKey (EDIVTran.rec_key,'EDIVTran').
  END.
  RUN msg ('Fixed ' + STRING(i) + ' Records' + CHR(10)).
END PROCEDURE.
 
PROCEDURE fixEDMAST:
  DEFINE VARIABLE i AS INTEGER NO-UNDO.

  DISABLE TRIGGERS FOR LOAD OF edmast.
 
  IF NOT CAN-FIND(FIRST EDMast WHERE EDMast.rec_key EQ '') THEN RETURN.
  RUN msg ('Processing EDMast ... ').
  FOR EACH EDMast EXCLUSIVE-LOCK WHERE EDMast.rec_key EQ '':
    ASSIGN
      EDMast.rec_key = nextRecKey()
      i = i + 1.
    RUN createRecKey (EDMast.rec_key,'EDMast').
  END.
  RUN msg ('Fixed ' + STRING(i) + ' Records' + CHR(10)).
END PROCEDURE.
 
PROCEDURE fixEDPD:
  DEFINE VARIABLE i AS INTEGER NO-UNDO.

  DISABLE TRIGGERS FOR LOAD OF edpd.
 
  IF NOT CAN-FIND(FIRST EDPD WHERE EDPD.rec_key EQ '') THEN RETURN.
  RUN msg ('Processing EDPD ... ').
  FOR EACH EDPD EXCLUSIVE-LOCK WHERE EDPD.rec_key EQ '':
    ASSIGN
      EDPD.rec_key = nextRecKey()
      i = i + 1.
    RUN createRecKey (EDPD.rec_key,'EDPD').
  END.
  RUN msg ('Fixed ' + STRING(i) + ' Records' + CHR(10)).
END PROCEDURE.
 
PROCEDURE fixEDPOADDON:
  DEFINE VARIABLE i AS INTEGER NO-UNDO.

  DISABLE TRIGGERS FOR LOAD OF edpoaddon.
 
  IF NOT CAN-FIND(FIRST EDPOAddon WHERE EDPOAddon.rec_key EQ '') THEN RETURN.
  RUN msg ('Processing EDPOAddon ... ').
  FOR EACH EDPOAddon EXCLUSIVE-LOCK WHERE EDPOAddon.rec_key EQ '':
    ASSIGN
      EDPOAddon.rec_key = nextRecKey()
      i = i + 1.
    RUN createRecKey (EDPOAddon.rec_key,'EDPOAddon').
  END.
  RUN msg ('Fixed ' + STRING(i) + ' Records' + CHR(10)).
END PROCEDURE.
 
PROCEDURE fixEDPOLINE:
  DEFINE VARIABLE i AS INTEGER NO-UNDO.

  DISABLE TRIGGERS FOR LOAD OF edpoline.
 
  IF NOT CAN-FIND(FIRST EDPOLine WHERE EDPOLine.rec_key EQ '') THEN RETURN.
  RUN msg ('Processing EDPOLine ... ').
  FOR EACH EDPOLine EXCLUSIVE-LOCK WHERE EDPOLine.rec_key EQ '':
    ASSIGN
      EDPOLine.rec_key = nextRecKey()
      i = i + 1.
    RUN createRecKey (EDPOLine.rec_key,'EDPOLine').
  END.
  RUN msg ('Fixed ' + STRING(i) + ' Records' + CHR(10)).
END PROCEDURE.
 
PROCEDURE fixEDPOTRAN:
  DEFINE VARIABLE i AS INTEGER NO-UNDO.

  DISABLE TRIGGERS FOR LOAD OF edpotran.
 
  IF NOT CAN-FIND(FIRST EDPOTran WHERE EDPOTran.rec_key EQ '') THEN RETURN.
  RUN msg ('Processing EDPOTran ... ').
  FOR EACH EDPOTran EXCLUSIVE-LOCK WHERE EDPOTran.rec_key EQ '':
    ASSIGN
      EDPOTran.rec_key = nextRecKey()
      i = i + 1.
    RUN createRecKey (EDPOTran.rec_key,'EDPOTran').
  END.
  RUN msg ('Fixed ' + STRING(i) + ' Records' + CHR(10)).
END PROCEDURE.
 
PROCEDURE fixEDSHIPTO:
  DEFINE VARIABLE i AS INTEGER NO-UNDO.

  DISABLE TRIGGERS FOR LOAD OF edshipto.
 
  IF NOT CAN-FIND(FIRST EDShipto WHERE EDShipto.rec_key EQ '') THEN RETURN.
  RUN msg ('Processing EDShipto ... ').
  FOR EACH EDShipto EXCLUSIVE-LOCK WHERE EDShipto.rec_key EQ '':
    ASSIGN
      EDShipto.rec_key = nextRecKey()
      i = i + 1.
    RUN createRecKey (EDShipto.rec_key,'EDShipto').
  END.
  RUN msg ('Fixed ' + STRING(i) + ' Records' + CHR(10)).
END PROCEDURE.
 
PROCEDURE fixEDSHIPVIA:
  DEFINE VARIABLE i AS INTEGER NO-UNDO.

  DISABLE TRIGGERS FOR LOAD OF edshipvia.
 
  IF NOT CAN-FIND(FIRST EDShipVia WHERE EDShipVia.rec_key EQ '') THEN RETURN.
  RUN msg ('Processing EDShipVia ... ').
  FOR EACH EDShipVia EXCLUSIVE-LOCK WHERE EDShipVia.rec_key EQ '':
    ASSIGN
      EDShipVia.rec_key = nextRecKey()
      i = i + 1.
    RUN createRecKey (EDShipVia.rec_key,'EDShipVia').
  END.
  RUN msg ('Fixed ' + STRING(i) + ' Records' + CHR(10)).
END PROCEDURE.
 
PROCEDURE fixEDSHLINE:
  DEFINE VARIABLE i AS INTEGER NO-UNDO.

  DISABLE TRIGGERS FOR LOAD OF edshline.
 
  IF NOT CAN-FIND(FIRST EDSHLine WHERE EDSHLine.rec_key EQ '') THEN RETURN.
  RUN msg ('Processing EDSHLine ... ').
  FOR EACH EDSHLine EXCLUSIVE-LOCK WHERE EDSHLine.rec_key EQ '':
    ASSIGN
      EDSHLine.rec_key = nextRecKey()
      i = i + 1.
    RUN createRecKey (EDSHLine.rec_key,'EDSHLine').
  END.
  RUN msg ('Fixed ' + STRING(i) + ' Records' + CHR(10)).
END PROCEDURE.
 
PROCEDURE fixEDSHORD:
  DEFINE VARIABLE i AS INTEGER NO-UNDO.

  DISABLE TRIGGERS FOR LOAD OF edshord.
 
  IF NOT CAN-FIND(FIRST EDSHOrd WHERE EDSHOrd.rec_key EQ '') THEN RETURN.
  RUN msg ('Processing EDSHOrd ... ').
  FOR EACH EDSHOrd EXCLUSIVE-LOCK WHERE EDSHOrd.rec_key EQ '':
    ASSIGN
      EDSHOrd.rec_key = nextRecKey()
      i = i + 1.
    RUN createRecKey (EDSHOrd.rec_key,'EDSHOrd').
  END.
  RUN msg ('Fixed ' + STRING(i) + ' Records' + CHR(10)).
END PROCEDURE.
 
PROCEDURE fixEDSHPACK:
  DEFINE VARIABLE i AS INTEGER NO-UNDO.

  DISABLE TRIGGERS FOR LOAD OF edshpack.
 
  IF NOT CAN-FIND(FIRST EDSHPack WHERE EDSHPack.rec_key EQ '') THEN RETURN.
  RUN msg ('Processing EDSHPack ... ').
  FOR EACH EDSHPack EXCLUSIVE-LOCK WHERE EDSHPack.rec_key EQ '':
    ASSIGN
      EDSHPack.rec_key = nextRecKey()
      i = i + 1.
    RUN createRecKey (EDSHPack.rec_key,'EDSHPack').
  END.
  RUN msg ('Fixed ' + STRING(i) + ' Records' + CHR(10)).
END PROCEDURE.
 
PROCEDURE fixEDSHTARE:
  DEFINE VARIABLE i AS INTEGER NO-UNDO.

  DISABLE TRIGGERS FOR LOAD OF edshtare.
 
  IF NOT CAN-FIND(FIRST EDSHTare WHERE EDSHTare.rec_key EQ '') THEN RETURN.
  RUN msg ('Processing EDSHTare ... ').
  FOR EACH EDSHTare EXCLUSIVE-LOCK WHERE EDSHTare.rec_key EQ '':
    ASSIGN
      EDSHTare.rec_key = nextRecKey()
      i = i + 1.
    RUN createRecKey (EDSHTare.rec_key,'EDSHTare').
  END.
  RUN msg ('Fixed ' + STRING(i) + ' Records' + CHR(10)).
END PROCEDURE.
 
PROCEDURE fixEDSHTRAN:
  DEFINE VARIABLE i AS INTEGER NO-UNDO.

  DISABLE TRIGGERS FOR LOAD OF edshtran.
 
  IF NOT CAN-FIND(FIRST EDSHTran WHERE EDSHTran.rec_key EQ '') THEN RETURN.
  RUN msg ('Processing EDSHTran ... ').
  FOR EACH EDSHTran EXCLUSIVE-LOCK WHERE EDSHTran.rec_key EQ '':
    ASSIGN
      EDSHTran.rec_key = nextRecKey()
      i = i + 1.
    RUN createRecKey (EDSHTran.rec_key,'EDSHTran').
  END.
  RUN msg ('Fixed ' + STRING(i) + ' Records' + CHR(10)).
END PROCEDURE.
 
PROCEDURE fixEF:
  DEFINE VARIABLE i AS INTEGER NO-UNDO.

  DISABLE TRIGGERS FOR LOAD OF ef.
 
  IF NOT CAN-FIND(FIRST ef WHERE ef.rec_key EQ '') THEN RETURN.
  RUN msg ('Processing ef ... ').
  FOR EACH ef EXCLUSIVE-LOCK WHERE ef.rec_key EQ '':
    ASSIGN
      ef.rec_key = nextRecKey()
      i = i + 1.
    RUN createRecKey (ef.rec_key,'ef').
  END.
  RUN msg ('Fixed ' + STRING(i) + ' Records' + CHR(10)).
END PROCEDURE.
 
PROCEDURE fixEF-NSH:
  DEFINE VARIABLE i AS INTEGER NO-UNDO.

  DISABLE TRIGGERS FOR LOAD OF ef-nsh.
 
  IF NOT CAN-FIND(FIRST ef-nsh WHERE ef-nsh.rec_key EQ '') THEN RETURN.
  RUN msg ('Processing ef-nsh ... ').
  FOR EACH ef-nsh EXCLUSIVE-LOCK WHERE ef-nsh.rec_key EQ '':
    ASSIGN
      ef-nsh.rec_key = nextRecKey()
      i = i + 1.
    RUN createRecKey (ef-nsh.rec_key,'ef-nsh').
  END.
  RUN msg ('Fixed ' + STRING(i) + ' Records' + CHR(10)).
END PROCEDURE.
 
PROCEDURE fixEMAILCOD:
  DEFINE VARIABLE i AS INTEGER NO-UNDO.

  DISABLE TRIGGERS FOR LOAD OF emailcod.
 
  IF NOT CAN-FIND(FIRST emailcod WHERE emailcod.rec_key EQ '') THEN RETURN.
  RUN msg ('Processing emailcod ... ').
  FOR EACH emailcod EXCLUSIVE-LOCK WHERE emailcod.rec_key EQ '':
    ASSIGN
      emailcod.rec_key = nextRecKey()
      i = i + 1.
    RUN createRecKey (emailcod.rec_key,'emailcod').
  END.
  RUN msg ('Fixed ' + STRING(i) + ' Records' + CHR(10)).
END PROCEDURE.
 
PROCEDURE fixEMAILDTL:
  DEFINE VARIABLE i AS INTEGER NO-UNDO.

  DISABLE TRIGGERS FOR LOAD OF emaildtl.
 
  IF NOT CAN-FIND(FIRST emaildtl WHERE emaildtl.rec_key EQ '') THEN RETURN.
  RUN msg ('Processing emaildtl ... ').
  FOR EACH emaildtl EXCLUSIVE-LOCK WHERE emaildtl.rec_key EQ '':
    ASSIGN
      emaildtl.rec_key = nextRecKey()
      i = i + 1.
    RUN createRecKey (emaildtl.rec_key,'emaildtl').
  END.
  RUN msg ('Fixed ' + STRING(i) + ' Records' + CHR(10)).
END PROCEDURE.
 
PROCEDURE fixEMP:
  DEFINE VARIABLE i AS INTEGER NO-UNDO.

  DISABLE TRIGGERS FOR LOAD OF emp.
 
  IF NOT CAN-FIND(FIRST emp WHERE emp.rec_key EQ '') THEN RETURN.
  RUN msg ('Processing emp ... ').
  FOR EACH emp EXCLUSIVE-LOCK WHERE emp.rec_key EQ '':
    ASSIGN
      emp.rec_key = nextRecKey()
      i = i + 1.
    RUN createRecKey (emp.rec_key,'emp').
  END.
  RUN msg ('Fixed ' + STRING(i) + ' Records' + CHR(10)).
END PROCEDURE.
 
PROCEDURE fixEST:
  DEFINE VARIABLE i AS INTEGER NO-UNDO.

  DISABLE TRIGGERS FOR LOAD OF est.
 
  IF NOT CAN-FIND(FIRST est WHERE est.rec_key EQ '') THEN RETURN.
  RUN msg ('Processing est ... ').
  FOR EACH est EXCLUSIVE-LOCK WHERE est.rec_key EQ '':
    ASSIGN
      est.rec_key = nextRecKey()
      i = i + 1.
    RUN createRecKey (est.rec_key,'est').
  END.
  RUN msg ('Fixed ' + STRING(i) + ' Records' + CHR(10)).
END PROCEDURE.
 
PROCEDURE fixEST-FLM:
  DEFINE VARIABLE i AS INTEGER NO-UNDO.

  DISABLE TRIGGERS FOR LOAD OF est-flm.
 
  IF NOT CAN-FIND(FIRST est-flm WHERE est-flm.rec_key EQ '') THEN RETURN.
  RUN msg ('Processing est-flm ... ').
  FOR EACH est-flm EXCLUSIVE-LOCK WHERE est-flm.rec_key EQ '':
    ASSIGN
      est-flm.rec_key = nextRecKey()
      i = i + 1.
    RUN createRecKey (est-flm.rec_key,'est-flm').
  END.
  RUN msg ('Fixed ' + STRING(i) + ' Records' + CHR(10)).
END PROCEDURE.
 
PROCEDURE fixEST-INST:
  DEFINE VARIABLE i AS INTEGER NO-UNDO.

  DISABLE TRIGGERS FOR LOAD OF est-inst.
 
  IF NOT CAN-FIND(FIRST est-inst WHERE est-inst.rec_key EQ '') THEN RETURN.
  RUN msg ('Processing est-inst ... ').
  FOR EACH est-inst EXCLUSIVE-LOCK WHERE est-inst.rec_key EQ '':
    ASSIGN
      est-inst.rec_key = nextRecKey()
      i = i + 1.
    RUN createRecKey (est-inst.rec_key,'est-inst').
  END.
  RUN msg ('Fixed ' + STRING(i) + ' Records' + CHR(10)).
END PROCEDURE.
 
PROCEDURE fixEST-OP:
  DEFINE VARIABLE i AS INTEGER NO-UNDO.

  DISABLE TRIGGERS FOR LOAD OF est-op.
 
  IF NOT CAN-FIND(FIRST est-op WHERE est-op.rec_key EQ '') THEN RETURN.
  RUN msg ('Processing est-op ... ').
  FOR EACH est-op EXCLUSIVE-LOCK WHERE est-op.rec_key EQ '':
    ASSIGN
      est-op.rec_key = nextRecKey()
      i = i + 1.
    RUN createRecKey (est-op.rec_key,'est-op').
  END.
  RUN msg ('Fixed ' + STRING(i) + ' Records' + CHR(10)).
END PROCEDURE.
 
PROCEDURE fixEST-PREP:
  DEFINE VARIABLE i AS INTEGER NO-UNDO.

  DISABLE TRIGGERS FOR LOAD OF est-prep.
 
  IF NOT CAN-FIND(FIRST est-prep WHERE est-prep.rec_key EQ '') THEN RETURN.
  RUN msg ('Processing est-prep ... ').
  FOR EACH est-prep EXCLUSIVE-LOCK WHERE est-prep.rec_key EQ '':
    ASSIGN
      est-prep.rec_key = nextRecKey()
      i = i + 1.
    RUN createRecKey (est-prep.rec_key,'est-prep').
  END.
  RUN msg ('Fixed ' + STRING(i) + ' Records' + CHR(10)).
END PROCEDURE.
 
PROCEDURE fixEST-QTY:
  DEFINE VARIABLE i AS INTEGER NO-UNDO.

  DISABLE TRIGGERS FOR LOAD OF est-qty.
 
  IF NOT CAN-FIND(FIRST est-qty WHERE est-qty.rec_key EQ '') THEN RETURN.
  RUN msg ('Processing est-qty ... ').
  FOR EACH est-qty EXCLUSIVE-LOCK WHERE est-qty.rec_key EQ '':
    ASSIGN
      est-qty.rec_key = nextRecKey()
      i = i + 1.
    RUN createRecKey (est-qty.rec_key,'est-qty').
  END.
  RUN msg ('Fixed ' + STRING(i) + ' Records' + CHR(10)).
END PROCEDURE.
 
PROCEDURE fixEST-SUMM:
  DEFINE VARIABLE i AS INTEGER NO-UNDO.

  DISABLE TRIGGERS FOR LOAD OF est-summ.
 
  IF NOT CAN-FIND(FIRST est-summ WHERE est-summ.rec_key EQ '') THEN RETURN.
  RUN msg ('Processing est-summ ... ').
  FOR EACH est-summ EXCLUSIVE-LOCK WHERE est-summ.rec_key EQ '':
    ASSIGN
      est-summ.rec_key = nextRecKey()
      i = i + 1.
    RUN createRecKey (est-summ.rec_key,'est-summ').
  END.
  RUN msg ('Fixed ' + STRING(i) + ' Records' + CHR(10)).
END PROCEDURE.
 
PROCEDURE fixEXPIRATION:
  DEFINE VARIABLE i AS INTEGER NO-UNDO.

  DISABLE TRIGGERS FOR LOAD OF expiration.
 
  IF NOT CAN-FIND(FIRST expiration WHERE expiration.rec_key EQ '') THEN RETURN.
  RUN msg ('Processing expiration ... ').
  FOR EACH expiration EXCLUSIVE-LOCK WHERE expiration.rec_key EQ '':
    ASSIGN
      expiration.rec_key = nextRecKey()
      i = i + 1.
    RUN createRecKey (expiration.rec_key,'expiration').
  END.
  RUN msg ('Fixed ' + STRING(i) + ' Records' + CHR(10)).
END PROCEDURE.
 
PROCEDURE fixFG-ACT:
  DEFINE VARIABLE i AS INTEGER NO-UNDO.

  DISABLE TRIGGERS FOR LOAD OF fg-act.
 
  IF NOT CAN-FIND(FIRST fg-act WHERE fg-act.rec_key EQ '') THEN RETURN.
  RUN msg ('Processing fg-act ... ').
  FOR EACH fg-act EXCLUSIVE-LOCK WHERE fg-act.rec_key EQ '':
    ASSIGN
      fg-act.rec_key = nextRecKey()
      i = i + 1.
    RUN createRecKey (fg-act.rec_key,'fg-act').
  END.
  RUN msg ('Fixed ' + STRING(i) + ' Records' + CHR(10)).
END PROCEDURE.
 
PROCEDURE fixFG-BIN:
  DEFINE VARIABLE i AS INTEGER NO-UNDO.

  DISABLE TRIGGERS FOR LOAD OF fg-bin.
 
  IF NOT CAN-FIND(FIRST fg-bin WHERE fg-bin.rec_key EQ '') THEN RETURN.
  RUN msg ('Processing fg-bin ... ').
  FOR EACH fg-bin EXCLUSIVE-LOCK WHERE fg-bin.rec_key EQ '':
    ASSIGN
      fg-bin.rec_key = nextRecKey()
      i = i + 1.
    RUN createRecKey (fg-bin.rec_key,'fg-bin').
  END.
  RUN msg ('Fixed ' + STRING(i) + ' Records' + CHR(10)).
END PROCEDURE.
 
PROCEDURE fixFG-CTRL:
  DEFINE VARIABLE i AS INTEGER NO-UNDO.

  DISABLE TRIGGERS FOR LOAD OF fg-ctrl.
 
  IF NOT CAN-FIND(FIRST fg-ctrl WHERE fg-ctrl.rec_key EQ '') THEN RETURN.
  RUN msg ('Processing fg-ctrl ... ').
  FOR EACH fg-ctrl EXCLUSIVE-LOCK WHERE fg-ctrl.rec_key EQ '':
    ASSIGN
      fg-ctrl.rec_key = nextRecKey()
      i = i + 1.
    RUN createRecKey (fg-ctrl.rec_key,'fg-ctrl').
  END.
  RUN msg ('Fixed ' + STRING(i) + ' Records' + CHR(10)).
END PROCEDURE.
 
PROCEDURE fixFG-HIST:
  DEFINE VARIABLE i AS INTEGER NO-UNDO.

  DISABLE TRIGGERS FOR LOAD OF fg-hist.
 
  IF NOT CAN-FIND(FIRST fg-hist WHERE fg-hist.rec_key EQ '') THEN RETURN.
  RUN msg ('Processing fg-hist ... ').
  FOR EACH fg-hist EXCLUSIVE-LOCK WHERE fg-hist.rec_key EQ '':
    ASSIGN
      fg-hist.rec_key = nextRecKey()
      i = i + 1.
    RUN createRecKey (fg-hist.rec_key,'fg-hist').
  END.
  RUN msg ('Fixed ' + STRING(i) + ' Records' + CHR(10)).
END PROCEDURE.
 
PROCEDURE fixFG-RCPTH:
  DEFINE VARIABLE i AS INTEGER NO-UNDO.

  DISABLE TRIGGERS FOR LOAD OF fg-rcpth.
 
  IF NOT CAN-FIND(FIRST fg-rcpth WHERE fg-rcpth.rec_key EQ '') THEN RETURN.
  RUN msg ('Processing fg-rcpth ... ').
  FOR EACH fg-rcpth EXCLUSIVE-LOCK WHERE fg-rcpth.rec_key EQ '':
    ASSIGN
      fg-rcpth.rec_key = nextRecKey()
      i = i + 1.
    RUN createRecKey (fg-rcpth.rec_key,'fg-rcpth').
  END.
  RUN msg ('Fixed ' + STRING(i) + ' Records' + CHR(10)).
END PROCEDURE.
 
PROCEDURE fixFG-RCPTS:
  DEFINE VARIABLE i AS INTEGER NO-UNDO.

  DISABLE TRIGGERS FOR LOAD OF fg-rcpts.
 
  IF NOT CAN-FIND(FIRST fg-rcpts WHERE fg-rcpts.rec_key EQ '') THEN RETURN.
  RUN msg ('Processing fg-rcpts ... ').
  FOR EACH fg-rcpts EXCLUSIVE-LOCK WHERE fg-rcpts.rec_key EQ '':
    ASSIGN
      fg-rcpts.rec_key = nextRecKey()
      i = i + 1.
    RUN createRecKey (fg-rcpts.rec_key,'fg-rcpts').
  END.
  RUN msg ('Fixed ' + STRING(i) + ' Records' + CHR(10)).
END PROCEDURE.
 
PROCEDURE fixFG-RCTD:
  DEFINE VARIABLE i AS INTEGER NO-UNDO.

  DISABLE TRIGGERS FOR LOAD OF fg-rctd.
 
  IF NOT CAN-FIND(FIRST fg-rctd WHERE fg-rctd.rec_key EQ '') THEN RETURN.
  RUN msg ('Processing fg-rctd ... ').
  FOR EACH fg-rctd EXCLUSIVE-LOCK WHERE fg-rctd.rec_key EQ '':
    ASSIGN
      fg-rctd.rec_key = nextRecKey()
      i = i + 1.
    RUN createRecKey (fg-rctd.rec_key,'fg-rctd').
  END.
  RUN msg ('Fixed ' + STRING(i) + ' Records' + CHR(10)).
END PROCEDURE.
 
PROCEDURE fixFG-RDTL:
  DEFINE VARIABLE i AS INTEGER NO-UNDO.

  DISABLE TRIGGERS FOR LOAD OF fg-rdtl.
 
  IF NOT CAN-FIND(FIRST fg-rdtl WHERE fg-rdtl.rec_key EQ '') THEN RETURN.
  RUN msg ('Processing fg-rdtl ... ').
  FOR EACH fg-rdtl EXCLUSIVE-LOCK WHERE fg-rdtl.rec_key EQ '':
    ASSIGN
      fg-rdtl.rec_key = nextRecKey()
      i = i + 1.
    RUN createRecKey (fg-rdtl.rec_key,'fg-rdtl').
  END.
  RUN msg ('Fixed ' + STRING(i) + ' Records' + CHR(10)).
END PROCEDURE.
 
PROCEDURE fixFG-RDTLH:
  DEFINE VARIABLE i AS INTEGER NO-UNDO.

  DISABLE TRIGGERS FOR LOAD OF fg-rdtlh.
 
  IF NOT CAN-FIND(FIRST fg-rdtlh WHERE fg-rdtlh.rec_key EQ '') THEN RETURN.
  RUN msg ('Processing fg-rdtlh ... ').
  FOR EACH fg-rdtlh EXCLUSIVE-LOCK WHERE fg-rdtlh.rec_key EQ '':
    ASSIGN
      fg-rdtlh.rec_key = nextRecKey()
      i = i + 1.
    RUN createRecKey (fg-rdtlh.rec_key,'fg-rdtlh').
  END.
  RUN msg ('Fixed ' + STRING(i) + ' Records' + CHR(10)).
END PROCEDURE.
 
PROCEDURE fixFG-SET:
  DEFINE VARIABLE i AS INTEGER NO-UNDO.

  DISABLE TRIGGERS FOR LOAD OF fg-set.
 
  IF NOT CAN-FIND(FIRST fg-set WHERE fg-set.rec_key EQ '') THEN RETURN.
  RUN msg ('Processing fg-set ... ').
  FOR EACH fg-set EXCLUSIVE-LOCK WHERE fg-set.rec_key EQ '':
    ASSIGN
      fg-set.rec_key = nextRecKey()
      i = i + 1.
    RUN createRecKey (fg-set.rec_key,'fg-set').
  END.
  RUN msg ('Fixed ' + STRING(i) + ' Records' + CHR(10)).
END PROCEDURE.
 
PROCEDURE fixFGCAT:
  DEFINE VARIABLE i AS INTEGER NO-UNDO.

  DISABLE TRIGGERS FOR LOAD OF fgcat.
 
  IF NOT CAN-FIND(FIRST fgcat WHERE fgcat.rec_key EQ '') THEN RETURN.
  RUN msg ('Processing fgcat ... ').
  FOR EACH fgcat EXCLUSIVE-LOCK WHERE fgcat.rec_key EQ '':
    ASSIGN
      fgcat.rec_key = nextRecKey()
      i = i + 1.
    RUN createRecKey (fgcat.rec_key,'fgcat').
  END.
  RUN msg ('Fixed ' + STRING(i) + ' Records' + CHR(10)).
END PROCEDURE.
 
PROCEDURE fixFILE:
  DEFINE VARIABLE i AS INTEGER NO-UNDO.

  DISABLE TRIGGERS FOR LOAD OF file.
 
  IF NOT CAN-FIND(FIRST file WHERE file.rec_key EQ '') THEN RETURN.
  RUN msg ('Processing file ... ').
  FOR EACH file EXCLUSIVE-LOCK WHERE file.rec_key EQ '':
    ASSIGN
      file.rec_key = nextRecKey()
      i = i + 1.
    RUN createRecKey (file.rec_key,'file').
  END.
  RUN msg ('Fixed ' + STRING(i) + ' Records' + CHR(10)).
END PROCEDURE.
 
PROCEDURE fixFLUTE:
  DEFINE VARIABLE i AS INTEGER NO-UNDO.

  DISABLE TRIGGERS FOR LOAD OF flute.
 
  IF NOT CAN-FIND(FIRST flute WHERE flute.rec_key EQ '') THEN RETURN.
  RUN msg ('Processing flute ... ').
  FOR EACH flute EXCLUSIVE-LOCK WHERE flute.rec_key EQ '':
    ASSIGN
      flute.rec_key = nextRecKey()
      i = i + 1.
    RUN createRecKey (flute.rec_key,'flute').
  END.
  RUN msg ('Fixed ' + STRING(i) + ' Records' + CHR(10)).
END PROCEDURE.
 
PROCEDURE fixFRACTION:
  DEFINE VARIABLE i AS INTEGER NO-UNDO.

  DISABLE TRIGGERS FOR LOAD OF fraction.
 
  IF NOT CAN-FIND(FIRST fraction WHERE fraction.rec_key EQ '') THEN RETURN.
  RUN msg ('Processing fraction ... ').
  FOR EACH fraction EXCLUSIVE-LOCK WHERE fraction.rec_key EQ '':
    ASSIGN
      fraction.rec_key = nextRecKey()
      i = i + 1.
    RUN createRecKey (fraction.rec_key,'fraction').
  END.
  RUN msg ('Fixed ' + STRING(i) + ' Records' + CHR(10)).
END PROCEDURE.
 
PROCEDURE fixGL-CTRL:
  DEFINE VARIABLE i AS INTEGER NO-UNDO.

  DISABLE TRIGGERS FOR LOAD OF gl-ctrl.
 
  IF NOT CAN-FIND(FIRST gl-ctrl WHERE gl-ctrl.rec_key EQ '') THEN RETURN.
  RUN msg ('Processing gl-ctrl ... ').
  FOR EACH gl-ctrl EXCLUSIVE-LOCK WHERE gl-ctrl.rec_key EQ '':
    ASSIGN
      gl-ctrl.rec_key = nextRecKey()
      i = i + 1.
    RUN createRecKey (gl-ctrl.rec_key,'gl-ctrl').
  END.
  RUN msg ('Fixed ' + STRING(i) + ' Records' + CHR(10)).
END PROCEDURE.
 
PROCEDURE fixGL-FREQ:
  DEFINE VARIABLE i AS INTEGER NO-UNDO.

  DISABLE TRIGGERS FOR LOAD OF gl-freq.
 
  IF NOT CAN-FIND(FIRST gl-freq WHERE gl-freq.rec_key EQ '') THEN RETURN.
  RUN msg ('Processing gl-freq ... ').
  FOR EACH gl-freq EXCLUSIVE-LOCK WHERE gl-freq.rec_key EQ '':
    ASSIGN
      gl-freq.rec_key = nextRecKey()
      i = i + 1.
    RUN createRecKey (gl-freq.rec_key,'gl-freq').
  END.
  RUN msg ('Fixed ' + STRING(i) + ' Records' + CHR(10)).
END PROCEDURE.
 
PROCEDURE fixGL-JRN:
  DEFINE VARIABLE i AS INTEGER NO-UNDO.

  DISABLE TRIGGERS FOR LOAD OF gl-jrn.
 
  IF NOT CAN-FIND(FIRST gl-jrn WHERE gl-jrn.rec_key EQ '') THEN RETURN.
  RUN msg ('Processing gl-jrn ... ').
  FOR EACH gl-jrn EXCLUSIVE-LOCK WHERE gl-jrn.rec_key EQ '':
    ASSIGN
      gl-jrn.rec_key = nextRecKey()
      i = i + 1.
    RUN createRecKey (gl-jrn.rec_key,'gl-jrn').
  END.
  RUN msg ('Fixed ' + STRING(i) + ' Records' + CHR(10)).
END PROCEDURE.
 
PROCEDURE fixGL-JRNL:
  DEFINE VARIABLE i AS INTEGER NO-UNDO.

  DISABLE TRIGGERS FOR LOAD OF gl-jrnl.
 
  IF NOT CAN-FIND(FIRST gl-jrnl WHERE gl-jrnl.rec_key EQ '') THEN RETURN.
  RUN msg ('Processing gl-jrnl ... ').
  FOR EACH gl-jrnl EXCLUSIVE-LOCK WHERE gl-jrnl.rec_key EQ '':
    ASSIGN
      gl-jrnl.rec_key = nextRecKey()
      i = i + 1.
    RUN createRecKey (gl-jrnl.rec_key,'gl-jrnl').
  END.
  RUN msg ('Fixed ' + STRING(i) + ' Records' + CHR(10)).
END PROCEDURE.
 
PROCEDURE fixGL-RPT:
  DEFINE VARIABLE i AS INTEGER NO-UNDO.

  DISABLE TRIGGERS FOR LOAD OF gl-rpt.
 
  IF NOT CAN-FIND(FIRST gl-rpt WHERE gl-rpt.rec_key EQ '') THEN RETURN.
  RUN msg ('Processing gl-rpt ... ').
  FOR EACH gl-rpt EXCLUSIVE-LOCK WHERE gl-rpt.rec_key EQ '':
    ASSIGN
      gl-rpt.rec_key = nextRecKey()
      i = i + 1.
    RUN createRecKey (gl-rpt.rec_key,'gl-rpt').
  END.
  RUN msg ('Fixed ' + STRING(i) + ' Records' + CHR(10)).
END PROCEDURE.
 
PROCEDURE fixGL-RPTD:
  DEFINE VARIABLE i AS INTEGER NO-UNDO.

  DISABLE TRIGGERS FOR LOAD OF gl-rptd.
 
  IF NOT CAN-FIND(FIRST gl-rptd WHERE gl-rptd.rec_key EQ '') THEN RETURN.
  RUN msg ('Processing gl-rptd ... ').
  FOR EACH gl-rptd EXCLUSIVE-LOCK WHERE gl-rptd.rec_key EQ '':
    ASSIGN
      gl-rptd.rec_key = nextRecKey()
      i = i + 1.
    RUN createRecKey (gl-rptd.rec_key,'gl-rptd').
  END.
  RUN msg ('Fixed ' + STRING(i) + ' Records' + CHR(10)).
END PROCEDURE.
 
PROCEDURE fixGLHIST:
  DEFINE VARIABLE i AS INTEGER NO-UNDO.

  DISABLE TRIGGERS FOR LOAD OF glhist.
 
  IF NOT CAN-FIND(FIRST glhist WHERE glhist.rec_key EQ '') THEN RETURN.
  RUN msg ('Processing glhist ... ').
  FOR EACH glhist EXCLUSIVE-LOCK WHERE glhist.rec_key EQ '':
    ASSIGN
      glhist.rec_key = nextRecKey()
      i = i + 1.
    RUN createRecKey (glhist.rec_key,'glhist').
  END.
  RUN msg ('Fixed ' + STRING(i) + ' Records' + CHR(10)).
END PROCEDURE.
 
PROCEDURE fixGLTRANS:
  DEFINE VARIABLE i AS INTEGER NO-UNDO.

  DISABLE TRIGGERS FOR LOAD OF gltrans.
 
  IF NOT CAN-FIND(FIRST gltrans WHERE gltrans.rec_key EQ '') THEN RETURN.
  RUN msg ('Processing gltrans ... ').
  FOR EACH gltrans EXCLUSIVE-LOCK WHERE gltrans.rec_key EQ '':
    ASSIGN
      gltrans.rec_key = nextRecKey()
      i = i + 1.
    RUN createRecKey (gltrans.rec_key,'gltrans').
  END.
  RUN msg ('Fixed ' + STRING(i) + ' Records' + CHR(10)).
END PROCEDURE.
 
PROCEDURE fixINV-HEAD:
  DEFINE VARIABLE i AS INTEGER NO-UNDO.

  DISABLE TRIGGERS FOR LOAD OF inv-head.
 
  IF NOT CAN-FIND(FIRST inv-head WHERE inv-head.rec_key EQ '') THEN RETURN.
  RUN msg ('Processing inv-head ... ').
  FOR EACH inv-head EXCLUSIVE-LOCK WHERE inv-head.rec_key EQ '':
    ASSIGN
      inv-head.rec_key = nextRecKey()
      i = i + 1.
    RUN createRecKey (inv-head.rec_key,'inv-head').
  END.
  RUN msg ('Fixed ' + STRING(i) + ' Records' + CHR(10)).
END PROCEDURE.
 
PROCEDURE fixINV-LINE:
  DEFINE VARIABLE i AS INTEGER NO-UNDO.

  DISABLE TRIGGERS FOR LOAD OF inv-line.
 
  IF NOT CAN-FIND(FIRST inv-line WHERE inv-line.rec_key EQ '') THEN RETURN.
  RUN msg ('Processing inv-line ... ').
  FOR EACH inv-line EXCLUSIVE-LOCK WHERE inv-line.rec_key EQ '':
    ASSIGN
      inv-line.rec_key = nextRecKey()
      i = i + 1.
    RUN createRecKey (inv-line.rec_key,'inv-line').
  END.
  RUN msg ('Fixed ' + STRING(i) + ' Records' + CHR(10)).
END PROCEDURE.
 
PROCEDURE fixINV-MISC:
  DEFINE VARIABLE i AS INTEGER NO-UNDO.

  DISABLE TRIGGERS FOR LOAD OF inv-misc.
 
  IF NOT CAN-FIND(FIRST inv-misc WHERE inv-misc.rec_key EQ '') THEN RETURN.
  RUN msg ('Processing inv-misc ... ').
  FOR EACH inv-misc EXCLUSIVE-LOCK WHERE inv-misc.rec_key EQ '':
    ASSIGN
      inv-misc.rec_key = nextRecKey()
      i = i + 1.
    RUN createRecKey (inv-misc.rec_key,'inv-misc').
  END.
  RUN msg ('Fixed ' + STRING(i) + ' Records' + CHR(10)).
END PROCEDURE.
 
PROCEDURE fixITEM:
  DEFINE VARIABLE i AS INTEGER NO-UNDO.

  DISABLE TRIGGERS FOR LOAD OF item.
 
  IF NOT CAN-FIND(FIRST item WHERE item.rec_key EQ '') THEN RETURN.
  RUN msg ('Processing item ... ').
  FOR EACH item EXCLUSIVE-LOCK WHERE item.rec_key EQ '':
    ASSIGN
      item.rec_key = nextRecKey()
      i = i + 1.
    RUN createRecKey (item.rec_key,'item').
  END.
  RUN msg ('Fixed ' + STRING(i) + ' Records' + CHR(10)).
END PROCEDURE.
 
PROCEDURE fixITEM-BOM:
  DEFINE VARIABLE i AS INTEGER NO-UNDO.

  DISABLE TRIGGERS FOR LOAD OF item-bom.
 
  IF NOT CAN-FIND(FIRST item-bom WHERE item-bom.rec_key EQ '') THEN RETURN.
  RUN msg ('Processing item-bom ... ').
  FOR EACH item-bom EXCLUSIVE-LOCK WHERE item-bom.rec_key EQ '':
    ASSIGN
      item-bom.rec_key = nextRecKey()
      i = i + 1.
    RUN createRecKey (item-bom.rec_key,'item-bom').
  END.
  RUN msg ('Fixed ' + STRING(i) + ' Records' + CHR(10)).
END PROCEDURE.
 
PROCEDURE fixITEM-SPEC:
  DEFINE VARIABLE i AS INTEGER NO-UNDO.

  DISABLE TRIGGERS FOR LOAD OF item-spec.
 
  IF NOT CAN-FIND(FIRST item-spec WHERE item-spec.rec_key EQ '') THEN RETURN.
  RUN msg ('Processing item-spec ... ').
  FOR EACH item-spec EXCLUSIVE-LOCK WHERE item-spec.rec_key EQ '':
    ASSIGN
      item-spec.rec_key = nextRecKey()
      i = i + 1.
    RUN createRecKey (item-spec.rec_key,'item-spec').
  END.
  RUN msg ('Fixed ' + STRING(i) + ' Records' + CHR(10)).
END PROCEDURE.
 
PROCEDURE fixITEMFG:
  DEFINE VARIABLE i AS INTEGER NO-UNDO.

  DISABLE TRIGGERS FOR LOAD OF itemfg.
 
  IF NOT CAN-FIND(FIRST itemfg WHERE itemfg.rec_key EQ '') THEN RETURN.
  RUN msg ('Processing itemfg ... ').
  FOR EACH itemfg EXCLUSIVE-LOCK WHERE itemfg.rec_key EQ '':
    ASSIGN
      itemfg.rec_key = nextRecKey()
      i = i + 1.
    RUN createRecKey (itemfg.rec_key,'itemfg').
  END.
  RUN msg ('Fixed ' + STRING(i) + ' Records' + CHR(10)).
END PROCEDURE.
 
PROCEDURE fixITEMFG-BOM:
  DEFINE VARIABLE i AS INTEGER NO-UNDO.

  DISABLE TRIGGERS FOR LOAD OF itemfg-bom.
 
  IF NOT CAN-FIND(FIRST itemfg-bom WHERE itemfg-bom.rec_key EQ '') THEN RETURN.
  RUN msg ('Processing itemfg-bom ... ').
  FOR EACH itemfg-bom EXCLUSIVE-LOCK WHERE itemfg-bom.rec_key EQ '':
    ASSIGN
      itemfg-bom.rec_key = nextRecKey()
      i = i + 1.
    RUN createRecKey (itemfg-bom.rec_key,'itemfg-bom').
  END.
  RUN msg ('Fixed ' + STRING(i) + ' Records' + CHR(10)).
END PROCEDURE.
 
PROCEDURE fixITEMFG-INK:
  DEFINE VARIABLE i AS INTEGER NO-UNDO.

  DISABLE TRIGGERS FOR LOAD OF itemfg-ink.
 
  IF NOT CAN-FIND(FIRST itemfg-ink WHERE itemfg-ink.rec_key EQ '') THEN RETURN.
  RUN msg ('Processing itemfg-ink ... ').
  FOR EACH itemfg-ink EXCLUSIVE-LOCK WHERE itemfg-ink.rec_key EQ '':
    ASSIGN
      itemfg-ink.rec_key = nextRecKey()
      i = i + 1.
    RUN createRecKey (itemfg-ink.rec_key,'itemfg-ink').
  END.
  RUN msg ('Fixed ' + STRING(i) + ' Records' + CHR(10)).
END PROCEDURE.
 
PROCEDURE fixITEMFG-LOC:
  DEFINE VARIABLE i AS INTEGER NO-UNDO.

  DISABLE TRIGGERS FOR LOAD OF itemfg-loc.
 
  IF NOT CAN-FIND(FIRST itemfg-loc WHERE itemfg-loc.rec_key EQ '') THEN RETURN.
  RUN msg ('Processing itemfg-loc ... ').
  FOR EACH itemfg-loc EXCLUSIVE-LOCK WHERE itemfg-loc.rec_key EQ '':
    ASSIGN
      itemfg-loc.rec_key = nextRecKey()
      i = i + 1.
    RUN createRecKey (itemfg-loc.rec_key,'itemfg-loc').
  END.
  RUN msg ('Fixed ' + STRING(i) + ' Records' + CHR(10)).
END PROCEDURE.
 
PROCEDURE fixITEMFGDTL:
  DEFINE VARIABLE i AS INTEGER NO-UNDO.

  DISABLE TRIGGERS FOR LOAD OF itemfgdtl.
 
  IF NOT CAN-FIND(FIRST itemfgdtl WHERE itemfgdtl.rec_key EQ '') THEN RETURN.
  RUN msg ('Processing itemfgdtl ... ').
  FOR EACH itemfgdtl EXCLUSIVE-LOCK WHERE itemfgdtl.rec_key EQ '':
    ASSIGN
      itemfgdtl.rec_key = nextRecKey()
      i = i + 1.
    RUN createRecKey (itemfgdtl.rec_key,'itemfgdtl').
  END.
  RUN msg ('Fixed ' + STRING(i) + ' Records' + CHR(10)).
END PROCEDURE.
 
PROCEDURE fixJC-CTRL:
  DEFINE VARIABLE i AS INTEGER NO-UNDO.

  DISABLE TRIGGERS FOR LOAD OF jc-ctrl.
 
  IF NOT CAN-FIND(FIRST jc-ctrl WHERE jc-ctrl.rec_key EQ '') THEN RETURN.
  RUN msg ('Processing jc-ctrl ... ').
  FOR EACH jc-ctrl EXCLUSIVE-LOCK WHERE jc-ctrl.rec_key EQ '':
    ASSIGN
      jc-ctrl.rec_key = nextRecKey()
      i = i + 1.
    RUN createRecKey (jc-ctrl.rec_key,'jc-ctrl').
  END.
  RUN msg ('Fixed ' + STRING(i) + ' Records' + CHR(10)).
END PROCEDURE.
 
PROCEDURE fixJOB:
  DEFINE VARIABLE i AS INTEGER NO-UNDO.

  DISABLE TRIGGERS FOR LOAD OF job.
 
  IF NOT CAN-FIND(FIRST job WHERE job.rec_key EQ '') THEN RETURN.
  RUN msg ('Processing job ... ').
  FOR EACH job EXCLUSIVE-LOCK WHERE job.rec_key EQ '':
    ASSIGN
      job.rec_key = nextRecKey()
      i = i + 1.
    RUN createRecKey (job.rec_key,'job').
  END.
  RUN msg ('Fixed ' + STRING(i) + ' Records' + CHR(10)).
END PROCEDURE.
 
PROCEDURE fixJOB-ALL:
  DEFINE VARIABLE i AS INTEGER NO-UNDO.

  DISABLE TRIGGERS FOR LOAD OF job-all.
 
  IF NOT CAN-FIND(FIRST job-all WHERE job-all.rec_key EQ '') THEN RETURN.
  RUN msg ('Processing job-all ... ').
  FOR EACH job-all EXCLUSIVE-LOCK WHERE job-all.rec_key EQ '':
    ASSIGN
      job-all.rec_key = nextRecKey()
      i = i + 1.
    RUN createRecKey (job-all.rec_key,'job-all').
  END.
  RUN msg ('Fixed ' + STRING(i) + ' Records' + CHR(10)).
END PROCEDURE.
 
PROCEDURE fixJOB-BRD:
  DEFINE VARIABLE i AS INTEGER NO-UNDO.

  DISABLE TRIGGERS FOR LOAD OF job-brd.
 
  IF NOT CAN-FIND(FIRST job-brd WHERE job-brd.rec_key EQ '') THEN RETURN.
  RUN msg ('Processing job-brd ... ').
  FOR EACH job-brd EXCLUSIVE-LOCK WHERE job-brd.rec_key EQ '':
    ASSIGN
      job-brd.rec_key = nextRecKey()
      i = i + 1.
    RUN createRecKey (job-brd.rec_key,'job-brd').
  END.
  RUN msg ('Fixed ' + STRING(i) + ' Records' + CHR(10)).
END PROCEDURE.
 
PROCEDURE fixJOB-CAT:
  DEFINE VARIABLE i AS INTEGER NO-UNDO.

  DISABLE TRIGGERS FOR LOAD OF job-cat.
 
  IF NOT CAN-FIND(FIRST job-cat WHERE job-cat.rec_key EQ '') THEN RETURN.
  RUN msg ('Processing job-cat ... ').
  FOR EACH job-cat EXCLUSIVE-LOCK WHERE job-cat.rec_key EQ '':
    ASSIGN
      job-cat.rec_key = nextRecKey()
      i = i + 1.
    RUN createRecKey (job-cat.rec_key,'job-cat').
  END.
  RUN msg ('Fixed ' + STRING(i) + ' Records' + CHR(10)).
END PROCEDURE.
 
PROCEDURE fixJOB-CODE:
  DEFINE VARIABLE i AS INTEGER NO-UNDO.

  DISABLE TRIGGERS FOR LOAD OF job-code.
 
  IF NOT CAN-FIND(FIRST job-code WHERE job-code.rec_key EQ '') THEN RETURN.
  RUN msg ('Processing job-code ... ').
  FOR EACH job-code EXCLUSIVE-LOCK WHERE job-code.rec_key EQ '':
    ASSIGN
      job-code.rec_key = nextRecKey()
      i = i + 1.
    RUN createRecKey (job-code.rec_key,'job-code').
  END.
  RUN msg ('Fixed ' + STRING(i) + ' Records' + CHR(10)).
END PROCEDURE.
 
PROCEDURE fixJOB-HDR:
  DEFINE VARIABLE i AS INTEGER NO-UNDO.

  DISABLE TRIGGERS FOR LOAD OF job-hdr.
 
  IF NOT CAN-FIND(FIRST job-hdr WHERE job-hdr.rec_key EQ '') THEN RETURN.
  RUN msg ('Processing job-hdr ... ').
  FOR EACH job-hdr EXCLUSIVE-LOCK WHERE job-hdr.rec_key EQ '':
    ASSIGN
      job-hdr.rec_key = nextRecKey()
      i = i + 1.
    RUN createRecKey (job-hdr.rec_key,'job-hdr').
  END.
  RUN msg ('Fixed ' + STRING(i) + ' Records' + CHR(10)).
END PROCEDURE.
 
PROCEDURE fixJOB-MAT:
  DEFINE VARIABLE i AS INTEGER NO-UNDO.

  DISABLE TRIGGERS FOR LOAD OF job-mat.
 
  IF NOT CAN-FIND(FIRST job-mat WHERE job-mat.rec_key EQ '') THEN RETURN.
  RUN msg ('Processing job-mat ... ').
  FOR EACH job-mat EXCLUSIVE-LOCK WHERE job-mat.rec_key EQ '':
    ASSIGN
      job-mat.rec_key = nextRecKey()
      i = i + 1.
    RUN createRecKey (job-mat.rec_key,'job-mat').
  END.
  RUN msg ('Fixed ' + STRING(i) + ' Records' + CHR(10)).
END PROCEDURE.
 
PROCEDURE fixJOB-MCH:
  DEFINE VARIABLE i AS INTEGER NO-UNDO.

  DISABLE TRIGGERS FOR LOAD OF job-mch.
 
  IF NOT CAN-FIND(FIRST job-mch WHERE job-mch.rec_key EQ '') THEN RETURN.
  RUN msg ('Processing job-mch ... ').
  FOR EACH job-mch EXCLUSIVE-LOCK WHERE job-mch.rec_key EQ '':
    ASSIGN
      job-mch.rec_key = nextRecKey()
      i = i + 1.
    RUN createRecKey (job-mch.rec_key,'job-mch').
  END.
  RUN msg ('Fixed ' + STRING(i) + ' Records' + CHR(10)).
END PROCEDURE.
 
PROCEDURE fixJOB-PREP:
  DEFINE VARIABLE i AS INTEGER NO-UNDO.

  DISABLE TRIGGERS FOR LOAD OF job-prep.
 
  IF NOT CAN-FIND(FIRST job-prep WHERE job-prep.rec_key EQ '') THEN RETURN.
  RUN msg ('Processing job-prep ... ').
  FOR EACH job-prep EXCLUSIVE-LOCK WHERE job-prep.rec_key EQ '':
    ASSIGN
      job-prep.rec_key = nextRecKey()
      i = i + 1.
    RUN createRecKey (job-prep.rec_key,'job-prep').
  END.
  RUN msg ('Fixed ' + STRING(i) + ' Records' + CHR(10)).
END PROCEDURE.
 
PROCEDURE fixJOB-SCH:
  DEFINE VARIABLE i AS INTEGER NO-UNDO.

  DISABLE TRIGGERS FOR LOAD OF job-sch.
 
  IF NOT CAN-FIND(FIRST job-sch WHERE job-sch.rec_key EQ '') THEN RETURN.
  RUN msg ('Processing job-sch ... ').
  FOR EACH job-sch EXCLUSIVE-LOCK WHERE job-sch.rec_key EQ '':
    ASSIGN
      job-sch.rec_key = nextRecKey()
      i = i + 1.
    RUN createRecKey (job-sch.rec_key,'job-sch').
  END.
  RUN msg ('Fixed ' + STRING(i) + ' Records' + CHR(10)).
END PROCEDURE.
 
PROCEDURE fixLOADTAG:
  DEFINE VARIABLE i AS INTEGER NO-UNDO.

  DISABLE TRIGGERS FOR LOAD OF loadtag.
 
  IF NOT CAN-FIND(FIRST loadtag WHERE loadtag.rec_key EQ '') THEN RETURN.
  RUN msg ('Processing loadtag ... ').
  FOR EACH loadtag EXCLUSIVE-LOCK WHERE loadtag.rec_key EQ '':
    ASSIGN
      loadtag.rec_key = nextRecKey()
      i = i + 1.
    RUN createRecKey (loadtag.rec_key,'loadtag').
  END.
  RUN msg ('Fixed ' + STRING(i) + ' Records' + CHR(10)).
END PROCEDURE.
 
PROCEDURE fixLOC:
  DEFINE VARIABLE i AS INTEGER NO-UNDO.

  DISABLE TRIGGERS FOR LOAD OF loc.
 
  IF NOT CAN-FIND(FIRST loc WHERE loc.rec_key EQ '') THEN RETURN.
  RUN msg ('Processing loc ... ').
  FOR EACH loc EXCLUSIVE-LOCK WHERE loc.rec_key EQ '':
    ASSIGN
      loc.rec_key = nextRecKey()
      i = i + 1.
    RUN createRecKey (loc.rec_key,'loc').
  END.
  RUN msg ('Fixed ' + STRING(i) + ' Records' + CHR(10)).
END PROCEDURE.
 
PROCEDURE fixMACH:
  DEFINE VARIABLE i AS INTEGER NO-UNDO.

  DISABLE TRIGGERS FOR LOAD OF mach.
 
  IF NOT CAN-FIND(FIRST mach WHERE mach.rec_key EQ '') THEN RETURN.
  RUN msg ('Processing mach ... ').
  FOR EACH mach EXCLUSIVE-LOCK WHERE mach.rec_key EQ '':
    ASSIGN
      mach.rec_key = nextRecKey()
      i = i + 1.
    RUN createRecKey (mach.rec_key,'mach').
  END.
  RUN msg ('Fixed ' + STRING(i) + ' Records' + CHR(10)).
END PROCEDURE.
 
PROCEDURE fixMACH-ADDER:
  DEFINE VARIABLE i AS INTEGER NO-UNDO.

  DISABLE TRIGGERS FOR LOAD OF mach-adder.
 
  IF NOT CAN-FIND(FIRST mach-adder WHERE mach-adder.rec_key EQ '') THEN RETURN.
  RUN msg ('Processing mach-adder ... ').
  FOR EACH mach-adder EXCLUSIVE-LOCK WHERE mach-adder.rec_key EQ '':
    ASSIGN
      mach-adder.rec_key = nextRecKey()
      i = i + 1.
    RUN createRecKey (mach-adder.rec_key,'mach-adder').
  END.
  RUN msg ('Fixed ' + STRING(i) + ' Records' + CHR(10)).
END PROCEDURE.
 
PROCEDURE fixMACH-CALENDAR:
  DEFINE VARIABLE i AS INTEGER NO-UNDO.

  DISABLE TRIGGERS FOR LOAD OF mach-calendar.
 
  IF NOT CAN-FIND(FIRST mach-calendar WHERE mach-calendar.rec_key EQ '') THEN RETURN.
  RUN msg ('Processing mach-calendar ... ').
  FOR EACH mach-calendar EXCLUSIVE-LOCK WHERE mach-calendar.rec_key EQ '':
    ASSIGN
      mach-calendar.rec_key = nextRecKey()
      i = i + 1.
    RUN createRecKey (mach-calendar.rec_key,'mach-calendar').
  END.
  RUN msg ('Fixed ' + STRING(i) + ' Records' + CHR(10)).
END PROCEDURE.
 
PROCEDURE fixMAT:
  DEFINE VARIABLE i AS INTEGER NO-UNDO.

  DISABLE TRIGGERS FOR LOAD OF mat.
 
  IF NOT CAN-FIND(FIRST mat WHERE mat.rec_key EQ '') THEN RETURN.
  RUN msg ('Processing mat ... ').
  FOR EACH mat EXCLUSIVE-LOCK WHERE mat.rec_key EQ '':
    ASSIGN
      mat.rec_key = nextRecKey()
      i = i + 1.
    RUN createRecKey (mat.rec_key,'mat').
  END.
  RUN msg ('Fixed ' + STRING(i) + ' Records' + CHR(10)).
END PROCEDURE.
 
PROCEDURE fixMAT-ACT:
  DEFINE VARIABLE i AS INTEGER NO-UNDO.

  DISABLE TRIGGERS FOR LOAD OF mat-act.
 
  IF NOT CAN-FIND(FIRST mat-act WHERE mat-act.rec_key EQ '') THEN RETURN.
  RUN msg ('Processing mat-act ... ').
  FOR EACH mat-act EXCLUSIVE-LOCK WHERE mat-act.rec_key EQ '':
    ASSIGN
      mat-act.rec_key = nextRecKey()
      i = i + 1.
    RUN createRecKey (mat-act.rec_key,'mat-act').
  END.
  RUN msg ('Fixed ' + STRING(i) + ' Records' + CHR(10)).
END PROCEDURE.
 
PROCEDURE fixMATPREP:
  DEFINE VARIABLE i AS INTEGER NO-UNDO.

  DISABLE TRIGGERS FOR LOAD OF matprep.
 
  IF NOT CAN-FIND(FIRST matprep WHERE matprep.rec_key EQ '') THEN RETURN.
  RUN msg ('Processing matprep ... ').
  FOR EACH matprep EXCLUSIVE-LOCK WHERE matprep.rec_key EQ '':
    ASSIGN
      matprep.rec_key = nextRecKey()
      i = i + 1.
    RUN createRecKey (matprep.rec_key,'matprep').
  END.
  RUN msg ('Fixed ' + STRING(i) + ' Records' + CHR(10)).
END PROCEDURE.
 
PROCEDURE fixMCH-ACT:
  DEFINE VARIABLE i AS INTEGER NO-UNDO.

  DISABLE TRIGGERS FOR LOAD OF mch-act.
 
  IF NOT CAN-FIND(FIRST mch-act WHERE mch-act.rec_key EQ '') THEN RETURN.
  RUN msg ('Processing mch-act ... ').
  FOR EACH mch-act EXCLUSIVE-LOCK WHERE mch-act.rec_key EQ '':
    ASSIGN
      mch-act.rec_key = nextRecKey()
      i = i + 1.
    RUN createRecKey (mch-act.rec_key,'mch-act').
  END.
  RUN msg ('Fixed ' + STRING(i) + ' Records' + CHR(10)).
END PROCEDURE.
 
PROCEDURE fixMCH-SRT:
  DEFINE VARIABLE i AS INTEGER NO-UNDO.

  DISABLE TRIGGERS FOR LOAD OF mch-srt.
 
  IF NOT CAN-FIND(FIRST mch-srt WHERE mch-srt.rec_key EQ '') THEN RETURN.
  RUN msg ('Processing mch-srt ... ').
  FOR EACH mch-srt EXCLUSIVE-LOCK WHERE mch-srt.rec_key EQ '':
    ASSIGN
      mch-srt.rec_key = nextRecKey()
      i = i + 1.
    RUN createRecKey (mch-srt.rec_key,'mch-srt').
  END.
  RUN msg ('Fixed ' + STRING(i) + ' Records' + CHR(10)).
END PROCEDURE.
 
PROCEDURE fixMISC-ACT:
  DEFINE VARIABLE i AS INTEGER NO-UNDO.

  DISABLE TRIGGERS FOR LOAD OF misc-act.
 
  IF NOT CAN-FIND(FIRST misc-act WHERE misc-act.rec_key EQ '') THEN RETURN.
  RUN msg ('Processing misc-act ... ').
  FOR EACH misc-act EXCLUSIVE-LOCK WHERE misc-act.rec_key EQ '':
    ASSIGN
      misc-act.rec_key = nextRecKey()
      i = i + 1.
    RUN createRecKey (misc-act.rec_key,'misc-act').
  END.
  RUN msg ('Fixed ' + STRING(i) + ' Records' + CHR(10)).
END PROCEDURE.
 
PROCEDURE fixMMTX:
  DEFINE VARIABLE i AS INTEGER NO-UNDO.

  DISABLE TRIGGERS FOR LOAD OF mmtx.
 
  IF NOT CAN-FIND(FIRST mmtx WHERE mmtx.rec_key EQ '') THEN RETURN.
  RUN msg ('Processing mmtx ... ').
  FOR EACH mmtx EXCLUSIVE-LOCK WHERE mmtx.rec_key EQ '':
    ASSIGN
      mmtx.rec_key = nextRecKey()
      i = i + 1.
    RUN createRecKey (mmtx.rec_key,'mmtx').
  END.
  RUN msg ('Fixed ' + STRING(i) + ' Records' + CHR(10)).
END PROCEDURE.
 
PROCEDURE fixMMTX2:
  DEFINE VARIABLE i AS INTEGER NO-UNDO.

  DISABLE TRIGGERS FOR LOAD OF mmtx2.
 
  IF NOT CAN-FIND(FIRST mmtx2 WHERE mmtx2.rec_key EQ '') THEN RETURN.
  RUN msg ('Processing mmtx2 ... ').
  FOR EACH mmtx2 EXCLUSIVE-LOCK WHERE mmtx2.rec_key EQ '':
    ASSIGN
      mmtx2.rec_key = nextRecKey()
      i = i + 1.
    RUN createRecKey (mmtx2.rec_key,'mmtx2').
  END.
  RUN msg ('Fixed ' + STRING(i) + ' Records' + CHR(10)).
END PROCEDURE.
 
PROCEDURE fixMMTY:
  DEFINE VARIABLE i AS INTEGER NO-UNDO.

  DISABLE TRIGGERS FOR LOAD OF mmty.
 
  IF NOT CAN-FIND(FIRST mmty WHERE mmty.rec_key EQ '') THEN RETURN.
  RUN msg ('Processing mmty ... ').
  FOR EACH mmty EXCLUSIVE-LOCK WHERE mmty.rec_key EQ '':
    ASSIGN
      mmty.rec_key = nextRecKey()
      i = i + 1.
    RUN createRecKey (mmty.rec_key,'mmty').
  END.
  RUN msg ('Fixed ' + STRING(i) + ' Records' + CHR(10)).
END PROCEDURE.
 
PROCEDURE fixMNU-ITEM:
  DEFINE VARIABLE i AS INTEGER NO-UNDO.

  DISABLE TRIGGERS FOR LOAD OF mnu-item.
 
  IF NOT CAN-FIND(FIRST mnu-item WHERE mnu-item.rec_key EQ '') THEN RETURN.
  RUN msg ('Processing mnu-item ... ').
  FOR EACH mnu-item EXCLUSIVE-LOCK WHERE mnu-item.rec_key EQ '':
    ASSIGN
      mnu-item.rec_key = nextRecKey()
      i = i + 1.
    RUN createRecKey (mnu-item.rec_key,'mnu-item').
  END.
  RUN msg ('Fixed ' + STRING(i) + ' Records' + CHR(10)).
END PROCEDURE.
 
PROCEDURE fixMONTHLY:
  DEFINE VARIABLE i AS INTEGER NO-UNDO.

  DISABLE TRIGGERS FOR LOAD OF monthly.
 
  IF NOT CAN-FIND(FIRST monthly WHERE monthly.rec_key EQ '') THEN RETURN.
  RUN msg ('Processing monthly ... ').
  FOR EACH monthly EXCLUSIVE-LOCK WHERE monthly.rec_key EQ '':
    ASSIGN
      monthly.rec_key = nextRecKey()
      i = i + 1.
    RUN createRecKey (monthly.rec_key,'monthly').
  END.
  RUN msg ('Fixed ' + STRING(i) + ' Records' + CHR(10)).
END PROCEDURE.
 
PROCEDURE fixMSTD:
  DEFINE VARIABLE i AS INTEGER NO-UNDO.

  DISABLE TRIGGERS FOR LOAD OF mstd.
 
  IF NOT CAN-FIND(FIRST mstd WHERE mstd.rec_key EQ '') THEN RETURN.
  RUN msg ('Processing mstd ... ').
  FOR EACH mstd EXCLUSIVE-LOCK WHERE mstd.rec_key EQ '':
    ASSIGN
      mstd.rec_key = nextRecKey()
      i = i + 1.
    RUN createRecKey (mstd.rec_key,'mstd').
  END.
  RUN msg ('Fixed ' + STRING(i) + ' Records' + CHR(10)).
END PROCEDURE.
 
PROCEDURE fixNRF-COLOR:
  DEFINE VARIABLE i AS INTEGER NO-UNDO.

  DISABLE TRIGGERS FOR LOAD OF nrf-color.
 
  IF NOT CAN-FIND(FIRST nrf-color WHERE nrf-color.rec_key EQ '') THEN RETURN.
  RUN msg ('Processing nrf-color ... ').
  FOR EACH nrf-color EXCLUSIVE-LOCK WHERE nrf-color.rec_key EQ '':
    ASSIGN
      nrf-color.rec_key = nextRecKey()
      i = i + 1.
    RUN createRecKey (nrf-color.rec_key,'nrf-color').
  END.
  RUN msg ('Fixed ' + STRING(i) + ' Records' + CHR(10)).
END PROCEDURE.
 
PROCEDURE fixNRF-SIZE:
  DEFINE VARIABLE i AS INTEGER NO-UNDO.

  DISABLE TRIGGERS FOR LOAD OF nrf-size.
 
  IF NOT CAN-FIND(FIRST nrf-size WHERE nrf-size.rec_key EQ '') THEN RETURN.
  RUN msg ('Processing nrf-size ... ').
  FOR EACH nrf-size EXCLUSIVE-LOCK WHERE nrf-size.rec_key EQ '':
    ASSIGN
      nrf-size.rec_key = nextRecKey()
      i = i + 1.
    RUN createRecKey (nrf-size.rec_key,'nrf-size').
  END.
  RUN msg ('Fixed ' + STRING(i) + ' Records' + CHR(10)).
END PROCEDURE.
 
PROCEDURE fixOE-BOLH:
  DEFINE VARIABLE i AS INTEGER NO-UNDO.

  DISABLE TRIGGERS FOR LOAD OF oe-bolh.
 
  IF NOT CAN-FIND(FIRST oe-bolh WHERE oe-bolh.rec_key EQ '') THEN RETURN.
  RUN msg ('Processing oe-bolh ... ').
  FOR EACH oe-bolh EXCLUSIVE-LOCK WHERE oe-bolh.rec_key EQ '':
    ASSIGN
      oe-bolh.rec_key = nextRecKey()
      i = i + 1.
    RUN createRecKey (oe-bolh.rec_key,'oe-bolh').
  END.
  RUN msg ('Fixed ' + STRING(i) + ' Records' + CHR(10)).
END PROCEDURE.
 
PROCEDURE fixOE-BOLL:
  DEFINE VARIABLE i AS INTEGER NO-UNDO.

  DISABLE TRIGGERS FOR LOAD OF oe-boll.
 
  IF NOT CAN-FIND(FIRST oe-boll WHERE oe-boll.rec_key EQ '') THEN RETURN.
  RUN msg ('Processing oe-boll ... ').
  FOR EACH oe-boll EXCLUSIVE-LOCK WHERE oe-boll.rec_key EQ '':
    ASSIGN
      oe-boll.rec_key = nextRecKey()
      i = i + 1.
    RUN createRecKey (oe-boll.rec_key,'oe-boll').
  END.
  RUN msg ('Fixed ' + STRING(i) + ' Records' + CHR(10)).
END PROCEDURE.
 
PROCEDURE fixOE-BOLL-QTY:
  DEFINE VARIABLE i AS INTEGER NO-UNDO.

  DISABLE TRIGGERS FOR LOAD OF oe-boll-qty.
 
  IF NOT CAN-FIND(FIRST oe-boll-qty WHERE oe-boll-qty.rec_key EQ '') THEN RETURN.
  RUN msg ('Processing oe-boll-qty ... ').
  FOR EACH oe-boll-qty EXCLUSIVE-LOCK WHERE oe-boll-qty.rec_key EQ '':
    ASSIGN
      oe-boll-qty.rec_key = nextRecKey()
      i = i + 1.
    RUN createRecKey (oe-boll-qty.rec_key,'oe-boll-qty').
  END.
  RUN msg ('Fixed ' + STRING(i) + ' Records' + CHR(10)).
END PROCEDURE.
 
PROCEDURE fixOE-CTRL:
  DEFINE VARIABLE i AS INTEGER NO-UNDO.

  DISABLE TRIGGERS FOR LOAD OF oe-ctrl.
 
  IF NOT CAN-FIND(FIRST oe-ctrl WHERE oe-ctrl.rec_key EQ '') THEN RETURN.
  RUN msg ('Processing oe-ctrl ... ').
  FOR EACH oe-ctrl EXCLUSIVE-LOCK WHERE oe-ctrl.rec_key EQ '':
    ASSIGN
      oe-ctrl.rec_key = nextRecKey()
      i = i + 1.
    RUN createRecKey (oe-ctrl.rec_key,'oe-ctrl').
  END.
  RUN msg ('Fixed ' + STRING(i) + ' Records' + CHR(10)).
END PROCEDURE.
 
PROCEDURE fixOE-ORD:
  DEFINE VARIABLE i AS INTEGER NO-UNDO.

  DISABLE TRIGGERS FOR LOAD OF oe-ord.
 
  IF NOT CAN-FIND(FIRST oe-ord WHERE oe-ord.rec_key EQ '') THEN RETURN.
  RUN msg ('Processing oe-ord ... ').
  FOR EACH oe-ord EXCLUSIVE-LOCK WHERE oe-ord.rec_key EQ '':
    ASSIGN
      oe-ord.rec_key = nextRecKey()
      i = i + 1.
    RUN createRecKey (oe-ord.rec_key,'oe-ord').
  END.
  RUN msg ('Fixed ' + STRING(i) + ' Records' + CHR(10)).
END PROCEDURE.
 
PROCEDURE fixOE-ORDL:
  DEFINE VARIABLE i AS INTEGER NO-UNDO.

  DISABLE TRIGGERS FOR LOAD OF oe-ordl.
 
  IF NOT CAN-FIND(FIRST oe-ordl WHERE oe-ordl.rec_key EQ '') THEN RETURN.
  RUN msg ('Processing oe-ordl ... ').
  FOR EACH oe-ordl EXCLUSIVE-LOCK WHERE oe-ordl.rec_key EQ '':
    ASSIGN
      oe-ordl.rec_key = nextRecKey()
      i = i + 1.
    RUN createRecKey (oe-ordl.rec_key,'oe-ordl').
  END.
  RUN msg ('Fixed ' + STRING(i) + ' Records' + CHR(10)).
END PROCEDURE.
 
PROCEDURE fixOE-ORDM:
  DEFINE VARIABLE i AS INTEGER NO-UNDO.

  DISABLE TRIGGERS FOR LOAD OF oe-ordm.
 
  IF NOT CAN-FIND(FIRST oe-ordm WHERE oe-ordm.rec_key EQ '') THEN RETURN.
  RUN msg ('Processing oe-ordm ... ').
  FOR EACH oe-ordm EXCLUSIVE-LOCK WHERE oe-ordm.rec_key EQ '':
    ASSIGN
      oe-ordm.rec_key = nextRecKey()
      i = i + 1.
    RUN createRecKey (oe-ordm.rec_key,'oe-ordm').
  END.
  RUN msg ('Fixed ' + STRING(i) + ' Records' + CHR(10)).
END PROCEDURE.
 
PROCEDURE fixOE-PRMTX:
  DEFINE VARIABLE i AS INTEGER NO-UNDO.

  DISABLE TRIGGERS FOR LOAD OF oe-prmtx.
 
  IF NOT CAN-FIND(FIRST oe-prmtx WHERE oe-prmtx.rec_key EQ '') THEN RETURN.
  RUN msg ('Processing oe-prmtx ... ').
  FOR EACH oe-prmtx EXCLUSIVE-LOCK WHERE oe-prmtx.rec_key EQ '':
    ASSIGN
      oe-prmtx.rec_key = nextRecKey()
      i = i + 1.
    RUN createRecKey (oe-prmtx.rec_key,'oe-prmtx').
  END.
  RUN msg ('Fixed ' + STRING(i) + ' Records' + CHR(10)).
END PROCEDURE.
 
PROCEDURE fixOE-REL:
  DEFINE VARIABLE i AS INTEGER NO-UNDO.

  DISABLE TRIGGERS FOR LOAD OF oe-rel.
 
  IF NOT CAN-FIND(FIRST oe-rel WHERE oe-rel.rec_key EQ '') THEN RETURN.
  RUN msg ('Processing oe-rel ... ').
  FOR EACH oe-rel EXCLUSIVE-LOCK WHERE oe-rel.rec_key EQ '':
    ASSIGN
      oe-rel.rec_key = nextRecKey()
      i = i + 1.
    RUN createRecKey (oe-rel.rec_key,'oe-rel').
  END.
  RUN msg ('Fixed ' + STRING(i) + ' Records' + CHR(10)).
END PROCEDURE.
 
PROCEDURE fixOE-RELH:
  DEFINE VARIABLE i AS INTEGER NO-UNDO.

  DISABLE TRIGGERS FOR LOAD OF oe-relh.
 
  IF NOT CAN-FIND(FIRST oe-relh WHERE oe-relh.rec_key EQ '') THEN RETURN.
  RUN msg ('Processing oe-relh ... ').
  FOR EACH oe-relh EXCLUSIVE-LOCK WHERE oe-relh.rec_key EQ '':
    ASSIGN
      oe-relh.rec_key = nextRecKey()
      i = i + 1.
    RUN createRecKey (oe-relh.rec_key,'oe-relh').
  END.
  RUN msg ('Fixed ' + STRING(i) + ' Records' + CHR(10)).
END PROCEDURE.
 
PROCEDURE fixOE-RELL:
  DEFINE VARIABLE i AS INTEGER NO-UNDO.

  DISABLE TRIGGERS FOR LOAD OF oe-rell.
 
  IF NOT CAN-FIND(FIRST oe-rell WHERE oe-rell.rec_key EQ '') THEN RETURN.
  RUN msg ('Processing oe-rell ... ').
  FOR EACH oe-rell EXCLUSIVE-LOCK WHERE oe-rell.rec_key EQ '':
    ASSIGN
      oe-rell.rec_key = nextRecKey()
      i = i + 1.
    RUN createRecKey (oe-rell.rec_key,'oe-rell').
  END.
  RUN msg ('Fixed ' + STRING(i) + ' Records' + CHR(10)).
END PROCEDURE.
 
PROCEDURE fixOE-RETH:
  DEFINE VARIABLE i AS INTEGER NO-UNDO.

  DISABLE TRIGGERS FOR LOAD OF oe-reth.
 
  IF NOT CAN-FIND(FIRST oe-reth WHERE oe-reth.rec_key EQ '') THEN RETURN.
  RUN msg ('Processing oe-reth ... ').
  FOR EACH oe-reth EXCLUSIVE-LOCK WHERE oe-reth.rec_key EQ '':
    ASSIGN
      oe-reth.rec_key = nextRecKey()
      i = i + 1.
    RUN createRecKey (oe-reth.rec_key,'oe-reth').
  END.
  RUN msg ('Fixed ' + STRING(i) + ' Records' + CHR(10)).
END PROCEDURE.
 
PROCEDURE fixOE-RETL:
  DEFINE VARIABLE i AS INTEGER NO-UNDO.

  DISABLE TRIGGERS FOR LOAD OF oe-retl.
 
  IF NOT CAN-FIND(FIRST oe-retl WHERE oe-retl.rec_key EQ '') THEN RETURN.
  RUN msg ('Processing oe-retl ... ').
  FOR EACH oe-retl EXCLUSIVE-LOCK WHERE oe-retl.rec_key EQ '':
    ASSIGN
      oe-retl.rec_key = nextRecKey()
      i = i + 1.
    RUN createRecKey (oe-retl.rec_key,'oe-retl').
  END.
  RUN msg ('Fixed ' + STRING(i) + ' Records' + CHR(10)).
END PROCEDURE.
 
PROCEDURE fixOE-SHIP:
  DEFINE VARIABLE i AS INTEGER NO-UNDO.

  DISABLE TRIGGERS FOR LOAD OF oe-ship.
 
  IF NOT CAN-FIND(FIRST oe-ship WHERE oe-ship.rec_key EQ '') THEN RETURN.
  RUN msg ('Processing oe-ship ... ').
  FOR EACH oe-ship EXCLUSIVE-LOCK WHERE oe-ship.rec_key EQ '':
    ASSIGN
      oe-ship.rec_key = nextRecKey()
      i = i + 1.
    RUN createRecKey (oe-ship.rec_key,'oe-ship').
  END.
  RUN msg ('Fixed ' + STRING(i) + ' Records' + CHR(10)).
END PROCEDURE.
 
PROCEDURE fixPC-MISC:
  DEFINE VARIABLE i AS INTEGER NO-UNDO.

  DISABLE TRIGGERS FOR LOAD OF pc-misc.
 
  IF NOT CAN-FIND(FIRST pc-misc WHERE pc-misc.rec_key EQ '') THEN RETURN.
  RUN msg ('Processing pc-misc ... ').
  FOR EACH pc-misc EXCLUSIVE-LOCK WHERE pc-misc.rec_key EQ '':
    ASSIGN
      pc-misc.rec_key = nextRecKey()
      i = i + 1.
    RUN createRecKey (pc-misc.rec_key,'pc-misc').
  END.
  RUN msg ('Fixed ' + STRING(i) + ' Records' + CHR(10)).
END PROCEDURE.
 
PROCEDURE fixPC-PRDD:
  DEFINE VARIABLE i AS INTEGER NO-UNDO.

  DISABLE TRIGGERS FOR LOAD OF pc-prdd.
 
  IF NOT CAN-FIND(FIRST pc-prdd WHERE pc-prdd.rec_key EQ '') THEN RETURN.
  RUN msg ('Processing pc-prdd ... ').
  FOR EACH pc-prdd EXCLUSIVE-LOCK WHERE pc-prdd.rec_key EQ '':
    ASSIGN
      pc-prdd.rec_key = nextRecKey()
      i = i + 1.
    RUN createRecKey (pc-prdd.rec_key,'pc-prdd').
  END.
  RUN msg ('Fixed ' + STRING(i) + ' Records' + CHR(10)).
END PROCEDURE.
 
PROCEDURE fixPC-PRDH:
  DEFINE VARIABLE i AS INTEGER NO-UNDO.

  DISABLE TRIGGERS FOR LOAD OF pc-prdh.
 
  IF NOT CAN-FIND(FIRST pc-prdh WHERE pc-prdh.rec_key EQ '') THEN RETURN.
  RUN msg ('Processing pc-prdh ... ').
  FOR EACH pc-prdh EXCLUSIVE-LOCK WHERE pc-prdh.rec_key EQ '':
    ASSIGN
      pc-prdh.rec_key = nextRecKey()
      i = i + 1.
    RUN createRecKey (pc-prdh.rec_key,'pc-prdh').
  END.
  RUN msg ('Fixed ' + STRING(i) + ' Records' + CHR(10)).
END PROCEDURE.
 
PROCEDURE fixPDD:
  DEFINE VARIABLE i AS INTEGER NO-UNDO.

  DISABLE TRIGGERS FOR LOAD OF pdd.
 
  IF NOT CAN-FIND(FIRST pdd WHERE pdd.rec_key EQ '') THEN RETURN.
  RUN msg ('Processing pdd ... ').
  FOR EACH pdd EXCLUSIVE-LOCK WHERE pdd.rec_key EQ '':
    ASSIGN
      pdd.rec_key = nextRecKey()
      i = i + 1.
    RUN createRecKey (pdd.rec_key,'pdd').
  END.
  RUN msg ('Fixed ' + STRING(i) + ' Records' + CHR(10)).
END PROCEDURE.
 
PROCEDURE fixPDH:
  DEFINE VARIABLE i AS INTEGER NO-UNDO.

  DISABLE TRIGGERS FOR LOAD OF pdh.
 
  IF NOT CAN-FIND(FIRST pdh WHERE pdh.rec_key EQ '') THEN RETURN.
  RUN msg ('Processing pdh ... ').
  FOR EACH pdh EXCLUSIVE-LOCK WHERE pdh.rec_key EQ '':
    ASSIGN
      pdh.rec_key = nextRecKey()
      i = i + 1.
    RUN createRecKey (pdh.rec_key,'pdh').
  END.
  RUN msg ('Fixed ' + STRING(i) + ' Records' + CHR(10)).
END PROCEDURE.
 
PROCEDURE fixPERIOD:
  DEFINE VARIABLE i AS INTEGER NO-UNDO.

  DISABLE TRIGGERS FOR LOAD OF period.
 
  IF NOT CAN-FIND(FIRST period WHERE period.rec_key EQ '') THEN RETURN.
  RUN msg ('Processing period ... ').
  FOR EACH period EXCLUSIVE-LOCK WHERE period.rec_key EQ '':
    ASSIGN
      period.rec_key = nextRecKey()
      i = i + 1.
    RUN createRecKey (period.rec_key,'period').
  END.
  RUN msg ('Fixed ' + STRING(i) + ' Records' + CHR(10)).
END PROCEDURE.
 
PROCEDURE fixPERM:
  DEFINE VARIABLE i AS INTEGER NO-UNDO.

  DISABLE TRIGGERS FOR LOAD OF perm.
 
  IF NOT CAN-FIND(FIRST perm WHERE perm.rec_key EQ '') THEN RETURN.
  RUN msg ('Processing perm ... ').
  FOR EACH perm EXCLUSIVE-LOCK WHERE perm.rec_key EQ '':
    ASSIGN
      perm.rec_key = nextRecKey()
      i = i + 1.
    RUN createRecKey (perm.rec_key,'perm').
  END.
  RUN msg ('Fixed ' + STRING(i) + ' Records' + CHR(10)).
END PROCEDURE.
 
PROCEDURE fixPERMG:
  DEFINE VARIABLE i AS INTEGER NO-UNDO.

  DISABLE TRIGGERS FOR LOAD OF permg.
 
  IF NOT CAN-FIND(FIRST permg WHERE permg.rec_key EQ '') THEN RETURN.
  RUN msg ('Processing permg ... ').
  FOR EACH permg EXCLUSIVE-LOCK WHERE permg.rec_key EQ '':
    ASSIGN
      permg.rec_key = nextRecKey()
      i = i + 1.
    RUN createRecKey (permg.rec_key,'permg').
  END.
  RUN msg ('Fixed ' + STRING(i) + ' Records' + CHR(10)).
END PROCEDURE.
 
PROCEDURE fixPERMX:
  DEFINE VARIABLE i AS INTEGER NO-UNDO.

  DISABLE TRIGGERS FOR LOAD OF permx.
 
  IF NOT CAN-FIND(FIRST permx WHERE permx.rec_key EQ '') THEN RETURN.
  RUN msg ('Processing permx ... ').
  FOR EACH permx EXCLUSIVE-LOCK WHERE permx.rec_key EQ '':
    ASSIGN
      permx.rec_key = nextRecKey()
      i = i + 1.
    RUN createRecKey (permx.rec_key,'permx').
  END.
  RUN msg ('Fixed ' + STRING(i) + ' Records' + CHR(10)).
END PROCEDURE.
 
PROCEDURE fixPO-ALL:
  DEFINE VARIABLE i AS INTEGER NO-UNDO.

  DISABLE TRIGGERS FOR LOAD OF po-all.
 
  IF NOT CAN-FIND(FIRST po-all WHERE po-all.rec_key EQ '') THEN RETURN.
  RUN msg ('Processing po-all ... ').
  FOR EACH po-all EXCLUSIVE-LOCK WHERE po-all.rec_key EQ '':
    ASSIGN
      po-all.rec_key = nextRecKey()
      i = i + 1.
    RUN createRecKey (po-all.rec_key,'po-all').
  END.
  RUN msg ('Fixed ' + STRING(i) + ' Records' + CHR(10)).
END PROCEDURE.
 
PROCEDURE fixPO-CTRL:
  DEFINE VARIABLE i AS INTEGER NO-UNDO.

  DISABLE TRIGGERS FOR LOAD OF po-ctrl.
 
  IF NOT CAN-FIND(FIRST po-ctrl WHERE po-ctrl.rec_key EQ '') THEN RETURN.
  RUN msg ('Processing po-ctrl ... ').
  FOR EACH po-ctrl EXCLUSIVE-LOCK WHERE po-ctrl.rec_key EQ '':
    ASSIGN
      po-ctrl.rec_key = nextRecKey()
      i = i + 1.
    RUN createRecKey (po-ctrl.rec_key,'po-ctrl').
  END.
  RUN msg ('Fixed ' + STRING(i) + ' Records' + CHR(10)).
END PROCEDURE.
 
PROCEDURE fixPO-ORD:
  DEFINE VARIABLE i AS INTEGER NO-UNDO.

  DISABLE TRIGGERS FOR LOAD OF po-ord.
 
  IF NOT CAN-FIND(FIRST po-ord WHERE po-ord.rec_key EQ '') THEN RETURN.
  RUN msg ('Processing po-ord ... ').
  FOR EACH po-ord EXCLUSIVE-LOCK WHERE po-ord.rec_key EQ '':
    ASSIGN
      po-ord.rec_key = nextRecKey()
      i = i + 1.
    RUN createRecKey (po-ord.rec_key,'po-ord').
  END.
  RUN msg ('Fixed ' + STRING(i) + ' Records' + CHR(10)).
END PROCEDURE.
 
PROCEDURE fixPO-ORDL:
  DEFINE VARIABLE i AS INTEGER NO-UNDO.

  DISABLE TRIGGERS FOR LOAD OF po-ordl.
 
  IF NOT CAN-FIND(FIRST po-ordl WHERE po-ordl.rec_key EQ '') THEN RETURN.
  RUN msg ('Processing po-ordl ... ').
  FOR EACH po-ordl EXCLUSIVE-LOCK WHERE po-ordl.rec_key EQ '':
    ASSIGN
      po-ordl.rec_key = nextRecKey()
      i = i + 1.
    RUN createRecKey (po-ordl.rec_key,'po-ordl').
  END.
  RUN msg ('Fixed ' + STRING(i) + ' Records' + CHR(10)).
END PROCEDURE.
 
PROCEDURE fixPO-RCPTS:
  DEFINE VARIABLE i AS INTEGER NO-UNDO.

  DISABLE TRIGGERS FOR LOAD OF po-rcpts.
 
  IF NOT CAN-FIND(FIRST po-rcpts WHERE po-rcpts.rec_key EQ '') THEN RETURN.
  RUN msg ('Processing po-rcpts ... ').
  FOR EACH po-rcpts EXCLUSIVE-LOCK WHERE po-rcpts.rec_key EQ '':
    ASSIGN
      po-rcpts.rec_key = nextRecKey()
      i = i + 1.
    RUN createRecKey (po-rcpts.rec_key,'po-rcpts').
  END.
  RUN msg ('Fixed ' + STRING(i) + ' Records' + CHR(10)).
END PROCEDURE.
 
PROCEDURE fixPREP:
  DEFINE VARIABLE i AS INTEGER NO-UNDO.

  DISABLE TRIGGERS FOR LOAD OF prep.
 
  IF NOT CAN-FIND(FIRST prep WHERE prep.rec_key EQ '') THEN RETURN.
  RUN msg ('Processing prep ... ').
  FOR EACH prep EXCLUSIVE-LOCK WHERE prep.rec_key EQ '':
    ASSIGN
      prep.rec_key = nextRecKey()
      i = i + 1.
    RUN createRecKey (prep.rec_key,'prep').
  END.
  RUN msg ('Fixed ' + STRING(i) + ' Records' + CHR(10)).
END PROCEDURE.
 
PROCEDURE fixPRINTER:
  DEFINE VARIABLE i AS INTEGER NO-UNDO.

  DISABLE TRIGGERS FOR LOAD OF printer.
 
  IF NOT CAN-FIND(FIRST printer WHERE printer.rec_key EQ '') THEN RETURN.
  RUN msg ('Processing printer ... ').
  FOR EACH printer EXCLUSIVE-LOCK WHERE printer.rec_key EQ '':
    ASSIGN
      printer.rec_key = nextRecKey()
      i = i + 1.
    RUN createRecKey (printer.rec_key,'printer').
  END.
  RUN msg ('Fixed ' + STRING(i) + ' Records' + CHR(10)).
END PROCEDURE.
 
PROCEDURE fixPROBE:
  DEFINE VARIABLE i AS INTEGER NO-UNDO.

  DISABLE TRIGGERS FOR LOAD OF probe.
 
  IF NOT CAN-FIND(FIRST probe WHERE probe.rec_key EQ '') THEN RETURN.
  RUN msg ('Processing probe ... ').
  FOR EACH probe EXCLUSIVE-LOCK WHERE probe.rec_key EQ '':
    ASSIGN
      probe.rec_key = nextRecKey()
      i = i + 1.
    RUN createRecKey (probe.rec_key,'probe').
  END.
  RUN msg ('Fixed ' + STRING(i) + ' Records' + CHR(10)).
END PROCEDURE.
 
PROCEDURE fixPROBEIT:
  DEFINE VARIABLE i AS INTEGER NO-UNDO.

  DISABLE TRIGGERS FOR LOAD OF probeit.
 
  IF NOT CAN-FIND(FIRST probeit WHERE probeit.rec_key EQ '') THEN RETURN.
  RUN msg ('Processing probeit ... ').
  FOR EACH probeit EXCLUSIVE-LOCK WHERE probeit.rec_key EQ '':
    ASSIGN
      probeit.rec_key = nextRecKey()
      i = i + 1.
    RUN createRecKey (probeit.rec_key,'probeit').
  END.
  RUN msg ('Fixed ' + STRING(i) + ' Records' + CHR(10)).
END PROCEDURE.
 
PROCEDURE fixPROBEIT-PRICE:
  DEFINE VARIABLE i AS INTEGER NO-UNDO.

  DISABLE TRIGGERS FOR LOAD OF probeit-price.
 
  IF NOT CAN-FIND(FIRST probeit-price WHERE probeit-price.rec_key EQ '') THEN RETURN.
  RUN msg ('Processing probeit-price ... ').
  FOR EACH probeit-price EXCLUSIVE-LOCK WHERE probeit-price.rec_key EQ '':
    ASSIGN
      probeit-price.rec_key = nextRecKey()
      i = i + 1.
    RUN createRecKey (probeit-price.rec_key,'probeit-price').
  END.
  RUN msg ('Fixed ' + STRING(i) + ' Records' + CHR(10)).
END PROCEDURE.
 
PROCEDURE fixPROCAT:
  DEFINE VARIABLE i AS INTEGER NO-UNDO.

  DISABLE TRIGGERS FOR LOAD OF procat.
 
  IF NOT CAN-FIND(FIRST procat WHERE procat.rec_key EQ '') THEN RETURN.
  RUN msg ('Processing procat ... ').
  FOR EACH procat EXCLUSIVE-LOCK WHERE procat.rec_key EQ '':
    ASSIGN
      procat.rec_key = nextRecKey()
      i = i + 1.
    RUN createRecKey (procat.rec_key,'procat').
  END.
  RUN msg ('Fixed ' + STRING(i) + ' Records' + CHR(10)).
END PROCEDURE.
 
PROCEDURE fixPROD:
  DEFINE VARIABLE i AS INTEGER NO-UNDO.

  DISABLE TRIGGERS FOR LOAD OF prod.
 
  IF NOT CAN-FIND(FIRST prod WHERE prod.rec_key EQ '') THEN RETURN.
  RUN msg ('Processing prod ... ').
  FOR EACH prod EXCLUSIVE-LOCK WHERE prod.rec_key EQ '':
    ASSIGN
      prod.rec_key = nextRecKey()
      i = i + 1.
    RUN createRecKey (prod.rec_key,'prod').
  END.
  RUN msg ('Fixed ' + STRING(i) + ' Records' + CHR(10)).
END PROCEDURE.
 
PROCEDURE fixPRODL:
  DEFINE VARIABLE i AS INTEGER NO-UNDO.

  DISABLE TRIGGERS FOR LOAD OF prodl.
 
  IF NOT CAN-FIND(FIRST prodl WHERE prodl.rec_key EQ '') THEN RETURN.
  RUN msg ('Processing prodl ... ').
  FOR EACH prodl EXCLUSIVE-LOCK WHERE prodl.rec_key EQ '':
    ASSIGN
      prodl.rec_key = nextRecKey()
      i = i + 1.
    RUN createRecKey (prodl.rec_key,'prodl').
  END.
  RUN msg ('Fixed ' + STRING(i) + ' Records' + CHR(10)).
END PROCEDURE.
 
PROCEDURE fixQUOTE:
  DEFINE VARIABLE i AS INTEGER NO-UNDO.

  DISABLE TRIGGERS FOR LOAD OF quote.
 
  IF NOT CAN-FIND(FIRST quote WHERE quote.rec_key EQ '') THEN RETURN.
  RUN msg ('Processing quote ... ').
  FOR EACH quote EXCLUSIVE-LOCK WHERE quote.rec_key EQ '':
    ASSIGN
      quote.rec_key = nextRecKey()
      i = i + 1.
    RUN createRecKey (quote.rec_key,'quote').
  END.
  RUN msg ('Fixed ' + STRING(i) + ' Records' + CHR(10)).
END PROCEDURE.
 
PROCEDURE fixQUOTECHG:
  DEFINE VARIABLE i AS INTEGER NO-UNDO.

  DISABLE TRIGGERS FOR LOAD OF quotechg.
 
  IF NOT CAN-FIND(FIRST quotechg WHERE quotechg.rec_key EQ '') THEN RETURN.
  RUN msg ('Processing quotechg ... ').
  FOR EACH quotechg EXCLUSIVE-LOCK WHERE quotechg.rec_key EQ '':
    ASSIGN
      quotechg.rec_key = nextRecKey()
      i = i + 1.
    RUN createRecKey (quotechg.rec_key,'quotechg').
  END.
  RUN msg ('Fixed ' + STRING(i) + ' Records' + CHR(10)).
END PROCEDURE.
 
PROCEDURE fixQUOTEHD:
  DEFINE VARIABLE i AS INTEGER NO-UNDO.

  DISABLE TRIGGERS FOR LOAD OF quotehd.
 
  IF NOT CAN-FIND(FIRST quotehd WHERE quotehd.rec_key EQ '') THEN RETURN.
  RUN msg ('Processing quotehd ... ').
  FOR EACH quotehd EXCLUSIVE-LOCK WHERE quotehd.rec_key EQ '':
    ASSIGN
      quotehd.rec_key = nextRecKey()
      i = i + 1.
    RUN createRecKey (quotehd.rec_key,'quotehd').
  END.
  RUN msg ('Fixed ' + STRING(i) + ' Records' + CHR(10)).
END PROCEDURE.
 
PROCEDURE fixQUOTEIT:
  DEFINE VARIABLE i AS INTEGER NO-UNDO.

  DISABLE TRIGGERS FOR LOAD OF quoteit.
 
  IF NOT CAN-FIND(FIRST quoteit WHERE quoteit.rec_key EQ '') THEN RETURN.
  RUN msg ('Processing quoteit ... ').
  FOR EACH quoteit EXCLUSIVE-LOCK WHERE quoteit.rec_key EQ '':
    ASSIGN
      quoteit.rec_key = nextRecKey()
      i = i + 1.
    RUN createRecKey (quoteit.rec_key,'quoteit').
  END.
  RUN msg ('Fixed ' + STRING(i) + ' Records' + CHR(10)).
END PROCEDURE.
 
PROCEDURE fixQUOTEITM:
  DEFINE VARIABLE i AS INTEGER NO-UNDO.

  DISABLE TRIGGERS FOR LOAD OF quoteitm.
 
  IF NOT CAN-FIND(FIRST quoteitm WHERE quoteitm.rec_key EQ '') THEN RETURN.
  RUN msg ('Processing quoteitm ... ').
  FOR EACH quoteitm EXCLUSIVE-LOCK WHERE quoteitm.rec_key EQ '':
    ASSIGN
      quoteitm.rec_key = nextRecKey()
      i = i + 1.
    RUN createRecKey (quoteitm.rec_key,'quoteitm').
  END.
  RUN msg ('Fixed ' + STRING(i) + ' Records' + CHR(10)).
END PROCEDURE.
 
PROCEDURE fixQUOTEQTY:
  DEFINE VARIABLE i AS INTEGER NO-UNDO.

  DISABLE TRIGGERS FOR LOAD OF quoteqty.
 
  IF NOT CAN-FIND(FIRST quoteqty WHERE quoteqty.rec_key EQ '') THEN RETURN.
  RUN msg ('Processing quoteqty ... ').
  FOR EACH quoteqty EXCLUSIVE-LOCK WHERE quoteqty.rec_key EQ '':
    ASSIGN
      quoteqty.rec_key = nextRecKey()
      i = i + 1.
    RUN createRecKey (quoteqty.rec_key,'quoteqty').
  END.
  RUN msg ('Fixed ' + STRING(i) + ' Records' + CHR(10)).
END PROCEDURE.
 
PROCEDURE fixRCERR:
  DEFINE VARIABLE i AS INTEGER NO-UNDO.

  DISABLE TRIGGERS FOR LOAD OF rcerr.
 
  IF NOT CAN-FIND(FIRST RCErr WHERE RCErr.rec_key EQ '') THEN RETURN.
  RUN msg ('Processing RCErr ... ').
  FOR EACH RCErr EXCLUSIVE-LOCK WHERE RCErr.rec_key EQ '':
    ASSIGN
      RCErr.rec_key = nextRecKey()
      i = i + 1.
    RUN createRecKey (RCErr.rec_key,'RCErr').
  END.
  RUN msg ('Fixed ' + STRING(i) + ' Records' + CHR(10)).
END PROCEDURE.
 
PROCEDURE fixREFTABLE:
  DEFINE VARIABLE i AS INTEGER NO-UNDO.

  DISABLE TRIGGERS FOR LOAD OF reftable.
 
  IF NOT CAN-FIND(FIRST reftable WHERE reftable.rec_key EQ '') THEN RETURN.
  RUN msg ('Processing reftable ... ').
  FOR EACH reftable EXCLUSIVE-LOCK WHERE reftable.rec_key EQ '':
    ASSIGN
      reftable.rec_key = nextRecKey()
      i = i + 1.
    RUN createRecKey (reftable.rec_key,'reftable').
  END.
  RUN msg ('Fixed ' + STRING(i) + ' Records' + CHR(10)).
END PROCEDURE.
 
PROCEDURE fixREJCT-CD:
  DEFINE VARIABLE i AS INTEGER NO-UNDO.

  DISABLE TRIGGERS FOR LOAD OF rejct-cd.
 
  IF NOT CAN-FIND(FIRST rejct-cd WHERE rejct-cd.rec_key EQ '') THEN RETURN.
  RUN msg ('Processing rejct-cd ... ').
  FOR EACH rejct-cd EXCLUSIVE-LOCK WHERE rejct-cd.rec_key EQ '':
    ASSIGN
      rejct-cd.rec_key = nextRecKey()
      i = i + 1.
    RUN createRecKey (rejct-cd.rec_key,'rejct-cd').
  END.
  RUN msg ('Fixed ' + STRING(i) + ' Records' + CHR(10)).
END PROCEDURE.
 
PROCEDURE fixREPORT:
  DEFINE VARIABLE i AS INTEGER NO-UNDO.

  DISABLE TRIGGERS FOR LOAD OF report.
 
  IF NOT CAN-FIND(FIRST report WHERE report.rec_key EQ '') THEN RETURN.
  RUN msg ('Processing report ... ').
  FOR EACH report EXCLUSIVE-LOCK WHERE report.rec_key EQ '':
    ASSIGN
      report.rec_key = nextRecKey()
      i = i + 1.
    RUN createRecKey (report.rec_key,'report').
  END.
  RUN msg ('Fixed ' + STRING(i) + ' Records' + CHR(10)).
END PROCEDURE.
 
PROCEDURE fixRM-BIN:
  DEFINE VARIABLE i AS INTEGER NO-UNDO.

  DISABLE TRIGGERS FOR LOAD OF rm-bin.
 
  IF NOT CAN-FIND(FIRST rm-bin WHERE rm-bin.rec_key EQ '') THEN RETURN.
  RUN msg ('Processing rm-bin ... ').
  FOR EACH rm-bin EXCLUSIVE-LOCK WHERE rm-bin.rec_key EQ '':
    ASSIGN
      rm-bin.rec_key = nextRecKey()
      i = i + 1.
    RUN createRecKey (rm-bin.rec_key,'rm-bin').
  END.
  RUN msg ('Fixed ' + STRING(i) + ' Records' + CHR(10)).
END PROCEDURE.
 
PROCEDURE fixRM-CTRL:
  DEFINE VARIABLE i AS INTEGER NO-UNDO.

  DISABLE TRIGGERS FOR LOAD OF rm-ctrl.
 
  IF NOT CAN-FIND(FIRST rm-ctrl WHERE rm-ctrl.rec_key EQ '') THEN RETURN.
  RUN msg ('Processing rm-ctrl ... ').
  FOR EACH rm-ctrl EXCLUSIVE-LOCK WHERE rm-ctrl.rec_key EQ '':
    ASSIGN
      rm-ctrl.rec_key = nextRecKey()
      i = i + 1.
    RUN createRecKey (rm-ctrl.rec_key,'rm-ctrl').
  END.
  RUN msg ('Fixed ' + STRING(i) + ' Records' + CHR(10)).
END PROCEDURE.
 
PROCEDURE fixRM-RCPT:
  DEFINE VARIABLE i AS INTEGER NO-UNDO.

  DISABLE TRIGGERS FOR LOAD OF rm-rcpt.
 
  IF NOT CAN-FIND(FIRST rm-rcpt WHERE rm-rcpt.rec_key EQ '') THEN RETURN.
  RUN msg ('Processing rm-rcpt ... ').
  FOR EACH rm-rcpt EXCLUSIVE-LOCK WHERE rm-rcpt.rec_key EQ '':
    ASSIGN
      rm-rcpt.rec_key = nextRecKey()
      i = i + 1.
    RUN createRecKey (rm-rcpt.rec_key,'rm-rcpt').
  END.
  RUN msg ('Fixed ' + STRING(i) + ' Records' + CHR(10)).
END PROCEDURE.
 
PROCEDURE fixRM-RCPTH:
  DEFINE VARIABLE i AS INTEGER NO-UNDO.

  DISABLE TRIGGERS FOR LOAD OF rm-rcpth.
 
  IF NOT CAN-FIND(FIRST rm-rcpth WHERE rm-rcpth.rec_key EQ '') THEN RETURN.
  RUN msg ('Processing rm-rcpth ... ').
  FOR EACH rm-rcpth EXCLUSIVE-LOCK WHERE rm-rcpth.rec_key EQ '':
    ASSIGN
      rm-rcpth.rec_key = nextRecKey()
      i = i + 1.
    RUN createRecKey (rm-rcpth.rec_key,'rm-rcpth').
  END.
  RUN msg ('Fixed ' + STRING(i) + ' Records' + CHR(10)).
END PROCEDURE.
 
PROCEDURE fixRM-RCTD:
  DEFINE VARIABLE i AS INTEGER NO-UNDO.

  DISABLE TRIGGERS FOR LOAD OF rm-rctd.
 
  IF NOT CAN-FIND(FIRST rm-rctd WHERE rm-rctd.rec_key EQ '') THEN RETURN.
  RUN msg ('Processing rm-rctd ... ').
  FOR EACH rm-rctd EXCLUSIVE-LOCK WHERE rm-rctd.rec_key EQ '':
    ASSIGN
      rm-rctd.rec_key = nextRecKey()
      i = i + 1.
    RUN createRecKey (rm-rctd.rec_key,'rm-rctd').
  END.
  RUN msg ('Fixed ' + STRING(i) + ' Records' + CHR(10)).
END PROCEDURE.
 
PROCEDURE fixRM-RCTH:
  DEFINE VARIABLE i AS INTEGER NO-UNDO.

  DISABLE TRIGGERS FOR LOAD OF rm-rcth.
 
  IF NOT CAN-FIND(FIRST rm-rcth WHERE rm-rcth.rec_key EQ '') THEN RETURN.
  RUN msg ('Processing rm-rcth ... ').
  FOR EACH rm-rcth EXCLUSIVE-LOCK WHERE rm-rcth.rec_key EQ '':
    ASSIGN
      rm-rcth.rec_key = nextRecKey()
      i = i + 1.
    RUN createRecKey (rm-rcth.rec_key,'rm-rcth').
  END.
  RUN msg ('Fixed ' + STRING(i) + ' Records' + CHR(10)).
END PROCEDURE.
 
PROCEDURE fixRM-RDTL:
  DEFINE VARIABLE i AS INTEGER NO-UNDO.

  DISABLE TRIGGERS FOR LOAD OF rm-rdtl.
 
  IF NOT CAN-FIND(FIRST rm-rdtl WHERE rm-rdtl.rec_key EQ '') THEN RETURN.
  RUN msg ('Processing rm-rdtl ... ').
  FOR EACH rm-rdtl EXCLUSIVE-LOCK WHERE rm-rdtl.rec_key EQ '':
    ASSIGN
      rm-rdtl.rec_key = nextRecKey()
      i = i + 1.
    RUN createRecKey (rm-rdtl.rec_key,'rm-rdtl').
  END.
  RUN msg ('Fixed ' + STRING(i) + ' Records' + CHR(10)).
END PROCEDURE.
 
PROCEDURE fixRM-RDTLH:
  DEFINE VARIABLE i AS INTEGER NO-UNDO.

  DISABLE TRIGGERS FOR LOAD OF rm-rdtlh.
 
  IF NOT CAN-FIND(FIRST rm-rdtlh WHERE rm-rdtlh.rec_key EQ '') THEN RETURN.
  RUN msg ('Processing rm-rdtlh ... ').
  FOR EACH rm-rdtlh EXCLUSIVE-LOCK WHERE rm-rdtlh.rec_key EQ '':
    ASSIGN
      rm-rdtlh.rec_key = nextRecKey()
      i = i + 1.
    RUN createRecKey (rm-rdtlh.rec_key,'rm-rdtlh').
  END.
  RUN msg ('Fixed ' + STRING(i) + ' Records' + CHR(10)).
END PROCEDURE.
 
PROCEDURE fixRM-RECEIPTS:
  DEFINE VARIABLE i AS INTEGER NO-UNDO.

  DISABLE TRIGGERS FOR LOAD OF rm-receipts.
 
  IF NOT CAN-FIND(FIRST rm-receipts WHERE rm-receipts.rec_key EQ '') THEN RETURN.
  RUN msg ('Processing rm-receipts ... ').
  FOR EACH rm-receipts EXCLUSIVE-LOCK WHERE rm-receipts.rec_key EQ '':
    ASSIGN
      rm-receipts.rec_key = nextRecKey()
      i = i + 1.
    RUN createRecKey (rm-receipts.rec_key,'rm-receipts').
  END.
  RUN msg ('Fixed ' + STRING(i) + ' Records' + CHR(10)).
END PROCEDURE.
 
PROCEDURE fixROUTING:
  DEFINE VARIABLE i AS INTEGER NO-UNDO.

  DISABLE TRIGGERS FOR LOAD OF routing.
 
  IF NOT CAN-FIND(FIRST routing WHERE routing.rec_key EQ '') THEN RETURN.
  RUN msg ('Processing routing ... ').
  FOR EACH routing EXCLUSIVE-LOCK WHERE routing.rec_key EQ '':
    ASSIGN
      routing.rec_key = nextRecKey()
      i = i + 1.
    RUN createRecKey (routing.rec_key,'routing').
  END.
  RUN msg ('Fixed ' + STRING(i) + ' Records' + CHR(10)).
END PROCEDURE.
 
PROCEDURE fixROUTING-MTX:
  DEFINE VARIABLE i AS INTEGER NO-UNDO.

  DISABLE TRIGGERS FOR LOAD OF routing-mtx.
 
  IF NOT CAN-FIND(FIRST routing-mtx WHERE routing-mtx.rec_key EQ '') THEN RETURN.
  RUN msg ('Processing routing-mtx ... ').
  FOR EACH routing-mtx EXCLUSIVE-LOCK WHERE routing-mtx.rec_key EQ '':
    ASSIGN
      routing-mtx.rec_key = nextRecKey()
      i = i + 1.
    RUN createRecKey (routing-mtx.rec_key,'routing-mtx').
  END.
  RUN msg ('Fixed ' + STRING(i) + ' Records' + CHR(10)).
END PROCEDURE.
 
PROCEDURE fixSBSTATUS:
  DEFINE VARIABLE i AS INTEGER NO-UNDO.

  DISABLE TRIGGERS FOR LOAD OF sbstatus.
 
  IF NOT CAN-FIND(FIRST sbStatus WHERE sbStatus.rec_key EQ '') THEN RETURN.
  RUN msg ('Processing sbStatus ... ').
  FOR EACH sbStatus EXCLUSIVE-LOCK WHERE sbStatus.rec_key EQ '':
    ASSIGN
      sbStatus.rec_key = nextRecKey()
      i = i + 1.
    RUN createRecKey (sbStatus.rec_key,'sbStatus').
  END.
  RUN msg ('Fixed ' + STRING(i) + ' Records' + CHR(10)).
END PROCEDURE.
 
PROCEDURE fixSHIFT:
  DEFINE VARIABLE i AS INTEGER NO-UNDO.

  DISABLE TRIGGERS FOR LOAD OF shift.
 
  IF NOT CAN-FIND(FIRST shift WHERE shift.rec_key EQ '') THEN RETURN.
  RUN msg ('Processing shift ... ').
  FOR EACH shift EXCLUSIVE-LOCK WHERE shift.rec_key EQ '':
    ASSIGN
      shift.rec_key = nextRecKey()
      i = i + 1.
    RUN createRecKey (shift.rec_key,'shift').
  END.
  RUN msg ('Fixed ' + STRING(i) + ' Records' + CHR(10)).
END PROCEDURE.
 
PROCEDURE fixSHIPTO:
  DEFINE VARIABLE i AS INTEGER NO-UNDO.

  DISABLE TRIGGERS FOR LOAD OF shipto.
 
  IF NOT CAN-FIND(FIRST shipto WHERE shipto.rec_key EQ '') THEN RETURN.
  RUN msg ('Processing shipto ... ').
  FOR EACH shipto EXCLUSIVE-LOCK WHERE shipto.rec_key EQ '':
    ASSIGN
      shipto.rec_key = nextRecKey()
      i = i + 1.
    RUN createRecKey (shipto.rec_key,'shipto').
  END.
  RUN msg ('Fixed ' + STRING(i) + ' Records' + CHR(10)).
END PROCEDURE.
 
PROCEDURE fixSMAN:
  DEFINE VARIABLE i AS INTEGER NO-UNDO.

  DISABLE TRIGGERS FOR LOAD OF sman.
 
  IF NOT CAN-FIND(FIRST sman WHERE sman.rec_key EQ '') THEN RETURN.
  RUN msg ('Processing sman ... ').
  FOR EACH sman EXCLUSIVE-LOCK WHERE sman.rec_key EQ '':
    ASSIGN
      sman.rec_key = nextRecKey()
      i = i + 1.
    RUN createRecKey (sman.rec_key,'sman').
  END.
  RUN msg ('Fixed ' + STRING(i) + ' Records' + CHR(10)).
END PROCEDURE.
 
PROCEDURE fixSMAN-MTX:
  DEFINE VARIABLE i AS INTEGER NO-UNDO.

  DISABLE TRIGGERS FOR LOAD OF sman-mtx.
 
  IF NOT CAN-FIND(FIRST sman-mtx WHERE sman-mtx.rec_key EQ '') THEN RETURN.
  RUN msg ('Processing sman-mtx ... ').
  FOR EACH sman-mtx EXCLUSIVE-LOCK WHERE sman-mtx.rec_key EQ '':
    ASSIGN
      sman-mtx.rec_key = nextRecKey()
      i = i + 1.
    RUN createRecKey (sman-mtx.rec_key,'sman-mtx').
  END.
  RUN msg ('Fixed ' + STRING(i) + ' Records' + CHR(10)).
END PROCEDURE.
 
PROCEDURE fixSOLDTO:
  DEFINE VARIABLE i AS INTEGER NO-UNDO.

  DISABLE TRIGGERS FOR LOAD OF soldto.
 
  IF NOT CAN-FIND(FIRST soldto WHERE soldto.rec_key EQ '') THEN RETURN.
  RUN msg ('Processing soldto ... ').
  FOR EACH soldto EXCLUSIVE-LOCK WHERE soldto.rec_key EQ '':
    ASSIGN
      soldto.rec_key = nextRecKey()
      i = i + 1.
    RUN createRecKey (soldto.rec_key,'soldto').
  END.
  RUN msg ('Fixed ' + STRING(i) + ' Records' + CHR(10)).
END PROCEDURE.
 
PROCEDURE fixSTATE:
  DEFINE VARIABLE i AS INTEGER NO-UNDO.

  DISABLE TRIGGERS FOR LOAD OF state.
 
  IF NOT CAN-FIND(FIRST state WHERE state.rec_key EQ '') THEN RETURN.
  RUN msg ('Processing state ... ').
  FOR EACH state EXCLUSIVE-LOCK WHERE state.rec_key EQ '':
    ASSIGN
      state.rec_key = nextRecKey()
      i = i + 1.
    RUN createRecKey (state.rec_key,'state').
  END.
  RUN msg ('Fixed ' + STRING(i) + ' Records' + CHR(10)).
END PROCEDURE.
 
PROCEDURE fixSTAX:
  DEFINE VARIABLE i AS INTEGER NO-UNDO.

  DISABLE TRIGGERS FOR LOAD OF stax.
 
  IF NOT CAN-FIND(FIRST stax WHERE stax.rec_key EQ '') THEN RETURN.
  RUN msg ('Processing stax ... ').
  FOR EACH stax EXCLUSIVE-LOCK WHERE stax.rec_key EQ '':
    ASSIGN
      stax.rec_key = nextRecKey()
      i = i + 1.
    RUN createRecKey (stax.rec_key,'stax').
  END.
  RUN msg ('Fixed ' + STRING(i) + ' Records' + CHR(10)).
END PROCEDURE.
 
PROCEDURE fixSTAX-GROUP:
  DEFINE VARIABLE i AS INTEGER NO-UNDO.

  DISABLE TRIGGERS FOR LOAD OF stax-group.
 
  IF NOT CAN-FIND(FIRST stax-group WHERE stax-group.rec_key EQ '') THEN RETURN.
  RUN msg ('Processing stax-group ... ').
  FOR EACH stax-group EXCLUSIVE-LOCK WHERE stax-group.rec_key EQ '':
    ASSIGN
      stax-group.rec_key = nextRecKey()
      i = i + 1.
    RUN createRecKey (stax-group.rec_key,'stax-group').
  END.
  RUN msg ('Fixed ' + STRING(i) + ' Records' + CHR(10)).
END PROCEDURE.
 
PROCEDURE fixSTD-CODE:
  DEFINE VARIABLE i AS INTEGER NO-UNDO.

  DISABLE TRIGGERS FOR LOAD OF std-code.
 
  IF NOT CAN-FIND(FIRST std-code WHERE std-code.rec_key EQ '') THEN RETURN.
  RUN msg ('Processing std-code ... ').
  FOR EACH std-code EXCLUSIVE-LOCK WHERE std-code.rec_key EQ '':
    ASSIGN
      std-code.rec_key = nextRecKey()
      i = i + 1.
    RUN createRecKey (std-code.rec_key,'std-code').
  END.
  RUN msg ('Fixed ' + STRING(i) + ' Records' + CHR(10)).
END PROCEDURE.
 
PROCEDURE fixSTYLE:
  DEFINE VARIABLE i AS INTEGER NO-UNDO.

  DISABLE TRIGGERS FOR LOAD OF style.
 
  IF NOT CAN-FIND(FIRST style WHERE style.rec_key EQ '') THEN RETURN.
  RUN msg ('Processing style ... ').
  FOR EACH style EXCLUSIVE-LOCK WHERE style.rec_key EQ '':
    ASSIGN
      style.rec_key = nextRecKey()
      i = i + 1.
    RUN createRecKey (style.rec_key,'style').
  END.
  RUN msg ('Fixed ' + STRING(i) + ' Records' + CHR(10)).
END PROCEDURE.
 
PROCEDURE fixSURCHARGE:
  DEFINE VARIABLE i AS INTEGER NO-UNDO.

  DISABLE TRIGGERS FOR LOAD OF surcharge.
 
  IF NOT CAN-FIND(FIRST surcharge WHERE surcharge.rec_key EQ '') THEN RETURN.
  RUN msg ('Processing surcharge ... ').
  FOR EACH surcharge EXCLUSIVE-LOCK WHERE surcharge.rec_key EQ '':
    ASSIGN
      surcharge.rec_key = nextRecKey()
      i = i + 1.
    RUN createRecKey (surcharge.rec_key,'surcharge').
  END.
  RUN msg ('Fixed ' + STRING(i) + ' Records' + CHR(10)).
END PROCEDURE.
 
PROCEDURE fixSYS-CTRL:
  DEFINE VARIABLE i AS INTEGER NO-UNDO.

  DISABLE TRIGGERS FOR LOAD OF sys-ctrl.
 
  IF NOT CAN-FIND(FIRST sys-ctrl WHERE sys-ctrl.rec_key EQ '') THEN RETURN.
  RUN msg ('Processing sys-ctrl ... ').
  FOR EACH sys-ctrl EXCLUSIVE-LOCK WHERE sys-ctrl.rec_key EQ '':
    ASSIGN
      sys-ctrl.rec_key = nextRecKey()
      i = i + 1.
    RUN createRecKey (sys-ctrl.rec_key,'sys-ctrl').
  END.
  RUN msg ('Fixed ' + STRING(i) + ' Records' + CHR(10)).
END PROCEDURE.
 
PROCEDURE fixSYS-CTRL-SHIPTO:
  DEFINE VARIABLE i AS INTEGER NO-UNDO.

  DISABLE TRIGGERS FOR LOAD OF sys-ctrl-shipto.
 
  IF NOT CAN-FIND(FIRST sys-ctrl-shipto WHERE sys-ctrl-shipto.rec_key EQ '') THEN RETURN.
  RUN msg ('Processing sys-ctrl-shipto ... ').
  FOR EACH sys-ctrl-shipto EXCLUSIVE-LOCK WHERE sys-ctrl-shipto.rec_key EQ '':
    ASSIGN
      sys-ctrl-shipto.rec_key = nextRecKey()
      i = i + 1.
    RUN createRecKey (sys-ctrl-shipto.rec_key,'sys-ctrl-shipto').
  END.
  RUN msg ('Fixed ' + STRING(i) + ' Records' + CHR(10)).
END PROCEDURE.
 
PROCEDURE fixTERMS:
  DEFINE VARIABLE i AS INTEGER NO-UNDO.

  DISABLE TRIGGERS FOR LOAD OF terms.
 
  IF NOT CAN-FIND(FIRST terms WHERE terms.rec_key EQ '') THEN RETURN.
  RUN msg ('Processing terms ... ').
  FOR EACH terms EXCLUSIVE-LOCK WHERE terms.rec_key EQ '':
    ASSIGN
      terms.rec_key = nextRecKey()
      i = i + 1.
    RUN createRecKey (terms.rec_key,'terms').
  END.
  RUN msg ('Fixed ' + STRING(i) + ' Records' + CHR(10)).
END PROCEDURE.
 
PROCEDURE fixTERR:
  DEFINE VARIABLE i AS INTEGER NO-UNDO.

  DISABLE TRIGGERS FOR LOAD OF terr.
 
  IF NOT CAN-FIND(FIRST terr WHERE terr.rec_key EQ '') THEN RETURN.
  RUN msg ('Processing terr ... ').
  FOR EACH terr EXCLUSIVE-LOCK WHERE terr.rec_key EQ '':
    ASSIGN
      terr.rec_key = nextRecKey()
      i = i + 1.
    RUN createRecKey (terr.rec_key,'terr').
  END.
  RUN msg ('Fixed ' + STRING(i) + ' Records' + CHR(10)).
END PROCEDURE.
 
PROCEDURE fixTEST-RED:
  DEFINE VARIABLE i AS INTEGER NO-UNDO.

  DISABLE TRIGGERS FOR LOAD OF test-red.
 
  IF NOT CAN-FIND(FIRST test-red WHERE test-red.rec_key EQ '') THEN RETURN.
  RUN msg ('Processing test-red ... ').
  FOR EACH test-red EXCLUSIVE-LOCK WHERE test-red.rec_key EQ '':
    ASSIGN
      test-red.rec_key = nextRecKey()
      i = i + 1.
    RUN createRecKey (test-red.rec_key,'test-red').
  END.
  RUN msg ('Fixed ' + STRING(i) + ' Records' + CHR(10)).
END PROCEDURE.
 
PROCEDURE fixUOM:
  DEFINE VARIABLE i AS INTEGER NO-UNDO.

  DISABLE TRIGGERS FOR LOAD OF uom.
 
  IF NOT CAN-FIND(FIRST uom WHERE uom.rec_key EQ '') THEN RETURN.
  RUN msg ('Processing uom ... ').
  FOR EACH uom EXCLUSIVE-LOCK WHERE uom.rec_key EQ '':
    ASSIGN
      uom.rec_key = nextRecKey()
      i = i + 1.
    RUN createRecKey (uom.rec_key,'uom').
  END.
  RUN msg ('Fixed ' + STRING(i) + ' Records' + CHR(10)).
END PROCEDURE.
 
PROCEDURE fixUSER-PRINT:
  DEFINE VARIABLE i AS INTEGER NO-UNDO.

  DISABLE TRIGGERS FOR LOAD OF user-print.
 
  IF NOT CAN-FIND(FIRST user-print WHERE user-print.rec_key EQ '') THEN RETURN.
  RUN msg ('Processing user-print ... ').
  FOR EACH user-print EXCLUSIVE-LOCK WHERE user-print.rec_key EQ '':
    ASSIGN
      user-print.rec_key = nextRecKey()
      i = i + 1.
    RUN createRecKey (user-print.rec_key,'user-print').
  END.
  RUN msg ('Fixed ' + STRING(i) + ' Records' + CHR(10)).
END PROCEDURE.
 
PROCEDURE fixUSERCOMP:
  DEFINE VARIABLE i AS INTEGER NO-UNDO.

  DISABLE TRIGGERS FOR LOAD OF usercomp.
 
  IF NOT CAN-FIND(FIRST usercomp WHERE usercomp.rec_key EQ '') THEN RETURN.
  RUN msg ('Processing usercomp ... ').
  FOR EACH usercomp EXCLUSIVE-LOCK WHERE usercomp.rec_key EQ '':
    ASSIGN
      usercomp.rec_key = nextRecKey()
      i = i + 1.
    RUN createRecKey (usercomp.rec_key,'usercomp').
  END.
  RUN msg ('Fixed ' + STRING(i) + ' Records' + CHR(10)).
END PROCEDURE.
 
PROCEDURE fixUSR:
  DEFINE VARIABLE i AS INTEGER NO-UNDO.

  DISABLE TRIGGERS FOR LOAD OF usr.
 
  IF NOT CAN-FIND(FIRST usr WHERE usr.rec_key EQ '') THEN RETURN.
  RUN msg ('Processing usr ... ').
  FOR EACH usr EXCLUSIVE-LOCK WHERE usr.rec_key EQ '':
    ASSIGN
      usr.rec_key = nextRecKey()
      i = i + 1.
    RUN createRecKey (usr.rec_key,'usr').
  END.
  RUN msg ('Fixed ' + STRING(i) + ' Records' + CHR(10)).
END PROCEDURE.
 
PROCEDURE fixUSR-GRP:
  DEFINE VARIABLE i AS INTEGER NO-UNDO.

  DISABLE TRIGGERS FOR LOAD OF usr-grp.
 
  IF NOT CAN-FIND(FIRST usr-grp WHERE usr-grp.rec_key EQ '') THEN RETURN.
  RUN msg ('Processing usr-grp ... ').
  FOR EACH usr-grp EXCLUSIVE-LOCK WHERE usr-grp.rec_key EQ '':
    ASSIGN
      usr-grp.rec_key = nextRecKey()
      i = i + 1.
    RUN createRecKey (usr-grp.rec_key,'usr-grp').
  END.
  RUN msg ('Fixed ' + STRING(i) + ' Records' + CHR(10)).
END PROCEDURE.
 
PROCEDURE fixUSRX:
  DEFINE VARIABLE i AS INTEGER NO-UNDO.

  DISABLE TRIGGERS FOR LOAD OF usrx.
 
  IF NOT CAN-FIND(FIRST usrx WHERE usrx.rec_key EQ '') THEN RETURN.
  RUN msg ('Processing usrx ... ').
  FOR EACH usrx EXCLUSIVE-LOCK WHERE usrx.rec_key EQ '':
    ASSIGN
      usrx.rec_key = nextRecKey()
      i = i + 1.
    RUN createRecKey (usrx.rec_key,'usrx').
  END.
  RUN msg ('Fixed ' + STRING(i) + ' Records' + CHR(10)).
END PROCEDURE.
 
PROCEDURE fixVEND:
  DEFINE VARIABLE i AS INTEGER NO-UNDO.

  DISABLE TRIGGERS FOR LOAD OF vend.
 
  IF NOT CAN-FIND(FIRST vend WHERE vend.rec_key EQ '') THEN RETURN.
  RUN msg ('Processing vend ... ').
  FOR EACH vend EXCLUSIVE-LOCK WHERE vend.rec_key EQ '':
    ASSIGN
      vend.rec_key = nextRecKey()
      i = i + 1.
    RUN createRecKey (vend.rec_key,'vend').
  END.
  RUN msg ('Fixed ' + STRING(i) + ' Records' + CHR(10)).
END PROCEDURE.
 
PROCEDURE fixVENTYPE:
  DEFINE VARIABLE i AS INTEGER NO-UNDO.

  DISABLE TRIGGERS FOR LOAD OF ventype.
 
  IF NOT CAN-FIND(FIRST ventype WHERE ventype.rec_key EQ '') THEN RETURN.
  RUN msg ('Processing ventype ... ').
  FOR EACH ventype EXCLUSIVE-LOCK WHERE ventype.rec_key EQ '':
    ASSIGN
      ventype.rec_key = nextRecKey()
      i = i + 1.
    RUN createRecKey (ventype.rec_key,'ventype').
  END.
  RUN msg ('Fixed ' + STRING(i) + ' Records' + CHR(10)).
END PROCEDURE.
 
PROCEDURE fixWASTE-TYPE:
  DEFINE VARIABLE i AS INTEGER NO-UNDO.

  DISABLE TRIGGERS FOR LOAD OF waste-type.
 
  IF NOT CAN-FIND(FIRST waste-type WHERE waste-type.rec_key EQ '') THEN RETURN.
  RUN msg ('Processing waste-type ... ').
  FOR EACH waste-type EXCLUSIVE-LOCK WHERE waste-type.rec_key EQ '':
    ASSIGN
      waste-type.rec_key = nextRecKey()
      i = i + 1.
    RUN createRecKey (waste-type.rec_key,'waste-type').
  END.
  RUN msg ('Fixed ' + STRING(i) + ' Records' + CHR(10)).
END PROCEDURE.
