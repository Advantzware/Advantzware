/* est-flm.p - generated 01/10/2001 - 13:00:02 by nosweat */

/* this program was generated by auditgn1.p and should not be modified */
/* modifications should be made to the auto generation program         */

DEFINE INPUT PARAMETER action AS CHARACTER NO-UNDO.

DEFINE VARIABLE i AS INTEGER NO-UNDO.

{methods/defines/audit.i}
FIND FIRST ttbl_header.

DEFINE WORK-TABLE ttbl_est-flm1 NO-UNDO LIKE est-flm.
DEFINE WORK-TABLE ttbl_est-flm2 NO-UNDO LIKE est-flm.

CREATE ttbl_est-flm1.
IMPORT STREAM s_audit_file ttbl_est-flm1.
CREATE ttbl_est-flm2.
IF action = "UPDATE" THEN
IMPORT STREAM s_audit_file ttbl_est-flm2.

PUT STREAM s-audit-rpt UNFORMATTED
  "Action    User ID/(User Name)            DB.Table/(Database)  Date       Time" SKIP
  "--------- ------------------------------ -------------------- ---------- --------" SKIP
  ttbl_header.v_action AT 1
  ttbl_header.v_userid AT 11
  ttbl_header.v_file AT 42
  ttbl_header.v_date FORMAT "99/99/9999" AT 63
  STRING(ttbl_header.v_time,"HH:MM:SS") AT 74.
FIND users WHERE users.user_id = v_userid NO-LOCK NO-ERROR.
IF AVAILABLE users THEN
PUT STREAM s-audit-rpt UNFORMATTED "(" AT 10 users.user_name ")".
ELSE
PUT STREAM s-audit-rpt UNFORMATTED "(user record does not exist)" AT 10.
PUT STREAM s-audit-rpt UNFORMATTED
  "(" AT 41 ttbl_header.v_db ")" SKIP(1)
  "Label: Value (database field-name)" AT 3.
IF action = "UPDATE" THEN
PUT STREAM s-audit-rpt UNFORMATTED " ( * - denotes field value change )".
PUT STREAM s-audit-rpt UNFORMATTED FILL("-",75) AT 3.
IF NOT v_field_changed OR ttbl_est-flm1.bnum NE ttbl_est-flm2.bnum OR action NE "UPDATE" THEN
DO:
  PUT STREAM s-audit-rpt UNFORMATTED "Blank #: " AT 3 ttbl_est-flm1.bnum FORMAT ">9" " (bnum)".
  IF action = "UPDATE" THEN
  DO:
    IF ttbl_est-flm1.bnum NE ttbl_est-flm2.bnum THEN
    PUT STREAM s-audit-rpt UNFORMATTED "*" AT 1.
    PUT STREAM s-audit-rpt UNFORMATTED "Blank #: " AT 3 ttbl_est-flm2.bnum FORMAT ">9".
  END.
END.
IF NOT v_field_changed OR ttbl_est-flm1.cost NE ttbl_est-flm2.cost OR action NE "UPDATE" THEN
DO:
  PUT STREAM s-audit-rpt UNFORMATTED "Cost/M: " AT 3 ttbl_est-flm1.cost FORMAT ">>9.99999" " (cost)".
  IF action = "UPDATE" THEN
  DO:
    IF ttbl_est-flm1.cost NE ttbl_est-flm2.cost THEN
    PUT STREAM s-audit-rpt UNFORMATTED "*" AT 1.
    PUT STREAM s-audit-rpt UNFORMATTED "Cost/M: " AT 3 ttbl_est-flm2.cost FORMAT ">>9.99999".
  END.
END.
IF NOT v_field_changed OR ttbl_est-flm1.dscr NE ttbl_est-flm2.dscr OR action NE "UPDATE" THEN
DO:
  PUT STREAM s-audit-rpt UNFORMATTED "Description: " AT 3 ttbl_est-flm1.dscr " (dscr)".
  IF action = "UPDATE" THEN
  DO:
    IF ttbl_est-flm1.dscr NE ttbl_est-flm2.dscr THEN
    PUT STREAM s-audit-rpt UNFORMATTED "*" AT 1.
    PUT STREAM s-audit-rpt UNFORMATTED "Description: " AT 3 ttbl_est-flm2.dscr.
  END.
END.
IF NOT v_field_changed OR ttbl_est-flm1.e-num NE ttbl_est-flm2.e-num OR action NE "UPDATE" THEN
DO:
  PUT STREAM s-audit-rpt UNFORMATTED "Est.#: " AT 3 ttbl_est-flm1.e-num FORMAT ">>>>>9" " (e-num)".
  IF action = "UPDATE" THEN
  DO:
    IF ttbl_est-flm1.e-num NE ttbl_est-flm2.e-num THEN
    PUT STREAM s-audit-rpt UNFORMATTED "*" AT 1.
    PUT STREAM s-audit-rpt UNFORMATTED "Est.#: " AT 3 ttbl_est-flm2.e-num FORMAT ">>>>>9".
  END.
END.
IF NOT v_field_changed OR ttbl_est-flm1.i-no NE ttbl_est-flm2.i-no OR action NE "UPDATE" THEN
DO:
  PUT STREAM s-audit-rpt UNFORMATTED "Item No: " AT 3 ttbl_est-flm1.i-no " (i-no)".
  IF action = "UPDATE" THEN
  DO:
    IF ttbl_est-flm1.i-no NE ttbl_est-flm2.i-no THEN
    PUT STREAM s-audit-rpt UNFORMATTED "*" AT 1.
    PUT STREAM s-audit-rpt UNFORMATTED "Item No: " AT 3 ttbl_est-flm2.i-no.
  END.
END.
IF NOT v_field_changed OR ttbl_est-flm1.len NE ttbl_est-flm2.len OR action NE "UPDATE" THEN
DO:
  PUT STREAM s-audit-rpt UNFORMATTED "Length: " AT 3 ttbl_est-flm1.len FORMAT ">9.9999" " (len)".
  IF action = "UPDATE" THEN
  DO:
    IF ttbl_est-flm1.len NE ttbl_est-flm2.len THEN
    PUT STREAM s-audit-rpt UNFORMATTED "*" AT 1.
    PUT STREAM s-audit-rpt UNFORMATTED "Length: " AT 3 ttbl_est-flm2.len FORMAT ">9.9999".
  END.
END.
IF NOT v_field_changed OR ttbl_est-flm1.line NE ttbl_est-flm2.line OR action NE "UPDATE" THEN
DO:
  PUT STREAM s-audit-rpt UNFORMATTED "Line #: " AT 3 ttbl_est-flm1.line FORMAT ">9" " (line)".
  IF action = "UPDATE" THEN
  DO:
    IF ttbl_est-flm1.line NE ttbl_est-flm2.line THEN
    PUT STREAM s-audit-rpt UNFORMATTED "*" AT 1.
    PUT STREAM s-audit-rpt UNFORMATTED "Line #: " AT 3 ttbl_est-flm2.line FORMAT ">9".
  END.
END.
IF NOT v_field_changed OR ttbl_est-flm1.snum NE ttbl_est-flm2.snum OR action NE "UPDATE" THEN
DO:
  PUT STREAM s-audit-rpt UNFORMATTED "Sheet #: " AT 3 ttbl_est-flm1.snum FORMAT ">9" " (snum)".
  IF action = "UPDATE" THEN
  DO:
    IF ttbl_est-flm1.snum NE ttbl_est-flm2.snum THEN
    PUT STREAM s-audit-rpt UNFORMATTED "*" AT 1.
    PUT STREAM s-audit-rpt UNFORMATTED "Sheet #: " AT 3 ttbl_est-flm2.snum FORMAT ">9".
  END.
END.
IF NOT v_field_changed OR ttbl_est-flm1.wid NE ttbl_est-flm2.wid OR action NE "UPDATE" THEN
DO:
  PUT STREAM s-audit-rpt UNFORMATTED "Width: " AT 3 ttbl_est-flm1.wid FORMAT ">9.9999" " (wid)".
  IF action = "UPDATE" THEN
  DO:
    IF ttbl_est-flm1.wid NE ttbl_est-flm2.wid THEN
    PUT STREAM s-audit-rpt UNFORMATTED "*" AT 1.
    PUT STREAM s-audit-rpt UNFORMATTED "Width: " AT 3 ttbl_est-flm2.wid FORMAT ">9.9999".
  END.
END.
IF v_show_index THEN
DO:
  PUT STREAM s-audit-rpt UNFORMATTED "Index Fields - Label: Value (database field-name)" AT 5.
  PUT STREAM s-audit-rpt UNFORMATTED "Est.#: " AT 7.
  IF action = "UPDATE" THEN
  PUT STREAM s-audit-rpt UNFORMATTED ttbl_est-flm2.e-num FORMAT ">>>>>9" " (e-num)".
  ELSE
  PUT STREAM s-audit-rpt UNFORMATTED ttbl_est-flm1.e-num FORMAT ">>>>>9" " (e-num)".
  PUT STREAM s-audit-rpt UNFORMATTED "Line #: " AT 7.
  IF action = "UPDATE" THEN
  PUT STREAM s-audit-rpt UNFORMATTED ttbl_est-flm2.line FORMAT ">9" " (line)".
  ELSE
  PUT STREAM s-audit-rpt UNFORMATTED ttbl_est-flm1.line FORMAT ">9" " (line)".
END.
PUT STREAM s-audit-rpt UNFORMATTED SKIP(1).
