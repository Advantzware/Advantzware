/* job-all.p - generated 01/10/2001 - 13:00:04 by nosweat */

/* this program was generated by auditgn1.p and should not be modified */
/* modifications should be made to the auto generation program         */

DEFINE INPUT PARAMETER action AS CHARACTER NO-UNDO.

DEFINE VARIABLE i AS INTEGER NO-UNDO.

{methods/defines/audit.i}
FIND FIRST ttbl_header.

DEFINE WORK-TABLE ttbl_job-all1 NO-UNDO LIKE job-all.
DEFINE WORK-TABLE ttbl_job-all2 NO-UNDO LIKE job-all.

CREATE ttbl_job-all1.
IMPORT STREAM s_audit_file ttbl_job-all1.
CREATE ttbl_job-all2.
IF action = "UPDATE" THEN
IMPORT STREAM s_audit_file ttbl_job-all2.

PUT STREAM s-audit-rpt UNFORMATTED
  "Action    User ID/(User Name)            DB.Table/(Database)  Date       Time" SKIP
  "--------- ------------------------------ -------------------- ---------- --------" SKIP
  ttbl_header.v_action AT 1
  ttbl_header.v_userid AT 11
  ttbl_header.v_file AT 42
  ttbl_header.v_date FORMAT "99/99/9999" AT 63
  STRING(ttbl_header.v_time,"HH:MM:SS") AT 74.
FIND users WHERE users.user_id = v_userid NO-LOCK NO-ERROR.
IF AVAILABLE users THEN
PUT STREAM s-audit-rpt UNFORMATTED "(" AT 10 users.user_name ")".
ELSE
PUT STREAM s-audit-rpt UNFORMATTED "(user record does not exist)" AT 10.
PUT STREAM s-audit-rpt UNFORMATTED
  "(" AT 41 ttbl_header.v_db ")" SKIP(1)
  "Label: Value (database field-name)" AT 3.
IF action = "UPDATE" THEN
PUT STREAM s-audit-rpt UNFORMATTED " ( * - denotes field value change )".
PUT STREAM s-audit-rpt UNFORMATTED FILL("-",75) AT 3.
IF NOT v_field_changed OR ttbl_job-all1.blank-no NE ttbl_job-all2.blank-no OR action NE "UPDATE" THEN
DO:
  PUT STREAM s-audit-rpt UNFORMATTED "BLANK: " AT 3 ttbl_job-all1.blank-no FORMAT ">9" " (blank-no)".
  IF action = "UPDATE" THEN
  DO:
    IF ttbl_job-all1.blank-no NE ttbl_job-all2.blank-no THEN
    PUT STREAM s-audit-rpt UNFORMATTED "*" AT 1.
    PUT STREAM s-audit-rpt UNFORMATTED "BLANK: " AT 3 ttbl_job-all2.blank-no FORMAT ">9".
  END.
END.
IF NOT v_field_changed OR ttbl_job-all1.company NE ttbl_job-all2.company OR action NE "UPDATE" THEN
DO:
  PUT STREAM s-audit-rpt UNFORMATTED "Company: " AT 3 ttbl_job-all1.company " (company)".
  IF action = "UPDATE" THEN
  DO:
    IF ttbl_job-all1.company NE ttbl_job-all2.company THEN
    PUT STREAM s-audit-rpt UNFORMATTED "*" AT 1.
    PUT STREAM s-audit-rpt UNFORMATTED "Company: " AT 3 ttbl_job-all2.company.
  END.
END.
IF NOT v_field_changed OR ttbl_job-all1.frm NE ttbl_job-all2.frm OR action NE "UPDATE" THEN
DO:
  PUT STREAM s-audit-rpt UNFORMATTED "Form: " AT 3 ttbl_job-all1.frm FORMAT ">>9" " (frm)".
  IF action = "UPDATE" THEN
  DO:
    IF ttbl_job-all1.frm NE ttbl_job-all2.frm THEN
    PUT STREAM s-audit-rpt UNFORMATTED "*" AT 1.
    PUT STREAM s-audit-rpt UNFORMATTED "Form: " AT 3 ttbl_job-all2.frm FORMAT ">>9".
  END.
END.
IF NOT v_field_changed OR ttbl_job-all1.job NE ttbl_job-all2.job OR action NE "UPDATE" THEN
DO:
  PUT STREAM s-audit-rpt UNFORMATTED "Unique Internial Job Number: " AT 3 ttbl_job-all1.job FORMAT ">>>>>>9" " (job)".
  IF action = "UPDATE" THEN
  DO:
    IF ttbl_job-all1.job NE ttbl_job-all2.job THEN
    PUT STREAM s-audit-rpt UNFORMATTED "*" AT 1.
    PUT STREAM s-audit-rpt UNFORMATTED "Unique Internial Job Number: " AT 3 ttbl_job-all2.job FORMAT ">>>>>>9".
  END.
END.
IF NOT v_field_changed OR ttbl_job-all1.job-no NE ttbl_job-all2.job-no OR action NE "UPDATE" THEN
DO:
  PUT STREAM s-audit-rpt UNFORMATTED "Job Number: " AT 3 ttbl_job-all1.job-no " (job-no)".
  IF action = "UPDATE" THEN
  DO:
    IF ttbl_job-all1.job-no NE ttbl_job-all2.job-no THEN
    PUT STREAM s-audit-rpt UNFORMATTED "*" AT 1.
    PUT STREAM s-audit-rpt UNFORMATTED "Job Number: " AT 3 ttbl_job-all2.job-no.
  END.
END.
IF NOT v_field_changed OR ttbl_job-all1.job-no2 NE ttbl_job-all2.job-no2 OR action NE "UPDATE" THEN
DO:
  PUT STREAM s-audit-rpt UNFORMATTED "Run #: " AT 3 ttbl_job-all1.job-no2 FORMAT ">9" " (job-no2)".
  IF action = "UPDATE" THEN
  DO:
    IF ttbl_job-all1.job-no2 NE ttbl_job-all2.job-no2 THEN
    PUT STREAM s-audit-rpt UNFORMATTED "*" AT 1.
    PUT STREAM s-audit-rpt UNFORMATTED "Run #: " AT 3 ttbl_job-all2.job-no2 FORMAT ">9".
  END.
END.
IF NOT v_field_changed OR ttbl_job-all1.qty NE ttbl_job-all2.qty OR action NE "UPDATE" THEN
DO:
  PUT STREAM s-audit-rpt UNFORMATTED "Tot MRP: " AT 3 ttbl_job-all1.qty FORMAT ">>>,>>9.99<" " (qty)".
  IF action = "UPDATE" THEN
  DO:
    IF ttbl_job-all1.qty NE ttbl_job-all2.qty THEN
    PUT STREAM s-audit-rpt UNFORMATTED "*" AT 1.
    PUT STREAM s-audit-rpt UNFORMATTED "Tot MRP: " AT 3 ttbl_job-all2.qty FORMAT ">>>,>>9.99<".
  END.
END.
IF NOT v_field_changed OR ttbl_job-all1.qty-uom NE ttbl_job-all2.qty-uom OR action NE "UPDATE" THEN
DO:
  PUT STREAM s-audit-rpt UNFORMATTED "UOM: " AT 3 ttbl_job-all1.qty-uom " (qty-uom)".
  IF action = "UPDATE" THEN
  DO:
    IF ttbl_job-all1.qty-uom NE ttbl_job-all2.qty-uom THEN
    PUT STREAM s-audit-rpt UNFORMATTED "*" AT 1.
    PUT STREAM s-audit-rpt UNFORMATTED "UOM: " AT 3 ttbl_job-all2.qty-uom.
  END.
END.
IF NOT v_field_changed OR ttbl_job-all1.rm-i-no NE ttbl_job-all2.rm-i-no OR action NE "UPDATE" THEN
DO:
  PUT STREAM s-audit-rpt UNFORMATTED "Raw Material Item Number: " AT 3 ttbl_job-all1.rm-i-no " (rm-i-no)".
  IF action = "UPDATE" THEN
  DO:
    IF ttbl_job-all1.rm-i-no NE ttbl_job-all2.rm-i-no THEN
    PUT STREAM s-audit-rpt UNFORMATTED "*" AT 1.
    PUT STREAM s-audit-rpt UNFORMATTED "Raw Material Item Number: " AT 3 ttbl_job-all2.rm-i-no.
  END.
END.
IF v_show_index THEN
DO:
  PUT STREAM s-audit-rpt UNFORMATTED "Index Fields - Label: Value (database field-name)" AT 5.
  PUT STREAM s-audit-rpt UNFORMATTED "Company: " AT 7.
  IF action = "UPDATE" THEN
  PUT STREAM s-audit-rpt UNFORMATTED ttbl_job-all2.company " (company)".
  ELSE
  PUT STREAM s-audit-rpt UNFORMATTED ttbl_job-all1.company " (company)".
  PUT STREAM s-audit-rpt UNFORMATTED "Unique Internial Job Number: " AT 7.
  IF action = "UPDATE" THEN
  PUT STREAM s-audit-rpt UNFORMATTED ttbl_job-all2.job FORMAT ">>>>>>9" " (job)".
  ELSE
  PUT STREAM s-audit-rpt UNFORMATTED ttbl_job-all1.job FORMAT ">>>>>>9" " (job)".
  PUT STREAM s-audit-rpt UNFORMATTED "Form: " AT 7.
  IF action = "UPDATE" THEN
  PUT STREAM s-audit-rpt UNFORMATTED ttbl_job-all2.frm FORMAT ">>9" " (frm)".
  ELSE
  PUT STREAM s-audit-rpt UNFORMATTED ttbl_job-all1.frm FORMAT ">>9" " (frm)".
  PUT STREAM s-audit-rpt UNFORMATTED "BLANK: " AT 7.
  IF action = "UPDATE" THEN
  PUT STREAM s-audit-rpt UNFORMATTED ttbl_job-all2.blank-no FORMAT ">9" " (blank-no)".
  ELSE
  PUT STREAM s-audit-rpt UNFORMATTED ttbl_job-all1.blank-no FORMAT ">9" " (blank-no)".
  PUT STREAM s-audit-rpt UNFORMATTED "Raw Material Item Number: " AT 7.
  IF action = "UPDATE" THEN
  PUT STREAM s-audit-rpt UNFORMATTED ttbl_job-all2.rm-i-no " (rm-i-no)".
  ELSE
  PUT STREAM s-audit-rpt UNFORMATTED ttbl_job-all1.rm-i-no " (rm-i-no)".
END.
PUT STREAM s-audit-rpt UNFORMATTED SKIP(1).
