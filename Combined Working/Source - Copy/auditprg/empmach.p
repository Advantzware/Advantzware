/* empmach.p - generated 12/02/2005 - 16:52:38 by NoSweat */

/* this program was generated by auditgn1.p and should not be modified */
/* modifications should be made to the auto generation program         */

DEFINE INPUT PARAMETER action AS CHARACTER NO-UNDO.

DEFINE VARIABLE i AS INTEGER NO-UNDO.

{methods/defines/audit.i}
FIND FIRST ttbl_header.

DEFINE WORK-TABLE ttbl_empmach1 NO-UNDO LIKE empmach.
DEFINE WORK-TABLE ttbl_empmach2 NO-UNDO LIKE empmach.

CREATE ttbl_empmach1.
IMPORT STREAM s_audit_file ttbl_empmach1.
CREATE ttbl_empmach2.
IF action = "UPDATE" THEN
IMPORT STREAM s_audit_file ttbl_empmach2.

PUT STREAM s-audit-rpt UNFORMATTED
  "Action    User ID/(User Name)            DB.Table/(Database)  Date       Time" SKIP
  "--------- ------------------------------ -------------------- ---------- --------" SKIP
  ttbl_header.v_action AT 1
  ttbl_header.v_userid AT 11
  ttbl_header.v_file AT 42
  ttbl_header.v_date FORMAT "99/99/9999" AT 63
  STRING(ttbl_header.v_time,"HH:MM:SS") AT 74.
FIND users WHERE users.user_id = v_userid NO-LOCK NO-ERROR.
IF AVAILABLE users THEN
PUT STREAM s-audit-rpt UNFORMATTED "(" AT 10 users.user_name ")".
ELSE
PUT STREAM s-audit-rpt UNFORMATTED "(user record does not exist)" AT 10.
PUT STREAM s-audit-rpt UNFORMATTED
  "(" AT 41 ttbl_header.v_db ")" SKIP(1)
  "Label: Value (database field-name)" AT 3.
IF action = "UPDATE" THEN
PUT STREAM s-audit-rpt UNFORMATTED " ( * - denotes field value change )".
PUT STREAM s-audit-rpt UNFORMATTED FILL("-",75) AT 3.
IF NOT v_field_changed OR ttbl_empmach1.company NE ttbl_empmach2.company OR action NE "UPDATE" THEN
DO:
  PUT STREAM s-audit-rpt UNFORMATTED "Company: " AT 3 ttbl_empmach1.company " (company)".
  IF action = "UPDATE" THEN
  DO:
    IF ttbl_empmach1.company NE ttbl_empmach2.company THEN
    PUT STREAM s-audit-rpt UNFORMATTED "*" AT 1.
    PUT STREAM s-audit-rpt UNFORMATTED "Company: " AT 3 ttbl_empmach2.company.
  END.
END.
IF NOT v_field_changed OR ttbl_empmach1.employee NE ttbl_empmach2.employee OR action NE "UPDATE" THEN
DO:
  PUT STREAM s-audit-rpt UNFORMATTED "Emp ID: " AT 3 ttbl_empmach1.employee " (employee)".
  IF action = "UPDATE" THEN
  DO:
    IF ttbl_empmach1.employee NE ttbl_empmach2.employee THEN
    PUT STREAM s-audit-rpt UNFORMATTED "*" AT 1.
    PUT STREAM s-audit-rpt UNFORMATTED "Emp ID: " AT 3 ttbl_empmach2.employee.
  END.
END.
IF NOT v_field_changed OR ttbl_empmach1.gl_account NE ttbl_empmach2.gl_account OR action NE "UPDATE" THEN
DO:
  PUT STREAM s-audit-rpt UNFORMATTED "G/L Account: " AT 3 ttbl_empmach1.gl_account " (gl_account)".
  IF action = "UPDATE" THEN
  DO:
    IF ttbl_empmach1.gl_account NE ttbl_empmach2.gl_account THEN
    PUT STREAM s-audit-rpt UNFORMATTED "*" AT 1.
    PUT STREAM s-audit-rpt UNFORMATTED "G/L Account: " AT 3 ttbl_empmach2.gl_account.
  END.
END.
IF NOT v_field_changed OR ttbl_empmach1.machine NE ttbl_empmach2.machine OR action NE "UPDATE" THEN
DO:
  PUT STREAM s-audit-rpt UNFORMATTED "Machine: " AT 3 ttbl_empmach1.machine " (machine)".
  IF action = "UPDATE" THEN
  DO:
    IF ttbl_empmach1.machine NE ttbl_empmach2.machine THEN
    PUT STREAM s-audit-rpt UNFORMATTED "*" AT 1.
    PUT STREAM s-audit-rpt UNFORMATTED "Machine: " AT 3 ttbl_empmach2.machine.
  END.
END.
IF v_show_index THEN
DO:
  PUT STREAM s-audit-rpt UNFORMATTED "Index Fields - Label: Value (database field-name)" AT 5.
  PUT STREAM s-audit-rpt UNFORMATTED "Company: " AT 7.
  IF action = "UPDATE" THEN
  PUT STREAM s-audit-rpt UNFORMATTED ttbl_empmach2.company " (company)".
  ELSE
  PUT STREAM s-audit-rpt UNFORMATTED ttbl_empmach1.company " (company)".
  PUT STREAM s-audit-rpt UNFORMATTED "Emp ID: " AT 7.
  IF action = "UPDATE" THEN
  PUT STREAM s-audit-rpt UNFORMATTED ttbl_empmach2.employee " (employee)".
  ELSE
  PUT STREAM s-audit-rpt UNFORMATTED ttbl_empmach1.employee " (employee)".
  PUT STREAM s-audit-rpt UNFORMATTED "Machine: " AT 7.
  IF action = "UPDATE" THEN
  PUT STREAM s-audit-rpt UNFORMATTED ttbl_empmach2.machine " (machine)".
  ELSE
  PUT STREAM s-audit-rpt UNFORMATTED ttbl_empmach1.machine " (machine)".
END.
PUT STREAM s-audit-rpt UNFORMATTED SKIP(1).
