/* inv-head.p - generated 12/09/2005 - 15:42:46 by NoSweat */

/* this program was generated by auditgn1.p and should not be modified */
/* modifications should be made to the auto generation program         */

DEFINE INPUT PARAMETER action AS CHARACTER NO-UNDO.

DEFINE VARIABLE i AS INTEGER NO-UNDO.

{methods/defines/audit.i}
FIND FIRST ttbl_header.

DEFINE WORK-TABLE ttbl_inv-head1 NO-UNDO LIKE inv-head.
DEFINE WORK-TABLE ttbl_inv-head2 NO-UNDO LIKE inv-head.

CREATE ttbl_inv-head1.
IMPORT STREAM s_audit_file ttbl_inv-head1.
CREATE ttbl_inv-head2.
IF action = "UPDATE" THEN
IMPORT STREAM s_audit_file ttbl_inv-head2.

PUT STREAM s-audit-rpt UNFORMATTED
  "Action    User ID/(User Name)            DB.Table/(Database)  Date       Time" SKIP
  "--------- ------------------------------ -------------------- ---------- --------" SKIP
  ttbl_header.v_action AT 1
  ttbl_header.v_userid AT 11
  ttbl_header.v_file AT 42
  ttbl_header.v_date FORMAT "99/99/9999" AT 63
  STRING(ttbl_header.v_time,"HH:MM:SS") AT 74.
FIND users WHERE users.user_id = v_userid NO-LOCK NO-ERROR.
IF AVAILABLE users THEN
PUT STREAM s-audit-rpt UNFORMATTED "(" AT 10 users.user_name ")".
ELSE
PUT STREAM s-audit-rpt UNFORMATTED "(user record does not exist)" AT 10.
PUT STREAM s-audit-rpt UNFORMATTED
  "(" AT 41 ttbl_header.v_db ")" SKIP(1)
  "Label: Value (database field-name)" AT 3.
IF action = "UPDATE" THEN
PUT STREAM s-audit-rpt UNFORMATTED " ( * - denotes field value change )".
PUT STREAM s-audit-rpt UNFORMATTED FILL("-",75) AT 3.
DO i = 1 TO 2:
IF NOT v_field_changed OR ttbl_inv-head1.addr[i] NE ttbl_inv-head2.addr[i] OR action NE "UPDATE" THEN
DO:
  PUT STREAM s-audit-rpt UNFORMATTED "Customer Address[" AT 3 i "]: " ttbl_inv-head1.addr[i] " (addr[" i "])".
  IF action = "UPDATE" THEN
  DO:
    IF ttbl_inv-head1.addr[i] NE ttbl_inv-head2.addr[i] THEN
    PUT STREAM s-audit-rpt UNFORMATTED "*" AT 1.
    PUT STREAM s-audit-rpt UNFORMATTED "Customer Address[" AT 3 i "]: " ttbl_inv-head2.addr[i].
  END.
END.
END.
IF NOT v_field_changed OR ttbl_inv-head1.bank-code NE ttbl_inv-head2.bank-code OR action NE "UPDATE" THEN
DO:
  PUT STREAM s-audit-rpt UNFORMATTED "Bank Code: " AT 3 ttbl_inv-head1.bank-code " (bank-code)".
  IF action = "UPDATE" THEN
  DO:
    IF ttbl_inv-head1.bank-code NE ttbl_inv-head2.bank-code THEN
    PUT STREAM s-audit-rpt UNFORMATTED "*" AT 1.
    PUT STREAM s-audit-rpt UNFORMATTED "Bank Code: " AT 3 ttbl_inv-head2.bank-code.
  END.
END.
DO i = 1 TO 4:
IF NOT v_field_changed OR ttbl_inv-head1.bill-i[i] NE ttbl_inv-head2.bill-i[i] OR action NE "UPDATE" THEN
DO:
  PUT STREAM s-audit-rpt UNFORMATTED "Billing Instructions[" AT 3 i "]: " ttbl_inv-head1.bill-i[i] " (bill-i[" i "])".
  IF action = "UPDATE" THEN
  DO:
    IF ttbl_inv-head1.bill-i[i] NE ttbl_inv-head2.bill-i[i] THEN
    PUT STREAM s-audit-rpt UNFORMATTED "*" AT 1.
    PUT STREAM s-audit-rpt UNFORMATTED "Billing Instructions[" AT 3 i "]: " ttbl_inv-head2.bill-i[i].
  END.
END.
END.
IF NOT v_field_changed OR ttbl_inv-head1.bill-to NE ttbl_inv-head2.bill-to OR action NE "UPDATE" THEN
DO:
  PUT STREAM s-audit-rpt UNFORMATTED "Bill to: " AT 3 ttbl_inv-head1.bill-to " (bill-to)".
  IF action = "UPDATE" THEN
  DO:
    IF ttbl_inv-head1.bill-to NE ttbl_inv-head2.bill-to THEN
    PUT STREAM s-audit-rpt UNFORMATTED "*" AT 1.
    PUT STREAM s-audit-rpt UNFORMATTED "Bill to: " AT 3 ttbl_inv-head2.bill-to.
  END.
END.
IF NOT v_field_changed OR ttbl_inv-head1.bol-no NE ttbl_inv-head2.bol-no OR action NE "UPDATE" THEN
DO:
  PUT STREAM s-audit-rpt UNFORMATTED "Bill Of Lading Number: " AT 3 ttbl_inv-head1.bol-no FORMAT ">>>>>>>9" " (bol-no)".
  IF action = "UPDATE" THEN
  DO:
    IF ttbl_inv-head1.bol-no NE ttbl_inv-head2.bol-no THEN
    PUT STREAM s-audit-rpt UNFORMATTED "*" AT 1.
    PUT STREAM s-audit-rpt UNFORMATTED "Bill Of Lading Number: " AT 3 ttbl_inv-head2.bol-no FORMAT ">>>>>>>9".
  END.
END.
IF NOT v_field_changed OR ttbl_inv-head1.carrier NE ttbl_inv-head2.carrier OR action NE "UPDATE" THEN
DO:
  PUT STREAM s-audit-rpt UNFORMATTED "Carrier: " AT 3 ttbl_inv-head1.carrier " (carrier)".
  IF action = "UPDATE" THEN
  DO:
    IF ttbl_inv-head1.carrier NE ttbl_inv-head2.carrier THEN
    PUT STREAM s-audit-rpt UNFORMATTED "*" AT 1.
    PUT STREAM s-audit-rpt UNFORMATTED "Carrier: " AT 3 ttbl_inv-head2.carrier.
  END.
END.
IF NOT v_field_changed OR ttbl_inv-head1.cc-auth NE ttbl_inv-head2.cc-auth OR action NE "UPDATE" THEN
DO:
  PUT STREAM s-audit-rpt UNFORMATTED "Credit Card Authorization: " AT 3 ttbl_inv-head1.cc-auth " (cc-auth)".
  IF action = "UPDATE" THEN
  DO:
    IF ttbl_inv-head1.cc-auth NE ttbl_inv-head2.cc-auth THEN
    PUT STREAM s-audit-rpt UNFORMATTED "*" AT 1.
    PUT STREAM s-audit-rpt UNFORMATTED "Credit Card Authorization: " AT 3 ttbl_inv-head2.cc-auth.
  END.
END.
IF NOT v_field_changed OR ttbl_inv-head1.cc-expiration NE ttbl_inv-head2.cc-expiration OR action NE "UPDATE" THEN
DO:
  PUT STREAM s-audit-rpt UNFORMATTED "Credit Card Expiration: " AT 3 ttbl_inv-head1.cc-expiration FORMAT "99/99/9999" " (cc-expiration)".
  IF action = "UPDATE" THEN
  DO:
    IF ttbl_inv-head1.cc-expiration NE ttbl_inv-head2.cc-expiration THEN
    PUT STREAM s-audit-rpt UNFORMATTED "*" AT 1.
    PUT STREAM s-audit-rpt UNFORMATTED "Credit Card Expiration: " AT 3 ttbl_inv-head2.cc-expiration FORMAT "99/99/9999".
  END.
END.
IF NOT v_field_changed OR ttbl_inv-head1.cc-name NE ttbl_inv-head2.cc-name OR action NE "UPDATE" THEN
DO:
  PUT STREAM s-audit-rpt UNFORMATTED "Credit Card Holders Name: " AT 3 ttbl_inv-head1.cc-name " (cc-name)".
  IF action = "UPDATE" THEN
  DO:
    IF ttbl_inv-head1.cc-name NE ttbl_inv-head2.cc-name THEN
    PUT STREAM s-audit-rpt UNFORMATTED "*" AT 1.
    PUT STREAM s-audit-rpt UNFORMATTED "Credit Card Holders Name: " AT 3 ttbl_inv-head2.cc-name.
  END.
END.
IF NOT v_field_changed OR ttbl_inv-head1.cc-num NE ttbl_inv-head2.cc-num OR action NE "UPDATE" THEN
DO:
  PUT STREAM s-audit-rpt UNFORMATTED "Credit Card Number: " AT 3 ttbl_inv-head1.cc-num " (cc-num)".
  IF action = "UPDATE" THEN
  DO:
    IF ttbl_inv-head1.cc-num NE ttbl_inv-head2.cc-num THEN
    PUT STREAM s-audit-rpt UNFORMATTED "*" AT 1.
    PUT STREAM s-audit-rpt UNFORMATTED "Credit Card Number: " AT 3 ttbl_inv-head2.cc-num.
  END.
END.
IF NOT v_field_changed OR ttbl_inv-head1.cc-type NE ttbl_inv-head2.cc-type OR action NE "UPDATE" THEN
DO:
  PUT STREAM s-audit-rpt UNFORMATTED "Credit Card Type: " AT 3 ttbl_inv-head1.cc-type " (cc-type)".
  IF action = "UPDATE" THEN
  DO:
    IF ttbl_inv-head1.cc-type NE ttbl_inv-head2.cc-type THEN
    PUT STREAM s-audit-rpt UNFORMATTED "*" AT 1.
    PUT STREAM s-audit-rpt UNFORMATTED "Credit Card Type: " AT 3 ttbl_inv-head2.cc-type.
  END.
END.
IF NOT v_field_changed OR ttbl_inv-head1.city NE ttbl_inv-head2.city OR action NE "UPDATE" THEN
DO:
  PUT STREAM s-audit-rpt UNFORMATTED "City: " AT 3 ttbl_inv-head1.city " (city)".
  IF action = "UPDATE" THEN
  DO:
    IF ttbl_inv-head1.city NE ttbl_inv-head2.city THEN
    PUT STREAM s-audit-rpt UNFORMATTED "*" AT 1.
    PUT STREAM s-audit-rpt UNFORMATTED "City: " AT 3 ttbl_inv-head2.city.
  END.
END.
IF NOT v_field_changed OR ttbl_inv-head1.company NE ttbl_inv-head2.company OR action NE "UPDATE" THEN
DO:
  PUT STREAM s-audit-rpt UNFORMATTED "Company: " AT 3 ttbl_inv-head1.company " (company)".
  IF action = "UPDATE" THEN
  DO:
    IF ttbl_inv-head1.company NE ttbl_inv-head2.company THEN
    PUT STREAM s-audit-rpt UNFORMATTED "*" AT 1.
    PUT STREAM s-audit-rpt UNFORMATTED "Company: " AT 3 ttbl_inv-head2.company.
  END.
END.
IF NOT v_field_changed OR ttbl_inv-head1.contact NE ttbl_inv-head2.contact OR action NE "UPDATE" THEN
DO:
  PUT STREAM s-audit-rpt UNFORMATTED "Contact Name: " AT 3 ttbl_inv-head1.contact " (contact)".
  IF action = "UPDATE" THEN
  DO:
    IF ttbl_inv-head1.contact NE ttbl_inv-head2.contact THEN
    PUT STREAM s-audit-rpt UNFORMATTED "*" AT 1.
    PUT STREAM s-audit-rpt UNFORMATTED "Contact Name: " AT 3 ttbl_inv-head2.contact.
  END.
END.
DO i = 1 TO 2:
IF NOT v_field_changed OR ttbl_inv-head1.curr-code[i] NE ttbl_inv-head2.curr-code[i] OR action NE "UPDATE" THEN
DO:
  PUT STREAM s-audit-rpt UNFORMATTED "Currency Code[" AT 3 i "]: " ttbl_inv-head1.curr-code[i] " (curr-code[" i "])".
  IF action = "UPDATE" THEN
  DO:
    IF ttbl_inv-head1.curr-code[i] NE ttbl_inv-head2.curr-code[i] THEN
    PUT STREAM s-audit-rpt UNFORMATTED "*" AT 1.
    PUT STREAM s-audit-rpt UNFORMATTED "Currency Code[" AT 3 i "]: " ttbl_inv-head2.curr-code[i].
  END.
END.
END.
IF NOT v_field_changed OR ttbl_inv-head1.cust-name NE ttbl_inv-head2.cust-name OR action NE "UPDATE" THEN
DO:
  PUT STREAM s-audit-rpt UNFORMATTED "Customer Name: " AT 3 ttbl_inv-head1.cust-name " (cust-name)".
  IF action = "UPDATE" THEN
  DO:
    IF ttbl_inv-head1.cust-name NE ttbl_inv-head2.cust-name THEN
    PUT STREAM s-audit-rpt UNFORMATTED "*" AT 1.
    PUT STREAM s-audit-rpt UNFORMATTED "Customer Name: " AT 3 ttbl_inv-head2.cust-name.
  END.
END.
IF NOT v_field_changed OR ttbl_inv-head1.cust-no NE ttbl_inv-head2.cust-no OR action NE "UPDATE" THEN
DO:
  PUT STREAM s-audit-rpt UNFORMATTED "Cust. #: " AT 3 ttbl_inv-head1.cust-no " (cust-no)".
  IF action = "UPDATE" THEN
  DO:
    IF ttbl_inv-head1.cust-no NE ttbl_inv-head2.cust-no THEN
    PUT STREAM s-audit-rpt UNFORMATTED "*" AT 1.
    PUT STREAM s-audit-rpt UNFORMATTED "Cust. #: " AT 3 ttbl_inv-head2.cust-no.
  END.
END.
IF NOT v_field_changed OR ttbl_inv-head1.deleted NE ttbl_inv-head2.deleted OR action NE "UPDATE" THEN
DO:
  PUT STREAM s-audit-rpt UNFORMATTED "Deleted?: " AT 3 ttbl_inv-head1.deleted FORMAT "Y/N" " (deleted)".
  IF action = "UPDATE" THEN
  DO:
    IF ttbl_inv-head1.deleted NE ttbl_inv-head2.deleted THEN
    PUT STREAM s-audit-rpt UNFORMATTED "*" AT 1.
    PUT STREAM s-audit-rpt UNFORMATTED "Deleted?: " AT 3 ttbl_inv-head2.deleted FORMAT "Y/N".
  END.
END.
IF NOT v_field_changed OR ttbl_inv-head1.ex-rate NE ttbl_inv-head2.ex-rate OR action NE "UPDATE" THEN
DO:
  PUT STREAM s-audit-rpt UNFORMATTED "?: " AT 3 ttbl_inv-head1.ex-rate FORMAT "->>,>>9.99<<<<<" " (ex-rate)".
  IF action = "UPDATE" THEN
  DO:
    IF ttbl_inv-head1.ex-rate NE ttbl_inv-head2.ex-rate THEN
    PUT STREAM s-audit-rpt UNFORMATTED "*" AT 1.
    PUT STREAM s-audit-rpt UNFORMATTED "?: " AT 3 ttbl_inv-head2.ex-rate FORMAT "->>,>>9.99<<<<<".
  END.
END.
IF NOT v_field_changed OR ttbl_inv-head1.f-bill NE ttbl_inv-head2.f-bill OR action NE "UPDATE" THEN
DO:
  PUT STREAM s-audit-rpt UNFORMATTED "Bill Freight?: " AT 3 ttbl_inv-head1.f-bill FORMAT "Y/N" " (f-bill)".
  IF action = "UPDATE" THEN
  DO:
    IF ttbl_inv-head1.f-bill NE ttbl_inv-head2.f-bill THEN
    PUT STREAM s-audit-rpt UNFORMATTED "*" AT 1.
    PUT STREAM s-audit-rpt UNFORMATTED "Bill Freight?: " AT 3 ttbl_inv-head2.f-bill FORMAT "Y/N".
  END.
END.
IF NOT v_field_changed OR ttbl_inv-head1.fob-code NE ttbl_inv-head2.fob-code OR action NE "UPDATE" THEN
DO:
  PUT STREAM s-audit-rpt UNFORMATTED "FOB Code: " AT 3 ttbl_inv-head1.fob-code " (fob-code)".
  IF action = "UPDATE" THEN
  DO:
    IF ttbl_inv-head1.fob-code NE ttbl_inv-head2.fob-code THEN
    PUT STREAM s-audit-rpt UNFORMATTED "*" AT 1.
    PUT STREAM s-audit-rpt UNFORMATTED "FOB Code: " AT 3 ttbl_inv-head2.fob-code.
  END.
END.
IF NOT v_field_changed OR ttbl_inv-head1.frt-pay NE ttbl_inv-head2.frt-pay OR action NE "UPDATE" THEN
DO:
  PUT STREAM s-audit-rpt UNFORMATTED "Freight Pay Code: " AT 3 ttbl_inv-head1.frt-pay " (frt-pay)".
  IF action = "UPDATE" THEN
  DO:
    IF ttbl_inv-head1.frt-pay NE ttbl_inv-head2.frt-pay THEN
    PUT STREAM s-audit-rpt UNFORMATTED "*" AT 1.
    PUT STREAM s-audit-rpt UNFORMATTED "Freight Pay Code: " AT 3 ttbl_inv-head2.frt-pay.
  END.
END.
IF NOT v_field_changed OR ttbl_inv-head1.fuel-bill NE ttbl_inv-head2.fuel-bill OR action NE "UPDATE" THEN
DO:
  PUT STREAM s-audit-rpt UNFORMATTED "Bill Fuel?: " AT 3 ttbl_inv-head1.fuel-bill FORMAT "Y/N" " (fuel-bill)".
  IF action = "UPDATE" THEN
  DO:
    IF ttbl_inv-head1.fuel-bill NE ttbl_inv-head2.fuel-bill THEN
    PUT STREAM s-audit-rpt UNFORMATTED "*" AT 1.
    PUT STREAM s-audit-rpt UNFORMATTED "Bill Fuel?: " AT 3 ttbl_inv-head2.fuel-bill FORMAT "Y/N".
  END.
END.
IF NOT v_field_changed OR ttbl_inv-head1.inv-date NE ttbl_inv-head2.inv-date OR action NE "UPDATE" THEN
DO:
  PUT STREAM s-audit-rpt UNFORMATTED "Invoice Date: " AT 3 ttbl_inv-head1.inv-date FORMAT "99/99/9999" " (inv-date)".
  IF action = "UPDATE" THEN
  DO:
    IF ttbl_inv-head1.inv-date NE ttbl_inv-head2.inv-date THEN
    PUT STREAM s-audit-rpt UNFORMATTED "*" AT 1.
    PUT STREAM s-audit-rpt UNFORMATTED "Invoice Date: " AT 3 ttbl_inv-head2.inv-date FORMAT "99/99/9999".
  END.
END.
IF NOT v_field_changed OR ttbl_inv-head1.inv-no NE ttbl_inv-head2.inv-no OR action NE "UPDATE" THEN
DO:
  PUT STREAM s-audit-rpt UNFORMATTED "Invoice Number: " AT 3 ttbl_inv-head1.inv-no FORMAT ">>>>>9" " (inv-no)".
  IF action = "UPDATE" THEN
  DO:
    IF ttbl_inv-head1.inv-no NE ttbl_inv-head2.inv-no THEN
    PUT STREAM s-audit-rpt UNFORMATTED "*" AT 1.
    PUT STREAM s-audit-rpt UNFORMATTED "Invoice Number: " AT 3 ttbl_inv-head2.inv-no FORMAT ">>>>>9".
  END.
END.
IF NOT v_field_changed OR ttbl_inv-head1.posted NE ttbl_inv-head2.posted OR action NE "UPDATE" THEN
DO:
  PUT STREAM s-audit-rpt UNFORMATTED "Posted: " AT 3 ttbl_inv-head1.posted FORMAT "y/n" " (posted)".
  IF action = "UPDATE" THEN
  DO:
    IF ttbl_inv-head1.posted NE ttbl_inv-head2.posted THEN
    PUT STREAM s-audit-rpt UNFORMATTED "*" AT 1.
    PUT STREAM s-audit-rpt UNFORMATTED "Posted: " AT 3 ttbl_inv-head2.posted FORMAT "y/n".
  END.
END.
IF NOT v_field_changed OR ttbl_inv-head1.printed NE ttbl_inv-head2.printed OR action NE "UPDATE" THEN
DO:
  PUT STREAM s-audit-rpt UNFORMATTED "Printed?: " AT 3 ttbl_inv-head1.printed FORMAT "Y/N" " (printed)".
  IF action = "UPDATE" THEN
  DO:
    IF ttbl_inv-head1.printed NE ttbl_inv-head2.printed THEN
    PUT STREAM s-audit-rpt UNFORMATTED "*" AT 1.
    PUT STREAM s-audit-rpt UNFORMATTED "Printed?: " AT 3 ttbl_inv-head2.printed FORMAT "Y/N".
  END.
END.
IF NOT v_field_changed OR ttbl_inv-head1.r-no NE ttbl_inv-head2.r-no OR action NE "UPDATE" THEN
DO:
  PUT STREAM s-audit-rpt UNFORMATTED "Unique Internal Number: " AT 3 ttbl_inv-head1.r-no FORMAT ">>>>>>>9" " (r-no)".
  IF action = "UPDATE" THEN
  DO:
    IF ttbl_inv-head1.r-no NE ttbl_inv-head2.r-no THEN
    PUT STREAM s-audit-rpt UNFORMATTED "*" AT 1.
    PUT STREAM s-audit-rpt UNFORMATTED "Unique Internal Number: " AT 3 ttbl_inv-head2.r-no FORMAT ">>>>>>>9".
  END.
END.
DO i = 1 TO 3:
IF NOT v_field_changed OR ttbl_inv-head1.s-comm[i] NE ttbl_inv-head2.s-comm[i] OR action NE "UPDATE" THEN
DO:
  PUT STREAM s-audit-rpt UNFORMATTED "Commission[" AT 3 i "]: " ttbl_inv-head1.s-comm[i] FORMAT ">>9.99" " (s-comm[" i "])".
  IF action = "UPDATE" THEN
  DO:
    IF ttbl_inv-head1.s-comm[i] NE ttbl_inv-head2.s-comm[i] THEN
    PUT STREAM s-audit-rpt UNFORMATTED "*" AT 1.
    PUT STREAM s-audit-rpt UNFORMATTED "Commission[" AT 3 i "]: " ttbl_inv-head2.s-comm[i] FORMAT ">>9.99".
  END.
END.
END.
DO i = 1 TO 3:
IF NOT v_field_changed OR ttbl_inv-head1.s-pct[i] NE ttbl_inv-head2.s-pct[i] OR action NE "UPDATE" THEN
DO:
  PUT STREAM s-audit-rpt UNFORMATTED "Percentage[" AT 3 i "]: " ttbl_inv-head1.s-pct[i] FORMAT ">>9.99" " (s-pct[" i "])".
  IF action = "UPDATE" THEN
  DO:
    IF ttbl_inv-head1.s-pct[i] NE ttbl_inv-head2.s-pct[i] THEN
    PUT STREAM s-audit-rpt UNFORMATTED "*" AT 1.
    PUT STREAM s-audit-rpt UNFORMATTED "Percentage[" AT 3 i "]: " ttbl_inv-head2.s-pct[i] FORMAT ">>9.99".
  END.
END.
END.
DO i = 1 TO 4:
IF NOT v_field_changed OR ttbl_inv-head1.ship-i[i] NE ttbl_inv-head2.ship-i[i] OR action NE "UPDATE" THEN
DO:
  PUT STREAM s-audit-rpt UNFORMATTED "Shipping Instructions[" AT 3 i "]: " ttbl_inv-head1.ship-i[i] " (ship-i[" i "])".
  IF action = "UPDATE" THEN
  DO:
    IF ttbl_inv-head1.ship-i[i] NE ttbl_inv-head2.ship-i[i] THEN
    PUT STREAM s-audit-rpt UNFORMATTED "*" AT 1.
    PUT STREAM s-audit-rpt UNFORMATTED "Shipping Instructions[" AT 3 i "]: " ttbl_inv-head2.ship-i[i].
  END.
END.
END.
DO i = 1 TO 3:
IF NOT v_field_changed OR ttbl_inv-head1.sman[i] NE ttbl_inv-head2.sman[i] OR action NE "UPDATE" THEN
DO:
  PUT STREAM s-audit-rpt UNFORMATTED "Salesman Code[" AT 3 i "]: " ttbl_inv-head1.sman[i] " (sman[" i "])".
  IF action = "UPDATE" THEN
  DO:
    IF ttbl_inv-head1.sman[i] NE ttbl_inv-head2.sman[i] THEN
    PUT STREAM s-audit-rpt UNFORMATTED "*" AT 1.
    PUT STREAM s-audit-rpt UNFORMATTED "Salesman Code[" AT 3 i "]: " ttbl_inv-head2.sman[i].
  END.
END.
END.
DO i = 1 TO 3:
IF NOT v_field_changed OR ttbl_inv-head1.sname[i] NE ttbl_inv-head2.sname[i] OR action NE "UPDATE" THEN
DO:
  PUT STREAM s-audit-rpt UNFORMATTED "Salesman Name[" AT 3 i "]: " ttbl_inv-head1.sname[i] " (sname[" i "])".
  IF action = "UPDATE" THEN
  DO:
    IF ttbl_inv-head1.sname[i] NE ttbl_inv-head2.sname[i] THEN
    PUT STREAM s-audit-rpt UNFORMATTED "*" AT 1.
    PUT STREAM s-audit-rpt UNFORMATTED "Salesman Name[" AT 3 i "]: " ttbl_inv-head2.sname[i].
  END.
END.
END.
DO i = 1 TO 2:
IF NOT v_field_changed OR ttbl_inv-head1.sold-addr[i] NE ttbl_inv-head2.sold-addr[i] OR action NE "UPDATE" THEN
DO:
  PUT STREAM s-audit-rpt UNFORMATTED "[" AT 3 i "]: " ttbl_inv-head1.sold-addr[i] " (sold-addr[" i "])".
  IF action = "UPDATE" THEN
  DO:
    IF ttbl_inv-head1.sold-addr[i] NE ttbl_inv-head2.sold-addr[i] THEN
    PUT STREAM s-audit-rpt UNFORMATTED "*" AT 1.
    PUT STREAM s-audit-rpt UNFORMATTED "[" AT 3 i "]: " ttbl_inv-head2.sold-addr[i].
  END.
END.
END.
IF NOT v_field_changed OR ttbl_inv-head1.sold-city NE ttbl_inv-head2.sold-city OR action NE "UPDATE" THEN
DO:
  PUT STREAM s-audit-rpt UNFORMATTED "City: " AT 3 ttbl_inv-head1.sold-city " (sold-city)".
  IF action = "UPDATE" THEN
  DO:
    IF ttbl_inv-head1.sold-city NE ttbl_inv-head2.sold-city THEN
    PUT STREAM s-audit-rpt UNFORMATTED "*" AT 1.
    PUT STREAM s-audit-rpt UNFORMATTED "City: " AT 3 ttbl_inv-head2.sold-city.
  END.
END.
IF NOT v_field_changed OR ttbl_inv-head1.sold-name NE ttbl_inv-head2.sold-name OR action NE "UPDATE" THEN
DO:
  PUT STREAM s-audit-rpt UNFORMATTED "Name: " AT 3 ttbl_inv-head1.sold-name " (sold-name)".
  IF action = "UPDATE" THEN
  DO:
    IF ttbl_inv-head1.sold-name NE ttbl_inv-head2.sold-name THEN
    PUT STREAM s-audit-rpt UNFORMATTED "*" AT 1.
    PUT STREAM s-audit-rpt UNFORMATTED "Name: " AT 3 ttbl_inv-head2.sold-name.
  END.
END.
IF NOT v_field_changed OR ttbl_inv-head1.sold-no NE ttbl_inv-head2.sold-no OR action NE "UPDATE" THEN
DO:
  PUT STREAM s-audit-rpt UNFORMATTED "Sold to: " AT 3 ttbl_inv-head1.sold-no " (sold-no)".
  IF action = "UPDATE" THEN
  DO:
    IF ttbl_inv-head1.sold-no NE ttbl_inv-head2.sold-no THEN
    PUT STREAM s-audit-rpt UNFORMATTED "*" AT 1.
    PUT STREAM s-audit-rpt UNFORMATTED "Sold to: " AT 3 ttbl_inv-head2.sold-no.
  END.
END.
IF NOT v_field_changed OR ttbl_inv-head1.sold-state NE ttbl_inv-head2.sold-state OR action NE "UPDATE" THEN
DO:
  PUT STREAM s-audit-rpt UNFORMATTED "State: " AT 3 ttbl_inv-head1.sold-state " (sold-state)".
  IF action = "UPDATE" THEN
  DO:
    IF ttbl_inv-head1.sold-state NE ttbl_inv-head2.sold-state THEN
    PUT STREAM s-audit-rpt UNFORMATTED "*" AT 1.
    PUT STREAM s-audit-rpt UNFORMATTED "State: " AT 3 ttbl_inv-head2.sold-state.
  END.
END.
IF NOT v_field_changed OR ttbl_inv-head1.sold-zip NE ttbl_inv-head2.sold-zip OR action NE "UPDATE" THEN
DO:
  PUT STREAM s-audit-rpt UNFORMATTED "Zip: " AT 3 ttbl_inv-head1.sold-zip " (sold-zip)".
  IF action = "UPDATE" THEN
  DO:
    IF ttbl_inv-head1.sold-zip NE ttbl_inv-head2.sold-zip THEN
    PUT STREAM s-audit-rpt UNFORMATTED "*" AT 1.
    PUT STREAM s-audit-rpt UNFORMATTED "Zip: " AT 3 ttbl_inv-head2.sold-zip.
  END.
END.
IF NOT v_field_changed OR ttbl_inv-head1.stat NE ttbl_inv-head2.stat OR action NE "UPDATE" THEN
DO:
  PUT STREAM s-audit-rpt UNFORMATTED "Order Status: " AT 3 ttbl_inv-head1.stat " (stat)".
  IF action = "UPDATE" THEN
  DO:
    IF ttbl_inv-head1.stat NE ttbl_inv-head2.stat THEN
    PUT STREAM s-audit-rpt UNFORMATTED "*" AT 1.
    PUT STREAM s-audit-rpt UNFORMATTED "Order Status: " AT 3 ttbl_inv-head2.stat.
  END.
END.
IF NOT v_field_changed OR ttbl_inv-head1.state NE ttbl_inv-head2.state OR action NE "UPDATE" THEN
DO:
  PUT STREAM s-audit-rpt UNFORMATTED "State: " AT 3 ttbl_inv-head1.state " (state)".
  IF action = "UPDATE" THEN
  DO:
    IF ttbl_inv-head1.state NE ttbl_inv-head2.state THEN
    PUT STREAM s-audit-rpt UNFORMATTED "*" AT 1.
    PUT STREAM s-audit-rpt UNFORMATTED "State: " AT 3 ttbl_inv-head2.state.
  END.
END.
IF NOT v_field_changed OR ttbl_inv-head1.t-comm NE ttbl_inv-head2.t-comm OR action NE "UPDATE" THEN
DO:
  PUT STREAM s-audit-rpt UNFORMATTED "Total Commissions: " AT 3 ttbl_inv-head1.t-comm FORMAT "->>>,>>9.99" " (t-comm)".
  IF action = "UPDATE" THEN
  DO:
    IF ttbl_inv-head1.t-comm NE ttbl_inv-head2.t-comm THEN
    PUT STREAM s-audit-rpt UNFORMATTED "*" AT 1.
    PUT STREAM s-audit-rpt UNFORMATTED "Total Commissions: " AT 3 ttbl_inv-head2.t-comm FORMAT "->>>,>>9.99".
  END.
END.
IF NOT v_field_changed OR ttbl_inv-head1.t-inv-cost NE ttbl_inv-head2.t-inv-cost OR action NE "UPDATE" THEN
DO:
  PUT STREAM s-audit-rpt UNFORMATTED "Total Cost Invoiced: " AT 3 ttbl_inv-head1.t-inv-cost FORMAT "->>,>>>,>>9.99" " (t-inv-cost)".
  IF action = "UPDATE" THEN
  DO:
    IF ttbl_inv-head1.t-inv-cost NE ttbl_inv-head2.t-inv-cost THEN
    PUT STREAM s-audit-rpt UNFORMATTED "*" AT 1.
    PUT STREAM s-audit-rpt UNFORMATTED "Total Cost Invoiced: " AT 3 ttbl_inv-head2.t-inv-cost FORMAT "->>,>>>,>>9.99".
  END.
END.
IF NOT v_field_changed OR ttbl_inv-head1.t-inv-freight NE ttbl_inv-head2.t-inv-freight OR action NE "UPDATE" THEN
DO:
  PUT STREAM s-audit-rpt UNFORMATTED "Total Freight Invoiced: " AT 3 ttbl_inv-head1.t-inv-freight FORMAT "->>,>>9.99" " (t-inv-freight)".
  IF action = "UPDATE" THEN
  DO:
    IF ttbl_inv-head1.t-inv-freight NE ttbl_inv-head2.t-inv-freight THEN
    PUT STREAM s-audit-rpt UNFORMATTED "*" AT 1.
    PUT STREAM s-audit-rpt UNFORMATTED "Total Freight Invoiced: " AT 3 ttbl_inv-head2.t-inv-freight FORMAT "->>,>>9.99".
  END.
END.
IF NOT v_field_changed OR ttbl_inv-head1.t-inv-fuel NE ttbl_inv-head2.t-inv-fuel OR action NE "UPDATE" THEN
DO:
  PUT STREAM s-audit-rpt UNFORMATTED "Total Fuel: " AT 3 ttbl_inv-head1.t-inv-fuel FORMAT "->>,>>9.99" " (t-inv-fuel)".
  IF action = "UPDATE" THEN
  DO:
    IF ttbl_inv-head1.t-inv-fuel NE ttbl_inv-head2.t-inv-fuel THEN
    PUT STREAM s-audit-rpt UNFORMATTED "*" AT 1.
    PUT STREAM s-audit-rpt UNFORMATTED "Total Fuel: " AT 3 ttbl_inv-head2.t-inv-fuel FORMAT "->>,>>9.99".
  END.
END.
IF NOT v_field_changed OR ttbl_inv-head1.t-inv-rev NE ttbl_inv-head2.t-inv-rev OR action NE "UPDATE" THEN
DO:
  PUT STREAM s-audit-rpt UNFORMATTED "Total Invoiced Revenue: " AT 3 ttbl_inv-head1.t-inv-rev FORMAT "->>,>>>,>>9.99" " (t-inv-rev)".
  IF action = "UPDATE" THEN
  DO:
    IF ttbl_inv-head1.t-inv-rev NE ttbl_inv-head2.t-inv-rev THEN
    PUT STREAM s-audit-rpt UNFORMATTED "*" AT 1.
    PUT STREAM s-audit-rpt UNFORMATTED "Total Invoiced Revenue: " AT 3 ttbl_inv-head2.t-inv-rev FORMAT "->>,>>>,>>9.99".
  END.
END.
IF NOT v_field_changed OR ttbl_inv-head1.t-inv-tax NE ttbl_inv-head2.t-inv-tax OR action NE "UPDATE" THEN
DO:
  PUT STREAM s-audit-rpt UNFORMATTED "Total Tax Invoiced: " AT 3 ttbl_inv-head1.t-inv-tax FORMAT "->,>>9.99" " (t-inv-tax)".
  IF action = "UPDATE" THEN
  DO:
    IF ttbl_inv-head1.t-inv-tax NE ttbl_inv-head2.t-inv-tax THEN
    PUT STREAM s-audit-rpt UNFORMATTED "*" AT 1.
    PUT STREAM s-audit-rpt UNFORMATTED "Total Tax Invoiced: " AT 3 ttbl_inv-head2.t-inv-tax FORMAT "->,>>9.99".
  END.
END.
IF NOT v_field_changed OR ttbl_inv-head1.t-inv-weight NE ttbl_inv-head2.t-inv-weight OR action NE "UPDATE" THEN
DO:
  PUT STREAM s-audit-rpt UNFORMATTED "Total Invoice Weight: " AT 3 ttbl_inv-head1.t-inv-weight FORMAT ">>>,>>9.99" " (t-inv-weight)".
  IF action = "UPDATE" THEN
  DO:
    IF ttbl_inv-head1.t-inv-weight NE ttbl_inv-head2.t-inv-weight THEN
    PUT STREAM s-audit-rpt UNFORMATTED "*" AT 1.
    PUT STREAM s-audit-rpt UNFORMATTED "Total Invoice Weight: " AT 3 ttbl_inv-head2.t-inv-weight FORMAT ">>>,>>9.99".
  END.
END.
IF NOT v_field_changed OR ttbl_inv-head1.tax-gr NE ttbl_inv-head2.tax-gr OR action NE "UPDATE" THEN
DO:
  PUT STREAM s-audit-rpt UNFORMATTED "Sales Tax Group: " AT 3 ttbl_inv-head1.tax-gr " (tax-gr)".
  IF action = "UPDATE" THEN
  DO:
    IF ttbl_inv-head1.tax-gr NE ttbl_inv-head2.tax-gr THEN
    PUT STREAM s-audit-rpt UNFORMATTED "*" AT 1.
    PUT STREAM s-audit-rpt UNFORMATTED "Sales Tax Group: " AT 3 ttbl_inv-head2.tax-gr.
  END.
END.
IF NOT v_field_changed OR ttbl_inv-head1.terms NE ttbl_inv-head2.terms OR action NE "UPDATE" THEN
DO:
  PUT STREAM s-audit-rpt UNFORMATTED "Terms Code: " AT 3 ttbl_inv-head1.terms " (terms)".
  IF action = "UPDATE" THEN
  DO:
    IF ttbl_inv-head1.terms NE ttbl_inv-head2.terms THEN
    PUT STREAM s-audit-rpt UNFORMATTED "*" AT 1.
    PUT STREAM s-audit-rpt UNFORMATTED "Terms Code: " AT 3 ttbl_inv-head2.terms.
  END.
END.
IF NOT v_field_changed OR ttbl_inv-head1.terms-d NE ttbl_inv-head2.terms-d OR action NE "UPDATE" THEN
DO:
  PUT STREAM s-audit-rpt UNFORMATTED "Description: " AT 3 ttbl_inv-head1.terms-d " (terms-d)".
  IF action = "UPDATE" THEN
  DO:
    IF ttbl_inv-head1.terms-d NE ttbl_inv-head2.terms-d THEN
    PUT STREAM s-audit-rpt UNFORMATTED "*" AT 1.
    PUT STREAM s-audit-rpt UNFORMATTED "Description: " AT 3 ttbl_inv-head2.terms-d.
  END.
END.
IF NOT v_field_changed OR ttbl_inv-head1.tot-ord NE ttbl_inv-head2.tot-ord OR action NE "UPDATE" THEN
DO:
  PUT STREAM s-audit-rpt UNFORMATTED "Total Order: " AT 3 ttbl_inv-head1.tot-ord FORMAT "->>,>>>,>>9.99" " (tot-ord)".
  IF action = "UPDATE" THEN
  DO:
    IF ttbl_inv-head1.tot-ord NE ttbl_inv-head2.tot-ord THEN
    PUT STREAM s-audit-rpt UNFORMATTED "*" AT 1.
    PUT STREAM s-audit-rpt UNFORMATTED "Total Order: " AT 3 ttbl_inv-head2.tot-ord FORMAT "->>,>>>,>>9.99".
  END.
END.
IF NOT v_field_changed OR ttbl_inv-head1.upd-date NE ttbl_inv-head2.upd-date OR action NE "UPDATE" THEN
DO:
  PUT STREAM s-audit-rpt UNFORMATTED "Updated Date: " AT 3 ttbl_inv-head1.upd-date FORMAT "99/99/9999" " (upd-date)".
  IF action = "UPDATE" THEN
  DO:
    IF ttbl_inv-head1.upd-date NE ttbl_inv-head2.upd-date THEN
    PUT STREAM s-audit-rpt UNFORMATTED "*" AT 1.
    PUT STREAM s-audit-rpt UNFORMATTED "Updated Date: " AT 3 ttbl_inv-head2.upd-date FORMAT "99/99/9999".
  END.
END.
IF NOT v_field_changed OR ttbl_inv-head1.upd-time NE ttbl_inv-head2.upd-time OR action NE "UPDATE" THEN
DO:
  PUT STREAM s-audit-rpt UNFORMATTED "Updated Time: " AT 3 ttbl_inv-head1.upd-time FORMAT "->,>>>,>>9" " (upd-time)".
  IF action = "UPDATE" THEN
  DO:
    IF ttbl_inv-head1.upd-time NE ttbl_inv-head2.upd-time THEN
    PUT STREAM s-audit-rpt UNFORMATTED "*" AT 1.
    PUT STREAM s-audit-rpt UNFORMATTED "Updated Time: " AT 3 ttbl_inv-head2.upd-time FORMAT "->,>>>,>>9".
  END.
END.
IF NOT v_field_changed OR ttbl_inv-head1.user-id NE ttbl_inv-head2.user-id OR action NE "UPDATE" THEN
DO:
  PUT STREAM s-audit-rpt UNFORMATTED "?: " AT 3 ttbl_inv-head1.user-id " (user-id)".
  IF action = "UPDATE" THEN
  DO:
    IF ttbl_inv-head1.user-id NE ttbl_inv-head2.user-id THEN
    PUT STREAM s-audit-rpt UNFORMATTED "*" AT 1.
    PUT STREAM s-audit-rpt UNFORMATTED "?: " AT 3 ttbl_inv-head2.user-id.
  END.
END.
IF NOT v_field_changed OR ttbl_inv-head1.zip NE ttbl_inv-head2.zip OR action NE "UPDATE" THEN
DO:
  PUT STREAM s-audit-rpt UNFORMATTED "Zip Code: " AT 3 ttbl_inv-head1.zip " (zip)".
  IF action = "UPDATE" THEN
  DO:
    IF ttbl_inv-head1.zip NE ttbl_inv-head2.zip THEN
    PUT STREAM s-audit-rpt UNFORMATTED "*" AT 1.
    PUT STREAM s-audit-rpt UNFORMATTED "Zip Code: " AT 3 ttbl_inv-head2.zip.
  END.
END.
IF v_show_index THEN
DO:
  PUT STREAM s-audit-rpt UNFORMATTED "Index Fields - Label: Value (database field-name)" AT 5.
  PUT STREAM s-audit-rpt UNFORMATTED "Unique Internal Number: " AT 7.
  IF action = "UPDATE" THEN
  PUT STREAM s-audit-rpt UNFORMATTED ttbl_inv-head2.r-no FORMAT ">>>>>>>9" " (r-no)".
  ELSE
  PUT STREAM s-audit-rpt UNFORMATTED ttbl_inv-head1.r-no FORMAT ">>>>>>>9" " (r-no)".
END.
PUT STREAM s-audit-rpt UNFORMATTED SKIP(1).
