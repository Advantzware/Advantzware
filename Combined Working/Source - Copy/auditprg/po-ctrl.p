/* po-ctrl.p - generated 12/09/2005 - 15:42:51 by NoSweat */

/* this program was generated by auditgn1.p and should not be modified */
/* modifications should be made to the auto generation program         */

DEFINE INPUT PARAMETER action AS CHARACTER NO-UNDO.

DEFINE VARIABLE i AS INTEGER NO-UNDO.

{methods/defines/audit.i}
FIND FIRST ttbl_header.

DEFINE WORK-TABLE ttbl_po-ctrl1 NO-UNDO LIKE po-ctrl.
DEFINE WORK-TABLE ttbl_po-ctrl2 NO-UNDO LIKE po-ctrl.

CREATE ttbl_po-ctrl1.
IMPORT STREAM s_audit_file ttbl_po-ctrl1.
CREATE ttbl_po-ctrl2.
IF action = "UPDATE" THEN
IMPORT STREAM s_audit_file ttbl_po-ctrl2.

PUT STREAM s-audit-rpt UNFORMATTED
  "Action    User ID/(User Name)            DB.Table/(Database)  Date       Time" SKIP
  "--------- ------------------------------ -------------------- ---------- --------" SKIP
  ttbl_header.v_action AT 1
  ttbl_header.v_userid AT 11
  ttbl_header.v_file AT 42
  ttbl_header.v_date FORMAT "99/99/9999" AT 63
  STRING(ttbl_header.v_time,"HH:MM:SS") AT 74.
FIND users WHERE users.user_id = v_userid NO-LOCK NO-ERROR.
IF AVAILABLE users THEN
PUT STREAM s-audit-rpt UNFORMATTED "(" AT 10 users.user_name ")".
ELSE
PUT STREAM s-audit-rpt UNFORMATTED "(user record does not exist)" AT 10.
PUT STREAM s-audit-rpt UNFORMATTED
  "(" AT 41 ttbl_header.v_db ")" SKIP(1)
  "Label: Value (database field-name)" AT 3.
IF action = "UPDATE" THEN
PUT STREAM s-audit-rpt UNFORMATTED " ( * - denotes field value change )".
PUT STREAM s-audit-rpt UNFORMATTED FILL("-",75) AT 3.
IF NOT v_field_changed OR ttbl_po-ctrl1.company NE ttbl_po-ctrl2.company OR action NE "UPDATE" THEN
DO:
  PUT STREAM s-audit-rpt UNFORMATTED "Company: " AT 3 ttbl_po-ctrl1.company " (company)".
  IF action = "UPDATE" THEN
  DO:
    IF ttbl_po-ctrl1.company NE ttbl_po-ctrl2.company THEN
    PUT STREAM s-audit-rpt UNFORMATTED "*" AT 1.
    PUT STREAM s-audit-rpt UNFORMATTED "Company: " AT 3 ttbl_po-ctrl2.company.
  END.
END.
IF NOT v_field_changed OR ttbl_po-ctrl1.next-po-no NE ttbl_po-ctrl2.next-po-no OR action NE "UPDATE" THEN
DO:
  PUT STREAM s-audit-rpt UNFORMATTED "Next Purchase Order Number: " AT 3 ttbl_po-ctrl1.next-po-no FORMAT ">>>>>9" " (next-po-no)".
  IF action = "UPDATE" THEN
  DO:
    IF ttbl_po-ctrl1.next-po-no NE ttbl_po-ctrl2.next-po-no THEN
    PUT STREAM s-audit-rpt UNFORMATTED "*" AT 1.
    PUT STREAM s-audit-rpt UNFORMATTED "Next Purchase Order Number: " AT 3 ttbl_po-ctrl2.next-po-no FORMAT ">>>>>9".
  END.
END.
IF NOT v_field_changed OR ttbl_po-ctrl1.prcom NE ttbl_po-ctrl2.prcom OR action NE "UPDATE" THEN
DO:
  PUT STREAM s-audit-rpt UNFORMATTED "Print Company Name: " AT 3 ttbl_po-ctrl1.prcom FORMAT "Y/N" " (prcom)".
  IF action = "UPDATE" THEN
  DO:
    IF ttbl_po-ctrl1.prcom NE ttbl_po-ctrl2.prcom THEN
    PUT STREAM s-audit-rpt UNFORMATTED "*" AT 1.
    PUT STREAM s-audit-rpt UNFORMATTED "Print Company Name: " AT 3 ttbl_po-ctrl2.prcom FORMAT "Y/N".
  END.
END.
IF NOT v_field_changed OR ttbl_po-ctrl1.pre-printed-forms NE ttbl_po-ctrl2.pre-printed-forms OR action NE "UPDATE" THEN
DO:
  PUT STREAM s-audit-rpt UNFORMATTED "Use Pre-printed Forms: " AT 3 ttbl_po-ctrl1.pre-printed-forms FORMAT "yes/no" " (pre-printed-forms)".
  IF action = "UPDATE" THEN
  DO:
    IF ttbl_po-ctrl1.pre-printed-forms NE ttbl_po-ctrl2.pre-printed-forms THEN
    PUT STREAM s-audit-rpt UNFORMATTED "*" AT 1.
    PUT STREAM s-audit-rpt UNFORMATTED "Use Pre-printed Forms: " AT 3 ttbl_po-ctrl2.pre-printed-forms FORMAT "yes/no".
  END.
END.
DO i = 1 TO 2:
IF NOT v_field_changed OR ttbl_po-ctrl1.rng-po-no[i] NE ttbl_po-ctrl2.rng-po-no[i] OR action NE "UPDATE" THEN
DO:
  PUT STREAM s-audit-rpt UNFORMATTED "Range of PO Numers[" AT 3 i "]: " ttbl_po-ctrl1.rng-po-no[i] FORMAT ">>>>>9" " (rng-po-no[" i "])".
  IF action = "UPDATE" THEN
  DO:
    IF ttbl_po-ctrl1.rng-po-no[i] NE ttbl_po-ctrl2.rng-po-no[i] THEN
    PUT STREAM s-audit-rpt UNFORMATTED "*" AT 1.
    PUT STREAM s-audit-rpt UNFORMATTED "Range of PO Numers[" AT 3 i "]: " ttbl_po-ctrl2.rng-po-no[i] FORMAT ">>>>>9".
  END.
END.
END.
IF v_show_index THEN
DO:
  PUT STREAM s-audit-rpt UNFORMATTED "Index Fields - Label: Value (database field-name)" AT 5.
  PUT STREAM s-audit-rpt UNFORMATTED "Company: " AT 7.
  IF action = "UPDATE" THEN
  PUT STREAM s-audit-rpt UNFORMATTED ttbl_po-ctrl2.company " (company)".
  ELSE
  PUT STREAM s-audit-rpt UNFORMATTED ttbl_po-ctrl1.company " (company)".
END.
PUT STREAM s-audit-rpt UNFORMATTED SKIP(1).
