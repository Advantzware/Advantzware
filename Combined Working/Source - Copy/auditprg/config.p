/* config.p - generated 12/02/2005 - 16:51:16 by NoSweat */

/* this program was generated by auditgn1.p and should not be modified */
/* modifications should be made to the auto generation program         */

DEFINE INPUT PARAMETER action AS CHARACTER NO-UNDO.

DEFINE VARIABLE i AS INTEGER NO-UNDO.

{methods/defines/audit.i}
FIND FIRST ttbl_header.

DEFINE WORK-TABLE ttbl_config1 NO-UNDO LIKE config.
DEFINE WORK-TABLE ttbl_config2 NO-UNDO LIKE config.

CREATE ttbl_config1.
IMPORT STREAM s_audit_file ttbl_config1.
CREATE ttbl_config2.
IF action = "UPDATE" THEN
IMPORT STREAM s_audit_file ttbl_config2.

PUT STREAM s-audit-rpt UNFORMATTED
  "Action    User ID/(User Name)            DB.Table/(Database)  Date       Time" SKIP
  "--------- ------------------------------ -------------------- ---------- --------" SKIP
  ttbl_header.v_action AT 1
  ttbl_header.v_userid AT 11
  ttbl_header.v_file AT 42
  ttbl_header.v_date FORMAT "99/99/9999" AT 63
  STRING(ttbl_header.v_time,"HH:MM:SS") AT 74.
FIND users WHERE users.user_id = v_userid NO-LOCK NO-ERROR.
IF AVAILABLE users THEN
PUT STREAM s-audit-rpt UNFORMATTED "(" AT 10 users.user_name ")".
ELSE
PUT STREAM s-audit-rpt UNFORMATTED "(user record does not exist)" AT 10.
PUT STREAM s-audit-rpt UNFORMATTED
  "(" AT 41 ttbl_header.v_db ")" SKIP(1)
  "Label: Value (database field-name)" AT 3.
IF action = "UPDATE" THEN
PUT STREAM s-audit-rpt UNFORMATTED " ( * - denotes field value change )".
PUT STREAM s-audit-rpt UNFORMATTED FILL("-",75) AT 3.
IF NOT v_field_changed OR ttbl_config1.audit_dir NE ttbl_config2.audit_dir OR action NE "UPDATE" THEN
DO:
  PUT STREAM s-audit-rpt UNFORMATTED "Audit Directory: " AT 3 ttbl_config1.audit_dir " (audit_dir)".
  IF action = "UPDATE" THEN
  DO:
    IF ttbl_config1.audit_dir NE ttbl_config2.audit_dir THEN
    PUT STREAM s-audit-rpt UNFORMATTED "*" AT 1.
    PUT STREAM s-audit-rpt UNFORMATTED "Audit Directory: " AT 3 ttbl_config2.audit_dir.
  END.
END.
IF NOT v_field_changed OR ttbl_config1.audit_dir_unix NE ttbl_config2.audit_dir_unix OR action NE "UPDATE" THEN
DO:
  PUT STREAM s-audit-rpt UNFORMATTED "Audit Directory (UNIX): " AT 3 ttbl_config1.audit_dir_unix " (audit_dir_unix)".
  IF action = "UPDATE" THEN
  DO:
    IF ttbl_config1.audit_dir_unix NE ttbl_config2.audit_dir_unix THEN
    PUT STREAM s-audit-rpt UNFORMATTED "*" AT 1.
    PUT STREAM s-audit-rpt UNFORMATTED "Audit Directory (UNIX): " AT 3 ttbl_config2.audit_dir_unix.
  END.
END.
IF NOT v_field_changed OR ttbl_config1.audit_tables NE ttbl_config2.audit_tables OR action NE "UPDATE" THEN
DO:
  PUT STREAM s-audit-rpt UNFORMATTED "Audit Tables: " AT 3 ttbl_config1.audit_tables " (audit_tables)".
  IF action = "UPDATE" THEN
  DO:
    IF ttbl_config1.audit_tables NE ttbl_config2.audit_tables THEN
    PUT STREAM s-audit-rpt UNFORMATTED "*" AT 1.
    PUT STREAM s-audit-rpt UNFORMATTED "Audit Tables: " AT 3 ttbl_config2.audit_tables.
  END.
END.
IF NOT v_field_changed OR ttbl_config1.image_filename NE ttbl_config2.image_filename OR action NE "UPDATE" THEN
DO:
  PUT STREAM s-audit-rpt UNFORMATTED "Image File Name: " AT 3 ttbl_config1.image_filename " (image_filename)".
  IF action = "UPDATE" THEN
  DO:
    IF ttbl_config1.image_filename NE ttbl_config2.image_filename THEN
    PUT STREAM s-audit-rpt UNFORMATTED "*" AT 1.
    PUT STREAM s-audit-rpt UNFORMATTED "Image File Name: " AT 3 ttbl_config2.image_filename.
  END.
END.
IF NOT v_field_changed OR ttbl_config1.logs_dir NE ttbl_config2.logs_dir OR action NE "UPDATE" THEN
DO:
  PUT STREAM s-audit-rpt UNFORMATTED "Logs Directory: " AT 3 ttbl_config1.logs_dir " (logs_dir)".
  IF action = "UPDATE" THEN
  DO:
    IF ttbl_config1.logs_dir NE ttbl_config2.logs_dir THEN
    PUT STREAM s-audit-rpt UNFORMATTED "*" AT 1.
    PUT STREAM s-audit-rpt UNFORMATTED "Logs Directory: " AT 3 ttbl_config2.logs_dir.
  END.
END.
IF NOT v_field_changed OR ttbl_config1.spool_dir NE ttbl_config2.spool_dir OR action NE "UPDATE" THEN
DO:
  PUT STREAM s-audit-rpt UNFORMATTED "Spool Directory: " AT 3 ttbl_config1.spool_dir " (spool_dir)".
  IF action = "UPDATE" THEN
  DO:
    IF ttbl_config1.spool_dir NE ttbl_config2.spool_dir THEN
    PUT STREAM s-audit-rpt UNFORMATTED "*" AT 1.
    PUT STREAM s-audit-rpt UNFORMATTED "Spool Directory: " AT 3 ttbl_config2.spool_dir.
  END.
END.
IF NOT v_field_changed OR ttbl_config1.start_page_no NE ttbl_config2.start_page_no OR action NE "UPDATE" THEN
DO:
  PUT STREAM s-audit-rpt UNFORMATTED "Starting Page Number: " AT 3 ttbl_config1.start_page_no FORMAT ">9" " (start_page_no)".
  IF action = "UPDATE" THEN
  DO:
    IF ttbl_config1.start_page_no NE ttbl_config2.start_page_no THEN
    PUT STREAM s-audit-rpt UNFORMATTED "*" AT 1.
    PUT STREAM s-audit-rpt UNFORMATTED "Starting Page Number: " AT 3 ttbl_config2.start_page_no FORMAT ">9".
  END.
END.
PUT STREAM s-audit-rpt UNFORMATTED SKIP(1).
