/* prodl.p - generated 12/09/2005 - 15:42:52 by NoSweat */

/* this program was generated by auditgn1.p and should not be modified */
/* modifications should be made to the auto generation program         */

DEFINE INPUT PARAMETER action AS CHARACTER NO-UNDO.

DEFINE VARIABLE i AS INTEGER NO-UNDO.

{methods/defines/audit.i}
FIND FIRST ttbl_header.

DEFINE WORK-TABLE ttbl_prodl1 NO-UNDO LIKE prodl.
DEFINE WORK-TABLE ttbl_prodl2 NO-UNDO LIKE prodl.

CREATE ttbl_prodl1.
IMPORT STREAM s_audit_file ttbl_prodl1.
CREATE ttbl_prodl2.
IF action = "UPDATE" THEN
IMPORT STREAM s_audit_file ttbl_prodl2.

PUT STREAM s-audit-rpt UNFORMATTED
  "Action    User ID/(User Name)            DB.Table/(Database)  Date       Time" SKIP
  "--------- ------------------------------ -------------------- ---------- --------" SKIP
  ttbl_header.v_action AT 1
  ttbl_header.v_userid AT 11
  ttbl_header.v_file AT 42
  ttbl_header.v_date FORMAT "99/99/9999" AT 63
  STRING(ttbl_header.v_time,"HH:MM:SS") AT 74.
FIND users WHERE users.user_id = v_userid NO-LOCK NO-ERROR.
IF AVAILABLE users THEN
PUT STREAM s-audit-rpt UNFORMATTED "(" AT 10 users.user_name ")".
ELSE
PUT STREAM s-audit-rpt UNFORMATTED "(user record does not exist)" AT 10.
PUT STREAM s-audit-rpt UNFORMATTED
  "(" AT 41 ttbl_header.v_db ")" SKIP(1)
  "Label: Value (database field-name)" AT 3.
IF action = "UPDATE" THEN
PUT STREAM s-audit-rpt UNFORMATTED " ( * - denotes field value change )".
PUT STREAM s-audit-rpt UNFORMATTED FILL("-",75) AT 3.
IF NOT v_field_changed OR ttbl_prodl1.company NE ttbl_prodl2.company OR action NE "UPDATE" THEN
DO:
  PUT STREAM s-audit-rpt UNFORMATTED "Company: " AT 3 ttbl_prodl1.company " (company)".
  IF action = "UPDATE" THEN
  DO:
    IF ttbl_prodl1.company NE ttbl_prodl2.company THEN
    PUT STREAM s-audit-rpt UNFORMATTED "*" AT 1.
    PUT STREAM s-audit-rpt UNFORMATTED "Company: " AT 3 ttbl_prodl2.company.
  END.
END.
IF NOT v_field_changed OR ttbl_prodl1.procat NE ttbl_prodl2.procat OR action NE "UPDATE" THEN
DO:
  PUT STREAM s-audit-rpt UNFORMATTED "Category: " AT 3 ttbl_prodl1.procat " (procat)".
  IF action = "UPDATE" THEN
  DO:
    IF ttbl_prodl1.procat NE ttbl_prodl2.procat THEN
    PUT STREAM s-audit-rpt UNFORMATTED "*" AT 1.
    PUT STREAM s-audit-rpt UNFORMATTED "Category: " AT 3 ttbl_prodl2.procat.
  END.
END.
IF NOT v_field_changed OR ttbl_prodl1.prolin NE ttbl_prodl2.prolin OR action NE "UPDATE" THEN
DO:
  PUT STREAM s-audit-rpt UNFORMATTED "Product Line: " AT 3 ttbl_prodl1.prolin " (prolin)".
  IF action = "UPDATE" THEN
  DO:
    IF ttbl_prodl1.prolin NE ttbl_prodl2.prolin THEN
    PUT STREAM s-audit-rpt UNFORMATTED "*" AT 1.
    PUT STREAM s-audit-rpt UNFORMATTED "Product Line: " AT 3 ttbl_prodl2.prolin.
  END.
END.
IF v_show_index THEN
DO:
  PUT STREAM s-audit-rpt UNFORMATTED "Index Fields - Label: Value (database field-name)" AT 5.
  PUT STREAM s-audit-rpt UNFORMATTED "Company: " AT 7.
  IF action = "UPDATE" THEN
  PUT STREAM s-audit-rpt UNFORMATTED ttbl_prodl2.company " (company)".
  ELSE
  PUT STREAM s-audit-rpt UNFORMATTED ttbl_prodl1.company " (company)".
  PUT STREAM s-audit-rpt UNFORMATTED "Product Line: " AT 7.
  IF action = "UPDATE" THEN
  PUT STREAM s-audit-rpt UNFORMATTED ttbl_prodl2.prolin " (prolin)".
  ELSE
  PUT STREAM s-audit-rpt UNFORMATTED ttbl_prodl1.prolin " (prolin)".
  PUT STREAM s-audit-rpt UNFORMATTED "Category: " AT 7.
  IF action = "UPDATE" THEN
  PUT STREAM s-audit-rpt UNFORMATTED ttbl_prodl2.procat " (procat)".
  ELSE
  PUT STREAM s-audit-rpt UNFORMATTED ttbl_prodl1.procat " (procat)".
END.
PUT STREAM s-audit-rpt UNFORMATTED SKIP(1).
