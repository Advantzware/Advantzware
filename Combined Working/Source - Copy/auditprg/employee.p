/* employee.p - generated 12/02/2005 - 16:52:38 by NoSweat */

/* this program was generated by auditgn1.p and should not be modified */
/* modifications should be made to the auto generation program         */

DEFINE INPUT PARAMETER action AS CHARACTER NO-UNDO.

DEFINE VARIABLE i AS INTEGER NO-UNDO.

{methods/defines/audit.i}
FIND FIRST ttbl_header.

DEFINE WORK-TABLE ttbl_employee1 NO-UNDO LIKE employee.
DEFINE WORK-TABLE ttbl_employee2 NO-UNDO LIKE employee.

CREATE ttbl_employee1.
IMPORT STREAM s_audit_file ttbl_employee1.
CREATE ttbl_employee2.
IF action = "UPDATE" THEN
IMPORT STREAM s_audit_file ttbl_employee2.

PUT STREAM s-audit-rpt UNFORMATTED
  "Action    User ID/(User Name)            DB.Table/(Database)  Date       Time" SKIP
  "--------- ------------------------------ -------------------- ---------- --------" SKIP
  ttbl_header.v_action AT 1
  ttbl_header.v_userid AT 11
  ttbl_header.v_file AT 42
  ttbl_header.v_date FORMAT "99/99/9999" AT 63
  STRING(ttbl_header.v_time,"HH:MM:SS") AT 74.
FIND users WHERE users.user_id = v_userid NO-LOCK NO-ERROR.
IF AVAILABLE users THEN
PUT STREAM s-audit-rpt UNFORMATTED "(" AT 10 users.user_name ")".
ELSE
PUT STREAM s-audit-rpt UNFORMATTED "(user record does not exist)" AT 10.
PUT STREAM s-audit-rpt UNFORMATTED
  "(" AT 41 ttbl_header.v_db ")" SKIP(1)
  "Label: Value (database field-name)" AT 3.
IF action = "UPDATE" THEN
PUT STREAM s-audit-rpt UNFORMATTED " ( * - denotes field value change )".
PUT STREAM s-audit-rpt UNFORMATTED FILL("-",75) AT 3.
IF NOT v_field_changed OR ttbl_employee1.actnum NE ttbl_employee2.actnum OR action NE "UPDATE" THEN
DO:
  PUT STREAM s-audit-rpt UNFORMATTED "GL Acct: " AT 3 ttbl_employee1.actnum " (actnum)".
  IF action = "UPDATE" THEN
  DO:
    IF ttbl_employee1.actnum NE ttbl_employee2.actnum THEN
    PUT STREAM s-audit-rpt UNFORMATTED "*" AT 1.
    PUT STREAM s-audit-rpt UNFORMATTED "GL Acct: " AT 3 ttbl_employee2.actnum.
  END.
END.
IF NOT v_field_changed OR ttbl_employee1.company NE ttbl_employee2.company OR action NE "UPDATE" THEN
DO:
  PUT STREAM s-audit-rpt UNFORMATTED "Company: " AT 3 ttbl_employee1.company " (company)".
  IF action = "UPDATE" THEN
  DO:
    IF ttbl_employee1.company NE ttbl_employee2.company THEN
    PUT STREAM s-audit-rpt UNFORMATTED "*" AT 1.
    PUT STREAM s-audit-rpt UNFORMATTED "Company: " AT 3 ttbl_employee2.company.
  END.
END.
IF NOT v_field_changed OR ttbl_employee1.dock-time NE ttbl_employee2.dock-time OR action NE "UPDATE" THEN
DO:
  PUT STREAM s-audit-rpt UNFORMATTED "Dock Time: " AT 3 ttbl_employee1.dock-time FORMAT ">>9" " (dock-time)".
  IF action = "UPDATE" THEN
  DO:
    IF ttbl_employee1.dock-time NE ttbl_employee2.dock-time THEN
    PUT STREAM s-audit-rpt UNFORMATTED "*" AT 1.
    PUT STREAM s-audit-rpt UNFORMATTED "Dock Time: " AT 3 ttbl_employee2.dock-time FORMAT ">>9".
  END.
END.
IF NOT v_field_changed OR ttbl_employee1.employee NE ttbl_employee2.employee OR action NE "UPDATE" THEN
DO:
  PUT STREAM s-audit-rpt UNFORMATTED "Emp ID: " AT 3 ttbl_employee1.employee " (employee)".
  IF action = "UPDATE" THEN
  DO:
    IF ttbl_employee1.employee NE ttbl_employee2.employee THEN
    PUT STREAM s-audit-rpt UNFORMATTED "*" AT 1.
    PUT STREAM s-audit-rpt UNFORMATTED "Emp ID: " AT 3 ttbl_employee2.employee.
  END.
END.
IF NOT v_field_changed OR ttbl_employee1.emp_type NE ttbl_employee2.emp_type OR action NE "UPDATE" THEN
DO:
  PUT STREAM s-audit-rpt UNFORMATTED "Employee Type: " AT 3 ttbl_employee1.emp_type " (emp_type)".
  IF action = "UPDATE" THEN
  DO:
    IF ttbl_employee1.emp_type NE ttbl_employee2.emp_type THEN
    PUT STREAM s-audit-rpt UNFORMATTED "*" AT 1.
    PUT STREAM s-audit-rpt UNFORMATTED "Employee Type: " AT 3 ttbl_employee2.emp_type.
  END.
END.
IF NOT v_field_changed OR ttbl_employee1.first_name NE ttbl_employee2.first_name OR action NE "UPDATE" THEN
DO:
  PUT STREAM s-audit-rpt UNFORMATTED "First Name: " AT 3 ttbl_employee1.first_name " (first_name)".
  IF action = "UPDATE" THEN
  DO:
    IF ttbl_employee1.first_name NE ttbl_employee2.first_name THEN
    PUT STREAM s-audit-rpt UNFORMATTED "*" AT 1.
    PUT STREAM s-audit-rpt UNFORMATTED "First Name: " AT 3 ttbl_employee2.first_name.
  END.
END.
IF NOT v_field_changed OR ttbl_employee1.keyboard_type NE ttbl_employee2.keyboard_type OR action NE "UPDATE" THEN
DO:
  PUT STREAM s-audit-rpt UNFORMATTED "Keyboard Type: " AT 3 ttbl_employee1.keyboard_type " (keyboard_type)".
  IF action = "UPDATE" THEN
  DO:
    IF ttbl_employee1.keyboard_type NE ttbl_employee2.keyboard_type THEN
    PUT STREAM s-audit-rpt UNFORMATTED "*" AT 1.
    PUT STREAM s-audit-rpt UNFORMATTED "Keyboard Type: " AT 3 ttbl_employee2.keyboard_type.
  END.
END.
IF NOT v_field_changed OR ttbl_employee1.last_name NE ttbl_employee2.last_name OR action NE "UPDATE" THEN
DO:
  PUT STREAM s-audit-rpt UNFORMATTED "Last Name: " AT 3 ttbl_employee1.last_name " (last_name)".
  IF action = "UPDATE" THEN
  DO:
    IF ttbl_employee1.last_name NE ttbl_employee2.last_name THEN
    PUT STREAM s-audit-rpt UNFORMATTED "*" AT 1.
    PUT STREAM s-audit-rpt UNFORMATTED "Last Name: " AT 3 ttbl_employee2.last_name.
  END.
END.
IF NOT v_field_changed OR ttbl_employee1.lunch_paid NE ttbl_employee2.lunch_paid OR action NE "UPDATE" THEN
DO:
  PUT STREAM s-audit-rpt UNFORMATTED "Lunch Paid: " AT 3 ttbl_employee1.lunch_paid FORMAT "yes/no" " (lunch_paid)".
  IF action = "UPDATE" THEN
  DO:
    IF ttbl_employee1.lunch_paid NE ttbl_employee2.lunch_paid THEN
    PUT STREAM s-audit-rpt UNFORMATTED "*" AT 1.
    PUT STREAM s-audit-rpt UNFORMATTED "Lunch Paid: " AT 3 ttbl_employee2.lunch_paid FORMAT "yes/no".
  END.
END.
IF NOT v_field_changed OR ttbl_employee1.middle_name NE ttbl_employee2.middle_name OR action NE "UPDATE" THEN
DO:
  PUT STREAM s-audit-rpt UNFORMATTED "Middle Name: " AT 3 ttbl_employee1.middle_name " (middle_name)".
  IF action = "UPDATE" THEN
  DO:
    IF ttbl_employee1.middle_name NE ttbl_employee2.middle_name THEN
    PUT STREAM s-audit-rpt UNFORMATTED "*" AT 1.
    PUT STREAM s-audit-rpt UNFORMATTED "Middle Name: " AT 3 ttbl_employee2.middle_name.
  END.
END.
IF NOT v_field_changed OR ttbl_employee1.passwd NE ttbl_employee2.passwd OR action NE "UPDATE" THEN
DO:
  PUT STREAM s-audit-rpt UNFORMATTED "Password: " AT 3 ttbl_employee1.passwd " (passwd)".
  IF action = "UPDATE" THEN
  DO:
    IF ttbl_employee1.passwd NE ttbl_employee2.passwd THEN
    PUT STREAM s-audit-rpt UNFORMATTED "*" AT 1.
    PUT STREAM s-audit-rpt UNFORMATTED "Password: " AT 3 ttbl_employee2.passwd.
  END.
END.
IF NOT v_field_changed OR ttbl_employee1.rate_usage NE ttbl_employee2.rate_usage OR action NE "UPDATE" THEN
DO:
  PUT STREAM s-audit-rpt UNFORMATTED "Rate Use: " AT 3 ttbl_employee1.rate_usage FORMAT "Shift/Machine" " (rate_usage)".
  IF action = "UPDATE" THEN
  DO:
    IF ttbl_employee1.rate_usage NE ttbl_employee2.rate_usage THEN
    PUT STREAM s-audit-rpt UNFORMATTED "*" AT 1.
    PUT STREAM s-audit-rpt UNFORMATTED "Rate Use: " AT 3 ttbl_employee2.rate_usage FORMAT "Shift/Machine".
  END.
END.
IF NOT v_field_changed OR ttbl_employee1.ref_no NE ttbl_employee2.ref_no OR action NE "UPDATE" THEN
DO:
  PUT STREAM s-audit-rpt UNFORMATTED "Reference Number: " AT 3 ttbl_employee1.ref_no " (ref_no)".
  IF action = "UPDATE" THEN
  DO:
    IF ttbl_employee1.ref_no NE ttbl_employee2.ref_no THEN
    PUT STREAM s-audit-rpt UNFORMATTED "*" AT 1.
    PUT STREAM s-audit-rpt UNFORMATTED "Reference Number: " AT 3 ttbl_employee2.ref_no.
  END.
END.
IF NOT v_field_changed OR ttbl_employee1.soc_sec NE ttbl_employee2.soc_sec OR action NE "UPDATE" THEN
DO:
  PUT STREAM s-audit-rpt UNFORMATTED "Social Security #: " AT 3 ttbl_employee1.soc_sec " (soc_sec)".
  IF action = "UPDATE" THEN
  DO:
    IF ttbl_employee1.soc_sec NE ttbl_employee2.soc_sec THEN
    PUT STREAM s-audit-rpt UNFORMATTED "*" AT 1.
    PUT STREAM s-audit-rpt UNFORMATTED "Social Security #: " AT 3 ttbl_employee2.soc_sec.
  END.
END.
IF NOT v_field_changed OR ttbl_employee1.start_date NE ttbl_employee2.start_date OR action NE "UPDATE" THEN
DO:
  PUT STREAM s-audit-rpt UNFORMATTED "Start Date: " AT 3 ttbl_employee1.start_date FORMAT "99/99/9999" " (start_date)".
  IF action = "UPDATE" THEN
  DO:
    IF ttbl_employee1.start_date NE ttbl_employee2.start_date THEN
    PUT STREAM s-audit-rpt UNFORMATTED "*" AT 1.
    PUT STREAM s-audit-rpt UNFORMATTED "Start Date: " AT 3 ttbl_employee2.start_date FORMAT "99/99/9999".
  END.
END.
IF NOT v_field_changed OR ttbl_employee1.user_language NE ttbl_employee2.user_language OR action NE "UPDATE" THEN
DO:
  PUT STREAM s-audit-rpt UNFORMATTED "User Language: " AT 3 ttbl_employee1.user_language " (user_language)".
  IF action = "UPDATE" THEN
  DO:
    IF ttbl_employee1.user_language NE ttbl_employee2.user_language THEN
    PUT STREAM s-audit-rpt UNFORMATTED "*" AT 1.
    PUT STREAM s-audit-rpt UNFORMATTED "User Language: " AT 3 ttbl_employee2.user_language.
  END.
END.
IF v_show_index THEN
DO:
  PUT STREAM s-audit-rpt UNFORMATTED "Index Fields - Label: Value (database field-name)" AT 5.
  PUT STREAM s-audit-rpt UNFORMATTED "Company: " AT 7.
  IF action = "UPDATE" THEN
  PUT STREAM s-audit-rpt UNFORMATTED ttbl_employee2.company " (company)".
  ELSE
  PUT STREAM s-audit-rpt UNFORMATTED ttbl_employee1.company " (company)".
  PUT STREAM s-audit-rpt UNFORMATTED "Emp ID: " AT 7.
  IF action = "UPDATE" THEN
  PUT STREAM s-audit-rpt UNFORMATTED ttbl_employee2.employee " (employee)".
  ELSE
  PUT STREAM s-audit-rpt UNFORMATTED ttbl_employee1.employee " (employee)".
END.
PUT STREAM s-audit-rpt UNFORMATTED SKIP(1).
