/* fraction.p - generated 12/09/2005 - 15:42:45 by NoSweat */

/* this program was generated by auditgn1.p and should not be modified */
/* modifications should be made to the auto generation program         */

DEFINE INPUT PARAMETER action AS CHARACTER NO-UNDO.

DEFINE VARIABLE i AS INTEGER NO-UNDO.

{methods/defines/audit.i}
FIND FIRST ttbl_header.

DEFINE WORK-TABLE ttbl_fraction1 NO-UNDO LIKE fraction.
DEFINE WORK-TABLE ttbl_fraction2 NO-UNDO LIKE fraction.

CREATE ttbl_fraction1.
IMPORT STREAM s_audit_file ttbl_fraction1.
CREATE ttbl_fraction2.
IF action = "UPDATE" THEN
IMPORT STREAM s_audit_file ttbl_fraction2.

PUT STREAM s-audit-rpt UNFORMATTED
  "Action    User ID/(User Name)            DB.Table/(Database)  Date       Time" SKIP
  "--------- ------------------------------ -------------------- ---------- --------" SKIP
  ttbl_header.v_action AT 1
  ttbl_header.v_userid AT 11
  ttbl_header.v_file AT 42
  ttbl_header.v_date FORMAT "99/99/9999" AT 63
  STRING(ttbl_header.v_time,"HH:MM:SS") AT 74.
FIND users WHERE users.user_id = v_userid NO-LOCK NO-ERROR.
IF AVAILABLE users THEN
PUT STREAM s-audit-rpt UNFORMATTED "(" AT 10 users.user_name ")".
ELSE
PUT STREAM s-audit-rpt UNFORMATTED "(user record does not exist)" AT 10.
PUT STREAM s-audit-rpt UNFORMATTED
  "(" AT 41 ttbl_header.v_db ")" SKIP(1)
  "Label: Value (database field-name)" AT 3.
IF action = "UPDATE" THEN
PUT STREAM s-audit-rpt UNFORMATTED " ( * - denotes field value change )".
PUT STREAM s-audit-rpt UNFORMATTED FILL("-",75) AT 3.
IF NOT v_field_changed OR ttbl_fraction1.conv NE ttbl_fraction2.conv OR action NE "UPDATE" THEN
DO:
  PUT STREAM s-audit-rpt UNFORMATTED "?: " AT 3 ttbl_fraction1.conv " (conv)".
  IF action = "UPDATE" THEN
  DO:
    IF ttbl_fraction1.conv NE ttbl_fraction2.conv THEN
    PUT STREAM s-audit-rpt UNFORMATTED "*" AT 1.
    PUT STREAM s-audit-rpt UNFORMATTED "?: " AT 3 ttbl_fraction2.conv.
  END.
END.
IF NOT v_field_changed OR ttbl_fraction1.denom NE ttbl_fraction2.denom OR action NE "UPDATE" THEN
DO:
  PUT STREAM s-audit-rpt UNFORMATTED "?: " AT 3 ttbl_fraction1.denom FORMAT ">9" " (denom)".
  IF action = "UPDATE" THEN
  DO:
    IF ttbl_fraction1.denom NE ttbl_fraction2.denom THEN
    PUT STREAM s-audit-rpt UNFORMATTED "*" AT 1.
    PUT STREAM s-audit-rpt UNFORMATTED "?: " AT 3 ttbl_fraction2.denom FORMAT ">9".
  END.
END.
IF NOT v_field_changed OR ttbl_fraction1.id NE ttbl_fraction2.id OR action NE "UPDATE" THEN
DO:
  PUT STREAM s-audit-rpt UNFORMATTED "id: " AT 3 ttbl_fraction1.id FORMAT ">>>>>>9" " (id)".
  IF action = "UPDATE" THEN
  DO:
    IF ttbl_fraction1.id NE ttbl_fraction2.id THEN
    PUT STREAM s-audit-rpt UNFORMATTED "*" AT 1.
    PUT STREAM s-audit-rpt UNFORMATTED "id: " AT 3 ttbl_fraction2.id FORMAT ">>>>>>9".
  END.
END.
IF NOT v_field_changed OR ttbl_fraction1.number NE ttbl_fraction2.number OR action NE "UPDATE" THEN
DO:
  PUT STREAM s-audit-rpt UNFORMATTED "?: " AT 3 ttbl_fraction1.number FORMAT ".9999999999" " (number)".
  IF action = "UPDATE" THEN
  DO:
    IF ttbl_fraction1.number NE ttbl_fraction2.number THEN
    PUT STREAM s-audit-rpt UNFORMATTED "*" AT 1.
    PUT STREAM s-audit-rpt UNFORMATTED "?: " AT 3 ttbl_fraction2.number FORMAT ".9999999999".
  END.
END.
IF NOT v_field_changed OR ttbl_fraction1.numer NE ttbl_fraction2.numer OR action NE "UPDATE" THEN
DO:
  PUT STREAM s-audit-rpt UNFORMATTED "?: " AT 3 ttbl_fraction1.numer FORMAT ">9" " (numer)".
  IF action = "UPDATE" THEN
  DO:
    IF ttbl_fraction1.numer NE ttbl_fraction2.numer THEN
    PUT STREAM s-audit-rpt UNFORMATTED "*" AT 1.
    PUT STREAM s-audit-rpt UNFORMATTED "?: " AT 3 ttbl_fraction2.numer FORMAT ">9".
  END.
END.
IF v_show_index THEN
DO:
  PUT STREAM s-audit-rpt UNFORMATTED "Index Fields - Label: Value (database field-name)" AT 5.
  PUT STREAM s-audit-rpt UNFORMATTED "?: " AT 7.
  IF action = "UPDATE" THEN
  PUT STREAM s-audit-rpt UNFORMATTED ttbl_fraction2.number FORMAT ".9999999999" " (number)".
  ELSE
  PUT STREAM s-audit-rpt UNFORMATTED ttbl_fraction1.number FORMAT ".9999999999" " (number)".
  PUT STREAM s-audit-rpt UNFORMATTED "?: " AT 7.
  IF action = "UPDATE" THEN
  PUT STREAM s-audit-rpt UNFORMATTED ttbl_fraction2.denom FORMAT ">9" " (denom)".
  ELSE
  PUT STREAM s-audit-rpt UNFORMATTED ttbl_fraction1.denom FORMAT ">9" " (denom)".
END.
PUT STREAM s-audit-rpt UNFORMATTED SKIP(1).
