/* gl-rpt.p - generated 12/09/2005 - 15:42:46 by NoSweat */

/* this program was generated by auditgn1.p and should not be modified */
/* modifications should be made to the auto generation program         */

DEFINE INPUT PARAMETER action AS CHARACTER NO-UNDO.

DEFINE VARIABLE i AS INTEGER NO-UNDO.

{methods/defines/audit.i}
FIND FIRST ttbl_header.

DEFINE WORK-TABLE ttbl_gl-rpt1 NO-UNDO LIKE gl-rpt.
DEFINE WORK-TABLE ttbl_gl-rpt2 NO-UNDO LIKE gl-rpt.

CREATE ttbl_gl-rpt1.
IMPORT STREAM s_audit_file ttbl_gl-rpt1.
CREATE ttbl_gl-rpt2.
IF action = "UPDATE" THEN
IMPORT STREAM s_audit_file ttbl_gl-rpt2.

PUT STREAM s-audit-rpt UNFORMATTED
  "Action    User ID/(User Name)            DB.Table/(Database)  Date       Time" SKIP
  "--------- ------------------------------ -------------------- ---------- --------" SKIP
  ttbl_header.v_action AT 1
  ttbl_header.v_userid AT 11
  ttbl_header.v_file AT 42
  ttbl_header.v_date FORMAT "99/99/9999" AT 63
  STRING(ttbl_header.v_time,"HH:MM:SS") AT 74.
FIND users WHERE users.user_id = v_userid NO-LOCK NO-ERROR.
IF AVAILABLE users THEN
PUT STREAM s-audit-rpt UNFORMATTED "(" AT 10 users.user_name ")".
ELSE
PUT STREAM s-audit-rpt UNFORMATTED "(user record does not exist)" AT 10.
PUT STREAM s-audit-rpt UNFORMATTED
  "(" AT 41 ttbl_header.v_db ")" SKIP(1)
  "Label: Value (database field-name)" AT 3.
IF action = "UPDATE" THEN
PUT STREAM s-audit-rpt UNFORMATTED " ( * - denotes field value change )".
PUT STREAM s-audit-rpt UNFORMATTED FILL("-",75) AT 3.
DO i = 1 TO 2:
IF NOT v_field_changed OR ttbl_gl-rpt1.acrange[i] NE ttbl_gl-rpt2.acrange[i] OR action NE "UPDATE" THEN
DO:
  PUT STREAM s-audit-rpt UNFORMATTED "Acct. range[" AT 3 i "]: " ttbl_gl-rpt1.acrange[i] " (acrange[" i "])".
  IF action = "UPDATE" THEN
  DO:
    IF ttbl_gl-rpt1.acrange[i] NE ttbl_gl-rpt2.acrange[i] THEN
    PUT STREAM s-audit-rpt UNFORMATTED "*" AT 1.
    PUT STREAM s-audit-rpt UNFORMATTED "Acct. range[" AT 3 i "]: " ttbl_gl-rpt2.acrange[i].
  END.
END.
END.
DO i = 1 TO 10:
IF NOT v_field_changed OR ttbl_gl-rpt1.acrange1[i] NE ttbl_gl-rpt2.acrange1[i] OR action NE "UPDATE" THEN
DO:
  PUT STREAM s-audit-rpt UNFORMATTED "Acct Ranges 2 - 5[" AT 3 i "]: " ttbl_gl-rpt1.acrange1[i] " (acrange1[" i "])".
  IF action = "UPDATE" THEN
  DO:
    IF ttbl_gl-rpt1.acrange1[i] NE ttbl_gl-rpt2.acrange1[i] THEN
    PUT STREAM s-audit-rpt UNFORMATTED "*" AT 1.
    PUT STREAM s-audit-rpt UNFORMATTED "Acct Ranges 2 - 5[" AT 3 i "]: " ttbl_gl-rpt2.acrange1[i].
  END.
END.
END.
DO i = 1 TO 60:
IF NOT v_field_changed OR ttbl_gl-rpt1.acrange2[i] NE ttbl_gl-rpt2.acrange2[i] OR action NE "UPDATE" THEN
DO:
  PUT STREAM s-audit-rpt UNFORMATTED "Acct. Range[" AT 3 i "]: " ttbl_gl-rpt1.acrange2[i] " (acrange2[" i "])".
  IF action = "UPDATE" THEN
  DO:
    IF ttbl_gl-rpt1.acrange2[i] NE ttbl_gl-rpt2.acrange2[i] THEN
    PUT STREAM s-audit-rpt UNFORMATTED "*" AT 1.
    PUT STREAM s-audit-rpt UNFORMATTED "Acct. Range[" AT 3 i "]: " ttbl_gl-rpt2.acrange2[i].
  END.
END.
END.
DO i = 1 TO 10:
IF NOT v_field_changed OR ttbl_gl-rpt1.actnum[i] NE ttbl_gl-rpt2.actnum[i] OR action NE "UPDATE" THEN
DO:
  PUT STREAM s-audit-rpt UNFORMATTED "Account Code[" AT 3 i "]: " ttbl_gl-rpt1.actnum[i] " (actnum[" i "])".
  IF action = "UPDATE" THEN
  DO:
    IF ttbl_gl-rpt1.actnum[i] NE ttbl_gl-rpt2.actnum[i] THEN
    PUT STREAM s-audit-rpt UNFORMATTED "*" AT 1.
    PUT STREAM s-audit-rpt UNFORMATTED "Account Code[" AT 3 i "]: " ttbl_gl-rpt2.actnum[i].
  END.
END.
END.
IF NOT v_field_changed OR ttbl_gl-rpt1.company NE ttbl_gl-rpt2.company OR action NE "UPDATE" THEN
DO:
  PUT STREAM s-audit-rpt UNFORMATTED "Company: " AT 3 ttbl_gl-rpt1.company " (company)".
  IF action = "UPDATE" THEN
  DO:
    IF ttbl_gl-rpt1.company NE ttbl_gl-rpt2.company THEN
    PUT STREAM s-audit-rpt UNFORMATTED "*" AT 1.
    PUT STREAM s-audit-rpt UNFORMATTED "Company: " AT 3 ttbl_gl-rpt2.company.
  END.
END.
IF NOT v_field_changed OR ttbl_gl-rpt1.dscr NE ttbl_gl-rpt2.dscr OR action NE "UPDATE" THEN
DO:
  PUT STREAM s-audit-rpt UNFORMATTED "Description: " AT 3 ttbl_gl-rpt1.dscr " (dscr)".
  IF action = "UPDATE" THEN
  DO:
    IF ttbl_gl-rpt1.dscr NE ttbl_gl-rpt2.dscr THEN
    PUT STREAM s-audit-rpt UNFORMATTED "*" AT 1.
    PUT STREAM s-audit-rpt UNFORMATTED "Description: " AT 3 ttbl_gl-rpt2.dscr.
  END.
END.
IF NOT v_field_changed OR ttbl_gl-rpt1.flag NE ttbl_gl-rpt2.flag OR action NE "UPDATE" THEN
DO:
  PUT STREAM s-audit-rpt UNFORMATTED "Flags: " AT 3 ttbl_gl-rpt1.flag " (flag)".
  IF action = "UPDATE" THEN
  DO:
    IF ttbl_gl-rpt1.flag NE ttbl_gl-rpt2.flag THEN
    PUT STREAM s-audit-rpt UNFORMATTED "*" AT 1.
    PUT STREAM s-audit-rpt UNFORMATTED "Flags: " AT 3 ttbl_gl-rpt2.flag.
  END.
END.
IF NOT v_field_changed OR ttbl_gl-rpt1.level NE ttbl_gl-rpt2.level OR action NE "UPDATE" THEN
DO:
  PUT STREAM s-audit-rpt UNFORMATTED "ST Level: " AT 3 ttbl_gl-rpt1.level FORMAT "99" " (level)".
  IF action = "UPDATE" THEN
  DO:
    IF ttbl_gl-rpt1.level NE ttbl_gl-rpt2.level THEN
    PUT STREAM s-audit-rpt UNFORMATTED "*" AT 1.
    PUT STREAM s-audit-rpt UNFORMATTED "ST Level: " AT 3 ttbl_gl-rpt2.level FORMAT "99".
  END.
END.
IF NOT v_field_changed OR ttbl_gl-rpt1.line NE ttbl_gl-rpt2.line OR action NE "UPDATE" THEN
DO:
  PUT STREAM s-audit-rpt UNFORMATTED "Line: " AT 3 ttbl_gl-rpt1.line FORMAT "9999" " (line)".
  IF action = "UPDATE" THEN
  DO:
    IF ttbl_gl-rpt1.line NE ttbl_gl-rpt2.line THEN
    PUT STREAM s-audit-rpt UNFORMATTED "*" AT 1.
    PUT STREAM s-audit-rpt UNFORMATTED "Line: " AT 3 ttbl_gl-rpt2.line FORMAT "9999".
  END.
END.
IF NOT v_field_changed OR ttbl_gl-rpt1.rpt NE ttbl_gl-rpt2.rpt OR action NE "UPDATE" THEN
DO:
  PUT STREAM s-audit-rpt UNFORMATTED "Report ID: " AT 3 ttbl_gl-rpt1.rpt " (rpt)".
  IF action = "UPDATE" THEN
  DO:
    IF ttbl_gl-rpt1.rpt NE ttbl_gl-rpt2.rpt THEN
    PUT STREAM s-audit-rpt UNFORMATTED "*" AT 1.
    PUT STREAM s-audit-rpt UNFORMATTED "Report ID: " AT 3 ttbl_gl-rpt2.rpt.
  END.
END.
IF NOT v_field_changed OR ttbl_gl-rpt1.type NE ttbl_gl-rpt2.type OR action NE "UPDATE" THEN
DO:
  PUT STREAM s-audit-rpt UNFORMATTED "Line Type: " AT 3 ttbl_gl-rpt1.type FORMAT "99" " (type)".
  IF action = "UPDATE" THEN
  DO:
    IF ttbl_gl-rpt1.type NE ttbl_gl-rpt2.type THEN
    PUT STREAM s-audit-rpt UNFORMATTED "*" AT 1.
    PUT STREAM s-audit-rpt UNFORMATTED "Line Type: " AT 3 ttbl_gl-rpt2.type FORMAT "99".
  END.
END.
IF v_show_index THEN
DO:
  PUT STREAM s-audit-rpt UNFORMATTED "Index Fields - Label: Value (database field-name)" AT 5.
  PUT STREAM s-audit-rpt UNFORMATTED "Company: " AT 7.
  IF action = "UPDATE" THEN
  PUT STREAM s-audit-rpt UNFORMATTED ttbl_gl-rpt2.company " (company)".
  ELSE
  PUT STREAM s-audit-rpt UNFORMATTED ttbl_gl-rpt1.company " (company)".
  PUT STREAM s-audit-rpt UNFORMATTED "Report ID: " AT 7.
  IF action = "UPDATE" THEN
  PUT STREAM s-audit-rpt UNFORMATTED ttbl_gl-rpt2.rpt " (rpt)".
  ELSE
  PUT STREAM s-audit-rpt UNFORMATTED ttbl_gl-rpt1.rpt " (rpt)".
  PUT STREAM s-audit-rpt UNFORMATTED "Line: " AT 7.
  IF action = "UPDATE" THEN
  PUT STREAM s-audit-rpt UNFORMATTED ttbl_gl-rpt2.line FORMAT "9999" " (line)".
  ELSE
  PUT STREAM s-audit-rpt UNFORMATTED ttbl_gl-rpt1.line FORMAT "9999" " (line)".
END.
PUT STREAM s-audit-rpt UNFORMATTED SKIP(1).
