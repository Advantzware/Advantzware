/* loc.p - generated 12/09/2005 - 15:42:48 by NoSweat */

/* this program was generated by auditgn1.p and should not be modified */
/* modifications should be made to the auto generation program         */

DEFINE INPUT PARAMETER action AS CHARACTER NO-UNDO.

DEFINE VARIABLE i AS INTEGER NO-UNDO.

{methods/defines/audit.i}
FIND FIRST ttbl_header.

DEFINE WORK-TABLE ttbl_loc1 NO-UNDO LIKE loc.
DEFINE WORK-TABLE ttbl_loc2 NO-UNDO LIKE loc.

CREATE ttbl_loc1.
IMPORT STREAM s_audit_file ttbl_loc1.
CREATE ttbl_loc2.
IF action = "UPDATE" THEN
IMPORT STREAM s_audit_file ttbl_loc2.

PUT STREAM s-audit-rpt UNFORMATTED
  "Action    User ID/(User Name)            DB.Table/(Database)  Date       Time" SKIP
  "--------- ------------------------------ -------------------- ---------- --------" SKIP
  ttbl_header.v_action AT 1
  ttbl_header.v_userid AT 11
  ttbl_header.v_file AT 42
  ttbl_header.v_date FORMAT "99/99/9999" AT 63
  STRING(ttbl_header.v_time,"HH:MM:SS") AT 74.
FIND users WHERE users.user_id = v_userid NO-LOCK NO-ERROR.
IF AVAILABLE users THEN
PUT STREAM s-audit-rpt UNFORMATTED "(" AT 10 users.user_name ")".
ELSE
PUT STREAM s-audit-rpt UNFORMATTED "(user record does not exist)" AT 10.
PUT STREAM s-audit-rpt UNFORMATTED
  "(" AT 41 ttbl_header.v_db ")" SKIP(1)
  "Label: Value (database field-name)" AT 3.
IF action = "UPDATE" THEN
PUT STREAM s-audit-rpt UNFORMATTED " ( * - denotes field value change )".
PUT STREAM s-audit-rpt UNFORMATTED FILL("-",75) AT 3.
IF NOT v_field_changed OR ttbl_loc1.company NE ttbl_loc2.company OR action NE "UPDATE" THEN
DO:
  PUT STREAM s-audit-rpt UNFORMATTED "Company: " AT 3 ttbl_loc1.company " (company)".
  IF action = "UPDATE" THEN
  DO:
    IF ttbl_loc1.company NE ttbl_loc2.company THEN
    PUT STREAM s-audit-rpt UNFORMATTED "*" AT 1.
    PUT STREAM s-audit-rpt UNFORMATTED "Company: " AT 3 ttbl_loc2.company.
  END.
END.
IF NOT v_field_changed OR ttbl_loc1.dscr NE ttbl_loc2.dscr OR action NE "UPDATE" THEN
DO:
  PUT STREAM s-audit-rpt UNFORMATTED "Description: " AT 3 ttbl_loc1.dscr " (dscr)".
  IF action = "UPDATE" THEN
  DO:
    IF ttbl_loc1.dscr NE ttbl_loc2.dscr THEN
    PUT STREAM s-audit-rpt UNFORMATTED "*" AT 1.
    PUT STREAM s-audit-rpt UNFORMATTED "Description: " AT 3 ttbl_loc2.dscr.
  END.
END.
IF NOT v_field_changed OR ttbl_loc1.loc NE ttbl_loc2.loc OR action NE "UPDATE" THEN
DO:
  PUT STREAM s-audit-rpt UNFORMATTED "Location: " AT 3 ttbl_loc1.loc " (loc)".
  IF action = "UPDATE" THEN
  DO:
    IF ttbl_loc1.loc NE ttbl_loc2.loc THEN
    PUT STREAM s-audit-rpt UNFORMATTED "*" AT 1.
    PUT STREAM s-audit-rpt UNFORMATTED "Location: " AT 3 ttbl_loc2.loc.
  END.
END.
IF NOT v_field_changed OR ttbl_loc1.wc-uom NE ttbl_loc2.wc-uom OR action NE "UPDATE" THEN
DO:
  PUT STREAM s-audit-rpt UNFORMATTED "Charge UOM: " AT 3 ttbl_loc1.wc-uom " (wc-uom)".
  IF action = "UPDATE" THEN
  DO:
    IF ttbl_loc1.wc-uom NE ttbl_loc2.wc-uom THEN
    PUT STREAM s-audit-rpt UNFORMATTED "*" AT 1.
    PUT STREAM s-audit-rpt UNFORMATTED "Charge UOM: " AT 3 ttbl_loc2.wc-uom.
  END.
END.
IF NOT v_field_changed OR ttbl_loc1.whs-chg NE ttbl_loc2.whs-chg OR action NE "UPDATE" THEN
DO:
  PUT STREAM s-audit-rpt UNFORMATTED "Warehouse Charge: " AT 3 ttbl_loc1.whs-chg FORMAT ">>9.9999" " (whs-chg)".
  IF action = "UPDATE" THEN
  DO:
    IF ttbl_loc1.whs-chg NE ttbl_loc2.whs-chg THEN
    PUT STREAM s-audit-rpt UNFORMATTED "*" AT 1.
    PUT STREAM s-audit-rpt UNFORMATTED "Warehouse Charge: " AT 3 ttbl_loc2.whs-chg FORMAT ">>9.9999".
  END.
END.
IF v_show_index THEN
DO:
  PUT STREAM s-audit-rpt UNFORMATTED "Index Fields - Label: Value (database field-name)" AT 5.
  PUT STREAM s-audit-rpt UNFORMATTED "Company: " AT 7.
  IF action = "UPDATE" THEN
  PUT STREAM s-audit-rpt UNFORMATTED ttbl_loc2.company " (company)".
  ELSE
  PUT STREAM s-audit-rpt UNFORMATTED ttbl_loc1.company " (company)".
  PUT STREAM s-audit-rpt UNFORMATTED "Location: " AT 7.
  IF action = "UPDATE" THEN
  PUT STREAM s-audit-rpt UNFORMATTED ttbl_loc2.loc " (loc)".
  ELSE
  PUT STREAM s-audit-rpt UNFORMATTED ttbl_loc1.loc " (loc)".
END.
PUT STREAM s-audit-rpt UNFORMATTED SKIP(1).
