/* contact.p - generated 12/02/2005 - 16:52:38 by NoSweat */

/* this program was generated by auditgn1.p and should not be modified */
/* modifications should be made to the auto generation program         */

DEFINE INPUT PARAMETER action AS CHARACTER NO-UNDO.

DEFINE VARIABLE i AS INTEGER NO-UNDO.

{methods/defines/audit.i}
FIND FIRST ttbl_header.

DEFINE WORK-TABLE ttbl_contact1 NO-UNDO LIKE contact.
DEFINE WORK-TABLE ttbl_contact2 NO-UNDO LIKE contact.

CREATE ttbl_contact1.
IMPORT STREAM s_audit_file ttbl_contact1.
CREATE ttbl_contact2.
IF action = "UPDATE" THEN
IMPORT STREAM s_audit_file ttbl_contact2.

PUT STREAM s-audit-rpt UNFORMATTED
  "Action    User ID/(User Name)            DB.Table/(Database)  Date       Time" SKIP
  "--------- ------------------------------ -------------------- ---------- --------" SKIP
  ttbl_header.v_action AT 1
  ttbl_header.v_userid AT 11
  ttbl_header.v_file AT 42
  ttbl_header.v_date FORMAT "99/99/9999" AT 63
  STRING(ttbl_header.v_time,"HH:MM:SS") AT 74.
FIND users WHERE users.user_id = v_userid NO-LOCK NO-ERROR.
IF AVAILABLE users THEN
PUT STREAM s-audit-rpt UNFORMATTED "(" AT 10 users.user_name ")".
ELSE
PUT STREAM s-audit-rpt UNFORMATTED "(user record does not exist)" AT 10.
PUT STREAM s-audit-rpt UNFORMATTED
  "(" AT 41 ttbl_header.v_db ")" SKIP(1)
  "Label: Value (database field-name)" AT 3.
IF action = "UPDATE" THEN
PUT STREAM s-audit-rpt UNFORMATTED " ( * - denotes field value change )".
PUT STREAM s-audit-rpt UNFORMATTED FILL("-",75) AT 3.
IF NOT v_field_changed OR ttbl_contact1.access-code NE ttbl_contact2.access-code OR action NE "UPDATE" THEN
DO:
  PUT STREAM s-audit-rpt UNFORMATTED "Access Code: " AT 3 ttbl_contact1.access-code " (access-code)".
  IF action = "UPDATE" THEN
  DO:
    IF ttbl_contact1.access-code NE ttbl_contact2.access-code THEN
    PUT STREAM s-audit-rpt UNFORMATTED "*" AT 1.
    PUT STREAM s-audit-rpt UNFORMATTED "Access Code: " AT 3 ttbl_contact2.access-code.
  END.
END.
IF NOT v_field_changed OR ttbl_contact1.addr1 NE ttbl_contact2.addr1 OR action NE "UPDATE" THEN
DO:
  PUT STREAM s-audit-rpt UNFORMATTED "Address1: " AT 3 ttbl_contact1.addr1 " (addr1)".
  IF action = "UPDATE" THEN
  DO:
    IF ttbl_contact1.addr1 NE ttbl_contact2.addr1 THEN
    PUT STREAM s-audit-rpt UNFORMATTED "*" AT 1.
    PUT STREAM s-audit-rpt UNFORMATTED "Address1: " AT 3 ttbl_contact2.addr1.
  END.
END.
IF NOT v_field_changed OR ttbl_contact1.addr2 NE ttbl_contact2.addr2 OR action NE "UPDATE" THEN
DO:
  PUT STREAM s-audit-rpt UNFORMATTED "Address2: " AT 3 ttbl_contact1.addr2 " (addr2)".
  IF action = "UPDATE" THEN
  DO:
    IF ttbl_contact1.addr2 NE ttbl_contact2.addr2 THEN
    PUT STREAM s-audit-rpt UNFORMATTED "*" AT 1.
    PUT STREAM s-audit-rpt UNFORMATTED "Address2: " AT 3 ttbl_contact2.addr2.
  END.
END.
IF NOT v_field_changed OR ttbl_contact1.cell-phone NE ttbl_contact2.cell-phone OR action NE "UPDATE" THEN
DO:
  PUT STREAM s-audit-rpt UNFORMATTED "Cell Phone: " AT 3 ttbl_contact1.cell-phone " (cell-phone)".
  IF action = "UPDATE" THEN
  DO:
    IF ttbl_contact1.cell-phone NE ttbl_contact2.cell-phone THEN
    PUT STREAM s-audit-rpt UNFORMATTED "*" AT 1.
    PUT STREAM s-audit-rpt UNFORMATTED "Cell Phone: " AT 3 ttbl_contact2.cell-phone.
  END.
END.
IF NOT v_field_changed OR ttbl_contact1.city NE ttbl_contact2.city OR action NE "UPDATE" THEN
DO:
  PUT STREAM s-audit-rpt UNFORMATTED "City: " AT 3 ttbl_contact1.city " (city)".
  IF action = "UPDATE" THEN
  DO:
    IF ttbl_contact1.city NE ttbl_contact2.city THEN
    PUT STREAM s-audit-rpt UNFORMATTED "*" AT 1.
    PUT STREAM s-audit-rpt UNFORMATTED "City: " AT 3 ttbl_contact2.city.
  END.
END.
IF NOT v_field_changed OR ttbl_contact1.company NE ttbl_contact2.company OR action NE "UPDATE" THEN
DO:
  PUT STREAM s-audit-rpt UNFORMATTED "Company: " AT 3 ttbl_contact1.company " (company)".
  IF action = "UPDATE" THEN
  DO:
    IF ttbl_contact1.company NE ttbl_contact2.company THEN
    PUT STREAM s-audit-rpt UNFORMATTED "*" AT 1.
    PUT STREAM s-audit-rpt UNFORMATTED "Company: " AT 3 ttbl_contact2.company.
  END.
END.
IF NOT v_field_changed OR ttbl_contact1.contact-loc NE ttbl_contact2.contact-loc OR action NE "UPDATE" THEN
DO:
  PUT STREAM s-audit-rpt UNFORMATTED "Location: " AT 3 ttbl_contact1.contact-loc " (contact-loc)".
  IF action = "UPDATE" THEN
  DO:
    IF ttbl_contact1.contact-loc NE ttbl_contact2.contact-loc THEN
    PUT STREAM s-audit-rpt UNFORMATTED "*" AT 1.
    PUT STREAM s-audit-rpt UNFORMATTED "Location: " AT 3 ttbl_contact2.contact-loc.
  END.
END.
IF NOT v_field_changed OR ttbl_contact1.contact-title NE ttbl_contact2.contact-title OR action NE "UPDATE" THEN
DO:
  PUT STREAM s-audit-rpt UNFORMATTED "Title: " AT 3 ttbl_contact1.contact-title " (contact-title)".
  IF action = "UPDATE" THEN
  DO:
    IF ttbl_contact1.contact-title NE ttbl_contact2.contact-title THEN
    PUT STREAM s-audit-rpt UNFORMATTED "*" AT 1.
    PUT STREAM s-audit-rpt UNFORMATTED "Title: " AT 3 ttbl_contact2.contact-title.
  END.
END.
IF NOT v_field_changed OR ttbl_contact1.country NE ttbl_contact2.country OR action NE "UPDATE" THEN
DO:
  PUT STREAM s-audit-rpt UNFORMATTED "Country: " AT 3 ttbl_contact1.country " (country)".
  IF action = "UPDATE" THEN
  DO:
    IF ttbl_contact1.country NE ttbl_contact2.country THEN
    PUT STREAM s-audit-rpt UNFORMATTED "*" AT 1.
    PUT STREAM s-audit-rpt UNFORMATTED "Country: " AT 3 ttbl_contact2.country.
  END.
END.
IF NOT v_field_changed OR ttbl_contact1.county NE ttbl_contact2.county OR action NE "UPDATE" THEN
DO:
  PUT STREAM s-audit-rpt UNFORMATTED "County: " AT 3 ttbl_contact1.county " (county)".
  IF action = "UPDATE" THEN
  DO:
    IF ttbl_contact1.county NE ttbl_contact2.county THEN
    PUT STREAM s-audit-rpt UNFORMATTED "*" AT 1.
    PUT STREAM s-audit-rpt UNFORMATTED "County: " AT 3 ttbl_contact2.county.
  END.
END.
IF NOT v_field_changed OR ttbl_contact1.cust-name NE ttbl_contact2.cust-name OR action NE "UPDATE" THEN
DO:
  PUT STREAM s-audit-rpt UNFORMATTED "Company: " AT 3 ttbl_contact1.cust-name " (cust-name)".
  IF action = "UPDATE" THEN
  DO:
    IF ttbl_contact1.cust-name NE ttbl_contact2.cust-name THEN
    PUT STREAM s-audit-rpt UNFORMATTED "*" AT 1.
    PUT STREAM s-audit-rpt UNFORMATTED "Company: " AT 3 ttbl_contact2.cust-name.
  END.
END.
IF NOT v_field_changed OR ttbl_contact1.cust-no NE ttbl_contact2.cust-no OR action NE "UPDATE" THEN
DO:
  PUT STREAM s-audit-rpt UNFORMATTED "Customer Number: " AT 3 ttbl_contact1.cust-no " (cust-no)".
  IF action = "UPDATE" THEN
  DO:
    IF ttbl_contact1.cust-no NE ttbl_contact2.cust-no THEN
    PUT STREAM s-audit-rpt UNFORMATTED "*" AT 1.
    PUT STREAM s-audit-rpt UNFORMATTED "Customer Number: " AT 3 ttbl_contact2.cust-no.
  END.
END.
IF NOT v_field_changed OR ttbl_contact1.email NE ttbl_contact2.email OR action NE "UPDATE" THEN
DO:
  PUT STREAM s-audit-rpt UNFORMATTED "Email: " AT 3 ttbl_contact1.email " (email)".
  IF action = "UPDATE" THEN
  DO:
    IF ttbl_contact1.email NE ttbl_contact2.email THEN
    PUT STREAM s-audit-rpt UNFORMATTED "*" AT 1.
    PUT STREAM s-audit-rpt UNFORMATTED "Email: " AT 3 ttbl_contact2.email.
  END.
END.
IF NOT v_field_changed OR ttbl_contact1.extension NE ttbl_contact2.extension OR action NE "UPDATE" THEN
DO:
  PUT STREAM s-audit-rpt UNFORMATTED "Extension: " AT 3 ttbl_contact1.extension " (extension)".
  IF action = "UPDATE" THEN
  DO:
    IF ttbl_contact1.extension NE ttbl_contact2.extension THEN
    PUT STREAM s-audit-rpt UNFORMATTED "*" AT 1.
    PUT STREAM s-audit-rpt UNFORMATTED "Extension: " AT 3 ttbl_contact2.extension.
  END.
END.
IF NOT v_field_changed OR ttbl_contact1.fax NE ttbl_contact2.fax OR action NE "UPDATE" THEN
DO:
  PUT STREAM s-audit-rpt UNFORMATTED "Fax: " AT 3 ttbl_contact1.fax " (fax)".
  IF action = "UPDATE" THEN
  DO:
    IF ttbl_contact1.fax NE ttbl_contact2.fax THEN
    PUT STREAM s-audit-rpt UNFORMATTED "*" AT 1.
    PUT STREAM s-audit-rpt UNFORMATTED "Fax: " AT 3 ttbl_contact2.fax.
  END.
END.
IF NOT v_field_changed OR ttbl_contact1.first-name NE ttbl_contact2.first-name OR action NE "UPDATE" THEN
DO:
  PUT STREAM s-audit-rpt UNFORMATTED "First Name: " AT 3 ttbl_contact1.first-name " (first-name)".
  IF action = "UPDATE" THEN
  DO:
    IF ttbl_contact1.first-name NE ttbl_contact2.first-name THEN
    PUT STREAM s-audit-rpt UNFORMATTED "*" AT 1.
    PUT STREAM s-audit-rpt UNFORMATTED "First Name: " AT 3 ttbl_contact2.first-name.
  END.
END.
IF NOT v_field_changed OR ttbl_contact1.last-name NE ttbl_contact2.last-name OR action NE "UPDATE" THEN
DO:
  PUT STREAM s-audit-rpt UNFORMATTED "Last Name: " AT 3 ttbl_contact1.last-name " (last-name)".
  IF action = "UPDATE" THEN
  DO:
    IF ttbl_contact1.last-name NE ttbl_contact2.last-name THEN
    PUT STREAM s-audit-rpt UNFORMATTED "*" AT 1.
    PUT STREAM s-audit-rpt UNFORMATTED "Last Name: " AT 3 ttbl_contact2.last-name.
  END.
END.
IF NOT v_field_changed OR ttbl_contact1.list-name NE ttbl_contact2.list-name OR action NE "UPDATE" THEN
DO:
  PUT STREAM s-audit-rpt UNFORMATTED "List Name: " AT 3 ttbl_contact1.list-name " (list-name)".
  IF action = "UPDATE" THEN
  DO:
    IF ttbl_contact1.list-name NE ttbl_contact2.list-name THEN
    PUT STREAM s-audit-rpt UNFORMATTED "*" AT 1.
    PUT STREAM s-audit-rpt UNFORMATTED "List Name: " AT 3 ttbl_contact2.list-name.
  END.
END.
IF NOT v_field_changed OR ttbl_contact1.list-no NE ttbl_contact2.list-no OR action NE "UPDATE" THEN
DO:
  PUT STREAM s-audit-rpt UNFORMATTED "List#: " AT 3 ttbl_contact1.list-no FORMAT ">>>>>9" " (list-no)".
  IF action = "UPDATE" THEN
  DO:
    IF ttbl_contact1.list-no NE ttbl_contact2.list-no THEN
    PUT STREAM s-audit-rpt UNFORMATTED "*" AT 1.
    PUT STREAM s-audit-rpt UNFORMATTED "List#: " AT 3 ttbl_contact2.list-no FORMAT ">>>>>9".
  END.
END.
IF NOT v_field_changed OR ttbl_contact1.maillist NE ttbl_contact2.maillist OR action NE "UPDATE" THEN
DO:
  PUT STREAM s-audit-rpt UNFORMATTED "Mail List: " AT 3 ttbl_contact1.maillist FORMAT "yes/no" " (maillist)".
  IF action = "UPDATE" THEN
  DO:
    IF ttbl_contact1.maillist NE ttbl_contact2.maillist THEN
    PUT STREAM s-audit-rpt UNFORMATTED "*" AT 1.
    PUT STREAM s-audit-rpt UNFORMATTED "Mail List: " AT 3 ttbl_contact2.maillist FORMAT "yes/no".
  END.
END.
IF NOT v_field_changed OR ttbl_contact1.middle-initial NE ttbl_contact2.middle-initial OR action NE "UPDATE" THEN
DO:
  PUT STREAM s-audit-rpt UNFORMATTED "Middle Initial: " AT 3 ttbl_contact1.middle-initial " (middle-initial)".
  IF action = "UPDATE" THEN
  DO:
    IF ttbl_contact1.middle-initial NE ttbl_contact2.middle-initial THEN
    PUT STREAM s-audit-rpt UNFORMATTED "*" AT 1.
    PUT STREAM s-audit-rpt UNFORMATTED "Middle Initial: " AT 3 ttbl_contact2.middle-initial.
  END.
END.
IF NOT v_field_changed OR ttbl_contact1.phone NE ttbl_contact2.phone OR action NE "UPDATE" THEN
DO:
  PUT STREAM s-audit-rpt UNFORMATTED "Phone: " AT 3 ttbl_contact1.phone " (phone)".
  IF action = "UPDATE" THEN
  DO:
    IF ttbl_contact1.phone NE ttbl_contact2.phone THEN
    PUT STREAM s-audit-rpt UNFORMATTED "*" AT 1.
    PUT STREAM s-audit-rpt UNFORMATTED "Phone: " AT 3 ttbl_contact2.phone.
  END.
END.
IF NOT v_field_changed OR ttbl_contact1.ship-id NE ttbl_contact2.ship-id OR action NE "UPDATE" THEN
DO:
  PUT STREAM s-audit-rpt UNFORMATTED "Shipto ID: " AT 3 ttbl_contact1.ship-id " (ship-id)".
  IF action = "UPDATE" THEN
  DO:
    IF ttbl_contact1.ship-id NE ttbl_contact2.ship-id THEN
    PUT STREAM s-audit-rpt UNFORMATTED "*" AT 1.
    PUT STREAM s-audit-rpt UNFORMATTED "Shipto ID: " AT 3 ttbl_contact2.ship-id.
  END.
END.
IF NOT v_field_changed OR ttbl_contact1.sirname NE ttbl_contact2.sirname OR action NE "UPDATE" THEN
DO:
  PUT STREAM s-audit-rpt UNFORMATTED "Sir Name: " AT 3 ttbl_contact1.sirname " (sirname)".
  IF action = "UPDATE" THEN
  DO:
    IF ttbl_contact1.sirname NE ttbl_contact2.sirname THEN
    PUT STREAM s-audit-rpt UNFORMATTED "*" AT 1.
    PUT STREAM s-audit-rpt UNFORMATTED "Sir Name: " AT 3 ttbl_contact2.sirname.
  END.
END.
IF NOT v_field_changed OR ttbl_contact1.sman NE ttbl_contact2.sman OR action NE "UPDATE" THEN
DO:
  PUT STREAM s-audit-rpt UNFORMATTED "Salesman: " AT 3 ttbl_contact1.sman " (sman)".
  IF action = "UPDATE" THEN
  DO:
    IF ttbl_contact1.sman NE ttbl_contact2.sman THEN
    PUT STREAM s-audit-rpt UNFORMATTED "*" AT 1.
    PUT STREAM s-audit-rpt UNFORMATTED "Salesman: " AT 3 ttbl_contact2.sman.
  END.
END.
IF NOT v_field_changed OR ttbl_contact1.state NE ttbl_contact2.state OR action NE "UPDATE" THEN
DO:
  PUT STREAM s-audit-rpt UNFORMATTED "State: " AT 3 ttbl_contact1.state " (state)".
  IF action = "UPDATE" THEN
  DO:
    IF ttbl_contact1.state NE ttbl_contact2.state THEN
    PUT STREAM s-audit-rpt UNFORMATTED "*" AT 1.
    PUT STREAM s-audit-rpt UNFORMATTED "State: " AT 3 ttbl_contact2.state.
  END.
END.
IF NOT v_field_changed OR ttbl_contact1.territory NE ttbl_contact2.territory OR action NE "UPDATE" THEN
DO:
  PUT STREAM s-audit-rpt UNFORMATTED "Territory: " AT 3 ttbl_contact1.territory " (territory)".
  IF action = "UPDATE" THEN
  DO:
    IF ttbl_contact1.territory NE ttbl_contact2.territory THEN
    PUT STREAM s-audit-rpt UNFORMATTED "*" AT 1.
    PUT STREAM s-audit-rpt UNFORMATTED "Territory: " AT 3 ttbl_contact2.territory.
  END.
END.
IF NOT v_field_changed OR ttbl_contact1.type NE ttbl_contact2.type OR action NE "UPDATE" THEN
DO:
  PUT STREAM s-audit-rpt UNFORMATTED "Type: " AT 3 ttbl_contact1.type " (type)".
  IF action = "UPDATE" THEN
  DO:
    IF ttbl_contact1.type NE ttbl_contact2.type THEN
    PUT STREAM s-audit-rpt UNFORMATTED "*" AT 1.
    PUT STREAM s-audit-rpt UNFORMATTED "Type: " AT 3 ttbl_contact2.type.
  END.
END.
IF NOT v_field_changed OR ttbl_contact1.website NE ttbl_contact2.website OR action NE "UPDATE" THEN
DO:
  PUT STREAM s-audit-rpt UNFORMATTED "Website: " AT 3 ttbl_contact1.website " (website)".
  IF action = "UPDATE" THEN
  DO:
    IF ttbl_contact1.website NE ttbl_contact2.website THEN
    PUT STREAM s-audit-rpt UNFORMATTED "*" AT 1.
    PUT STREAM s-audit-rpt UNFORMATTED "Website: " AT 3 ttbl_contact2.website.
  END.
END.
IF NOT v_field_changed OR ttbl_contact1.zip NE ttbl_contact2.zip OR action NE "UPDATE" THEN
DO:
  PUT STREAM s-audit-rpt UNFORMATTED "Zip: " AT 3 ttbl_contact1.zip " (zip)".
  IF action = "UPDATE" THEN
  DO:
    IF ttbl_contact1.zip NE ttbl_contact2.zip THEN
    PUT STREAM s-audit-rpt UNFORMATTED "*" AT 1.
    PUT STREAM s-audit-rpt UNFORMATTED "Zip: " AT 3 ttbl_contact2.zip.
  END.
END.
IF v_show_index THEN
DO:
  PUT STREAM s-audit-rpt UNFORMATTED "Index Fields - Label: Value (database field-name)" AT 5.
  PUT STREAM s-audit-rpt UNFORMATTED "Company: " AT 7.
  IF action = "UPDATE" THEN
  PUT STREAM s-audit-rpt UNFORMATTED ttbl_contact2.company " (company)".
  ELSE
  PUT STREAM s-audit-rpt UNFORMATTED ttbl_contact1.company " (company)".
  PUT STREAM s-audit-rpt UNFORMATTED "Customer Number: " AT 7.
  IF action = "UPDATE" THEN
  PUT STREAM s-audit-rpt UNFORMATTED ttbl_contact2.cust-no " (cust-no)".
  ELSE
  PUT STREAM s-audit-rpt UNFORMATTED ttbl_contact1.cust-no " (cust-no)".
  PUT STREAM s-audit-rpt UNFORMATTED "Shipto ID: " AT 7.
  IF action = "UPDATE" THEN
  PUT STREAM s-audit-rpt UNFORMATTED ttbl_contact2.ship-id " (ship-id)".
  ELSE
  PUT STREAM s-audit-rpt UNFORMATTED ttbl_contact1.ship-id " (ship-id)".
  PUT STREAM s-audit-rpt UNFORMATTED "Last Name: " AT 7.
  IF action = "UPDATE" THEN
  PUT STREAM s-audit-rpt UNFORMATTED ttbl_contact2.last-name " (last-name)".
  ELSE
  PUT STREAM s-audit-rpt UNFORMATTED ttbl_contact1.last-name " (last-name)".
  PUT STREAM s-audit-rpt UNFORMATTED "First Name: " AT 7.
  IF action = "UPDATE" THEN
  PUT STREAM s-audit-rpt UNFORMATTED ttbl_contact2.first-name " (first-name)".
  ELSE
  PUT STREAM s-audit-rpt UNFORMATTED ttbl_contact1.first-name " (first-name)".
  PUT STREAM s-audit-rpt UNFORMATTED "Middle Initial: " AT 7.
  IF action = "UPDATE" THEN
  PUT STREAM s-audit-rpt UNFORMATTED ttbl_contact2.middle-initial " (middle-initial)".
  ELSE
  PUT STREAM s-audit-rpt UNFORMATTED ttbl_contact1.middle-initial " (middle-initial)".
END.
PUT STREAM s-audit-rpt UNFORMATTED SKIP(1).
