/* terr.p - generated 12/09/2005 - 15:42:55 by NoSweat */

/* this program was generated by auditgn1.p and should not be modified */
/* modifications should be made to the auto generation program         */

DEFINE INPUT PARAMETER action AS CHARACTER NO-UNDO.

DEFINE VARIABLE i AS INTEGER NO-UNDO.

{methods/defines/audit.i}
FIND FIRST ttbl_header.

DEFINE WORK-TABLE ttbl_terr1 NO-UNDO LIKE terr.
DEFINE WORK-TABLE ttbl_terr2 NO-UNDO LIKE terr.

CREATE ttbl_terr1.
IMPORT STREAM s_audit_file ttbl_terr1.
CREATE ttbl_terr2.
IF action = "UPDATE" THEN
IMPORT STREAM s_audit_file ttbl_terr2.

PUT STREAM s-audit-rpt UNFORMATTED
  "Action    User ID/(User Name)            DB.Table/(Database)  Date       Time" SKIP
  "--------- ------------------------------ -------------------- ---------- --------" SKIP
  ttbl_header.v_action AT 1
  ttbl_header.v_userid AT 11
  ttbl_header.v_file AT 42
  ttbl_header.v_date FORMAT "99/99/9999" AT 63
  STRING(ttbl_header.v_time,"HH:MM:SS") AT 74.
FIND users WHERE users.user_id = v_userid NO-LOCK NO-ERROR.
IF AVAILABLE users THEN
PUT STREAM s-audit-rpt UNFORMATTED "(" AT 10 users.user_name ")".
ELSE
PUT STREAM s-audit-rpt UNFORMATTED "(user record does not exist)" AT 10.
PUT STREAM s-audit-rpt UNFORMATTED
  "(" AT 41 ttbl_header.v_db ")" SKIP(1)
  "Label: Value (database field-name)" AT 3.
IF action = "UPDATE" THEN
PUT STREAM s-audit-rpt UNFORMATTED " ( * - denotes field value change )".
PUT STREAM s-audit-rpt UNFORMATTED FILL("-",75) AT 3.
IF NOT v_field_changed OR ttbl_terr1.company NE ttbl_terr2.company OR action NE "UPDATE" THEN
DO:
  PUT STREAM s-audit-rpt UNFORMATTED "Company: " AT 3 ttbl_terr1.company " (company)".
  IF action = "UPDATE" THEN
  DO:
    IF ttbl_terr1.company NE ttbl_terr2.company THEN
    PUT STREAM s-audit-rpt UNFORMATTED "*" AT 1.
    PUT STREAM s-audit-rpt UNFORMATTED "Company: " AT 3 ttbl_terr2.company.
  END.
END.
IF NOT v_field_changed OR ttbl_terr1.dscr NE ttbl_terr2.dscr OR action NE "UPDATE" THEN
DO:
  PUT STREAM s-audit-rpt UNFORMATTED "Description: " AT 3 ttbl_terr1.dscr " (dscr)".
  IF action = "UPDATE" THEN
  DO:
    IF ttbl_terr1.dscr NE ttbl_terr2.dscr THEN
    PUT STREAM s-audit-rpt UNFORMATTED "*" AT 1.
    PUT STREAM s-audit-rpt UNFORMATTED "Description: " AT 3 ttbl_terr2.dscr.
  END.
END.
IF NOT v_field_changed OR ttbl_terr1.terr NE ttbl_terr2.terr OR action NE "UPDATE" THEN
DO:
  PUT STREAM s-audit-rpt UNFORMATTED "Territory: " AT 3 ttbl_terr1.terr " (terr)".
  IF action = "UPDATE" THEN
  DO:
    IF ttbl_terr1.terr NE ttbl_terr2.terr THEN
    PUT STREAM s-audit-rpt UNFORMATTED "*" AT 1.
    PUT STREAM s-audit-rpt UNFORMATTED "Territory: " AT 3 ttbl_terr2.terr.
  END.
END.
IF v_show_index THEN
DO:
  PUT STREAM s-audit-rpt UNFORMATTED "Index Fields - Label: Value (database field-name)" AT 5.
  PUT STREAM s-audit-rpt UNFORMATTED "Company: " AT 7.
  IF action = "UPDATE" THEN
  PUT STREAM s-audit-rpt UNFORMATTED ttbl_terr2.company " (company)".
  ELSE
  PUT STREAM s-audit-rpt UNFORMATTED ttbl_terr1.company " (company)".
  PUT STREAM s-audit-rpt UNFORMATTED "Territory: " AT 7.
  IF action = "UPDATE" THEN
  PUT STREAM s-audit-rpt UNFORMATTED ttbl_terr2.terr " (terr)".
  ELSE
  PUT STREAM s-audit-rpt UNFORMATTED ttbl_terr1.terr " (terr)".
END.
PUT STREAM s-audit-rpt UNFORMATTED SKIP(1).
