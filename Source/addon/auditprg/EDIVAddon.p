/* EDIVAddon.p - generated 01/10/2001 - 13:00:00 by nosweat */

/* this program was generated by auditgn1.p and should not be modified */
/* modifications should be made to the auto generation program         */

DEFINE INPUT PARAMETER action AS CHARACTER NO-UNDO.

DEFINE VARIABLE i AS INTEGER NO-UNDO.

{methods/defines/audit.i}
FIND FIRST ttbl_header.

DEFINE WORK-TABLE ttbl_EDIVAddon1 NO-UNDO LIKE EDIVAddon.
DEFINE WORK-TABLE ttbl_EDIVAddon2 NO-UNDO LIKE EDIVAddon.

CREATE ttbl_EDIVAddon1.
IMPORT STREAM s_audit_file ttbl_EDIVAddon1.
CREATE ttbl_EDIVAddon2.
IF action = "UPDATE" THEN
IMPORT STREAM s_audit_file ttbl_EDIVAddon2.

PUT STREAM s-audit-rpt UNFORMATTED
  "Action    User ID/(User Name)            DB.Table/(Database)  Date       Time" SKIP
  "--------- ------------------------------ -------------------- ---------- --------" SKIP
  ttbl_header.v_action AT 1
  ttbl_header.v_userid AT 11
  ttbl_header.v_file AT 42
  ttbl_header.v_date FORMAT "99/99/9999" AT 63
  STRING(ttbl_header.v_time,"HH:MM:SS") AT 74.
FIND users WHERE users.user_id = v_userid NO-LOCK NO-ERROR.
IF AVAILABLE users THEN
PUT STREAM s-audit-rpt UNFORMATTED "(" AT 10 users.user_name ")".
ELSE
PUT STREAM s-audit-rpt UNFORMATTED "(user record does not exist)" AT 10.
PUT STREAM s-audit-rpt UNFORMATTED
  "(" AT 41 ttbl_header.v_db ")" SKIP(1)
  "Label: Value (database field-name)" AT 3.
IF action = "UPDATE" THEN
PUT STREAM s-audit-rpt UNFORMATTED " ( * - denotes field value change )".
PUT STREAM s-audit-rpt UNFORMATTED FILL("-",75) AT 3.
IF NOT v_field_changed OR ttbl_EDIVAddon1.Addon-line NE ttbl_EDIVAddon2.Addon-line OR action NE "UPDATE" THEN
DO:
  PUT STREAM s-audit-rpt UNFORMATTED "?: " AT 3 ttbl_EDIVAddon1.Addon-line FORMAT ">>9" " (Addon-line)".
  IF action = "UPDATE" THEN
  DO:
    IF ttbl_EDIVAddon1.Addon-line NE ttbl_EDIVAddon2.Addon-line THEN
    PUT STREAM s-audit-rpt UNFORMATTED "*" AT 1.
    PUT STREAM s-audit-rpt UNFORMATTED "?: " AT 3 ttbl_EDIVAddon2.Addon-line FORMAT ">>9".
  END.
END.
IF NOT v_field_changed OR ttbl_EDIVAddon1.Agency-code NE ttbl_EDIVAddon2.Agency-code OR action NE "UPDATE" THEN
DO:
  PUT STREAM s-audit-rpt UNFORMATTED "?: " AT 3 ttbl_EDIVAddon1.Agency-code " (Agency-code)".
  IF action = "UPDATE" THEN
  DO:
    IF ttbl_EDIVAddon1.Agency-code NE ttbl_EDIVAddon2.Agency-code THEN
    PUT STREAM s-audit-rpt UNFORMATTED "*" AT 1.
    PUT STREAM s-audit-rpt UNFORMATTED "?: " AT 3 ttbl_EDIVAddon2.Agency-code.
  END.
END.
IF NOT v_field_changed OR ttbl_EDIVAddon1.Agency-qual NE ttbl_EDIVAddon2.Agency-qual OR action NE "UPDATE" THEN
DO:
  PUT STREAM s-audit-rpt UNFORMATTED "?: " AT 3 ttbl_EDIVAddon1.Agency-qual " (Agency-qual)".
  IF action = "UPDATE" THEN
  DO:
    IF ttbl_EDIVAddon1.Agency-qual NE ttbl_EDIVAddon2.Agency-qual THEN
    PUT STREAM s-audit-rpt UNFORMATTED "*" AT 1.
    PUT STREAM s-audit-rpt UNFORMATTED "?: " AT 3 ttbl_EDIVAddon2.Agency-qual.
  END.
END.
IF NOT v_field_changed OR ttbl_EDIVAddon1.Allow-charge NE ttbl_EDIVAddon2.Allow-charge OR action NE "UPDATE" THEN
DO:
  PUT STREAM s-audit-rpt UNFORMATTED "a-c: " AT 3 ttbl_EDIVAddon1.Allow-charge FORMAT "A/C" " (Allow-charge)".
  IF action = "UPDATE" THEN
  DO:
    IF ttbl_EDIVAddon1.Allow-charge NE ttbl_EDIVAddon2.Allow-charge THEN
    PUT STREAM s-audit-rpt UNFORMATTED "*" AT 1.
    PUT STREAM s-audit-rpt UNFORMATTED "a-c: " AT 3 ttbl_EDIVAddon2.Allow-charge FORMAT "A/C".
  END.
END.
IF NOT v_field_changed OR ttbl_EDIVAddon1.Amount NE ttbl_EDIVAddon2.Amount OR action NE "UPDATE" THEN
DO:
  PUT STREAM s-audit-rpt UNFORMATTED "?: " AT 3 ttbl_EDIVAddon1.Amount FORMAT "->>>,>>>.99" " (Amount)".
  IF action = "UPDATE" THEN
  DO:
    IF ttbl_EDIVAddon1.Amount NE ttbl_EDIVAddon2.Amount THEN
    PUT STREAM s-audit-rpt UNFORMATTED "*" AT 1.
    PUT STREAM s-audit-rpt UNFORMATTED "?: " AT 3 ttbl_EDIVAddon2.Amount FORMAT "->>>,>>>.99".
  END.
END.
IF NOT v_field_changed OR ttbl_EDIVAddon1.Basis-qual NE ttbl_EDIVAddon2.Basis-qual OR action NE "UPDATE" THEN
DO:
  PUT STREAM s-audit-rpt UNFORMATTED "?: " AT 3 ttbl_EDIVAddon1.Basis-qual " (Basis-qual)".
  IF action = "UPDATE" THEN
  DO:
    IF ttbl_EDIVAddon1.Basis-qual NE ttbl_EDIVAddon2.Basis-qual THEN
    PUT STREAM s-audit-rpt UNFORMATTED "*" AT 1.
    PUT STREAM s-audit-rpt UNFORMATTED "?: " AT 3 ttbl_EDIVAddon2.Basis-qual.
  END.
END.
IF NOT v_field_changed OR ttbl_EDIVAddon1.Company NE ttbl_EDIVAddon2.Company OR action NE "UPDATE" THEN
DO:
  PUT STREAM s-audit-rpt UNFORMATTED "?: " AT 3 ttbl_EDIVAddon1.Company " (Company)".
  IF action = "UPDATE" THEN
  DO:
    IF ttbl_EDIVAddon1.Company NE ttbl_EDIVAddon2.Company THEN
    PUT STREAM s-audit-rpt UNFORMATTED "*" AT 1.
    PUT STREAM s-audit-rpt UNFORMATTED "?: " AT 3 ttbl_EDIVAddon2.Company.
  END.
END.
DO i = 1 TO 2:
IF NOT v_field_changed OR ttbl_EDIVAddon1.Description[i] NE ttbl_EDIVAddon2.Description[i] OR action NE "UPDATE" THEN
DO:
  PUT STREAM s-audit-rpt UNFORMATTED "?[" AT 3 i "]: " ttbl_EDIVAddon1.Description[i] " (Description[" i "])".
  IF action = "UPDATE" THEN
  DO:
    IF ttbl_EDIVAddon1.Description[i] NE ttbl_EDIVAddon2.Description[i] THEN
    PUT STREAM s-audit-rpt UNFORMATTED "*" AT 1.
    PUT STREAM s-audit-rpt UNFORMATTED "?[" AT 3 i "]: " ttbl_EDIVAddon2.Description[i].
  END.
END.
END.
IF NOT v_field_changed OR ttbl_EDIVAddon1.Hand-meth NE ttbl_EDIVAddon2.Hand-meth OR action NE "UPDATE" THEN
DO:
  PUT STREAM s-audit-rpt UNFORMATTED "Handling: " AT 3 ttbl_EDIVAddon1.Hand-meth " (Hand-meth)".
  IF action = "UPDATE" THEN
  DO:
    IF ttbl_EDIVAddon1.Hand-meth NE ttbl_EDIVAddon2.Hand-meth THEN
    PUT STREAM s-audit-rpt UNFORMATTED "*" AT 1.
    PUT STREAM s-audit-rpt UNFORMATTED "Handling: " AT 3 ttbl_EDIVAddon2.Hand-meth.
  END.
END.
IF NOT v_field_changed OR ttbl_EDIVAddon1.Invoice-no NE ttbl_EDIVAddon2.Invoice-no OR action NE "UPDATE" THEN
DO:
  PUT STREAM s-audit-rpt UNFORMATTED "?: " AT 3 ttbl_EDIVAddon1.Invoice-no " (Invoice-no)".
  IF action = "UPDATE" THEN
  DO:
    IF ttbl_EDIVAddon1.Invoice-no NE ttbl_EDIVAddon2.Invoice-no THEN
    PUT STREAM s-audit-rpt UNFORMATTED "*" AT 1.
    PUT STREAM s-audit-rpt UNFORMATTED "?: " AT 3 ttbl_EDIVAddon2.Invoice-no.
  END.
END.
IF NOT v_field_changed OR ttbl_EDIVAddon1.Line NE ttbl_EDIVAddon2.Line OR action NE "UPDATE" THEN
DO:
  PUT STREAM s-audit-rpt UNFORMATTED "?: " AT 3 ttbl_EDIVAddon1.Line FORMAT ">>9" " (Line)".
  IF action = "UPDATE" THEN
  DO:
    IF ttbl_EDIVAddon1.Line NE ttbl_EDIVAddon2.Line THEN
    PUT STREAM s-audit-rpt UNFORMATTED "*" AT 1.
    PUT STREAM s-audit-rpt UNFORMATTED "?: " AT 3 ttbl_EDIVAddon2.Line FORMAT ">>9".
  END.
END.
DO i = 1 TO 9:
IF NOT v_field_changed OR ttbl_EDIVAddon1.Note[i] NE ttbl_EDIVAddon2.Note[i] OR action NE "UPDATE" THEN
DO:
  PUT STREAM s-audit-rpt UNFORMATTED "?[" AT 3 i "]: " ttbl_EDIVAddon1.Note[i] " (Note[" i "])".
  IF action = "UPDATE" THEN
  DO:
    IF ttbl_EDIVAddon1.Note[i] NE ttbl_EDIVAddon2.Note[i] THEN
    PUT STREAM s-audit-rpt UNFORMATTED "*" AT 1.
    PUT STREAM s-audit-rpt UNFORMATTED "?[" AT 3 i "]: " ttbl_EDIVAddon2.Note[i].
  END.
END.
END.
IF NOT v_field_changed OR ttbl_EDIVAddon1.Option-code NE ttbl_EDIVAddon2.Option-code OR action NE "UPDATE" THEN
DO:
  PUT STREAM s-audit-rpt UNFORMATTED "?: " AT 3 ttbl_EDIVAddon1.Option-code " (Option-code)".
  IF action = "UPDATE" THEN
  DO:
    IF ttbl_EDIVAddon1.Option-code NE ttbl_EDIVAddon2.Option-code THEN
    PUT STREAM s-audit-rpt UNFORMATTED "*" AT 1.
    PUT STREAM s-audit-rpt UNFORMATTED "?: " AT 3 ttbl_EDIVAddon2.Option-code.
  END.
END.
IF NOT v_field_changed OR ttbl_EDIVAddon1.Partner NE ttbl_EDIVAddon2.Partner OR action NE "UPDATE" THEN
DO:
  PUT STREAM s-audit-rpt UNFORMATTED "?: " AT 3 ttbl_EDIVAddon1.Partner " (Partner)".
  IF action = "UPDATE" THEN
  DO:
    IF ttbl_EDIVAddon1.Partner NE ttbl_EDIVAddon2.Partner THEN
    PUT STREAM s-audit-rpt UNFORMATTED "*" AT 1.
    PUT STREAM s-audit-rpt UNFORMATTED "?: " AT 3 ttbl_EDIVAddon2.Partner.
  END.
END.
IF NOT v_field_changed OR ttbl_EDIVAddon1.Percent NE ttbl_EDIVAddon2.Percent OR action NE "UPDATE" THEN
DO:
  PUT STREAM s-audit-rpt UNFORMATTED "?: " AT 3 ttbl_EDIVAddon1.Percent FORMAT "->>>>>.999" " (Percent)".
  IF action = "UPDATE" THEN
  DO:
    IF ttbl_EDIVAddon1.Percent NE ttbl_EDIVAddon2.Percent THEN
    PUT STREAM s-audit-rpt UNFORMATTED "*" AT 1.
    PUT STREAM s-audit-rpt UNFORMATTED "?: " AT 3 ttbl_EDIVAddon2.Percent FORMAT "->>>>>.999".
  END.
END.
IF NOT v_field_changed OR ttbl_EDIVAddon1.Qty NE ttbl_EDIVAddon2.Qty OR action NE "UPDATE" THEN
DO:
  PUT STREAM s-audit-rpt UNFORMATTED "?: " AT 3 ttbl_EDIVAddon1.Qty FORMAT "->,>>>,>>>.99" " (Qty)".
  IF action = "UPDATE" THEN
  DO:
    IF ttbl_EDIVAddon1.Qty NE ttbl_EDIVAddon2.Qty THEN
    PUT STREAM s-audit-rpt UNFORMATTED "*" AT 1.
    PUT STREAM s-audit-rpt UNFORMATTED "?: " AT 3 ttbl_EDIVAddon2.Qty FORMAT "->,>>>,>>>.99".
  END.
END.
IF NOT v_field_changed OR ttbl_EDIVAddon1.Rate NE ttbl_EDIVAddon2.Rate OR action NE "UPDATE" THEN
DO:
  PUT STREAM s-audit-rpt UNFORMATTED "?: " AT 3 ttbl_EDIVAddon1.Rate FORMAT "->>>>>.99<<" " (Rate)".
  IF action = "UPDATE" THEN
  DO:
    IF ttbl_EDIVAddon1.Rate NE ttbl_EDIVAddon2.Rate THEN
    PUT STREAM s-audit-rpt UNFORMATTED "*" AT 1.
    PUT STREAM s-audit-rpt UNFORMATTED "?: " AT 3 ttbl_EDIVAddon2.Rate FORMAT "->>>>>.99<<".
  END.
END.
IF NOT v_field_changed OR ttbl_EDIVAddon1.Ref-Num NE ttbl_EDIVAddon2.Ref-Num OR action NE "UPDATE" THEN
DO:
  PUT STREAM s-audit-rpt UNFORMATTED "?: " AT 3 ttbl_EDIVAddon1.Ref-Num " (Ref-Num)".
  IF action = "UPDATE" THEN
  DO:
    IF ttbl_EDIVAddon1.Ref-Num NE ttbl_EDIVAddon2.Ref-Num THEN
    PUT STREAM s-audit-rpt UNFORMATTED "*" AT 1.
    PUT STREAM s-audit-rpt UNFORMATTED "?: " AT 3 ttbl_EDIVAddon2.Ref-Num.
  END.
END.
IF NOT v_field_changed OR ttbl_EDIVAddon1.Seq NE ttbl_EDIVAddon2.Seq OR action NE "UPDATE" THEN
DO:
  PUT STREAM s-audit-rpt UNFORMATTED "?: " AT 3 ttbl_EDIVAddon1.Seq FORMAT ">>>>>>9" " (Seq)".
  IF action = "UPDATE" THEN
  DO:
    IF ttbl_EDIVAddon1.Seq NE ttbl_EDIVAddon2.Seq THEN
    PUT STREAM s-audit-rpt UNFORMATTED "*" AT 1.
    PUT STREAM s-audit-rpt UNFORMATTED "?: " AT 3 ttbl_EDIVAddon2.Seq FORMAT ">>>>>>9".
  END.
END.
IF NOT v_field_changed OR ttbl_EDIVAddon1.Special-svc-code NE ttbl_EDIVAddon2.Special-svc-code OR action NE "UPDATE" THEN
DO:
  PUT STREAM s-audit-rpt UNFORMATTED "?: " AT 3 ttbl_EDIVAddon1.Special-svc-code " (Special-svc-code)".
  IF action = "UPDATE" THEN
  DO:
    IF ttbl_EDIVAddon1.Special-svc-code NE ttbl_EDIVAddon2.Special-svc-code THEN
    PUT STREAM s-audit-rpt UNFORMATTED "*" AT 1.
    PUT STREAM s-audit-rpt UNFORMATTED "?: " AT 3 ttbl_EDIVAddon2.Special-svc-code.
  END.
END.
IF NOT v_field_changed OR ttbl_EDIVAddon1.Uom-code NE ttbl_EDIVAddon2.Uom-code OR action NE "UPDATE" THEN
DO:
  PUT STREAM s-audit-rpt UNFORMATTED "?: " AT 3 ttbl_EDIVAddon1.Uom-code " (Uom-code)".
  IF action = "UPDATE" THEN
  DO:
    IF ttbl_EDIVAddon1.Uom-code NE ttbl_EDIVAddon2.Uom-code THEN
    PUT STREAM s-audit-rpt UNFORMATTED "*" AT 1.
    PUT STREAM s-audit-rpt UNFORMATTED "?: " AT 3 ttbl_EDIVAddon2.Uom-code.
  END.
END.
IF v_show_index THEN
DO:
  PUT STREAM s-audit-rpt UNFORMATTED "Index Fields - Label: Value (database field-name)" AT 5.
  PUT STREAM s-audit-rpt UNFORMATTED "?: " AT 7.
  IF action = "UPDATE" THEN
  PUT STREAM s-audit-rpt UNFORMATTED ttbl_EDIVAddon2.Partner " (Partner)".
  ELSE
  PUT STREAM s-audit-rpt UNFORMATTED ttbl_EDIVAddon1.Partner " (Partner)".
  PUT STREAM s-audit-rpt UNFORMATTED "?: " AT 7.
  IF action = "UPDATE" THEN
  PUT STREAM s-audit-rpt UNFORMATTED ttbl_EDIVAddon2.Seq FORMAT ">>>>>>9" " (Seq)".
  ELSE
  PUT STREAM s-audit-rpt UNFORMATTED ttbl_EDIVAddon1.Seq FORMAT ">>>>>>9" " (Seq)".
  PUT STREAM s-audit-rpt UNFORMATTED "?: " AT 7.
  IF action = "UPDATE" THEN
  PUT STREAM s-audit-rpt UNFORMATTED ttbl_EDIVAddon2.Addon-line FORMAT ">>9" " (Addon-line)".
  ELSE
  PUT STREAM s-audit-rpt UNFORMATTED ttbl_EDIVAddon1.Addon-line FORMAT ">>9" " (Addon-line)".
END.
PUT STREAM s-audit-rpt UNFORMATTED SKIP(1).
