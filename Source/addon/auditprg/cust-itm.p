/* cust-itm.p - generated 12/02/2005 - 16:52:40 by NoSweat */

/* this program was generated by auditgn1.p and should not be modified */
/* modifications should be made to the auto generation program         */

DEFINE INPUT PARAMETER action AS CHARACTER NO-UNDO.

DEFINE VARIABLE i AS INTEGER NO-UNDO.

{methods/defines/audit.i}
FIND FIRST ttbl_header.

DEFINE WORK-TABLE ttbl_cust-itm1 NO-UNDO LIKE cust-itm.
DEFINE WORK-TABLE ttbl_cust-itm2 NO-UNDO LIKE cust-itm.

CREATE ttbl_cust-itm1.
IMPORT STREAM s_audit_file ttbl_cust-itm1.
CREATE ttbl_cust-itm2.
IF action = "UPDATE" THEN
IMPORT STREAM s_audit_file ttbl_cust-itm2.

PUT STREAM s-audit-rpt UNFORMATTED
  "Action    User ID/(User Name)            DB.Table/(Database)  Date       Time" SKIP
  "--------- ------------------------------ -------------------- ---------- --------" SKIP
  ttbl_header.v_action AT 1
  ttbl_header.v_userid AT 11
  ttbl_header.v_file AT 42
  ttbl_header.v_date FORMAT "99/99/9999" AT 63
  STRING(ttbl_header.v_time,"HH:MM:SS") AT 74.
FIND users WHERE users.user_id = v_userid NO-LOCK NO-ERROR.
IF AVAILABLE users THEN
PUT STREAM s-audit-rpt UNFORMATTED "(" AT 10 users.user_name ")".
ELSE
PUT STREAM s-audit-rpt UNFORMATTED "(user record does not exist)" AT 10.
PUT STREAM s-audit-rpt UNFORMATTED
  "(" AT 41 ttbl_header.v_db ")" SKIP(1)
  "Label: Value (database field-name)" AT 3.
IF action = "UPDATE" THEN
PUT STREAM s-audit-rpt UNFORMATTED " ( * - denotes field value change )".
PUT STREAM s-audit-rpt UNFORMATTED FILL("-",75) AT 3.
IF NOT v_field_changed OR ttbl_cust-itm1.company NE ttbl_cust-itm2.company OR action NE "UPDATE" THEN
DO:
  PUT STREAM s-audit-rpt UNFORMATTED "Company: " AT 3 ttbl_cust-itm1.company " (company)".
  IF action = "UPDATE" THEN
  DO:
    IF ttbl_cust-itm1.company NE ttbl_cust-itm2.company THEN
    PUT STREAM s-audit-rpt UNFORMATTED "*" AT 1.
    PUT STREAM s-audit-rpt UNFORMATTED "Company: " AT 3 ttbl_cust-itm2.company.
  END.
END.
IF NOT v_field_changed OR ttbl_cust-itm1.consum NE ttbl_cust-itm2.consum OR action NE "UPDATE" THEN
DO:
  PUT STREAM s-audit-rpt UNFORMATTED "Annual Consumption: " AT 3 ttbl_cust-itm1.consum FORMAT "->>>,>>>,>>9" " (consum)".
  IF action = "UPDATE" THEN
  DO:
    IF ttbl_cust-itm1.consum NE ttbl_cust-itm2.consum THEN
    PUT STREAM s-audit-rpt UNFORMATTED "*" AT 1.
    PUT STREAM s-audit-rpt UNFORMATTED "Annual Consumption: " AT 3 ttbl_cust-itm2.consum FORMAT "->>>,>>>,>>9".
  END.
END.
IF NOT v_field_changed OR ttbl_cust-itm1.cust-no NE ttbl_cust-itm2.cust-no OR action NE "UPDATE" THEN
DO:
  PUT STREAM s-audit-rpt UNFORMATTED "Customer: " AT 3 ttbl_cust-itm1.cust-no " (cust-no)".
  IF action = "UPDATE" THEN
  DO:
    IF ttbl_cust-itm1.cust-no NE ttbl_cust-itm2.cust-no THEN
    PUT STREAM s-audit-rpt UNFORMATTED "*" AT 1.
    PUT STREAM s-audit-rpt UNFORMATTED "Customer: " AT 3 ttbl_cust-itm2.cust-no.
  END.
END.
IF NOT v_field_changed OR ttbl_cust-itm1.i-no NE ttbl_cust-itm2.i-no OR action NE "UPDATE" THEN
DO:
  PUT STREAM s-audit-rpt UNFORMATTED "FG Item #: " AT 3 ttbl_cust-itm1.i-no " (i-no)".
  IF action = "UPDATE" THEN
  DO:
    IF ttbl_cust-itm1.i-no NE ttbl_cust-itm2.i-no THEN
    PUT STREAM s-audit-rpt UNFORMATTED "*" AT 1.
    PUT STREAM s-audit-rpt UNFORMATTED "FG Item #: " AT 3 ttbl_cust-itm2.i-no.
  END.
END.
IF NOT v_field_changed OR ttbl_cust-itm1.loc NE ttbl_cust-itm2.loc OR action NE "UPDATE" THEN
DO:
  PUT STREAM s-audit-rpt UNFORMATTED "Warehouse: " AT 3 ttbl_cust-itm1.loc " (loc)".
  IF action = "UPDATE" THEN
  DO:
    IF ttbl_cust-itm1.loc NE ttbl_cust-itm2.loc THEN
    PUT STREAM s-audit-rpt UNFORMATTED "*" AT 1.
    PUT STREAM s-audit-rpt UNFORMATTED "Warehouse: " AT 3 ttbl_cust-itm2.loc.
  END.
END.
IF NOT v_field_changed OR ttbl_cust-itm1.qty NE ttbl_cust-itm2.qty OR action NE "UPDATE" THEN
DO:
  PUT STREAM s-audit-rpt UNFORMATTED "On Hand Qty: " AT 3 ttbl_cust-itm1.qty FORMAT "->,>>>,>>9" " (qty)".
  IF action = "UPDATE" THEN
  DO:
    IF ttbl_cust-itm1.qty NE ttbl_cust-itm2.qty THEN
    PUT STREAM s-audit-rpt UNFORMATTED "*" AT 1.
    PUT STREAM s-audit-rpt UNFORMATTED "On Hand Qty: " AT 3 ttbl_cust-itm2.qty FORMAT "->,>>>,>>9".
  END.
END.
IF v_show_index THEN
DO:
  PUT STREAM s-audit-rpt UNFORMATTED "Index Fields - Label: Value (database field-name)" AT 5.
  PUT STREAM s-audit-rpt UNFORMATTED "Company: " AT 7.
  IF action = "UPDATE" THEN
  PUT STREAM s-audit-rpt UNFORMATTED ttbl_cust-itm2.company " (company)".
  ELSE
  PUT STREAM s-audit-rpt UNFORMATTED ttbl_cust-itm1.company " (company)".
  PUT STREAM s-audit-rpt UNFORMATTED "Customer: " AT 7.
  IF action = "UPDATE" THEN
  PUT STREAM s-audit-rpt UNFORMATTED ttbl_cust-itm2.cust-no " (cust-no)".
  ELSE
  PUT STREAM s-audit-rpt UNFORMATTED ttbl_cust-itm1.cust-no " (cust-no)".
  PUT STREAM s-audit-rpt UNFORMATTED "Warehouse: " AT 7.
  IF action = "UPDATE" THEN
  PUT STREAM s-audit-rpt UNFORMATTED ttbl_cust-itm2.loc " (loc)".
  ELSE
  PUT STREAM s-audit-rpt UNFORMATTED ttbl_cust-itm1.loc " (loc)".
  PUT STREAM s-audit-rpt UNFORMATTED "FG Item #: " AT 7.
  IF action = "UPDATE" THEN
  PUT STREAM s-audit-rpt UNFORMATTED ttbl_cust-itm2.i-no " (i-no)".
  ELSE
  PUT STREAM s-audit-rpt UNFORMATTED ttbl_cust-itm1.i-no " (i-no)".
END.
PUT STREAM s-audit-rpt UNFORMATTED SKIP(1).
