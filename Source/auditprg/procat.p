/* procat.p - generated 12/09/2005 - 15:42:52 by NoSweat */

/* this program was generated by auditgn1.p and should not be modified */
/* modifications should be made to the auto generation program         */

DEFINE INPUT PARAMETER action AS CHARACTER NO-UNDO.

DEFINE VARIABLE i AS INTEGER NO-UNDO.

{methods/defines/audit.i}
FIND FIRST ttbl_header.

DEFINE WORK-TABLE ttbl_procat1 NO-UNDO LIKE procat.
DEFINE WORK-TABLE ttbl_procat2 NO-UNDO LIKE procat.

CREATE ttbl_procat1.
IMPORT STREAM s_audit_file ttbl_procat1.
CREATE ttbl_procat2.
IF action = "UPDATE" THEN
IMPORT STREAM s_audit_file ttbl_procat2.

PUT STREAM s-audit-rpt UNFORMATTED
  "Action    User ID/(User Name)            DB.Table/(Database)  Date       Time" SKIP
  "--------- ------------------------------ -------------------- ---------- --------" SKIP
  ttbl_header.v_action AT 1
  ttbl_header.v_userid AT 11
  ttbl_header.v_file AT 42
  ttbl_header.v_date FORMAT "99/99/9999" AT 63
  STRING(ttbl_header.v_time,"HH:MM:SS") AT 74.
FIND users WHERE users.user_id = v_userid NO-LOCK NO-ERROR.
IF AVAILABLE users THEN
PUT STREAM s-audit-rpt UNFORMATTED "(" AT 10 users.user_name ")".
ELSE
PUT STREAM s-audit-rpt UNFORMATTED "(user record does not exist)" AT 10.
PUT STREAM s-audit-rpt UNFORMATTED
  "(" AT 41 ttbl_header.v_db ")" SKIP(1)
  "Label: Value (database field-name)" AT 3.
IF action = "UPDATE" THEN
PUT STREAM s-audit-rpt UNFORMATTED " ( * - denotes field value change )".
PUT STREAM s-audit-rpt UNFORMATTED FILL("-",75) AT 3.
IF NOT v_field_changed OR ttbl_procat1.commbasis NE ttbl_procat2.commbasis OR action NE "UPDATE" THEN
DO:
  PUT STREAM s-audit-rpt UNFORMATTED "Basis: " AT 3 ttbl_procat1.commbasis " (commbasis)".
  IF action = "UPDATE" THEN
  DO:
    IF ttbl_procat1.commbasis NE ttbl_procat2.commbasis THEN
    PUT STREAM s-audit-rpt UNFORMATTED "*" AT 1.
    PUT STREAM s-audit-rpt UNFORMATTED "Basis: " AT 3 ttbl_procat2.commbasis.
  END.
END.
IF NOT v_field_changed OR ttbl_procat1.commrate NE ttbl_procat2.commrate OR action NE "UPDATE" THEN
DO:
  PUT STREAM s-audit-rpt UNFORMATTED "Commission: " AT 3 ttbl_procat1.commrate FORMAT ">>9.99%" " (commrate)".
  IF action = "UPDATE" THEN
  DO:
    IF ttbl_procat1.commrate NE ttbl_procat2.commrate THEN
    PUT STREAM s-audit-rpt UNFORMATTED "*" AT 1.
    PUT STREAM s-audit-rpt UNFORMATTED "Commission: " AT 3 ttbl_procat2.commrate FORMAT ">>9.99%".
  END.
END.
IF NOT v_field_changed OR ttbl_procat1.company NE ttbl_procat2.company OR action NE "UPDATE" THEN
DO:
  PUT STREAM s-audit-rpt UNFORMATTED "Company: " AT 3 ttbl_procat1.company " (company)".
  IF action = "UPDATE" THEN
  DO:
    IF ttbl_procat1.company NE ttbl_procat2.company THEN
    PUT STREAM s-audit-rpt UNFORMATTED "*" AT 1.
    PUT STREAM s-audit-rpt UNFORMATTED "Company: " AT 3 ttbl_procat2.company.
  END.
END.
IF NOT v_field_changed OR ttbl_procat1.def-loc NE ttbl_procat2.def-loc OR action NE "UPDATE" THEN
DO:
  PUT STREAM s-audit-rpt UNFORMATTED "Warehouse: " AT 3 ttbl_procat1.def-loc " (def-loc)".
  IF action = "UPDATE" THEN
  DO:
    IF ttbl_procat1.def-loc NE ttbl_procat2.def-loc THEN
    PUT STREAM s-audit-rpt UNFORMATTED "*" AT 1.
    PUT STREAM s-audit-rpt UNFORMATTED "Warehouse: " AT 3 ttbl_procat2.def-loc.
  END.
END.
IF NOT v_field_changed OR ttbl_procat1.def-loc-bin NE ttbl_procat2.def-loc-bin OR action NE "UPDATE" THEN
DO:
  PUT STREAM s-audit-rpt UNFORMATTED "Bin: " AT 3 ttbl_procat1.def-loc-bin " (def-loc-bin)".
  IF action = "UPDATE" THEN
  DO:
    IF ttbl_procat1.def-loc-bin NE ttbl_procat2.def-loc-bin THEN
    PUT STREAM s-audit-rpt UNFORMATTED "*" AT 1.
    PUT STREAM s-audit-rpt UNFORMATTED "Bin: " AT 3 ttbl_procat2.def-loc-bin.
  END.
END.
IF NOT v_field_changed OR ttbl_procat1.dscr NE ttbl_procat2.dscr OR action NE "UPDATE" THEN
DO:
  PUT STREAM s-audit-rpt UNFORMATTED "Description: " AT 3 ttbl_procat1.dscr " (dscr)".
  IF action = "UPDATE" THEN
  DO:
    IF ttbl_procat1.dscr NE ttbl_procat2.dscr THEN
    PUT STREAM s-audit-rpt UNFORMATTED "*" AT 1.
    PUT STREAM s-audit-rpt UNFORMATTED "Description: " AT 3 ttbl_procat2.dscr.
  END.
END.
IF NOT v_field_changed OR ttbl_procat1.glacc NE ttbl_procat2.glacc OR action NE "UPDATE" THEN
DO:
  PUT STREAM s-audit-rpt UNFORMATTED "GL Account: " AT 3 ttbl_procat1.glacc " (glacc)".
  IF action = "UPDATE" THEN
  DO:
    IF ttbl_procat1.glacc NE ttbl_procat2.glacc THEN
    PUT STREAM s-audit-rpt UNFORMATTED "*" AT 1.
    PUT STREAM s-audit-rpt UNFORMATTED "GL Account: " AT 3 ttbl_procat2.glacc.
  END.
END.
IF NOT v_field_changed OR ttbl_procat1.LYR NE ttbl_procat2.LYR OR action NE "UPDATE" THEN
DO:
  PUT STREAM s-audit-rpt UNFORMATTED "Last Year: " AT 3 ttbl_procat1.LYR FORMAT ">>>,>>>,>>9.99" " (LYR)".
  IF action = "UPDATE" THEN
  DO:
    IF ttbl_procat1.LYR NE ttbl_procat2.LYR THEN
    PUT STREAM s-audit-rpt UNFORMATTED "*" AT 1.
    PUT STREAM s-audit-rpt UNFORMATTED "Last Year: " AT 3 ttbl_procat2.LYR FORMAT ">>>,>>>,>>9.99".
  END.
END.
IF NOT v_field_changed OR ttbl_procat1.procat NE ttbl_procat2.procat OR action NE "UPDATE" THEN
DO:
  PUT STREAM s-audit-rpt UNFORMATTED "Category: " AT 3 ttbl_procat1.procat " (procat)".
  IF action = "UPDATE" THEN
  DO:
    IF ttbl_procat1.procat NE ttbl_procat2.procat THEN
    PUT STREAM s-audit-rpt UNFORMATTED "*" AT 1.
    PUT STREAM s-audit-rpt UNFORMATTED "Category: " AT 3 ttbl_procat2.procat.
  END.
END.
IF NOT v_field_changed OR ttbl_procat1.prolin NE ttbl_procat2.prolin OR action NE "UPDATE" THEN
DO:
  PUT STREAM s-audit-rpt UNFORMATTED "Product Line: " AT 3 ttbl_procat1.prolin " (prolin)".
  IF action = "UPDATE" THEN
  DO:
    IF ttbl_procat1.prolin NE ttbl_procat2.prolin THEN
    PUT STREAM s-audit-rpt UNFORMATTED "*" AT 1.
    PUT STREAM s-audit-rpt UNFORMATTED "Product Line: " AT 3 ttbl_procat2.prolin.
  END.
END.
IF NOT v_field_changed OR ttbl_procat1.PTD NE ttbl_procat2.PTD OR action NE "UPDATE" THEN
DO:
  PUT STREAM s-audit-rpt UNFORMATTED "PTD: " AT 3 ttbl_procat1.PTD FORMAT ">>>,>>>,>>9.99" " (PTD)".
  IF action = "UPDATE" THEN
  DO:
    IF ttbl_procat1.PTD NE ttbl_procat2.PTD THEN
    PUT STREAM s-audit-rpt UNFORMATTED "*" AT 1.
    PUT STREAM s-audit-rpt UNFORMATTED "PTD: " AT 3 ttbl_procat2.PTD FORMAT ">>>,>>>,>>9.99".
  END.
END.
IF NOT v_field_changed OR ttbl_procat1.YTD NE ttbl_procat2.YTD OR action NE "UPDATE" THEN
DO:
  PUT STREAM s-audit-rpt UNFORMATTED "Year To Date: " AT 3 ttbl_procat1.YTD FORMAT ">>>,>>>,>>9.99" " (YTD)".
  IF action = "UPDATE" THEN
  DO:
    IF ttbl_procat1.YTD NE ttbl_procat2.YTD THEN
    PUT STREAM s-audit-rpt UNFORMATTED "*" AT 1.
    PUT STREAM s-audit-rpt UNFORMATTED "Year To Date: " AT 3 ttbl_procat2.YTD FORMAT ">>>,>>>,>>9.99".
  END.
END.
IF v_show_index THEN
DO:
  PUT STREAM s-audit-rpt UNFORMATTED "Index Fields - Label: Value (database field-name)" AT 5.
  PUT STREAM s-audit-rpt UNFORMATTED "Company: " AT 7.
  IF action = "UPDATE" THEN
  PUT STREAM s-audit-rpt UNFORMATTED ttbl_procat2.company " (company)".
  ELSE
  PUT STREAM s-audit-rpt UNFORMATTED ttbl_procat1.company " (company)".
  PUT STREAM s-audit-rpt UNFORMATTED "Category: " AT 7.
  IF action = "UPDATE" THEN
  PUT STREAM s-audit-rpt UNFORMATTED ttbl_procat2.procat " (procat)".
  ELSE
  PUT STREAM s-audit-rpt UNFORMATTED ttbl_procat1.procat " (procat)".
END.
PUT STREAM s-audit-rpt UNFORMATTED SKIP(1).
