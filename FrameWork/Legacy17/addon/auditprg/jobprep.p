/* jobprep.p - generated 12/02/2005 - 16:52:40 by NoSweat */

/* this program was generated by auditgn1.p and should not be modified */
/* modifications should be made to the auto generation program         */

DEFINE INPUT PARAMETER action AS CHARACTER NO-UNDO.

DEFINE VARIABLE i AS INTEGER NO-UNDO.

{methods/defines/audit.i}
FIND FIRST ttbl_header.

DEFINE WORK-TABLE ttbl_jobprep1 NO-UNDO LIKE jobprep.
DEFINE WORK-TABLE ttbl_jobprep2 NO-UNDO LIKE jobprep.

CREATE ttbl_jobprep1.
IMPORT STREAM s_audit_file ttbl_jobprep1.
CREATE ttbl_jobprep2.
IF action = "UPDATE" THEN
IMPORT STREAM s_audit_file ttbl_jobprep2.

PUT STREAM s-audit-rpt UNFORMATTED
  "Action    User ID/(User Name)            DB.Table/(Database)  Date       Time" SKIP
  "--------- ------------------------------ -------------------- ---------- --------" SKIP
  ttbl_header.v_action AT 1
  ttbl_header.v_userid AT 11
  ttbl_header.v_file AT 42
  ttbl_header.v_date FORMAT "99/99/9999" AT 63
  STRING(ttbl_header.v_time,"HH:MM:SS") AT 74.
FIND users WHERE users.user_id = v_userid NO-LOCK NO-ERROR.
IF AVAILABLE users THEN
PUT STREAM s-audit-rpt UNFORMATTED "(" AT 10 users.user_name ")".
ELSE
PUT STREAM s-audit-rpt UNFORMATTED "(user record does not exist)" AT 10.
PUT STREAM s-audit-rpt UNFORMATTED
  "(" AT 41 ttbl_header.v_db ")" SKIP(1)
  "Label: Value (database field-name)" AT 3.
IF action = "UPDATE" THEN
PUT STREAM s-audit-rpt UNFORMATTED " ( * - denotes field value change )".
PUT STREAM s-audit-rpt UNFORMATTED FILL("-",75) AT 3.
IF NOT v_field_changed OR ttbl_jobprep1.blank_number NE ttbl_jobprep2.blank_number OR action NE "UPDATE" THEN
DO:
  PUT STREAM s-audit-rpt UNFORMATTED "Blank Number: " AT 3 ttbl_jobprep1.blank_number FORMAT ">9" " (blank_number)".
  IF action = "UPDATE" THEN
  DO:
    IF ttbl_jobprep1.blank_number NE ttbl_jobprep2.blank_number THEN
    PUT STREAM s-audit-rpt UNFORMATTED "*" AT 1.
    PUT STREAM s-audit-rpt UNFORMATTED "Blank Number: " AT 3 ttbl_jobprep2.blank_number FORMAT ">9".
  END.
END.
IF NOT v_field_changed OR ttbl_jobprep1.code NE ttbl_jobprep2.code OR action NE "UPDATE" THEN
DO:
  PUT STREAM s-audit-rpt UNFORMATTED "Code: " AT 3 ttbl_jobprep1.code " (code)".
  IF action = "UPDATE" THEN
  DO:
    IF ttbl_jobprep1.code NE ttbl_jobprep2.code THEN
    PUT STREAM s-audit-rpt UNFORMATTED "*" AT 1.
    PUT STREAM s-audit-rpt UNFORMATTED "Code: " AT 3 ttbl_jobprep2.code.
  END.
END.
IF NOT v_field_changed OR ttbl_jobprep1.cost-m NE ttbl_jobprep2.cost-m OR action NE "UPDATE" THEN
DO:
  PUT STREAM s-audit-rpt UNFORMATTED "Cost/M: " AT 3 ttbl_jobprep1.cost-m FORMAT "->,>>9.9999" " (cost-m)".
  IF action = "UPDATE" THEN
  DO:
    IF ttbl_jobprep1.cost-m NE ttbl_jobprep2.cost-m THEN
    PUT STREAM s-audit-rpt UNFORMATTED "*" AT 1.
    PUT STREAM s-audit-rpt UNFORMATTED "Cost/M: " AT 3 ttbl_jobprep2.cost-m FORMAT "->,>>9.9999".
  END.
END.
IF NOT v_field_changed OR ttbl_jobprep1.form_number NE ttbl_jobprep2.form_number OR action NE "UPDATE" THEN
DO:
  PUT STREAM s-audit-rpt UNFORMATTED "Form Number: " AT 3 ttbl_jobprep1.form_number FORMAT ">>9" " (form_number)".
  IF action = "UPDATE" THEN
  DO:
    IF ttbl_jobprep1.form_number NE ttbl_jobprep2.form_number THEN
    PUT STREAM s-audit-rpt UNFORMATTED "*" AT 1.
    PUT STREAM s-audit-rpt UNFORMATTED "Form Number: " AT 3 ttbl_jobprep2.form_number FORMAT ">>9".
  END.
END.
IF NOT v_field_changed OR ttbl_jobprep1.job NE ttbl_jobprep2.job OR action NE "UPDATE" THEN
DO:
  PUT STREAM s-audit-rpt UNFORMATTED "Job Number: " AT 3 ttbl_jobprep1.job " (job)".
  IF action = "UPDATE" THEN
  DO:
    IF ttbl_jobprep1.job NE ttbl_jobprep2.job THEN
    PUT STREAM s-audit-rpt UNFORMATTED "*" AT 1.
    PUT STREAM s-audit-rpt UNFORMATTED "Job Number: " AT 3 ttbl_jobprep2.job.
  END.
END.
IF NOT v_field_changed OR ttbl_jobprep1.ml NE ttbl_jobprep2.ml OR action NE "UPDATE" THEN
DO:
  PUT STREAM s-audit-rpt UNFORMATTED "M/L: " AT 3 ttbl_jobprep1.ml FORMAT "M/L" " (ml)".
  IF action = "UPDATE" THEN
  DO:
    IF ttbl_jobprep1.ml NE ttbl_jobprep2.ml THEN
    PUT STREAM s-audit-rpt UNFORMATTED "*" AT 1.
    PUT STREAM s-audit-rpt UNFORMATTED "M/L: " AT 3 ttbl_jobprep2.ml FORMAT "M/L".
  END.
END.
IF NOT v_field_changed OR ttbl_jobprep1.qty NE ttbl_jobprep2.qty OR action NE "UPDATE" THEN
DO:
  PUT STREAM s-audit-rpt UNFORMATTED "Qty: " AT 3 ttbl_jobprep1.qty FORMAT ">>>>9.9<" " (qty)".
  IF action = "UPDATE" THEN
  DO:
    IF ttbl_jobprep1.qty NE ttbl_jobprep2.qty THEN
    PUT STREAM s-audit-rpt UNFORMATTED "*" AT 1.
    PUT STREAM s-audit-rpt UNFORMATTED "Qty: " AT 3 ttbl_jobprep2.qty FORMAT ">>>>9.9<".
  END.
END.
IF NOT v_field_changed OR ttbl_jobprep1.simon NE ttbl_jobprep2.simon OR action NE "UPDATE" THEN
DO:
  PUT STREAM s-audit-rpt UNFORMATTED "SIMON: " AT 3 ttbl_jobprep1.simon " (simon)".
  IF action = "UPDATE" THEN
  DO:
    IF ttbl_jobprep1.simon NE ttbl_jobprep2.simon THEN
    PUT STREAM s-audit-rpt UNFORMATTED "*" AT 1.
    PUT STREAM s-audit-rpt UNFORMATTED "SIMON: " AT 3 ttbl_jobprep2.simon.
  END.
END.
IF NOT v_field_changed OR ttbl_jobprep1.std-cost NE ttbl_jobprep2.std-cost OR action NE "UPDATE" THEN
DO:
  PUT STREAM s-audit-rpt UNFORMATTED "Costs: " AT 3 ttbl_jobprep1.std-cost FORMAT ">>>,>>9.99<<" " (std-cost)".
  IF action = "UPDATE" THEN
  DO:
    IF ttbl_jobprep1.std-cost NE ttbl_jobprep2.std-cost THEN
    PUT STREAM s-audit-rpt UNFORMATTED "*" AT 1.
    PUT STREAM s-audit-rpt UNFORMATTED "Costs: " AT 3 ttbl_jobprep2.std-cost FORMAT ">>>,>>9.99<<".
  END.
END.
IF v_show_index THEN
DO:
  PUT STREAM s-audit-rpt UNFORMATTED "Index Fields - Label: Value (database field-name)" AT 5.
  PUT STREAM s-audit-rpt UNFORMATTED "Job Number: " AT 7.
  IF action = "UPDATE" THEN
  PUT STREAM s-audit-rpt UNFORMATTED ttbl_jobprep2.job " (job)".
  ELSE
  PUT STREAM s-audit-rpt UNFORMATTED ttbl_jobprep1.job " (job)".
  PUT STREAM s-audit-rpt UNFORMATTED "Form Number: " AT 7.
  IF action = "UPDATE" THEN
  PUT STREAM s-audit-rpt UNFORMATTED ttbl_jobprep2.form_number FORMAT ">>9" " (form_number)".
  ELSE
  PUT STREAM s-audit-rpt UNFORMATTED ttbl_jobprep1.form_number FORMAT ">>9" " (form_number)".
  PUT STREAM s-audit-rpt UNFORMATTED "Blank Number: " AT 7.
  IF action = "UPDATE" THEN
  PUT STREAM s-audit-rpt UNFORMATTED ttbl_jobprep2.blank_number FORMAT ">9" " (blank_number)".
  ELSE
  PUT STREAM s-audit-rpt UNFORMATTED ttbl_jobprep1.blank_number FORMAT ">9" " (blank_number)".
  PUT STREAM s-audit-rpt UNFORMATTED "Code: " AT 7.
  IF action = "UPDATE" THEN
  PUT STREAM s-audit-rpt UNFORMATTED ttbl_jobprep2.code " (code)".
  ELSE
  PUT STREAM s-audit-rpt UNFORMATTED ttbl_jobprep1.code " (code)".
END.
PUT STREAM s-audit-rpt UNFORMATTED SKIP(1).
