/* company.p - generated 01/10/2001 - 12:59:59 by nosweat */

/* this program was generated by auditgn1.p and should not be modified */
/* modifications should be made to the auto generation program         */

DEFINE INPUT PARAMETER action AS CHARACTER NO-UNDO.

DEFINE VARIABLE i AS INTEGER NO-UNDO.

{methods/defines/audit.i}
FIND FIRST ttbl_header.

DEFINE WORK-TABLE ttbl_company1 NO-UNDO LIKE company.
DEFINE WORK-TABLE ttbl_company2 NO-UNDO LIKE company.

CREATE ttbl_company1.
IMPORT STREAM s_audit_file ttbl_company1.
CREATE ttbl_company2.
IF action = "UPDATE" THEN
IMPORT STREAM s_audit_file ttbl_company2.

PUT STREAM s-audit-rpt UNFORMATTED
  "Action    User ID/(User Name)            DB.Table/(Database)  Date       Time" SKIP
  "--------- ------------------------------ -------------------- ---------- --------" SKIP
  ttbl_header.v_action AT 1
  ttbl_header.v_userid AT 11
  ttbl_header.v_file AT 42
  ttbl_header.v_date FORMAT "99/99/9999" AT 63
  STRING(ttbl_header.v_time,"HH:MM:SS") AT 74.
FIND users WHERE users.user_id = v_userid NO-LOCK NO-ERROR.
IF AVAILABLE users THEN
PUT STREAM s-audit-rpt UNFORMATTED "(" AT 10 users.user_name ")".
ELSE
PUT STREAM s-audit-rpt UNFORMATTED "(user record does not exist)" AT 10.
PUT STREAM s-audit-rpt UNFORMATTED
  "(" AT 41 ttbl_header.v_db ")" SKIP(1)
  "Label: Value (database field-name)" AT 3.
IF action = "UPDATE" THEN
PUT STREAM s-audit-rpt UNFORMATTED " ( * - denotes field value change )".
PUT STREAM s-audit-rpt UNFORMATTED FILL("-",75) AT 3.
DO i = 1 TO 5:
IF NOT v_field_changed OR ttbl_company1.acc-dig[i] NE ttbl_company2.acc-dig[i] OR action NE "UPDATE" THEN
DO:
  PUT STREAM s-audit-rpt UNFORMATTED "Number of digits[" AT 3 i "]: " ttbl_company1.acc-dig[i] FORMAT "9" " (acc-dig[" i "])".
  IF action = "UPDATE" THEN
  DO:
    IF ttbl_company1.acc-dig[i] NE ttbl_company2.acc-dig[i] THEN
    PUT STREAM s-audit-rpt UNFORMATTED "*" AT 1.
    PUT STREAM s-audit-rpt UNFORMATTED "Number of digits[" AT 3 i "]: " ttbl_company2.acc-dig[i] FORMAT "9".
  END.
END.
END.
IF NOT v_field_changed OR ttbl_company1.acc-level NE ttbl_company2.acc-level OR action NE "UPDATE" THEN
DO:
  PUT STREAM s-audit-rpt UNFORMATTED "# of levels per G/L Account: " AT 3 ttbl_company1.acc-level FORMAT "9" " (acc-level)".
  IF action = "UPDATE" THEN
  DO:
    IF ttbl_company1.acc-level NE ttbl_company2.acc-level THEN
    PUT STREAM s-audit-rpt UNFORMATTED "*" AT 1.
    PUT STREAM s-audit-rpt UNFORMATTED "# of levels per G/L Account: " AT 3 ttbl_company2.acc-level FORMAT "9".
  END.
END.
DO i = 1 TO 2:
IF NOT v_field_changed OR ttbl_company1.addr[i] NE ttbl_company2.addr[i] OR action NE "UPDATE" THEN
DO:
  PUT STREAM s-audit-rpt UNFORMATTED "Address[" AT 3 i "]: " ttbl_company1.addr[i] " (addr[" i "])".
  IF action = "UPDATE" THEN
  DO:
    IF ttbl_company1.addr[i] NE ttbl_company2.addr[i] THEN
    PUT STREAM s-audit-rpt UNFORMATTED "*" AT 1.
    PUT STREAM s-audit-rpt UNFORMATTED "Address[" AT 3 i "]: " ttbl_company2.addr[i].
  END.
END.
END.
IF NOT v_field_changed OR ttbl_company1.city NE ttbl_company2.city OR action NE "UPDATE" THEN
DO:
  PUT STREAM s-audit-rpt UNFORMATTED "City: " AT 3 ttbl_company1.city " (city)".
  IF action = "UPDATE" THEN
  DO:
    IF ttbl_company1.city NE ttbl_company2.city THEN
    PUT STREAM s-audit-rpt UNFORMATTED "*" AT 1.
    PUT STREAM s-audit-rpt UNFORMATTED "City: " AT 3 ttbl_company2.city.
  END.
END.
IF NOT v_field_changed OR ttbl_company1.co-acc NE ttbl_company2.co-acc OR action NE "UPDATE" THEN
DO:
  PUT STREAM s-audit-rpt UNFORMATTED "Accounts Company: " AT 3 ttbl_company1.co-acc " (co-acc)".
  IF action = "UPDATE" THEN
  DO:
    IF ttbl_company1.co-acc NE ttbl_company2.co-acc THEN
    PUT STREAM s-audit-rpt UNFORMATTED "*" AT 1.
    PUT STREAM s-audit-rpt UNFORMATTED "Accounts Company: " AT 3 ttbl_company2.co-acc.
  END.
END.
IF NOT v_field_changed OR ttbl_company1.company NE ttbl_company2.company OR action NE "UPDATE" THEN
DO:
  PUT STREAM s-audit-rpt UNFORMATTED "Company: " AT 3 ttbl_company1.company " (company)".
  IF action = "UPDATE" THEN
  DO:
    IF ttbl_company1.company NE ttbl_company2.company THEN
    PUT STREAM s-audit-rpt UNFORMATTED "*" AT 1.
    PUT STREAM s-audit-rpt UNFORMATTED "Company: " AT 3 ttbl_company2.company.
  END.
END.
IF NOT v_field_changed OR ttbl_company1.fid NE ttbl_company2.fid OR action NE "UPDATE" THEN
DO:
  PUT STREAM s-audit-rpt UNFORMATTED "Federal ID: " AT 3 ttbl_company1.fid " (fid)".
  IF action = "UPDATE" THEN
  DO:
    IF ttbl_company1.fid NE ttbl_company2.fid THEN
    PUT STREAM s-audit-rpt UNFORMATTED "*" AT 1.
    PUT STREAM s-audit-rpt UNFORMATTED "Federal ID: " AT 3 ttbl_company2.fid.
  END.
END.
IF NOT v_field_changed OR ttbl_company1.name NE ttbl_company2.name OR action NE "UPDATE" THEN
DO:
  PUT STREAM s-audit-rpt UNFORMATTED "Company Name: " AT 3 ttbl_company1.name " (name)".
  IF action = "UPDATE" THEN
  DO:
    IF ttbl_company1.name NE ttbl_company2.name THEN
    PUT STREAM s-audit-rpt UNFORMATTED "*" AT 1.
    PUT STREAM s-audit-rpt UNFORMATTED "Company Name: " AT 3 ttbl_company2.name.
  END.
END.
IF NOT v_field_changed OR ttbl_company1.num-per NE ttbl_company2.num-per OR action NE "UPDATE" THEN
DO:
  PUT STREAM s-audit-rpt UNFORMATTED "Number of periods: " AT 3 ttbl_company1.num-per FORMAT "99" " (num-per)".
  IF action = "UPDATE" THEN
  DO:
    IF ttbl_company1.num-per NE ttbl_company2.num-per THEN
    PUT STREAM s-audit-rpt UNFORMATTED "*" AT 1.
    PUT STREAM s-audit-rpt UNFORMATTED "Number of periods: " AT 3 ttbl_company2.num-per FORMAT "99".
  END.
END.
IF NOT v_field_changed OR ttbl_company1.sid NE ttbl_company2.sid OR action NE "UPDATE" THEN
DO:
  PUT STREAM s-audit-rpt UNFORMATTED "State ID#: " AT 3 ttbl_company1.sid " (sid)".
  IF action = "UPDATE" THEN
  DO:
    IF ttbl_company1.sid NE ttbl_company2.sid THEN
    PUT STREAM s-audit-rpt UNFORMATTED "*" AT 1.
    PUT STREAM s-audit-rpt UNFORMATTED "State ID#: " AT 3 ttbl_company2.sid.
  END.
END.
IF NOT v_field_changed OR ttbl_company1.state NE ttbl_company2.state OR action NE "UPDATE" THEN
DO:
  PUT STREAM s-audit-rpt UNFORMATTED "State: " AT 3 ttbl_company1.state " (state)".
  IF action = "UPDATE" THEN
  DO:
    IF ttbl_company1.state NE ttbl_company2.state THEN
    PUT STREAM s-audit-rpt UNFORMATTED "*" AT 1.
    PUT STREAM s-audit-rpt UNFORMATTED "State: " AT 3 ttbl_company2.state.
  END.
END.
IF NOT v_field_changed OR ttbl_company1.yend-off NE ttbl_company2.yend-off OR action NE "UPDATE" THEN
DO:
  PUT STREAM s-audit-rpt UNFORMATTED "Yr End Offset: " AT 3 ttbl_company1.yend-off FORMAT ">9" " (yend-off)".
  IF action = "UPDATE" THEN
  DO:
    IF ttbl_company1.yend-off NE ttbl_company2.yend-off THEN
    PUT STREAM s-audit-rpt UNFORMATTED "*" AT 1.
    PUT STREAM s-audit-rpt UNFORMATTED "Yr End Offset: " AT 3 ttbl_company2.yend-off FORMAT ">9".
  END.
END.
IF NOT v_field_changed OR ttbl_company1.yend-per NE ttbl_company2.yend-per OR action NE "UPDATE" THEN
DO:
  PUT STREAM s-audit-rpt UNFORMATTED "Year-End Adjustment: " AT 3 ttbl_company1.yend-per FORMAT "yes/no" " (yend-per)".
  IF action = "UPDATE" THEN
  DO:
    IF ttbl_company1.yend-per NE ttbl_company2.yend-per THEN
    PUT STREAM s-audit-rpt UNFORMATTED "*" AT 1.
    PUT STREAM s-audit-rpt UNFORMATTED "Year-End Adjustment: " AT 3 ttbl_company2.yend-per FORMAT "yes/no".
  END.
END.
IF NOT v_field_changed OR ttbl_company1.zip NE ttbl_company2.zip OR action NE "UPDATE" THEN
DO:
  PUT STREAM s-audit-rpt UNFORMATTED "Postal Code: " AT 3 ttbl_company1.zip " (zip)".
  IF action = "UPDATE" THEN
  DO:
    IF ttbl_company1.zip NE ttbl_company2.zip THEN
    PUT STREAM s-audit-rpt UNFORMATTED "*" AT 1.
    PUT STREAM s-audit-rpt UNFORMATTED "Postal Code: " AT 3 ttbl_company2.zip.
  END.
END.
IF v_show_index THEN
DO:
  PUT STREAM s-audit-rpt UNFORMATTED "Index Fields - Label: Value (database field-name)" AT 5.
  PUT STREAM s-audit-rpt UNFORMATTED "Company Name: " AT 7.
  IF action = "UPDATE" THEN
  PUT STREAM s-audit-rpt UNFORMATTED ttbl_company2.name " (name)".
  ELSE
  PUT STREAM s-audit-rpt UNFORMATTED ttbl_company1.name " (name)".
  PUT STREAM s-audit-rpt UNFORMATTED "Company: " AT 7.
  IF action = "UPDATE" THEN
  PUT STREAM s-audit-rpt UNFORMATTED ttbl_company2.company " (company)".
  ELSE
  PUT STREAM s-audit-rpt UNFORMATTED ttbl_company1.company " (company)".
END.
PUT STREAM s-audit-rpt UNFORMATTED SKIP(1).
