/* crew.p - generated 01/10/2001 - 12:59:59 by nosweat */

/* this program was generated by auditgn1.p and should not be modified */
/* modifications should be made to the auto generation program         */

DEFINE INPUT PARAMETER action AS CHARACTER NO-UNDO.

DEFINE VARIABLE i AS INTEGER NO-UNDO.

{methods/defines/audit.i}
FIND FIRST ttbl_header.

DEFINE WORK-TABLE ttbl_crew1 NO-UNDO LIKE crew.
DEFINE WORK-TABLE ttbl_crew2 NO-UNDO LIKE crew.

CREATE ttbl_crew1.
IMPORT STREAM s_audit_file ttbl_crew1.
CREATE ttbl_crew2.
IF action = "UPDATE" THEN
IMPORT STREAM s_audit_file ttbl_crew2.

PUT STREAM s-audit-rpt UNFORMATTED
  "Action    User ID/(User Name)            DB.Table/(Database)  Date       Time" SKIP
  "--------- ------------------------------ -------------------- ---------- --------" SKIP
  ttbl_header.v_action AT 1
  ttbl_header.v_userid AT 11
  ttbl_header.v_file AT 42
  ttbl_header.v_date FORMAT "99/99/9999" AT 63
  STRING(ttbl_header.v_time,"HH:MM:SS") AT 74.
FIND users WHERE users.user_id = v_userid NO-LOCK NO-ERROR.
IF AVAILABLE users THEN
PUT STREAM s-audit-rpt UNFORMATTED "(" AT 10 users.user_name ")".
ELSE
PUT STREAM s-audit-rpt UNFORMATTED "(user record does not exist)" AT 10.
PUT STREAM s-audit-rpt UNFORMATTED
  "(" AT 41 ttbl_header.v_db ")" SKIP(1)
  "Label: Value (database field-name)" AT 3.
IF action = "UPDATE" THEN
PUT STREAM s-audit-rpt UNFORMATTED " ( * - denotes field value change )".
PUT STREAM s-audit-rpt UNFORMATTED FILL("-",75) AT 3.
IF NOT v_field_changed OR ttbl_crew1.company NE ttbl_crew2.company OR action NE "UPDATE" THEN
DO:
  PUT STREAM s-audit-rpt UNFORMATTED "Company: " AT 3 ttbl_crew1.company " (company)".
  IF action = "UPDATE" THEN
  DO:
    IF ttbl_crew1.company NE ttbl_crew2.company THEN
    PUT STREAM s-audit-rpt UNFORMATTED "*" AT 1.
    PUT STREAM s-audit-rpt UNFORMATTED "Company: " AT 3 ttbl_crew2.company.
  END.
END.
IF NOT v_field_changed OR ttbl_crew1.contribution NE ttbl_crew2.contribution OR action NE "UPDATE" THEN
DO:
  PUT STREAM s-audit-rpt UNFORMATTED "Contribution Factor: " AT 3 ttbl_crew1.contribution FORMAT ">>9.999" " (contribution)".
  IF action = "UPDATE" THEN
  DO:
    IF ttbl_crew1.contribution NE ttbl_crew2.contribution THEN
    PUT STREAM s-audit-rpt UNFORMATTED "*" AT 1.
    PUT STREAM s-audit-rpt UNFORMATTED "Contribution Factor: " AT 3 ttbl_crew2.contribution FORMAT ">>9.999".
  END.
END.
IF NOT v_field_changed OR ttbl_crew1.crew-id NE ttbl_crew2.crew-id OR action NE "UPDATE" THEN
DO:
  PUT STREAM s-audit-rpt UNFORMATTED "Crew ID: " AT 3 ttbl_crew1.crew-id " (crew-id)".
  IF action = "UPDATE" THEN
  DO:
    IF ttbl_crew1.crew-id NE ttbl_crew2.crew-id THEN
    PUT STREAM s-audit-rpt UNFORMATTED "*" AT 1.
    PUT STREAM s-audit-rpt UNFORMATTED "Crew ID: " AT 3 ttbl_crew2.crew-id.
  END.
END.
DO i = 1 TO 3:
IF NOT v_field_changed OR ttbl_crew1.descr[i] NE ttbl_crew2.descr[i] OR action NE "UPDATE" THEN
DO:
  PUT STREAM s-audit-rpt UNFORMATTED "Crew Description[" AT 3 i "]: " ttbl_crew1.descr[i] " (descr[" i "])".
  IF action = "UPDATE" THEN
  DO:
    IF ttbl_crew1.descr[i] NE ttbl_crew2.descr[i] THEN
    PUT STREAM s-audit-rpt UNFORMATTED "*" AT 1.
    PUT STREAM s-audit-rpt UNFORMATTED "Crew Description[" AT 3 i "]: " ttbl_crew2.descr[i].
  END.
END.
END.
IF NOT v_field_changed OR ttbl_crew1.emp-id NE ttbl_crew2.emp-id OR action NE "UPDATE" THEN
DO:
  PUT STREAM s-audit-rpt UNFORMATTED "Emp ID: " AT 3 ttbl_crew1.emp-id " (emp-id)".
  IF action = "UPDATE" THEN
  DO:
    IF ttbl_crew1.emp-id NE ttbl_crew2.emp-id THEN
    PUT STREAM s-audit-rpt UNFORMATTED "*" AT 1.
    PUT STREAM s-audit-rpt UNFORMATTED "Emp ID: " AT 3 ttbl_crew2.emp-id.
  END.
END.
IF NOT v_field_changed OR ttbl_crew1.m-code NE ttbl_crew2.m-code OR action NE "UPDATE" THEN
DO:
  PUT STREAM s-audit-rpt UNFORMATTED "Machine: " AT 3 ttbl_crew1.m-code " (m-code)".
  IF action = "UPDATE" THEN
  DO:
    IF ttbl_crew1.m-code NE ttbl_crew2.m-code THEN
    PUT STREAM s-audit-rpt UNFORMATTED "*" AT 1.
    PUT STREAM s-audit-rpt UNFORMATTED "Machine: " AT 3 ttbl_crew2.m-code.
  END.
END.
DO i = 1 TO 3:
IF NOT v_field_changed OR ttbl_crew1.rate[i] NE ttbl_crew2.rate[i] OR action NE "UPDATE" THEN
DO:
  PUT STREAM s-audit-rpt UNFORMATTED "Rate[" AT 3 i "]: " ttbl_crew1.rate[i] FORMAT ">>>>9.99<<<" " (rate[" i "])".
  IF action = "UPDATE" THEN
  DO:
    IF ttbl_crew1.rate[i] NE ttbl_crew2.rate[i] THEN
    PUT STREAM s-audit-rpt UNFORMATTED "*" AT 1.
    PUT STREAM s-audit-rpt UNFORMATTED "Rate[" AT 3 i "]: " ttbl_crew2.rate[i] FORMAT ">>>>9.99<<<".
  END.
END.
END.
IF v_show_index THEN
DO:
  PUT STREAM s-audit-rpt UNFORMATTED "Index Fields - Label: Value (database field-name)" AT 5.
  PUT STREAM s-audit-rpt UNFORMATTED "Company: " AT 7.
  IF action = "UPDATE" THEN
  PUT STREAM s-audit-rpt UNFORMATTED ttbl_crew2.company " (company)".
  ELSE
  PUT STREAM s-audit-rpt UNFORMATTED ttbl_crew1.company " (company)".
  PUT STREAM s-audit-rpt UNFORMATTED "Machine: " AT 7.
  IF action = "UPDATE" THEN
  PUT STREAM s-audit-rpt UNFORMATTED ttbl_crew2.m-code " (m-code)".
  ELSE
  PUT STREAM s-audit-rpt UNFORMATTED ttbl_crew1.m-code " (m-code)".
  PUT STREAM s-audit-rpt UNFORMATTED "Crew ID: " AT 7.
  IF action = "UPDATE" THEN
  PUT STREAM s-audit-rpt UNFORMATTED ttbl_crew2.crew-id " (crew-id)".
  ELSE
  PUT STREAM s-audit-rpt UNFORMATTED ttbl_crew1.crew-id " (crew-id)".
  PUT STREAM s-audit-rpt UNFORMATTED "Emp ID: " AT 7.
  IF action = "UPDATE" THEN
  PUT STREAM s-audit-rpt UNFORMATTED ttbl_crew2.emp-id " (emp-id)".
  ELSE
  PUT STREAM s-audit-rpt UNFORMATTED ttbl_crew1.emp-id " (emp-id)".
END.
PUT STREAM s-audit-rpt UNFORMATTED SKIP(1).
