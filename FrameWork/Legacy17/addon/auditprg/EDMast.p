/* EDMast.p - generated 01/10/2001 - 13:00:01 by nosweat */

/* this program was generated by auditgn1.p and should not be modified */
/* modifications should be made to the auto generation program         */

DEFINE INPUT PARAMETER action AS CHARACTER NO-UNDO.

DEFINE VARIABLE i AS INTEGER NO-UNDO.

{methods/defines/audit.i}
FIND FIRST ttbl_header.

DEFINE WORK-TABLE ttbl_EDMast1 NO-UNDO LIKE EDMast.
DEFINE WORK-TABLE ttbl_EDMast2 NO-UNDO LIKE EDMast.

CREATE ttbl_EDMast1.
IMPORT STREAM s_audit_file ttbl_EDMast1.
CREATE ttbl_EDMast2.
IF action = "UPDATE" THEN
IMPORT STREAM s_audit_file ttbl_EDMast2.

PUT STREAM s-audit-rpt UNFORMATTED
  "Action    User ID/(User Name)            DB.Table/(Database)  Date       Time" SKIP
  "--------- ------------------------------ -------------------- ---------- --------" SKIP
  ttbl_header.v_action AT 1
  ttbl_header.v_userid AT 11
  ttbl_header.v_file AT 42
  ttbl_header.v_date FORMAT "99/99/9999" AT 63
  STRING(ttbl_header.v_time,"HH:MM:SS") AT 74.
FIND users WHERE users.user_id = v_userid NO-LOCK NO-ERROR.
IF AVAILABLE users THEN
PUT STREAM s-audit-rpt UNFORMATTED "(" AT 10 users.user_name ")".
ELSE
PUT STREAM s-audit-rpt UNFORMATTED "(user record does not exist)" AT 10.
PUT STREAM s-audit-rpt UNFORMATTED
  "(" AT 41 ttbl_header.v_db ")" SKIP(1)
  "Label: Value (database field-name)" AT 3.
IF action = "UPDATE" THEN
PUT STREAM s-audit-rpt UNFORMATTED " ( * - denotes field value change )".
PUT STREAM s-audit-rpt UNFORMATTED FILL("-",75) AT 3.
IF NOT v_field_changed OR ttbl_EDMast1.ASN-on-DS NE ttbl_EDMast2.ASN-on-DS OR action NE "UPDATE" THEN
DO:
  PUT STREAM s-audit-rpt UNFORMATTED "?: " AT 3 ttbl_EDMast1.ASN-on-DS FORMAT "yes/no" " (ASN-on-DS)".
  IF action = "UPDATE" THEN
  DO:
    IF ttbl_EDMast1.ASN-on-DS NE ttbl_EDMast2.ASN-on-DS THEN
    PUT STREAM s-audit-rpt UNFORMATTED "*" AT 1.
    PUT STREAM s-audit-rpt UNFORMATTED "?: " AT 3 ttbl_EDMast2.ASN-on-DS FORMAT "yes/no".
  END.
END.
IF NOT v_field_changed OR ttbl_EDMast1.Cust NE ttbl_EDMast2.Cust OR action NE "UPDATE" THEN
DO:
  PUT STREAM s-audit-rpt UNFORMATTED "?: " AT 3 ttbl_EDMast1.Cust " (Cust)".
  IF action = "UPDATE" THEN
  DO:
    IF ttbl_EDMast1.Cust NE ttbl_EDMast2.Cust THEN
    PUT STREAM s-audit-rpt UNFORMATTED "*" AT 1.
    PUT STREAM s-audit-rpt UNFORMATTED "?: " AT 3 ttbl_EDMast2.Cust.
  END.
END.
IF NOT v_field_changed OR ttbl_EDMast1.Del-Days NE ttbl_EDMast2.Del-Days OR action NE "UPDATE" THEN
DO:
  PUT STREAM s-audit-rpt UNFORMATTED "Ship On SMTWTFS: " AT 3 ttbl_EDMast1.Del-Days " (Del-Days)".
  IF action = "UPDATE" THEN
  DO:
    IF ttbl_EDMast1.Del-Days NE ttbl_EDMast2.Del-Days THEN
    PUT STREAM s-audit-rpt UNFORMATTED "*" AT 1.
    PUT STREAM s-audit-rpt UNFORMATTED "Ship On SMTWTFS: " AT 3 ttbl_EDMast2.Del-Days.
  END.
END.
IF NOT v_field_changed OR ttbl_EDMast1.ID-Len NE ttbl_EDMast2.ID-Len OR action NE "UPDATE" THEN
DO:
  PUT STREAM s-audit-rpt UNFORMATTED "ID Length: " AT 3 ttbl_EDMast1.ID-Len FORMAT ">9" " (ID-Len)".
  IF action = "UPDATE" THEN
  DO:
    IF ttbl_EDMast1.ID-Len NE ttbl_EDMast2.ID-Len THEN
    PUT STREAM s-audit-rpt UNFORMATTED "*" AT 1.
    PUT STREAM s-audit-rpt UNFORMATTED "ID Length: " AT 3 ttbl_EDMast2.ID-Len FORMAT ">9".
  END.
END.
IF NOT v_field_changed OR ttbl_EDMast1.ID-Out NE ttbl_EDMast2.ID-Out OR action NE "UPDATE" THEN
DO:
  PUT STREAM s-audit-rpt UNFORMATTED "ID Length Outbound: " AT 3 ttbl_EDMast1.ID-Out FORMAT ">9" " (ID-Out)".
  IF action = "UPDATE" THEN
  DO:
    IF ttbl_EDMast1.ID-Out NE ttbl_EDMast2.ID-Out THEN
    PUT STREAM s-audit-rpt UNFORMATTED "*" AT 1.
    PUT STREAM s-audit-rpt UNFORMATTED "ID Length Outbound: " AT 3 ttbl_EDMast2.ID-Out FORMAT ">9".
  END.
END.
IF NOT v_field_changed OR ttbl_EDMast1.Id-trim NE ttbl_EDMast2.Id-trim OR action NE "UPDATE" THEN
DO:
  PUT STREAM s-audit-rpt UNFORMATTED "Trim ID: " AT 3 ttbl_EDMast1.Id-trim FORMAT "yes/no" " (Id-trim)".
  IF action = "UPDATE" THEN
  DO:
    IF ttbl_EDMast1.Id-trim NE ttbl_EDMast2.Id-trim THEN
    PUT STREAM s-audit-rpt UNFORMATTED "*" AT 1.
    PUT STREAM s-audit-rpt UNFORMATTED "Trim ID: " AT 3 ttbl_EDMast2.Id-trim FORMAT "yes/no".
  END.
END.
IF NOT v_field_changed OR ttbl_EDMast1.Item-Length NE ttbl_EDMast2.Item-Length OR action NE "UPDATE" THEN
DO:
  PUT STREAM s-audit-rpt UNFORMATTED "?: " AT 3 ttbl_EDMast1.Item-Length FORMAT ">9" " (Item-Length)".
  IF action = "UPDATE" THEN
  DO:
    IF ttbl_EDMast1.Item-Length NE ttbl_EDMast2.Item-Length THEN
    PUT STREAM s-audit-rpt UNFORMATTED "*" AT 1.
    PUT STREAM s-audit-rpt UNFORMATTED "?: " AT 3 ttbl_EDMast2.Item-Length FORMAT ">9".
  END.
END.
IF NOT v_field_changed OR ttbl_EDMast1.Item-Prefix NE ttbl_EDMast2.Item-Prefix OR action NE "UPDATE" THEN
DO:
  PUT STREAM s-audit-rpt UNFORMATTED "?: " AT 3 ttbl_EDMast1.Item-Prefix " (Item-Prefix)".
  IF action = "UPDATE" THEN
  DO:
    IF ttbl_EDMast1.Item-Prefix NE ttbl_EDMast2.Item-Prefix THEN
    PUT STREAM s-audit-rpt UNFORMATTED "*" AT 1.
    PUT STREAM s-audit-rpt UNFORMATTED "?: " AT 3 ttbl_EDMast2.Item-Prefix.
  END.
END.
IF NOT v_field_changed OR ttbl_EDMast1.Item-suffix NE ttbl_EDMast2.Item-suffix OR action NE "UPDATE" THEN
DO:
  PUT STREAM s-audit-rpt UNFORMATTED "?: " AT 3 ttbl_EDMast1.Item-suffix " (Item-suffix)".
  IF action = "UPDATE" THEN
  DO:
    IF ttbl_EDMast1.Item-suffix NE ttbl_EDMast2.Item-suffix THEN
    PUT STREAM s-audit-rpt UNFORMATTED "*" AT 1.
    PUT STREAM s-audit-rpt UNFORMATTED "?: " AT 3 ttbl_EDMast2.Item-suffix.
  END.
END.
IF NOT v_field_changed OR ttbl_EDMast1.Order-no-mask NE ttbl_EDMast2.Order-no-mask OR action NE "UPDATE" THEN
DO:
  PUT STREAM s-audit-rpt UNFORMATTED "?: " AT 3 ttbl_EDMast1.Order-no-mask FORMAT ">>>>>>>>" " (Order-no-mask)".
  IF action = "UPDATE" THEN
  DO:
    IF ttbl_EDMast1.Order-no-mask NE ttbl_EDMast2.Order-no-mask THEN
    PUT STREAM s-audit-rpt UNFORMATTED "*" AT 1.
    PUT STREAM s-audit-rpt UNFORMATTED "?: " AT 3 ttbl_EDMast2.Order-no-mask FORMAT ">>>>>>>>".
  END.
END.
IF NOT v_field_changed OR ttbl_EDMast1.Partner NE ttbl_EDMast2.Partner OR action NE "UPDATE" THEN
DO:
  PUT STREAM s-audit-rpt UNFORMATTED "?: " AT 3 ttbl_EDMast1.Partner " (Partner)".
  IF action = "UPDATE" THEN
  DO:
    IF ttbl_EDMast1.Partner NE ttbl_EDMast2.Partner THEN
    PUT STREAM s-audit-rpt UNFORMATTED "*" AT 1.
    PUT STREAM s-audit-rpt UNFORMATTED "?: " AT 3 ttbl_EDMast2.Partner.
  END.
END.
IF NOT v_field_changed OR ttbl_EDMast1.Path-in NE ttbl_EDMast2.Path-in OR action NE "UPDATE" THEN
DO:
  PUT STREAM s-audit-rpt UNFORMATTED "?: " AT 3 ttbl_EDMast1.Path-in " (Path-in)".
  IF action = "UPDATE" THEN
  DO:
    IF ttbl_EDMast1.Path-in NE ttbl_EDMast2.Path-in THEN
    PUT STREAM s-audit-rpt UNFORMATTED "*" AT 1.
    PUT STREAM s-audit-rpt UNFORMATTED "?: " AT 3 ttbl_EDMast2.Path-in.
  END.
END.
IF NOT v_field_changed OR ttbl_EDMast1.Path-out NE ttbl_EDMast2.Path-out OR action NE "UPDATE" THEN
DO:
  PUT STREAM s-audit-rpt UNFORMATTED "?: " AT 3 ttbl_EDMast1.Path-out " (Path-out)".
  IF action = "UPDATE" THEN
  DO:
    IF ttbl_EDMast1.Path-out NE ttbl_EDMast2.Path-out THEN
    PUT STREAM s-audit-rpt UNFORMATTED "*" AT 1.
    PUT STREAM s-audit-rpt UNFORMATTED "?: " AT 3 ttbl_EDMast2.Path-out.
  END.
END.
IF NOT v_field_changed OR ttbl_EDMast1.RE-code NE ttbl_EDMast2.RE-code OR action NE "UPDATE" THEN
DO:
  PUT STREAM s-audit-rpt UNFORMATTED "Remit Code: " AT 3 ttbl_EDMast1.RE-code " (RE-code)".
  IF action = "UPDATE" THEN
  DO:
    IF ttbl_EDMast1.RE-code NE ttbl_EDMast2.RE-code THEN
    PUT STREAM s-audit-rpt UNFORMATTED "*" AT 1.
    PUT STREAM s-audit-rpt UNFORMATTED "Remit Code: " AT 3 ttbl_EDMast2.RE-code.
  END.
END.
IF NOT v_field_changed OR ttbl_EDMast1.Seq NE ttbl_EDMast2.Seq OR action NE "UPDATE" THEN
DO:
  PUT STREAM s-audit-rpt UNFORMATTED "?: " AT 3 ttbl_EDMast1.Seq FORMAT ">>>>>>9" " (Seq)".
  IF action = "UPDATE" THEN
  DO:
    IF ttbl_EDMast1.Seq NE ttbl_EDMast2.Seq THEN
    PUT STREAM s-audit-rpt UNFORMATTED "*" AT 1.
    PUT STREAM s-audit-rpt UNFORMATTED "?: " AT 3 ttbl_EDMast2.Seq FORMAT ">>>>>>9".
  END.
END.
IF NOT v_field_changed OR ttbl_EDMast1.Sf-code NE ttbl_EDMast2.Sf-code OR action NE "UPDATE" THEN
DO:
  PUT STREAM s-audit-rpt UNFORMATTED "?: " AT 3 ttbl_EDMast1.Sf-code " (Sf-code)".
  IF action = "UPDATE" THEN
  DO:
    IF ttbl_EDMast1.Sf-code NE ttbl_EDMast2.Sf-code THEN
    PUT STREAM s-audit-rpt UNFORMATTED "*" AT 1.
    PUT STREAM s-audit-rpt UNFORMATTED "?: " AT 3 ttbl_EDMast2.Sf-code.
  END.
END.
IF NOT v_field_changed OR ttbl_EDMast1.Ship-to-mask NE ttbl_EDMast2.Ship-to-mask OR action NE "UPDATE" THEN
DO:
  PUT STREAM s-audit-rpt UNFORMATTED "?: " AT 3 ttbl_EDMast1.Ship-to-mask FORMAT ">>>>>>>" " (Ship-to-mask)".
  IF action = "UPDATE" THEN
  DO:
    IF ttbl_EDMast1.Ship-to-mask NE ttbl_EDMast2.Ship-to-mask THEN
    PUT STREAM s-audit-rpt UNFORMATTED "*" AT 1.
    PUT STREAM s-audit-rpt UNFORMATTED "?: " AT 3 ttbl_EDMast2.Ship-to-mask FORMAT ">>>>>>>".
  END.
END.
IF NOT v_field_changed OR ttbl_EDMast1.User-name NE ttbl_EDMast2.User-name OR action NE "UPDATE" THEN
DO:
  PUT STREAM s-audit-rpt UNFORMATTED "?: " AT 3 ttbl_EDMast1.User-name " (User-name)".
  IF action = "UPDATE" THEN
  DO:
    IF ttbl_EDMast1.User-name NE ttbl_EDMast2.User-name THEN
    PUT STREAM s-audit-rpt UNFORMATTED "*" AT 1.
    PUT STREAM s-audit-rpt UNFORMATTED "?: " AT 3 ttbl_EDMast2.User-name.
  END.
END.
IF NOT v_field_changed OR ttbl_EDMast1.Vendor NE ttbl_EDMast2.Vendor OR action NE "UPDATE" THEN
DO:
  PUT STREAM s-audit-rpt UNFORMATTED "?: " AT 3 ttbl_EDMast1.Vendor " (Vendor)".
  IF action = "UPDATE" THEN
  DO:
    IF ttbl_EDMast1.Vendor NE ttbl_EDMast2.Vendor THEN
    PUT STREAM s-audit-rpt UNFORMATTED "*" AT 1.
    PUT STREAM s-audit-rpt UNFORMATTED "?: " AT 3 ttbl_EDMast2.Vendor.
  END.
END.
IF NOT v_field_changed OR ttbl_EDMast1.We-cust NE ttbl_EDMast2.We-cust OR action NE "UPDATE" THEN
DO:
  PUT STREAM s-audit-rpt UNFORMATTED "Our Cust#: " AT 3 ttbl_EDMast1.We-cust " (We-cust)".
  IF action = "UPDATE" THEN
  DO:
    IF ttbl_EDMast1.We-cust NE ttbl_EDMast2.We-cust THEN
    PUT STREAM s-audit-rpt UNFORMATTED "*" AT 1.
    PUT STREAM s-audit-rpt UNFORMATTED "Our Cust#: " AT 3 ttbl_EDMast2.We-cust.
  END.
END.
IF NOT v_field_changed OR ttbl_EDMast1.We-vend-no NE ttbl_EDMast2.We-vend-no OR action NE "UPDATE" THEN
DO:
  PUT STREAM s-audit-rpt UNFORMATTED "Our Vend#: " AT 3 ttbl_EDMast1.We-vend-no " (We-vend-no)".
  IF action = "UPDATE" THEN
  DO:
    IF ttbl_EDMast1.We-vend-no NE ttbl_EDMast2.We-vend-no THEN
    PUT STREAM s-audit-rpt UNFORMATTED "*" AT 1.
    PUT STREAM s-audit-rpt UNFORMATTED "Our Vend#: " AT 3 ttbl_EDMast2.We-vend-no.
  END.
END.
IF v_show_index THEN
DO:
  PUT STREAM s-audit-rpt UNFORMATTED "Index Fields - Label: Value (database field-name)" AT 5.
  PUT STREAM s-audit-rpt UNFORMATTED "?: " AT 7.
  IF action = "UPDATE" THEN
  PUT STREAM s-audit-rpt UNFORMATTED ttbl_EDMast2.Partner " (Partner)".
  ELSE
  PUT STREAM s-audit-rpt UNFORMATTED ttbl_EDMast1.Partner " (Partner)".
END.
PUT STREAM s-audit-rpt UNFORMATTED SKIP(1).
