/* inv-line.p - generated 01/10/2001 - 13:00:03 by nosweat */

/* this program was generated by auditgn1.p and should not be modified */
/* modifications should be made to the auto generation program         */

DEFINE INPUT PARAMETER action AS CHARACTER NO-UNDO.

DEFINE VARIABLE i AS INTEGER NO-UNDO.

{methods/defines/audit.i}
FIND FIRST ttbl_header.

DEFINE WORK-TABLE ttbl_inv-line1 NO-UNDO LIKE inv-line.
DEFINE WORK-TABLE ttbl_inv-line2 NO-UNDO LIKE inv-line.

CREATE ttbl_inv-line1.
IMPORT STREAM s_audit_file ttbl_inv-line1.
CREATE ttbl_inv-line2.
IF action = "UPDATE" THEN
IMPORT STREAM s_audit_file ttbl_inv-line2.

PUT STREAM s-audit-rpt UNFORMATTED
  "Action    User ID/(User Name)            DB.Table/(Database)  Date       Time" SKIP
  "--------- ------------------------------ -------------------- ---------- --------" SKIP
  ttbl_header.v_action AT 1
  ttbl_header.v_userid AT 11
  ttbl_header.v_file AT 42
  ttbl_header.v_date FORMAT "99/99/9999" AT 63
  STRING(ttbl_header.v_time,"HH:MM:SS") AT 74.
FIND users WHERE users.user_id = v_userid NO-LOCK NO-ERROR.
IF AVAILABLE users THEN
PUT STREAM s-audit-rpt UNFORMATTED "(" AT 10 users.user_name ")".
ELSE
PUT STREAM s-audit-rpt UNFORMATTED "(user record does not exist)" AT 10.
PUT STREAM s-audit-rpt UNFORMATTED
  "(" AT 41 ttbl_header.v_db ")" SKIP(1)
  "Label: Value (database field-name)" AT 3.
IF action = "UPDATE" THEN
PUT STREAM s-audit-rpt UNFORMATTED " ( * - denotes field value change )".
PUT STREAM s-audit-rpt UNFORMATTED FILL("-",75) AT 3.
IF NOT v_field_changed OR ttbl_inv-line1.amt-msf NE ttbl_inv-line2.amt-msf OR action NE "UPDATE" THEN
DO:
  PUT STREAM s-audit-rpt UNFORMATTED "Amount MSF: " AT 3 ttbl_inv-line1.amt-msf FORMAT "->>>>,>>9.99" " (amt-msf)".
  IF action = "UPDATE" THEN
  DO:
    IF ttbl_inv-line1.amt-msf NE ttbl_inv-line2.amt-msf THEN
    PUT STREAM s-audit-rpt UNFORMATTED "*" AT 1.
    PUT STREAM s-audit-rpt UNFORMATTED "Amount MSF: " AT 3 ttbl_inv-line2.amt-msf FORMAT "->>>>,>>9.99".
  END.
END.
IF NOT v_field_changed OR ttbl_inv-line1.b-no NE ttbl_inv-line2.b-no OR action NE "UPDATE" THEN
DO:
  PUT STREAM s-audit-rpt UNFORMATTED "Unique Id#: " AT 3 ttbl_inv-line1.b-no FORMAT ">>>>>9" " (b-no)".
  IF action = "UPDATE" THEN
  DO:
    IF ttbl_inv-line1.b-no NE ttbl_inv-line2.b-no THEN
    PUT STREAM s-audit-rpt UNFORMATTED "*" AT 1.
    PUT STREAM s-audit-rpt UNFORMATTED "Unique Id#: " AT 3 ttbl_inv-line2.b-no FORMAT ">>>>>9".
  END.
END.
IF NOT v_field_changed OR ttbl_inv-line1.blank-no NE ttbl_inv-line2.blank-no OR action NE "UPDATE" THEN
DO:
  PUT STREAM s-audit-rpt UNFORMATTED "BLANK: " AT 3 ttbl_inv-line1.blank-no FORMAT ">9" " (blank-no)".
  IF action = "UPDATE" THEN
  DO:
    IF ttbl_inv-line1.blank-no NE ttbl_inv-line2.blank-no THEN
    PUT STREAM s-audit-rpt UNFORMATTED "*" AT 1.
    PUT STREAM s-audit-rpt UNFORMATTED "BLANK: " AT 3 ttbl_inv-line2.blank-no FORMAT ">9".
  END.
END.
IF NOT v_field_changed OR ttbl_inv-line1.cas-cnt NE ttbl_inv-line2.cas-cnt OR action NE "UPDATE" THEN
DO:
  PUT STREAM s-audit-rpt UNFORMATTED "Case Count: " AT 3 ttbl_inv-line1.cas-cnt FORMAT ">>>>9" " (cas-cnt)".
  IF action = "UPDATE" THEN
  DO:
    IF ttbl_inv-line1.cas-cnt NE ttbl_inv-line2.cas-cnt THEN
    PUT STREAM s-audit-rpt UNFORMATTED "*" AT 1.
    PUT STREAM s-audit-rpt UNFORMATTED "Case Count: " AT 3 ttbl_inv-line2.cas-cnt FORMAT ">>>>9".
  END.
END.
DO i = 1 TO 3:
IF NOT v_field_changed OR ttbl_inv-line1.comm-amt[i] NE ttbl_inv-line2.comm-amt[i] OR action NE "UPDATE" THEN
DO:
  PUT STREAM s-audit-rpt UNFORMATTED "Commission Amount[" AT 3 i "]: " ttbl_inv-line1.comm-amt[i] FORMAT ">>>,>>9.99" " (comm-amt[" i "])".
  IF action = "UPDATE" THEN
  DO:
    IF ttbl_inv-line1.comm-amt[i] NE ttbl_inv-line2.comm-amt[i] THEN
    PUT STREAM s-audit-rpt UNFORMATTED "*" AT 1.
    PUT STREAM s-audit-rpt UNFORMATTED "Commission Amount[" AT 3 i "]: " ttbl_inv-line2.comm-amt[i] FORMAT ">>>,>>9.99".
  END.
END.
END.
IF NOT v_field_changed OR ttbl_inv-line1.company NE ttbl_inv-line2.company OR action NE "UPDATE" THEN
DO:
  PUT STREAM s-audit-rpt UNFORMATTED "Company: " AT 3 ttbl_inv-line1.company " (company)".
  IF action = "UPDATE" THEN
  DO:
    IF ttbl_inv-line1.company NE ttbl_inv-line2.company THEN
    PUT STREAM s-audit-rpt UNFORMATTED "*" AT 1.
    PUT STREAM s-audit-rpt UNFORMATTED "Company: " AT 3 ttbl_inv-line2.company.
  END.
END.
IF NOT v_field_changed OR ttbl_inv-line1.cons-uom NE ttbl_inv-line2.cons-uom OR action NE "UPDATE" THEN
DO:
  PUT STREAM s-audit-rpt UNFORMATTED "Cons UOM: " AT 3 ttbl_inv-line1.cons-uom " (cons-uom)".
  IF action = "UPDATE" THEN
  DO:
    IF ttbl_inv-line1.cons-uom NE ttbl_inv-line2.cons-uom THEN
    PUT STREAM s-audit-rpt UNFORMATTED "*" AT 1.
    PUT STREAM s-audit-rpt UNFORMATTED "Cons UOM: " AT 3 ttbl_inv-line2.cons-uom.
  END.
END.
IF NOT v_field_changed OR ttbl_inv-line1.cost NE ttbl_inv-line2.cost OR action NE "UPDATE" THEN
DO:
  PUT STREAM s-audit-rpt UNFORMATTED "Costs: " AT 3 ttbl_inv-line1.cost FORMAT "->>>,>>>,>>9.99" " (cost)".
  IF action = "UPDATE" THEN
  DO:
    IF ttbl_inv-line1.cost NE ttbl_inv-line2.cost THEN
    PUT STREAM s-audit-rpt UNFORMATTED "*" AT 1.
    PUT STREAM s-audit-rpt UNFORMATTED "Costs: " AT 3 ttbl_inv-line2.cost FORMAT "->>>,>>>,>>9.99".
  END.
END.
IF NOT v_field_changed OR ttbl_inv-line1.cust-no NE ttbl_inv-line2.cust-no OR action NE "UPDATE" THEN
DO:
  PUT STREAM s-audit-rpt UNFORMATTED "Customer: " AT 3 ttbl_inv-line1.cust-no " (cust-no)".
  IF action = "UPDATE" THEN
  DO:
    IF ttbl_inv-line1.cust-no NE ttbl_inv-line2.cust-no THEN
    PUT STREAM s-audit-rpt UNFORMATTED "*" AT 1.
    PUT STREAM s-audit-rpt UNFORMATTED "Customer: " AT 3 ttbl_inv-line2.cust-no.
  END.
END.
IF NOT v_field_changed OR ttbl_inv-line1.deleted NE ttbl_inv-line2.deleted OR action NE "UPDATE" THEN
DO:
  PUT STREAM s-audit-rpt UNFORMATTED "Deleted?: " AT 3 ttbl_inv-line1.deleted FORMAT "Y/N" " (deleted)".
  IF action = "UPDATE" THEN
  DO:
    IF ttbl_inv-line1.deleted NE ttbl_inv-line2.deleted THEN
    PUT STREAM s-audit-rpt UNFORMATTED "*" AT 1.
    PUT STREAM s-audit-rpt UNFORMATTED "Deleted?: " AT 3 ttbl_inv-line2.deleted FORMAT "Y/N".
  END.
END.
IF NOT v_field_changed OR ttbl_inv-line1.disc NE ttbl_inv-line2.disc OR action NE "UPDATE" THEN
DO:
  PUT STREAM s-audit-rpt UNFORMATTED "Discount: " AT 3 ttbl_inv-line1.disc FORMAT "(>>>,>>9.99)" " (disc)".
  IF action = "UPDATE" THEN
  DO:
    IF ttbl_inv-line1.disc NE ttbl_inv-line2.disc THEN
    PUT STREAM s-audit-rpt UNFORMATTED "*" AT 1.
    PUT STREAM s-audit-rpt UNFORMATTED "Discount: " AT 3 ttbl_inv-line2.disc FORMAT "(>>>,>>9.99)".
  END.
END.
IF NOT v_field_changed OR ttbl_inv-line1.e-num NE ttbl_inv-line2.e-num OR action NE "UPDATE" THEN
DO:
  PUT STREAM s-audit-rpt UNFORMATTED "Estimate Number: " AT 3 ttbl_inv-line1.e-num FORMAT ">>>>>9" " (e-num)".
  IF action = "UPDATE" THEN
  DO:
    IF ttbl_inv-line1.e-num NE ttbl_inv-line2.e-num THEN
    PUT STREAM s-audit-rpt UNFORMATTED "*" AT 1.
    PUT STREAM s-audit-rpt UNFORMATTED "Estimate Number: " AT 3 ttbl_inv-line2.e-num FORMAT ">>>>>9".
  END.
END.
IF NOT v_field_changed OR ttbl_inv-line1.est-no NE ttbl_inv-line2.est-no OR action NE "UPDATE" THEN
DO:
  PUT STREAM s-audit-rpt UNFORMATTED "Estimate#: " AT 3 ttbl_inv-line1.est-no " (est-no)".
  IF action = "UPDATE" THEN
  DO:
    IF ttbl_inv-line1.est-no NE ttbl_inv-line2.est-no THEN
    PUT STREAM s-audit-rpt UNFORMATTED "*" AT 1.
    PUT STREAM s-audit-rpt UNFORMATTED "Estimate#: " AT 3 ttbl_inv-line2.est-no.
  END.
END.
IF NOT v_field_changed OR ttbl_inv-line1.est-type NE ttbl_inv-line2.est-type OR action NE "UPDATE" THEN
DO:
  PUT STREAM s-audit-rpt UNFORMATTED "Type: " AT 3 ttbl_inv-line1.est-type FORMAT "9" " (est-type)".
  IF action = "UPDATE" THEN
  DO:
    IF ttbl_inv-line1.est-type NE ttbl_inv-line2.est-type THEN
    PUT STREAM s-audit-rpt UNFORMATTED "*" AT 1.
    PUT STREAM s-audit-rpt UNFORMATTED "Type: " AT 3 ttbl_inv-line2.est-type FORMAT "9".
  END.
END.
IF NOT v_field_changed OR ttbl_inv-line1.form-no NE ttbl_inv-line2.form-no OR action NE "UPDATE" THEN
DO:
  PUT STREAM s-audit-rpt UNFORMATTED "FORM: " AT 3 ttbl_inv-line1.form-no FORMAT ">9" " (form-no)".
  IF action = "UPDATE" THEN
  DO:
    IF ttbl_inv-line1.form-no NE ttbl_inv-line2.form-no THEN
    PUT STREAM s-audit-rpt UNFORMATTED "*" AT 1.
    PUT STREAM s-audit-rpt UNFORMATTED "FORM: " AT 3 ttbl_inv-line2.form-no FORMAT ">9".
  END.
END.
IF NOT v_field_changed OR ttbl_inv-line1.i-dscr NE ttbl_inv-line2.i-dscr OR action NE "UPDATE" THEN
DO:
  PUT STREAM s-audit-rpt UNFORMATTED "Item Description: " AT 3 ttbl_inv-line1.i-dscr " (i-dscr)".
  IF action = "UPDATE" THEN
  DO:
    IF ttbl_inv-line1.i-dscr NE ttbl_inv-line2.i-dscr THEN
    PUT STREAM s-audit-rpt UNFORMATTED "*" AT 1.
    PUT STREAM s-audit-rpt UNFORMATTED "Item Description: " AT 3 ttbl_inv-line2.i-dscr.
  END.
END.
IF NOT v_field_changed OR ttbl_inv-line1.i-name NE ttbl_inv-line2.i-name OR action NE "UPDATE" THEN
DO:
  PUT STREAM s-audit-rpt UNFORMATTED "Name: " AT 3 ttbl_inv-line1.i-name " (i-name)".
  IF action = "UPDATE" THEN
  DO:
    IF ttbl_inv-line1.i-name NE ttbl_inv-line2.i-name THEN
    PUT STREAM s-audit-rpt UNFORMATTED "*" AT 1.
    PUT STREAM s-audit-rpt UNFORMATTED "Name: " AT 3 ttbl_inv-line2.i-name.
  END.
END.
IF NOT v_field_changed OR ttbl_inv-line1.i-no NE ttbl_inv-line2.i-no OR action NE "UPDATE" THEN
DO:
  PUT STREAM s-audit-rpt UNFORMATTED "Item#: " AT 3 ttbl_inv-line1.i-no " (i-no)".
  IF action = "UPDATE" THEN
  DO:
    IF ttbl_inv-line1.i-no NE ttbl_inv-line2.i-no THEN
    PUT STREAM s-audit-rpt UNFORMATTED "*" AT 1.
    PUT STREAM s-audit-rpt UNFORMATTED "Item#: " AT 3 ttbl_inv-line2.i-no.
  END.
END.
IF NOT v_field_changed OR ttbl_inv-line1.inv-no NE ttbl_inv-line2.inv-no OR action NE "UPDATE" THEN
DO:
  PUT STREAM s-audit-rpt UNFORMATTED "Invoice Number: " AT 3 ttbl_inv-line1.inv-no FORMAT ">>>>>9" " (inv-no)".
  IF action = "UPDATE" THEN
  DO:
    IF ttbl_inv-line1.inv-no NE ttbl_inv-line2.inv-no THEN
    PUT STREAM s-audit-rpt UNFORMATTED "*" AT 1.
    PUT STREAM s-audit-rpt UNFORMATTED "Invoice Number: " AT 3 ttbl_inv-line2.inv-no FORMAT ">>>>>9".
  END.
END.
IF NOT v_field_changed OR ttbl_inv-line1.inv-qty NE ttbl_inv-line2.inv-qty OR action NE "UPDATE" THEN
DO:
  PUT STREAM s-audit-rpt UNFORMATTED "Invoice Quantity: " AT 3 ttbl_inv-line1.inv-qty FORMAT "->>,>>>,>>9.99" " (inv-qty)".
  IF action = "UPDATE" THEN
  DO:
    IF ttbl_inv-line1.inv-qty NE ttbl_inv-line2.inv-qty THEN
    PUT STREAM s-audit-rpt UNFORMATTED "*" AT 1.
    PUT STREAM s-audit-rpt UNFORMATTED "Invoice Quantity: " AT 3 ttbl_inv-line2.inv-qty FORMAT "->>,>>>,>>9.99".
  END.
END.
IF NOT v_field_changed OR ttbl_inv-line1.j-no NE ttbl_inv-line2.j-no OR action NE "UPDATE" THEN
DO:
  PUT STREAM s-audit-rpt UNFORMATTED "Internial Job Number: " AT 3 ttbl_inv-line1.j-no FORMAT ">>>>>>9" " (j-no)".
  IF action = "UPDATE" THEN
  DO:
    IF ttbl_inv-line1.j-no NE ttbl_inv-line2.j-no THEN
    PUT STREAM s-audit-rpt UNFORMATTED "*" AT 1.
    PUT STREAM s-audit-rpt UNFORMATTED "Internial Job Number: " AT 3 ttbl_inv-line2.j-no FORMAT ">>>>>>9".
  END.
END.
IF NOT v_field_changed OR ttbl_inv-line1.job-no NE ttbl_inv-line2.job-no OR action NE "UPDATE" THEN
DO:
  PUT STREAM s-audit-rpt UNFORMATTED "Job Number: " AT 3 ttbl_inv-line1.job-no " (job-no)".
  IF action = "UPDATE" THEN
  DO:
    IF ttbl_inv-line1.job-no NE ttbl_inv-line2.job-no THEN
    PUT STREAM s-audit-rpt UNFORMATTED "*" AT 1.
    PUT STREAM s-audit-rpt UNFORMATTED "Job Number: " AT 3 ttbl_inv-line2.job-no.
  END.
END.
IF NOT v_field_changed OR ttbl_inv-line1.job-no2 NE ttbl_inv-line2.job-no2 OR action NE "UPDATE" THEN
DO:
  PUT STREAM s-audit-rpt UNFORMATTED "Run #: " AT 3 ttbl_inv-line1.job-no2 FORMAT ">9" " (job-no2)".
  IF action = "UPDATE" THEN
  DO:
    IF ttbl_inv-line1.job-no2 NE ttbl_inv-line2.job-no2 THEN
    PUT STREAM s-audit-rpt UNFORMATTED "*" AT 1.
    PUT STREAM s-audit-rpt UNFORMATTED "Run #: " AT 3 ttbl_inv-line2.job-no2 FORMAT ">9".
  END.
END.
IF NOT v_field_changed OR ttbl_inv-line1.line NE ttbl_inv-line2.line OR action NE "UPDATE" THEN
DO:
  PUT STREAM s-audit-rpt UNFORMATTED "Line: " AT 3 ttbl_inv-line1.line FORMAT "99" " (line)".
  IF action = "UPDATE" THEN
  DO:
    IF ttbl_inv-line1.line NE ttbl_inv-line2.line THEN
    PUT STREAM s-audit-rpt UNFORMATTED "*" AT 1.
    PUT STREAM s-audit-rpt UNFORMATTED "Line: " AT 3 ttbl_inv-line2.line FORMAT "99".
  END.
END.
IF NOT v_field_changed OR ttbl_inv-line1.ord-date NE ttbl_inv-line2.ord-date OR action NE "UPDATE" THEN
DO:
  PUT STREAM s-audit-rpt UNFORMATTED "Order Date: " AT 3 ttbl_inv-line1.ord-date FORMAT "99/99/99" " (ord-date)".
  IF action = "UPDATE" THEN
  DO:
    IF ttbl_inv-line1.ord-date NE ttbl_inv-line2.ord-date THEN
    PUT STREAM s-audit-rpt UNFORMATTED "*" AT 1.
    PUT STREAM s-audit-rpt UNFORMATTED "Order Date: " AT 3 ttbl_inv-line2.ord-date FORMAT "99/99/99".
  END.
END.
IF NOT v_field_changed OR ttbl_inv-line1.ord-no NE ttbl_inv-line2.ord-no OR action NE "UPDATE" THEN
DO:
  PUT STREAM s-audit-rpt UNFORMATTED "Order#: " AT 3 ttbl_inv-line1.ord-no FORMAT ">>>>>9" " (ord-no)".
  IF action = "UPDATE" THEN
  DO:
    IF ttbl_inv-line1.ord-no NE ttbl_inv-line2.ord-no THEN
    PUT STREAM s-audit-rpt UNFORMATTED "*" AT 1.
    PUT STREAM s-audit-rpt UNFORMATTED "Order#: " AT 3 ttbl_inv-line2.ord-no FORMAT ">>>>>9".
  END.
END.
IF NOT v_field_changed OR ttbl_inv-line1.p-c NE ttbl_inv-line2.p-c OR action NE "UPDATE" THEN
DO:
  PUT STREAM s-audit-rpt UNFORMATTED "Partial/Complete: " AT 3 ttbl_inv-line1.p-c FORMAT "C/P" " (p-c)".
  IF action = "UPDATE" THEN
  DO:
    IF ttbl_inv-line1.p-c NE ttbl_inv-line2.p-c THEN
    PUT STREAM s-audit-rpt UNFORMATTED "*" AT 1.
    PUT STREAM s-audit-rpt UNFORMATTED "Partial/Complete: " AT 3 ttbl_inv-line2.p-c FORMAT "C/P".
  END.
END.
IF NOT v_field_changed OR ttbl_inv-line1.part-dscr1 NE ttbl_inv-line2.part-dscr1 OR action NE "UPDATE" THEN
DO:
  PUT STREAM s-audit-rpt UNFORMATTED "Item Description: " AT 3 ttbl_inv-line1.part-dscr1 " (part-dscr1)".
  IF action = "UPDATE" THEN
  DO:
    IF ttbl_inv-line1.part-dscr1 NE ttbl_inv-line2.part-dscr1 THEN
    PUT STREAM s-audit-rpt UNFORMATTED "*" AT 1.
    PUT STREAM s-audit-rpt UNFORMATTED "Item Description: " AT 3 ttbl_inv-line2.part-dscr1.
  END.
END.
IF NOT v_field_changed OR ttbl_inv-line1.part-dscr2 NE ttbl_inv-line2.part-dscr2 OR action NE "UPDATE" THEN
DO:
  PUT STREAM s-audit-rpt UNFORMATTED "Item Description: " AT 3 ttbl_inv-line1.part-dscr2 " (part-dscr2)".
  IF action = "UPDATE" THEN
  DO:
    IF ttbl_inv-line1.part-dscr2 NE ttbl_inv-line2.part-dscr2 THEN
    PUT STREAM s-audit-rpt UNFORMATTED "*" AT 1.
    PUT STREAM s-audit-rpt UNFORMATTED "Item Description: " AT 3 ttbl_inv-line2.part-dscr2.
  END.
END.
IF NOT v_field_changed OR ttbl_inv-line1.part-no NE ttbl_inv-line2.part-no OR action NE "UPDATE" THEN
DO:
  PUT STREAM s-audit-rpt UNFORMATTED "Cust Part #: " AT 3 ttbl_inv-line1.part-no " (part-no)".
  IF action = "UPDATE" THEN
  DO:
    IF ttbl_inv-line1.part-no NE ttbl_inv-line2.part-no THEN
    PUT STREAM s-audit-rpt UNFORMATTED "*" AT 1.
    PUT STREAM s-audit-rpt UNFORMATTED "Cust Part #: " AT 3 ttbl_inv-line2.part-no.
  END.
END.
IF NOT v_field_changed OR ttbl_inv-line1.po-no NE ttbl_inv-line2.po-no OR action NE "UPDATE" THEN
DO:
  PUT STREAM s-audit-rpt UNFORMATTED "Cust PO#: " AT 3 ttbl_inv-line1.po-no " (po-no)".
  IF action = "UPDATE" THEN
  DO:
    IF ttbl_inv-line1.po-no NE ttbl_inv-line2.po-no THEN
    PUT STREAM s-audit-rpt UNFORMATTED "*" AT 1.
    PUT STREAM s-audit-rpt UNFORMATTED "Cust PO#: " AT 3 ttbl_inv-line2.po-no.
  END.
END.
IF NOT v_field_changed OR ttbl_inv-line1.po-no-po NE ttbl_inv-line2.po-no-po OR action NE "UPDATE" THEN
DO:
  PUT STREAM s-audit-rpt UNFORMATTED "Board PO #: " AT 3 ttbl_inv-line1.po-no-po FORMAT ">>>>>9" " (po-no-po)".
  IF action = "UPDATE" THEN
  DO:
    IF ttbl_inv-line1.po-no-po NE ttbl_inv-line2.po-no-po THEN
    PUT STREAM s-audit-rpt UNFORMATTED "*" AT 1.
    PUT STREAM s-audit-rpt UNFORMATTED "Board PO #: " AT 3 ttbl_inv-line2.po-no-po FORMAT ">>>>>9".
  END.
END.
IF NOT v_field_changed OR ttbl_inv-line1.pr-qty-uom NE ttbl_inv-line2.pr-qty-uom OR action NE "UPDATE" THEN
DO:
  PUT STREAM s-audit-rpt UNFORMATTED "Purch UOM: " AT 3 ttbl_inv-line1.pr-qty-uom " (pr-qty-uom)".
  IF action = "UPDATE" THEN
  DO:
    IF ttbl_inv-line1.pr-qty-uom NE ttbl_inv-line2.pr-qty-uom THEN
    PUT STREAM s-audit-rpt UNFORMATTED "*" AT 1.
    PUT STREAM s-audit-rpt UNFORMATTED "Purch UOM: " AT 3 ttbl_inv-line2.pr-qty-uom.
  END.
END.
IF NOT v_field_changed OR ttbl_inv-line1.pr-uom NE ttbl_inv-line2.pr-uom OR action NE "UPDATE" THEN
DO:
  PUT STREAM s-audit-rpt UNFORMATTED "Purchased UOM: " AT 3 ttbl_inv-line1.pr-uom " (pr-uom)".
  IF action = "UPDATE" THEN
  DO:
    IF ttbl_inv-line1.pr-uom NE ttbl_inv-line2.pr-uom THEN
    PUT STREAM s-audit-rpt UNFORMATTED "*" AT 1.
    PUT STREAM s-audit-rpt UNFORMATTED "Purchased UOM: " AT 3 ttbl_inv-line2.pr-uom.
  END.
END.
IF NOT v_field_changed OR ttbl_inv-line1.price NE ttbl_inv-line2.price OR action NE "UPDATE" THEN
DO:
  PUT STREAM s-audit-rpt UNFORMATTED "Price: " AT 3 ttbl_inv-line1.price FORMAT ">>,>>>,>>9.99<<<<" " (price)".
  IF action = "UPDATE" THEN
  DO:
    IF ttbl_inv-line1.price NE ttbl_inv-line2.price THEN
    PUT STREAM s-audit-rpt UNFORMATTED "*" AT 1.
    PUT STREAM s-audit-rpt UNFORMATTED "Price: " AT 3 ttbl_inv-line2.price FORMAT ">>,>>>,>>9.99<<<<".
  END.
END.
IF NOT v_field_changed OR ttbl_inv-line1.prom-code NE ttbl_inv-line2.prom-code OR action NE "UPDATE" THEN
DO:
  PUT STREAM s-audit-rpt UNFORMATTED "Prom. Date Code: " AT 3 ttbl_inv-line1.prom-code " (prom-code)".
  IF action = "UPDATE" THEN
  DO:
    IF ttbl_inv-line1.prom-code NE ttbl_inv-line2.prom-code THEN
    PUT STREAM s-audit-rpt UNFORMATTED "*" AT 1.
    PUT STREAM s-audit-rpt UNFORMATTED "Prom. Date Code: " AT 3 ttbl_inv-line2.prom-code.
  END.
END.
IF NOT v_field_changed OR ttbl_inv-line1.prom-date NE ttbl_inv-line2.prom-date OR action NE "UPDATE" THEN
DO:
  PUT STREAM s-audit-rpt UNFORMATTED "Prom. Date: " AT 3 ttbl_inv-line1.prom-date FORMAT "99/99/99" " (prom-date)".
  IF action = "UPDATE" THEN
  DO:
    IF ttbl_inv-line1.prom-date NE ttbl_inv-line2.prom-date THEN
    PUT STREAM s-audit-rpt UNFORMATTED "*" AT 1.
    PUT STREAM s-audit-rpt UNFORMATTED "Prom. Date: " AT 3 ttbl_inv-line2.prom-date FORMAT "99/99/99".
  END.
END.
IF NOT v_field_changed OR ttbl_inv-line1.qty NE ttbl_inv-line2.qty OR action NE "UPDATE" THEN
DO:
  PUT STREAM s-audit-rpt UNFORMATTED "Quantity: " AT 3 ttbl_inv-line1.qty FORMAT "->>,>>>,>>9.9<<" " (qty)".
  IF action = "UPDATE" THEN
  DO:
    IF ttbl_inv-line1.qty NE ttbl_inv-line2.qty THEN
    PUT STREAM s-audit-rpt UNFORMATTED "*" AT 1.
    PUT STREAM s-audit-rpt UNFORMATTED "Quantity: " AT 3 ttbl_inv-line2.qty FORMAT "->>,>>>,>>9.9<<".
  END.
END.
IF NOT v_field_changed OR ttbl_inv-line1.r-no NE ttbl_inv-line2.r-no OR action NE "UPDATE" THEN
DO:
  PUT STREAM s-audit-rpt UNFORMATTED "Unique Internal Number: " AT 3 ttbl_inv-line1.r-no FORMAT ">>>>>>>9" " (r-no)".
  IF action = "UPDATE" THEN
  DO:
    IF ttbl_inv-line1.r-no NE ttbl_inv-line2.r-no THEN
    PUT STREAM s-audit-rpt UNFORMATTED "*" AT 1.
    PUT STREAM s-audit-rpt UNFORMATTED "Unique Internal Number: " AT 3 ttbl_inv-line2.r-no FORMAT ">>>>>>>9".
  END.
END.
IF NOT v_field_changed OR ttbl_inv-line1.req-code NE ttbl_inv-line2.req-code OR action NE "UPDATE" THEN
DO:
  PUT STREAM s-audit-rpt UNFORMATTED "Req. Date Code: " AT 3 ttbl_inv-line1.req-code " (req-code)".
  IF action = "UPDATE" THEN
  DO:
    IF ttbl_inv-line1.req-code NE ttbl_inv-line2.req-code THEN
    PUT STREAM s-audit-rpt UNFORMATTED "*" AT 1.
    PUT STREAM s-audit-rpt UNFORMATTED "Req. Date Code: " AT 3 ttbl_inv-line2.req-code.
  END.
END.
IF NOT v_field_changed OR ttbl_inv-line1.req-date NE ttbl_inv-line2.req-date OR action NE "UPDATE" THEN
DO:
  PUT STREAM s-audit-rpt UNFORMATTED "Req. Date: " AT 3 ttbl_inv-line1.req-date FORMAT "99/99/99" " (req-date)".
  IF action = "UPDATE" THEN
  DO:
    IF ttbl_inv-line1.req-date NE ttbl_inv-line2.req-date THEN
    PUT STREAM s-audit-rpt UNFORMATTED "*" AT 1.
    PUT STREAM s-audit-rpt UNFORMATTED "Req. Date: " AT 3 ttbl_inv-line2.req-date FORMAT "99/99/99".
  END.
END.
DO i = 1 TO 3:
IF NOT v_field_changed OR ttbl_inv-line1.s-comm[i] NE ttbl_inv-line2.s-comm[i] OR action NE "UPDATE" THEN
DO:
  PUT STREAM s-audit-rpt UNFORMATTED "Commission[" AT 3 i "]: " ttbl_inv-line1.s-comm[i] FORMAT ">>9.99" " (s-comm[" i "])".
  IF action = "UPDATE" THEN
  DO:
    IF ttbl_inv-line1.s-comm[i] NE ttbl_inv-line2.s-comm[i] THEN
    PUT STREAM s-audit-rpt UNFORMATTED "*" AT 1.
    PUT STREAM s-audit-rpt UNFORMATTED "Commission[" AT 3 i "]: " ttbl_inv-line2.s-comm[i] FORMAT ">>9.99".
  END.
END.
END.
DO i = 1 TO 3:
IF NOT v_field_changed OR ttbl_inv-line1.s-pct[i] NE ttbl_inv-line2.s-pct[i] OR action NE "UPDATE" THEN
DO:
  PUT STREAM s-audit-rpt UNFORMATTED "Percentage[" AT 3 i "]: " ttbl_inv-line1.s-pct[i] FORMAT ">>9.99" " (s-pct[" i "])".
  IF action = "UPDATE" THEN
  DO:
    IF ttbl_inv-line1.s-pct[i] NE ttbl_inv-line2.s-pct[i] THEN
    PUT STREAM s-audit-rpt UNFORMATTED "*" AT 1.
    PUT STREAM s-audit-rpt UNFORMATTED "Percentage[" AT 3 i "]: " ttbl_inv-line2.s-pct[i] FORMAT ">>9.99".
  END.
END.
END.
IF NOT v_field_changed OR ttbl_inv-line1.sf-sht NE ttbl_inv-line2.sf-sht OR action NE "UPDATE" THEN
DO:
  PUT STREAM s-audit-rpt UNFORMATTED "SqFt/Sheet: " AT 3 ttbl_inv-line1.sf-sht FORMAT ">>9.99<<" " (sf-sht)".
  IF action = "UPDATE" THEN
  DO:
    IF ttbl_inv-line1.sf-sht NE ttbl_inv-line2.sf-sht THEN
    PUT STREAM s-audit-rpt UNFORMATTED "*" AT 1.
    PUT STREAM s-audit-rpt UNFORMATTED "SqFt/Sheet: " AT 3 ttbl_inv-line2.sf-sht FORMAT ">>9.99<<".
  END.
END.
IF NOT v_field_changed OR ttbl_inv-line1.ship-qty NE ttbl_inv-line2.ship-qty OR action NE "UPDATE" THEN
DO:
  PUT STREAM s-audit-rpt UNFORMATTED "Shipped Quantity: " AT 3 ttbl_inv-line1.ship-qty FORMAT "->>,>>>,>>9.99" " (ship-qty)".
  IF action = "UPDATE" THEN
  DO:
    IF ttbl_inv-line1.ship-qty NE ttbl_inv-line2.ship-qty THEN
    PUT STREAM s-audit-rpt UNFORMATTED "*" AT 1.
    PUT STREAM s-audit-rpt UNFORMATTED "Shipped Quantity: " AT 3 ttbl_inv-line2.ship-qty FORMAT "->>,>>>,>>9.99".
  END.
END.
DO i = 1 TO 3:
IF NOT v_field_changed OR ttbl_inv-line1.sman[i] NE ttbl_inv-line2.sman[i] OR action NE "UPDATE" THEN
DO:
  PUT STREAM s-audit-rpt UNFORMATTED "Salesman Code[" AT 3 i "]: " ttbl_inv-line1.sman[i] " (sman[" i "])".
  IF action = "UPDATE" THEN
  DO:
    IF ttbl_inv-line1.sman[i] NE ttbl_inv-line2.sman[i] THEN
    PUT STREAM s-audit-rpt UNFORMATTED "*" AT 1.
    PUT STREAM s-audit-rpt UNFORMATTED "Salesman Code[" AT 3 i "]: " ttbl_inv-line2.sman[i].
  END.
END.
END.
DO i = 1 TO 3:
IF NOT v_field_changed OR ttbl_inv-line1.sname[i] NE ttbl_inv-line2.sname[i] OR action NE "UPDATE" THEN
DO:
  PUT STREAM s-audit-rpt UNFORMATTED "Salesman Name[" AT 3 i "]: " ttbl_inv-line1.sname[i] " (sname[" i "])".
  IF action = "UPDATE" THEN
  DO:
    IF ttbl_inv-line1.sname[i] NE ttbl_inv-line2.sname[i] THEN
    PUT STREAM s-audit-rpt UNFORMATTED "*" AT 1.
    PUT STREAM s-audit-rpt UNFORMATTED "Salesman Name[" AT 3 i "]: " ttbl_inv-line2.sname[i].
  END.
END.
END.
IF NOT v_field_changed OR ttbl_inv-line1.stat NE ttbl_inv-line2.stat OR action NE "UPDATE" THEN
DO:
  PUT STREAM s-audit-rpt UNFORMATTED "Order Line Status: " AT 3 ttbl_inv-line1.stat " (stat)".
  IF action = "UPDATE" THEN
  DO:
    IF ttbl_inv-line1.stat NE ttbl_inv-line2.stat THEN
    PUT STREAM s-audit-rpt UNFORMATTED "*" AT 1.
    PUT STREAM s-audit-rpt UNFORMATTED "Order Line Status: " AT 3 ttbl_inv-line2.stat.
  END.
END.
IF NOT v_field_changed OR ttbl_inv-line1.t-cost NE ttbl_inv-line2.t-cost OR action NE "UPDATE" THEN
DO:
  PUT STREAM s-audit-rpt UNFORMATTED "?: " AT 3 ttbl_inv-line1.t-cost FORMAT "->>,>>>,>>9.99" " (t-cost)".
  IF action = "UPDATE" THEN
  DO:
    IF ttbl_inv-line1.t-cost NE ttbl_inv-line2.t-cost THEN
    PUT STREAM s-audit-rpt UNFORMATTED "*" AT 1.
    PUT STREAM s-audit-rpt UNFORMATTED "?: " AT 3 ttbl_inv-line2.t-cost FORMAT "->>,>>>,>>9.99".
  END.
END.
IF NOT v_field_changed OR ttbl_inv-line1.t-freight NE ttbl_inv-line2.t-freight OR action NE "UPDATE" THEN
DO:
  PUT STREAM s-audit-rpt UNFORMATTED "Total Freight: " AT 3 ttbl_inv-line1.t-freight FORMAT "->>,>>9.99" " (t-freight)".
  IF action = "UPDATE" THEN
  DO:
    IF ttbl_inv-line1.t-freight NE ttbl_inv-line2.t-freight THEN
    PUT STREAM s-audit-rpt UNFORMATTED "*" AT 1.
    PUT STREAM s-audit-rpt UNFORMATTED "Total Freight: " AT 3 ttbl_inv-line2.t-freight FORMAT "->>,>>9.99".
  END.
END.
IF NOT v_field_changed OR ttbl_inv-line1.t-price NE ttbl_inv-line2.t-price OR action NE "UPDATE" THEN
DO:
  PUT STREAM s-audit-rpt UNFORMATTED "?: " AT 3 ttbl_inv-line1.t-price FORMAT "->>,>>>,>>9.99" " (t-price)".
  IF action = "UPDATE" THEN
  DO:
    IF ttbl_inv-line1.t-price NE ttbl_inv-line2.t-price THEN
    PUT STREAM s-audit-rpt UNFORMATTED "*" AT 1.
    PUT STREAM s-audit-rpt UNFORMATTED "?: " AT 3 ttbl_inv-line2.t-price FORMAT "->>,>>>,>>9.99".
  END.
END.
IF NOT v_field_changed OR ttbl_inv-line1.t-weight NE ttbl_inv-line2.t-weight OR action NE "UPDATE" THEN
DO:
  PUT STREAM s-audit-rpt UNFORMATTED "Total Weight: " AT 3 ttbl_inv-line1.t-weight FORMAT ">>>,>>9.99" " (t-weight)".
  IF action = "UPDATE" THEN
  DO:
    IF ttbl_inv-line1.t-weight NE ttbl_inv-line2.t-weight THEN
    PUT STREAM s-audit-rpt UNFORMATTED "*" AT 1.
    PUT STREAM s-audit-rpt UNFORMATTED "Total Weight: " AT 3 ttbl_inv-line2.t-weight FORMAT ">>>,>>9.99".
  END.
END.
IF NOT v_field_changed OR ttbl_inv-line1.tax NE ttbl_inv-line2.tax OR action NE "UPDATE" THEN
DO:
  PUT STREAM s-audit-rpt UNFORMATTED "?: " AT 3 ttbl_inv-line1.tax FORMAT "Y/N" " (tax)".
  IF action = "UPDATE" THEN
  DO:
    IF ttbl_inv-line1.tax NE ttbl_inv-line2.tax THEN
    PUT STREAM s-audit-rpt UNFORMATTED "*" AT 1.
    PUT STREAM s-audit-rpt UNFORMATTED "?: " AT 3 ttbl_inv-line2.tax FORMAT "Y/N".
  END.
END.
IF v_show_index THEN
DO:
  PUT STREAM s-audit-rpt UNFORMATTED "Index Fields - Label: Value (database field-name)" AT 5.
  PUT STREAM s-audit-rpt UNFORMATTED "Unique Internal Number: " AT 7.
  IF action = "UPDATE" THEN
  PUT STREAM s-audit-rpt UNFORMATTED ttbl_inv-line2.r-no FORMAT ">>>>>>>9" " (r-no)".
  ELSE
  PUT STREAM s-audit-rpt UNFORMATTED ttbl_inv-line1.r-no FORMAT ">>>>>>>9" " (r-no)".
  PUT STREAM s-audit-rpt UNFORMATTED "Item#: " AT 7.
  IF action = "UPDATE" THEN
  PUT STREAM s-audit-rpt UNFORMATTED ttbl_inv-line2.i-no " (i-no)".
  ELSE
  PUT STREAM s-audit-rpt UNFORMATTED ttbl_inv-line1.i-no " (i-no)".
END.
PUT STREAM s-audit-rpt UNFORMATTED SKIP(1).
