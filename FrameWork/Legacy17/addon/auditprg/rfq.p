/* rfq.p - generated 12/02/2005 - 16:52:40 by NoSweat */

/* this program was generated by auditgn1.p and should not be modified */
/* modifications should be made to the auto generation program         */

DEFINE INPUT PARAMETER action AS CHARACTER NO-UNDO.

DEFINE VARIABLE i AS INTEGER NO-UNDO.

{methods/defines/audit.i}
FIND FIRST ttbl_header.

DEFINE WORK-TABLE ttbl_rfq1 NO-UNDO LIKE rfq.
DEFINE WORK-TABLE ttbl_rfq2 NO-UNDO LIKE rfq.

CREATE ttbl_rfq1.
IMPORT STREAM s_audit_file ttbl_rfq1.
CREATE ttbl_rfq2.
IF action = "UPDATE" THEN
IMPORT STREAM s_audit_file ttbl_rfq2.

PUT STREAM s-audit-rpt UNFORMATTED
  "Action    User ID/(User Name)            DB.Table/(Database)  Date       Time" SKIP
  "--------- ------------------------------ -------------------- ---------- --------" SKIP
  ttbl_header.v_action AT 1
  ttbl_header.v_userid AT 11
  ttbl_header.v_file AT 42
  ttbl_header.v_date FORMAT "99/99/9999" AT 63
  STRING(ttbl_header.v_time,"HH:MM:SS") AT 74.
FIND users WHERE users.user_id = v_userid NO-LOCK NO-ERROR.
IF AVAILABLE users THEN
PUT STREAM s-audit-rpt UNFORMATTED "(" AT 10 users.user_name ")".
ELSE
PUT STREAM s-audit-rpt UNFORMATTED "(user record does not exist)" AT 10.
PUT STREAM s-audit-rpt UNFORMATTED
  "(" AT 41 ttbl_header.v_db ")" SKIP(1)
  "Label: Value (database field-name)" AT 3.
IF action = "UPDATE" THEN
PUT STREAM s-audit-rpt UNFORMATTED " ( * - denotes field value change )".
PUT STREAM s-audit-rpt UNFORMATTED FILL("-",75) AT 3.
IF NOT v_field_changed OR ttbl_rfq1.chg-method NE ttbl_rfq2.chg-method OR action NE "UPDATE" THEN
DO:
  PUT STREAM s-audit-rpt UNFORMATTED "Charge Method: " AT 3 ttbl_rfq1.chg-method " (chg-method)".
  IF action = "UPDATE" THEN
  DO:
    IF ttbl_rfq1.chg-method NE ttbl_rfq2.chg-method THEN
    PUT STREAM s-audit-rpt UNFORMATTED "*" AT 1.
    PUT STREAM s-audit-rpt UNFORMATTED "Charge Method: " AT 3 ttbl_rfq2.chg-method.
  END.
END.
IF NOT v_field_changed OR ttbl_rfq1.comm NE ttbl_rfq2.comm OR action NE "UPDATE" THEN
DO:
  PUT STREAM s-audit-rpt UNFORMATTED "Comm.%: " AT 3 ttbl_rfq1.comm FORMAT ">>9.99" " (comm)".
  IF action = "UPDATE" THEN
  DO:
    IF ttbl_rfq1.comm NE ttbl_rfq2.comm THEN
    PUT STREAM s-audit-rpt UNFORMATTED "*" AT 1.
    PUT STREAM s-audit-rpt UNFORMATTED "Comm.%: " AT 3 ttbl_rfq2.comm FORMAT ">>9.99".
  END.
END.
IF NOT v_field_changed OR ttbl_rfq1.company NE ttbl_rfq2.company OR action NE "UPDATE" THEN
DO:
  PUT STREAM s-audit-rpt UNFORMATTED "Company: " AT 3 ttbl_rfq1.company " (company)".
  IF action = "UPDATE" THEN
  DO:
    IF ttbl_rfq1.company NE ttbl_rfq2.company THEN
    PUT STREAM s-audit-rpt UNFORMATTED "*" AT 1.
    PUT STREAM s-audit-rpt UNFORMATTED "Company: " AT 3 ttbl_rfq2.company.
  END.
END.
IF NOT v_field_changed OR ttbl_rfq1.cust-no NE ttbl_rfq2.cust-no OR action NE "UPDATE" THEN
DO:
  PUT STREAM s-audit-rpt UNFORMATTED "Cust.#: " AT 3 ttbl_rfq1.cust-no " (cust-no)".
  IF action = "UPDATE" THEN
  DO:
    IF ttbl_rfq1.cust-no NE ttbl_rfq2.cust-no THEN
    PUT STREAM s-audit-rpt UNFORMATTED "*" AT 1.
    PUT STREAM s-audit-rpt UNFORMATTED "Cust.#: " AT 3 ttbl_rfq2.cust-no.
  END.
END.
IF NOT v_field_changed OR ttbl_rfq1.due-date NE ttbl_rfq2.due-date OR action NE "UPDATE" THEN
DO:
  PUT STREAM s-audit-rpt UNFORMATTED "Due Date: " AT 3 ttbl_rfq1.due-date FORMAT "99/99/9999" " (due-date)".
  IF action = "UPDATE" THEN
  DO:
    IF ttbl_rfq1.due-date NE ttbl_rfq2.due-date THEN
    PUT STREAM s-audit-rpt UNFORMATTED "*" AT 1.
    PUT STREAM s-audit-rpt UNFORMATTED "Due Date: " AT 3 ttbl_rfq2.due-date FORMAT "99/99/9999".
  END.
END.
IF NOT v_field_changed OR ttbl_rfq1.fob-code NE ttbl_rfq2.fob-code OR action NE "UPDATE" THEN
DO:
  PUT STREAM s-audit-rpt UNFORMATTED "FOB Code: " AT 3 ttbl_rfq1.fob-code " (fob-code)".
  IF action = "UPDATE" THEN
  DO:
    IF ttbl_rfq1.fob-code NE ttbl_rfq2.fob-code THEN
    PUT STREAM s-audit-rpt UNFORMATTED "*" AT 1.
    PUT STREAM s-audit-rpt UNFORMATTED "FOB Code: " AT 3 ttbl_rfq2.fob-code.
  END.
END.
IF NOT v_field_changed OR ttbl_rfq1.inst NE ttbl_rfq2.inst OR action NE "UPDATE" THEN
DO:
  PUT STREAM s-audit-rpt UNFORMATTED "Special Instruction: " AT 3 ttbl_rfq1.inst " (inst)".
  IF action = "UPDATE" THEN
  DO:
    IF ttbl_rfq1.inst NE ttbl_rfq2.inst THEN
    PUT STREAM s-audit-rpt UNFORMATTED "*" AT 1.
    PUT STREAM s-audit-rpt UNFORMATTED "Special Instruction: " AT 3 ttbl_rfq2.inst.
  END.
END.
IF NOT v_field_changed OR ttbl_rfq1.loc NE ttbl_rfq2.loc OR action NE "UPDATE" THEN
DO:
  PUT STREAM s-audit-rpt UNFORMATTED "Warehouse: " AT 3 ttbl_rfq1.loc " (loc)".
  IF action = "UPDATE" THEN
  DO:
    IF ttbl_rfq1.loc NE ttbl_rfq2.loc THEN
    PUT STREAM s-audit-rpt UNFORMATTED "*" AT 1.
    PUT STREAM s-audit-rpt UNFORMATTED "Warehouse: " AT 3 ttbl_rfq2.loc.
  END.
END.
IF NOT v_field_changed OR ttbl_rfq1.req-date NE ttbl_rfq2.req-date OR action NE "UPDATE" THEN
DO:
  PUT STREAM s-audit-rpt UNFORMATTED "Request Date: " AT 3 ttbl_rfq1.req-date FORMAT "99/99/9999" " (req-date)".
  IF action = "UPDATE" THEN
  DO:
    IF ttbl_rfq1.req-date NE ttbl_rfq2.req-date THEN
    PUT STREAM s-audit-rpt UNFORMATTED "*" AT 1.
    PUT STREAM s-audit-rpt UNFORMATTED "Request Date: " AT 3 ttbl_rfq2.req-date FORMAT "99/99/9999".
  END.
END.
IF NOT v_field_changed OR ttbl_rfq1.rfq-no NE ttbl_rfq2.rfq-no OR action NE "UPDATE" THEN
DO:
  PUT STREAM s-audit-rpt UNFORMATTED "RFQ#: " AT 3 ttbl_rfq1.rfq-no FORMAT "->,>>>,>>9" " (rfq-no)".
  IF action = "UPDATE" THEN
  DO:
    IF ttbl_rfq1.rfq-no NE ttbl_rfq2.rfq-no THEN
    PUT STREAM s-audit-rpt UNFORMATTED "*" AT 1.
    PUT STREAM s-audit-rpt UNFORMATTED "RFQ#: " AT 3 ttbl_rfq2.rfq-no FORMAT "->,>>>,>>9".
  END.
END.
DO i = 1 TO 2:
IF NOT v_field_changed OR ttbl_rfq1.ship-addr[i] NE ttbl_rfq2.ship-addr[i] OR action NE "UPDATE" THEN
DO:
  PUT STREAM s-audit-rpt UNFORMATTED "Address[" AT 3 i "]: " ttbl_rfq1.ship-addr[i] " (ship-addr[" i "])".
  IF action = "UPDATE" THEN
  DO:
    IF ttbl_rfq1.ship-addr[i] NE ttbl_rfq2.ship-addr[i] THEN
    PUT STREAM s-audit-rpt UNFORMATTED "*" AT 1.
    PUT STREAM s-audit-rpt UNFORMATTED "Address[" AT 3 i "]: " ttbl_rfq2.ship-addr[i].
  END.
END.
END.
IF NOT v_field_changed OR ttbl_rfq1.ship-city NE ttbl_rfq2.ship-city OR action NE "UPDATE" THEN
DO:
  PUT STREAM s-audit-rpt UNFORMATTED "City: " AT 3 ttbl_rfq1.ship-city " (ship-city)".
  IF action = "UPDATE" THEN
  DO:
    IF ttbl_rfq1.ship-city NE ttbl_rfq2.ship-city THEN
    PUT STREAM s-audit-rpt UNFORMATTED "*" AT 1.
    PUT STREAM s-audit-rpt UNFORMATTED "City: " AT 3 ttbl_rfq2.ship-city.
  END.
END.
IF NOT v_field_changed OR ttbl_rfq1.ship-name NE ttbl_rfq2.ship-name OR action NE "UPDATE" THEN
DO:
  PUT STREAM s-audit-rpt UNFORMATTED "Cust. Name: " AT 3 ttbl_rfq1.ship-name " (ship-name)".
  IF action = "UPDATE" THEN
  DO:
    IF ttbl_rfq1.ship-name NE ttbl_rfq2.ship-name THEN
    PUT STREAM s-audit-rpt UNFORMATTED "*" AT 1.
    PUT STREAM s-audit-rpt UNFORMATTED "Cust. Name: " AT 3 ttbl_rfq2.ship-name.
  END.
END.
IF NOT v_field_changed OR ttbl_rfq1.ship-state NE ttbl_rfq2.ship-state OR action NE "UPDATE" THEN
DO:
  PUT STREAM s-audit-rpt UNFORMATTED "State: " AT 3 ttbl_rfq1.ship-state " (ship-state)".
  IF action = "UPDATE" THEN
  DO:
    IF ttbl_rfq1.ship-state NE ttbl_rfq2.ship-state THEN
    PUT STREAM s-audit-rpt UNFORMATTED "*" AT 1.
    PUT STREAM s-audit-rpt UNFORMATTED "State: " AT 3 ttbl_rfq2.ship-state.
  END.
END.
IF NOT v_field_changed OR ttbl_rfq1.ship-zip NE ttbl_rfq2.ship-zip OR action NE "UPDATE" THEN
DO:
  PUT STREAM s-audit-rpt UNFORMATTED "Postal Code: " AT 3 ttbl_rfq1.ship-zip " (ship-zip)".
  IF action = "UPDATE" THEN
  DO:
    IF ttbl_rfq1.ship-zip NE ttbl_rfq2.ship-zip THEN
    PUT STREAM s-audit-rpt UNFORMATTED "*" AT 1.
    PUT STREAM s-audit-rpt UNFORMATTED "Postal Code: " AT 3 ttbl_rfq2.ship-zip.
  END.
END.
IF NOT v_field_changed OR ttbl_rfq1.sman NE ttbl_rfq2.sman OR action NE "UPDATE" THEN
DO:
  PUT STREAM s-audit-rpt UNFORMATTED "Salesman: " AT 3 ttbl_rfq1.sman " (sman)".
  IF action = "UPDATE" THEN
  DO:
    IF ttbl_rfq1.sman NE ttbl_rfq2.sman THEN
    PUT STREAM s-audit-rpt UNFORMATTED "*" AT 1.
    PUT STREAM s-audit-rpt UNFORMATTED "Salesman: " AT 3 ttbl_rfq2.sman.
  END.
END.
IF NOT v_field_changed OR ttbl_rfq1.wh-month NE ttbl_rfq2.wh-month OR action NE "UPDATE" THEN
DO:
  PUT STREAM s-audit-rpt UNFORMATTED "Whse.Month: " AT 3 ttbl_rfq1.wh-month FORMAT ">9" " (wh-month)".
  IF action = "UPDATE" THEN
  DO:
    IF ttbl_rfq1.wh-month NE ttbl_rfq2.wh-month THEN
    PUT STREAM s-audit-rpt UNFORMATTED "*" AT 1.
    PUT STREAM s-audit-rpt UNFORMATTED "Whse.Month: " AT 3 ttbl_rfq2.wh-month FORMAT ">9".
  END.
END.
IF v_show_index THEN
DO:
  PUT STREAM s-audit-rpt UNFORMATTED "Index Fields - Label: Value (database field-name)" AT 5.
  PUT STREAM s-audit-rpt UNFORMATTED "Company: " AT 7.
  IF action = "UPDATE" THEN
  PUT STREAM s-audit-rpt UNFORMATTED ttbl_rfq2.company " (company)".
  ELSE
  PUT STREAM s-audit-rpt UNFORMATTED ttbl_rfq1.company " (company)".
  PUT STREAM s-audit-rpt UNFORMATTED "Warehouse: " AT 7.
  IF action = "UPDATE" THEN
  PUT STREAM s-audit-rpt UNFORMATTED ttbl_rfq2.loc " (loc)".
  ELSE
  PUT STREAM s-audit-rpt UNFORMATTED ttbl_rfq1.loc " (loc)".
  PUT STREAM s-audit-rpt UNFORMATTED "RFQ#: " AT 7.
  IF action = "UPDATE" THEN
  PUT STREAM s-audit-rpt UNFORMATTED ttbl_rfq2.rfq-no FORMAT "->,>>>,>>9" " (rfq-no)".
  ELSE
  PUT STREAM s-audit-rpt UNFORMATTED ttbl_rfq1.rfq-no FORMAT "->,>>>,>>9" " (rfq-no)".
END.
PUT STREAM s-audit-rpt UNFORMATTED SKIP(1).
