/* printer.p - generated 01/10/2001 - 13:00:08 by nosweat */

/* this program was generated by auditgn1.p and should not be modified */
/* modifications should be made to the auto generation program         */

DEFINE INPUT PARAMETER action AS CHARACTER NO-UNDO.

DEFINE VARIABLE i AS INTEGER NO-UNDO.

{methods/defines/audit.i}
FIND FIRST ttbl_header.

DEFINE WORK-TABLE ttbl_printer1 NO-UNDO LIKE printer.
DEFINE WORK-TABLE ttbl_printer2 NO-UNDO LIKE printer.

CREATE ttbl_printer1.
IMPORT STREAM s_audit_file ttbl_printer1.
CREATE ttbl_printer2.
IF action = "UPDATE" THEN
IMPORT STREAM s_audit_file ttbl_printer2.

PUT STREAM s-audit-rpt UNFORMATTED
  "Action    User ID/(User Name)            DB.Table/(Database)  Date       Time" SKIP
  "--------- ------------------------------ -------------------- ---------- --------" SKIP
  ttbl_header.v_action AT 1
  ttbl_header.v_userid AT 11
  ttbl_header.v_file AT 42
  ttbl_header.v_date FORMAT "99/99/9999" AT 63
  STRING(ttbl_header.v_time,"HH:MM:SS") AT 74.
FIND users WHERE users.user_id = v_userid NO-LOCK NO-ERROR.
IF AVAILABLE users THEN
PUT STREAM s-audit-rpt UNFORMATTED "(" AT 10 users.user_name ")".
ELSE
PUT STREAM s-audit-rpt UNFORMATTED "(user record does not exist)" AT 10.
PUT STREAM s-audit-rpt UNFORMATTED
  "(" AT 41 ttbl_header.v_db ")" SKIP(1)
  "Label: Value (database field-name)" AT 3.
IF action = "UPDATE" THEN
PUT STREAM s-audit-rpt UNFORMATTED " ( * - denotes field value change )".
PUT STREAM s-audit-rpt UNFORMATTED FILL("-",75) AT 3.
IF NOT v_field_changed OR ttbl_printer1.company NE ttbl_printer2.company OR action NE "UPDATE" THEN
DO:
  PUT STREAM s-audit-rpt UNFORMATTED "Company: " AT 3 ttbl_printer1.company " (company)".
  IF action = "UPDATE" THEN
  DO:
    IF ttbl_printer1.company NE ttbl_printer2.company THEN
    PUT STREAM s-audit-rpt UNFORMATTED "*" AT 1.
    PUT STREAM s-audit-rpt UNFORMATTED "Company: " AT 3 ttbl_printer2.company.
  END.
END.
IF NOT v_field_changed OR ttbl_printer1.l-margin NE ttbl_printer2.l-margin OR action NE "UPDATE" THEN
DO:
  PUT STREAM s-audit-rpt UNFORMATTED "Left Margin: " AT 3 ttbl_printer1.l-margin " (l-margin)".
  IF action = "UPDATE" THEN
  DO:
    IF ttbl_printer1.l-margin NE ttbl_printer2.l-margin THEN
    PUT STREAM s-audit-rpt UNFORMATTED "*" AT 1.
    PUT STREAM s-audit-rpt UNFORMATTED "Left Margin: " AT 3 ttbl_printer2.l-margin.
  END.
END.
IF NOT v_field_changed OR ttbl_printer1.loc NE ttbl_printer2.loc OR action NE "UPDATE" THEN
DO:
  PUT STREAM s-audit-rpt UNFORMATTED "Warehouse: " AT 3 ttbl_printer1.loc " (loc)".
  IF action = "UPDATE" THEN
  DO:
    IF ttbl_printer1.loc NE ttbl_printer2.loc THEN
    PUT STREAM s-audit-rpt UNFORMATTED "*" AT 1.
    PUT STREAM s-audit-rpt UNFORMATTED "Warehouse: " AT 3 ttbl_printer2.loc.
  END.
END.
IF NOT v_field_changed OR ttbl_printer1.max-col NE ttbl_printer2.max-col OR action NE "UPDATE" THEN
DO:
  PUT STREAM s-audit-rpt UNFORMATTED "Max Cols: " AT 3 ttbl_printer1.max-col FORMAT ">>9" " (max-col)".
  IF action = "UPDATE" THEN
  DO:
    IF ttbl_printer1.max-col NE ttbl_printer2.max-col THEN
    PUT STREAM s-audit-rpt UNFORMATTED "*" AT 1.
    PUT STREAM s-audit-rpt UNFORMATTED "Max Cols: " AT 3 ttbl_printer2.max-col FORMAT ">>9".
  END.
END.
IF NOT v_field_changed OR ttbl_printer1.pitch NE ttbl_printer2.pitch OR action NE "UPDATE" THEN
DO:
  PUT STREAM s-audit-rpt UNFORMATTED "Pitch: " AT 3 ttbl_printer1.pitch " (pitch)".
  IF action = "UPDATE" THEN
  DO:
    IF ttbl_printer1.pitch NE ttbl_printer2.pitch THEN
    PUT STREAM s-audit-rpt UNFORMATTED "*" AT 1.
    PUT STREAM s-audit-rpt UNFORMATTED "Pitch: " AT 3 ttbl_printer2.pitch.
  END.
END.
IF NOT v_field_changed OR ttbl_printer1.pr-cmd NE ttbl_printer2.pr-cmd OR action NE "UPDATE" THEN
DO:
  PUT STREAM s-audit-rpt UNFORMATTED "Printer Command: " AT 3 ttbl_printer1.pr-cmd " (pr-cmd)".
  IF action = "UPDATE" THEN
  DO:
    IF ttbl_printer1.pr-cmd NE ttbl_printer2.pr-cmd THEN
    PUT STREAM s-audit-rpt UNFORMATTED "*" AT 1.
    PUT STREAM s-audit-rpt UNFORMATTED "Printer Command: " AT 3 ttbl_printer2.pr-cmd.
  END.
END.
IF NOT v_field_changed OR ttbl_printer1.pr-name NE ttbl_printer2.pr-name OR action NE "UPDATE" THEN
DO:
  PUT STREAM s-audit-rpt UNFORMATTED "Printer Name: " AT 3 ttbl_printer1.pr-name " (pr-name)".
  IF action = "UPDATE" THEN
  DO:
    IF ttbl_printer1.pr-name NE ttbl_printer2.pr-name THEN
    PUT STREAM s-audit-rpt UNFORMATTED "*" AT 1.
    PUT STREAM s-audit-rpt UNFORMATTED "Printer Name: " AT 3 ttbl_printer2.pr-name.
  END.
END.
IF NOT v_field_changed OR ttbl_printer1.pr-no NE ttbl_printer2.pr-no OR action NE "UPDATE" THEN
DO:
  PUT STREAM s-audit-rpt UNFORMATTED "Printer #: " AT 3 ttbl_printer1.pr-no FORMAT ">9" " (pr-no)".
  IF action = "UPDATE" THEN
  DO:
    IF ttbl_printer1.pr-no NE ttbl_printer2.pr-no THEN
    PUT STREAM s-audit-rpt UNFORMATTED "*" AT 1.
    PUT STREAM s-audit-rpt UNFORMATTED "Printer #: " AT 3 ttbl_printer2.pr-no FORMAT ">9".
  END.
END.
IF NOT v_field_changed OR ttbl_printer1.pr-port NE ttbl_printer2.pr-port OR action NE "UPDATE" THEN
DO:
  PUT STREAM s-audit-rpt UNFORMATTED "Port: " AT 3 ttbl_printer1.pr-port " (pr-port)".
  IF action = "UPDATE" THEN
  DO:
    IF ttbl_printer1.pr-port NE ttbl_printer2.pr-port THEN
    PUT STREAM s-audit-rpt UNFORMATTED "*" AT 1.
    PUT STREAM s-audit-rpt UNFORMATTED "Port: " AT 3 ttbl_printer2.pr-port.
  END.
END.
IF v_show_index THEN
DO:
  PUT STREAM s-audit-rpt UNFORMATTED "Index Fields - Label: Value (database field-name)" AT 5.
  PUT STREAM s-audit-rpt UNFORMATTED "Company: " AT 7.
  IF action = "UPDATE" THEN
  PUT STREAM s-audit-rpt UNFORMATTED ttbl_printer2.company " (company)".
  ELSE
  PUT STREAM s-audit-rpt UNFORMATTED ttbl_printer1.company " (company)".
  PUT STREAM s-audit-rpt UNFORMATTED "Warehouse: " AT 7.
  IF action = "UPDATE" THEN
  PUT STREAM s-audit-rpt UNFORMATTED ttbl_printer2.loc " (loc)".
  ELSE
  PUT STREAM s-audit-rpt UNFORMATTED ttbl_printer1.loc " (loc)".
  PUT STREAM s-audit-rpt UNFORMATTED "Printer #: " AT 7.
  IF action = "UPDATE" THEN
  PUT STREAM s-audit-rpt UNFORMATTED ttbl_printer2.pr-no FORMAT ">9" " (pr-no)".
  ELSE
  PUT STREAM s-audit-rpt UNFORMATTED ttbl_printer1.pr-no FORMAT ">9" " (pr-no)".
END.
PUT STREAM s-audit-rpt UNFORMATTED SKIP(1).
