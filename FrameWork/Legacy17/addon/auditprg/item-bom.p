/* item-bom.p - generated 01/10/2001 - 13:00:03 by nosweat */

/* this program was generated by auditgn1.p and should not be modified */
/* modifications should be made to the auto generation program         */

DEFINE INPUT PARAMETER action AS CHARACTER NO-UNDO.

DEFINE VARIABLE i AS INTEGER NO-UNDO.

{methods/defines/audit.i}
FIND FIRST ttbl_header.

DEFINE WORK-TABLE ttbl_item-bom1 NO-UNDO LIKE item-bom.
DEFINE WORK-TABLE ttbl_item-bom2 NO-UNDO LIKE item-bom.

CREATE ttbl_item-bom1.
IMPORT STREAM s_audit_file ttbl_item-bom1.
CREATE ttbl_item-bom2.
IF action = "UPDATE" THEN
IMPORT STREAM s_audit_file ttbl_item-bom2.

PUT STREAM s-audit-rpt UNFORMATTED
  "Action    User ID/(User Name)            DB.Table/(Database)  Date       Time" SKIP
  "--------- ------------------------------ -------------------- ---------- --------" SKIP
  ttbl_header.v_action AT 1
  ttbl_header.v_userid AT 11
  ttbl_header.v_file AT 42
  ttbl_header.v_date FORMAT "99/99/9999" AT 63
  STRING(ttbl_header.v_time,"HH:MM:SS") AT 74.
FIND users WHERE users.user_id = v_userid NO-LOCK NO-ERROR.
IF AVAILABLE users THEN
PUT STREAM s-audit-rpt UNFORMATTED "(" AT 10 users.user_name ")".
ELSE
PUT STREAM s-audit-rpt UNFORMATTED "(user record does not exist)" AT 10.
PUT STREAM s-audit-rpt UNFORMATTED
  "(" AT 41 ttbl_header.v_db ")" SKIP(1)
  "Label: Value (database field-name)" AT 3.
IF action = "UPDATE" THEN
PUT STREAM s-audit-rpt UNFORMATTED " ( * - denotes field value change )".
PUT STREAM s-audit-rpt UNFORMATTED FILL("-",75) AT 3.
IF NOT v_field_changed OR ttbl_item-bom1.company NE ttbl_item-bom2.company OR action NE "UPDATE" THEN
DO:
  PUT STREAM s-audit-rpt UNFORMATTED "Company: " AT 3 ttbl_item-bom1.company " (company)".
  IF action = "UPDATE" THEN
  DO:
    IF ttbl_item-bom1.company NE ttbl_item-bom2.company THEN
    PUT STREAM s-audit-rpt UNFORMATTED "*" AT 1.
    PUT STREAM s-audit-rpt UNFORMATTED "Company: " AT 3 ttbl_item-bom2.company.
  END.
END.
IF NOT v_field_changed OR ttbl_item-bom1.i-no NE ttbl_item-bom2.i-no OR action NE "UPDATE" THEN
DO:
  PUT STREAM s-audit-rpt UNFORMATTED "Item Number: " AT 3 ttbl_item-bom1.i-no " (i-no)".
  IF action = "UPDATE" THEN
  DO:
    IF ttbl_item-bom1.i-no NE ttbl_item-bom2.i-no THEN
    PUT STREAM s-audit-rpt UNFORMATTED "*" AT 1.
    PUT STREAM s-audit-rpt UNFORMATTED "Item Number: " AT 3 ttbl_item-bom2.i-no.
  END.
END.
IF NOT v_field_changed OR ttbl_item-bom1.line# NE ttbl_item-bom2.line# OR action NE "UPDATE" THEN
DO:
  PUT STREAM s-audit-rpt UNFORMATTED "Line#: " AT 3 ttbl_item-bom1.line# FORMAT ">>>>>9" " (line#)".
  IF action = "UPDATE" THEN
  DO:
    IF ttbl_item-bom1.line# NE ttbl_item-bom2.line# THEN
    PUT STREAM s-audit-rpt UNFORMATTED "*" AT 1.
    PUT STREAM s-audit-rpt UNFORMATTED "Line#: " AT 3 ttbl_item-bom2.line# FORMAT ">>>>>9".
  END.
END.
IF NOT v_field_changed OR ttbl_item-bom1.parent-i NE ttbl_item-bom2.parent-i OR action NE "UPDATE" THEN
DO:
  PUT STREAM s-audit-rpt UNFORMATTED "Parent Item: " AT 3 ttbl_item-bom1.parent-i " (parent-i)".
  IF action = "UPDATE" THEN
  DO:
    IF ttbl_item-bom1.parent-i NE ttbl_item-bom2.parent-i THEN
    PUT STREAM s-audit-rpt UNFORMATTED "*" AT 1.
    PUT STREAM s-audit-rpt UNFORMATTED "Parent Item: " AT 3 ttbl_item-bom2.parent-i.
  END.
END.
IF NOT v_field_changed OR ttbl_item-bom1.shrink NE ttbl_item-bom2.shrink OR action NE "UPDATE" THEN
DO:
  PUT STREAM s-audit-rpt UNFORMATTED "Shrink %: " AT 3 ttbl_item-bom1.shrink FORMAT ">>9.9999" " (shrink)".
  IF action = "UPDATE" THEN
  DO:
    IF ttbl_item-bom1.shrink NE ttbl_item-bom2.shrink THEN
    PUT STREAM s-audit-rpt UNFORMATTED "*" AT 1.
    PUT STREAM s-audit-rpt UNFORMATTED "Shrink %: " AT 3 ttbl_item-bom2.shrink FORMAT ">>9.9999".
  END.
END.
IF v_show_index THEN
DO:
  PUT STREAM s-audit-rpt UNFORMATTED "Index Fields - Label: Value (database field-name)" AT 5.
  PUT STREAM s-audit-rpt UNFORMATTED "Company: " AT 7.
  IF action = "UPDATE" THEN
  PUT STREAM s-audit-rpt UNFORMATTED ttbl_item-bom2.company " (company)".
  ELSE
  PUT STREAM s-audit-rpt UNFORMATTED ttbl_item-bom1.company " (company)".
  PUT STREAM s-audit-rpt UNFORMATTED "Parent Item: " AT 7.
  IF action = "UPDATE" THEN
  PUT STREAM s-audit-rpt UNFORMATTED ttbl_item-bom2.parent-i " (parent-i)".
  ELSE
  PUT STREAM s-audit-rpt UNFORMATTED ttbl_item-bom1.parent-i " (parent-i)".
  PUT STREAM s-audit-rpt UNFORMATTED "Line#: " AT 7.
  IF action = "UPDATE" THEN
  PUT STREAM s-audit-rpt UNFORMATTED ttbl_item-bom2.line# FORMAT ">>>>>9" " (line#)".
  ELSE
  PUT STREAM s-audit-rpt UNFORMATTED ttbl_item-bom1.line# FORMAT ">>>>>9" " (line#)".
END.
PUT STREAM s-audit-rpt UNFORMATTED SKIP(1).
