/* shifts.p - generated 12/02/2005 - 16:52:39 by NoSweat */

/* this program was generated by auditgn1.p and should not be modified */
/* modifications should be made to the auto generation program         */

DEFINE INPUT PARAMETER action AS CHARACTER NO-UNDO.

DEFINE VARIABLE i AS INTEGER NO-UNDO.

{methods/defines/audit.i}
FIND FIRST ttbl_header.

DEFINE WORK-TABLE ttbl_shifts1 NO-UNDO LIKE shifts.
DEFINE WORK-TABLE ttbl_shifts2 NO-UNDO LIKE shifts.

CREATE ttbl_shifts1.
IMPORT STREAM s_audit_file ttbl_shifts1.
CREATE ttbl_shifts2.
IF action = "UPDATE" THEN
IMPORT STREAM s_audit_file ttbl_shifts2.

PUT STREAM s-audit-rpt UNFORMATTED
  "Action    User ID/(User Name)            DB.Table/(Database)  Date       Time" SKIP
  "--------- ------------------------------ -------------------- ---------- --------" SKIP
  ttbl_header.v_action AT 1
  ttbl_header.v_userid AT 11
  ttbl_header.v_file AT 42
  ttbl_header.v_date FORMAT "99/99/9999" AT 63
  STRING(ttbl_header.v_time,"HH:MM:SS") AT 74.
FIND users WHERE users.user_id = v_userid NO-LOCK NO-ERROR.
IF AVAILABLE users THEN
PUT STREAM s-audit-rpt UNFORMATTED "(" AT 10 users.user_name ")".
ELSE
PUT STREAM s-audit-rpt UNFORMATTED "(user record does not exist)" AT 10.
PUT STREAM s-audit-rpt UNFORMATTED
  "(" AT 41 ttbl_header.v_db ")" SKIP(1)
  "Label: Value (database field-name)" AT 3.
IF action = "UPDATE" THEN
PUT STREAM s-audit-rpt UNFORMATTED " ( * - denotes field value change )".
PUT STREAM s-audit-rpt UNFORMATTED FILL("-",75) AT 3.
IF NOT v_field_changed OR ttbl_shifts1.company NE ttbl_shifts2.company OR action NE "UPDATE" THEN
DO:
  PUT STREAM s-audit-rpt UNFORMATTED "Company: " AT 3 ttbl_shifts1.company " (company)".
  IF action = "UPDATE" THEN
  DO:
    IF ttbl_shifts1.company NE ttbl_shifts2.company THEN
    PUT STREAM s-audit-rpt UNFORMATTED "*" AT 1.
    PUT STREAM s-audit-rpt UNFORMATTED "Company: " AT 3 ttbl_shifts2.company.
  END.
END.
IF NOT v_field_changed OR ttbl_shifts1.description NE ttbl_shifts2.description OR action NE "UPDATE" THEN
DO:
  PUT STREAM s-audit-rpt UNFORMATTED "Description: " AT 3 ttbl_shifts1.description " (description)".
  IF action = "UPDATE" THEN
  DO:
    IF ttbl_shifts1.description NE ttbl_shifts2.description THEN
    PUT STREAM s-audit-rpt UNFORMATTED "*" AT 1.
    PUT STREAM s-audit-rpt UNFORMATTED "Description: " AT 3 ttbl_shifts2.description.
  END.
END.
IF NOT v_field_changed OR ttbl_shifts1.end_time NE ttbl_shifts2.end_time OR action NE "UPDATE" THEN
DO:
  PUT STREAM s-audit-rpt UNFORMATTED "End Time: " AT 3 ttbl_shifts1.end_time FORMAT "->,>>>,>>9" " (end_time)".
  IF action = "UPDATE" THEN
  DO:
    IF ttbl_shifts1.end_time NE ttbl_shifts2.end_time THEN
    PUT STREAM s-audit-rpt UNFORMATTED "*" AT 1.
    PUT STREAM s-audit-rpt UNFORMATTED "End Time: " AT 3 ttbl_shifts2.end_time FORMAT "->,>>>,>>9".
  END.
END.
IF NOT v_field_changed OR ttbl_shifts1.lunch_end NE ttbl_shifts2.lunch_end OR action NE "UPDATE" THEN
DO:
  PUT STREAM s-audit-rpt UNFORMATTED "Lunch End Time: " AT 3 ttbl_shifts1.lunch_end FORMAT "->,>>>,>>9" " (lunch_end)".
  IF action = "UPDATE" THEN
  DO:
    IF ttbl_shifts1.lunch_end NE ttbl_shifts2.lunch_end THEN
    PUT STREAM s-audit-rpt UNFORMATTED "*" AT 1.
    PUT STREAM s-audit-rpt UNFORMATTED "Lunch End Time: " AT 3 ttbl_shifts2.lunch_end FORMAT "->,>>>,>>9".
  END.
END.
IF NOT v_field_changed OR ttbl_shifts1.lunch_paid NE ttbl_shifts2.lunch_paid OR action NE "UPDATE" THEN
DO:
  PUT STREAM s-audit-rpt UNFORMATTED "Lunch Paid: " AT 3 ttbl_shifts1.lunch_paid FORMAT "yes/no" " (lunch_paid)".
  IF action = "UPDATE" THEN
  DO:
    IF ttbl_shifts1.lunch_paid NE ttbl_shifts2.lunch_paid THEN
    PUT STREAM s-audit-rpt UNFORMATTED "*" AT 1.
    PUT STREAM s-audit-rpt UNFORMATTED "Lunch Paid: " AT 3 ttbl_shifts2.lunch_paid FORMAT "yes/no".
  END.
END.
IF NOT v_field_changed OR ttbl_shifts1.lunch_start NE ttbl_shifts2.lunch_start OR action NE "UPDATE" THEN
DO:
  PUT STREAM s-audit-rpt UNFORMATTED "Lunch Start Time: " AT 3 ttbl_shifts1.lunch_start FORMAT "->,>>>,>>9" " (lunch_start)".
  IF action = "UPDATE" THEN
  DO:
    IF ttbl_shifts1.lunch_start NE ttbl_shifts2.lunch_start THEN
    PUT STREAM s-audit-rpt UNFORMATTED "*" AT 1.
    PUT STREAM s-audit-rpt UNFORMATTED "Lunch Start Time: " AT 3 ttbl_shifts2.lunch_start FORMAT "->,>>>,>>9".
  END.
END.
IF NOT v_field_changed OR ttbl_shifts1.shift NE ttbl_shifts2.shift OR action NE "UPDATE" THEN
DO:
  PUT STREAM s-audit-rpt UNFORMATTED "Shift: " AT 3 ttbl_shifts1.shift " (shift)".
  IF action = "UPDATE" THEN
  DO:
    IF ttbl_shifts1.shift NE ttbl_shifts2.shift THEN
    PUT STREAM s-audit-rpt UNFORMATTED "*" AT 1.
    PUT STREAM s-audit-rpt UNFORMATTED "Shift: " AT 3 ttbl_shifts2.shift.
  END.
END.
IF NOT v_field_changed OR ttbl_shifts1.start_time NE ttbl_shifts2.start_time OR action NE "UPDATE" THEN
DO:
  PUT STREAM s-audit-rpt UNFORMATTED "Start Time: " AT 3 ttbl_shifts1.start_time FORMAT "->,>>>,>>9" " (start_time)".
  IF action = "UPDATE" THEN
  DO:
    IF ttbl_shifts1.start_time NE ttbl_shifts2.start_time THEN
    PUT STREAM s-audit-rpt UNFORMATTED "*" AT 1.
    PUT STREAM s-audit-rpt UNFORMATTED "Start Time: " AT 3 ttbl_shifts2.start_time FORMAT "->,>>>,>>9".
  END.
END.
IF v_show_index THEN
DO:
  PUT STREAM s-audit-rpt UNFORMATTED "Index Fields - Label: Value (database field-name)" AT 5.
  PUT STREAM s-audit-rpt UNFORMATTED "Company: " AT 7.
  IF action = "UPDATE" THEN
  PUT STREAM s-audit-rpt UNFORMATTED ttbl_shifts2.company " (company)".
  ELSE
  PUT STREAM s-audit-rpt UNFORMATTED ttbl_shifts1.company " (company)".
  PUT STREAM s-audit-rpt UNFORMATTED "Shift: " AT 7.
  IF action = "UPDATE" THEN
  PUT STREAM s-audit-rpt UNFORMATTED ttbl_shifts2.shift " (shift)".
  ELSE
  PUT STREAM s-audit-rpt UNFORMATTED ttbl_shifts1.shift " (shift)".
END.
PUT STREAM s-audit-rpt UNFORMATTED SKIP(1).
