/* maillist.p - generated 12/02/2005 - 16:52:39 by NoSweat */

/* this program was generated by auditgn1.p and should not be modified */
/* modifications should be made to the auto generation program         */

DEFINE INPUT PARAMETER action AS CHARACTER NO-UNDO.

DEFINE VARIABLE i AS INTEGER NO-UNDO.

{methods/defines/audit.i}
FIND FIRST ttbl_header.

DEFINE WORK-TABLE ttbl_maillist1 NO-UNDO LIKE maillist.
DEFINE WORK-TABLE ttbl_maillist2 NO-UNDO LIKE maillist.

CREATE ttbl_maillist1.
IMPORT STREAM s_audit_file ttbl_maillist1.
CREATE ttbl_maillist2.
IF action = "UPDATE" THEN
IMPORT STREAM s_audit_file ttbl_maillist2.

PUT STREAM s-audit-rpt UNFORMATTED
  "Action    User ID/(User Name)            DB.Table/(Database)  Date       Time" SKIP
  "--------- ------------------------------ -------------------- ---------- --------" SKIP
  ttbl_header.v_action AT 1
  ttbl_header.v_userid AT 11
  ttbl_header.v_file AT 42
  ttbl_header.v_date FORMAT "99/99/9999" AT 63
  STRING(ttbl_header.v_time,"HH:MM:SS") AT 74.
FIND users WHERE users.user_id = v_userid NO-LOCK NO-ERROR.
IF AVAILABLE users THEN
PUT STREAM s-audit-rpt UNFORMATTED "(" AT 10 users.user_name ")".
ELSE
PUT STREAM s-audit-rpt UNFORMATTED "(user record does not exist)" AT 10.
PUT STREAM s-audit-rpt UNFORMATTED
  "(" AT 41 ttbl_header.v_db ")" SKIP(1)
  "Label: Value (database field-name)" AT 3.
IF action = "UPDATE" THEN
PUT STREAM s-audit-rpt UNFORMATTED " ( * - denotes field value change )".
PUT STREAM s-audit-rpt UNFORMATTED FILL("-",75) AT 3.
IF NOT v_field_changed OR ttbl_maillist1.active NE ttbl_maillist2.active OR action NE "UPDATE" THEN
DO:
  PUT STREAM s-audit-rpt UNFORMATTED "Is Active?: " AT 3 ttbl_maillist1.active FORMAT "yes/no" " (active)".
  IF action = "UPDATE" THEN
  DO:
    IF ttbl_maillist1.active NE ttbl_maillist2.active THEN
    PUT STREAM s-audit-rpt UNFORMATTED "*" AT 1.
    PUT STREAM s-audit-rpt UNFORMATTED "Is Active?: " AT 3 ttbl_maillist2.active FORMAT "yes/no".
  END.
END.
IF NOT v_field_changed OR ttbl_maillist1.list-date NE ttbl_maillist2.list-date OR action NE "UPDATE" THEN
DO:
  PUT STREAM s-audit-rpt UNFORMATTED "List Date: " AT 3 ttbl_maillist1.list-date FORMAT "99/99/9999" " (list-date)".
  IF action = "UPDATE" THEN
  DO:
    IF ttbl_maillist1.list-date NE ttbl_maillist2.list-date THEN
    PUT STREAM s-audit-rpt UNFORMATTED "*" AT 1.
    PUT STREAM s-audit-rpt UNFORMATTED "List Date: " AT 3 ttbl_maillist2.list-date FORMAT "99/99/9999".
  END.
END.
IF NOT v_field_changed OR ttbl_maillist1.list-for NE ttbl_maillist2.list-for OR action NE "UPDATE" THEN
DO:
  PUT STREAM s-audit-rpt UNFORMATTED "List For: " AT 3 ttbl_maillist1.list-for " (list-for)".
  IF action = "UPDATE" THEN
  DO:
    IF ttbl_maillist1.list-for NE ttbl_maillist2.list-for THEN
    PUT STREAM s-audit-rpt UNFORMATTED "*" AT 1.
    PUT STREAM s-audit-rpt UNFORMATTED "List For: " AT 3 ttbl_maillist2.list-for.
  END.
END.
IF NOT v_field_changed OR ttbl_maillist1.list-name NE ttbl_maillist2.list-name OR action NE "UPDATE" THEN
DO:
  PUT STREAM s-audit-rpt UNFORMATTED "List Name: " AT 3 ttbl_maillist1.list-name " (list-name)".
  IF action = "UPDATE" THEN
  DO:
    IF ttbl_maillist1.list-name NE ttbl_maillist2.list-name THEN
    PUT STREAM s-audit-rpt UNFORMATTED "*" AT 1.
    PUT STREAM s-audit-rpt UNFORMATTED "List Name: " AT 3 ttbl_maillist2.list-name.
  END.
END.
IF NOT v_field_changed OR ttbl_maillist1.list-no NE ttbl_maillist2.list-no OR action NE "UPDATE" THEN
DO:
  PUT STREAM s-audit-rpt UNFORMATTED "List#: " AT 3 ttbl_maillist1.list-no FORMAT ">>>>>9" " (list-no)".
  IF action = "UPDATE" THEN
  DO:
    IF ttbl_maillist1.list-no NE ttbl_maillist2.list-no THEN
    PUT STREAM s-audit-rpt UNFORMATTED "*" AT 1.
    PUT STREAM s-audit-rpt UNFORMATTED "List#: " AT 3 ttbl_maillist2.list-no FORMAT ">>>>>9".
  END.
END.
IF NOT v_field_changed OR ttbl_maillist1.purpose NE ttbl_maillist2.purpose OR action NE "UPDATE" THEN
DO:
  PUT STREAM s-audit-rpt UNFORMATTED "Purpose: " AT 3 ttbl_maillist1.purpose " (purpose)".
  IF action = "UPDATE" THEN
  DO:
    IF ttbl_maillist1.purpose NE ttbl_maillist2.purpose THEN
    PUT STREAM s-audit-rpt UNFORMATTED "*" AT 1.
    PUT STREAM s-audit-rpt UNFORMATTED "Purpose: " AT 3 ttbl_maillist2.purpose.
  END.
END.
IF NOT v_field_changed OR ttbl_maillist1.sman NE ttbl_maillist2.sman OR action NE "UPDATE" THEN
DO:
  PUT STREAM s-audit-rpt UNFORMATTED "Salesman: " AT 3 ttbl_maillist1.sman " (sman)".
  IF action = "UPDATE" THEN
  DO:
    IF ttbl_maillist1.sman NE ttbl_maillist2.sman THEN
    PUT STREAM s-audit-rpt UNFORMATTED "*" AT 1.
    PUT STREAM s-audit-rpt UNFORMATTED "Salesman: " AT 3 ttbl_maillist2.sman.
  END.
END.
IF v_show_index THEN
DO:
  PUT STREAM s-audit-rpt UNFORMATTED "Index Fields - Label: Value (database field-name)" AT 5.
  PUT STREAM s-audit-rpt UNFORMATTED "List#: " AT 7.
  IF action = "UPDATE" THEN
  PUT STREAM s-audit-rpt UNFORMATTED ttbl_maillist2.list-no FORMAT ">>>>>9" " (list-no)".
  ELSE
  PUT STREAM s-audit-rpt UNFORMATTED ttbl_maillist1.list-no FORMAT ">>>>>9" " (list-no)".
END.
PUT STREAM s-audit-rpt UNFORMATTED SKIP(1).
