/* machtran.p - generated 12/02/2005 - 16:52:38 by NoSweat */

/* this program was generated by auditgn1.p and should not be modified */
/* modifications should be made to the auto generation program         */

DEFINE INPUT PARAMETER action AS CHARACTER NO-UNDO.

DEFINE VARIABLE i AS INTEGER NO-UNDO.

{methods/defines/audit.i}
FIND FIRST ttbl_header.

DEFINE WORK-TABLE ttbl_machtran1 NO-UNDO LIKE machtran.
DEFINE WORK-TABLE ttbl_machtran2 NO-UNDO LIKE machtran.

CREATE ttbl_machtran1.
IMPORT STREAM s_audit_file ttbl_machtran1.
CREATE ttbl_machtran2.
IF action = "UPDATE" THEN
IMPORT STREAM s_audit_file ttbl_machtran2.

PUT STREAM s-audit-rpt UNFORMATTED
  "Action    User ID/(User Name)            DB.Table/(Database)  Date       Time" SKIP
  "--------- ------------------------------ -------------------- ---------- --------" SKIP
  ttbl_header.v_action AT 1
  ttbl_header.v_userid AT 11
  ttbl_header.v_file AT 42
  ttbl_header.v_date FORMAT "99/99/9999" AT 63
  STRING(ttbl_header.v_time,"HH:MM:SS") AT 74.
FIND users WHERE users.user_id = v_userid NO-LOCK NO-ERROR.
IF AVAILABLE users THEN
PUT STREAM s-audit-rpt UNFORMATTED "(" AT 10 users.user_name ")".
ELSE
PUT STREAM s-audit-rpt UNFORMATTED "(user record does not exist)" AT 10.
PUT STREAM s-audit-rpt UNFORMATTED
  "(" AT 41 ttbl_header.v_db ")" SKIP(1)
  "Label: Value (database field-name)" AT 3.
IF action = "UPDATE" THEN
PUT STREAM s-audit-rpt UNFORMATTED " ( * - denotes field value change )".
PUT STREAM s-audit-rpt UNFORMATTED FILL("-",75) AT 3.
IF NOT v_field_changed OR ttbl_machtran1.blank_number NE ttbl_machtran2.blank_number OR action NE "UPDATE" THEN
DO:
  PUT STREAM s-audit-rpt UNFORMATTED "Blank Number: " AT 3 ttbl_machtran1.blank_number FORMAT ">9" " (blank_number)".
  IF action = "UPDATE" THEN
  DO:
    IF ttbl_machtran1.blank_number NE ttbl_machtran2.blank_number THEN
    PUT STREAM s-audit-rpt UNFORMATTED "*" AT 1.
    PUT STREAM s-audit-rpt UNFORMATTED "Blank Number: " AT 3 ttbl_machtran2.blank_number FORMAT ">9".
  END.
END.
IF NOT v_field_changed OR ttbl_machtran1.charge_code NE ttbl_machtran2.charge_code OR action NE "UPDATE" THEN
DO:
  PUT STREAM s-audit-rpt UNFORMATTED "Charge Code: " AT 3 ttbl_machtran1.charge_code " (charge_code)".
  IF action = "UPDATE" THEN
  DO:
    IF ttbl_machtran1.charge_code NE ttbl_machtran2.charge_code THEN
    PUT STREAM s-audit-rpt UNFORMATTED "*" AT 1.
    PUT STREAM s-audit-rpt UNFORMATTED "Charge Code: " AT 3 ttbl_machtran2.charge_code.
  END.
END.
IF NOT v_field_changed OR ttbl_machtran1.company NE ttbl_machtran2.company OR action NE "UPDATE" THEN
DO:
  PUT STREAM s-audit-rpt UNFORMATTED "Company: " AT 3 ttbl_machtran1.company " (company)".
  IF action = "UPDATE" THEN
  DO:
    IF ttbl_machtran1.company NE ttbl_machtran2.company THEN
    PUT STREAM s-audit-rpt UNFORMATTED "*" AT 1.
    PUT STREAM s-audit-rpt UNFORMATTED "Company: " AT 3 ttbl_machtran2.company.
  END.
END.
IF NOT v_field_changed OR ttbl_machtran1.completed NE ttbl_machtran2.completed OR action NE "UPDATE" THEN
DO:
  PUT STREAM s-audit-rpt UNFORMATTED "Completed?: " AT 3 ttbl_machtran1.completed FORMAT "yes/no" " (completed)".
  IF action = "UPDATE" THEN
  DO:
    IF ttbl_machtran1.completed NE ttbl_machtran2.completed THEN
    PUT STREAM s-audit-rpt UNFORMATTED "*" AT 1.
    PUT STREAM s-audit-rpt UNFORMATTED "Completed?: " AT 3 ttbl_machtran2.completed FORMAT "yes/no".
  END.
END.
IF NOT v_field_changed OR ttbl_machtran1.end_date NE ttbl_machtran2.end_date OR action NE "UPDATE" THEN
DO:
  PUT STREAM s-audit-rpt UNFORMATTED "End Date: " AT 3 ttbl_machtran1.end_date FORMAT "99/99/9999" " (end_date)".
  IF action = "UPDATE" THEN
  DO:
    IF ttbl_machtran1.end_date NE ttbl_machtran2.end_date THEN
    PUT STREAM s-audit-rpt UNFORMATTED "*" AT 1.
    PUT STREAM s-audit-rpt UNFORMATTED "End Date: " AT 3 ttbl_machtran2.end_date FORMAT "99/99/9999".
  END.
END.
IF NOT v_field_changed OR ttbl_machtran1.end_time NE ttbl_machtran2.end_time OR action NE "UPDATE" THEN
DO:
  PUT STREAM s-audit-rpt UNFORMATTED "End Time: " AT 3 ttbl_machtran1.end_time FORMAT "->,>>>,>>9" " (end_time)".
  IF action = "UPDATE" THEN
  DO:
    IF ttbl_machtran1.end_time NE ttbl_machtran2.end_time THEN
    PUT STREAM s-audit-rpt UNFORMATTED "*" AT 1.
    PUT STREAM s-audit-rpt UNFORMATTED "End Time: " AT 3 ttbl_machtran2.end_time FORMAT "->,>>>,>>9".
  END.
END.
IF NOT v_field_changed OR ttbl_machtran1.form_number NE ttbl_machtran2.form_number OR action NE "UPDATE" THEN
DO:
  PUT STREAM s-audit-rpt UNFORMATTED "Form Number: " AT 3 ttbl_machtran1.form_number FORMAT ">>9" " (form_number)".
  IF action = "UPDATE" THEN
  DO:
    IF ttbl_machtran1.form_number NE ttbl_machtran2.form_number THEN
    PUT STREAM s-audit-rpt UNFORMATTED "*" AT 1.
    PUT STREAM s-audit-rpt UNFORMATTED "Form Number: " AT 3 ttbl_machtran2.form_number FORMAT ">>9".
  END.
END.
IF NOT v_field_changed OR ttbl_machtran1.jobseq NE ttbl_machtran2.jobseq OR action NE "UPDATE" THEN
DO:
  PUT STREAM s-audit-rpt UNFORMATTED "Job Sequence: " AT 3 ttbl_machtran1.jobseq FORMAT ">9" " (jobseq)".
  IF action = "UPDATE" THEN
  DO:
    IF ttbl_machtran1.jobseq NE ttbl_machtran2.jobseq THEN
    PUT STREAM s-audit-rpt UNFORMATTED "*" AT 1.
    PUT STREAM s-audit-rpt UNFORMATTED "Job Sequence: " AT 3 ttbl_machtran2.jobseq FORMAT ">9".
  END.
END.
IF NOT v_field_changed OR ttbl_machtran1.job_number NE ttbl_machtran2.job_number OR action NE "UPDATE" THEN
DO:
  PUT STREAM s-audit-rpt UNFORMATTED "Job Number: " AT 3 ttbl_machtran1.job_number " (job_number)".
  IF action = "UPDATE" THEN
  DO:
    IF ttbl_machtran1.job_number NE ttbl_machtran2.job_number THEN
    PUT STREAM s-audit-rpt UNFORMATTED "*" AT 1.
    PUT STREAM s-audit-rpt UNFORMATTED "Job Number: " AT 3 ttbl_machtran2.job_number.
  END.
END.
IF NOT v_field_changed OR ttbl_machtran1.job_sub NE ttbl_machtran2.job_sub OR action NE "UPDATE" THEN
DO:
  PUT STREAM s-audit-rpt UNFORMATTED "Job Sub Number: " AT 3 ttbl_machtran1.job_sub FORMAT ">9" " (job_sub)".
  IF action = "UPDATE" THEN
  DO:
    IF ttbl_machtran1.job_sub NE ttbl_machtran2.job_sub THEN
    PUT STREAM s-audit-rpt UNFORMATTED "*" AT 1.
    PUT STREAM s-audit-rpt UNFORMATTED "Job Sub Number: " AT 3 ttbl_machtran2.job_sub FORMAT ">9".
  END.
END.
IF NOT v_field_changed OR ttbl_machtran1.login_time NE ttbl_machtran2.login_time OR action NE "UPDATE" THEN
DO:
  PUT STREAM s-audit-rpt UNFORMATTED "Login Time: " AT 3 ttbl_machtran1.login_time FORMAT "->,>>>,>>9" " (login_time)".
  IF action = "UPDATE" THEN
  DO:
    IF ttbl_machtran1.login_time NE ttbl_machtran2.login_time THEN
    PUT STREAM s-audit-rpt UNFORMATTED "*" AT 1.
    PUT STREAM s-audit-rpt UNFORMATTED "Login Time: " AT 3 ttbl_machtran2.login_time FORMAT "->,>>>,>>9".
  END.
END.
IF NOT v_field_changed OR ttbl_machtran1.logout_time NE ttbl_machtran2.logout_time OR action NE "UPDATE" THEN
DO:
  PUT STREAM s-audit-rpt UNFORMATTED "Logout Time: " AT 3 ttbl_machtran1.logout_time FORMAT "->,>>>,>>9" " (logout_time)".
  IF action = "UPDATE" THEN
  DO:
    IF ttbl_machtran1.logout_time NE ttbl_machtran2.logout_time THEN
    PUT STREAM s-audit-rpt UNFORMATTED "*" AT 1.
    PUT STREAM s-audit-rpt UNFORMATTED "Logout Time: " AT 3 ttbl_machtran2.logout_time FORMAT "->,>>>,>>9".
  END.
END.
IF NOT v_field_changed OR ttbl_machtran1.machine NE ttbl_machtran2.machine OR action NE "UPDATE" THEN
DO:
  PUT STREAM s-audit-rpt UNFORMATTED "Machine: " AT 3 ttbl_machtran1.machine " (machine)".
  IF action = "UPDATE" THEN
  DO:
    IF ttbl_machtran1.machine NE ttbl_machtran2.machine THEN
    PUT STREAM s-audit-rpt UNFORMATTED "*" AT 1.
    PUT STREAM s-audit-rpt UNFORMATTED "Machine: " AT 3 ttbl_machtran2.machine.
  END.
END.
IF NOT v_field_changed OR ttbl_machtran1.pass_sequence NE ttbl_machtran2.pass_sequence OR action NE "UPDATE" THEN
DO:
  PUT STREAM s-audit-rpt UNFORMATTED "Pass Sequence: " AT 3 ttbl_machtran1.pass_sequence FORMAT ">>9" " (pass_sequence)".
  IF action = "UPDATE" THEN
  DO:
    IF ttbl_machtran1.pass_sequence NE ttbl_machtran2.pass_sequence THEN
    PUT STREAM s-audit-rpt UNFORMATTED "*" AT 1.
    PUT STREAM s-audit-rpt UNFORMATTED "Pass Sequence: " AT 3 ttbl_machtran2.pass_sequence FORMAT ">>9".
  END.
END.
IF NOT v_field_changed OR ttbl_machtran1.posted NE ttbl_machtran2.posted OR action NE "UPDATE" THEN
DO:
  PUT STREAM s-audit-rpt UNFORMATTED "Posted: " AT 3 ttbl_machtran1.posted FORMAT "yes/no" " (posted)".
  IF action = "UPDATE" THEN
  DO:
    IF ttbl_machtran1.posted NE ttbl_machtran2.posted THEN
    PUT STREAM s-audit-rpt UNFORMATTED "*" AT 1.
    PUT STREAM s-audit-rpt UNFORMATTED "Posted: " AT 3 ttbl_machtran2.posted FORMAT "yes/no".
  END.
END.
IF NOT v_field_changed OR ttbl_machtran1.run_qty NE ttbl_machtran2.run_qty OR action NE "UPDATE" THEN
DO:
  PUT STREAM s-audit-rpt UNFORMATTED "Run Quantity: " AT 3 ttbl_machtran1.run_qty FORMAT "->>,>>>,>>9" " (run_qty)".
  IF action = "UPDATE" THEN
  DO:
    IF ttbl_machtran1.run_qty NE ttbl_machtran2.run_qty THEN
    PUT STREAM s-audit-rpt UNFORMATTED "*" AT 1.
    PUT STREAM s-audit-rpt UNFORMATTED "Run Quantity: " AT 3 ttbl_machtran2.run_qty FORMAT "->>,>>>,>>9".
  END.
END.
IF NOT v_field_changed OR ttbl_machtran1.shift NE ttbl_machtran2.shift OR action NE "UPDATE" THEN
DO:
  PUT STREAM s-audit-rpt UNFORMATTED "Shift: " AT 3 ttbl_machtran1.shift " (shift)".
  IF action = "UPDATE" THEN
  DO:
    IF ttbl_machtran1.shift NE ttbl_machtran2.shift THEN
    PUT STREAM s-audit-rpt UNFORMATTED "*" AT 1.
    PUT STREAM s-audit-rpt UNFORMATTED "Shift: " AT 3 ttbl_machtran2.shift.
  END.
END.
IF NOT v_field_changed OR ttbl_machtran1.start_date NE ttbl_machtran2.start_date OR action NE "UPDATE" THEN
DO:
  PUT STREAM s-audit-rpt UNFORMATTED "Start Date: " AT 3 ttbl_machtran1.start_date FORMAT "99/99/9999" " (start_date)".
  IF action = "UPDATE" THEN
  DO:
    IF ttbl_machtran1.start_date NE ttbl_machtran2.start_date THEN
    PUT STREAM s-audit-rpt UNFORMATTED "*" AT 1.
    PUT STREAM s-audit-rpt UNFORMATTED "Start Date: " AT 3 ttbl_machtran2.start_date FORMAT "99/99/9999".
  END.
END.
IF NOT v_field_changed OR ttbl_machtran1.start_time NE ttbl_machtran2.start_time OR action NE "UPDATE" THEN
DO:
  PUT STREAM s-audit-rpt UNFORMATTED "Start Time: " AT 3 ttbl_machtran1.start_time FORMAT "->,>>>,>>9" " (start_time)".
  IF action = "UPDATE" THEN
  DO:
    IF ttbl_machtran1.start_time NE ttbl_machtran2.start_time THEN
    PUT STREAM s-audit-rpt UNFORMATTED "*" AT 1.
    PUT STREAM s-audit-rpt UNFORMATTED "Start Time: " AT 3 ttbl_machtran2.start_time FORMAT "->,>>>,>>9".
  END.
END.
IF NOT v_field_changed OR ttbl_machtran1.total_time NE ttbl_machtran2.total_time OR action NE "UPDATE" THEN
DO:
  PUT STREAM s-audit-rpt UNFORMATTED "Total Time: " AT 3 ttbl_machtran1.total_time FORMAT "->,>>>,>>9" " (total_time)".
  IF action = "UPDATE" THEN
  DO:
    IF ttbl_machtran1.total_time NE ttbl_machtran2.total_time THEN
    PUT STREAM s-audit-rpt UNFORMATTED "*" AT 1.
    PUT STREAM s-audit-rpt UNFORMATTED "Total Time: " AT 3 ttbl_machtran2.total_time FORMAT "->,>>>,>>9".
  END.
END.
IF NOT v_field_changed OR ttbl_machtran1.waste_qty NE ttbl_machtran2.waste_qty OR action NE "UPDATE" THEN
DO:
  PUT STREAM s-audit-rpt UNFORMATTED "Waste Quantity: " AT 3 ttbl_machtran1.waste_qty FORMAT "->>>>9" " (waste_qty)".
  IF action = "UPDATE" THEN
  DO:
    IF ttbl_machtran1.waste_qty NE ttbl_machtran2.waste_qty THEN
    PUT STREAM s-audit-rpt UNFORMATTED "*" AT 1.
    PUT STREAM s-audit-rpt UNFORMATTED "Waste Quantity: " AT 3 ttbl_machtran2.waste_qty FORMAT "->>>>9".
  END.
END.
IF v_show_index THEN
DO:
  PUT STREAM s-audit-rpt UNFORMATTED "Index Fields - Label: Value (database field-name)" AT 5.
  PUT STREAM s-audit-rpt UNFORMATTED "Company: " AT 7.
  IF action = "UPDATE" THEN
  PUT STREAM s-audit-rpt UNFORMATTED ttbl_machtran2.company " (company)".
  ELSE
  PUT STREAM s-audit-rpt UNFORMATTED ttbl_machtran1.company " (company)".
  PUT STREAM s-audit-rpt UNFORMATTED "Machine: " AT 7.
  IF action = "UPDATE" THEN
  PUT STREAM s-audit-rpt UNFORMATTED ttbl_machtran2.machine " (machine)".
  ELSE
  PUT STREAM s-audit-rpt UNFORMATTED ttbl_machtran1.machine " (machine)".
  PUT STREAM s-audit-rpt UNFORMATTED "Job Number: " AT 7.
  IF action = "UPDATE" THEN
  PUT STREAM s-audit-rpt UNFORMATTED ttbl_machtran2.job_number " (job_number)".
  ELSE
  PUT STREAM s-audit-rpt UNFORMATTED ttbl_machtran1.job_number " (job_number)".
  PUT STREAM s-audit-rpt UNFORMATTED "Job Sub Number: " AT 7.
  IF action = "UPDATE" THEN
  PUT STREAM s-audit-rpt UNFORMATTED ttbl_machtran2.job_sub FORMAT ">9" " (job_sub)".
  ELSE
  PUT STREAM s-audit-rpt UNFORMATTED ttbl_machtran1.job_sub FORMAT ">9" " (job_sub)".
  PUT STREAM s-audit-rpt UNFORMATTED "Form Number: " AT 7.
  IF action = "UPDATE" THEN
  PUT STREAM s-audit-rpt UNFORMATTED ttbl_machtran2.form_number FORMAT ">>9" " (form_number)".
  ELSE
  PUT STREAM s-audit-rpt UNFORMATTED ttbl_machtran1.form_number FORMAT ">>9" " (form_number)".
  PUT STREAM s-audit-rpt UNFORMATTED "Blank Number: " AT 7.
  IF action = "UPDATE" THEN
  PUT STREAM s-audit-rpt UNFORMATTED ttbl_machtran2.blank_number FORMAT ">9" " (blank_number)".
  ELSE
  PUT STREAM s-audit-rpt UNFORMATTED ttbl_machtran1.blank_number FORMAT ">9" " (blank_number)".
  PUT STREAM s-audit-rpt UNFORMATTED "Pass Sequence: " AT 7.
  IF action = "UPDATE" THEN
  PUT STREAM s-audit-rpt UNFORMATTED ttbl_machtran2.pass_sequence FORMAT ">>9" " (pass_sequence)".
  ELSE
  PUT STREAM s-audit-rpt UNFORMATTED ttbl_machtran1.pass_sequence FORMAT ">>9" " (pass_sequence)".
  PUT STREAM s-audit-rpt UNFORMATTED "Start Date: " AT 7.
  IF action = "UPDATE" THEN
  PUT STREAM s-audit-rpt UNFORMATTED ttbl_machtran2.start_date FORMAT "99/99/9999" " (start_date)".
  ELSE
  PUT STREAM s-audit-rpt UNFORMATTED ttbl_machtran1.start_date FORMAT "99/99/9999" " (start_date)".
  PUT STREAM s-audit-rpt UNFORMATTED "Start Time: " AT 7.
  IF action = "UPDATE" THEN
  PUT STREAM s-audit-rpt UNFORMATTED ttbl_machtran2.start_time FORMAT "->,>>>,>>9" " (start_time)".
  ELSE
  PUT STREAM s-audit-rpt UNFORMATTED ttbl_machtran1.start_time FORMAT "->,>>>,>>9" " (start_time)".
  PUT STREAM s-audit-rpt UNFORMATTED "Job Sequence: " AT 7.
  IF action = "UPDATE" THEN
  PUT STREAM s-audit-rpt UNFORMATTED ttbl_machtran2.jobseq FORMAT ">9" " (jobseq)".
  ELSE
  PUT STREAM s-audit-rpt UNFORMATTED ttbl_machtran1.jobseq FORMAT ">9" " (jobseq)".
  PUT STREAM s-audit-rpt UNFORMATTED "Charge Code: " AT 7.
  IF action = "UPDATE" THEN
  PUT STREAM s-audit-rpt UNFORMATTED ttbl_machtran2.charge_code " (charge_code)".
  ELSE
  PUT STREAM s-audit-rpt UNFORMATTED ttbl_machtran1.charge_code " (charge_code)".
END.
PUT STREAM s-audit-rpt UNFORMATTED SKIP(1).
