/* ar-mcash.p - generated 01/10/2001 - 12:59:58 by nosweat */

/* this program was generated by auditgn1.p and should not be modified */
/* modifications should be made to the auto generation program         */

DEFINE INPUT PARAMETER action AS CHARACTER NO-UNDO.

DEFINE VARIABLE i AS INTEGER NO-UNDO.

{methods/defines/audit.i}
FIND FIRST ttbl_header.

DEFINE WORK-TABLE ttbl_ar-mcash1 NO-UNDO LIKE ar-mcash.
DEFINE WORK-TABLE ttbl_ar-mcash2 NO-UNDO LIKE ar-mcash.

CREATE ttbl_ar-mcash1.
IMPORT STREAM s_audit_file ttbl_ar-mcash1.
CREATE ttbl_ar-mcash2.
IF action = "UPDATE" THEN
IMPORT STREAM s_audit_file ttbl_ar-mcash2.

PUT STREAM s-audit-rpt UNFORMATTED
  "Action    User ID/(User Name)            DB.Table/(Database)  Date       Time" SKIP
  "--------- ------------------------------ -------------------- ---------- --------" SKIP
  ttbl_header.v_action AT 1
  ttbl_header.v_userid AT 11
  ttbl_header.v_file AT 42
  ttbl_header.v_date FORMAT "99/99/9999" AT 63
  STRING(ttbl_header.v_time,"HH:MM:SS") AT 74.
FIND users WHERE users.user_id = v_userid NO-LOCK NO-ERROR.
IF AVAILABLE users THEN
PUT STREAM s-audit-rpt UNFORMATTED "(" AT 10 users.user_name ")".
ELSE
PUT STREAM s-audit-rpt UNFORMATTED "(user record does not exist)" AT 10.
PUT STREAM s-audit-rpt UNFORMATTED
  "(" AT 41 ttbl_header.v_db ")" SKIP(1)
  "Label: Value (database field-name)" AT 3.
IF action = "UPDATE" THEN
PUT STREAM s-audit-rpt UNFORMATTED " ( * - denotes field value change )".
PUT STREAM s-audit-rpt UNFORMATTED FILL("-",75) AT 3.
IF NOT v_field_changed OR ttbl_ar-mcash1.actnum NE ttbl_ar-mcash2.actnum OR action NE "UPDATE" THEN
DO:
  PUT STREAM s-audit-rpt UNFORMATTED "Account No: " AT 3 ttbl_ar-mcash1.actnum " (actnum)".
  IF action = "UPDATE" THEN
  DO:
    IF ttbl_ar-mcash1.actnum NE ttbl_ar-mcash2.actnum THEN
    PUT STREAM s-audit-rpt UNFORMATTED "*" AT 1.
    PUT STREAM s-audit-rpt UNFORMATTED "Account No: " AT 3 ttbl_ar-mcash2.actnum.
  END.
END.
IF NOT v_field_changed OR ttbl_ar-mcash1.bank-code NE ttbl_ar-mcash2.bank-code OR action NE "UPDATE" THEN
DO:
  PUT STREAM s-audit-rpt UNFORMATTED "?: " AT 3 ttbl_ar-mcash1.bank-code " (bank-code)".
  IF action = "UPDATE" THEN
  DO:
    IF ttbl_ar-mcash1.bank-code NE ttbl_ar-mcash2.bank-code THEN
    PUT STREAM s-audit-rpt UNFORMATTED "*" AT 1.
    PUT STREAM s-audit-rpt UNFORMATTED "?: " AT 3 ttbl_ar-mcash2.bank-code.
  END.
END.
IF NOT v_field_changed OR ttbl_ar-mcash1.check-amt NE ttbl_ar-mcash2.check-amt OR action NE "UPDATE" THEN
DO:
  PUT STREAM s-audit-rpt UNFORMATTED "Amount: " AT 3 ttbl_ar-mcash1.check-amt FORMAT "->>,>>>,>>9.99" " (check-amt)".
  IF action = "UPDATE" THEN
  DO:
    IF ttbl_ar-mcash1.check-amt NE ttbl_ar-mcash2.check-amt THEN
    PUT STREAM s-audit-rpt UNFORMATTED "*" AT 1.
    PUT STREAM s-audit-rpt UNFORMATTED "Amount: " AT 3 ttbl_ar-mcash2.check-amt FORMAT "->>,>>>,>>9.99".
  END.
END.
IF NOT v_field_changed OR ttbl_ar-mcash1.check-date NE ttbl_ar-mcash2.check-date OR action NE "UPDATE" THEN
DO:
  PUT STREAM s-audit-rpt UNFORMATTED "Date: " AT 3 ttbl_ar-mcash1.check-date FORMAT "99/99/99" " (check-date)".
  IF action = "UPDATE" THEN
  DO:
    IF ttbl_ar-mcash1.check-date NE ttbl_ar-mcash2.check-date THEN
    PUT STREAM s-audit-rpt UNFORMATTED "*" AT 1.
    PUT STREAM s-audit-rpt UNFORMATTED "Date: " AT 3 ttbl_ar-mcash2.check-date FORMAT "99/99/99".
  END.
END.
IF NOT v_field_changed OR ttbl_ar-mcash1.company NE ttbl_ar-mcash2.company OR action NE "UPDATE" THEN
DO:
  PUT STREAM s-audit-rpt UNFORMATTED "Company: " AT 3 ttbl_ar-mcash1.company " (company)".
  IF action = "UPDATE" THEN
  DO:
    IF ttbl_ar-mcash1.company NE ttbl_ar-mcash2.company THEN
    PUT STREAM s-audit-rpt UNFORMATTED "*" AT 1.
    PUT STREAM s-audit-rpt UNFORMATTED "Company: " AT 3 ttbl_ar-mcash2.company.
  END.
END.
IF NOT v_field_changed OR ttbl_ar-mcash1.dscr NE ttbl_ar-mcash2.dscr OR action NE "UPDATE" THEN
DO:
  PUT STREAM s-audit-rpt UNFORMATTED "Desc: " AT 3 ttbl_ar-mcash1.dscr " (dscr)".
  IF action = "UPDATE" THEN
  DO:
    IF ttbl_ar-mcash1.dscr NE ttbl_ar-mcash2.dscr THEN
    PUT STREAM s-audit-rpt UNFORMATTED "*" AT 1.
    PUT STREAM s-audit-rpt UNFORMATTED "Desc: " AT 3 ttbl_ar-mcash2.dscr.
  END.
END.
IF NOT v_field_changed OR ttbl_ar-mcash1.m-no NE ttbl_ar-mcash2.m-no OR action NE "UPDATE" THEN
DO:
  PUT STREAM s-audit-rpt UNFORMATTED "Internal#: " AT 3 ttbl_ar-mcash1.m-no FORMAT ">>>>>>9" " (m-no)".
  IF action = "UPDATE" THEN
  DO:
    IF ttbl_ar-mcash1.m-no NE ttbl_ar-mcash2.m-no THEN
    PUT STREAM s-audit-rpt UNFORMATTED "*" AT 1.
    PUT STREAM s-audit-rpt UNFORMATTED "Internal#: " AT 3 ttbl_ar-mcash2.m-no FORMAT ">>>>>>9".
  END.
END.
IF NOT v_field_changed OR ttbl_ar-mcash1.payer NE ttbl_ar-mcash2.payer OR action NE "UPDATE" THEN
DO:
  PUT STREAM s-audit-rpt UNFORMATTED "Payer: " AT 3 ttbl_ar-mcash1.payer " (payer)".
  IF action = "UPDATE" THEN
  DO:
    IF ttbl_ar-mcash1.payer NE ttbl_ar-mcash2.payer THEN
    PUT STREAM s-audit-rpt UNFORMATTED "*" AT 1.
    PUT STREAM s-audit-rpt UNFORMATTED "Payer: " AT 3 ttbl_ar-mcash2.payer.
  END.
END.
IF NOT v_field_changed OR ttbl_ar-mcash1.posted NE ttbl_ar-mcash2.posted OR action NE "UPDATE" THEN
DO:
  PUT STREAM s-audit-rpt UNFORMATTED "Posted: " AT 3 ttbl_ar-mcash1.posted FORMAT "y/n" " (posted)".
  IF action = "UPDATE" THEN
  DO:
    IF ttbl_ar-mcash1.posted NE ttbl_ar-mcash2.posted THEN
    PUT STREAM s-audit-rpt UNFORMATTED "*" AT 1.
    PUT STREAM s-audit-rpt UNFORMATTED "Posted: " AT 3 ttbl_ar-mcash2.posted FORMAT "y/n".
  END.
END.
IF v_show_index THEN
DO:
  PUT STREAM s-audit-rpt UNFORMATTED "Index Fields - Label: Value (database field-name)" AT 5.
  PUT STREAM s-audit-rpt UNFORMATTED "Company: " AT 7.
  IF action = "UPDATE" THEN
  PUT STREAM s-audit-rpt UNFORMATTED ttbl_ar-mcash2.company " (company)".
  ELSE
  PUT STREAM s-audit-rpt UNFORMATTED ttbl_ar-mcash1.company " (company)".
  PUT STREAM s-audit-rpt UNFORMATTED "Date: " AT 7.
  IF action = "UPDATE" THEN
  PUT STREAM s-audit-rpt UNFORMATTED ttbl_ar-mcash2.check-date FORMAT "99/99/99" " (check-date)".
  ELSE
  PUT STREAM s-audit-rpt UNFORMATTED ttbl_ar-mcash1.check-date FORMAT "99/99/99" " (check-date)".
  PUT STREAM s-audit-rpt UNFORMATTED "Internal#: " AT 7.
  IF action = "UPDATE" THEN
  PUT STREAM s-audit-rpt UNFORMATTED ttbl_ar-mcash2.m-no FORMAT ">>>>>>9" " (m-no)".
  ELSE
  PUT STREAM s-audit-rpt UNFORMATTED ttbl_ar-mcash1.m-no FORMAT ">>>>>>9" " (m-no)".
END.
PUT STREAM s-audit-rpt UNFORMATTED SKIP(1).
