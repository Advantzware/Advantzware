/* EDCode.p - generated 01/10/2001 - 13:00:00 by nosweat */

/* this program was generated by auditgn1.p and should not be modified */
/* modifications should be made to the auto generation program         */

DEFINE INPUT PARAMETER action AS CHARACTER NO-UNDO.

DEFINE VARIABLE i AS INTEGER NO-UNDO.

{methods/defines/audit.i}
FIND FIRST ttbl_header.

DEFINE WORK-TABLE ttbl_EDCode1 NO-UNDO LIKE EDCode.
DEFINE WORK-TABLE ttbl_EDCode2 NO-UNDO LIKE EDCode.

CREATE ttbl_EDCode1.
IMPORT STREAM s_audit_file ttbl_EDCode1.
CREATE ttbl_EDCode2.
IF action = "UPDATE" THEN
IMPORT STREAM s_audit_file ttbl_EDCode2.

PUT STREAM s-audit-rpt UNFORMATTED
  "Action    User ID/(User Name)            DB.Table/(Database)  Date       Time" SKIP
  "--------- ------------------------------ -------------------- ---------- --------" SKIP
  ttbl_header.v_action AT 1
  ttbl_header.v_userid AT 11
  ttbl_header.v_file AT 42
  ttbl_header.v_date FORMAT "99/99/9999" AT 63
  STRING(ttbl_header.v_time,"HH:MM:SS") AT 74.
FIND users WHERE users.user_id = v_userid NO-LOCK NO-ERROR.
IF AVAILABLE users THEN
PUT STREAM s-audit-rpt UNFORMATTED "(" AT 10 users.user_name ")".
ELSE
PUT STREAM s-audit-rpt UNFORMATTED "(user record does not exist)" AT 10.
PUT STREAM s-audit-rpt UNFORMATTED
  "(" AT 41 ttbl_header.v_db ")" SKIP(1)
  "Label: Value (database field-name)" AT 3.
IF action = "UPDATE" THEN
PUT STREAM s-audit-rpt UNFORMATTED " ( * - denotes field value change )".
PUT STREAM s-audit-rpt UNFORMATTED FILL("-",75) AT 3.
IF NOT v_field_changed OR ttbl_EDCode1.Agency NE ttbl_EDCode2.Agency OR action NE "UPDATE" THEN
DO:
  PUT STREAM s-audit-rpt UNFORMATTED "Agncy: " AT 3 ttbl_EDCode1.Agency " (Agency)".
  IF action = "UPDATE" THEN
  DO:
    IF ttbl_EDCode1.Agency NE ttbl_EDCode2.Agency THEN
    PUT STREAM s-audit-rpt UNFORMATTED "*" AT 1.
    PUT STREAM s-audit-rpt UNFORMATTED "Agncy: " AT 3 ttbl_EDCode2.Agency.
  END.
END.
IF NOT v_field_changed OR ttbl_EDCode1.Custom-proc NE ttbl_EDCode2.Custom-proc OR action NE "UPDATE" THEN
DO:
  PUT STREAM s-audit-rpt UNFORMATTED "Proc: " AT 3 ttbl_EDCode1.Custom-proc " (Custom-proc)".
  IF action = "UPDATE" THEN
  DO:
    IF ttbl_EDCode1.Custom-proc NE ttbl_EDCode2.Custom-proc THEN
    PUT STREAM s-audit-rpt UNFORMATTED "*" AT 1.
    PUT STREAM s-audit-rpt UNFORMATTED "Proc: " AT 3 ttbl_EDCode2.Custom-proc.
  END.
END.
IF NOT v_field_changed OR ttbl_EDCode1.Customized NE ttbl_EDCode2.Customized OR action NE "UPDATE" THEN
DO:
  PUT STREAM s-audit-rpt UNFORMATTED "Custom?: " AT 3 ttbl_EDCode1.Customized FORMAT "yes/no" " (Customized)".
  IF action = "UPDATE" THEN
  DO:
    IF ttbl_EDCode1.Customized NE ttbl_EDCode2.Customized THEN
    PUT STREAM s-audit-rpt UNFORMATTED "*" AT 1.
    PUT STREAM s-audit-rpt UNFORMATTED "Custom?: " AT 3 ttbl_EDCode2.Customized FORMAT "yes/no".
  END.
END.
IF NOT v_field_changed OR ttbl_EDCode1.Direction NE ttbl_EDCode2.Direction OR action NE "UPDATE" THEN
DO:
  PUT STREAM s-audit-rpt UNFORMATTED "Dir: " AT 3 ttbl_EDCode1.Direction " (Direction)".
  IF action = "UPDATE" THEN
  DO:
    IF ttbl_EDCode1.Direction NE ttbl_EDCode2.Direction THEN
    PUT STREAM s-audit-rpt UNFORMATTED "*" AT 1.
    PUT STREAM s-audit-rpt UNFORMATTED "Dir: " AT 3 ttbl_EDCode2.Direction.
  END.
END.
IF NOT v_field_changed OR ttbl_EDCode1.Partner NE ttbl_EDCode2.Partner OR action NE "UPDATE" THEN
DO:
  PUT STREAM s-audit-rpt UNFORMATTED "?: " AT 3 ttbl_EDCode1.Partner " (Partner)".
  IF action = "UPDATE" THEN
  DO:
    IF ttbl_EDCode1.Partner NE ttbl_EDCode2.Partner THEN
    PUT STREAM s-audit-rpt UNFORMATTED "*" AT 1.
    PUT STREAM s-audit-rpt UNFORMATTED "?: " AT 3 ttbl_EDCode2.Partner.
  END.
END.
IF NOT v_field_changed OR ttbl_EDCode1.Path-in NE ttbl_EDCode2.Path-in OR action NE "UPDATE" THEN
DO:
  PUT STREAM s-audit-rpt UNFORMATTED "?: " AT 3 ttbl_EDCode1.Path-in " (Path-in)".
  IF action = "UPDATE" THEN
  DO:
    IF ttbl_EDCode1.Path-in NE ttbl_EDCode2.Path-in THEN
    PUT STREAM s-audit-rpt UNFORMATTED "*" AT 1.
    PUT STREAM s-audit-rpt UNFORMATTED "?: " AT 3 ttbl_EDCode2.Path-in.
  END.
END.
IF NOT v_field_changed OR ttbl_EDCode1.Path-out NE ttbl_EDCode2.Path-out OR action NE "UPDATE" THEN
DO:
  PUT STREAM s-audit-rpt UNFORMATTED "?: " AT 3 ttbl_EDCode1.Path-out " (Path-out)".
  IF action = "UPDATE" THEN
  DO:
    IF ttbl_EDCode1.Path-out NE ttbl_EDCode2.Path-out THEN
    PUT STREAM s-audit-rpt UNFORMATTED "*" AT 1.
    PUT STREAM s-audit-rpt UNFORMATTED "?: " AT 3 ttbl_EDCode2.Path-out.
  END.
END.
IF NOT v_field_changed OR ttbl_EDCode1.SetID NE ttbl_EDCode2.SetID OR action NE "UPDATE" THEN
DO:
  PUT STREAM s-audit-rpt UNFORMATTED "?: " AT 3 ttbl_EDCode1.SetID " (SetID)".
  IF action = "UPDATE" THEN
  DO:
    IF ttbl_EDCode1.SetID NE ttbl_EDCode2.SetID THEN
    PUT STREAM s-audit-rpt UNFORMATTED "*" AT 1.
    PUT STREAM s-audit-rpt UNFORMATTED "?: " AT 3 ttbl_EDCode2.SetID.
  END.
END.
IF NOT v_field_changed OR ttbl_EDCode1.Test-prod NE ttbl_EDCode2.Test-prod OR action NE "UPDATE" THEN
DO:
  PUT STREAM s-audit-rpt UNFORMATTED "Mode: " AT 3 ttbl_EDCode1.Test-prod FORMAT "test/prod" " (Test-prod)".
  IF action = "UPDATE" THEN
  DO:
    IF ttbl_EDCode1.Test-prod NE ttbl_EDCode2.Test-prod THEN
    PUT STREAM s-audit-rpt UNFORMATTED "*" AT 1.
    PUT STREAM s-audit-rpt UNFORMATTED "Mode: " AT 3 ttbl_EDCode2.Test-prod FORMAT "test/prod".
  END.
END.
IF NOT v_field_changed OR ttbl_EDCode1.Version NE ttbl_EDCode2.Version OR action NE "UPDATE" THEN
DO:
  PUT STREAM s-audit-rpt UNFORMATTED "Ver: " AT 3 ttbl_EDCode1.Version FORMAT ">>>9" " (Version)".
  IF action = "UPDATE" THEN
  DO:
    IF ttbl_EDCode1.Version NE ttbl_EDCode2.Version THEN
    PUT STREAM s-audit-rpt UNFORMATTED "*" AT 1.
    PUT STREAM s-audit-rpt UNFORMATTED "Ver: " AT 3 ttbl_EDCode2.Version FORMAT ">>>9".
  END.
END.
IF v_show_index THEN
DO:
  PUT STREAM s-audit-rpt UNFORMATTED "Index Fields - Label: Value (database field-name)" AT 5.
  PUT STREAM s-audit-rpt UNFORMATTED "?: " AT 7.
  IF action = "UPDATE" THEN
  PUT STREAM s-audit-rpt UNFORMATTED ttbl_EDCode2.Partner " (Partner)".
  ELSE
  PUT STREAM s-audit-rpt UNFORMATTED ttbl_EDCode1.Partner " (Partner)".
  PUT STREAM s-audit-rpt UNFORMATTED "?: " AT 7.
  IF action = "UPDATE" THEN
  PUT STREAM s-audit-rpt UNFORMATTED ttbl_EDCode2.SetID " (SetID)".
  ELSE
  PUT STREAM s-audit-rpt UNFORMATTED ttbl_EDCode1.SetID " (SetID)".
  PUT STREAM s-audit-rpt UNFORMATTED "Dir: " AT 7.
  IF action = "UPDATE" THEN
  PUT STREAM s-audit-rpt UNFORMATTED ttbl_EDCode2.Direction " (Direction)".
  ELSE
  PUT STREAM s-audit-rpt UNFORMATTED ttbl_EDCode1.Direction " (Direction)".
  PUT STREAM s-audit-rpt UNFORMATTED "Ver: " AT 7.
  IF action = "UPDATE" THEN
  PUT STREAM s-audit-rpt UNFORMATTED ttbl_EDCode2.Version FORMAT ">>>9" " (Version)".
  ELSE
  PUT STREAM s-audit-rpt UNFORMATTED ttbl_EDCode1.Version FORMAT ">>>9" " (Version)".
END.
PUT STREAM s-audit-rpt UNFORMATTED SKIP(1).
