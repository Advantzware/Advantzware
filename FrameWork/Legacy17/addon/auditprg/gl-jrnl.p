/* gl-jrnl.p - generated 01/10/2001 - 13:00:03 by nosweat */

/* this program was generated by auditgn1.p and should not be modified */
/* modifications should be made to the auto generation program         */

DEFINE INPUT PARAMETER action AS CHARACTER NO-UNDO.

DEFINE VARIABLE i AS INTEGER NO-UNDO.

{methods/defines/audit.i}
FIND FIRST ttbl_header.

DEFINE WORK-TABLE ttbl_gl-jrnl1 NO-UNDO LIKE gl-jrnl.
DEFINE WORK-TABLE ttbl_gl-jrnl2 NO-UNDO LIKE gl-jrnl.

CREATE ttbl_gl-jrnl1.
IMPORT STREAM s_audit_file ttbl_gl-jrnl1.
CREATE ttbl_gl-jrnl2.
IF action = "UPDATE" THEN
IMPORT STREAM s_audit_file ttbl_gl-jrnl2.

PUT STREAM s-audit-rpt UNFORMATTED
  "Action    User ID/(User Name)            DB.Table/(Database)  Date       Time" SKIP
  "--------- ------------------------------ -------------------- ---------- --------" SKIP
  ttbl_header.v_action AT 1
  ttbl_header.v_userid AT 11
  ttbl_header.v_file AT 42
  ttbl_header.v_date FORMAT "99/99/9999" AT 63
  STRING(ttbl_header.v_time,"HH:MM:SS") AT 74.
FIND users WHERE users.user_id = v_userid NO-LOCK NO-ERROR.
IF AVAILABLE users THEN
PUT STREAM s-audit-rpt UNFORMATTED "(" AT 10 users.user_name ")".
ELSE
PUT STREAM s-audit-rpt UNFORMATTED "(user record does not exist)" AT 10.
PUT STREAM s-audit-rpt UNFORMATTED
  "(" AT 41 ttbl_header.v_db ")" SKIP(1)
  "Label: Value (database field-name)" AT 3.
IF action = "UPDATE" THEN
PUT STREAM s-audit-rpt UNFORMATTED " ( * - denotes field value change )".
PUT STREAM s-audit-rpt UNFORMATTED FILL("-",75) AT 3.
IF NOT v_field_changed OR ttbl_gl-jrnl1.actnum NE ttbl_gl-jrnl2.actnum OR action NE "UPDATE" THEN
DO:
  PUT STREAM s-audit-rpt UNFORMATTED "Account: " AT 3 ttbl_gl-jrnl1.actnum " (actnum)".
  IF action = "UPDATE" THEN
  DO:
    IF ttbl_gl-jrnl1.actnum NE ttbl_gl-jrnl2.actnum THEN
    PUT STREAM s-audit-rpt UNFORMATTED "*" AT 1.
    PUT STREAM s-audit-rpt UNFORMATTED "Account: " AT 3 ttbl_gl-jrnl2.actnum.
  END.
END.
IF NOT v_field_changed OR ttbl_gl-jrnl1.dscr NE ttbl_gl-jrnl2.dscr OR action NE "UPDATE" THEN
DO:
  PUT STREAM s-audit-rpt UNFORMATTED "Description: " AT 3 ttbl_gl-jrnl1.dscr " (dscr)".
  IF action = "UPDATE" THEN
  DO:
    IF ttbl_gl-jrnl1.dscr NE ttbl_gl-jrnl2.dscr THEN
    PUT STREAM s-audit-rpt UNFORMATTED "*" AT 1.
    PUT STREAM s-audit-rpt UNFORMATTED "Description: " AT 3 ttbl_gl-jrnl2.dscr.
  END.
END.
IF NOT v_field_changed OR ttbl_gl-jrnl1.j-no NE ttbl_gl-jrnl2.j-no OR action NE "UPDATE" THEN
DO:
  PUT STREAM s-audit-rpt UNFORMATTED "Internal Journal#: " AT 3 ttbl_gl-jrnl1.j-no FORMAT ">>>>>>9" " (j-no)".
  IF action = "UPDATE" THEN
  DO:
    IF ttbl_gl-jrnl1.j-no NE ttbl_gl-jrnl2.j-no THEN
    PUT STREAM s-audit-rpt UNFORMATTED "*" AT 1.
    PUT STREAM s-audit-rpt UNFORMATTED "Internal Journal#: " AT 3 ttbl_gl-jrnl2.j-no FORMAT ">>>>>>9".
  END.
END.
IF NOT v_field_changed OR ttbl_gl-jrnl1.line NE ttbl_gl-jrnl2.line OR action NE "UPDATE" THEN
DO:
  PUT STREAM s-audit-rpt UNFORMATTED "?: " AT 3 ttbl_gl-jrnl1.line FORMAT "999" " (line)".
  IF action = "UPDATE" THEN
  DO:
    IF ttbl_gl-jrnl1.line NE ttbl_gl-jrnl2.line THEN
    PUT STREAM s-audit-rpt UNFORMATTED "*" AT 1.
    PUT STREAM s-audit-rpt UNFORMATTED "?: " AT 3 ttbl_gl-jrnl2.line FORMAT "999".
  END.
END.
IF NOT v_field_changed OR ttbl_gl-jrnl1.tr-amt NE ttbl_gl-jrnl2.tr-amt OR action NE "UPDATE" THEN
DO:
  PUT STREAM s-audit-rpt UNFORMATTED "Amount: " AT 3 ttbl_gl-jrnl1.tr-amt FORMAT "->>,>>>,>>9.99" " (tr-amt)".
  IF action = "UPDATE" THEN
  DO:
    IF ttbl_gl-jrnl1.tr-amt NE ttbl_gl-jrnl2.tr-amt THEN
    PUT STREAM s-audit-rpt UNFORMATTED "*" AT 1.
    PUT STREAM s-audit-rpt UNFORMATTED "Amount: " AT 3 ttbl_gl-jrnl2.tr-amt FORMAT "->>,>>>,>>9.99".
  END.
END.
IF v_show_index THEN
DO:
  PUT STREAM s-audit-rpt UNFORMATTED "Index Fields - Label: Value (database field-name)" AT 5.
  PUT STREAM s-audit-rpt UNFORMATTED "Internal Journal#: " AT 7.
  IF action = "UPDATE" THEN
  PUT STREAM s-audit-rpt UNFORMATTED ttbl_gl-jrnl2.j-no FORMAT ">>>>>>9" " (j-no)".
  ELSE
  PUT STREAM s-audit-rpt UNFORMATTED ttbl_gl-jrnl1.j-no FORMAT ">>>>>>9" " (j-no)".
END.
PUT STREAM s-audit-rpt UNFORMATTED SKIP(1).
