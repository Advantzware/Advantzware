/* routing.p - generated 12/09/2005 - 15:42:54 by NoSweat */

/* this program was generated by auditgn1.p and should not be modified */
/* modifications should be made to the auto generation program         */

DEFINE INPUT PARAMETER action AS CHARACTER NO-UNDO.

DEFINE VARIABLE i AS INTEGER NO-UNDO.

{methods/defines/audit.i}
FIND FIRST ttbl_header.

DEFINE WORK-TABLE ttbl_routing1 NO-UNDO LIKE routing.
DEFINE WORK-TABLE ttbl_routing2 NO-UNDO LIKE routing.

CREATE ttbl_routing1.
IMPORT STREAM s_audit_file ttbl_routing1.
CREATE ttbl_routing2.
IF action = "UPDATE" THEN
IMPORT STREAM s_audit_file ttbl_routing2.

PUT STREAM s-audit-rpt UNFORMATTED
  "Action    User ID/(User Name)            DB.Table/(Database)  Date       Time" SKIP
  "--------- ------------------------------ -------------------- ---------- --------" SKIP
  ttbl_header.v_action AT 1
  ttbl_header.v_userid AT 11
  ttbl_header.v_file AT 42
  ttbl_header.v_date FORMAT "99/99/9999" AT 63
  STRING(ttbl_header.v_time,"HH:MM:SS") AT 74.
FIND users WHERE users.user_id = v_userid NO-LOCK NO-ERROR.
IF AVAILABLE users THEN
PUT STREAM s-audit-rpt UNFORMATTED "(" AT 10 users.user_name ")".
ELSE
PUT STREAM s-audit-rpt UNFORMATTED "(user record does not exist)" AT 10.
PUT STREAM s-audit-rpt UNFORMATTED
  "(" AT 41 ttbl_header.v_db ")" SKIP(1)
  "Label: Value (database field-name)" AT 3.
IF action = "UPDATE" THEN
PUT STREAM s-audit-rpt UNFORMATTED " ( * - denotes field value change )".
PUT STREAM s-audit-rpt UNFORMATTED FILL("-",75) AT 3.
IF NOT v_field_changed OR ttbl_routing1.company NE ttbl_routing2.company OR action NE "UPDATE" THEN
DO:
  PUT STREAM s-audit-rpt UNFORMATTED "Company: " AT 3 ttbl_routing1.company " (company)".
  IF action = "UPDATE" THEN
  DO:
    IF ttbl_routing1.company NE ttbl_routing2.company THEN
    PUT STREAM s-audit-rpt UNFORMATTED "*" AT 1.
    PUT STREAM s-audit-rpt UNFORMATTED "Company: " AT 3 ttbl_routing2.company.
  END.
END.
IF NOT v_field_changed OR ttbl_routing1.dscr NE ttbl_routing2.dscr OR action NE "UPDATE" THEN
DO:
  PUT STREAM s-audit-rpt UNFORMATTED "Description: " AT 3 ttbl_routing1.dscr " (dscr)".
  IF action = "UPDATE" THEN
  DO:
    IF ttbl_routing1.dscr NE ttbl_routing2.dscr THEN
    PUT STREAM s-audit-rpt UNFORMATTED "*" AT 1.
    PUT STREAM s-audit-rpt UNFORMATTED "Description: " AT 3 ttbl_routing2.dscr.
  END.
END.
IF NOT v_field_changed OR ttbl_routing1.loc NE ttbl_routing2.loc OR action NE "UPDATE" THEN
DO:
  PUT STREAM s-audit-rpt UNFORMATTED "Location: " AT 3 ttbl_routing1.loc " (loc)".
  IF action = "UPDATE" THEN
  DO:
    IF ttbl_routing1.loc NE ttbl_routing2.loc THEN
    PUT STREAM s-audit-rpt UNFORMATTED "*" AT 1.
    PUT STREAM s-audit-rpt UNFORMATTED "Location: " AT 3 ttbl_routing2.loc.
  END.
END.
DO i = 1 TO 10:
IF NOT v_field_changed OR ttbl_routing1.m-code[i] NE ttbl_routing2.m-code[i] OR action NE "UPDATE" THEN
DO:
  PUT STREAM s-audit-rpt UNFORMATTED "Machine Code[" AT 3 i "]: " ttbl_routing1.m-code[i] " (m-code[" i "])".
  IF action = "UPDATE" THEN
  DO:
    IF ttbl_routing1.m-code[i] NE ttbl_routing2.m-code[i] THEN
    PUT STREAM s-audit-rpt UNFORMATTED "*" AT 1.
    PUT STREAM s-audit-rpt UNFORMATTED "Machine Code[" AT 3 i "]: " ttbl_routing2.m-code[i].
  END.
END.
END.
IF NOT v_field_changed OR ttbl_routing1.r-code NE ttbl_routing2.r-code OR action NE "UPDATE" THEN
DO:
  PUT STREAM s-audit-rpt UNFORMATTED "Routing Code: " AT 3 ttbl_routing1.r-code " (r-code)".
  IF action = "UPDATE" THEN
  DO:
    IF ttbl_routing1.r-code NE ttbl_routing2.r-code THEN
    PUT STREAM s-audit-rpt UNFORMATTED "*" AT 1.
    PUT STREAM s-audit-rpt UNFORMATTED "Routing Code: " AT 3 ttbl_routing2.r-code.
  END.
END.
IF v_show_index THEN
DO:
  PUT STREAM s-audit-rpt UNFORMATTED "Index Fields - Label: Value (database field-name)" AT 5.
  PUT STREAM s-audit-rpt UNFORMATTED "Company: " AT 7.
  IF action = "UPDATE" THEN
  PUT STREAM s-audit-rpt UNFORMATTED ttbl_routing2.company " (company)".
  ELSE
  PUT STREAM s-audit-rpt UNFORMATTED ttbl_routing1.company " (company)".
  PUT STREAM s-audit-rpt UNFORMATTED "Location: " AT 7.
  IF action = "UPDATE" THEN
  PUT STREAM s-audit-rpt UNFORMATTED ttbl_routing2.loc " (loc)".
  ELSE
  PUT STREAM s-audit-rpt UNFORMATTED ttbl_routing1.loc " (loc)".
  PUT STREAM s-audit-rpt UNFORMATTED "Routing Code: " AT 7.
  IF action = "UPDATE" THEN
  PUT STREAM s-audit-rpt UNFORMATTED ttbl_routing2.r-code " (r-code)".
  ELSE
  PUT STREAM s-audit-rpt UNFORMATTED ttbl_routing1.r-code " (r-code)".
END.
PUT STREAM s-audit-rpt UNFORMATTED SKIP(1).
