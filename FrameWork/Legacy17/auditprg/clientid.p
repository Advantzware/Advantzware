/* clientid.p - generated 12/09/2005 - 15:42:40 by NoSweat */

/* this program was generated by auditgn1.p and should not be modified */
/* modifications should be made to the auto generation program         */

DEFINE INPUT PARAMETER action AS CHARACTER NO-UNDO.

DEFINE VARIABLE i AS INTEGER NO-UNDO.

{methods/defines/audit.i}
FIND FIRST ttbl_header.

DEFINE WORK-TABLE ttbl_clientid1 NO-UNDO LIKE clientid.
DEFINE WORK-TABLE ttbl_clientid2 NO-UNDO LIKE clientid.

CREATE ttbl_clientid1.
IMPORT STREAM s_audit_file ttbl_clientid1.
CREATE ttbl_clientid2.
IF action = "UPDATE" THEN
IMPORT STREAM s_audit_file ttbl_clientid2.

PUT STREAM s-audit-rpt UNFORMATTED
  "Action    User ID/(User Name)            DB.Table/(Database)  Date       Time" SKIP
  "--------- ------------------------------ -------------------- ---------- --------" SKIP
  ttbl_header.v_action AT 1
  ttbl_header.v_userid AT 11
  ttbl_header.v_file AT 42
  ttbl_header.v_date FORMAT "99/99/9999" AT 63
  STRING(ttbl_header.v_time,"HH:MM:SS") AT 74.
FIND users WHERE users.user_id = v_userid NO-LOCK NO-ERROR.
IF AVAILABLE users THEN
PUT STREAM s-audit-rpt UNFORMATTED "(" AT 10 users.user_name ")".
ELSE
PUT STREAM s-audit-rpt UNFORMATTED "(user record does not exist)" AT 10.
PUT STREAM s-audit-rpt UNFORMATTED
  "(" AT 41 ttbl_header.v_db ")" SKIP(1)
  "Label: Value (database field-name)" AT 3.
IF action = "UPDATE" THEN
PUT STREAM s-audit-rpt UNFORMATTED " ( * - denotes field value change )".
PUT STREAM s-audit-rpt UNFORMATTED FILL("-",75) AT 3.
IF NOT v_field_changed OR ttbl_clientid1.client-id NE ttbl_clientid2.client-id OR action NE "UPDATE" THEN
DO:
  PUT STREAM s-audit-rpt UNFORMATTED "Client ID: " AT 3 ttbl_clientid1.client-id " (client-id)".
  IF action = "UPDATE" THEN
  DO:
    IF ttbl_clientid1.client-id NE ttbl_clientid2.client-id THEN
    PUT STREAM s-audit-rpt UNFORMATTED "*" AT 1.
    PUT STREAM s-audit-rpt UNFORMATTED "Client ID: " AT 3 ttbl_clientid2.client-id.
  END.
END.
IF NOT v_field_changed OR ttbl_clientid1.patch NE ttbl_clientid2.patch OR action NE "UPDATE" THEN
DO:
  PUT STREAM s-audit-rpt UNFORMATTED "Patch#: " AT 3 ttbl_clientid1.patch FORMAT ">>>>>>9" " (patch)".
  IF action = "UPDATE" THEN
  DO:
    IF ttbl_clientid1.patch NE ttbl_clientid2.patch THEN
    PUT STREAM s-audit-rpt UNFORMATTED "*" AT 1.
    PUT STREAM s-audit-rpt UNFORMATTED "Patch#: " AT 3 ttbl_clientid2.patch FORMAT ">>>>>>9".
  END.
END.
IF NOT v_field_changed OR ttbl_clientid1.seq NE ttbl_clientid2.seq OR action NE "UPDATE" THEN
DO:
  PUT STREAM s-audit-rpt UNFORMATTED "Version Seq: " AT 3 ttbl_clientid1.seq " (seq)".
  IF action = "UPDATE" THEN
  DO:
    IF ttbl_clientid1.seq NE ttbl_clientid2.seq THEN
    PUT STREAM s-audit-rpt UNFORMATTED "*" AT 1.
    PUT STREAM s-audit-rpt UNFORMATTED "Version Seq: " AT 3 ttbl_clientid2.seq.
  END.
END.
IF NOT v_field_changed OR ttbl_clientid1.version NE ttbl_clientid2.version OR action NE "UPDATE" THEN
DO:
  PUT STREAM s-audit-rpt UNFORMATTED "Version: " AT 3 ttbl_clientid1.version " (version)".
  IF action = "UPDATE" THEN
  DO:
    IF ttbl_clientid1.version NE ttbl_clientid2.version THEN
    PUT STREAM s-audit-rpt UNFORMATTED "*" AT 1.
    PUT STREAM s-audit-rpt UNFORMATTED "Version: " AT 3 ttbl_clientid2.version.
  END.
END.
IF v_show_index THEN
DO:
  PUT STREAM s-audit-rpt UNFORMATTED "Index Fields - Label: Value (database field-name)" AT 5.
  PUT STREAM s-audit-rpt UNFORMATTED "Client ID: " AT 7.
  IF action = "UPDATE" THEN
  PUT STREAM s-audit-rpt UNFORMATTED ttbl_clientid2.client-id " (client-id)".
  ELSE
  PUT STREAM s-audit-rpt UNFORMATTED ttbl_clientid1.client-id " (client-id)".
END.
PUT STREAM s-audit-rpt UNFORMATTED SKIP(1).
