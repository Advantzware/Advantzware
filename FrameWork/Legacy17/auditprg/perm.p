/* perm.p - generated 12/09/2005 - 15:42:51 by NoSweat */

/* this program was generated by auditgn1.p and should not be modified */
/* modifications should be made to the auto generation program         */

DEFINE INPUT PARAMETER action AS CHARACTER NO-UNDO.

DEFINE VARIABLE i AS INTEGER NO-UNDO.

{methods/defines/audit.i}
FIND FIRST ttbl_header.

DEFINE WORK-TABLE ttbl_perm1 NO-UNDO LIKE perm.
DEFINE WORK-TABLE ttbl_perm2 NO-UNDO LIKE perm.

CREATE ttbl_perm1.
IMPORT STREAM s_audit_file ttbl_perm1.
CREATE ttbl_perm2.
IF action = "UPDATE" THEN
IMPORT STREAM s_audit_file ttbl_perm2.

PUT STREAM s-audit-rpt UNFORMATTED
  "Action    User ID/(User Name)            DB.Table/(Database)  Date       Time" SKIP
  "--------- ------------------------------ -------------------- ---------- --------" SKIP
  ttbl_header.v_action AT 1
  ttbl_header.v_userid AT 11
  ttbl_header.v_file AT 42
  ttbl_header.v_date FORMAT "99/99/9999" AT 63
  STRING(ttbl_header.v_time,"HH:MM:SS") AT 74.
FIND users WHERE users.user_id = v_userid NO-LOCK NO-ERROR.
IF AVAILABLE users THEN
PUT STREAM s-audit-rpt UNFORMATTED "(" AT 10 users.user_name ")".
ELSE
PUT STREAM s-audit-rpt UNFORMATTED "(user record does not exist)" AT 10.
PUT STREAM s-audit-rpt UNFORMATTED
  "(" AT 41 ttbl_header.v_db ")" SKIP(1)
  "Label: Value (database field-name)" AT 3.
IF action = "UPDATE" THEN
PUT STREAM s-audit-rpt UNFORMATTED " ( * - denotes field value change )".
PUT STREAM s-audit-rpt UNFORMATTED FILL("-",75) AT 3.
IF NOT v_field_changed OR ttbl_perm1.exec-help NE ttbl_perm2.exec-help OR action NE "UPDATE" THEN
DO:
  PUT STREAM s-audit-rpt UNFORMATTED "Help File: " AT 3 ttbl_perm1.exec-help " (exec-help)".
  IF action = "UPDATE" THEN
  DO:
    IF ttbl_perm1.exec-help NE ttbl_perm2.exec-help THEN
    PUT STREAM s-audit-rpt UNFORMATTED "*" AT 1.
    PUT STREAM s-audit-rpt UNFORMATTED "Help File: " AT 3 ttbl_perm2.exec-help.
  END.
END.
IF NOT v_field_changed OR ttbl_perm1.exec-path NE ttbl_perm2.exec-path OR action NE "UPDATE" THEN
DO:
  PUT STREAM s-audit-rpt UNFORMATTED "Directory Path: " AT 3 ttbl_perm1.exec-path " (exec-path)".
  IF action = "UPDATE" THEN
  DO:
    IF ttbl_perm1.exec-path NE ttbl_perm2.exec-path THEN
    PUT STREAM s-audit-rpt UNFORMATTED "*" AT 1.
    PUT STREAM s-audit-rpt UNFORMATTED "Directory Path: " AT 3 ttbl_perm2.exec-path.
  END.
END.
IF NOT v_field_changed OR ttbl_perm1.menu-alias NE ttbl_perm2.menu-alias OR action NE "UPDATE" THEN
DO:
  PUT STREAM s-audit-rpt UNFORMATTED "Alias: " AT 3 ttbl_perm1.menu-alias " (menu-alias)".
  IF action = "UPDATE" THEN
  DO:
    IF ttbl_perm1.menu-alias NE ttbl_perm2.menu-alias THEN
    PUT STREAM s-audit-rpt UNFORMATTED "*" AT 1.
    PUT STREAM s-audit-rpt UNFORMATTED "Alias: " AT 3 ttbl_perm2.menu-alias.
  END.
END.
IF NOT v_field_changed OR ttbl_perm1.menu-item NE ttbl_perm2.menu-item OR action NE "UPDATE" THEN
DO:
  PUT STREAM s-audit-rpt UNFORMATTED "Menu: " AT 3 ttbl_perm1.menu-item " (menu-item)".
  IF action = "UPDATE" THEN
  DO:
    IF ttbl_perm1.menu-item NE ttbl_perm2.menu-item THEN
    PUT STREAM s-audit-rpt UNFORMATTED "*" AT 1.
    PUT STREAM s-audit-rpt UNFORMATTED "Menu: " AT 3 ttbl_perm2.menu-item.
  END.
END.
IF NOT v_field_changed OR ttbl_perm1.menu-label NE ttbl_perm2.menu-label OR action NE "UPDATE" THEN
DO:
  PUT STREAM s-audit-rpt UNFORMATTED "Selection Label: " AT 3 ttbl_perm1.menu-label " (menu-label)".
  IF action = "UPDATE" THEN
  DO:
    IF ttbl_perm1.menu-label NE ttbl_perm2.menu-label THEN
    PUT STREAM s-audit-rpt UNFORMATTED "*" AT 1.
    PUT STREAM s-audit-rpt UNFORMATTED "Selection Label: " AT 3 ttbl_perm2.menu-label.
  END.
END.
IF NOT v_field_changed OR ttbl_perm1.menu-select NE ttbl_perm2.menu-select OR action NE "UPDATE" THEN
DO:
  PUT STREAM s-audit-rpt UNFORMATTED "Selection: " AT 3 ttbl_perm1.menu-select " (menu-select)".
  IF action = "UPDATE" THEN
  DO:
    IF ttbl_perm1.menu-select NE ttbl_perm2.menu-select THEN
    PUT STREAM s-audit-rpt UNFORMATTED "*" AT 1.
    PUT STREAM s-audit-rpt UNFORMATTED "Selection: " AT 3 ttbl_perm2.menu-select.
  END.
END.
IF NOT v_field_changed OR ttbl_perm1.task NE ttbl_perm2.task OR action NE "UPDATE" THEN
DO:
  PUT STREAM s-audit-rpt UNFORMATTED "Task: " AT 3 ttbl_perm1.task " (task)".
  IF action = "UPDATE" THEN
  DO:
    IF ttbl_perm1.task NE ttbl_perm2.task THEN
    PUT STREAM s-audit-rpt UNFORMATTED "*" AT 1.
    PUT STREAM s-audit-rpt UNFORMATTED "Task: " AT 3 ttbl_perm2.task.
  END.
END.
IF NOT v_field_changed OR ttbl_perm1.user1 NE ttbl_perm2.user1 OR action NE "UPDATE" THEN
DO:
  PUT STREAM s-audit-rpt UNFORMATTED "UserFld1: " AT 3 ttbl_perm1.user1 " (user1)".
  IF action = "UPDATE" THEN
  DO:
    IF ttbl_perm1.user1 NE ttbl_perm2.user1 THEN
    PUT STREAM s-audit-rpt UNFORMATTED "*" AT 1.
    PUT STREAM s-audit-rpt UNFORMATTED "UserFld1: " AT 3 ttbl_perm2.user1.
  END.
END.
IF NOT v_field_changed OR ttbl_perm1.user2 NE ttbl_perm2.user2 OR action NE "UPDATE" THEN
DO:
  PUT STREAM s-audit-rpt UNFORMATTED "UserFld2: " AT 3 ttbl_perm1.user2 " (user2)".
  IF action = "UPDATE" THEN
  DO:
    IF ttbl_perm1.user2 NE ttbl_perm2.user2 THEN
    PUT STREAM s-audit-rpt UNFORMATTED "*" AT 1.
    PUT STREAM s-audit-rpt UNFORMATTED "UserFld2: " AT 3 ttbl_perm2.user2.
  END.
END.
IF NOT v_field_changed OR ttbl_perm1.users NE ttbl_perm2.users OR action NE "UPDATE" THEN
DO:
  PUT STREAM s-audit-rpt UNFORMATTED "Passwords/Groups: " AT 3 ttbl_perm1.users " (users)".
  IF action = "UPDATE" THEN
  DO:
    IF ttbl_perm1.users NE ttbl_perm2.users THEN
    PUT STREAM s-audit-rpt UNFORMATTED "*" AT 1.
    PUT STREAM s-audit-rpt UNFORMATTED "Passwords/Groups: " AT 3 ttbl_perm2.users.
  END.
END.
IF v_show_index THEN
DO:
  PUT STREAM s-audit-rpt UNFORMATTED "Index Fields - Label: Value (database field-name)" AT 5.
  PUT STREAM s-audit-rpt UNFORMATTED "Menu: " AT 7.
  IF action = "UPDATE" THEN
  PUT STREAM s-audit-rpt UNFORMATTED ttbl_perm2.menu-item " (menu-item)".
  ELSE
  PUT STREAM s-audit-rpt UNFORMATTED ttbl_perm1.menu-item " (menu-item)".
  PUT STREAM s-audit-rpt UNFORMATTED "Selection: " AT 7.
  IF action = "UPDATE" THEN
  PUT STREAM s-audit-rpt UNFORMATTED ttbl_perm2.menu-select " (menu-select)".
  ELSE
  PUT STREAM s-audit-rpt UNFORMATTED ttbl_perm1.menu-select " (menu-select)".
END.
PUT STREAM s-audit-rpt UNFORMATTED SKIP(1).
