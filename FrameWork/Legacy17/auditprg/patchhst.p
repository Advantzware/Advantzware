/* patchhst.p - generated 12/09/2005 - 15:42:51 by NoSweat */

/* this program was generated by auditgn1.p and should not be modified */
/* modifications should be made to the auto generation program         */

DEFINE INPUT PARAMETER action AS CHARACTER NO-UNDO.

DEFINE VARIABLE i AS INTEGER NO-UNDO.

{methods/defines/audit.i}
FIND FIRST ttbl_header.

DEFINE WORK-TABLE ttbl_patchhst1 NO-UNDO LIKE patchhst.
DEFINE WORK-TABLE ttbl_patchhst2 NO-UNDO LIKE patchhst.

CREATE ttbl_patchhst1.
IMPORT STREAM s_audit_file ttbl_patchhst1.
CREATE ttbl_patchhst2.
IF action = "UPDATE" THEN
IMPORT STREAM s_audit_file ttbl_patchhst2.

PUT STREAM s-audit-rpt UNFORMATTED
  "Action    User ID/(User Name)            DB.Table/(Database)  Date       Time" SKIP
  "--------- ------------------------------ -------------------- ---------- --------" SKIP
  ttbl_header.v_action AT 1
  ttbl_header.v_userid AT 11
  ttbl_header.v_file AT 42
  ttbl_header.v_date FORMAT "99/99/9999" AT 63
  STRING(ttbl_header.v_time,"HH:MM:SS") AT 74.
FIND users WHERE users.user_id = v_userid NO-LOCK NO-ERROR.
IF AVAILABLE users THEN
PUT STREAM s-audit-rpt UNFORMATTED "(" AT 10 users.user_name ")".
ELSE
PUT STREAM s-audit-rpt UNFORMATTED "(user record does not exist)" AT 10.
PUT STREAM s-audit-rpt UNFORMATTED
  "(" AT 41 ttbl_header.v_db ")" SKIP(1)
  "Label: Value (database field-name)" AT 3.
IF action = "UPDATE" THEN
PUT STREAM s-audit-rpt UNFORMATTED " ( * - denotes field value change )".
PUT STREAM s-audit-rpt UNFORMATTED FILL("-",75) AT 3.
IF NOT v_field_changed OR ttbl_patchhst1.completed NE ttbl_patchhst2.completed OR action NE "UPDATE" THEN
DO:
  PUT STREAM s-audit-rpt UNFORMATTED "Completed: " AT 3 ttbl_patchhst1.completed FORMAT "yes/no" " (completed)".
  IF action = "UPDATE" THEN
  DO:
    IF ttbl_patchhst1.completed NE ttbl_patchhst2.completed THEN
    PUT STREAM s-audit-rpt UNFORMATTED "*" AT 1.
    PUT STREAM s-audit-rpt UNFORMATTED "Completed: " AT 3 ttbl_patchhst2.completed FORMAT "yes/no".
  END.
END.
IF NOT v_field_changed OR ttbl_patchhst1.dependancy NE ttbl_patchhst2.dependancy OR action NE "UPDATE" THEN
DO:
  PUT STREAM s-audit-rpt UNFORMATTED "Dependancy: " AT 3 ttbl_patchhst1.dependancy " (dependancy)".
  IF action = "UPDATE" THEN
  DO:
    IF ttbl_patchhst1.dependancy NE ttbl_patchhst2.dependancy THEN
    PUT STREAM s-audit-rpt UNFORMATTED "*" AT 1.
    PUT STREAM s-audit-rpt UNFORMATTED "Dependancy: " AT 3 ttbl_patchhst2.dependancy.
  END.
END.
IF NOT v_field_changed OR ttbl_patchhst1.descr NE ttbl_patchhst2.descr OR action NE "UPDATE" THEN
DO:
  PUT STREAM s-audit-rpt UNFORMATTED "Description: " AT 3 ttbl_patchhst1.descr " (descr)".
  IF action = "UPDATE" THEN
  DO:
    IF ttbl_patchhst1.descr NE ttbl_patchhst2.descr THEN
    PUT STREAM s-audit-rpt UNFORMATTED "*" AT 1.
    PUT STREAM s-audit-rpt UNFORMATTED "Description: " AT 3 ttbl_patchhst2.descr.
  END.
END.
IF NOT v_field_changed OR ttbl_patchhst1.id NE ttbl_patchhst2.id OR action NE "UPDATE" THEN
DO:
  PUT STREAM s-audit-rpt UNFORMATTED "Unique ID: " AT 3 ttbl_patchhst1.id FORMAT "->,>>>,>>9" " (id)".
  IF action = "UPDATE" THEN
  DO:
    IF ttbl_patchhst1.id NE ttbl_patchhst2.id THEN
    PUT STREAM s-audit-rpt UNFORMATTED "*" AT 1.
    PUT STREAM s-audit-rpt UNFORMATTED "Unique ID: " AT 3 ttbl_patchhst2.id FORMAT "->,>>>,>>9".
  END.
END.
IF NOT v_field_changed OR ttbl_patchhst1.patch NE ttbl_patchhst2.patch OR action NE "UPDATE" THEN
DO:
  PUT STREAM s-audit-rpt UNFORMATTED "Patch#: " AT 3 ttbl_patchhst1.patch FORMAT ">>>>>>9" " (patch)".
  IF action = "UPDATE" THEN
  DO:
    IF ttbl_patchhst1.patch NE ttbl_patchhst2.patch THEN
    PUT STREAM s-audit-rpt UNFORMATTED "*" AT 1.
    PUT STREAM s-audit-rpt UNFORMATTED "Patch#: " AT 3 ttbl_patchhst2.patch FORMAT ">>>>>>9".
  END.
END.
IF NOT v_field_changed OR ttbl_patchhst1.patch-date NE ttbl_patchhst2.patch-date OR action NE "UPDATE" THEN
DO:
  PUT STREAM s-audit-rpt UNFORMATTED "Patch Date: " AT 3 ttbl_patchhst1.patch-date FORMAT "99/99/9999" " (patch-date)".
  IF action = "UPDATE" THEN
  DO:
    IF ttbl_patchhst1.patch-date NE ttbl_patchhst2.patch-date THEN
    PUT STREAM s-audit-rpt UNFORMATTED "*" AT 1.
    PUT STREAM s-audit-rpt UNFORMATTED "Patch Date: " AT 3 ttbl_patchhst2.patch-date FORMAT "99/99/9999".
  END.
END.
IF NOT v_field_changed OR ttbl_patchhst1.patch-time NE ttbl_patchhst2.patch-time OR action NE "UPDATE" THEN
DO:
  PUT STREAM s-audit-rpt UNFORMATTED "Patch Time: " AT 3 ttbl_patchhst1.patch-time FORMAT ">>>>9" " (patch-time)".
  IF action = "UPDATE" THEN
  DO:
    IF ttbl_patchhst1.patch-time NE ttbl_patchhst2.patch-time THEN
    PUT STREAM s-audit-rpt UNFORMATTED "*" AT 1.
    PUT STREAM s-audit-rpt UNFORMATTED "Patch Time: " AT 3 ttbl_patchhst2.patch-time FORMAT ">>>>9".
  END.
END.
IF NOT v_field_changed OR ttbl_patchhst1.returnvalue NE ttbl_patchhst2.returnvalue OR action NE "UPDATE" THEN
DO:
  PUT STREAM s-audit-rpt UNFORMATTED "Return Value: " AT 3 ttbl_patchhst1.returnvalue " (returnvalue)".
  IF action = "UPDATE" THEN
  DO:
    IF ttbl_patchhst1.returnvalue NE ttbl_patchhst2.returnvalue THEN
    PUT STREAM s-audit-rpt UNFORMATTED "*" AT 1.
    PUT STREAM s-audit-rpt UNFORMATTED "Return Value: " AT 3 ttbl_patchhst2.returnvalue.
  END.
END.
IF NOT v_field_changed OR ttbl_patchhst1.run-once NE ttbl_patchhst2.run-once OR action NE "UPDATE" THEN
DO:
  PUT STREAM s-audit-rpt UNFORMATTED "Run Once: " AT 3 ttbl_patchhst1.run-once FORMAT "yes/no" " (run-once)".
  IF action = "UPDATE" THEN
  DO:
    IF ttbl_patchhst1.run-once NE ttbl_patchhst2.run-once THEN
    PUT STREAM s-audit-rpt UNFORMATTED "*" AT 1.
    PUT STREAM s-audit-rpt UNFORMATTED "Run Once: " AT 3 ttbl_patchhst2.run-once FORMAT "yes/no".
  END.
END.
IF NOT v_field_changed OR ttbl_patchhst1.run-order NE ttbl_patchhst2.run-order OR action NE "UPDATE" THEN
DO:
  PUT STREAM s-audit-rpt UNFORMATTED "Order: " AT 3 ttbl_patchhst1.run-order FORMAT ">>>>9" " (run-order)".
  IF action = "UPDATE" THEN
  DO:
    IF ttbl_patchhst1.run-order NE ttbl_patchhst2.run-order THEN
    PUT STREAM s-audit-rpt UNFORMATTED "*" AT 1.
    PUT STREAM s-audit-rpt UNFORMATTED "Order: " AT 3 ttbl_patchhst2.run-order FORMAT ">>>>9".
  END.
END.
IF NOT v_field_changed OR ttbl_patchhst1.seq NE ttbl_patchhst2.seq OR action NE "UPDATE" THEN
DO:
  PUT STREAM s-audit-rpt UNFORMATTED "Version Seq: " AT 3 ttbl_patchhst1.seq " (seq)".
  IF action = "UPDATE" THEN
  DO:
    IF ttbl_patchhst1.seq NE ttbl_patchhst2.seq THEN
    PUT STREAM s-audit-rpt UNFORMATTED "*" AT 1.
    PUT STREAM s-audit-rpt UNFORMATTED "Version Seq: " AT 3 ttbl_patchhst2.seq.
  END.
END.
IF NOT v_field_changed OR ttbl_patchhst1.user_id NE ttbl_patchhst2.user_id OR action NE "UPDATE" THEN
DO:
  PUT STREAM s-audit-rpt UNFORMATTED "User ID: " AT 3 ttbl_patchhst1.user_id " (user_id)".
  IF action = "UPDATE" THEN
  DO:
    IF ttbl_patchhst1.user_id NE ttbl_patchhst2.user_id THEN
    PUT STREAM s-audit-rpt UNFORMATTED "*" AT 1.
    PUT STREAM s-audit-rpt UNFORMATTED "User ID: " AT 3 ttbl_patchhst2.user_id.
  END.
END.
IF NOT v_field_changed OR ttbl_patchhst1.utility NE ttbl_patchhst2.utility OR action NE "UPDATE" THEN
DO:
  PUT STREAM s-audit-rpt UNFORMATTED "Utility Name: " AT 3 ttbl_patchhst1.utility " (utility)".
  IF action = "UPDATE" THEN
  DO:
    IF ttbl_patchhst1.utility NE ttbl_patchhst2.utility THEN
    PUT STREAM s-audit-rpt UNFORMATTED "*" AT 1.
    PUT STREAM s-audit-rpt UNFORMATTED "Utility Name: " AT 3 ttbl_patchhst2.utility.
  END.
END.
IF NOT v_field_changed OR ttbl_patchhst1.version NE ttbl_patchhst2.version OR action NE "UPDATE" THEN
DO:
  PUT STREAM s-audit-rpt UNFORMATTED "Version: " AT 3 ttbl_patchhst1.version " (version)".
  IF action = "UPDATE" THEN
  DO:
    IF ttbl_patchhst1.version NE ttbl_patchhst2.version THEN
    PUT STREAM s-audit-rpt UNFORMATTED "*" AT 1.
    PUT STREAM s-audit-rpt UNFORMATTED "Version: " AT 3 ttbl_patchhst2.version.
  END.
END.
IF v_show_index THEN
DO:
  PUT STREAM s-audit-rpt UNFORMATTED "Index Fields - Label: Value (database field-name)" AT 5.
  PUT STREAM s-audit-rpt UNFORMATTED "Patch#: " AT 7.
  IF action = "UPDATE" THEN
  PUT STREAM s-audit-rpt UNFORMATTED ttbl_patchhst2.patch FORMAT ">>>>>>9" " (patch)".
  ELSE
  PUT STREAM s-audit-rpt UNFORMATTED ttbl_patchhst1.patch FORMAT ">>>>>>9" " (patch)".
  PUT STREAM s-audit-rpt UNFORMATTED "Version: " AT 7.
  IF action = "UPDATE" THEN
  PUT STREAM s-audit-rpt UNFORMATTED ttbl_patchhst2.version " (version)".
  ELSE
  PUT STREAM s-audit-rpt UNFORMATTED ttbl_patchhst1.version " (version)".
  PUT STREAM s-audit-rpt UNFORMATTED "Version Seq: " AT 7.
  IF action = "UPDATE" THEN
  PUT STREAM s-audit-rpt UNFORMATTED ttbl_patchhst2.seq " (seq)".
  ELSE
  PUT STREAM s-audit-rpt UNFORMATTED ttbl_patchhst1.seq " (seq)".
  PUT STREAM s-audit-rpt UNFORMATTED "Patch Date: " AT 7.
  IF action = "UPDATE" THEN
  PUT STREAM s-audit-rpt UNFORMATTED ttbl_patchhst2.patch-date FORMAT "99/99/9999" " (patch-date)".
  ELSE
  PUT STREAM s-audit-rpt UNFORMATTED ttbl_patchhst1.patch-date FORMAT "99/99/9999" " (patch-date)".
  PUT STREAM s-audit-rpt UNFORMATTED "Patch Time: " AT 7.
  IF action = "UPDATE" THEN
  PUT STREAM s-audit-rpt UNFORMATTED ttbl_patchhst2.patch-time FORMAT ">>>>9" " (patch-time)".
  ELSE
  PUT STREAM s-audit-rpt UNFORMATTED ttbl_patchhst1.patch-time FORMAT ">>>>9" " (patch-time)".
  PUT STREAM s-audit-rpt UNFORMATTED "Order: " AT 7.
  IF action = "UPDATE" THEN
  PUT STREAM s-audit-rpt UNFORMATTED ttbl_patchhst2.run-order FORMAT ">>>>9" " (run-order)".
  ELSE
  PUT STREAM s-audit-rpt UNFORMATTED ttbl_patchhst1.run-order FORMAT ">>>>9" " (run-order)".
END.
PUT STREAM s-audit-rpt UNFORMATTED SKIP(1).
