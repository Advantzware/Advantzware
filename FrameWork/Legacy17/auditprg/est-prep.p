/* est-prep.p - generated 12/09/2005 - 15:42:44 by NoSweat */

/* this program was generated by auditgn1.p and should not be modified */
/* modifications should be made to the auto generation program         */

DEFINE INPUT PARAMETER action AS CHARACTER NO-UNDO.

DEFINE VARIABLE i AS INTEGER NO-UNDO.

{methods/defines/audit.i}
FIND FIRST ttbl_header.

DEFINE WORK-TABLE ttbl_est-prep1 NO-UNDO LIKE est-prep.
DEFINE WORK-TABLE ttbl_est-prep2 NO-UNDO LIKE est-prep.

CREATE ttbl_est-prep1.
IMPORT STREAM s_audit_file ttbl_est-prep1.
CREATE ttbl_est-prep2.
IF action = "UPDATE" THEN
IMPORT STREAM s_audit_file ttbl_est-prep2.

PUT STREAM s-audit-rpt UNFORMATTED
  "Action    User ID/(User Name)            DB.Table/(Database)  Date       Time" SKIP
  "--------- ------------------------------ -------------------- ---------- --------" SKIP
  ttbl_header.v_action AT 1
  ttbl_header.v_userid AT 11
  ttbl_header.v_file AT 42
  ttbl_header.v_date FORMAT "99/99/9999" AT 63
  STRING(ttbl_header.v_time,"HH:MM:SS") AT 74.
FIND users WHERE users.user_id = v_userid NO-LOCK NO-ERROR.
IF AVAILABLE users THEN
PUT STREAM s-audit-rpt UNFORMATTED "(" AT 10 users.user_name ")".
ELSE
PUT STREAM s-audit-rpt UNFORMATTED "(user record does not exist)" AT 10.
PUT STREAM s-audit-rpt UNFORMATTED
  "(" AT 41 ttbl_header.v_db ")" SKIP(1)
  "Label: Value (database field-name)" AT 3.
IF action = "UPDATE" THEN
PUT STREAM s-audit-rpt UNFORMATTED " ( * - denotes field value change )".
PUT STREAM s-audit-rpt UNFORMATTED FILL("-",75) AT 3.
IF NOT v_field_changed OR ttbl_est-prep1.amtz NE ttbl_est-prep2.amtz OR action NE "UPDATE" THEN
DO:
  PUT STREAM s-audit-rpt UNFORMATTED "Amtz: " AT 3 ttbl_est-prep1.amtz FORMAT ">>9.99" " (amtz)".
  IF action = "UPDATE" THEN
  DO:
    IF ttbl_est-prep1.amtz NE ttbl_est-prep2.amtz THEN
    PUT STREAM s-audit-rpt UNFORMATTED "*" AT 1.
    PUT STREAM s-audit-rpt UNFORMATTED "Amtz: " AT 3 ttbl_est-prep2.amtz FORMAT ">>9.99".
  END.
END.
IF NOT v_field_changed OR ttbl_est-prep1.b-num NE ttbl_est-prep2.b-num OR action NE "UPDATE" THEN
DO:
  PUT STREAM s-audit-rpt UNFORMATTED "Blank #: " AT 3 ttbl_est-prep1.b-num FORMAT ">9" " (b-num)".
  IF action = "UPDATE" THEN
  DO:
    IF ttbl_est-prep1.b-num NE ttbl_est-prep2.b-num THEN
    PUT STREAM s-audit-rpt UNFORMATTED "*" AT 1.
    PUT STREAM s-audit-rpt UNFORMATTED "Blank #: " AT 3 ttbl_est-prep2.b-num FORMAT ">9".
  END.
END.
IF NOT v_field_changed OR ttbl_est-prep1.code NE ttbl_est-prep2.code OR action NE "UPDATE" THEN
DO:
  PUT STREAM s-audit-rpt UNFORMATTED "Code: " AT 3 ttbl_est-prep1.code " (code)".
  IF action = "UPDATE" THEN
  DO:
    IF ttbl_est-prep1.code NE ttbl_est-prep2.code THEN
    PUT STREAM s-audit-rpt UNFORMATTED "*" AT 1.
    PUT STREAM s-audit-rpt UNFORMATTED "Code: " AT 3 ttbl_est-prep2.code.
  END.
END.
IF NOT v_field_changed OR ttbl_est-prep1.company NE ttbl_est-prep2.company OR action NE "UPDATE" THEN
DO:
  PUT STREAM s-audit-rpt UNFORMATTED "Company: " AT 3 ttbl_est-prep1.company " (company)".
  IF action = "UPDATE" THEN
  DO:
    IF ttbl_est-prep1.company NE ttbl_est-prep2.company THEN
    PUT STREAM s-audit-rpt UNFORMATTED "*" AT 1.
    PUT STREAM s-audit-rpt UNFORMATTED "Company: " AT 3 ttbl_est-prep2.company.
  END.
END.
IF NOT v_field_changed OR ttbl_est-prep1.cost NE ttbl_est-prep2.cost OR action NE "UPDATE" THEN
DO:
  PUT STREAM s-audit-rpt UNFORMATTED "Cost: " AT 3 ttbl_est-prep1.cost FORMAT "->>,>>9.99" " (cost)".
  IF action = "UPDATE" THEN
  DO:
    IF ttbl_est-prep1.cost NE ttbl_est-prep2.cost THEN
    PUT STREAM s-audit-rpt UNFORMATTED "*" AT 1.
    PUT STREAM s-audit-rpt UNFORMATTED "Cost: " AT 3 ttbl_est-prep2.cost FORMAT "->>,>>9.99".
  END.
END.
IF NOT v_field_changed OR ttbl_est-prep1.dscr NE ttbl_est-prep2.dscr OR action NE "UPDATE" THEN
DO:
  PUT STREAM s-audit-rpt UNFORMATTED "Description: " AT 3 ttbl_est-prep1.dscr " (dscr)".
  IF action = "UPDATE" THEN
  DO:
    IF ttbl_est-prep1.dscr NE ttbl_est-prep2.dscr THEN
    PUT STREAM s-audit-rpt UNFORMATTED "*" AT 1.
    PUT STREAM s-audit-rpt UNFORMATTED "Description: " AT 3 ttbl_est-prep2.dscr.
  END.
END.
IF NOT v_field_changed OR ttbl_est-prep1.e-num NE ttbl_est-prep2.e-num OR action NE "UPDATE" THEN
DO:
  PUT STREAM s-audit-rpt UNFORMATTED "Est.#: " AT 3 ttbl_est-prep1.e-num FORMAT ">>>>>9" " (e-num)".
  IF action = "UPDATE" THEN
  DO:
    IF ttbl_est-prep1.e-num NE ttbl_est-prep2.e-num THEN
    PUT STREAM s-audit-rpt UNFORMATTED "*" AT 1.
    PUT STREAM s-audit-rpt UNFORMATTED "Est.#: " AT 3 ttbl_est-prep2.e-num FORMAT ">>>>>9".
  END.
END.
IF NOT v_field_changed OR ttbl_est-prep1.eqty NE ttbl_est-prep2.eqty OR action NE "UPDATE" THEN
DO:
  PUT STREAM s-audit-rpt UNFORMATTED "Qty: " AT 3 ttbl_est-prep1.eqty FORMAT "->>,>>9.99" " (eqty)".
  IF action = "UPDATE" THEN
  DO:
    IF ttbl_est-prep1.eqty NE ttbl_est-prep2.eqty THEN
    PUT STREAM s-audit-rpt UNFORMATTED "*" AT 1.
    PUT STREAM s-audit-rpt UNFORMATTED "Qty: " AT 3 ttbl_est-prep2.eqty FORMAT "->>,>>9.99".
  END.
END.
IF NOT v_field_changed OR ttbl_est-prep1.est-no NE ttbl_est-prep2.est-no OR action NE "UPDATE" THEN
DO:
  PUT STREAM s-audit-rpt UNFORMATTED "Estimate #: " AT 3 ttbl_est-prep1.est-no " (est-no)".
  IF action = "UPDATE" THEN
  DO:
    IF ttbl_est-prep1.est-no NE ttbl_est-prep2.est-no THEN
    PUT STREAM s-audit-rpt UNFORMATTED "*" AT 1.
    PUT STREAM s-audit-rpt UNFORMATTED "Estimate #: " AT 3 ttbl_est-prep2.est-no.
  END.
END.
IF NOT v_field_changed OR ttbl_est-prep1.i-no NE ttbl_est-prep2.i-no OR action NE "UPDATE" THEN
DO:
  PUT STREAM s-audit-rpt UNFORMATTED "Item No: " AT 3 ttbl_est-prep1.i-no " (i-no)".
  IF action = "UPDATE" THEN
  DO:
    IF ttbl_est-prep1.i-no NE ttbl_est-prep2.i-no THEN
    PUT STREAM s-audit-rpt UNFORMATTED "*" AT 1.
    PUT STREAM s-audit-rpt UNFORMATTED "Item No: " AT 3 ttbl_est-prep2.i-no.
  END.
END.
IF NOT v_field_changed OR ttbl_est-prep1.line NE ttbl_est-prep2.line OR action NE "UPDATE" THEN
DO:
  PUT STREAM s-audit-rpt UNFORMATTED "Line #: " AT 3 ttbl_est-prep1.line FORMAT ">9" " (line)".
  IF action = "UPDATE" THEN
  DO:
    IF ttbl_est-prep1.line NE ttbl_est-prep2.line THEN
    PUT STREAM s-audit-rpt UNFORMATTED "*" AT 1.
    PUT STREAM s-audit-rpt UNFORMATTED "Line #: " AT 3 ttbl_est-prep2.line FORMAT ">9".
  END.
END.
IF NOT v_field_changed OR ttbl_est-prep1.mat-type NE ttbl_est-prep2.mat-type OR action NE "UPDATE" THEN
DO:
  PUT STREAM s-audit-rpt UNFORMATTED "Mat'l Type: " AT 3 ttbl_est-prep1.mat-type " (mat-type)".
  IF action = "UPDATE" THEN
  DO:
    IF ttbl_est-prep1.mat-type NE ttbl_est-prep2.mat-type THEN
    PUT STREAM s-audit-rpt UNFORMATTED "*" AT 1.
    PUT STREAM s-audit-rpt UNFORMATTED "Mat'l Type: " AT 3 ttbl_est-prep2.mat-type.
  END.
END.
IF NOT v_field_changed OR ttbl_est-prep1.mkup NE ttbl_est-prep2.mkup OR action NE "UPDATE" THEN
DO:
  PUT STREAM s-audit-rpt UNFORMATTED "Markup: " AT 3 ttbl_est-prep1.mkup FORMAT ">>9.99" " (mkup)".
  IF action = "UPDATE" THEN
  DO:
    IF ttbl_est-prep1.mkup NE ttbl_est-prep2.mkup THEN
    PUT STREAM s-audit-rpt UNFORMATTED "*" AT 1.
    PUT STREAM s-audit-rpt UNFORMATTED "Markup: " AT 3 ttbl_est-prep2.mkup FORMAT ">>9.99".
  END.
END.
IF NOT v_field_changed OR ttbl_est-prep1.ml NE ttbl_est-prep2.ml OR action NE "UPDATE" THEN
DO:
  PUT STREAM s-audit-rpt UNFORMATTED "M/L: " AT 3 ttbl_est-prep1.ml FORMAT "M/L" " (ml)".
  IF action = "UPDATE" THEN
  DO:
    IF ttbl_est-prep1.ml NE ttbl_est-prep2.ml THEN
    PUT STREAM s-audit-rpt UNFORMATTED "*" AT 1.
    PUT STREAM s-audit-rpt UNFORMATTED "M/L: " AT 3 ttbl_est-prep2.ml FORMAT "M/L".
  END.
END.
IF NOT v_field_changed OR ttbl_est-prep1.qty NE ttbl_est-prep2.qty OR action NE "UPDATE" THEN
DO:
  PUT STREAM s-audit-rpt UNFORMATTED "Qty: " AT 3 ttbl_est-prep1.qty FORMAT "->>>,>>>,>>9.9<<<<<" " (qty)".
  IF action = "UPDATE" THEN
  DO:
    IF ttbl_est-prep1.qty NE ttbl_est-prep2.qty THEN
    PUT STREAM s-audit-rpt UNFORMATTED "*" AT 1.
    PUT STREAM s-audit-rpt UNFORMATTED "Qty: " AT 3 ttbl_est-prep2.qty FORMAT "->>>,>>>,>>9.9<<<<<".
  END.
END.
IF NOT v_field_changed OR ttbl_est-prep1.s-num NE ttbl_est-prep2.s-num OR action NE "UPDATE" THEN
DO:
  PUT STREAM s-audit-rpt UNFORMATTED "Sheet #: " AT 3 ttbl_est-prep1.s-num FORMAT ">9" " (s-num)".
  IF action = "UPDATE" THEN
  DO:
    IF ttbl_est-prep1.s-num NE ttbl_est-prep2.s-num THEN
    PUT STREAM s-audit-rpt UNFORMATTED "*" AT 1.
    PUT STREAM s-audit-rpt UNFORMATTED "Sheet #: " AT 3 ttbl_est-prep2.s-num FORMAT ">9".
  END.
END.
IF NOT v_field_changed OR ttbl_est-prep1.simon NE ttbl_est-prep2.simon OR action NE "UPDATE" THEN
DO:
  PUT STREAM s-audit-rpt UNFORMATTED "SIMON: " AT 3 ttbl_est-prep1.simon " (simon)".
  IF action = "UPDATE" THEN
  DO:
    IF ttbl_est-prep1.simon NE ttbl_est-prep2.simon THEN
    PUT STREAM s-audit-rpt UNFORMATTED "*" AT 1.
    PUT STREAM s-audit-rpt UNFORMATTED "SIMON: " AT 3 ttbl_est-prep2.simon.
  END.
END.
IF v_show_index THEN
DO:
  PUT STREAM s-audit-rpt UNFORMATTED "Index Fields - Label: Value (database field-name)" AT 5.
  PUT STREAM s-audit-rpt UNFORMATTED "Company: " AT 7.
  IF action = "UPDATE" THEN
  PUT STREAM s-audit-rpt UNFORMATTED ttbl_est-prep2.company " (company)".
  ELSE
  PUT STREAM s-audit-rpt UNFORMATTED ttbl_est-prep1.company " (company)".
  PUT STREAM s-audit-rpt UNFORMATTED "Estimate #: " AT 7.
  IF action = "UPDATE" THEN
  PUT STREAM s-audit-rpt UNFORMATTED ttbl_est-prep2.est-no " (est-no)".
  ELSE
  PUT STREAM s-audit-rpt UNFORMATTED ttbl_est-prep1.est-no " (est-no)".
  PUT STREAM s-audit-rpt UNFORMATTED "Qty: " AT 7.
  IF action = "UPDATE" THEN
  PUT STREAM s-audit-rpt UNFORMATTED ttbl_est-prep2.eqty FORMAT "->>,>>9.99" " (eqty)".
  ELSE
  PUT STREAM s-audit-rpt UNFORMATTED ttbl_est-prep1.eqty FORMAT "->>,>>9.99" " (eqty)".
  PUT STREAM s-audit-rpt UNFORMATTED "Line #: " AT 7.
  IF action = "UPDATE" THEN
  PUT STREAM s-audit-rpt UNFORMATTED ttbl_est-prep2.line FORMAT ">9" " (line)".
  ELSE
  PUT STREAM s-audit-rpt UNFORMATTED ttbl_est-prep1.line FORMAT ">9" " (line)".
END.
PUT STREAM s-audit-rpt UNFORMATTED SKIP(1).
