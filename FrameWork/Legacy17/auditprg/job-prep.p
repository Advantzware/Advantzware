/* job-prep.p - generated 12/09/2005 - 15:42:48 by NoSweat */

/* this program was generated by auditgn1.p and should not be modified */
/* modifications should be made to the auto generation program         */

DEFINE INPUT PARAMETER action AS CHARACTER NO-UNDO.

DEFINE VARIABLE i AS INTEGER NO-UNDO.

{methods/defines/audit.i}
FIND FIRST ttbl_header.

DEFINE WORK-TABLE ttbl_job-prep1 NO-UNDO LIKE job-prep.
DEFINE WORK-TABLE ttbl_job-prep2 NO-UNDO LIKE job-prep.

CREATE ttbl_job-prep1.
IMPORT STREAM s_audit_file ttbl_job-prep1.
CREATE ttbl_job-prep2.
IF action = "UPDATE" THEN
IMPORT STREAM s_audit_file ttbl_job-prep2.

PUT STREAM s-audit-rpt UNFORMATTED
  "Action    User ID/(User Name)            DB.Table/(Database)  Date       Time" SKIP
  "--------- ------------------------------ -------------------- ---------- --------" SKIP
  ttbl_header.v_action AT 1
  ttbl_header.v_userid AT 11
  ttbl_header.v_file AT 42
  ttbl_header.v_date FORMAT "99/99/9999" AT 63
  STRING(ttbl_header.v_time,"HH:MM:SS") AT 74.
FIND users WHERE users.user_id = v_userid NO-LOCK NO-ERROR.
IF AVAILABLE users THEN
PUT STREAM s-audit-rpt UNFORMATTED "(" AT 10 users.user_name ")".
ELSE
PUT STREAM s-audit-rpt UNFORMATTED "(user record does not exist)" AT 10.
PUT STREAM s-audit-rpt UNFORMATTED
  "(" AT 41 ttbl_header.v_db ")" SKIP(1)
  "Label: Value (database field-name)" AT 3.
IF action = "UPDATE" THEN
PUT STREAM s-audit-rpt UNFORMATTED " ( * - denotes field value change )".
PUT STREAM s-audit-rpt UNFORMATTED FILL("-",75) AT 3.
IF NOT v_field_changed OR ttbl_job-prep1.blank-no NE ttbl_job-prep2.blank-no OR action NE "UPDATE" THEN
DO:
  PUT STREAM s-audit-rpt UNFORMATTED "BLANK: " AT 3 ttbl_job-prep1.blank-no FORMAT ">9" " (blank-no)".
  IF action = "UPDATE" THEN
  DO:
    IF ttbl_job-prep1.blank-no NE ttbl_job-prep2.blank-no THEN
    PUT STREAM s-audit-rpt UNFORMATTED "*" AT 1.
    PUT STREAM s-audit-rpt UNFORMATTED "BLANK: " AT 3 ttbl_job-prep2.blank-no FORMAT ">9".
  END.
END.
IF NOT v_field_changed OR ttbl_job-prep1.code NE ttbl_job-prep2.code OR action NE "UPDATE" THEN
DO:
  PUT STREAM s-audit-rpt UNFORMATTED "Code: " AT 3 ttbl_job-prep1.code " (code)".
  IF action = "UPDATE" THEN
  DO:
    IF ttbl_job-prep1.code NE ttbl_job-prep2.code THEN
    PUT STREAM s-audit-rpt UNFORMATTED "*" AT 1.
    PUT STREAM s-audit-rpt UNFORMATTED "Code: " AT 3 ttbl_job-prep2.code.
  END.
END.
IF NOT v_field_changed OR ttbl_job-prep1.company NE ttbl_job-prep2.company OR action NE "UPDATE" THEN
DO:
  PUT STREAM s-audit-rpt UNFORMATTED "Company: " AT 3 ttbl_job-prep1.company " (company)".
  IF action = "UPDATE" THEN
  DO:
    IF ttbl_job-prep1.company NE ttbl_job-prep2.company THEN
    PUT STREAM s-audit-rpt UNFORMATTED "*" AT 1.
    PUT STREAM s-audit-rpt UNFORMATTED "Company: " AT 3 ttbl_job-prep2.company.
  END.
END.
IF NOT v_field_changed OR ttbl_job-prep1.cost-m NE ttbl_job-prep2.cost-m OR action NE "UPDATE" THEN
DO:
  PUT STREAM s-audit-rpt UNFORMATTED "Cost/M: " AT 3 ttbl_job-prep1.cost-m FORMAT "->,>>9.9999" " (cost-m)".
  IF action = "UPDATE" THEN
  DO:
    IF ttbl_job-prep1.cost-m NE ttbl_job-prep2.cost-m THEN
    PUT STREAM s-audit-rpt UNFORMATTED "*" AT 1.
    PUT STREAM s-audit-rpt UNFORMATTED "Cost/M: " AT 3 ttbl_job-prep2.cost-m FORMAT "->,>>9.9999".
  END.
END.
IF NOT v_field_changed OR ttbl_job-prep1.frm NE ttbl_job-prep2.frm OR action NE "UPDATE" THEN
DO:
  PUT STREAM s-audit-rpt UNFORMATTED "Form: " AT 3 ttbl_job-prep1.frm FORMAT ">>9" " (frm)".
  IF action = "UPDATE" THEN
  DO:
    IF ttbl_job-prep1.frm NE ttbl_job-prep2.frm THEN
    PUT STREAM s-audit-rpt UNFORMATTED "*" AT 1.
    PUT STREAM s-audit-rpt UNFORMATTED "Form: " AT 3 ttbl_job-prep2.frm FORMAT ">>9".
  END.
END.
IF NOT v_field_changed OR ttbl_job-prep1.job NE ttbl_job-prep2.job OR action NE "UPDATE" THEN
DO:
  PUT STREAM s-audit-rpt UNFORMATTED "Unique Internial Job Number: " AT 3 ttbl_job-prep1.job FORMAT ">>>>>>9" " (job)".
  IF action = "UPDATE" THEN
  DO:
    IF ttbl_job-prep1.job NE ttbl_job-prep2.job THEN
    PUT STREAM s-audit-rpt UNFORMATTED "*" AT 1.
    PUT STREAM s-audit-rpt UNFORMATTED "Unique Internial Job Number: " AT 3 ttbl_job-prep2.job FORMAT ">>>>>>9".
  END.
END.
IF NOT v_field_changed OR ttbl_job-prep1.job-no NE ttbl_job-prep2.job-no OR action NE "UPDATE" THEN
DO:
  PUT STREAM s-audit-rpt UNFORMATTED "Job#: " AT 3 ttbl_job-prep1.job-no " (job-no)".
  IF action = "UPDATE" THEN
  DO:
    IF ttbl_job-prep1.job-no NE ttbl_job-prep2.job-no THEN
    PUT STREAM s-audit-rpt UNFORMATTED "*" AT 1.
    PUT STREAM s-audit-rpt UNFORMATTED "Job#: " AT 3 ttbl_job-prep2.job-no.
  END.
END.
IF NOT v_field_changed OR ttbl_job-prep1.job-no2 NE ttbl_job-prep2.job-no2 OR action NE "UPDATE" THEN
DO:
  PUT STREAM s-audit-rpt UNFORMATTED "Run #: " AT 3 ttbl_job-prep1.job-no2 FORMAT ">9" " (job-no2)".
  IF action = "UPDATE" THEN
  DO:
    IF ttbl_job-prep1.job-no2 NE ttbl_job-prep2.job-no2 THEN
    PUT STREAM s-audit-rpt UNFORMATTED "*" AT 1.
    PUT STREAM s-audit-rpt UNFORMATTED "Run #: " AT 3 ttbl_job-prep2.job-no2 FORMAT ">9".
  END.
END.
IF NOT v_field_changed OR ttbl_job-prep1.ml NE ttbl_job-prep2.ml OR action NE "UPDATE" THEN
DO:
  PUT STREAM s-audit-rpt UNFORMATTED "M/L: " AT 3 ttbl_job-prep1.ml FORMAT "M/L" " (ml)".
  IF action = "UPDATE" THEN
  DO:
    IF ttbl_job-prep1.ml NE ttbl_job-prep2.ml THEN
    PUT STREAM s-audit-rpt UNFORMATTED "*" AT 1.
    PUT STREAM s-audit-rpt UNFORMATTED "M/L: " AT 3 ttbl_job-prep2.ml FORMAT "M/L".
  END.
END.
IF NOT v_field_changed OR ttbl_job-prep1.opn NE ttbl_job-prep2.opn OR action NE "UPDATE" THEN
DO:
  PUT STREAM s-audit-rpt UNFORMATTED "Open Flg: " AT 3 ttbl_job-prep1.opn FORMAT "yes/no" " (opn)".
  IF action = "UPDATE" THEN
  DO:
    IF ttbl_job-prep1.opn NE ttbl_job-prep2.opn THEN
    PUT STREAM s-audit-rpt UNFORMATTED "*" AT 1.
    PUT STREAM s-audit-rpt UNFORMATTED "Open Flg: " AT 3 ttbl_job-prep2.opn FORMAT "yes/no".
  END.
END.
IF NOT v_field_changed OR ttbl_job-prep1.prep-date NE ttbl_job-prep2.prep-date OR action NE "UPDATE" THEN
DO:
  PUT STREAM s-audit-rpt UNFORMATTED "Transaction Date: " AT 3 ttbl_job-prep1.prep-date FORMAT "99/99/9999" " (prep-date)".
  IF action = "UPDATE" THEN
  DO:
    IF ttbl_job-prep1.prep-date NE ttbl_job-prep2.prep-date THEN
    PUT STREAM s-audit-rpt UNFORMATTED "*" AT 1.
    PUT STREAM s-audit-rpt UNFORMATTED "Transaction Date: " AT 3 ttbl_job-prep2.prep-date FORMAT "99/99/9999".
  END.
END.
IF NOT v_field_changed OR ttbl_job-prep1.prep-time NE ttbl_job-prep2.prep-time OR action NE "UPDATE" THEN
DO:
  PUT STREAM s-audit-rpt UNFORMATTED "Preparation Time: " AT 3 ttbl_job-prep1.prep-time FORMAT "->,>>>,>>9" " (prep-time)".
  IF action = "UPDATE" THEN
  DO:
    IF ttbl_job-prep1.prep-time NE ttbl_job-prep2.prep-time THEN
    PUT STREAM s-audit-rpt UNFORMATTED "*" AT 1.
    PUT STREAM s-audit-rpt UNFORMATTED "Preparation Time: " AT 3 ttbl_job-prep2.prep-time FORMAT "->,>>>,>>9".
  END.
END.
IF NOT v_field_changed OR ttbl_job-prep1.qty NE ttbl_job-prep2.qty OR action NE "UPDATE" THEN
DO:
  PUT STREAM s-audit-rpt UNFORMATTED "Qty: " AT 3 ttbl_job-prep1.qty FORMAT "->>>,>>>,>>9.9<<<<<" " (qty)".
  IF action = "UPDATE" THEN
  DO:
    IF ttbl_job-prep1.qty NE ttbl_job-prep2.qty THEN
    PUT STREAM s-audit-rpt UNFORMATTED "*" AT 1.
    PUT STREAM s-audit-rpt UNFORMATTED "Qty: " AT 3 ttbl_job-prep2.qty FORMAT "->>>,>>>,>>9.9<<<<<".
  END.
END.
IF NOT v_field_changed OR ttbl_job-prep1.sc-uom NE ttbl_job-prep2.sc-uom OR action NE "UPDATE" THEN
DO:
  PUT STREAM s-audit-rpt UNFORMATTED "UOM: " AT 3 ttbl_job-prep1.sc-uom " (sc-uom)".
  IF action = "UPDATE" THEN
  DO:
    IF ttbl_job-prep1.sc-uom NE ttbl_job-prep2.sc-uom THEN
    PUT STREAM s-audit-rpt UNFORMATTED "*" AT 1.
    PUT STREAM s-audit-rpt UNFORMATTED "UOM: " AT 3 ttbl_job-prep2.sc-uom.
  END.
END.
IF NOT v_field_changed OR ttbl_job-prep1.simon NE ttbl_job-prep2.simon OR action NE "UPDATE" THEN
DO:
  PUT STREAM s-audit-rpt UNFORMATTED "SIMON: " AT 3 ttbl_job-prep1.simon " (simon)".
  IF action = "UPDATE" THEN
  DO:
    IF ttbl_job-prep1.simon NE ttbl_job-prep2.simon THEN
    PUT STREAM s-audit-rpt UNFORMATTED "*" AT 1.
    PUT STREAM s-audit-rpt UNFORMATTED "SIMON: " AT 3 ttbl_job-prep2.simon.
  END.
END.
IF NOT v_field_changed OR ttbl_job-prep1.std-cost NE ttbl_job-prep2.std-cost OR action NE "UPDATE" THEN
DO:
  PUT STREAM s-audit-rpt UNFORMATTED "Costs: " AT 3 ttbl_job-prep1.std-cost FORMAT ">>>,>>9.99<<" " (std-cost)".
  IF action = "UPDATE" THEN
  DO:
    IF ttbl_job-prep1.std-cost NE ttbl_job-prep2.std-cost THEN
    PUT STREAM s-audit-rpt UNFORMATTED "*" AT 1.
    PUT STREAM s-audit-rpt UNFORMATTED "Costs: " AT 3 ttbl_job-prep2.std-cost FORMAT ">>>,>>9.99<<".
  END.
END.
IF NOT v_field_changed OR ttbl_job-prep1.sts NE ttbl_job-prep2.sts OR action NE "UPDATE" THEN
DO:
  PUT STREAM s-audit-rpt UNFORMATTED "In/Out: " AT 3 ttbl_job-prep1.sts FORMAT "yes/no" " (sts)".
  IF action = "UPDATE" THEN
  DO:
    IF ttbl_job-prep1.sts NE ttbl_job-prep2.sts THEN
    PUT STREAM s-audit-rpt UNFORMATTED "*" AT 1.
    PUT STREAM s-audit-rpt UNFORMATTED "In/Out: " AT 3 ttbl_job-prep2.sts FORMAT "yes/no".
  END.
END.
IF v_show_index THEN
DO:
  PUT STREAM s-audit-rpt UNFORMATTED "Index Fields - Label: Value (database field-name)" AT 5.
  PUT STREAM s-audit-rpt UNFORMATTED "Company: " AT 7.
  IF action = "UPDATE" THEN
  PUT STREAM s-audit-rpt UNFORMATTED ttbl_job-prep2.company " (company)".
  ELSE
  PUT STREAM s-audit-rpt UNFORMATTED ttbl_job-prep1.company " (company)".
  PUT STREAM s-audit-rpt UNFORMATTED "Unique Internial Job Number: " AT 7.
  IF action = "UPDATE" THEN
  PUT STREAM s-audit-rpt UNFORMATTED ttbl_job-prep2.job FORMAT ">>>>>>9" " (job)".
  ELSE
  PUT STREAM s-audit-rpt UNFORMATTED ttbl_job-prep1.job FORMAT ">>>>>>9" " (job)".
  PUT STREAM s-audit-rpt UNFORMATTED "Form: " AT 7.
  IF action = "UPDATE" THEN
  PUT STREAM s-audit-rpt UNFORMATTED ttbl_job-prep2.frm FORMAT ">>9" " (frm)".
  ELSE
  PUT STREAM s-audit-rpt UNFORMATTED ttbl_job-prep1.frm FORMAT ">>9" " (frm)".
  PUT STREAM s-audit-rpt UNFORMATTED "BLANK: " AT 7.
  IF action = "UPDATE" THEN
  PUT STREAM s-audit-rpt UNFORMATTED ttbl_job-prep2.blank-no FORMAT ">9" " (blank-no)".
  ELSE
  PUT STREAM s-audit-rpt UNFORMATTED ttbl_job-prep1.blank-no FORMAT ">9" " (blank-no)".
  PUT STREAM s-audit-rpt UNFORMATTED "Code: " AT 7.
  IF action = "UPDATE" THEN
  PUT STREAM s-audit-rpt UNFORMATTED ttbl_job-prep2.code " (code)".
  ELSE
  PUT STREAM s-audit-rpt UNFORMATTED ttbl_job-prep1.code " (code)".
END.
PUT STREAM s-audit-rpt UNFORMATTED SKIP(1).
