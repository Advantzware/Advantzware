/* file.p - generated 12/09/2005 - 15:42:45 by NoSweat */

/* this program was generated by auditgn1.p and should not be modified */
/* modifications should be made to the auto generation program         */

DEFINE INPUT PARAMETER action AS CHARACTER NO-UNDO.

DEFINE VARIABLE i AS INTEGER NO-UNDO.

{methods/defines/audit.i}
FIND FIRST ttbl_header.

DEFINE WORK-TABLE ttbl_file1 NO-UNDO LIKE file.
DEFINE WORK-TABLE ttbl_file2 NO-UNDO LIKE file.

CREATE ttbl_file1.
IMPORT STREAM s_audit_file ttbl_file1.
CREATE ttbl_file2.
IF action = "UPDATE" THEN
IMPORT STREAM s_audit_file ttbl_file2.

PUT STREAM s-audit-rpt UNFORMATTED
  "Action    User ID/(User Name)            DB.Table/(Database)  Date       Time" SKIP
  "--------- ------------------------------ -------------------- ---------- --------" SKIP
  ttbl_header.v_action AT 1
  ttbl_header.v_userid AT 11
  ttbl_header.v_file AT 42
  ttbl_header.v_date FORMAT "99/99/9999" AT 63
  STRING(ttbl_header.v_time,"HH:MM:SS") AT 74.
FIND users WHERE users.user_id = v_userid NO-LOCK NO-ERROR.
IF AVAILABLE users THEN
PUT STREAM s-audit-rpt UNFORMATTED "(" AT 10 users.user_name ")".
ELSE
PUT STREAM s-audit-rpt UNFORMATTED "(user record does not exist)" AT 10.
PUT STREAM s-audit-rpt UNFORMATTED
  "(" AT 41 ttbl_header.v_db ")" SKIP(1)
  "Label: Value (database field-name)" AT 3.
IF action = "UPDATE" THEN
PUT STREAM s-audit-rpt UNFORMATTED " ( * - denotes field value change )".
PUT STREAM s-audit-rpt UNFORMATTED FILL("-",75) AT 3.
IF NOT v_field_changed OR ttbl_file1.dscr NE ttbl_file2.dscr OR action NE "UPDATE" THEN
DO:
  PUT STREAM s-audit-rpt UNFORMATTED "Description: " AT 3 ttbl_file1.dscr " (dscr)".
  IF action = "UPDATE" THEN
  DO:
    IF ttbl_file1.dscr NE ttbl_file2.dscr THEN
    PUT STREAM s-audit-rpt UNFORMATTED "*" AT 1.
    PUT STREAM s-audit-rpt UNFORMATTED "Description: " AT 3 ttbl_file2.dscr.
  END.
END.
IF NOT v_field_changed OR ttbl_file1.grp NE ttbl_file2.grp OR action NE "UPDATE" THEN
DO:
  PUT STREAM s-audit-rpt UNFORMATTED "Group: " AT 3 ttbl_file1.grp " (grp)".
  IF action = "UPDATE" THEN
  DO:
    IF ttbl_file1.grp NE ttbl_file2.grp THEN
    PUT STREAM s-audit-rpt UNFORMATTED "*" AT 1.
    PUT STREAM s-audit-rpt UNFORMATTED "Group: " AT 3 ttbl_file2.grp.
  END.
END.
IF NOT v_field_changed OR ttbl_file1.name NE ttbl_file2.name OR action NE "UPDATE" THEN
DO:
  PUT STREAM s-audit-rpt UNFORMATTED "File Name: " AT 3 ttbl_file1.name " (name)".
  IF action = "UPDATE" THEN
  DO:
    IF ttbl_file1.name NE ttbl_file2.name THEN
    PUT STREAM s-audit-rpt UNFORMATTED "*" AT 1.
    PUT STREAM s-audit-rpt UNFORMATTED "File Name: " AT 3 ttbl_file2.name.
  END.
END.
IF NOT v_field_changed OR ttbl_file1.program NE ttbl_file2.program OR action NE "UPDATE" THEN
DO:
  PUT STREAM s-audit-rpt UNFORMATTED "Program: " AT 3 ttbl_file1.program " (program)".
  IF action = "UPDATE" THEN
  DO:
    IF ttbl_file1.program NE ttbl_file2.program THEN
    PUT STREAM s-audit-rpt UNFORMATTED "*" AT 1.
    PUT STREAM s-audit-rpt UNFORMATTED "Program: " AT 3 ttbl_file2.program.
  END.
END.
IF NOT v_field_changed OR ttbl_file1.seq NE ttbl_file2.seq OR action NE "UPDATE" THEN
DO:
  PUT STREAM s-audit-rpt UNFORMATTED "Sequence: " AT 3 ttbl_file1.seq FORMAT ">>9" " (seq)".
  IF action = "UPDATE" THEN
  DO:
    IF ttbl_file1.seq NE ttbl_file2.seq THEN
    PUT STREAM s-audit-rpt UNFORMATTED "*" AT 1.
    PUT STREAM s-audit-rpt UNFORMATTED "Sequence: " AT 3 ttbl_file2.seq FORMAT ">>9".
  END.
END.
IF v_show_index THEN
DO:
  PUT STREAM s-audit-rpt UNFORMATTED "Index Fields - Label: Value (database field-name)" AT 5.
  PUT STREAM s-audit-rpt UNFORMATTED "Sequence: " AT 7.
  IF action = "UPDATE" THEN
  PUT STREAM s-audit-rpt UNFORMATTED ttbl_file2.seq FORMAT ">>9" " (seq)".
  ELSE
  PUT STREAM s-audit-rpt UNFORMATTED ttbl_file1.seq FORMAT ">>9" " (seq)".
  PUT STREAM s-audit-rpt UNFORMATTED "Group: " AT 7.
  IF action = "UPDATE" THEN
  PUT STREAM s-audit-rpt UNFORMATTED ttbl_file2.grp " (grp)".
  ELSE
  PUT STREAM s-audit-rpt UNFORMATTED ttbl_file1.grp " (grp)".
  PUT STREAM s-audit-rpt UNFORMATTED "File Name: " AT 7.
  IF action = "UPDATE" THEN
  PUT STREAM s-audit-rpt UNFORMATTED ttbl_file2.name " (name)".
  ELSE
  PUT STREAM s-audit-rpt UNFORMATTED ttbl_file1.name " (name)".
END.
PUT STREAM s-audit-rpt UNFORMATTED SKIP(1).
