/* sbNote.p - generated 05/22/2017 - 09:19:13 by NoSweat */

/* this program was generated by auditgn1.p and should not be modified */
/* modifications should be made to the auto generation program         */

DEFINE INPUT PARAMETER action AS CHARACTER NO-UNDO.

DEFINE VARIABLE i AS INTEGER NO-UNDO.

{methods/defines/audit.i}
FIND FIRST ttbl_header.

DEFINE WORK-TABLE ttbl_sbNote1 NO-UNDO LIKE sbNote.
DEFINE WORK-TABLE ttbl_sbNote2 NO-UNDO LIKE sbNote.

CREATE ttbl_sbNote1.
IMPORT STREAM s_audit_file ttbl_sbNote1.
CREATE ttbl_sbNote2.
IF action = "UPDATE" THEN
IMPORT STREAM s_audit_file ttbl_sbNote2.

PUT STREAM s-audit-rpt UNFORMATTED
  "Action    User ID/(User Name)            DB.Table/(Database)  Date       Time" SKIP
  "--------- ------------------------------ -------------------- ---------- --------" SKIP
  ttbl_header.v_action AT 1
  ttbl_header.v_userid AT 11
  ttbl_header.v_file AT 42
  ttbl_header.v_date FORMAT "99/99/9999" AT 63
  STRING(ttbl_header.v_time,"HH:MM:SS") AT 74.
FIND users WHERE users.user_id = v_userid NO-LOCK NO-ERROR.
IF AVAILABLE users THEN
PUT STREAM s-audit-rpt UNFORMATTED "(" AT 10 users.user_name ")".
ELSE
PUT STREAM s-audit-rpt UNFORMATTED "(user record does not exist)" AT 10.
PUT STREAM s-audit-rpt UNFORMATTED
  "(" AT 41 ttbl_header.v_db ")" SKIP(1)
  "Label: Value (database field-name)" AT 3.
IF action = "UPDATE" THEN
PUT STREAM s-audit-rpt UNFORMATTED " ( * - denotes field value change )".
PUT STREAM s-audit-rpt UNFORMATTED FILL("-",75) AT 3.
IF NOT v_field_changed OR ttbl_sbNote1.company NE ttbl_sbNote2.company OR action NE "UPDATE" THEN
DO:
  PUT STREAM s-audit-rpt UNFORMATTED "Company: " AT 3 ttbl_sbNote1.company " (company)".
  IF action = "UPDATE" THEN
  DO:
    IF ttbl_sbNote1.company NE ttbl_sbNote2.company THEN
    PUT STREAM s-audit-rpt UNFORMATTED "*" AT 1.
    PUT STREAM s-audit-rpt UNFORMATTED "Company: " AT 3 ttbl_sbNote2.company.
  END.
END.
IF NOT v_field_changed OR ttbl_sbNote1.frm NE ttbl_sbNote2.frm OR action NE "UPDATE" THEN
DO:
  PUT STREAM s-audit-rpt UNFORMATTED "Form: " AT 3 ttbl_sbNote1.frm FORMAT ">>9" " (frm)".
  IF action = "UPDATE" THEN
  DO:
    IF ttbl_sbNote1.frm NE ttbl_sbNote2.frm THEN
    PUT STREAM s-audit-rpt UNFORMATTED "*" AT 1.
    PUT STREAM s-audit-rpt UNFORMATTED "Form: " AT 3 ttbl_sbNote2.frm FORMAT ">>9".
  END.
END.
IF NOT v_field_changed OR ttbl_sbNote1.job-no NE ttbl_sbNote2.job-no OR action NE "UPDATE" THEN
DO:
  PUT STREAM s-audit-rpt UNFORMATTED "Job Number: " AT 3 ttbl_sbNote1.job-no " (job-no)".
  IF action = "UPDATE" THEN
  DO:
    IF ttbl_sbNote1.job-no NE ttbl_sbNote2.job-no THEN
    PUT STREAM s-audit-rpt UNFORMATTED "*" AT 1.
    PUT STREAM s-audit-rpt UNFORMATTED "Job Number: " AT 3 ttbl_sbNote2.job-no.
  END.
END.
IF NOT v_field_changed OR ttbl_sbNote1.job-no2 NE ttbl_sbNote2.job-no2 OR action NE "UPDATE" THEN
DO:
  PUT STREAM s-audit-rpt UNFORMATTED "Run: " AT 3 ttbl_sbNote1.job-no2 FORMAT ">9" " (job-no2)".
  IF action = "UPDATE" THEN
  DO:
    IF ttbl_sbNote1.job-no2 NE ttbl_sbNote2.job-no2 THEN
    PUT STREAM s-audit-rpt UNFORMATTED "*" AT 1.
    PUT STREAM s-audit-rpt UNFORMATTED "Run: " AT 3 ttbl_sbNote2.job-no2 FORMAT ">9".
  END.
END.
IF NOT v_field_changed OR ttbl_sbNote1.jobNote NE ttbl_sbNote2.jobNote OR action NE "UPDATE" THEN
DO:
  PUT STREAM s-audit-rpt UNFORMATTED "Job Note: " AT 3 ttbl_sbNote1.jobNote " (jobNote)".
  IF action = "UPDATE" THEN
  DO:
    IF ttbl_sbNote1.jobNote NE ttbl_sbNote2.jobNote THEN
    PUT STREAM s-audit-rpt UNFORMATTED "*" AT 1.
    PUT STREAM s-audit-rpt UNFORMATTED "Job Note: " AT 3 ttbl_sbNote2.jobNote.
  END.
END.
IF NOT v_field_changed OR ttbl_sbNote1.m-code NE ttbl_sbNote2.m-code OR action NE "UPDATE" THEN
DO:
  PUT STREAM s-audit-rpt UNFORMATTED "Machine: " AT 3 ttbl_sbNote1.m-code " (m-code)".
  IF action = "UPDATE" THEN
  DO:
    IF ttbl_sbNote1.m-code NE ttbl_sbNote2.m-code THEN
    PUT STREAM s-audit-rpt UNFORMATTED "*" AT 1.
    PUT STREAM s-audit-rpt UNFORMATTED "Machine: " AT 3 ttbl_sbNote2.m-code.
  END.
END.
IF NOT v_field_changed OR ttbl_sbNote1.noteDate NE ttbl_sbNote2.noteDate OR action NE "UPDATE" THEN
DO:
  PUT STREAM s-audit-rpt UNFORMATTED "Note Date: " AT 3 ttbl_sbNote1.noteDate FORMAT "99/99/9999" " (noteDate)".
  IF action = "UPDATE" THEN
  DO:
    IF ttbl_sbNote1.noteDate NE ttbl_sbNote2.noteDate THEN
    PUT STREAM s-audit-rpt UNFORMATTED "*" AT 1.
    PUT STREAM s-audit-rpt UNFORMATTED "Note Date: " AT 3 ttbl_sbNote2.noteDate FORMAT "99/99/9999".
  END.
END.
IF NOT v_field_changed OR ttbl_sbNote1.noteTime NE ttbl_sbNote2.noteTime OR action NE "UPDATE" THEN
DO:
  PUT STREAM s-audit-rpt UNFORMATTED "Note Time: " AT 3 ttbl_sbNote1.noteTime FORMAT ">>>>9" " (noteTime)".
  IF action = "UPDATE" THEN
  DO:
    IF ttbl_sbNote1.noteTime NE ttbl_sbNote2.noteTime THEN
    PUT STREAM s-audit-rpt UNFORMATTED "*" AT 1.
    PUT STREAM s-audit-rpt UNFORMATTED "Note Time: " AT 3 ttbl_sbNote2.noteTime FORMAT ">>>>9".
  END.
END.
IF v_show_index THEN
DO:
  PUT STREAM s-audit-rpt UNFORMATTED "Index Fields - Label: Value (database field-name)" AT 5.
  PUT STREAM s-audit-rpt UNFORMATTED "Company: " AT 7.
  IF action = "UPDATE" THEN
  PUT STREAM s-audit-rpt UNFORMATTED ttbl_sbNote2.company " (company)".
  ELSE
  PUT STREAM s-audit-rpt UNFORMATTED ttbl_sbNote1.company " (company)".
  PUT STREAM s-audit-rpt UNFORMATTED "Machine: " AT 7.
  IF action = "UPDATE" THEN
  PUT STREAM s-audit-rpt UNFORMATTED ttbl_sbNote2.m-code " (m-code)".
  ELSE
  PUT STREAM s-audit-rpt UNFORMATTED ttbl_sbNote1.m-code " (m-code)".
  PUT STREAM s-audit-rpt UNFORMATTED "Job Number: " AT 7.
  IF action = "UPDATE" THEN
  PUT STREAM s-audit-rpt UNFORMATTED ttbl_sbNote2.job-no " (job-no)".
  ELSE
  PUT STREAM s-audit-rpt UNFORMATTED ttbl_sbNote1.job-no " (job-no)".
  PUT STREAM s-audit-rpt UNFORMATTED "Run: " AT 7.
  IF action = "UPDATE" THEN
  PUT STREAM s-audit-rpt UNFORMATTED ttbl_sbNote2.job-no2 FORMAT ">9" " (job-no2)".
  ELSE
  PUT STREAM s-audit-rpt UNFORMATTED ttbl_sbNote1.job-no2 FORMAT ">9" " (job-no2)".
  PUT STREAM s-audit-rpt UNFORMATTED "Form: " AT 7.
  IF action = "UPDATE" THEN
  PUT STREAM s-audit-rpt UNFORMATTED ttbl_sbNote2.frm FORMAT ">>9" " (frm)".
  ELSE
  PUT STREAM s-audit-rpt UNFORMATTED ttbl_sbNote1.frm FORMAT ">>9" " (frm)".
  PUT STREAM s-audit-rpt UNFORMATTED "Note Date: " AT 7.
  IF action = "UPDATE" THEN
  PUT STREAM s-audit-rpt UNFORMATTED ttbl_sbNote2.noteDate FORMAT "99/99/9999" " (noteDate)".
  ELSE
  PUT STREAM s-audit-rpt UNFORMATTED ttbl_sbNote1.noteDate FORMAT "99/99/9999" " (noteDate)".
  PUT STREAM s-audit-rpt UNFORMATTED "Note Time: " AT 7.
  IF action = "UPDATE" THEN
  PUT STREAM s-audit-rpt UNFORMATTED ttbl_sbNote2.noteTime FORMAT ">>>>9" " (noteTime)".
  ELSE
  PUT STREAM s-audit-rpt UNFORMATTED ttbl_sbNote1.noteTime FORMAT ">>>>9" " (noteTime)".
END.
PUT STREAM s-audit-rpt UNFORMATTED SKIP(1).
