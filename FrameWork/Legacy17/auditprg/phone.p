/* phone.p - generated 12/02/2005 - 16:51:17 by NoSweat */

/* this program was generated by auditgn1.p and should not be modified */
/* modifications should be made to the auto generation program         */

DEFINE INPUT PARAMETER action AS CHARACTER NO-UNDO.

DEFINE VARIABLE i AS INTEGER NO-UNDO.

{methods/defines/audit.i}
FIND FIRST ttbl_header.

DEFINE WORK-TABLE ttbl_phone1 NO-UNDO LIKE phone.
DEFINE WORK-TABLE ttbl_phone2 NO-UNDO LIKE phone.

CREATE ttbl_phone1.
IMPORT STREAM s_audit_file ttbl_phone1.
CREATE ttbl_phone2.
IF action = "UPDATE" THEN
IMPORT STREAM s_audit_file ttbl_phone2.

PUT STREAM s-audit-rpt UNFORMATTED
  "Action    User ID/(User Name)            DB.Table/(Database)  Date       Time" SKIP
  "--------- ------------------------------ -------------------- ---------- --------" SKIP
  ttbl_header.v_action AT 1
  ttbl_header.v_userid AT 11
  ttbl_header.v_file AT 42
  ttbl_header.v_date FORMAT "99/99/9999" AT 63
  STRING(ttbl_header.v_time,"HH:MM:SS") AT 74.
FIND users WHERE users.user_id = v_userid NO-LOCK NO-ERROR.
IF AVAILABLE users THEN
PUT STREAM s-audit-rpt UNFORMATTED "(" AT 10 users.user_name ")".
ELSE
PUT STREAM s-audit-rpt UNFORMATTED "(user record does not exist)" AT 10.
PUT STREAM s-audit-rpt UNFORMATTED
  "(" AT 41 ttbl_header.v_db ")" SKIP(1)
  "Label: Value (database field-name)" AT 3.
IF action = "UPDATE" THEN
PUT STREAM s-audit-rpt UNFORMATTED " ( * - denotes field value change )".
PUT STREAM s-audit-rpt UNFORMATTED FILL("-",75) AT 3.
IF NOT v_field_changed OR ttbl_phone1.attention NE ttbl_phone2.attention OR action NE "UPDATE" THEN
DO:
  PUT STREAM s-audit-rpt UNFORMATTED "Attention: " AT 3 ttbl_phone1.attention " (attention)".
  IF action = "UPDATE" THEN
  DO:
    IF ttbl_phone1.attention NE ttbl_phone2.attention THEN
    PUT STREAM s-audit-rpt UNFORMATTED "*" AT 1.
    PUT STREAM s-audit-rpt UNFORMATTED "Attention: " AT 3 ttbl_phone2.attention.
  END.
END.
IF NOT v_field_changed OR ttbl_phone1.e_mail NE ttbl_phone2.e_mail OR action NE "UPDATE" THEN
DO:
  PUT STREAM s-audit-rpt UNFORMATTED "e-mail Address: " AT 3 ttbl_phone1.e_mail " (e_mail)".
  IF action = "UPDATE" THEN
  DO:
    IF ttbl_phone1.e_mail NE ttbl_phone2.e_mail THEN
    PUT STREAM s-audit-rpt UNFORMATTED "*" AT 1.
    PUT STREAM s-audit-rpt UNFORMATTED "e-mail Address: " AT 3 ttbl_phone2.e_mail.
  END.
END.
IF NOT v_field_changed OR ttbl_phone1.fax NE ttbl_phone2.fax OR action NE "UPDATE" THEN
DO:
  PUT STREAM s-audit-rpt UNFORMATTED "Fax Number: " AT 3 ttbl_phone1.fax " (fax)".
  IF action = "UPDATE" THEN
  DO:
    IF ttbl_phone1.fax NE ttbl_phone2.fax THEN
    PUT STREAM s-audit-rpt UNFORMATTED "*" AT 1.
    PUT STREAM s-audit-rpt UNFORMATTED "Fax Number: " AT 3 ttbl_phone2.fax.
  END.
END.
IF NOT v_field_changed OR ttbl_phone1.fax_city_code NE ttbl_phone2.fax_city_code OR action NE "UPDATE" THEN
DO:
  PUT STREAM s-audit-rpt UNFORMATTED "City Code: " AT 3 ttbl_phone1.fax_city_code " (fax_city_code)".
  IF action = "UPDATE" THEN
  DO:
    IF ttbl_phone1.fax_city_code NE ttbl_phone2.fax_city_code THEN
    PUT STREAM s-audit-rpt UNFORMATTED "*" AT 1.
    PUT STREAM s-audit-rpt UNFORMATTED "City Code: " AT 3 ttbl_phone2.fax_city_code.
  END.
END.
IF NOT v_field_changed OR ttbl_phone1.fax_ctry_code NE ttbl_phone2.fax_ctry_code OR action NE "UPDATE" THEN
DO:
  PUT STREAM s-audit-rpt UNFORMATTED "Country Code: " AT 3 ttbl_phone1.fax_ctry_code " (fax_ctry_code)".
  IF action = "UPDATE" THEN
  DO:
    IF ttbl_phone1.fax_ctry_code NE ttbl_phone2.fax_ctry_code THEN
    PUT STREAM s-audit-rpt UNFORMATTED "*" AT 1.
    PUT STREAM s-audit-rpt UNFORMATTED "Country Code: " AT 3 ttbl_phone2.fax_ctry_code.
  END.
END.
IF NOT v_field_changed OR ttbl_phone1.phone NE ttbl_phone2.phone OR action NE "UPDATE" THEN
DO:
  PUT STREAM s-audit-rpt UNFORMATTED "Phone Number: " AT 3 ttbl_phone1.phone " (phone)".
  IF action = "UPDATE" THEN
  DO:
    IF ttbl_phone1.phone NE ttbl_phone2.phone THEN
    PUT STREAM s-audit-rpt UNFORMATTED "*" AT 1.
    PUT STREAM s-audit-rpt UNFORMATTED "Phone Number: " AT 3 ttbl_phone2.phone.
  END.
END.
IF NOT v_field_changed OR ttbl_phone1.phone_city_code NE ttbl_phone2.phone_city_code OR action NE "UPDATE" THEN
DO:
  PUT STREAM s-audit-rpt UNFORMATTED "City Code: " AT 3 ttbl_phone1.phone_city_code " (phone_city_code)".
  IF action = "UPDATE" THEN
  DO:
    IF ttbl_phone1.phone_city_code NE ttbl_phone2.phone_city_code THEN
    PUT STREAM s-audit-rpt UNFORMATTED "*" AT 1.
    PUT STREAM s-audit-rpt UNFORMATTED "City Code: " AT 3 ttbl_phone2.phone_city_code.
  END.
END.
IF NOT v_field_changed OR ttbl_phone1.phone_ctry_code NE ttbl_phone2.phone_ctry_code OR action NE "UPDATE" THEN
DO:
  PUT STREAM s-audit-rpt UNFORMATTED "Country Code: " AT 3 ttbl_phone1.phone_ctry_code " (phone_ctry_code)".
  IF action = "UPDATE" THEN
  DO:
    IF ttbl_phone1.phone_ctry_code NE ttbl_phone2.phone_ctry_code THEN
    PUT STREAM s-audit-rpt UNFORMATTED "*" AT 1.
    PUT STREAM s-audit-rpt UNFORMATTED "Country Code: " AT 3 ttbl_phone2.phone_ctry_code.
  END.
END.
IF NOT v_field_changed OR ttbl_phone1.phone_ext NE ttbl_phone2.phone_ext OR action NE "UPDATE" THEN
DO:
  PUT STREAM s-audit-rpt UNFORMATTED "Extension: " AT 3 ttbl_phone1.phone_ext " (phone_ext)".
  IF action = "UPDATE" THEN
  DO:
    IF ttbl_phone1.phone_ext NE ttbl_phone2.phone_ext THEN
    PUT STREAM s-audit-rpt UNFORMATTED "*" AT 1.
    PUT STREAM s-audit-rpt UNFORMATTED "Extension: " AT 3 ttbl_phone2.phone_ext.
  END.
END.
IF NOT v_field_changed OR ttbl_phone1.table_rec_key NE ttbl_phone2.table_rec_key OR action NE "UPDATE" THEN
DO:
  PUT STREAM s-audit-rpt UNFORMATTED "Table Rec Key: " AT 3 ttbl_phone1.table_rec_key " (table_rec_key)".
  IF action = "UPDATE" THEN
  DO:
    IF ttbl_phone1.table_rec_key NE ttbl_phone2.table_rec_key THEN
    PUT STREAM s-audit-rpt UNFORMATTED "*" AT 1.
    PUT STREAM s-audit-rpt UNFORMATTED "Table Rec Key: " AT 3 ttbl_phone2.table_rec_key.
  END.
END.
IF NOT v_field_changed OR ttbl_phone1.titlcode NE ttbl_phone2.titlcode OR action NE "UPDATE" THEN
DO:
  PUT STREAM s-audit-rpt UNFORMATTED "Title Code: " AT 3 ttbl_phone1.titlcode " (titlcode)".
  IF action = "UPDATE" THEN
  DO:
    IF ttbl_phone1.titlcode NE ttbl_phone2.titlcode THEN
    PUT STREAM s-audit-rpt UNFORMATTED "*" AT 1.
    PUT STREAM s-audit-rpt UNFORMATTED "Title Code: " AT 3 ttbl_phone2.titlcode.
  END.
END.
IF v_show_index THEN
DO:
  PUT STREAM s-audit-rpt UNFORMATTED "Index Fields - Label: Value (database field-name)" AT 5.
  PUT STREAM s-audit-rpt UNFORMATTED "Table Rec Key: " AT 7.
  IF action = "UPDATE" THEN
  PUT STREAM s-audit-rpt UNFORMATTED ttbl_phone2.table_rec_key " (table_rec_key)".
  ELSE
  PUT STREAM s-audit-rpt UNFORMATTED ttbl_phone1.table_rec_key " (table_rec_key)".
  PUT STREAM s-audit-rpt UNFORMATTED "Attention: " AT 7.
  IF action = "UPDATE" THEN
  PUT STREAM s-audit-rpt UNFORMATTED ttbl_phone2.attention " (attention)".
  ELSE
  PUT STREAM s-audit-rpt UNFORMATTED ttbl_phone1.attention " (attention)".
END.
PUT STREAM s-audit-rpt UNFORMATTED SKIP(1).
