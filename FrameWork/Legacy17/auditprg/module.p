/* module.p - generated 12/09/2005 - 15:42:49 by NoSweat */

/* this program was generated by auditgn1.p and should not be modified */
/* modifications should be made to the auto generation program         */

DEFINE INPUT PARAMETER action AS CHARACTER NO-UNDO.

DEFINE VARIABLE i AS INTEGER NO-UNDO.

{methods/defines/audit.i}
FIND FIRST ttbl_header.

DEFINE WORK-TABLE ttbl_module1 NO-UNDO LIKE module.
DEFINE WORK-TABLE ttbl_module2 NO-UNDO LIKE module.

CREATE ttbl_module1.
IMPORT STREAM s_audit_file ttbl_module1.
CREATE ttbl_module2.
IF action = "UPDATE" THEN
IMPORT STREAM s_audit_file ttbl_module2.

PUT STREAM s-audit-rpt UNFORMATTED
  "Action    User ID/(User Name)            DB.Table/(Database)  Date       Time" SKIP
  "--------- ------------------------------ -------------------- ---------- --------" SKIP
  ttbl_header.v_action AT 1
  ttbl_header.v_userid AT 11
  ttbl_header.v_file AT 42
  ttbl_header.v_date FORMAT "99/99/9999" AT 63
  STRING(ttbl_header.v_time,"HH:MM:SS") AT 74.
FIND users WHERE users.user_id = v_userid NO-LOCK NO-ERROR.
IF AVAILABLE users THEN
PUT STREAM s-audit-rpt UNFORMATTED "(" AT 10 users.user_name ")".
ELSE
PUT STREAM s-audit-rpt UNFORMATTED "(user record does not exist)" AT 10.
PUT STREAM s-audit-rpt UNFORMATTED
  "(" AT 41 ttbl_header.v_db ")" SKIP(1)
  "Label: Value (database field-name)" AT 3.
IF action = "UPDATE" THEN
PUT STREAM s-audit-rpt UNFORMATTED " ( * - denotes field value change )".
PUT STREAM s-audit-rpt UNFORMATTED FILL("-",75) AT 3.
IF NOT v_field_changed OR ttbl_module1.db-name NE ttbl_module2.db-name OR action NE "UPDATE" THEN
DO:
  PUT STREAM s-audit-rpt UNFORMATTED "Database: " AT 3 ttbl_module1.db-name " (db-name)".
  IF action = "UPDATE" THEN
  DO:
    IF ttbl_module1.db-name NE ttbl_module2.db-name THEN
    PUT STREAM s-audit-rpt UNFORMATTED "*" AT 1.
    PUT STREAM s-audit-rpt UNFORMATTED "Database: " AT 3 ttbl_module2.db-name.
  END.
END.
IF NOT v_field_changed OR ttbl_module1.dscr NE ttbl_module2.dscr OR action NE "UPDATE" THEN
DO:
  PUT STREAM s-audit-rpt UNFORMATTED "Module Description: " AT 3 ttbl_module1.dscr " (dscr)".
  IF action = "UPDATE" THEN
  DO:
    IF ttbl_module1.dscr NE ttbl_module2.dscr THEN
    PUT STREAM s-audit-rpt UNFORMATTED "*" AT 1.
    PUT STREAM s-audit-rpt UNFORMATTED "Module Description: " AT 3 ttbl_module2.dscr.
  END.
END.
IF NOT v_field_changed OR ttbl_module1.expire-date NE ttbl_module2.expire-date OR action NE "UPDATE" THEN
DO:
  PUT STREAM s-audit-rpt UNFORMATTED "Expiration Date: " AT 3 ttbl_module1.expire-date FORMAT "99/99/9999" " (expire-date)".
  IF action = "UPDATE" THEN
  DO:
    IF ttbl_module1.expire-date NE ttbl_module2.expire-date THEN
    PUT STREAM s-audit-rpt UNFORMATTED "*" AT 1.
    PUT STREAM s-audit-rpt UNFORMATTED "Expiration Date: " AT 3 ttbl_module2.expire-date FORMAT "99/99/9999".
  END.
END.
IF NOT v_field_changed OR ttbl_module1.hot-key NE ttbl_module2.hot-key OR action NE "UPDATE" THEN
DO:
  PUT STREAM s-audit-rpt UNFORMATTED "Hot Key: " AT 3 ttbl_module1.hot-key " (hot-key)".
  IF action = "UPDATE" THEN
  DO:
    IF ttbl_module1.hot-key NE ttbl_module2.hot-key THEN
    PUT STREAM s-audit-rpt UNFORMATTED "*" AT 1.
    PUT STREAM s-audit-rpt UNFORMATTED "Hot Key: " AT 3 ttbl_module2.hot-key.
  END.
END.
IF NOT v_field_changed OR ttbl_module1.is-used NE ttbl_module2.is-used OR action NE "UPDATE" THEN
DO:
  PUT STREAM s-audit-rpt UNFORMATTED "Module Used?: " AT 3 ttbl_module1.is-used FORMAT "yes/no" " (is-used)".
  IF action = "UPDATE" THEN
  DO:
    IF ttbl_module1.is-used NE ttbl_module2.is-used THEN
    PUT STREAM s-audit-rpt UNFORMATTED "*" AT 1.
    PUT STREAM s-audit-rpt UNFORMATTED "Module Used?: " AT 3 ttbl_module2.is-used FORMAT "yes/no".
  END.
END.
IF NOT v_field_changed OR ttbl_module1.module NE ttbl_module2.module OR action NE "UPDATE" THEN
DO:
  PUT STREAM s-audit-rpt UNFORMATTED "Module Code: " AT 3 ttbl_module1.module " (module)".
  IF action = "UPDATE" THEN
  DO:
    IF ttbl_module1.module NE ttbl_module2.module THEN
    PUT STREAM s-audit-rpt UNFORMATTED "*" AT 1.
    PUT STREAM s-audit-rpt UNFORMATTED "Module Code: " AT 3 ttbl_module2.module.
  END.
END.
IF v_show_index THEN
DO:
  PUT STREAM s-audit-rpt UNFORMATTED "Index Fields - Label: Value (database field-name)" AT 5.
  PUT STREAM s-audit-rpt UNFORMATTED "Database: " AT 7.
  IF action = "UPDATE" THEN
  PUT STREAM s-audit-rpt UNFORMATTED ttbl_module2.db-name " (db-name)".
  ELSE
  PUT STREAM s-audit-rpt UNFORMATTED ttbl_module1.db-name " (db-name)".
  PUT STREAM s-audit-rpt UNFORMATTED "Module Code: " AT 7.
  IF action = "UPDATE" THEN
  PUT STREAM s-audit-rpt UNFORMATTED ttbl_module2.module " (module)".
  ELSE
  PUT STREAM s-audit-rpt UNFORMATTED ttbl_module1.module " (module)".
END.
PUT STREAM s-audit-rpt UNFORMATTED SKIP(1).
