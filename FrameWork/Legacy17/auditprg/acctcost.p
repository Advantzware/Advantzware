/* acctcost.p - generated 12/09/2005 - 15:42:37 by NoSweat */

/* this program was generated by auditgn1.p and should not be modified */
/* modifications should be made to the auto generation program         */

DEFINE INPUT PARAMETER action AS CHARACTER NO-UNDO.

DEFINE VARIABLE i AS INTEGER NO-UNDO.

{methods/defines/audit.i}
FIND FIRST ttbl_header.

DEFINE WORK-TABLE ttbl_acctcost1 NO-UNDO LIKE acctcost.
DEFINE WORK-TABLE ttbl_acctcost2 NO-UNDO LIKE acctcost.

CREATE ttbl_acctcost1.
IMPORT STREAM s_audit_file ttbl_acctcost1.
CREATE ttbl_acctcost2.
IF action = "UPDATE" THEN
IMPORT STREAM s_audit_file ttbl_acctcost2.

PUT STREAM s-audit-rpt UNFORMATTED
  "Action    User ID/(User Name)            DB.Table/(Database)  Date       Time" SKIP
  "--------- ------------------------------ -------------------- ---------- --------" SKIP
  ttbl_header.v_action AT 1
  ttbl_header.v_userid AT 11
  ttbl_header.v_file AT 42
  ttbl_header.v_date FORMAT "99/99/9999" AT 63
  STRING(ttbl_header.v_time,"HH:MM:SS") AT 74.
FIND users WHERE users.user_id = v_userid NO-LOCK NO-ERROR.
IF AVAILABLE users THEN
PUT STREAM s-audit-rpt UNFORMATTED "(" AT 10 users.user_name ")".
ELSE
PUT STREAM s-audit-rpt UNFORMATTED "(user record does not exist)" AT 10.
PUT STREAM s-audit-rpt UNFORMATTED
  "(" AT 41 ttbl_header.v_db ")" SKIP(1)
  "Label: Value (database field-name)" AT 3.
IF action = "UPDATE" THEN
PUT STREAM s-audit-rpt UNFORMATTED " ( * - denotes field value change )".
PUT STREAM s-audit-rpt UNFORMATTED FILL("-",75) AT 3.
IF NOT v_field_changed OR ttbl_acctcost1.actnum NE ttbl_acctcost2.actnum OR action NE "UPDATE" THEN
DO:
  PUT STREAM s-audit-rpt UNFORMATTED "Account No: " AT 3 ttbl_acctcost1.actnum " (actnum)".
  IF action = "UPDATE" THEN
  DO:
    IF ttbl_acctcost1.actnum NE ttbl_acctcost2.actnum THEN
    PUT STREAM s-audit-rpt UNFORMATTED "*" AT 1.
    PUT STREAM s-audit-rpt UNFORMATTED "Account No: " AT 3 ttbl_acctcost2.actnum.
  END.
END.
IF NOT v_field_changed OR ttbl_acctcost1.c-rate NE ttbl_acctcost2.c-rate OR action NE "UPDATE" THEN
DO:
  PUT STREAM s-audit-rpt UNFORMATTED "Percentage: " AT 3 ttbl_acctcost1.c-rate FORMAT ">>9.99" " (c-rate)".
  IF action = "UPDATE" THEN
  DO:
    IF ttbl_acctcost1.c-rate NE ttbl_acctcost2.c-rate THEN
    PUT STREAM s-audit-rpt UNFORMATTED "*" AT 1.
    PUT STREAM s-audit-rpt UNFORMATTED "Percentage: " AT 3 ttbl_acctcost2.c-rate FORMAT ">>9.99".
  END.
END.
IF NOT v_field_changed OR ttbl_acctcost1.company NE ttbl_acctcost2.company OR action NE "UPDATE" THEN
DO:
  PUT STREAM s-audit-rpt UNFORMATTED "Company: " AT 3 ttbl_acctcost1.company " (company)".
  IF action = "UPDATE" THEN
  DO:
    IF ttbl_acctcost1.company NE ttbl_acctcost2.company THEN
    PUT STREAM s-audit-rpt UNFORMATTED "*" AT 1.
    PUT STREAM s-audit-rpt UNFORMATTED "Company: " AT 3 ttbl_acctcost2.company.
  END.
END.
IF NOT v_field_changed OR ttbl_acctcost1.costacct NE ttbl_acctcost2.costacct OR action NE "UPDATE" THEN
DO:
  PUT STREAM s-audit-rpt UNFORMATTED "Account No: " AT 3 ttbl_acctcost1.costacct " (costacct)".
  IF action = "UPDATE" THEN
  DO:
    IF ttbl_acctcost1.costacct NE ttbl_acctcost2.costacct THEN
    PUT STREAM s-audit-rpt UNFORMATTED "*" AT 1.
    PUT STREAM s-audit-rpt UNFORMATTED "Account No: " AT 3 ttbl_acctcost2.costacct.
  END.
END.
IF NOT v_field_changed OR ttbl_acctcost1.trx-date NE ttbl_acctcost2.trx-date OR action NE "UPDATE" THEN
DO:
  PUT STREAM s-audit-rpt UNFORMATTED "Trx-Date: " AT 3 ttbl_acctcost1.trx-date FORMAT "99/99/9999" " (trx-date)".
  IF action = "UPDATE" THEN
  DO:
    IF ttbl_acctcost1.trx-date NE ttbl_acctcost2.trx-date THEN
    PUT STREAM s-audit-rpt UNFORMATTED "*" AT 1.
    PUT STREAM s-audit-rpt UNFORMATTED "Trx-Date: " AT 3 ttbl_acctcost2.trx-date FORMAT "99/99/9999".
  END.
END.
IF NOT v_field_changed OR ttbl_acctcost1.type NE ttbl_acctcost2.type OR action NE "UPDATE" THEN
DO:
  PUT STREAM s-audit-rpt UNFORMATTED "Type: " AT 3 ttbl_acctcost1.type " (type)".
  IF action = "UPDATE" THEN
  DO:
    IF ttbl_acctcost1.type NE ttbl_acctcost2.type THEN
    PUT STREAM s-audit-rpt UNFORMATTED "*" AT 1.
    PUT STREAM s-audit-rpt UNFORMATTED "Type: " AT 3 ttbl_acctcost2.type.
  END.
END.
IF v_show_index THEN
DO:
  PUT STREAM s-audit-rpt UNFORMATTED "Index Fields - Label: Value (database field-name)" AT 5.
  PUT STREAM s-audit-rpt UNFORMATTED "Company: " AT 7.
  IF action = "UPDATE" THEN
  PUT STREAM s-audit-rpt UNFORMATTED ttbl_acctcost2.company " (company)".
  ELSE
  PUT STREAM s-audit-rpt UNFORMATTED ttbl_acctcost1.company " (company)".
  PUT STREAM s-audit-rpt UNFORMATTED "Account No: " AT 7.
  IF action = "UPDATE" THEN
  PUT STREAM s-audit-rpt UNFORMATTED ttbl_acctcost2.actnum " (actnum)".
  ELSE
  PUT STREAM s-audit-rpt UNFORMATTED ttbl_acctcost1.actnum " (actnum)".
  PUT STREAM s-audit-rpt UNFORMATTED "Account No: " AT 7.
  IF action = "UPDATE" THEN
  PUT STREAM s-audit-rpt UNFORMATTED ttbl_acctcost2.costacct " (costacct)".
  ELSE
  PUT STREAM s-audit-rpt UNFORMATTED ttbl_acctcost1.costacct " (costacct)".
END.
PUT STREAM s-audit-rpt UNFORMATTED SKIP(1).
