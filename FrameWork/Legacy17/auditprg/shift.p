/* shift.p - generated 12/09/2005 - 15:42:54 by NoSweat */

/* this program was generated by auditgn1.p and should not be modified */
/* modifications should be made to the auto generation program         */

DEFINE INPUT PARAMETER action AS CHARACTER NO-UNDO.

DEFINE VARIABLE i AS INTEGER NO-UNDO.

{methods/defines/audit.i}
FIND FIRST ttbl_header.

DEFINE WORK-TABLE ttbl_shift1 NO-UNDO LIKE shift.
DEFINE WORK-TABLE ttbl_shift2 NO-UNDO LIKE shift.

CREATE ttbl_shift1.
IMPORT STREAM s_audit_file ttbl_shift1.
CREATE ttbl_shift2.
IF action = "UPDATE" THEN
IMPORT STREAM s_audit_file ttbl_shift2.

PUT STREAM s-audit-rpt UNFORMATTED
  "Action    User ID/(User Name)            DB.Table/(Database)  Date       Time" SKIP
  "--------- ------------------------------ -------------------- ---------- --------" SKIP
  ttbl_header.v_action AT 1
  ttbl_header.v_userid AT 11
  ttbl_header.v_file AT 42
  ttbl_header.v_date FORMAT "99/99/9999" AT 63
  STRING(ttbl_header.v_time,"HH:MM:SS") AT 74.
FIND users WHERE users.user_id = v_userid NO-LOCK NO-ERROR.
IF AVAILABLE users THEN
PUT STREAM s-audit-rpt UNFORMATTED "(" AT 10 users.user_name ")".
ELSE
PUT STREAM s-audit-rpt UNFORMATTED "(user record does not exist)" AT 10.
PUT STREAM s-audit-rpt UNFORMATTED
  "(" AT 41 ttbl_header.v_db ")" SKIP(1)
  "Label: Value (database field-name)" AT 3.
IF action = "UPDATE" THEN
PUT STREAM s-audit-rpt UNFORMATTED " ( * - denotes field value change )".
PUT STREAM s-audit-rpt UNFORMATTED FILL("-",75) AT 3.
IF NOT v_field_changed OR ttbl_shift1.company NE ttbl_shift2.company OR action NE "UPDATE" THEN
DO:
  PUT STREAM s-audit-rpt UNFORMATTED "?: " AT 3 ttbl_shift1.company " (company)".
  IF action = "UPDATE" THEN
  DO:
    IF ttbl_shift1.company NE ttbl_shift2.company THEN
    PUT STREAM s-audit-rpt UNFORMATTED "*" AT 1.
    PUT STREAM s-audit-rpt UNFORMATTED "?: " AT 3 ttbl_shift2.company.
  END.
END.
IF NOT v_field_changed OR ttbl_shift1.descr NE ttbl_shift2.descr OR action NE "UPDATE" THEN
DO:
  PUT STREAM s-audit-rpt UNFORMATTED "Shift Description: " AT 3 ttbl_shift1.descr " (descr)".
  IF action = "UPDATE" THEN
  DO:
    IF ttbl_shift1.descr NE ttbl_shift2.descr THEN
    PUT STREAM s-audit-rpt UNFORMATTED "*" AT 1.
    PUT STREAM s-audit-rpt UNFORMATTED "Shift Description: " AT 3 ttbl_shift2.descr.
  END.
END.
IF NOT v_field_changed OR ttbl_shift1.end-time NE ttbl_shift2.end-time OR action NE "UPDATE" THEN
DO:
  PUT STREAM s-audit-rpt UNFORMATTED "?: " AT 3 ttbl_shift1.end-time FORMAT "->,>>>,>>9" " (end-time)".
  IF action = "UPDATE" THEN
  DO:
    IF ttbl_shift1.end-time NE ttbl_shift2.end-time THEN
    PUT STREAM s-audit-rpt UNFORMATTED "*" AT 1.
    PUT STREAM s-audit-rpt UNFORMATTED "?: " AT 3 ttbl_shift2.end-time FORMAT "->,>>>,>>9".
  END.
END.
IF NOT v_field_changed OR ttbl_shift1.factor NE ttbl_shift2.factor OR action NE "UPDATE" THEN
DO:
  PUT STREAM s-audit-rpt UNFORMATTED "Shift Differential: " AT 3 ttbl_shift1.factor FORMAT ">>>>9.99<<<" " (factor)".
  IF action = "UPDATE" THEN
  DO:
    IF ttbl_shift1.factor NE ttbl_shift2.factor THEN
    PUT STREAM s-audit-rpt UNFORMATTED "*" AT 1.
    PUT STREAM s-audit-rpt UNFORMATTED "Shift Differential: " AT 3 ttbl_shift2.factor FORMAT ">>>>9.99<<<".
  END.
END.
IF NOT v_field_changed OR ttbl_shift1.shift NE ttbl_shift2.shift OR action NE "UPDATE" THEN
DO:
  PUT STREAM s-audit-rpt UNFORMATTED "Shift: " AT 3 ttbl_shift1.shift FORMAT ">9" " (shift)".
  IF action = "UPDATE" THEN
  DO:
    IF ttbl_shift1.shift NE ttbl_shift2.shift THEN
    PUT STREAM s-audit-rpt UNFORMATTED "*" AT 1.
    PUT STREAM s-audit-rpt UNFORMATTED "Shift: " AT 3 ttbl_shift2.shift FORMAT ">9".
  END.
END.
IF NOT v_field_changed OR ttbl_shift1.start-time NE ttbl_shift2.start-time OR action NE "UPDATE" THEN
DO:
  PUT STREAM s-audit-rpt UNFORMATTED "?: " AT 3 ttbl_shift1.start-time FORMAT "->,>>>,>>9" " (start-time)".
  IF action = "UPDATE" THEN
  DO:
    IF ttbl_shift1.start-time NE ttbl_shift2.start-time THEN
    PUT STREAM s-audit-rpt UNFORMATTED "*" AT 1.
    PUT STREAM s-audit-rpt UNFORMATTED "?: " AT 3 ttbl_shift2.start-time FORMAT "->,>>>,>>9".
  END.
END.
IF v_show_index THEN
DO:
  PUT STREAM s-audit-rpt UNFORMATTED "Index Fields - Label: Value (database field-name)" AT 5.
  PUT STREAM s-audit-rpt UNFORMATTED "?: " AT 7.
  IF action = "UPDATE" THEN
  PUT STREAM s-audit-rpt UNFORMATTED ttbl_shift2.company " (company)".
  ELSE
  PUT STREAM s-audit-rpt UNFORMATTED ttbl_shift1.company " (company)".
  PUT STREAM s-audit-rpt UNFORMATTED "Shift: " AT 7.
  IF action = "UPDATE" THEN
  PUT STREAM s-audit-rpt UNFORMATTED ttbl_shift2.shift FORMAT ">9" " (shift)".
  ELSE
  PUT STREAM s-audit-rpt UNFORMATTED ttbl_shift1.shift FORMAT ">9" " (shift)".
END.
PUT STREAM s-audit-rpt UNFORMATTED SKIP(1).
