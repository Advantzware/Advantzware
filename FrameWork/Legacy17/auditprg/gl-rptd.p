/* gl-rptd.p - generated 12/09/2005 - 15:42:46 by NoSweat */

/* this program was generated by auditgn1.p and should not be modified */
/* modifications should be made to the auto generation program         */

DEFINE INPUT PARAMETER action AS CHARACTER NO-UNDO.

DEFINE VARIABLE i AS INTEGER NO-UNDO.

{methods/defines/audit.i}
FIND FIRST ttbl_header.

DEFINE WORK-TABLE ttbl_gl-rptd1 NO-UNDO LIKE gl-rptd.
DEFINE WORK-TABLE ttbl_gl-rptd2 NO-UNDO LIKE gl-rptd.

CREATE ttbl_gl-rptd1.
IMPORT STREAM s_audit_file ttbl_gl-rptd1.
CREATE ttbl_gl-rptd2.
IF action = "UPDATE" THEN
IMPORT STREAM s_audit_file ttbl_gl-rptd2.

PUT STREAM s-audit-rpt UNFORMATTED
  "Action    User ID/(User Name)            DB.Table/(Database)  Date       Time" SKIP
  "--------- ------------------------------ -------------------- ---------- --------" SKIP
  ttbl_header.v_action AT 1
  ttbl_header.v_userid AT 11
  ttbl_header.v_file AT 42
  ttbl_header.v_date FORMAT "99/99/9999" AT 63
  STRING(ttbl_header.v_time,"HH:MM:SS") AT 74.
FIND users WHERE users.user_id = v_userid NO-LOCK NO-ERROR.
IF AVAILABLE users THEN
PUT STREAM s-audit-rpt UNFORMATTED "(" AT 10 users.user_name ")".
ELSE
PUT STREAM s-audit-rpt UNFORMATTED "(user record does not exist)" AT 10.
PUT STREAM s-audit-rpt UNFORMATTED
  "(" AT 41 ttbl_header.v_db ")" SKIP(1)
  "Label: Value (database field-name)" AT 3.
IF action = "UPDATE" THEN
PUT STREAM s-audit-rpt UNFORMATTED " ( * - denotes field value change )".
PUT STREAM s-audit-rpt UNFORMATTED FILL("-",75) AT 3.
IF NOT v_field_changed OR ttbl_gl-rptd1.actnum NE ttbl_gl-rptd2.actnum OR action NE "UPDATE" THEN
DO:
  PUT STREAM s-audit-rpt UNFORMATTED "Account Code: " AT 3 ttbl_gl-rptd1.actnum " (actnum)".
  IF action = "UPDATE" THEN
  DO:
    IF ttbl_gl-rptd1.actnum NE ttbl_gl-rptd2.actnum THEN
    PUT STREAM s-audit-rpt UNFORMATTED "*" AT 1.
    PUT STREAM s-audit-rpt UNFORMATTED "Account Code: " AT 3 ttbl_gl-rptd2.actnum.
  END.
END.
IF NOT v_field_changed OR ttbl_gl-rptd1.company NE ttbl_gl-rptd2.company OR action NE "UPDATE" THEN
DO:
  PUT STREAM s-audit-rpt UNFORMATTED "Company: " AT 3 ttbl_gl-rptd1.company " (company)".
  IF action = "UPDATE" THEN
  DO:
    IF ttbl_gl-rptd1.company NE ttbl_gl-rptd2.company THEN
    PUT STREAM s-audit-rpt UNFORMATTED "*" AT 1.
    PUT STREAM s-audit-rpt UNFORMATTED "Company: " AT 3 ttbl_gl-rptd2.company.
  END.
END.
IF NOT v_field_changed OR ttbl_gl-rptd1.flag NE ttbl_gl-rptd2.flag OR action NE "UPDATE" THEN
DO:
  PUT STREAM s-audit-rpt UNFORMATTED "Flags: " AT 3 ttbl_gl-rptd1.flag " (flag)".
  IF action = "UPDATE" THEN
  DO:
    IF ttbl_gl-rptd1.flag NE ttbl_gl-rptd2.flag THEN
    PUT STREAM s-audit-rpt UNFORMATTED "*" AT 1.
    PUT STREAM s-audit-rpt UNFORMATTED "Flags: " AT 3 ttbl_gl-rptd2.flag.
  END.
END.
IF NOT v_field_changed OR ttbl_gl-rptd1.fr-actnum NE ttbl_gl-rptd2.fr-actnum OR action NE "UPDATE" THEN
DO:
  PUT STREAM s-audit-rpt UNFORMATTED "From Account No: " AT 3 ttbl_gl-rptd1.fr-actnum " (fr-actnum)".
  IF action = "UPDATE" THEN
  DO:
    IF ttbl_gl-rptd1.fr-actnum NE ttbl_gl-rptd2.fr-actnum THEN
    PUT STREAM s-audit-rpt UNFORMATTED "*" AT 1.
    PUT STREAM s-audit-rpt UNFORMATTED "From Account No: " AT 3 ttbl_gl-rptd2.fr-actnum.
  END.
END.
IF NOT v_field_changed OR ttbl_gl-rptd1.line NE ttbl_gl-rptd2.line OR action NE "UPDATE" THEN
DO:
  PUT STREAM s-audit-rpt UNFORMATTED "Line: " AT 3 ttbl_gl-rptd1.line FORMAT "9999" " (line)".
  IF action = "UPDATE" THEN
  DO:
    IF ttbl_gl-rptd1.line NE ttbl_gl-rptd2.line THEN
    PUT STREAM s-audit-rpt UNFORMATTED "*" AT 1.
    PUT STREAM s-audit-rpt UNFORMATTED "Line: " AT 3 ttbl_gl-rptd2.line FORMAT "9999".
  END.
END.
IF NOT v_field_changed OR ttbl_gl-rptd1.rpt NE ttbl_gl-rptd2.rpt OR action NE "UPDATE" THEN
DO:
  PUT STREAM s-audit-rpt UNFORMATTED "Report ID: " AT 3 ttbl_gl-rptd1.rpt " (rpt)".
  IF action = "UPDATE" THEN
  DO:
    IF ttbl_gl-rptd1.rpt NE ttbl_gl-rptd2.rpt THEN
    PUT STREAM s-audit-rpt UNFORMATTED "*" AT 1.
    PUT STREAM s-audit-rpt UNFORMATTED "Report ID: " AT 3 ttbl_gl-rptd2.rpt.
  END.
END.
IF NOT v_field_changed OR ttbl_gl-rptd1.seq NE ttbl_gl-rptd2.seq OR action NE "UPDATE" THEN
DO:
  PUT STREAM s-audit-rpt UNFORMATTED "Seq#: " AT 3 ttbl_gl-rptd1.seq FORMAT ">>9" " (seq)".
  IF action = "UPDATE" THEN
  DO:
    IF ttbl_gl-rptd1.seq NE ttbl_gl-rptd2.seq THEN
    PUT STREAM s-audit-rpt UNFORMATTED "*" AT 1.
    PUT STREAM s-audit-rpt UNFORMATTED "Seq#: " AT 3 ttbl_gl-rptd2.seq FORMAT ">>9".
  END.
END.
IF NOT v_field_changed OR ttbl_gl-rptd1.to-actnum NE ttbl_gl-rptd2.to-actnum OR action NE "UPDATE" THEN
DO:
  PUT STREAM s-audit-rpt UNFORMATTED "To Account No: " AT 3 ttbl_gl-rptd1.to-actnum " (to-actnum)".
  IF action = "UPDATE" THEN
  DO:
    IF ttbl_gl-rptd1.to-actnum NE ttbl_gl-rptd2.to-actnum THEN
    PUT STREAM s-audit-rpt UNFORMATTED "*" AT 1.
    PUT STREAM s-audit-rpt UNFORMATTED "To Account No: " AT 3 ttbl_gl-rptd2.to-actnum.
  END.
END.
IF v_show_index THEN
DO:
  PUT STREAM s-audit-rpt UNFORMATTED "Index Fields - Label: Value (database field-name)" AT 5.
  PUT STREAM s-audit-rpt UNFORMATTED "Company: " AT 7.
  IF action = "UPDATE" THEN
  PUT STREAM s-audit-rpt UNFORMATTED ttbl_gl-rptd2.company " (company)".
  ELSE
  PUT STREAM s-audit-rpt UNFORMATTED ttbl_gl-rptd1.company " (company)".
  PUT STREAM s-audit-rpt UNFORMATTED "Report ID: " AT 7.
  IF action = "UPDATE" THEN
  PUT STREAM s-audit-rpt UNFORMATTED ttbl_gl-rptd2.rpt " (rpt)".
  ELSE
  PUT STREAM s-audit-rpt UNFORMATTED ttbl_gl-rptd1.rpt " (rpt)".
  PUT STREAM s-audit-rpt UNFORMATTED "Line: " AT 7.
  IF action = "UPDATE" THEN
  PUT STREAM s-audit-rpt UNFORMATTED ttbl_gl-rptd2.line FORMAT "9999" " (line)".
  ELSE
  PUT STREAM s-audit-rpt UNFORMATTED ttbl_gl-rptd1.line FORMAT "9999" " (line)".
  PUT STREAM s-audit-rpt UNFORMATTED "Seq#: " AT 7.
  IF action = "UPDATE" THEN
  PUT STREAM s-audit-rpt UNFORMATTED ttbl_gl-rptd2.seq FORMAT ">>9" " (seq)".
  ELSE
  PUT STREAM s-audit-rpt UNFORMATTED ttbl_gl-rptd1.seq FORMAT ">>9" " (seq)".
END.
PUT STREAM s-audit-rpt UNFORMATTED SKIP(1).
