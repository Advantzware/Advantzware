/* mnu-item.p - generated 12/09/2005 - 15:42:49 by NoSweat */

/* this program was generated by auditgn1.p and should not be modified */
/* modifications should be made to the auto generation program         */

DEFINE INPUT PARAMETER action AS CHARACTER NO-UNDO.

DEFINE VARIABLE i AS INTEGER NO-UNDO.

{methods/defines/audit.i}
FIND FIRST ttbl_header.

DEFINE WORK-TABLE ttbl_mnu-item1 NO-UNDO LIKE mnu-item.
DEFINE WORK-TABLE ttbl_mnu-item2 NO-UNDO LIKE mnu-item.

CREATE ttbl_mnu-item1.
IMPORT STREAM s_audit_file ttbl_mnu-item1.
CREATE ttbl_mnu-item2.
IF action = "UPDATE" THEN
IMPORT STREAM s_audit_file ttbl_mnu-item2.

PUT STREAM s-audit-rpt UNFORMATTED
  "Action    User ID/(User Name)            DB.Table/(Database)  Date       Time" SKIP
  "--------- ------------------------------ -------------------- ---------- --------" SKIP
  ttbl_header.v_action AT 1
  ttbl_header.v_userid AT 11
  ttbl_header.v_file AT 42
  ttbl_header.v_date FORMAT "99/99/9999" AT 63
  STRING(ttbl_header.v_time,"HH:MM:SS") AT 74.
FIND users WHERE users.user_id = v_userid NO-LOCK NO-ERROR.
IF AVAILABLE users THEN
PUT STREAM s-audit-rpt UNFORMATTED "(" AT 10 users.user_name ")".
ELSE
PUT STREAM s-audit-rpt UNFORMATTED "(user record does not exist)" AT 10.
PUT STREAM s-audit-rpt UNFORMATTED
  "(" AT 41 ttbl_header.v_db ")" SKIP(1)
  "Label: Value (database field-name)" AT 3.
IF action = "UPDATE" THEN
PUT STREAM s-audit-rpt UNFORMATTED " ( * - denotes field value change )".
PUT STREAM s-audit-rpt UNFORMATTED FILL("-",75) AT 3.
IF NOT v_field_changed OR ttbl_mnu-item1.descrip NE ttbl_mnu-item2.descrip OR action NE "UPDATE" THEN
DO:
  PUT STREAM s-audit-rpt UNFORMATTED "Program Descrip: " AT 3 ttbl_mnu-item1.descrip " (descrip)".
  IF action = "UPDATE" THEN
  DO:
    IF ttbl_mnu-item1.descrip NE ttbl_mnu-item2.descrip THEN
    PUT STREAM s-audit-rpt UNFORMATTED "*" AT 1.
    PUT STREAM s-audit-rpt UNFORMATTED "Program Descrip: " AT 3 ttbl_mnu-item2.descrip.
  END.
END.
IF NOT v_field_changed OR ttbl_mnu-item1.menu-num NE ttbl_mnu-item2.menu-num OR action NE "UPDATE" THEN
DO:
  PUT STREAM s-audit-rpt UNFORMATTED "Menu Number: " AT 3 ttbl_mnu-item1.menu-num FORMAT ">>>>9" " (menu-num)".
  IF action = "UPDATE" THEN
  DO:
    IF ttbl_mnu-item1.menu-num NE ttbl_mnu-item2.menu-num THEN
    PUT STREAM s-audit-rpt UNFORMATTED "*" AT 1.
    PUT STREAM s-audit-rpt UNFORMATTED "Menu Number: " AT 3 ttbl_mnu-item2.menu-num FORMAT ">>>>9".
  END.
END.
IF NOT v_field_changed OR ttbl_mnu-item1.pgm-name NE ttbl_mnu-item2.pgm-name OR action NE "UPDATE" THEN
DO:
  PUT STREAM s-audit-rpt UNFORMATTED "Program Name: " AT 3 ttbl_mnu-item1.pgm-name " (pgm-name)".
  IF action = "UPDATE" THEN
  DO:
    IF ttbl_mnu-item1.pgm-name NE ttbl_mnu-item2.pgm-name THEN
    PUT STREAM s-audit-rpt UNFORMATTED "*" AT 1.
    PUT STREAM s-audit-rpt UNFORMATTED "Program Name: " AT 3 ttbl_mnu-item2.pgm-name.
  END.
END.
IF NOT v_field_changed OR ttbl_mnu-item1.user-initials NE ttbl_mnu-item2.user-initials OR action NE "UPDATE" THEN
DO:
  PUT STREAM s-audit-rpt UNFORMATTED "Initials: " AT 3 ttbl_mnu-item1.user-initials " (user-initials)".
  IF action = "UPDATE" THEN
  DO:
    IF ttbl_mnu-item1.user-initials NE ttbl_mnu-item2.user-initials THEN
    PUT STREAM s-audit-rpt UNFORMATTED "*" AT 1.
    PUT STREAM s-audit-rpt UNFORMATTED "Initials: " AT 3 ttbl_mnu-item2.user-initials.
  END.
END.
IF v_show_index THEN
DO:
  PUT STREAM s-audit-rpt UNFORMATTED "Index Fields - Label: Value (database field-name)" AT 5.
  PUT STREAM s-audit-rpt UNFORMATTED "Initials: " AT 7.
  IF action = "UPDATE" THEN
  PUT STREAM s-audit-rpt UNFORMATTED ttbl_mnu-item2.user-initials " (user-initials)".
  ELSE
  PUT STREAM s-audit-rpt UNFORMATTED ttbl_mnu-item1.user-initials " (user-initials)".
  PUT STREAM s-audit-rpt UNFORMATTED "Menu Number: " AT 7.
  IF action = "UPDATE" THEN
  PUT STREAM s-audit-rpt UNFORMATTED ttbl_mnu-item2.menu-num FORMAT ">>>>9" " (menu-num)".
  ELSE
  PUT STREAM s-audit-rpt UNFORMATTED ttbl_mnu-item1.menu-num FORMAT ">>>>9" " (menu-num)".
END.
PUT STREAM s-audit-rpt UNFORMATTED SKIP(1).
