/* emailcod.p - generated 12/09/2005 - 15:42:43 by NoSweat */

/* this program was generated by auditgn1.p and should not be modified */
/* modifications should be made to the auto generation program         */

DEFINE INPUT PARAMETER action AS CHARACTER NO-UNDO.

DEFINE VARIABLE i AS INTEGER NO-UNDO.

{methods/defines/audit.i}
FIND FIRST ttbl_header.

DEFINE WORK-TABLE ttbl_emailcod1 NO-UNDO LIKE emailcod.
DEFINE WORK-TABLE ttbl_emailcod2 NO-UNDO LIKE emailcod.

CREATE ttbl_emailcod1.
IMPORT STREAM s_audit_file ttbl_emailcod1.
CREATE ttbl_emailcod2.
IF action = "UPDATE" THEN
IMPORT STREAM s_audit_file ttbl_emailcod2.

PUT STREAM s-audit-rpt UNFORMATTED
  "Action    User ID/(User Name)            DB.Table/(Database)  Date       Time" SKIP
  "--------- ------------------------------ -------------------- ---------- --------" SKIP
  ttbl_header.v_action AT 1
  ttbl_header.v_userid AT 11
  ttbl_header.v_file AT 42
  ttbl_header.v_date FORMAT "99/99/9999" AT 63
  STRING(ttbl_header.v_time,"HH:MM:SS") AT 74.
FIND users WHERE users.user_id = v_userid NO-LOCK NO-ERROR.
IF AVAILABLE users THEN
PUT STREAM s-audit-rpt UNFORMATTED "(" AT 10 users.user_name ")".
ELSE
PUT STREAM s-audit-rpt UNFORMATTED "(user record does not exist)" AT 10.
PUT STREAM s-audit-rpt UNFORMATTED
  "(" AT 41 ttbl_header.v_db ")" SKIP(1)
  "Label: Value (database field-name)" AT 3.
IF action = "UPDATE" THEN
PUT STREAM s-audit-rpt UNFORMATTED " ( * - denotes field value change )".
PUT STREAM s-audit-rpt UNFORMATTED FILL("-",75) AT 3.
IF NOT v_field_changed OR ttbl_emailcod1.description NE ttbl_emailcod2.description OR action NE "UPDATE" THEN
DO:
  PUT STREAM s-audit-rpt UNFORMATTED "Description: " AT 3 ttbl_emailcod1.description " (description)".
  IF action = "UPDATE" THEN
  DO:
    IF ttbl_emailcod1.description NE ttbl_emailcod2.description THEN
    PUT STREAM s-audit-rpt UNFORMATTED "*" AT 1.
    PUT STREAM s-audit-rpt UNFORMATTED "Description: " AT 3 ttbl_emailcod2.description.
  END.
END.
IF NOT v_field_changed OR ttbl_emailcod1.emailcod NE ttbl_emailcod2.emailcod OR action NE "UPDATE" THEN
DO:
  PUT STREAM s-audit-rpt UNFORMATTED "Email Code: " AT 3 ttbl_emailcod1.emailcod " (emailcod)".
  IF action = "UPDATE" THEN
  DO:
    IF ttbl_emailcod1.emailcod NE ttbl_emailcod2.emailcod THEN
    PUT STREAM s-audit-rpt UNFORMATTED "*" AT 1.
    PUT STREAM s-audit-rpt UNFORMATTED "Email Code: " AT 3 ttbl_emailcod2.emailcod.
  END.
END.
IF v_show_index THEN
DO:
  PUT STREAM s-audit-rpt UNFORMATTED "Index Fields - Label: Value (database field-name)" AT 5.
  PUT STREAM s-audit-rpt UNFORMATTED "Email Code: " AT 7.
  IF action = "UPDATE" THEN
  PUT STREAM s-audit-rpt UNFORMATTED ttbl_emailcod2.emailcod " (emailcod)".
  ELSE
  PUT STREAM s-audit-rpt UNFORMATTED ttbl_emailcod1.emailcod " (emailcod)".
END.
PUT STREAM s-audit-rpt UNFORMATTED SKIP(1).
