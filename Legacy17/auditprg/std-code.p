/* std-code.p - generated 12/09/2005 - 15:42:55 by NoSweat */

/* this program was generated by auditgn1.p and should not be modified */
/* modifications should be made to the auto generation program         */

DEFINE INPUT PARAMETER action AS CHARACTER NO-UNDO.

DEFINE VARIABLE i AS INTEGER NO-UNDO.

{methods/defines/audit.i}
FIND FIRST ttbl_header.

DEFINE WORK-TABLE ttbl_std-code1 NO-UNDO LIKE std-code.
DEFINE WORK-TABLE ttbl_std-code2 NO-UNDO LIKE std-code.

CREATE ttbl_std-code1.
IMPORT STREAM s_audit_file ttbl_std-code1.
CREATE ttbl_std-code2.
IF action = "UPDATE" THEN
IMPORT STREAM s_audit_file ttbl_std-code2.

PUT STREAM s-audit-rpt UNFORMATTED
  "Action    User ID/(User Name)            DB.Table/(Database)  Date       Time" SKIP
  "--------- ------------------------------ -------------------- ---------- --------" SKIP
  ttbl_header.v_action AT 1
  ttbl_header.v_userid AT 11
  ttbl_header.v_file AT 42
  ttbl_header.v_date FORMAT "99/99/9999" AT 63
  STRING(ttbl_header.v_time,"HH:MM:SS") AT 74.
FIND users WHERE users.user_id = v_userid NO-LOCK NO-ERROR.
IF AVAILABLE users THEN
PUT STREAM s-audit-rpt UNFORMATTED "(" AT 10 users.user_name ")".
ELSE
PUT STREAM s-audit-rpt UNFORMATTED "(user record does not exist)" AT 10.
PUT STREAM s-audit-rpt UNFORMATTED
  "(" AT 41 ttbl_header.v_db ")" SKIP(1)
  "Label: Value (database field-name)" AT 3.
IF action = "UPDATE" THEN
PUT STREAM s-audit-rpt UNFORMATTED " ( * - denotes field value change )".
PUT STREAM s-audit-rpt UNFORMATTED FILL("-",75) AT 3.
IF NOT v_field_changed OR ttbl_std-code1.code NE ttbl_std-code2.code OR action NE "UPDATE" THEN
DO:
  PUT STREAM s-audit-rpt UNFORMATTED "Code: " AT 3 ttbl_std-code1.code " (code)".
  IF action = "UPDATE" THEN
  DO:
    IF ttbl_std-code1.code NE ttbl_std-code2.code THEN
    PUT STREAM s-audit-rpt UNFORMATTED "*" AT 1.
    PUT STREAM s-audit-rpt UNFORMATTED "Code: " AT 3 ttbl_std-code2.code.
  END.
END.
IF NOT v_field_changed OR ttbl_std-code1.company NE ttbl_std-code2.company OR action NE "UPDATE" THEN
DO:
  PUT STREAM s-audit-rpt UNFORMATTED "Company: " AT 3 ttbl_std-code1.company " (company)".
  IF action = "UPDATE" THEN
  DO:
    IF ttbl_std-code1.company NE ttbl_std-code2.company THEN
    PUT STREAM s-audit-rpt UNFORMATTED "*" AT 1.
    PUT STREAM s-audit-rpt UNFORMATTED "Company: " AT 3 ttbl_std-code2.company.
  END.
END.
IF NOT v_field_changed OR ttbl_std-code1.dscr NE ttbl_std-code2.dscr OR action NE "UPDATE" THEN
DO:
  PUT STREAM s-audit-rpt UNFORMATTED "Description: " AT 3 ttbl_std-code1.dscr " (dscr)".
  IF action = "UPDATE" THEN
  DO:
    IF ttbl_std-code1.dscr NE ttbl_std-code2.dscr THEN
    PUT STREAM s-audit-rpt UNFORMATTED "*" AT 1.
    PUT STREAM s-audit-rpt UNFORMATTED "Description: " AT 3 ttbl_std-code2.dscr.
  END.
END.
IF NOT v_field_changed OR ttbl_std-code1.fil NE ttbl_std-code2.fil OR action NE "UPDATE" THEN
DO:
  PUT STREAM s-audit-rpt UNFORMATTED "File: " AT 3 ttbl_std-code1.fil " (fil)".
  IF action = "UPDATE" THEN
  DO:
    IF ttbl_std-code1.fil NE ttbl_std-code2.fil THEN
    PUT STREAM s-audit-rpt UNFORMATTED "*" AT 1.
    PUT STREAM s-audit-rpt UNFORMATTED "File: " AT 3 ttbl_std-code2.fil.
  END.
END.
IF NOT v_field_changed OR ttbl_std-code1.fld NE ttbl_std-code2.fld OR action NE "UPDATE" THEN
DO:
  PUT STREAM s-audit-rpt UNFORMATTED "Field: " AT 3 ttbl_std-code1.fld " (fld)".
  IF action = "UPDATE" THEN
  DO:
    IF ttbl_std-code1.fld NE ttbl_std-code2.fld THEN
    PUT STREAM s-audit-rpt UNFORMATTED "*" AT 1.
    PUT STREAM s-audit-rpt UNFORMATTED "Field: " AT 3 ttbl_std-code2.fld.
  END.
END.
IF v_show_index THEN
DO:
  PUT STREAM s-audit-rpt UNFORMATTED "Index Fields - Label: Value (database field-name)" AT 5.
  PUT STREAM s-audit-rpt UNFORMATTED "Company: " AT 7.
  IF action = "UPDATE" THEN
  PUT STREAM s-audit-rpt UNFORMATTED ttbl_std-code2.company " (company)".
  ELSE
  PUT STREAM s-audit-rpt UNFORMATTED ttbl_std-code1.company " (company)".
  PUT STREAM s-audit-rpt UNFORMATTED "Code: " AT 7.
  IF action = "UPDATE" THEN
  PUT STREAM s-audit-rpt UNFORMATTED ttbl_std-code2.code " (code)".
  ELSE
  PUT STREAM s-audit-rpt UNFORMATTED ttbl_std-code1.code " (code)".
END.
PUT STREAM s-audit-rpt UNFORMATTED SKIP(1).
