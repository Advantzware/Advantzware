/* rm-bin.p - generated 12/09/2005 - 15:42:53 by NoSweat */

/* this program was generated by auditgn1.p and should not be modified */
/* modifications should be made to the auto generation program         */

DEFINE INPUT PARAMETER action AS CHARACTER NO-UNDO.

DEFINE VARIABLE i AS INTEGER NO-UNDO.

{methods/defines/audit.i}
FIND FIRST ttbl_header.

DEFINE WORK-TABLE ttbl_rm-bin1 NO-UNDO LIKE rm-bin.
DEFINE WORK-TABLE ttbl_rm-bin2 NO-UNDO LIKE rm-bin.

CREATE ttbl_rm-bin1.
IMPORT STREAM s_audit_file ttbl_rm-bin1.
CREATE ttbl_rm-bin2.
IF action = "UPDATE" THEN
IMPORT STREAM s_audit_file ttbl_rm-bin2.

PUT STREAM s-audit-rpt UNFORMATTED
  "Action    User ID/(User Name)            DB.Table/(Database)  Date       Time" SKIP
  "--------- ------------------------------ -------------------- ---------- --------" SKIP
  ttbl_header.v_action AT 1
  ttbl_header.v_userid AT 11
  ttbl_header.v_file AT 42
  ttbl_header.v_date FORMAT "99/99/9999" AT 63
  STRING(ttbl_header.v_time,"HH:MM:SS") AT 74.
FIND users WHERE users.user_id = v_userid NO-LOCK NO-ERROR.
IF AVAILABLE users THEN
PUT STREAM s-audit-rpt UNFORMATTED "(" AT 10 users.user_name ")".
ELSE
PUT STREAM s-audit-rpt UNFORMATTED "(user record does not exist)" AT 10.
PUT STREAM s-audit-rpt UNFORMATTED
  "(" AT 41 ttbl_header.v_db ")" SKIP(1)
  "Label: Value (database field-name)" AT 3.
IF action = "UPDATE" THEN
PUT STREAM s-audit-rpt UNFORMATTED " ( * - denotes field value change )".
PUT STREAM s-audit-rpt UNFORMATTED FILL("-",75) AT 3.
IF NOT v_field_changed OR ttbl_rm-bin1.company NE ttbl_rm-bin2.company OR action NE "UPDATE" THEN
DO:
  PUT STREAM s-audit-rpt UNFORMATTED "Company: " AT 3 ttbl_rm-bin1.company " (company)".
  IF action = "UPDATE" THEN
  DO:
    IF ttbl_rm-bin1.company NE ttbl_rm-bin2.company THEN
    PUT STREAM s-audit-rpt UNFORMATTED "*" AT 1.
    PUT STREAM s-audit-rpt UNFORMATTED "Company: " AT 3 ttbl_rm-bin2.company.
  END.
END.
IF NOT v_field_changed OR ttbl_rm-bin1.cost NE ttbl_rm-bin2.cost OR action NE "UPDATE" THEN
DO:
  PUT STREAM s-audit-rpt UNFORMATTED "Costs: " AT 3 ttbl_rm-bin1.cost FORMAT "->>>,>>9.99<<<<" " (cost)".
  IF action = "UPDATE" THEN
  DO:
    IF ttbl_rm-bin1.cost NE ttbl_rm-bin2.cost THEN
    PUT STREAM s-audit-rpt UNFORMATTED "*" AT 1.
    PUT STREAM s-audit-rpt UNFORMATTED "Costs: " AT 3 ttbl_rm-bin2.cost FORMAT "->>>,>>9.99<<<<".
  END.
END.
IF NOT v_field_changed OR ttbl_rm-bin1.cust-no NE ttbl_rm-bin2.cust-no OR action NE "UPDATE" THEN
DO:
  PUT STREAM s-audit-rpt UNFORMATTED "Customer: " AT 3 ttbl_rm-bin1.cust-no " (cust-no)".
  IF action = "UPDATE" THEN
  DO:
    IF ttbl_rm-bin1.cust-no NE ttbl_rm-bin2.cust-no THEN
    PUT STREAM s-audit-rpt UNFORMATTED "*" AT 1.
    PUT STREAM s-audit-rpt UNFORMATTED "Customer: " AT 3 ttbl_rm-bin2.cust-no.
  END.
END.
IF NOT v_field_changed OR ttbl_rm-bin1.diameter NE ttbl_rm-bin2.diameter OR action NE "UPDATE" THEN
DO:
  PUT STREAM s-audit-rpt UNFORMATTED "Starting Outside Diameter: " AT 3 ttbl_rm-bin1.diameter FORMAT ">>>>>9.99<<<<" " (diameter)".
  IF action = "UPDATE" THEN
  DO:
    IF ttbl_rm-bin1.diameter NE ttbl_rm-bin2.diameter THEN
    PUT STREAM s-audit-rpt UNFORMATTED "*" AT 1.
    PUT STREAM s-audit-rpt UNFORMATTED "Starting Outside Diameter: " AT 3 ttbl_rm-bin2.diameter FORMAT ">>>>>9.99<<<<".
  END.
END.
IF NOT v_field_changed OR ttbl_rm-bin1.i-no NE ttbl_rm-bin2.i-no OR action NE "UPDATE" THEN
DO:
  PUT STREAM s-audit-rpt UNFORMATTED "Item No: " AT 3 ttbl_rm-bin1.i-no " (i-no)".
  IF action = "UPDATE" THEN
  DO:
    IF ttbl_rm-bin1.i-no NE ttbl_rm-bin2.i-no THEN
    PUT STREAM s-audit-rpt UNFORMATTED "*" AT 1.
    PUT STREAM s-audit-rpt UNFORMATTED "Item No: " AT 3 ttbl_rm-bin2.i-no.
  END.
END.
IF NOT v_field_changed OR ttbl_rm-bin1.in-diameter NE ttbl_rm-bin2.in-diameter OR action NE "UPDATE" THEN
DO:
  PUT STREAM s-audit-rpt UNFORMATTED "Inside Diameter: " AT 3 ttbl_rm-bin1.in-diameter FORMAT ">>>>>9.99<<<<" " (in-diameter)".
  IF action = "UPDATE" THEN
  DO:
    IF ttbl_rm-bin1.in-diameter NE ttbl_rm-bin2.in-diameter THEN
    PUT STREAM s-audit-rpt UNFORMATTED "*" AT 1.
    PUT STREAM s-audit-rpt UNFORMATTED "Inside Diameter: " AT 3 ttbl_rm-bin2.in-diameter FORMAT ">>>>>9.99<<<<".
  END.
END.
IF NOT v_field_changed OR ttbl_rm-bin1.loc NE ttbl_rm-bin2.loc OR action NE "UPDATE" THEN
DO:
  PUT STREAM s-audit-rpt UNFORMATTED "Warehouse: " AT 3 ttbl_rm-bin1.loc " (loc)".
  IF action = "UPDATE" THEN
  DO:
    IF ttbl_rm-bin1.loc NE ttbl_rm-bin2.loc THEN
    PUT STREAM s-audit-rpt UNFORMATTED "*" AT 1.
    PUT STREAM s-audit-rpt UNFORMATTED "Warehouse: " AT 3 ttbl_rm-bin2.loc.
  END.
END.
IF NOT v_field_changed OR ttbl_rm-bin1.loc-bin NE ttbl_rm-bin2.loc-bin OR action NE "UPDATE" THEN
DO:
  PUT STREAM s-audit-rpt UNFORMATTED "Primary Bin Loc.: " AT 3 ttbl_rm-bin1.loc-bin " (loc-bin)".
  IF action = "UPDATE" THEN
  DO:
    IF ttbl_rm-bin1.loc-bin NE ttbl_rm-bin2.loc-bin THEN
    PUT STREAM s-audit-rpt UNFORMATTED "*" AT 1.
    PUT STREAM s-audit-rpt UNFORMATTED "Primary Bin Loc.: " AT 3 ttbl_rm-bin2.loc-bin.
  END.
END.
IF NOT v_field_changed OR ttbl_rm-bin1.misc1 NE ttbl_rm-bin2.misc1 OR action NE "UPDATE" THEN
DO:
  PUT STREAM s-audit-rpt UNFORMATTED "Misc Info: " AT 3 ttbl_rm-bin1.misc1 " (misc1)".
  IF action = "UPDATE" THEN
  DO:
    IF ttbl_rm-bin1.misc1 NE ttbl_rm-bin2.misc1 THEN
    PUT STREAM s-audit-rpt UNFORMATTED "*" AT 1.
    PUT STREAM s-audit-rpt UNFORMATTED "Misc Info: " AT 3 ttbl_rm-bin2.misc1.
  END.
END.
IF NOT v_field_changed OR ttbl_rm-bin1.misc2 NE ttbl_rm-bin2.misc2 OR action NE "UPDATE" THEN
DO:
  PUT STREAM s-audit-rpt UNFORMATTED "?: " AT 3 ttbl_rm-bin1.misc2 FORMAT "->>,>>9.99" " (misc2)".
  IF action = "UPDATE" THEN
  DO:
    IF ttbl_rm-bin1.misc2 NE ttbl_rm-bin2.misc2 THEN
    PUT STREAM s-audit-rpt UNFORMATTED "*" AT 1.
    PUT STREAM s-audit-rpt UNFORMATTED "?: " AT 3 ttbl_rm-bin2.misc2 FORMAT "->>,>>9.99".
  END.
END.
IF NOT v_field_changed OR ttbl_rm-bin1.qty NE ttbl_rm-bin2.qty OR action NE "UPDATE" THEN
DO:
  PUT STREAM s-audit-rpt UNFORMATTED "Qty: " AT 3 ttbl_rm-bin1.qty FORMAT ">>>,>>9.9<<<<<" " (qty)".
  IF action = "UPDATE" THEN
  DO:
    IF ttbl_rm-bin1.qty NE ttbl_rm-bin2.qty THEN
    PUT STREAM s-audit-rpt UNFORMATTED "*" AT 1.
    PUT STREAM s-audit-rpt UNFORMATTED "Qty: " AT 3 ttbl_rm-bin2.qty FORMAT ">>>,>>9.9<<<<<".
  END.
END.
IF NOT v_field_changed OR ttbl_rm-bin1.roll-lf NE ttbl_rm-bin2.roll-lf OR action NE "UPDATE" THEN
DO:
  PUT STREAM s-audit-rpt UNFORMATTED "Roll LF: " AT 3 ttbl_rm-bin1.roll-lf FORMAT "->>,>>9.99<<<" " (roll-lf)".
  IF action = "UPDATE" THEN
  DO:
    IF ttbl_rm-bin1.roll-lf NE ttbl_rm-bin2.roll-lf THEN
    PUT STREAM s-audit-rpt UNFORMATTED "*" AT 1.
    PUT STREAM s-audit-rpt UNFORMATTED "Roll LF: " AT 3 ttbl_rm-bin2.roll-lf FORMAT "->>,>>9.99<<<".
  END.
END.
IF NOT v_field_changed OR ttbl_rm-bin1.roll-wt NE ttbl_rm-bin2.roll-wt OR action NE "UPDATE" THEN
DO:
  PUT STREAM s-audit-rpt UNFORMATTED "Roll Weight: " AT 3 ttbl_rm-bin1.roll-wt FORMAT "->>,>>9.99" " (roll-wt)".
  IF action = "UPDATE" THEN
  DO:
    IF ttbl_rm-bin1.roll-wt NE ttbl_rm-bin2.roll-wt THEN
    PUT STREAM s-audit-rpt UNFORMATTED "*" AT 1.
    PUT STREAM s-audit-rpt UNFORMATTED "Roll Weight: " AT 3 ttbl_rm-bin2.roll-wt FORMAT "->>,>>9.99".
  END.
END.
IF NOT v_field_changed OR ttbl_rm-bin1.tag NE ttbl_rm-bin2.tag OR action NE "UPDATE" THEN
DO:
  PUT STREAM s-audit-rpt UNFORMATTED "?: " AT 3 ttbl_rm-bin1.tag " (tag)".
  IF action = "UPDATE" THEN
  DO:
    IF ttbl_rm-bin1.tag NE ttbl_rm-bin2.tag THEN
    PUT STREAM s-audit-rpt UNFORMATTED "*" AT 1.
    PUT STREAM s-audit-rpt UNFORMATTED "?: " AT 3 ttbl_rm-bin2.tag.
  END.
END.
IF v_show_index THEN
DO:
  PUT STREAM s-audit-rpt UNFORMATTED "Index Fields - Label: Value (database field-name)" AT 5.
  PUT STREAM s-audit-rpt UNFORMATTED "Company: " AT 7.
  IF action = "UPDATE" THEN
  PUT STREAM s-audit-rpt UNFORMATTED ttbl_rm-bin2.company " (company)".
  ELSE
  PUT STREAM s-audit-rpt UNFORMATTED ttbl_rm-bin1.company " (company)".
  PUT STREAM s-audit-rpt UNFORMATTED "Warehouse: " AT 7.
  IF action = "UPDATE" THEN
  PUT STREAM s-audit-rpt UNFORMATTED ttbl_rm-bin2.loc " (loc)".
  ELSE
  PUT STREAM s-audit-rpt UNFORMATTED ttbl_rm-bin1.loc " (loc)".
  PUT STREAM s-audit-rpt UNFORMATTED "Item No: " AT 7.
  IF action = "UPDATE" THEN
  PUT STREAM s-audit-rpt UNFORMATTED ttbl_rm-bin2.i-no " (i-no)".
  ELSE
  PUT STREAM s-audit-rpt UNFORMATTED ttbl_rm-bin1.i-no " (i-no)".
  PUT STREAM s-audit-rpt UNFORMATTED "Primary Bin Loc.: " AT 7.
  IF action = "UPDATE" THEN
  PUT STREAM s-audit-rpt UNFORMATTED ttbl_rm-bin2.loc-bin " (loc-bin)".
  ELSE
  PUT STREAM s-audit-rpt UNFORMATTED ttbl_rm-bin1.loc-bin " (loc-bin)".
  PUT STREAM s-audit-rpt UNFORMATTED "?: " AT 7.
  IF action = "UPDATE" THEN
  PUT STREAM s-audit-rpt UNFORMATTED ttbl_rm-bin2.tag " (tag)".
  ELSE
  PUT STREAM s-audit-rpt UNFORMATTED ttbl_rm-bin1.tag " (tag)".
END.
PUT STREAM s-audit-rpt UNFORMATTED SKIP(1).
