/* test-red.p - generated 12/09/2005 - 15:42:55 by NoSweat */

/* this program was generated by auditgn1.p and should not be modified */
/* modifications should be made to the auto generation program         */

DEFINE INPUT PARAMETER action AS CHARACTER NO-UNDO.

DEFINE VARIABLE i AS INTEGER NO-UNDO.

{methods/defines/audit.i}
FIND FIRST ttbl_header.

DEFINE WORK-TABLE ttbl_test-red1 NO-UNDO LIKE test-red.
DEFINE WORK-TABLE ttbl_test-red2 NO-UNDO LIKE test-red.

CREATE ttbl_test-red1.
IMPORT STREAM s_audit_file ttbl_test-red1.
CREATE ttbl_test-red2.
IF action = "UPDATE" THEN
IMPORT STREAM s_audit_file ttbl_test-red2.

PUT STREAM s-audit-rpt UNFORMATTED
  "Action    User ID/(User Name)            DB.Table/(Database)  Date       Time" SKIP
  "--------- ------------------------------ -------------------- ---------- --------" SKIP
  ttbl_header.v_action AT 1
  ttbl_header.v_userid AT 11
  ttbl_header.v_file AT 42
  ttbl_header.v_date FORMAT "99/99/9999" AT 63
  STRING(ttbl_header.v_time,"HH:MM:SS") AT 74.
FIND users WHERE users.user_id = v_userid NO-LOCK NO-ERROR.
IF AVAILABLE users THEN
PUT STREAM s-audit-rpt UNFORMATTED "(" AT 10 users.user_name ")".
ELSE
PUT STREAM s-audit-rpt UNFORMATTED "(user record does not exist)" AT 10.
PUT STREAM s-audit-rpt UNFORMATTED
  "(" AT 41 ttbl_header.v_db ")" SKIP(1)
  "Label: Value (database field-name)" AT 3.
IF action = "UPDATE" THEN
PUT STREAM s-audit-rpt UNFORMATTED " ( * - denotes field value change )".
PUT STREAM s-audit-rpt UNFORMATTED FILL("-",75) AT 3.
IF NOT v_field_changed OR ttbl_test-red1.code NE ttbl_test-red2.code OR action NE "UPDATE" THEN
DO:
  PUT STREAM s-audit-rpt UNFORMATTED "?: " AT 3 ttbl_test-red1.code " (code)".
  IF action = "UPDATE" THEN
  DO:
    IF ttbl_test-red1.code NE ttbl_test-red2.code THEN
    PUT STREAM s-audit-rpt UNFORMATTED "*" AT 1.
    PUT STREAM s-audit-rpt UNFORMATTED "?: " AT 3 ttbl_test-red2.code.
  END.
END.
IF NOT v_field_changed OR ttbl_test-red1.code1 NE ttbl_test-red2.code1 OR action NE "UPDATE" THEN
DO:
  PUT STREAM s-audit-rpt UNFORMATTED "?: " AT 3 ttbl_test-red1.code1 " (code1)".
  IF action = "UPDATE" THEN
  DO:
    IF ttbl_test-red1.code1 NE ttbl_test-red2.code1 THEN
    PUT STREAM s-audit-rpt UNFORMATTED "*" AT 1.
    PUT STREAM s-audit-rpt UNFORMATTED "?: " AT 3 ttbl_test-red2.code1.
  END.
END.
IF NOT v_field_changed OR ttbl_test-red1.code2 NE ttbl_test-red2.code2 OR action NE "UPDATE" THEN
DO:
  PUT STREAM s-audit-rpt UNFORMATTED "?: " AT 3 ttbl_test-red1.code2 " (code2)".
  IF action = "UPDATE" THEN
  DO:
    IF ttbl_test-red1.code2 NE ttbl_test-red2.code2 THEN
    PUT STREAM s-audit-rpt UNFORMATTED "*" AT 1.
    PUT STREAM s-audit-rpt UNFORMATTED "?: " AT 3 ttbl_test-red2.code2.
  END.
END.
IF NOT v_field_changed OR ttbl_test-red1.company NE ttbl_test-red2.company OR action NE "UPDATE" THEN
DO:
  PUT STREAM s-audit-rpt UNFORMATTED "?: " AT 3 ttbl_test-red1.company " (company)".
  IF action = "UPDATE" THEN
  DO:
    IF ttbl_test-red1.company NE ttbl_test-red2.company THEN
    PUT STREAM s-audit-rpt UNFORMATTED "*" AT 1.
    PUT STREAM s-audit-rpt UNFORMATTED "?: " AT 3 ttbl_test-red2.company.
  END.
END.
IF NOT v_field_changed OR ttbl_test-red1.dw-pct NE ttbl_test-red2.dw-pct OR action NE "UPDATE" THEN
DO:
  PUT STREAM s-audit-rpt UNFORMATTED "DW Reduction: " AT 3 ttbl_test-red1.dw-pct FORMAT ">>9.99" " (dw-pct)".
  IF action = "UPDATE" THEN
  DO:
    IF ttbl_test-red1.dw-pct NE ttbl_test-red2.dw-pct THEN
    PUT STREAM s-audit-rpt UNFORMATTED "*" AT 1.
    PUT STREAM s-audit-rpt UNFORMATTED "DW Reduction: " AT 3 ttbl_test-red2.dw-pct FORMAT ">>9.99".
  END.
END.
IF NOT v_field_changed OR ttbl_test-red1.loc NE ttbl_test-red2.loc OR action NE "UPDATE" THEN
DO:
  PUT STREAM s-audit-rpt UNFORMATTED "?: " AT 3 ttbl_test-red1.loc " (loc)".
  IF action = "UPDATE" THEN
  DO:
    IF ttbl_test-red1.loc NE ttbl_test-red2.loc THEN
    PUT STREAM s-audit-rpt UNFORMATTED "*" AT 1.
    PUT STREAM s-audit-rpt UNFORMATTED "?: " AT 3 ttbl_test-red2.loc.
  END.
END.
IF NOT v_field_changed OR ttbl_test-red1.m-code NE ttbl_test-red2.m-code OR action NE "UPDATE" THEN
DO:
  PUT STREAM s-audit-rpt UNFORMATTED "?: " AT 3 ttbl_test-red1.m-code " (m-code)".
  IF action = "UPDATE" THEN
  DO:
    IF ttbl_test-red1.m-code NE ttbl_test-red2.m-code THEN
    PUT STREAM s-audit-rpt UNFORMATTED "*" AT 1.
    PUT STREAM s-audit-rpt UNFORMATTED "?: " AT 3 ttbl_test-red2.m-code.
  END.
END.
IF NOT v_field_changed OR ttbl_test-red1.style NE ttbl_test-red2.style OR action NE "UPDATE" THEN
DO:
  PUT STREAM s-audit-rpt UNFORMATTED "?: " AT 3 ttbl_test-red1.style " (style)".
  IF action = "UPDATE" THEN
  DO:
    IF ttbl_test-red1.style NE ttbl_test-red2.style THEN
    PUT STREAM s-audit-rpt UNFORMATTED "*" AT 1.
    PUT STREAM s-audit-rpt UNFORMATTED "?: " AT 3 ttbl_test-red2.style.
  END.
END.
IF NOT v_field_changed OR ttbl_test-red1.sw-pct NE ttbl_test-red2.sw-pct OR action NE "UPDATE" THEN
DO:
  PUT STREAM s-audit-rpt UNFORMATTED "SW Reduction: " AT 3 ttbl_test-red1.sw-pct FORMAT ">>9.99" " (sw-pct)".
  IF action = "UPDATE" THEN
  DO:
    IF ttbl_test-red1.sw-pct NE ttbl_test-red2.sw-pct THEN
    PUT STREAM s-audit-rpt UNFORMATTED "*" AT 1.
    PUT STREAM s-audit-rpt UNFORMATTED "SW Reduction: " AT 3 ttbl_test-red2.sw-pct FORMAT ">>9.99".
  END.
END.
IF NOT v_field_changed OR ttbl_test-red1.test NE ttbl_test-red2.test OR action NE "UPDATE" THEN
DO:
  PUT STREAM s-audit-rpt UNFORMATTED "Test: " AT 3 ttbl_test-red1.test " (test)".
  IF action = "UPDATE" THEN
  DO:
    IF ttbl_test-red1.test NE ttbl_test-red2.test THEN
    PUT STREAM s-audit-rpt UNFORMATTED "*" AT 1.
    PUT STREAM s-audit-rpt UNFORMATTED "Test: " AT 3 ttbl_test-red2.test.
  END.
END.
IF NOT v_field_changed OR ttbl_test-red1.tw-pct NE ttbl_test-red2.tw-pct OR action NE "UPDATE" THEN
DO:
  PUT STREAM s-audit-rpt UNFORMATTED "TW Reduction: " AT 3 ttbl_test-red1.tw-pct FORMAT ">>9.99" " (tw-pct)".
  IF action = "UPDATE" THEN
  DO:
    IF ttbl_test-red1.tw-pct NE ttbl_test-red2.tw-pct THEN
    PUT STREAM s-audit-rpt UNFORMATTED "*" AT 1.
    PUT STREAM s-audit-rpt UNFORMATTED "TW Reduction: " AT 3 ttbl_test-red2.tw-pct FORMAT ">>9.99".
  END.
END.
IF v_show_index THEN
DO:
  PUT STREAM s-audit-rpt UNFORMATTED "Index Fields - Label: Value (database field-name)" AT 5.
  PUT STREAM s-audit-rpt UNFORMATTED "?: " AT 7.
  IF action = "UPDATE" THEN
  PUT STREAM s-audit-rpt UNFORMATTED ttbl_test-red2.company " (company)".
  ELSE
  PUT STREAM s-audit-rpt UNFORMATTED ttbl_test-red1.company " (company)".
  PUT STREAM s-audit-rpt UNFORMATTED "?: " AT 7.
  IF action = "UPDATE" THEN
  PUT STREAM s-audit-rpt UNFORMATTED ttbl_test-red2.loc " (loc)".
  ELSE
  PUT STREAM s-audit-rpt UNFORMATTED ttbl_test-red1.loc " (loc)".
  PUT STREAM s-audit-rpt UNFORMATTED "?: " AT 7.
  IF action = "UPDATE" THEN
  PUT STREAM s-audit-rpt UNFORMATTED ttbl_test-red2.m-code " (m-code)".
  ELSE
  PUT STREAM s-audit-rpt UNFORMATTED ttbl_test-red1.m-code " (m-code)".
  PUT STREAM s-audit-rpt UNFORMATTED "?: " AT 7.
  IF action = "UPDATE" THEN
  PUT STREAM s-audit-rpt UNFORMATTED ttbl_test-red2.style " (style)".
  ELSE
  PUT STREAM s-audit-rpt UNFORMATTED ttbl_test-red1.style " (style)".
  PUT STREAM s-audit-rpt UNFORMATTED "Test: " AT 7.
  IF action = "UPDATE" THEN
  PUT STREAM s-audit-rpt UNFORMATTED ttbl_test-red2.test " (test)".
  ELSE
  PUT STREAM s-audit-rpt UNFORMATTED ttbl_test-red1.test " (test)".
  PUT STREAM s-audit-rpt UNFORMATTED "?: " AT 7.
  IF action = "UPDATE" THEN
  PUT STREAM s-audit-rpt UNFORMATTED ttbl_test-red2.code " (code)".
  ELSE
  PUT STREAM s-audit-rpt UNFORMATTED ttbl_test-red1.code " (code)".
  PUT STREAM s-audit-rpt UNFORMATTED "?: " AT 7.
  IF action = "UPDATE" THEN
  PUT STREAM s-audit-rpt UNFORMATTED ttbl_test-red2.code1 " (code1)".
  ELSE
  PUT STREAM s-audit-rpt UNFORMATTED ttbl_test-red1.code1 " (code1)".
  PUT STREAM s-audit-rpt UNFORMATTED "?: " AT 7.
  IF action = "UPDATE" THEN
  PUT STREAM s-audit-rpt UNFORMATTED ttbl_test-red2.code2 " (code2)".
  ELSE
  PUT STREAM s-audit-rpt UNFORMATTED ttbl_test-red1.code2 " (code2)".
END.
PUT STREAM s-audit-rpt UNFORMATTED SKIP(1).
