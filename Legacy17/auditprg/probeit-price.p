/* probeit-price.p - generated 12/09/2005 - 15:42:52 by NoSweat */

/* this program was generated by auditgn1.p and should not be modified */
/* modifications should be made to the auto generation program         */

DEFINE INPUT PARAMETER action AS CHARACTER NO-UNDO.

DEFINE VARIABLE i AS INTEGER NO-UNDO.

{methods/defines/audit.i}
FIND FIRST ttbl_header.

DEFINE WORK-TABLE ttbl_probeit-price1 NO-UNDO LIKE probeit-price.
DEFINE WORK-TABLE ttbl_probeit-price2 NO-UNDO LIKE probeit-price.

CREATE ttbl_probeit-price1.
IMPORT STREAM s_audit_file ttbl_probeit-price1.
CREATE ttbl_probeit-price2.
IF action = "UPDATE" THEN
IMPORT STREAM s_audit_file ttbl_probeit-price2.

PUT STREAM s-audit-rpt UNFORMATTED
  "Action    User ID/(User Name)            DB.Table/(Database)  Date       Time" SKIP
  "--------- ------------------------------ -------------------- ---------- --------" SKIP
  ttbl_header.v_action AT 1
  ttbl_header.v_userid AT 11
  ttbl_header.v_file AT 42
  ttbl_header.v_date FORMAT "99/99/9999" AT 63
  STRING(ttbl_header.v_time,"HH:MM:SS") AT 74.
FIND users WHERE users.user_id = v_userid NO-LOCK NO-ERROR.
IF AVAILABLE users THEN
PUT STREAM s-audit-rpt UNFORMATTED "(" AT 10 users.user_name ")".
ELSE
PUT STREAM s-audit-rpt UNFORMATTED "(user record does not exist)" AT 10.
PUT STREAM s-audit-rpt UNFORMATTED
  "(" AT 41 ttbl_header.v_db ")" SKIP(1)
  "Label: Value (database field-name)" AT 3.
IF action = "UPDATE" THEN
PUT STREAM s-audit-rpt UNFORMATTED " ( * - denotes field value change )".
PUT STREAM s-audit-rpt UNFORMATTED FILL("-",75) AT 3.
IF NOT v_field_changed OR ttbl_probeit-price1.company NE ttbl_probeit-price2.company OR action NE "UPDATE" THEN
DO:
  PUT STREAM s-audit-rpt UNFORMATTED "Company: " AT 3 ttbl_probeit-price1.company " (company)".
  IF action = "UPDATE" THEN
  DO:
    IF ttbl_probeit-price1.company NE ttbl_probeit-price2.company THEN
    PUT STREAM s-audit-rpt UNFORMATTED "*" AT 1.
    PUT STREAM s-audit-rpt UNFORMATTED "Company: " AT 3 ttbl_probeit-price2.company.
  END.
END.
IF NOT v_field_changed OR ttbl_probeit-price1.cost-msf NE ttbl_probeit-price2.cost-msf OR action NE "UPDATE" THEN
DO:
  PUT STREAM s-audit-rpt UNFORMATTED "Cost/MSF: " AT 3 ttbl_probeit-price1.cost-msf FORMAT ">>,>>9.99<<<" " (cost-msf)".
  IF action = "UPDATE" THEN
  DO:
    IF ttbl_probeit-price1.cost-msf NE ttbl_probeit-price2.cost-msf THEN
    PUT STREAM s-audit-rpt UNFORMATTED "*" AT 1.
    PUT STREAM s-audit-rpt UNFORMATTED "Cost/MSF: " AT 3 ttbl_probeit-price2.cost-msf FORMAT ">>,>>9.99<<<".
  END.
END.
IF NOT v_field_changed OR ttbl_probeit-price1.est-no NE ttbl_probeit-price2.est-no OR action NE "UPDATE" THEN
DO:
  PUT STREAM s-audit-rpt UNFORMATTED "Estimate #: " AT 3 ttbl_probeit-price1.est-no " (est-no)".
  IF action = "UPDATE" THEN
  DO:
    IF ttbl_probeit-price1.est-no NE ttbl_probeit-price2.est-no THEN
    PUT STREAM s-audit-rpt UNFORMATTED "*" AT 1.
    PUT STREAM s-audit-rpt UNFORMATTED "Estimate #: " AT 3 ttbl_probeit-price2.est-no.
  END.
END.
IF NOT v_field_changed OR ttbl_probeit-price1.i-no NE ttbl_probeit-price2.i-no OR action NE "UPDATE" THEN
DO:
  PUT STREAM s-audit-rpt UNFORMATTED "Item No: " AT 3 ttbl_probeit-price1.i-no " (i-no)".
  IF action = "UPDATE" THEN
  DO:
    IF ttbl_probeit-price1.i-no NE ttbl_probeit-price2.i-no THEN
    PUT STREAM s-audit-rpt UNFORMATTED "*" AT 1.
    PUT STREAM s-audit-rpt UNFORMATTED "Item No: " AT 3 ttbl_probeit-price2.i-no.
  END.
END.
IF NOT v_field_changed OR ttbl_probeit-price1.line NE ttbl_probeit-price2.line OR action NE "UPDATE" THEN
DO:
  PUT STREAM s-audit-rpt UNFORMATTED "Line #: " AT 3 ttbl_probeit-price1.line FORMAT ">9" " (line)".
  IF action = "UPDATE" THEN
  DO:
    IF ttbl_probeit-price1.line NE ttbl_probeit-price2.line THEN
    PUT STREAM s-audit-rpt UNFORMATTED "*" AT 1.
    PUT STREAM s-audit-rpt UNFORMATTED "Line #: " AT 3 ttbl_probeit-price2.line FORMAT ">9".
  END.
END.
IF NOT v_field_changed OR ttbl_probeit-price1.sequence NE ttbl_probeit-price2.sequence OR action NE "UPDATE" THEN
DO:
  PUT STREAM s-audit-rpt UNFORMATTED "?: " AT 3 ttbl_probeit-price1.sequence FORMAT "->,>>>,>>9" " (sequence)".
  IF action = "UPDATE" THEN
  DO:
    IF ttbl_probeit-price1.sequence NE ttbl_probeit-price2.sequence THEN
    PUT STREAM s-audit-rpt UNFORMATTED "*" AT 1.
    PUT STREAM s-audit-rpt UNFORMATTED "?: " AT 3 ttbl_probeit-price2.sequence FORMAT "->,>>>,>>9".
  END.
END.
IF NOT v_field_changed OR ttbl_probeit-price1.setup NE ttbl_probeit-price2.setup OR action NE "UPDATE" THEN
DO:
  PUT STREAM s-audit-rpt UNFORMATTED "Setup Charge: " AT 3 ttbl_probeit-price1.setup FORMAT ">>,>>9.99<<<" " (setup)".
  IF action = "UPDATE" THEN
  DO:
    IF ttbl_probeit-price1.setup NE ttbl_probeit-price2.setup THEN
    PUT STREAM s-audit-rpt UNFORMATTED "*" AT 1.
    PUT STREAM s-audit-rpt UNFORMATTED "Setup Charge: " AT 3 ttbl_probeit-price2.setup FORMAT ">>,>>9.99<<<".
  END.
END.
IF v_show_index THEN
DO:
  PUT STREAM s-audit-rpt UNFORMATTED "Index Fields - Label: Value (database field-name)" AT 5.
  PUT STREAM s-audit-rpt UNFORMATTED "Company: " AT 7.
  IF action = "UPDATE" THEN
  PUT STREAM s-audit-rpt UNFORMATTED ttbl_probeit-price2.company " (company)".
  ELSE
  PUT STREAM s-audit-rpt UNFORMATTED ttbl_probeit-price1.company " (company)".
  PUT STREAM s-audit-rpt UNFORMATTED "Estimate #: " AT 7.
  IF action = "UPDATE" THEN
  PUT STREAM s-audit-rpt UNFORMATTED ttbl_probeit-price2.est-no " (est-no)".
  ELSE
  PUT STREAM s-audit-rpt UNFORMATTED ttbl_probeit-price1.est-no " (est-no)".
  PUT STREAM s-audit-rpt UNFORMATTED "Line #: " AT 7.
  IF action = "UPDATE" THEN
  PUT STREAM s-audit-rpt UNFORMATTED ttbl_probeit-price2.line FORMAT ">9" " (line)".
  ELSE
  PUT STREAM s-audit-rpt UNFORMATTED ttbl_probeit-price1.line FORMAT ">9" " (line)".
  PUT STREAM s-audit-rpt UNFORMATTED "?: " AT 7.
  IF action = "UPDATE" THEN
  PUT STREAM s-audit-rpt UNFORMATTED ttbl_probeit-price2.sequence FORMAT "->,>>>,>>9" " (sequence)".
  ELSE
  PUT STREAM s-audit-rpt UNFORMATTED ttbl_probeit-price1.sequence FORMAT "->,>>>,>>9" " (sequence)".
END.
PUT STREAM s-audit-rpt UNFORMATTED SKIP(1).
