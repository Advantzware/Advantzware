/* currency.p - generated 12/09/2005 - 15:42:40 by NoSweat */

/* this program was generated by auditgn1.p and should not be modified */
/* modifications should be made to the auto generation program         */

DEFINE INPUT PARAMETER action AS CHARACTER NO-UNDO.

DEFINE VARIABLE i AS INTEGER NO-UNDO.

{methods/defines/audit.i}
FIND FIRST ttbl_header.

DEFINE WORK-TABLE ttbl_currency1 NO-UNDO LIKE currency.
DEFINE WORK-TABLE ttbl_currency2 NO-UNDO LIKE currency.

CREATE ttbl_currency1.
IMPORT STREAM s_audit_file ttbl_currency1.
CREATE ttbl_currency2.
IF action = "UPDATE" THEN
IMPORT STREAM s_audit_file ttbl_currency2.

PUT STREAM s-audit-rpt UNFORMATTED
  "Action    User ID/(User Name)            DB.Table/(Database)  Date       Time" SKIP
  "--------- ------------------------------ -------------------- ---------- --------" SKIP
  ttbl_header.v_action AT 1
  ttbl_header.v_userid AT 11
  ttbl_header.v_file AT 42
  ttbl_header.v_date FORMAT "99/99/9999" AT 63
  STRING(ttbl_header.v_time,"HH:MM:SS") AT 74.
FIND users WHERE users.user_id = v_userid NO-LOCK NO-ERROR.
IF AVAILABLE users THEN
PUT STREAM s-audit-rpt UNFORMATTED "(" AT 10 users.user_name ")".
ELSE
PUT STREAM s-audit-rpt UNFORMATTED "(user record does not exist)" AT 10.
PUT STREAM s-audit-rpt UNFORMATTED
  "(" AT 41 ttbl_header.v_db ")" SKIP(1)
  "Label: Value (database field-name)" AT 3.
IF action = "UPDATE" THEN
PUT STREAM s-audit-rpt UNFORMATTED " ( * - denotes field value change )".
PUT STREAM s-audit-rpt UNFORMATTED FILL("-",75) AT 3.
IF NOT v_field_changed OR ttbl_currency1.ap-lia-acct NE ttbl_currency2.ap-lia-acct OR action NE "UPDATE" THEN
DO:
  PUT STREAM s-audit-rpt UNFORMATTED "A/P Liability Account: " AT 3 ttbl_currency1.ap-lia-acct " (ap-lia-acct)".
  IF action = "UPDATE" THEN
  DO:
    IF ttbl_currency1.ap-lia-acct NE ttbl_currency2.ap-lia-acct THEN
    PUT STREAM s-audit-rpt UNFORMATTED "*" AT 1.
    PUT STREAM s-audit-rpt UNFORMATTED "A/P Liability Account: " AT 3 ttbl_currency2.ap-lia-acct.
  END.
END.
IF NOT v_field_changed OR ttbl_currency1.ar-ast-acct NE ttbl_currency2.ar-ast-acct OR action NE "UPDATE" THEN
DO:
  PUT STREAM s-audit-rpt UNFORMATTED "A/R Asset Account: " AT 3 ttbl_currency1.ar-ast-acct " (ar-ast-acct)".
  IF action = "UPDATE" THEN
  DO:
    IF ttbl_currency1.ar-ast-acct NE ttbl_currency2.ar-ast-acct THEN
    PUT STREAM s-audit-rpt UNFORMATTED "*" AT 1.
    PUT STREAM s-audit-rpt UNFORMATTED "A/R Asset Account: " AT 3 ttbl_currency2.ar-ast-acct.
  END.
END.
IF NOT v_field_changed OR ttbl_currency1.c-code NE ttbl_currency2.c-code OR action NE "UPDATE" THEN
DO:
  PUT STREAM s-audit-rpt UNFORMATTED "Code: " AT 3 ttbl_currency1.c-code " (c-code)".
  IF action = "UPDATE" THEN
  DO:
    IF ttbl_currency1.c-code NE ttbl_currency2.c-code THEN
    PUT STREAM s-audit-rpt UNFORMATTED "*" AT 1.
    PUT STREAM s-audit-rpt UNFORMATTED "Code: " AT 3 ttbl_currency2.c-code.
  END.
END.
IF NOT v_field_changed OR ttbl_currency1.c-desc NE ttbl_currency2.c-desc OR action NE "UPDATE" THEN
DO:
  PUT STREAM s-audit-rpt UNFORMATTED "description: " AT 3 ttbl_currency1.c-desc " (c-desc)".
  IF action = "UPDATE" THEN
  DO:
    IF ttbl_currency1.c-desc NE ttbl_currency2.c-desc THEN
    PUT STREAM s-audit-rpt UNFORMATTED "*" AT 1.
    PUT STREAM s-audit-rpt UNFORMATTED "description: " AT 3 ttbl_currency2.c-desc.
  END.
END.
IF NOT v_field_changed OR ttbl_currency1.c-num NE ttbl_currency2.c-num OR action NE "UPDATE" THEN
DO:
  PUT STREAM s-audit-rpt UNFORMATTED "C. Num: " AT 3 ttbl_currency1.c-num FORMAT ">>9" " (c-num)".
  IF action = "UPDATE" THEN
  DO:
    IF ttbl_currency1.c-num NE ttbl_currency2.c-num THEN
    PUT STREAM s-audit-rpt UNFORMATTED "*" AT 1.
    PUT STREAM s-audit-rpt UNFORMATTED "C. Num: " AT 3 ttbl_currency2.c-num FORMAT ">>9".
  END.
END.
IF NOT v_field_changed OR ttbl_currency1.company NE ttbl_currency2.company OR action NE "UPDATE" THEN
DO:
  PUT STREAM s-audit-rpt UNFORMATTED "Company: " AT 3 ttbl_currency1.company " (company)".
  IF action = "UPDATE" THEN
  DO:
    IF ttbl_currency1.company NE ttbl_currency2.company THEN
    PUT STREAM s-audit-rpt UNFORMATTED "*" AT 1.
    PUT STREAM s-audit-rpt UNFORMATTED "Company: " AT 3 ttbl_currency2.company.
  END.
END.
IF NOT v_field_changed OR ttbl_currency1.country NE ttbl_currency2.country OR action NE "UPDATE" THEN
DO:
  PUT STREAM s-audit-rpt UNFORMATTED "Country: " AT 3 ttbl_currency1.country " (country)".
  IF action = "UPDATE" THEN
  DO:
    IF ttbl_currency1.country NE ttbl_currency2.country THEN
    PUT STREAM s-audit-rpt UNFORMATTED "*" AT 1.
    PUT STREAM s-audit-rpt UNFORMATTED "Country: " AT 3 ttbl_currency2.country.
  END.
END.
IF NOT v_field_changed OR ttbl_currency1.ex-rate NE ttbl_currency2.ex-rate OR action NE "UPDATE" THEN
DO:
  PUT STREAM s-audit-rpt UNFORMATTED "?: " AT 3 ttbl_currency1.ex-rate FORMAT "->>,>>9.99<<<<<" " (ex-rate)".
  IF action = "UPDATE" THEN
  DO:
    IF ttbl_currency1.ex-rate NE ttbl_currency2.ex-rate THEN
    PUT STREAM s-audit-rpt UNFORMATTED "*" AT 1.
    PUT STREAM s-audit-rpt UNFORMATTED "?: " AT 3 ttbl_currency2.ex-rate FORMAT "->>,>>9.99<<<<<".
  END.
END.
IF NOT v_field_changed OR ttbl_currency1.is-base NE ttbl_currency2.is-base OR action NE "UPDATE" THEN
DO:
  PUT STREAM s-audit-rpt UNFORMATTED "Base?: " AT 3 ttbl_currency1.is-base FORMAT "yes/no" " (is-base)".
  IF action = "UPDATE" THEN
  DO:
    IF ttbl_currency1.is-base NE ttbl_currency2.is-base THEN
    PUT STREAM s-audit-rpt UNFORMATTED "*" AT 1.
    PUT STREAM s-audit-rpt UNFORMATTED "Base?: " AT 3 ttbl_currency2.is-base FORMAT "yes/no".
  END.
END.
IF NOT v_field_changed OR ttbl_currency1.minor-unit NE ttbl_currency2.minor-unit OR action NE "UPDATE" THEN
DO:
  PUT STREAM s-audit-rpt UNFORMATTED "Minor Unit: " AT 3 ttbl_currency1.minor-unit " (minor-unit)".
  IF action = "UPDATE" THEN
  DO:
    IF ttbl_currency1.minor-unit NE ttbl_currency2.minor-unit THEN
    PUT STREAM s-audit-rpt UNFORMATTED "*" AT 1.
    PUT STREAM s-audit-rpt UNFORMATTED "Minor Unit: " AT 3 ttbl_currency2.minor-unit.
  END.
END.
IF NOT v_field_changed OR ttbl_currency1.pd-aprate-acct NE ttbl_currency2.pd-aprate-acct OR action NE "UPDATE" THEN
DO:
  PUT STREAM s-audit-rpt UNFORMATTED "Paid A/P Invoice Rate Variance Account: " AT 3 ttbl_currency1.pd-aprate-acct " (pd-aprate-acct)".
  IF action = "UPDATE" THEN
  DO:
    IF ttbl_currency1.pd-aprate-acct NE ttbl_currency2.pd-aprate-acct THEN
    PUT STREAM s-audit-rpt UNFORMATTED "*" AT 1.
    PUT STREAM s-audit-rpt UNFORMATTED "Paid A/P Invoice Rate Variance Account: " AT 3 ttbl_currency2.pd-aprate-acct.
  END.
END.
IF NOT v_field_changed OR ttbl_currency1.pd-arrate-acct NE ttbl_currency2.pd-arrate-acct OR action NE "UPDATE" THEN
DO:
  PUT STREAM s-audit-rpt UNFORMATTED "Paid A/R Invoice Rate Variance Account: " AT 3 ttbl_currency1.pd-arrate-acct " (pd-arrate-acct)".
  IF action = "UPDATE" THEN
  DO:
    IF ttbl_currency1.pd-arrate-acct NE ttbl_currency2.pd-arrate-acct THEN
    PUT STREAM s-audit-rpt UNFORMATTED "*" AT 1.
    PUT STREAM s-audit-rpt UNFORMATTED "Paid A/R Invoice Rate Variance Account: " AT 3 ttbl_currency2.pd-arrate-acct.
  END.
END.
DO i = 1 TO 5:
IF NOT v_field_changed OR ttbl_currency1.rate-var-act[i] NE ttbl_currency2.rate-var-act[i] OR action NE "UPDATE" THEN
DO:
  PUT STREAM s-audit-rpt UNFORMATTED "Rate Var. Account[" AT 3 i "]: " ttbl_currency1.rate-var-act[i] " (rate-var-act[" i "])".
  IF action = "UPDATE" THEN
  DO:
    IF ttbl_currency1.rate-var-act[i] NE ttbl_currency2.rate-var-act[i] THEN
    PUT STREAM s-audit-rpt UNFORMATTED "*" AT 1.
    PUT STREAM s-audit-rpt UNFORMATTED "Rate Var. Account[" AT 3 i "]: " ttbl_currency2.rate-var-act[i].
  END.
END.
END.
IF NOT v_field_changed OR ttbl_currency1.un-aprate-acct NE ttbl_currency2.un-aprate-acct OR action NE "UPDATE" THEN
DO:
  PUT STREAM s-audit-rpt UNFORMATTED "UnPaid A/P Invoice Rate Variance: " AT 3 ttbl_currency1.un-aprate-acct " (un-aprate-acct)".
  IF action = "UPDATE" THEN
  DO:
    IF ttbl_currency1.un-aprate-acct NE ttbl_currency2.un-aprate-acct THEN
    PUT STREAM s-audit-rpt UNFORMATTED "*" AT 1.
    PUT STREAM s-audit-rpt UNFORMATTED "UnPaid A/P Invoice Rate Variance: " AT 3 ttbl_currency2.un-aprate-acct.
  END.
END.
IF NOT v_field_changed OR ttbl_currency1.un-arrate-acct NE ttbl_currency2.un-arrate-acct OR action NE "UPDATE" THEN
DO:
  PUT STREAM s-audit-rpt UNFORMATTED "UnPaid A/R Invoice Rate Variance: " AT 3 ttbl_currency1.un-arrate-acct " (un-arrate-acct)".
  IF action = "UPDATE" THEN
  DO:
    IF ttbl_currency1.un-arrate-acct NE ttbl_currency2.un-arrate-acct THEN
    PUT STREAM s-audit-rpt UNFORMATTED "*" AT 1.
    PUT STREAM s-audit-rpt UNFORMATTED "UnPaid A/R Invoice Rate Variance: " AT 3 ttbl_currency2.un-arrate-acct.
  END.
END.
IF v_show_index THEN
DO:
  PUT STREAM s-audit-rpt UNFORMATTED "Index Fields - Label: Value (database field-name)" AT 5.
  PUT STREAM s-audit-rpt UNFORMATTED "Company: " AT 7.
  IF action = "UPDATE" THEN
  PUT STREAM s-audit-rpt UNFORMATTED ttbl_currency2.company " (company)".
  ELSE
  PUT STREAM s-audit-rpt UNFORMATTED ttbl_currency1.company " (company)".
  PUT STREAM s-audit-rpt UNFORMATTED "Code: " AT 7.
  IF action = "UPDATE" THEN
  PUT STREAM s-audit-rpt UNFORMATTED ttbl_currency2.c-code " (c-code)".
  ELSE
  PUT STREAM s-audit-rpt UNFORMATTED ttbl_currency1.c-code " (c-code)".
END.
PUT STREAM s-audit-rpt UNFORMATTED SKIP(1).
