/* quoteit.p - generated 12/09/2005 - 15:42:53 by NoSweat */

/* this program was generated by auditgn1.p and should not be modified */
/* modifications should be made to the auto generation program         */

DEFINE INPUT PARAMETER action AS CHARACTER NO-UNDO.

DEFINE VARIABLE i AS INTEGER NO-UNDO.

{methods/defines/audit.i}
FIND FIRST ttbl_header.

DEFINE WORK-TABLE ttbl_quoteit1 NO-UNDO LIKE quoteit.
DEFINE WORK-TABLE ttbl_quoteit2 NO-UNDO LIKE quoteit.

CREATE ttbl_quoteit1.
IMPORT STREAM s_audit_file ttbl_quoteit1.
CREATE ttbl_quoteit2.
IF action = "UPDATE" THEN
IMPORT STREAM s_audit_file ttbl_quoteit2.

PUT STREAM s-audit-rpt UNFORMATTED
  "Action    User ID/(User Name)            DB.Table/(Database)  Date       Time" SKIP
  "--------- ------------------------------ -------------------- ---------- --------" SKIP
  ttbl_header.v_action AT 1
  ttbl_header.v_userid AT 11
  ttbl_header.v_file AT 42
  ttbl_header.v_date FORMAT "99/99/9999" AT 63
  STRING(ttbl_header.v_time,"HH:MM:SS") AT 74.
FIND users WHERE users.user_id = v_userid NO-LOCK NO-ERROR.
IF AVAILABLE users THEN
PUT STREAM s-audit-rpt UNFORMATTED "(" AT 10 users.user_name ")".
ELSE
PUT STREAM s-audit-rpt UNFORMATTED "(user record does not exist)" AT 10.
PUT STREAM s-audit-rpt UNFORMATTED
  "(" AT 41 ttbl_header.v_db ")" SKIP(1)
  "Label: Value (database field-name)" AT 3.
IF action = "UPDATE" THEN
PUT STREAM s-audit-rpt UNFORMATTED " ( * - denotes field value change )".
PUT STREAM s-audit-rpt UNFORMATTED FILL("-",75) AT 3.
IF NOT v_field_changed OR ttbl_quoteit1.company NE ttbl_quoteit2.company OR action NE "UPDATE" THEN
DO:
  PUT STREAM s-audit-rpt UNFORMATTED "Company: " AT 3 ttbl_quoteit1.company " (company)".
  IF action = "UPDATE" THEN
  DO:
    IF ttbl_quoteit1.company NE ttbl_quoteit2.company THEN
    PUT STREAM s-audit-rpt UNFORMATTED "*" AT 1.
    PUT STREAM s-audit-rpt UNFORMATTED "Company: " AT 3 ttbl_quoteit2.company.
  END.
END.
IF NOT v_field_changed OR ttbl_quoteit1.e-num NE ttbl_quoteit2.e-num OR action NE "UPDATE" THEN
DO:
  PUT STREAM s-audit-rpt UNFORMATTED "Est.#: " AT 3 ttbl_quoteit1.e-num FORMAT ">>>>>9" " (e-num)".
  IF action = "UPDATE" THEN
  DO:
    IF ttbl_quoteit1.e-num NE ttbl_quoteit2.e-num THEN
    PUT STREAM s-audit-rpt UNFORMATTED "*" AT 1.
    PUT STREAM s-audit-rpt UNFORMATTED "Est.#: " AT 3 ttbl_quoteit2.e-num FORMAT ">>>>>9".
  END.
END.
IF NOT v_field_changed OR ttbl_quoteit1.eqty NE ttbl_quoteit2.eqty OR action NE "UPDATE" THEN
DO:
  PUT STREAM s-audit-rpt UNFORMATTED "Qty: " AT 3 ttbl_quoteit1.eqty FORMAT "->>,>>9.99" " (eqty)".
  IF action = "UPDATE" THEN
  DO:
    IF ttbl_quoteit1.eqty NE ttbl_quoteit2.eqty THEN
    PUT STREAM s-audit-rpt UNFORMATTED "*" AT 1.
    PUT STREAM s-audit-rpt UNFORMATTED "Qty: " AT 3 ttbl_quoteit2.eqty FORMAT "->>,>>9.99".
  END.
END.
IF NOT v_field_changed OR ttbl_quoteit1.est-no NE ttbl_quoteit2.est-no OR action NE "UPDATE" THEN
DO:
  PUT STREAM s-audit-rpt UNFORMATTED "Estimate #: " AT 3 ttbl_quoteit1.est-no " (est-no)".
  IF action = "UPDATE" THEN
  DO:
    IF ttbl_quoteit1.est-no NE ttbl_quoteit2.est-no THEN
    PUT STREAM s-audit-rpt UNFORMATTED "*" AT 1.
    PUT STREAM s-audit-rpt UNFORMATTED "Estimate #: " AT 3 ttbl_quoteit2.est-no.
  END.
END.
IF NOT v_field_changed OR ttbl_quoteit1.i-coldscr NE ttbl_quoteit2.i-coldscr OR action NE "UPDATE" THEN
DO:
  PUT STREAM s-audit-rpt UNFORMATTED "Color Desc.: " AT 3 ttbl_quoteit1.i-coldscr " (i-coldscr)".
  IF action = "UPDATE" THEN
  DO:
    IF ttbl_quoteit1.i-coldscr NE ttbl_quoteit2.i-coldscr THEN
    PUT STREAM s-audit-rpt UNFORMATTED "*" AT 1.
    PUT STREAM s-audit-rpt UNFORMATTED "Color Desc.: " AT 3 ttbl_quoteit2.i-coldscr.
  END.
END.
IF NOT v_field_changed OR ttbl_quoteit1.i-dscr NE ttbl_quoteit2.i-dscr OR action NE "UPDATE" THEN
DO:
  PUT STREAM s-audit-rpt UNFORMATTED "Desc: " AT 3 ttbl_quoteit1.i-dscr " (i-dscr)".
  IF action = "UPDATE" THEN
  DO:
    IF ttbl_quoteit1.i-dscr NE ttbl_quoteit2.i-dscr THEN
    PUT STREAM s-audit-rpt UNFORMATTED "*" AT 1.
    PUT STREAM s-audit-rpt UNFORMATTED "Desc: " AT 3 ttbl_quoteit2.i-dscr.
  END.
END.
IF NOT v_field_changed OR ttbl_quoteit1.line NE ttbl_quoteit2.line OR action NE "UPDATE" THEN
DO:
  PUT STREAM s-audit-rpt UNFORMATTED "Line #: " AT 3 ttbl_quoteit1.line FORMAT ">9" " (line)".
  IF action = "UPDATE" THEN
  DO:
    IF ttbl_quoteit1.line NE ttbl_quoteit2.line THEN
    PUT STREAM s-audit-rpt UNFORMATTED "*" AT 1.
    PUT STREAM s-audit-rpt UNFORMATTED "Line #: " AT 3 ttbl_quoteit2.line FORMAT ">9".
  END.
END.
IF NOT v_field_changed OR ttbl_quoteit1.part-dscr1 NE ttbl_quoteit2.part-dscr1 OR action NE "UPDATE" THEN
DO:
  PUT STREAM s-audit-rpt UNFORMATTED "Item Description: " AT 3 ttbl_quoteit1.part-dscr1 " (part-dscr1)".
  IF action = "UPDATE" THEN
  DO:
    IF ttbl_quoteit1.part-dscr1 NE ttbl_quoteit2.part-dscr1 THEN
    PUT STREAM s-audit-rpt UNFORMATTED "*" AT 1.
    PUT STREAM s-audit-rpt UNFORMATTED "Item Description: " AT 3 ttbl_quoteit2.part-dscr1.
  END.
END.
IF NOT v_field_changed OR ttbl_quoteit1.part-dscr2 NE ttbl_quoteit2.part-dscr2 OR action NE "UPDATE" THEN
DO:
  PUT STREAM s-audit-rpt UNFORMATTED "Item Description: " AT 3 ttbl_quoteit1.part-dscr2 " (part-dscr2)".
  IF action = "UPDATE" THEN
  DO:
    IF ttbl_quoteit1.part-dscr2 NE ttbl_quoteit2.part-dscr2 THEN
    PUT STREAM s-audit-rpt UNFORMATTED "*" AT 1.
    PUT STREAM s-audit-rpt UNFORMATTED "Item Description: " AT 3 ttbl_quoteit2.part-dscr2.
  END.
END.
IF NOT v_field_changed OR ttbl_quoteit1.part-no NE ttbl_quoteit2.part-no OR action NE "UPDATE" THEN
DO:
  PUT STREAM s-audit-rpt UNFORMATTED "Cust Part #: " AT 3 ttbl_quoteit1.part-no " (part-no)".
  IF action = "UPDATE" THEN
  DO:
    IF ttbl_quoteit1.part-no NE ttbl_quoteit2.part-no THEN
    PUT STREAM s-audit-rpt UNFORMATTED "*" AT 1.
    PUT STREAM s-audit-rpt UNFORMATTED "Cust Part #: " AT 3 ttbl_quoteit2.part-no.
  END.
END.
IF NOT v_field_changed OR ttbl_quoteit1.price NE ttbl_quoteit2.price OR action NE "UPDATE" THEN
DO:
  PUT STREAM s-audit-rpt UNFORMATTED "Price: " AT 3 ttbl_quoteit1.price FORMAT ">,>>9.99" " (price)".
  IF action = "UPDATE" THEN
  DO:
    IF ttbl_quoteit1.price NE ttbl_quoteit2.price THEN
    PUT STREAM s-audit-rpt UNFORMATTED "*" AT 1.
    PUT STREAM s-audit-rpt UNFORMATTED "Price: " AT 3 ttbl_quoteit2.price FORMAT ">,>>9.99".
  END.
END.
IF NOT v_field_changed OR ttbl_quoteit1.q-no NE ttbl_quoteit2.q-no OR action NE "UPDATE" THEN
DO:
  PUT STREAM s-audit-rpt UNFORMATTED "Quote #: " AT 3 ttbl_quoteit1.q-no FORMAT ">>>>9" " (q-no)".
  IF action = "UPDATE" THEN
  DO:
    IF ttbl_quoteit1.q-no NE ttbl_quoteit2.q-no THEN
    PUT STREAM s-audit-rpt UNFORMATTED "*" AT 1.
    PUT STREAM s-audit-rpt UNFORMATTED "Quote #: " AT 3 ttbl_quoteit2.q-no FORMAT ">>>>9".
  END.
END.
IF NOT v_field_changed OR ttbl_quoteit1.qty NE ttbl_quoteit2.qty OR action NE "UPDATE" THEN
DO:
  PUT STREAM s-audit-rpt UNFORMATTED "Qty: " AT 3 ttbl_quoteit1.qty FORMAT ">>>>>>>" " (qty)".
  IF action = "UPDATE" THEN
  DO:
    IF ttbl_quoteit1.qty NE ttbl_quoteit2.qty THEN
    PUT STREAM s-audit-rpt UNFORMATTED "*" AT 1.
    PUT STREAM s-audit-rpt UNFORMATTED "Qty: " AT 3 ttbl_quoteit2.qty FORMAT ">>>>>>>".
  END.
END.
IF NOT v_field_changed OR ttbl_quoteit1.size NE ttbl_quoteit2.size OR action NE "UPDATE" THEN
DO:
  PUT STREAM s-audit-rpt UNFORMATTED "Dimensions: " AT 3 ttbl_quoteit1.size " (size)".
  IF action = "UPDATE" THEN
  DO:
    IF ttbl_quoteit1.size NE ttbl_quoteit2.size THEN
    PUT STREAM s-audit-rpt UNFORMATTED "*" AT 1.
    PUT STREAM s-audit-rpt UNFORMATTED "Dimensions: " AT 3 ttbl_quoteit2.size.
  END.
END.
IF NOT v_field_changed OR ttbl_quoteit1.style NE ttbl_quoteit2.style OR action NE "UPDATE" THEN
DO:
  PUT STREAM s-audit-rpt UNFORMATTED "Style Code: " AT 3 ttbl_quoteit1.style " (style)".
  IF action = "UPDATE" THEN
  DO:
    IF ttbl_quoteit1.style NE ttbl_quoteit2.style THEN
    PUT STREAM s-audit-rpt UNFORMATTED "*" AT 1.
    PUT STREAM s-audit-rpt UNFORMATTED "Style Code: " AT 3 ttbl_quoteit2.style.
  END.
END.
IF NOT v_field_changed OR ttbl_quoteit1.uom NE ttbl_quoteit2.uom OR action NE "UPDATE" THEN
DO:
  PUT STREAM s-audit-rpt UNFORMATTED "UOM: " AT 3 ttbl_quoteit1.uom " (uom)".
  IF action = "UPDATE" THEN
  DO:
    IF ttbl_quoteit1.uom NE ttbl_quoteit2.uom THEN
    PUT STREAM s-audit-rpt UNFORMATTED "*" AT 1.
    PUT STREAM s-audit-rpt UNFORMATTED "UOM: " AT 3 ttbl_quoteit2.uom.
  END.
END.
IF v_show_index THEN
DO:
  PUT STREAM s-audit-rpt UNFORMATTED "Index Fields - Label: Value (database field-name)" AT 5.
  PUT STREAM s-audit-rpt UNFORMATTED "Company: " AT 7.
  IF action = "UPDATE" THEN
  PUT STREAM s-audit-rpt UNFORMATTED ttbl_quoteit2.company " (company)".
  ELSE
  PUT STREAM s-audit-rpt UNFORMATTED ttbl_quoteit1.company " (company)".
  PUT STREAM s-audit-rpt UNFORMATTED "Estimate #: " AT 7.
  IF action = "UPDATE" THEN
  PUT STREAM s-audit-rpt UNFORMATTED ttbl_quoteit2.est-no " (est-no)".
  ELSE
  PUT STREAM s-audit-rpt UNFORMATTED ttbl_quoteit1.est-no " (est-no)".
  PUT STREAM s-audit-rpt UNFORMATTED "Qty: " AT 7.
  IF action = "UPDATE" THEN
  PUT STREAM s-audit-rpt UNFORMATTED ttbl_quoteit2.eqty FORMAT "->>,>>9.99" " (eqty)".
  ELSE
  PUT STREAM s-audit-rpt UNFORMATTED ttbl_quoteit1.eqty FORMAT "->>,>>9.99" " (eqty)".
  PUT STREAM s-audit-rpt UNFORMATTED "Quote #: " AT 7.
  IF action = "UPDATE" THEN
  PUT STREAM s-audit-rpt UNFORMATTED ttbl_quoteit2.q-no FORMAT ">>>>9" " (q-no)".
  ELSE
  PUT STREAM s-audit-rpt UNFORMATTED ttbl_quoteit1.q-no FORMAT ">>>>9" " (q-no)".
  PUT STREAM s-audit-rpt UNFORMATTED "Line #: " AT 7.
  IF action = "UPDATE" THEN
  PUT STREAM s-audit-rpt UNFORMATTED ttbl_quoteit2.line FORMAT ">9" " (line)".
  ELSE
  PUT STREAM s-audit-rpt UNFORMATTED ttbl_quoteit1.line FORMAT ">9" " (line)".
END.
PUT STREAM s-audit-rpt UNFORMATTED SKIP(1).
