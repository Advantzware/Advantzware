/* EDCo.p - generated 01/10/2001 - 13:00:00 by nosweat */

/* this program was generated by auditgn1.p and should not be modified */
/* modifications should be made to the auto generation program         */

DEFINE INPUT PARAMETER action AS CHARACTER NO-UNDO.

DEFINE VARIABLE i AS INTEGER NO-UNDO.

{methods/defines/audit.i}
FIND FIRST ttbl_header.

DEFINE WORK-TABLE ttbl_EDCo1 NO-UNDO LIKE EDCo.
DEFINE WORK-TABLE ttbl_EDCo2 NO-UNDO LIKE EDCo.

CREATE ttbl_EDCo1.
IMPORT STREAM s_audit_file ttbl_EDCo1.
CREATE ttbl_EDCo2.
IF action = "UPDATE" THEN
IMPORT STREAM s_audit_file ttbl_EDCo2.

PUT STREAM s-audit-rpt UNFORMATTED
  "Action    User ID/(User Name)            DB.Table/(Database)  Date       Time" SKIP
  "--------- ------------------------------ -------------------- ---------- --------" SKIP
  ttbl_header.v_action AT 1
  ttbl_header.v_userid AT 11
  ttbl_header.v_file AT 42
  ttbl_header.v_date FORMAT "99/99/9999" AT 63
  STRING(ttbl_header.v_time,"HH:MM:SS") AT 74.
FIND users WHERE users.user_id = v_userid NO-LOCK NO-ERROR.
IF AVAILABLE users THEN
PUT STREAM s-audit-rpt UNFORMATTED "(" AT 10 users.user_name ")".
ELSE
PUT STREAM s-audit-rpt UNFORMATTED "(user record does not exist)" AT 10.
PUT STREAM s-audit-rpt UNFORMATTED
  "(" AT 41 ttbl_header.v_db ")" SKIP(1)
  "Label: Value (database field-name)" AT 3.
IF action = "UPDATE" THEN
PUT STREAM s-audit-rpt UNFORMATTED " ( * - denotes field value change )".
PUT STREAM s-audit-rpt UNFORMATTED FILL("-",75) AT 3.
IF NOT v_field_changed OR ttbl_EDCo1.ASN-on-inv NE ttbl_EDCo2.ASN-on-inv OR action NE "UPDATE" THEN
DO:
  PUT STREAM s-audit-rpt UNFORMATTED "?: " AT 3 ttbl_EDCo1.ASN-on-inv FORMAT "INV/BOL" " (ASN-on-inv)".
  IF action = "UPDATE" THEN
  DO:
    IF ttbl_EDCo1.ASN-on-inv NE ttbl_EDCo2.ASN-on-inv THEN
    PUT STREAM s-audit-rpt UNFORMATTED "*" AT 1.
    PUT STREAM s-audit-rpt UNFORMATTED "?: " AT 3 ttbl_EDCo2.ASN-on-inv FORMAT "INV/BOL".
  END.
END.
IF NOT v_field_changed OR ttbl_EDCo1.Company NE ttbl_EDCo2.Company OR action NE "UPDATE" THEN
DO:
  PUT STREAM s-audit-rpt UNFORMATTED "?: " AT 3 ttbl_EDCo1.Company " (Company)".
  IF action = "UPDATE" THEN
  DO:
    IF ttbl_EDCo1.Company NE ttbl_EDCo2.Company THEN
    PUT STREAM s-audit-rpt UNFORMATTED "*" AT 1.
    PUT STREAM s-audit-rpt UNFORMATTED "?: " AT 3 ttbl_EDCo2.Company.
  END.
END.
IF NOT v_field_changed OR ttbl_EDCo1.Path-err NE ttbl_EDCo2.Path-err OR action NE "UPDATE" THEN
DO:
  PUT STREAM s-audit-rpt UNFORMATTED "?: " AT 3 ttbl_EDCo1.Path-err " (Path-err)".
  IF action = "UPDATE" THEN
  DO:
    IF ttbl_EDCo1.Path-err NE ttbl_EDCo2.Path-err THEN
    PUT STREAM s-audit-rpt UNFORMATTED "*" AT 1.
    PUT STREAM s-audit-rpt UNFORMATTED "?: " AT 3 ttbl_EDCo2.Path-err.
  END.
END.
IF NOT v_field_changed OR ttbl_EDCo1.Path-in NE ttbl_EDCo2.Path-in OR action NE "UPDATE" THEN
DO:
  PUT STREAM s-audit-rpt UNFORMATTED "?: " AT 3 ttbl_EDCo1.Path-in " (Path-in)".
  IF action = "UPDATE" THEN
  DO:
    IF ttbl_EDCo1.Path-in NE ttbl_EDCo2.Path-in THEN
    PUT STREAM s-audit-rpt UNFORMATTED "*" AT 1.
    PUT STREAM s-audit-rpt UNFORMATTED "?: " AT 3 ttbl_EDCo2.Path-in.
  END.
END.
IF NOT v_field_changed OR ttbl_EDCo1.Path-out NE ttbl_EDCo2.Path-out OR action NE "UPDATE" THEN
DO:
  PUT STREAM s-audit-rpt UNFORMATTED "?: " AT 3 ttbl_EDCo1.Path-out " (Path-out)".
  IF action = "UPDATE" THEN
  DO:
    IF ttbl_EDCo1.Path-out NE ttbl_EDCo2.Path-out THEN
    PUT STREAM s-audit-rpt UNFORMATTED "*" AT 1.
    PUT STREAM s-audit-rpt UNFORMATTED "?: " AT 3 ttbl_EDCo2.Path-out.
  END.
END.
IF NOT v_field_changed OR ttbl_EDCo1.Sf-code NE ttbl_EDCo2.Sf-code OR action NE "UPDATE" THEN
DO:
  PUT STREAM s-audit-rpt UNFORMATTED "?: " AT 3 ttbl_EDCo1.Sf-code " (Sf-code)".
  IF action = "UPDATE" THEN
  DO:
    IF ttbl_EDCo1.Sf-code NE ttbl_EDCo2.Sf-code THEN
    PUT STREAM s-audit-rpt UNFORMATTED "*" AT 1.
    PUT STREAM s-audit-rpt UNFORMATTED "?: " AT 3 ttbl_EDCo2.Sf-code.
  END.
END.
IF NOT v_field_changed OR ttbl_EDCo1.System NE ttbl_EDCo2.System OR action NE "UPDATE" THEN
DO:
  PUT STREAM s-audit-rpt UNFORMATTED "?: " AT 3 ttbl_EDCo1.System " (System)".
  IF action = "UPDATE" THEN
  DO:
    IF ttbl_EDCo1.System NE ttbl_EDCo2.System THEN
    PUT STREAM s-audit-rpt UNFORMATTED "*" AT 1.
    PUT STREAM s-audit-rpt UNFORMATTED "?: " AT 3 ttbl_EDCo2.System.
  END.
END.
IF NOT v_field_changed OR ttbl_EDCo1.Translator NE ttbl_EDCo2.Translator OR action NE "UPDATE" THEN
DO:
  PUT STREAM s-audit-rpt UNFORMATTED "?: " AT 3 ttbl_EDCo1.Translator " (Translator)".
  IF action = "UPDATE" THEN
  DO:
    IF ttbl_EDCo1.Translator NE ttbl_EDCo2.Translator THEN
    PUT STREAM s-audit-rpt UNFORMATTED "*" AT 1.
    PUT STREAM s-audit-rpt UNFORMATTED "?: " AT 3 ttbl_EDCo2.Translator.
  END.
END.
IF v_show_index THEN
DO:
  PUT STREAM s-audit-rpt UNFORMATTED "Index Fields - Label: Value (database field-name)" AT 5.
  PUT STREAM s-audit-rpt UNFORMATTED "?: " AT 7.
  IF action = "UPDATE" THEN
  PUT STREAM s-audit-rpt UNFORMATTED ttbl_EDCo2.Company " (Company)".
  ELSE
  PUT STREAM s-audit-rpt UNFORMATTED ttbl_EDCo1.Company " (Company)".
END.
PUT STREAM s-audit-rpt UNFORMATTED SKIP(1).
