/* shipto.p - generated 01/10/2001 - 13:00:09 by nosweat */

/* this program was generated by auditgn1.p and should not be modified */
/* modifications should be made to the auto generation program         */

DEFINE INPUT PARAMETER action AS CHARACTER NO-UNDO.

DEFINE VARIABLE i AS INTEGER NO-UNDO.

{methods/defines/audit.i}
FIND FIRST ttbl_header.

DEFINE WORK-TABLE ttbl_shipto1 NO-UNDO LIKE shipto.
DEFINE WORK-TABLE ttbl_shipto2 NO-UNDO LIKE shipto.

CREATE ttbl_shipto1.
IMPORT STREAM s_audit_file ttbl_shipto1.
CREATE ttbl_shipto2.
IF action = "UPDATE" THEN
IMPORT STREAM s_audit_file ttbl_shipto2.

PUT STREAM s-audit-rpt UNFORMATTED
  "Action    User ID/(User Name)            DB.Table/(Database)  Date       Time" SKIP
  "--------- ------------------------------ -------------------- ---------- --------" SKIP
  ttbl_header.v_action AT 1
  ttbl_header.v_userid AT 11
  ttbl_header.v_file AT 42
  ttbl_header.v_date FORMAT "99/99/9999" AT 63
  STRING(ttbl_header.v_time,"HH:MM:SS") AT 74.
FIND users WHERE users.user_id = v_userid NO-LOCK NO-ERROR.
IF AVAILABLE users THEN
PUT STREAM s-audit-rpt UNFORMATTED "(" AT 10 users.user_name ")".
ELSE
PUT STREAM s-audit-rpt UNFORMATTED "(user record does not exist)" AT 10.
PUT STREAM s-audit-rpt UNFORMATTED
  "(" AT 41 ttbl_header.v_db ")" SKIP(1)
  "Label: Value (database field-name)" AT 3.
IF action = "UPDATE" THEN
PUT STREAM s-audit-rpt UNFORMATTED " ( * - denotes field value change )".
PUT STREAM s-audit-rpt UNFORMATTED FILL("-",75) AT 3.
IF NOT v_field_changed OR ttbl_shipto1.bill NE ttbl_shipto2.bill OR action NE "UPDATE" THEN
DO:
  PUT STREAM s-audit-rpt UNFORMATTED "Billable: " AT 3 ttbl_shipto1.bill FORMAT "yes/no" " (bill)".
  IF action = "UPDATE" THEN
  DO:
    IF ttbl_shipto1.bill NE ttbl_shipto2.bill THEN
    PUT STREAM s-audit-rpt UNFORMATTED "*" AT 1.
    PUT STREAM s-audit-rpt UNFORMATTED "Billable: " AT 3 ttbl_shipto2.bill FORMAT "yes/no".
  END.
END.
IF NOT v_field_changed OR ttbl_shipto1.broker NE ttbl_shipto2.broker OR action NE "UPDATE" THEN
DO:
  PUT STREAM s-audit-rpt UNFORMATTED "Broker: " AT 3 ttbl_shipto1.broker FORMAT "yes/no" " (broker)".
  IF action = "UPDATE" THEN
  DO:
    IF ttbl_shipto1.broker NE ttbl_shipto2.broker THEN
    PUT STREAM s-audit-rpt UNFORMATTED "*" AT 1.
    PUT STREAM s-audit-rpt UNFORMATTED "Broker: " AT 3 ttbl_shipto2.broker FORMAT "yes/no".
  END.
END.
IF NOT v_field_changed OR ttbl_shipto1.carrier NE ttbl_shipto2.carrier OR action NE "UPDATE" THEN
DO:
  PUT STREAM s-audit-rpt UNFORMATTED "Carrier: " AT 3 ttbl_shipto1.carrier " (carrier)".
  IF action = "UPDATE" THEN
  DO:
    IF ttbl_shipto1.carrier NE ttbl_shipto2.carrier THEN
    PUT STREAM s-audit-rpt UNFORMATTED "*" AT 1.
    PUT STREAM s-audit-rpt UNFORMATTED "Carrier: " AT 3 ttbl_shipto2.carrier.
  END.
END.
IF NOT v_field_changed OR ttbl_shipto1.company NE ttbl_shipto2.company OR action NE "UPDATE" THEN
DO:
  PUT STREAM s-audit-rpt UNFORMATTED "Company: " AT 3 ttbl_shipto1.company " (company)".
  IF action = "UPDATE" THEN
  DO:
    IF ttbl_shipto1.company NE ttbl_shipto2.company THEN
    PUT STREAM s-audit-rpt UNFORMATTED "*" AT 1.
    PUT STREAM s-audit-rpt UNFORMATTED "Company: " AT 3 ttbl_shipto2.company.
  END.
END.
IF NOT v_field_changed OR ttbl_shipto1.country NE ttbl_shipto2.country OR action NE "UPDATE" THEN
DO:
  PUT STREAM s-audit-rpt UNFORMATTED "Country: " AT 3 ttbl_shipto1.country " (country)".
  IF action = "UPDATE" THEN
  DO:
    IF ttbl_shipto1.country NE ttbl_shipto2.country THEN
    PUT STREAM s-audit-rpt UNFORMATTED "*" AT 1.
    PUT STREAM s-audit-rpt UNFORMATTED "Country: " AT 3 ttbl_shipto2.country.
  END.
END.
IF NOT v_field_changed OR ttbl_shipto1.cust-no NE ttbl_shipto2.cust-no OR action NE "UPDATE" THEN
DO:
  PUT STREAM s-audit-rpt UNFORMATTED "Customer Number: " AT 3 ttbl_shipto1.cust-no " (cust-no)".
  IF action = "UPDATE" THEN
  DO:
    IF ttbl_shipto1.cust-no NE ttbl_shipto2.cust-no THEN
    PUT STREAM s-audit-rpt UNFORMATTED "*" AT 1.
    PUT STREAM s-audit-rpt UNFORMATTED "Customer Number: " AT 3 ttbl_shipto2.cust-no.
  END.
END.
IF NOT v_field_changed OR ttbl_shipto1.del-chg NE ttbl_shipto2.del-chg OR action NE "UPDATE" THEN
DO:
  PUT STREAM s-audit-rpt UNFORMATTED "Delivery Charge: " AT 3 ttbl_shipto1.del-chg FORMAT ">>9.99" " (del-chg)".
  IF action = "UPDATE" THEN
  DO:
    IF ttbl_shipto1.del-chg NE ttbl_shipto2.del-chg THEN
    PUT STREAM s-audit-rpt UNFORMATTED "*" AT 1.
    PUT STREAM s-audit-rpt UNFORMATTED "Delivery Charge: " AT 3 ttbl_shipto2.del-chg FORMAT ">>9.99".
  END.
END.
IF NOT v_field_changed OR ttbl_shipto1.del-time NE ttbl_shipto2.del-time OR action NE "UPDATE" THEN
DO:
  PUT STREAM s-audit-rpt UNFORMATTED "Delivery Hours: " AT 3 ttbl_shipto1.del-time FORMAT ">9.9" " (del-time)".
  IF action = "UPDATE" THEN
  DO:
    IF ttbl_shipto1.del-time NE ttbl_shipto2.del-time THEN
    PUT STREAM s-audit-rpt UNFORMATTED "*" AT 1.
    PUT STREAM s-audit-rpt UNFORMATTED "Delivery Hours: " AT 3 ttbl_shipto2.del-time FORMAT ">9.9".
  END.
END.
IF NOT v_field_changed OR ttbl_shipto1.dest-code NE ttbl_shipto2.dest-code OR action NE "UPDATE" THEN
DO:
  PUT STREAM s-audit-rpt UNFORMATTED "Dest. Code: " AT 3 ttbl_shipto1.dest-code " (dest-code)".
  IF action = "UPDATE" THEN
  DO:
    IF ttbl_shipto1.dest-code NE ttbl_shipto2.dest-code THEN
    PUT STREAM s-audit-rpt UNFORMATTED "*" AT 1.
    PUT STREAM s-audit-rpt UNFORMATTED "Dest. Code: " AT 3 ttbl_shipto2.dest-code.
  END.
END.
IF NOT v_field_changed OR ttbl_shipto1.dest-dscr NE ttbl_shipto2.dest-dscr OR action NE "UPDATE" THEN
DO:
  PUT STREAM s-audit-rpt UNFORMATTED "?: " AT 3 ttbl_shipto1.dest-dscr " (dest-dscr)".
  IF action = "UPDATE" THEN
  DO:
    IF ttbl_shipto1.dest-dscr NE ttbl_shipto2.dest-dscr THEN
    PUT STREAM s-audit-rpt UNFORMATTED "*" AT 1.
    PUT STREAM s-audit-rpt UNFORMATTED "?: " AT 3 ttbl_shipto2.dest-dscr.
  END.
END.
IF NOT v_field_changed OR ttbl_shipto1.loc NE ttbl_shipto2.loc OR action NE "UPDATE" THEN
DO:
  PUT STREAM s-audit-rpt UNFORMATTED "Warehouse: " AT 3 ttbl_shipto1.loc " (loc)".
  IF action = "UPDATE" THEN
  DO:
    IF ttbl_shipto1.loc NE ttbl_shipto2.loc THEN
    PUT STREAM s-audit-rpt UNFORMATTED "*" AT 1.
    PUT STREAM s-audit-rpt UNFORMATTED "Warehouse: " AT 3 ttbl_shipto2.loc.
  END.
END.
IF NOT v_field_changed OR ttbl_shipto1.loc-bin NE ttbl_shipto2.loc-bin OR action NE "UPDATE" THEN
DO:
  PUT STREAM s-audit-rpt UNFORMATTED "Bin Location: " AT 3 ttbl_shipto1.loc-bin " (loc-bin)".
  IF action = "UPDATE" THEN
  DO:
    IF ttbl_shipto1.loc-bin NE ttbl_shipto2.loc-bin THEN
    PUT STREAM s-audit-rpt UNFORMATTED "*" AT 1.
    PUT STREAM s-audit-rpt UNFORMATTED "Bin Location: " AT 3 ttbl_shipto2.loc-bin.
  END.
END.
DO i = 1 TO 4:
IF NOT v_field_changed OR ttbl_shipto1.notes[i] NE ttbl_shipto2.notes[i] OR action NE "UPDATE" THEN
DO:
  PUT STREAM s-audit-rpt UNFORMATTED "Notes[" AT 3 i "]: " ttbl_shipto1.notes[i] " (notes[" i "])".
  IF action = "UPDATE" THEN
  DO:
    IF ttbl_shipto1.notes[i] NE ttbl_shipto2.notes[i] THEN
    PUT STREAM s-audit-rpt UNFORMATTED "*" AT 1.
    PUT STREAM s-audit-rpt UNFORMATTED "Notes[" AT 3 i "]: " ttbl_shipto2.notes[i].
  END.
END.
END.
IF NOT v_field_changed OR ttbl_shipto1.pallet NE ttbl_shipto2.pallet OR action NE "UPDATE" THEN
DO:
  PUT STREAM s-audit-rpt UNFORMATTED "Pallet: " AT 3 ttbl_shipto1.pallet " (pallet)".
  IF action = "UPDATE" THEN
  DO:
    IF ttbl_shipto1.pallet NE ttbl_shipto2.pallet THEN
    PUT STREAM s-audit-rpt UNFORMATTED "*" AT 1.
    PUT STREAM s-audit-rpt UNFORMATTED "Pallet: " AT 3 ttbl_shipto2.pallet.
  END.
END.
IF NOT v_field_changed OR ttbl_shipto1.Postal NE ttbl_shipto2.Postal OR action NE "UPDATE" THEN
DO:
  PUT STREAM s-audit-rpt UNFORMATTED "Postal Code: " AT 3 ttbl_shipto1.Postal " (Postal)".
  IF action = "UPDATE" THEN
  DO:
    IF ttbl_shipto1.Postal NE ttbl_shipto2.Postal THEN
    PUT STREAM s-audit-rpt UNFORMATTED "*" AT 1.
    PUT STREAM s-audit-rpt UNFORMATTED "Postal Code: " AT 3 ttbl_shipto2.Postal.
  END.
END.
IF NOT v_field_changed OR ttbl_shipto1.scac NE ttbl_shipto2.scac OR action NE "UPDATE" THEN
DO:
  PUT STREAM s-audit-rpt UNFORMATTED "SCAC: " AT 3 ttbl_shipto1.scac " (scac)".
  IF action = "UPDATE" THEN
  DO:
    IF ttbl_shipto1.scac NE ttbl_shipto2.scac THEN
    PUT STREAM s-audit-rpt UNFORMATTED "*" AT 1.
    PUT STREAM s-audit-rpt UNFORMATTED "SCAC: " AT 3 ttbl_shipto2.scac.
  END.
END.
DO i = 1 TO 2:
IF NOT v_field_changed OR ttbl_shipto1.ship-addr[i] NE ttbl_shipto2.ship-addr[i] OR action NE "UPDATE" THEN
DO:
  PUT STREAM s-audit-rpt UNFORMATTED "Ship To[" AT 3 i "]: " ttbl_shipto1.ship-addr[i] " (ship-addr[" i "])".
  IF action = "UPDATE" THEN
  DO:
    IF ttbl_shipto1.ship-addr[i] NE ttbl_shipto2.ship-addr[i] THEN
    PUT STREAM s-audit-rpt UNFORMATTED "*" AT 1.
    PUT STREAM s-audit-rpt UNFORMATTED "Ship To[" AT 3 i "]: " ttbl_shipto2.ship-addr[i].
  END.
END.
END.
IF NOT v_field_changed OR ttbl_shipto1.ship-city NE ttbl_shipto2.ship-city OR action NE "UPDATE" THEN
DO:
  PUT STREAM s-audit-rpt UNFORMATTED "City: " AT 3 ttbl_shipto1.ship-city " (ship-city)".
  IF action = "UPDATE" THEN
  DO:
    IF ttbl_shipto1.ship-city NE ttbl_shipto2.ship-city THEN
    PUT STREAM s-audit-rpt UNFORMATTED "*" AT 1.
    PUT STREAM s-audit-rpt UNFORMATTED "City: " AT 3 ttbl_shipto2.ship-city.
  END.
END.
IF NOT v_field_changed OR ttbl_shipto1.ship-id NE ttbl_shipto2.ship-id OR action NE "UPDATE" THEN
DO:
  PUT STREAM s-audit-rpt UNFORMATTED "Ship To ID: " AT 3 ttbl_shipto1.ship-id " (ship-id)".
  IF action = "UPDATE" THEN
  DO:
    IF ttbl_shipto1.ship-id NE ttbl_shipto2.ship-id THEN
    PUT STREAM s-audit-rpt UNFORMATTED "*" AT 1.
    PUT STREAM s-audit-rpt UNFORMATTED "Ship To ID: " AT 3 ttbl_shipto2.ship-id.
  END.
END.
IF NOT v_field_changed OR ttbl_shipto1.ship-name NE ttbl_shipto2.ship-name OR action NE "UPDATE" THEN
DO:
  PUT STREAM s-audit-rpt UNFORMATTED "Name: " AT 3 ttbl_shipto1.ship-name " (ship-name)".
  IF action = "UPDATE" THEN
  DO:
    IF ttbl_shipto1.ship-name NE ttbl_shipto2.ship-name THEN
    PUT STREAM s-audit-rpt UNFORMATTED "*" AT 1.
    PUT STREAM s-audit-rpt UNFORMATTED "Name: " AT 3 ttbl_shipto2.ship-name.
  END.
END.
IF NOT v_field_changed OR ttbl_shipto1.ship-no NE ttbl_shipto2.ship-no OR action NE "UPDATE" THEN
DO:
  PUT STREAM s-audit-rpt UNFORMATTED "Ship To #: " AT 3 ttbl_shipto1.ship-no FORMAT ">>9" " (ship-no)".
  IF action = "UPDATE" THEN
  DO:
    IF ttbl_shipto1.ship-no NE ttbl_shipto2.ship-no THEN
    PUT STREAM s-audit-rpt UNFORMATTED "*" AT 1.
    PUT STREAM s-audit-rpt UNFORMATTED "Ship To #: " AT 3 ttbl_shipto2.ship-no FORMAT ">>9".
  END.
END.
IF NOT v_field_changed OR ttbl_shipto1.ship-state NE ttbl_shipto2.ship-state OR action NE "UPDATE" THEN
DO:
  PUT STREAM s-audit-rpt UNFORMATTED "State: " AT 3 ttbl_shipto1.ship-state " (ship-state)".
  IF action = "UPDATE" THEN
  DO:
    IF ttbl_shipto1.ship-state NE ttbl_shipto2.ship-state THEN
    PUT STREAM s-audit-rpt UNFORMATTED "*" AT 1.
    PUT STREAM s-audit-rpt UNFORMATTED "State: " AT 3 ttbl_shipto2.ship-state.
  END.
END.
IF NOT v_field_changed OR ttbl_shipto1.ship-zip NE ttbl_shipto2.ship-zip OR action NE "UPDATE" THEN
DO:
  PUT STREAM s-audit-rpt UNFORMATTED "Zip: " AT 3 ttbl_shipto1.ship-zip " (ship-zip)".
  IF action = "UPDATE" THEN
  DO:
    IF ttbl_shipto1.ship-zip NE ttbl_shipto2.ship-zip THEN
    PUT STREAM s-audit-rpt UNFORMATTED "*" AT 1.
    PUT STREAM s-audit-rpt UNFORMATTED "Zip: " AT 3 ttbl_shipto2.ship-zip.
  END.
END.
IF NOT v_field_changed OR ttbl_shipto1.tax-code NE ttbl_shipto2.tax-code OR action NE "UPDATE" THEN
DO:
  PUT STREAM s-audit-rpt UNFORMATTED "Tax Group: " AT 3 ttbl_shipto1.tax-code " (tax-code)".
  IF action = "UPDATE" THEN
  DO:
    IF ttbl_shipto1.tax-code NE ttbl_shipto2.tax-code THEN
    PUT STREAM s-audit-rpt UNFORMATTED "*" AT 1.
    PUT STREAM s-audit-rpt UNFORMATTED "Tax Group: " AT 3 ttbl_shipto2.tax-code.
  END.
END.
IF NOT v_field_changed OR ttbl_shipto1.tax-dscr NE ttbl_shipto2.tax-dscr OR action NE "UPDATE" THEN
DO:
  PUT STREAM s-audit-rpt UNFORMATTED "?: " AT 3 ttbl_shipto1.tax-dscr " (tax-dscr)".
  IF action = "UPDATE" THEN
  DO:
    IF ttbl_shipto1.tax-dscr NE ttbl_shipto2.tax-dscr THEN
    PUT STREAM s-audit-rpt UNFORMATTED "*" AT 1.
    PUT STREAM s-audit-rpt UNFORMATTED "?: " AT 3 ttbl_shipto2.tax-dscr.
  END.
END.
IF v_show_index THEN
DO:
  PUT STREAM s-audit-rpt UNFORMATTED "Index Fields - Label: Value (database field-name)" AT 5.
  PUT STREAM s-audit-rpt UNFORMATTED "Company: " AT 7.
  IF action = "UPDATE" THEN
  PUT STREAM s-audit-rpt UNFORMATTED ttbl_shipto2.company " (company)".
  ELSE
  PUT STREAM s-audit-rpt UNFORMATTED ttbl_shipto1.company " (company)".
  PUT STREAM s-audit-rpt UNFORMATTED "Customer Number: " AT 7.
  IF action = "UPDATE" THEN
  PUT STREAM s-audit-rpt UNFORMATTED ttbl_shipto2.cust-no " (cust-no)".
  ELSE
  PUT STREAM s-audit-rpt UNFORMATTED ttbl_shipto1.cust-no " (cust-no)".
  PUT STREAM s-audit-rpt UNFORMATTED "Ship To #: " AT 7.
  IF action = "UPDATE" THEN
  PUT STREAM s-audit-rpt UNFORMATTED ttbl_shipto2.ship-no FORMAT ">>9" " (ship-no)".
  ELSE
  PUT STREAM s-audit-rpt UNFORMATTED ttbl_shipto1.ship-no FORMAT ">>9" " (ship-no)".
END.
PUT STREAM s-audit-rpt UNFORMATTED SKIP(1).
