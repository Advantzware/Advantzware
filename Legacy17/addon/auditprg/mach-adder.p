/* mach-adder.p - generated 01/10/2001 - 13:00:04 by nosweat */

/* this program was generated by auditgn1.p and should not be modified */
/* modifications should be made to the auto generation program         */

DEFINE INPUT PARAMETER action AS CHARACTER NO-UNDO.

DEFINE VARIABLE i AS INTEGER NO-UNDO.

{methods/defines/audit.i}
FIND FIRST ttbl_header.

DEFINE WORK-TABLE ttbl_mach-adder1 NO-UNDO LIKE mach-adder.
DEFINE WORK-TABLE ttbl_mach-adder2 NO-UNDO LIKE mach-adder.

CREATE ttbl_mach-adder1.
IMPORT STREAM s_audit_file ttbl_mach-adder1.
CREATE ttbl_mach-adder2.
IF action = "UPDATE" THEN
IMPORT STREAM s_audit_file ttbl_mach-adder2.

PUT STREAM s-audit-rpt UNFORMATTED
  "Action    User ID/(User Name)            DB.Table/(Database)  Date       Time" SKIP
  "--------- ------------------------------ -------------------- ---------- --------" SKIP
  ttbl_header.v_action AT 1
  ttbl_header.v_userid AT 11
  ttbl_header.v_file AT 42
  ttbl_header.v_date FORMAT "99/99/9999" AT 63
  STRING(ttbl_header.v_time,"HH:MM:SS") AT 74.
FIND users WHERE users.user_id = v_userid NO-LOCK NO-ERROR.
IF AVAILABLE users THEN
PUT STREAM s-audit-rpt UNFORMATTED "(" AT 10 users.user_name ")".
ELSE
PUT STREAM s-audit-rpt UNFORMATTED "(user record does not exist)" AT 10.
PUT STREAM s-audit-rpt UNFORMATTED
  "(" AT 41 ttbl_header.v_db ")" SKIP(1)
  "Label: Value (database field-name)" AT 3.
IF action = "UPDATE" THEN
PUT STREAM s-audit-rpt UNFORMATTED " ( * - denotes field value change )".
PUT STREAM s-audit-rpt UNFORMATTED FILL("-",75) AT 3.
IF NOT v_field_changed OR ttbl_mach-adder1.adder-code NE ttbl_mach-adder2.adder-code OR action NE "UPDATE" THEN
DO:
  PUT STREAM s-audit-rpt UNFORMATTED "Adder Code: " AT 3 ttbl_mach-adder1.adder-code " (adder-code)".
  IF action = "UPDATE" THEN
  DO:
    IF ttbl_mach-adder1.adder-code NE ttbl_mach-adder2.adder-code THEN
    PUT STREAM s-audit-rpt UNFORMATTED "*" AT 1.
    PUT STREAM s-audit-rpt UNFORMATTED "Adder Code: " AT 3 ttbl_mach-adder2.adder-code.
  END.
END.
IF NOT v_field_changed OR ttbl_mach-adder1.adder-factor NE ttbl_mach-adder2.adder-factor OR action NE "UPDATE" THEN
DO:
  PUT STREAM s-audit-rpt UNFORMATTED "Adder Factor: " AT 3 ttbl_mach-adder1.adder-factor FORMAT "->,>>9.99" " (adder-factor)".
  IF action = "UPDATE" THEN
  DO:
    IF ttbl_mach-adder1.adder-factor NE ttbl_mach-adder2.adder-factor THEN
    PUT STREAM s-audit-rpt UNFORMATTED "*" AT 1.
    PUT STREAM s-audit-rpt UNFORMATTED "Adder Factor: " AT 3 ttbl_mach-adder2.adder-factor FORMAT "->,>>9.99".
  END.
END.
IF NOT v_field_changed OR ttbl_mach-adder1.code NE ttbl_mach-adder2.code OR action NE "UPDATE" THEN
DO:
  PUT STREAM s-audit-rpt UNFORMATTED "?: " AT 3 ttbl_mach-adder1.code " (code)".
  IF action = "UPDATE" THEN
  DO:
    IF ttbl_mach-adder1.code NE ttbl_mach-adder2.code THEN
    PUT STREAM s-audit-rpt UNFORMATTED "*" AT 1.
    PUT STREAM s-audit-rpt UNFORMATTED "?: " AT 3 ttbl_mach-adder2.code.
  END.
END.
IF NOT v_field_changed OR ttbl_mach-adder1.code1 NE ttbl_mach-adder2.code1 OR action NE "UPDATE" THEN
DO:
  PUT STREAM s-audit-rpt UNFORMATTED "?: " AT 3 ttbl_mach-adder1.code1 " (code1)".
  IF action = "UPDATE" THEN
  DO:
    IF ttbl_mach-adder1.code1 NE ttbl_mach-adder2.code1 THEN
    PUT STREAM s-audit-rpt UNFORMATTED "*" AT 1.
    PUT STREAM s-audit-rpt UNFORMATTED "?: " AT 3 ttbl_mach-adder2.code1.
  END.
END.
IF NOT v_field_changed OR ttbl_mach-adder1.code2 NE ttbl_mach-adder2.code2 OR action NE "UPDATE" THEN
DO:
  PUT STREAM s-audit-rpt UNFORMATTED "?: " AT 3 ttbl_mach-adder1.code2 " (code2)".
  IF action = "UPDATE" THEN
  DO:
    IF ttbl_mach-adder1.code2 NE ttbl_mach-adder2.code2 THEN
    PUT STREAM s-audit-rpt UNFORMATTED "*" AT 1.
    PUT STREAM s-audit-rpt UNFORMATTED "?: " AT 3 ttbl_mach-adder2.code2.
  END.
END.
IF NOT v_field_changed OR ttbl_mach-adder1.company NE ttbl_mach-adder2.company OR action NE "UPDATE" THEN
DO:
  PUT STREAM s-audit-rpt UNFORMATTED "?: " AT 3 ttbl_mach-adder1.company " (company)".
  IF action = "UPDATE" THEN
  DO:
    IF ttbl_mach-adder1.company NE ttbl_mach-adder2.company THEN
    PUT STREAM s-audit-rpt UNFORMATTED "*" AT 1.
    PUT STREAM s-audit-rpt UNFORMATTED "?: " AT 3 ttbl_mach-adder2.company.
  END.
END.
IF NOT v_field_changed OR ttbl_mach-adder1.dscr NE ttbl_mach-adder2.dscr OR action NE "UPDATE" THEN
DO:
  PUT STREAM s-audit-rpt UNFORMATTED "Description: " AT 3 ttbl_mach-adder1.dscr " (dscr)".
  IF action = "UPDATE" THEN
  DO:
    IF ttbl_mach-adder1.dscr NE ttbl_mach-adder2.dscr THEN
    PUT STREAM s-audit-rpt UNFORMATTED "*" AT 1.
    PUT STREAM s-audit-rpt UNFORMATTED "Description: " AT 3 ttbl_mach-adder2.dscr.
  END.
END.
IF NOT v_field_changed OR ttbl_mach-adder1.loc NE ttbl_mach-adder2.loc OR action NE "UPDATE" THEN
DO:
  PUT STREAM s-audit-rpt UNFORMATTED "?: " AT 3 ttbl_mach-adder1.loc " (loc)".
  IF action = "UPDATE" THEN
  DO:
    IF ttbl_mach-adder1.loc NE ttbl_mach-adder2.loc THEN
    PUT STREAM s-audit-rpt UNFORMATTED "*" AT 1.
    PUT STREAM s-audit-rpt UNFORMATTED "?: " AT 3 ttbl_mach-adder2.loc.
  END.
END.
IF NOT v_field_changed OR ttbl_mach-adder1.m-code NE ttbl_mach-adder2.m-code OR action NE "UPDATE" THEN
DO:
  PUT STREAM s-audit-rpt UNFORMATTED "?: " AT 3 ttbl_mach-adder1.m-code " (m-code)".
  IF action = "UPDATE" THEN
  DO:
    IF ttbl_mach-adder1.m-code NE ttbl_mach-adder2.m-code THEN
    PUT STREAM s-audit-rpt UNFORMATTED "*" AT 1.
    PUT STREAM s-audit-rpt UNFORMATTED "?: " AT 3 ttbl_mach-adder2.m-code.
  END.
END.
IF NOT v_field_changed OR ttbl_mach-adder1.style NE ttbl_mach-adder2.style OR action NE "UPDATE" THEN
DO:
  PUT STREAM s-audit-rpt UNFORMATTED "?: " AT 3 ttbl_mach-adder1.style " (style)".
  IF action = "UPDATE" THEN
  DO:
    IF ttbl_mach-adder1.style NE ttbl_mach-adder2.style THEN
    PUT STREAM s-audit-rpt UNFORMATTED "*" AT 1.
    PUT STREAM s-audit-rpt UNFORMATTED "?: " AT 3 ttbl_mach-adder2.style.
  END.
END.
IF NOT v_field_changed OR ttbl_mach-adder1.type NE ttbl_mach-adder2.type OR action NE "UPDATE" THEN
DO:
  PUT STREAM s-audit-rpt UNFORMATTED "Adder Type: " AT 3 ttbl_mach-adder1.type " (type)".
  IF action = "UPDATE" THEN
  DO:
    IF ttbl_mach-adder1.type NE ttbl_mach-adder2.type THEN
    PUT STREAM s-audit-rpt UNFORMATTED "*" AT 1.
    PUT STREAM s-audit-rpt UNFORMATTED "Adder Type: " AT 3 ttbl_mach-adder2.type.
  END.
END.
IF v_show_index THEN
DO:
  PUT STREAM s-audit-rpt UNFORMATTED "Index Fields - Label: Value (database field-name)" AT 5.
  PUT STREAM s-audit-rpt UNFORMATTED "?: " AT 7.
  IF action = "UPDATE" THEN
  PUT STREAM s-audit-rpt UNFORMATTED ttbl_mach-adder2.company " (company)".
  ELSE
  PUT STREAM s-audit-rpt UNFORMATTED ttbl_mach-adder1.company " (company)".
  PUT STREAM s-audit-rpt UNFORMATTED "?: " AT 7.
  IF action = "UPDATE" THEN
  PUT STREAM s-audit-rpt UNFORMATTED ttbl_mach-adder2.loc " (loc)".
  ELSE
  PUT STREAM s-audit-rpt UNFORMATTED ttbl_mach-adder1.loc " (loc)".
  PUT STREAM s-audit-rpt UNFORMATTED "?: " AT 7.
  IF action = "UPDATE" THEN
  PUT STREAM s-audit-rpt UNFORMATTED ttbl_mach-adder2.m-code " (m-code)".
  ELSE
  PUT STREAM s-audit-rpt UNFORMATTED ttbl_mach-adder1.m-code " (m-code)".
  PUT STREAM s-audit-rpt UNFORMATTED "?: " AT 7.
  IF action = "UPDATE" THEN
  PUT STREAM s-audit-rpt UNFORMATTED ttbl_mach-adder2.style " (style)".
  ELSE
  PUT STREAM s-audit-rpt UNFORMATTED ttbl_mach-adder1.style " (style)".
  PUT STREAM s-audit-rpt UNFORMATTED "Adder Code: " AT 7.
  IF action = "UPDATE" THEN
  PUT STREAM s-audit-rpt UNFORMATTED ttbl_mach-adder2.adder-code " (adder-code)".
  ELSE
  PUT STREAM s-audit-rpt UNFORMATTED ttbl_mach-adder1.adder-code " (adder-code)".
  PUT STREAM s-audit-rpt UNFORMATTED "?: " AT 7.
  IF action = "UPDATE" THEN
  PUT STREAM s-audit-rpt UNFORMATTED ttbl_mach-adder2.code " (code)".
  ELSE
  PUT STREAM s-audit-rpt UNFORMATTED ttbl_mach-adder1.code " (code)".
  PUT STREAM s-audit-rpt UNFORMATTED "?: " AT 7.
  IF action = "UPDATE" THEN
  PUT STREAM s-audit-rpt UNFORMATTED ttbl_mach-adder2.code1 " (code1)".
  ELSE
  PUT STREAM s-audit-rpt UNFORMATTED ttbl_mach-adder1.code1 " (code1)".
  PUT STREAM s-audit-rpt UNFORMATTED "?: " AT 7.
  IF action = "UPDATE" THEN
  PUT STREAM s-audit-rpt UNFORMATTED ttbl_mach-adder2.code2 " (code2)".
  ELSE
  PUT STREAM s-audit-rpt UNFORMATTED ttbl_mach-adder1.code2 " (code2)".
END.
PUT STREAM s-audit-rpt UNFORMATTED SKIP(1).
