/* ar-cashl.p - generated 01/10/2001 - 12:59:58 by nosweat */

/* this program was generated by auditgn1.p and should not be modified */
/* modifications should be made to the auto generation program         */

DEFINE INPUT PARAMETER action AS CHARACTER NO-UNDO.

DEFINE VARIABLE i AS INTEGER NO-UNDO.

{methods/defines/audit.i}
FIND FIRST ttbl_header.

DEFINE WORK-TABLE ttbl_ar-cashl1 NO-UNDO LIKE ar-cashl.
DEFINE WORK-TABLE ttbl_ar-cashl2 NO-UNDO LIKE ar-cashl.

CREATE ttbl_ar-cashl1.
IMPORT STREAM s_audit_file ttbl_ar-cashl1.
CREATE ttbl_ar-cashl2.
IF action = "UPDATE" THEN
IMPORT STREAM s_audit_file ttbl_ar-cashl2.

PUT STREAM s-audit-rpt UNFORMATTED
  "Action    User ID/(User Name)            DB.Table/(Database)  Date       Time" SKIP
  "--------- ------------------------------ -------------------- ---------- --------" SKIP
  ttbl_header.v_action AT 1
  ttbl_header.v_userid AT 11
  ttbl_header.v_file AT 42
  ttbl_header.v_date FORMAT "99/99/9999" AT 63
  STRING(ttbl_header.v_time,"HH:MM:SS") AT 74.
FIND users WHERE users.user_id = v_userid NO-LOCK NO-ERROR.
IF AVAILABLE users THEN
PUT STREAM s-audit-rpt UNFORMATTED "(" AT 10 users.user_name ")".
ELSE
PUT STREAM s-audit-rpt UNFORMATTED "(user record does not exist)" AT 10.
PUT STREAM s-audit-rpt UNFORMATTED
  "(" AT 41 ttbl_header.v_db ")" SKIP(1)
  "Label: Value (database field-name)" AT 3.
IF action = "UPDATE" THEN
PUT STREAM s-audit-rpt UNFORMATTED " ( * - denotes field value change )".
PUT STREAM s-audit-rpt UNFORMATTED FILL("-",75) AT 3.
IF NOT v_field_changed OR ttbl_ar-cashl1.actnum NE ttbl_ar-cashl2.actnum OR action NE "UPDATE" THEN
DO:
  PUT STREAM s-audit-rpt UNFORMATTED "Account No: " AT 3 ttbl_ar-cashl1.actnum " (actnum)".
  IF action = "UPDATE" THEN
  DO:
    IF ttbl_ar-cashl1.actnum NE ttbl_ar-cashl2.actnum THEN
    PUT STREAM s-audit-rpt UNFORMATTED "*" AT 1.
    PUT STREAM s-audit-rpt UNFORMATTED "Account No: " AT 3 ttbl_ar-cashl2.actnum.
  END.
END.
IF NOT v_field_changed OR ttbl_ar-cashl1.amt-disc NE ttbl_ar-cashl2.amt-disc OR action NE "UPDATE" THEN
DO:
  PUT STREAM s-audit-rpt UNFORMATTED "Discount: " AT 3 ttbl_ar-cashl1.amt-disc FORMAT "->>,>>9.99" " (amt-disc)".
  IF action = "UPDATE" THEN
  DO:
    IF ttbl_ar-cashl1.amt-disc NE ttbl_ar-cashl2.amt-disc THEN
    PUT STREAM s-audit-rpt UNFORMATTED "*" AT 1.
    PUT STREAM s-audit-rpt UNFORMATTED "Discount: " AT 3 ttbl_ar-cashl2.amt-disc FORMAT "->>,>>9.99".
  END.
END.
IF NOT v_field_changed OR ttbl_ar-cashl1.amt-due NE ttbl_ar-cashl2.amt-due OR action NE "UPDATE" THEN
DO:
  PUT STREAM s-audit-rpt UNFORMATTED "Balance: " AT 3 ttbl_ar-cashl1.amt-due FORMAT "->>,>>>,>>9.99" " (amt-due)".
  IF action = "UPDATE" THEN
  DO:
    IF ttbl_ar-cashl1.amt-due NE ttbl_ar-cashl2.amt-due THEN
    PUT STREAM s-audit-rpt UNFORMATTED "*" AT 1.
    PUT STREAM s-audit-rpt UNFORMATTED "Balance: " AT 3 ttbl_ar-cashl2.amt-due FORMAT "->>,>>>,>>9.99".
  END.
END.
IF NOT v_field_changed OR ttbl_ar-cashl1.amt-paid NE ttbl_ar-cashl2.amt-paid OR action NE "UPDATE" THEN
DO:
  PUT STREAM s-audit-rpt UNFORMATTED "Paid: " AT 3 ttbl_ar-cashl1.amt-paid FORMAT "->>,>>>,>>9.99" " (amt-paid)".
  IF action = "UPDATE" THEN
  DO:
    IF ttbl_ar-cashl1.amt-paid NE ttbl_ar-cashl2.amt-paid THEN
    PUT STREAM s-audit-rpt UNFORMATTED "*" AT 1.
    PUT STREAM s-audit-rpt UNFORMATTED "Paid: " AT 3 ttbl_ar-cashl2.amt-paid FORMAT "->>,>>>,>>9.99".
  END.
END.
IF NOT v_field_changed OR ttbl_ar-cashl1.c-no NE ttbl_ar-cashl2.c-no OR action NE "UPDATE" THEN
DO:
  PUT STREAM s-audit-rpt UNFORMATTED "?: " AT 3 ttbl_ar-cashl1.c-no FORMAT ">>>>>>>>9" " (c-no)".
  IF action = "UPDATE" THEN
  DO:
    IF ttbl_ar-cashl1.c-no NE ttbl_ar-cashl2.c-no THEN
    PUT STREAM s-audit-rpt UNFORMATTED "*" AT 1.
    PUT STREAM s-audit-rpt UNFORMATTED "?: " AT 3 ttbl_ar-cashl2.c-no FORMAT ">>>>>>>>9".
  END.
END.
IF NOT v_field_changed OR ttbl_ar-cashl1.check-no NE ttbl_ar-cashl2.check-no OR action NE "UPDATE" THEN
DO:
  PUT STREAM s-audit-rpt UNFORMATTED "Check: " AT 3 ttbl_ar-cashl1.check-no " (check-no)".
  IF action = "UPDATE" THEN
  DO:
    IF ttbl_ar-cashl1.check-no NE ttbl_ar-cashl2.check-no THEN
    PUT STREAM s-audit-rpt UNFORMATTED "*" AT 1.
    PUT STREAM s-audit-rpt UNFORMATTED "Check: " AT 3 ttbl_ar-cashl2.check-no.
  END.
END.
IF NOT v_field_changed OR ttbl_ar-cashl1.company NE ttbl_ar-cashl2.company OR action NE "UPDATE" THEN
DO:
  PUT STREAM s-audit-rpt UNFORMATTED "Company: " AT 3 ttbl_ar-cashl1.company " (company)".
  IF action = "UPDATE" THEN
  DO:
    IF ttbl_ar-cashl1.company NE ttbl_ar-cashl2.company THEN
    PUT STREAM s-audit-rpt UNFORMATTED "*" AT 1.
    PUT STREAM s-audit-rpt UNFORMATTED "Company: " AT 3 ttbl_ar-cashl2.company.
  END.
END.
IF NOT v_field_changed OR ttbl_ar-cashl1.cust-no NE ttbl_ar-cashl2.cust-no OR action NE "UPDATE" THEN
DO:
  PUT STREAM s-audit-rpt UNFORMATTED "Customer: " AT 3 ttbl_ar-cashl1.cust-no " (cust-no)".
  IF action = "UPDATE" THEN
  DO:
    IF ttbl_ar-cashl1.cust-no NE ttbl_ar-cashl2.cust-no THEN
    PUT STREAM s-audit-rpt UNFORMATTED "*" AT 1.
    PUT STREAM s-audit-rpt UNFORMATTED "Customer: " AT 3 ttbl_ar-cashl2.cust-no.
  END.
END.
IF NOT v_field_changed OR ttbl_ar-cashl1.dscr NE ttbl_ar-cashl2.dscr OR action NE "UPDATE" THEN
DO:
  PUT STREAM s-audit-rpt UNFORMATTED "Desc: " AT 3 ttbl_ar-cashl1.dscr " (dscr)".
  IF action = "UPDATE" THEN
  DO:
    IF ttbl_ar-cashl1.dscr NE ttbl_ar-cashl2.dscr THEN
    PUT STREAM s-audit-rpt UNFORMATTED "*" AT 1.
    PUT STREAM s-audit-rpt UNFORMATTED "Desc: " AT 3 ttbl_ar-cashl2.dscr.
  END.
END.
DO i = 1 TO 2:
IF NOT v_field_changed OR ttbl_ar-cashl1.dscr1[i] NE ttbl_ar-cashl2.dscr1[i] OR action NE "UPDATE" THEN
DO:
  PUT STREAM s-audit-rpt UNFORMATTED "Description2[" AT 3 i "]: " ttbl_ar-cashl1.dscr1[i] " (dscr1[" i "])".
  IF action = "UPDATE" THEN
  DO:
    IF ttbl_ar-cashl1.dscr1[i] NE ttbl_ar-cashl2.dscr1[i] THEN
    PUT STREAM s-audit-rpt UNFORMATTED "*" AT 1.
    PUT STREAM s-audit-rpt UNFORMATTED "Description2[" AT 3 i "]: " ttbl_ar-cashl2.dscr1[i].
  END.
END.
END.
IF NOT v_field_changed OR ttbl_ar-cashl1.inv-date NE ttbl_ar-cashl2.inv-date OR action NE "UPDATE" THEN
DO:
  PUT STREAM s-audit-rpt UNFORMATTED "Inv Date: " AT 3 ttbl_ar-cashl1.inv-date FORMAT "99/99/99" " (inv-date)".
  IF action = "UPDATE" THEN
  DO:
    IF ttbl_ar-cashl1.inv-date NE ttbl_ar-cashl2.inv-date THEN
    PUT STREAM s-audit-rpt UNFORMATTED "*" AT 1.
    PUT STREAM s-audit-rpt UNFORMATTED "Inv Date: " AT 3 ttbl_ar-cashl2.inv-date FORMAT "99/99/99".
  END.
END.
IF NOT v_field_changed OR ttbl_ar-cashl1.inv-no NE ttbl_ar-cashl2.inv-no OR action NE "UPDATE" THEN
DO:
  PUT STREAM s-audit-rpt UNFORMATTED "Invoice#: " AT 3 ttbl_ar-cashl1.inv-no FORMAT ">>>>>9" " (inv-no)".
  IF action = "UPDATE" THEN
  DO:
    IF ttbl_ar-cashl1.inv-no NE ttbl_ar-cashl2.inv-no THEN
    PUT STREAM s-audit-rpt UNFORMATTED "*" AT 1.
    PUT STREAM s-audit-rpt UNFORMATTED "Invoice#: " AT 3 ttbl_ar-cashl2.inv-no FORMAT ">>>>>9".
  END.
END.
IF NOT v_field_changed OR ttbl_ar-cashl1.line NE ttbl_ar-cashl2.line OR action NE "UPDATE" THEN
DO:
  PUT STREAM s-audit-rpt UNFORMATTED "?: " AT 3 ttbl_ar-cashl1.line FORMAT ">>9" " (line)".
  IF action = "UPDATE" THEN
  DO:
    IF ttbl_ar-cashl1.line NE ttbl_ar-cashl2.line THEN
    PUT STREAM s-audit-rpt UNFORMATTED "*" AT 1.
    PUT STREAM s-audit-rpt UNFORMATTED "?: " AT 3 ttbl_ar-cashl2.line FORMAT ">>9".
  END.
END.
IF NOT v_field_changed OR ttbl_ar-cashl1.memo NE ttbl_ar-cashl2.memo OR action NE "UPDATE" THEN
DO:
  PUT STREAM s-audit-rpt UNFORMATTED "?: " AT 3 ttbl_ar-cashl1.memo FORMAT "yes/no" " (memo)".
  IF action = "UPDATE" THEN
  DO:
    IF ttbl_ar-cashl1.memo NE ttbl_ar-cashl2.memo THEN
    PUT STREAM s-audit-rpt UNFORMATTED "*" AT 1.
    PUT STREAM s-audit-rpt UNFORMATTED "?: " AT 3 ttbl_ar-cashl2.memo FORMAT "yes/no".
  END.
END.
IF NOT v_field_changed OR ttbl_ar-cashl1.on-account NE ttbl_ar-cashl2.on-account OR action NE "UPDATE" THEN
DO:
  PUT STREAM s-audit-rpt UNFORMATTED "?: " AT 3 ttbl_ar-cashl1.on-account FORMAT "Yes/No" " (on-account)".
  IF action = "UPDATE" THEN
  DO:
    IF ttbl_ar-cashl1.on-account NE ttbl_ar-cashl2.on-account THEN
    PUT STREAM s-audit-rpt UNFORMATTED "*" AT 1.
    PUT STREAM s-audit-rpt UNFORMATTED "?: " AT 3 ttbl_ar-cashl2.on-account FORMAT "Yes/No".
  END.
END.
IF NOT v_field_changed OR ttbl_ar-cashl1.posted NE ttbl_ar-cashl2.posted OR action NE "UPDATE" THEN
DO:
  PUT STREAM s-audit-rpt UNFORMATTED "Posted: " AT 3 ttbl_ar-cashl1.posted FORMAT "y/n" " (posted)".
  IF action = "UPDATE" THEN
  DO:
    IF ttbl_ar-cashl1.posted NE ttbl_ar-cashl2.posted THEN
    PUT STREAM s-audit-rpt UNFORMATTED "*" AT 1.
    PUT STREAM s-audit-rpt UNFORMATTED "Posted: " AT 3 ttbl_ar-cashl2.posted FORMAT "y/n".
  END.
END.
IF v_show_index THEN
DO:
  PUT STREAM s-audit-rpt UNFORMATTED "Index Fields - Label: Value (database field-name)" AT 5.
  PUT STREAM s-audit-rpt UNFORMATTED "Company: " AT 7.
  IF action = "UPDATE" THEN
  PUT STREAM s-audit-rpt UNFORMATTED ttbl_ar-cashl2.company " (company)".
  ELSE
  PUT STREAM s-audit-rpt UNFORMATTED ttbl_ar-cashl1.company " (company)".
  PUT STREAM s-audit-rpt UNFORMATTED "Posted: " AT 7.
  IF action = "UPDATE" THEN
  PUT STREAM s-audit-rpt UNFORMATTED ttbl_ar-cashl2.posted FORMAT "y/n" " (posted)".
  ELSE
  PUT STREAM s-audit-rpt UNFORMATTED ttbl_ar-cashl1.posted FORMAT "y/n" " (posted)".
  PUT STREAM s-audit-rpt UNFORMATTED "Customer: " AT 7.
  IF action = "UPDATE" THEN
  PUT STREAM s-audit-rpt UNFORMATTED ttbl_ar-cashl2.cust-no " (cust-no)".
  ELSE
  PUT STREAM s-audit-rpt UNFORMATTED ttbl_ar-cashl1.cust-no " (cust-no)".
  PUT STREAM s-audit-rpt UNFORMATTED "Invoice#: " AT 7.
  IF action = "UPDATE" THEN
  PUT STREAM s-audit-rpt UNFORMATTED ttbl_ar-cashl2.inv-no FORMAT ">>>>>9" " (inv-no)".
  ELSE
  PUT STREAM s-audit-rpt UNFORMATTED ttbl_ar-cashl1.inv-no FORMAT ">>>>>9" " (inv-no)".
  PUT STREAM s-audit-rpt UNFORMATTED "Inv Date: " AT 7.
  IF action = "UPDATE" THEN
  PUT STREAM s-audit-rpt UNFORMATTED ttbl_ar-cashl2.inv-date FORMAT "99/99/99" " (inv-date)".
  ELSE
  PUT STREAM s-audit-rpt UNFORMATTED ttbl_ar-cashl1.inv-date FORMAT "99/99/99" " (inv-date)".
  PUT STREAM s-audit-rpt UNFORMATTED "?: " AT 7.
  IF action = "UPDATE" THEN
  PUT STREAM s-audit-rpt UNFORMATTED ttbl_ar-cashl2.c-no FORMAT ">>>>>>>>9" " (c-no)".
  ELSE
  PUT STREAM s-audit-rpt UNFORMATTED ttbl_ar-cashl1.c-no FORMAT ">>>>>>>>9" " (c-no)".
END.
PUT STREAM s-audit-rpt UNFORMATTED SKIP(1).
