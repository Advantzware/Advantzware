/* stack-size.p - generated 01/10/2001 - 13:00:09 by nosweat */

/* this program was generated by auditgn1.p and should not be modified */
/* modifications should be made to the auto generation program         */

DEFINE INPUT PARAMETER action AS CHARACTER NO-UNDO.

DEFINE VARIABLE i AS INTEGER NO-UNDO.

{methods/defines/audit.i}
FIND FIRST ttbl_header.

DEFINE WORK-TABLE ttbl_stack-size1 NO-UNDO LIKE stack-size.
DEFINE WORK-TABLE ttbl_stack-size2 NO-UNDO LIKE stack-size.

CREATE ttbl_stack-size1.
IMPORT STREAM s_audit_file ttbl_stack-size1.
CREATE ttbl_stack-size2.
IF action = "UPDATE" THEN
IMPORT STREAM s_audit_file ttbl_stack-size2.

PUT STREAM s-audit-rpt UNFORMATTED
  "Action    User ID/(User Name)            DB.Table/(Database)  Date       Time" SKIP
  "--------- ------------------------------ -------------------- ---------- --------" SKIP
  ttbl_header.v_action AT 1
  ttbl_header.v_userid AT 11
  ttbl_header.v_file AT 42
  ttbl_header.v_date FORMAT "99/99/9999" AT 63
  STRING(ttbl_header.v_time,"HH:MM:SS") AT 74.
FIND users WHERE users.user_id = v_userid NO-LOCK NO-ERROR.
IF AVAILABLE users THEN
PUT STREAM s-audit-rpt UNFORMATTED "(" AT 10 users.user_name ")".
ELSE
PUT STREAM s-audit-rpt UNFORMATTED "(user record does not exist)" AT 10.
PUT STREAM s-audit-rpt UNFORMATTED
  "(" AT 41 ttbl_header.v_db ")" SKIP(1)
  "Label: Value (database field-name)" AT 3.
IF action = "UPDATE" THEN
PUT STREAM s-audit-rpt UNFORMATTED " ( * - denotes field value change )".
PUT STREAM s-audit-rpt UNFORMATTED FILL("-",75) AT 3.
IF NOT v_field_changed OR ttbl_stack-size1.company NE ttbl_stack-size2.company OR action NE "UPDATE" THEN
DO:
  PUT STREAM s-audit-rpt UNFORMATTED "Company: " AT 3 ttbl_stack-size1.company " (company)".
  IF action = "UPDATE" THEN
  DO:
    IF ttbl_stack-size1.company NE ttbl_stack-size2.company THEN
    PUT STREAM s-audit-rpt UNFORMATTED "*" AT 1.
    PUT STREAM s-audit-rpt UNFORMATTED "Company: " AT 3 ttbl_stack-size2.company.
  END.
END.
IF NOT v_field_changed OR ttbl_stack-size1.line# NE ttbl_stack-size2.line# OR action NE "UPDATE" THEN
DO:
  PUT STREAM s-audit-rpt UNFORMATTED "Line#: " AT 3 ttbl_stack-size1.line# FORMAT ">9" " (line#)".
  IF action = "UPDATE" THEN
  DO:
    IF ttbl_stack-size1.line# NE ttbl_stack-size2.line# THEN
    PUT STREAM s-audit-rpt UNFORMATTED "*" AT 1.
    PUT STREAM s-audit-rpt UNFORMATTED "Line#: " AT 3 ttbl_stack-size2.line# FORMAT ">9".
  END.
END.
IF NOT v_field_changed OR ttbl_stack-size1.loc NE ttbl_stack-size2.loc OR action NE "UPDATE" THEN
DO:
  PUT STREAM s-audit-rpt UNFORMATTED "Location: " AT 3 ttbl_stack-size1.loc " (loc)".
  IF action = "UPDATE" THEN
  DO:
    IF ttbl_stack-size1.loc NE ttbl_stack-size2.loc THEN
    PUT STREAM s-audit-rpt UNFORMATTED "*" AT 1.
    PUT STREAM s-audit-rpt UNFORMATTED "Location: " AT 3 ttbl_stack-size2.loc.
  END.
END.
IF NOT v_field_changed OR ttbl_stack-size1.pallet NE ttbl_stack-size2.pallet OR action NE "UPDATE" THEN
DO:
  PUT STREAM s-audit-rpt UNFORMATTED "Pallet: " AT 3 ttbl_stack-size1.pallet " (pallet)".
  IF action = "UPDATE" THEN
  DO:
    IF ttbl_stack-size1.pallet NE ttbl_stack-size2.pallet THEN
    PUT STREAM s-audit-rpt UNFORMATTED "*" AT 1.
    PUT STREAM s-audit-rpt UNFORMATTED "Pallet: " AT 3 ttbl_stack-size2.pallet.
  END.
END.
IF NOT v_field_changed OR ttbl_stack-size1.vals NE ttbl_stack-size2.vals OR action NE "UPDATE" THEN
DO:
  PUT STREAM s-audit-rpt UNFORMATTED "Values: " AT 3 ttbl_stack-size1.vals " (vals)".
  IF action = "UPDATE" THEN
  DO:
    IF ttbl_stack-size1.vals NE ttbl_stack-size2.vals THEN
    PUT STREAM s-audit-rpt UNFORMATTED "*" AT 1.
    PUT STREAM s-audit-rpt UNFORMATTED "Values: " AT 3 ttbl_stack-size2.vals.
  END.
END.
IF v_show_index THEN
DO:
  PUT STREAM s-audit-rpt UNFORMATTED "Index Fields - Label: Value (database field-name)" AT 5.
  PUT STREAM s-audit-rpt UNFORMATTED "Company: " AT 7.
  IF action = "UPDATE" THEN
  PUT STREAM s-audit-rpt UNFORMATTED ttbl_stack-size2.company " (company)".
  ELSE
  PUT STREAM s-audit-rpt UNFORMATTED ttbl_stack-size1.company " (company)".
  PUT STREAM s-audit-rpt UNFORMATTED "Location: " AT 7.
  IF action = "UPDATE" THEN
  PUT STREAM s-audit-rpt UNFORMATTED ttbl_stack-size2.loc " (loc)".
  ELSE
  PUT STREAM s-audit-rpt UNFORMATTED ttbl_stack-size1.loc " (loc)".
  PUT STREAM s-audit-rpt UNFORMATTED "Pallet: " AT 7.
  IF action = "UPDATE" THEN
  PUT STREAM s-audit-rpt UNFORMATTED ttbl_stack-size2.pallet " (pallet)".
  ELSE
  PUT STREAM s-audit-rpt UNFORMATTED ttbl_stack-size1.pallet " (pallet)".
  PUT STREAM s-audit-rpt UNFORMATTED "Line#: " AT 7.
  IF action = "UPDATE" THEN
  PUT STREAM s-audit-rpt UNFORMATTED ttbl_stack-size2.line# FORMAT ">9" " (line#)".
  ELSE
  PUT STREAM s-audit-rpt UNFORMATTED ttbl_stack-size1.line# FORMAT ">9" " (line#)".
END.
PUT STREAM s-audit-rpt UNFORMATTED SKIP(1).
