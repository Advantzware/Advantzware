/* gl-jrn.p - generated 01/10/2001 - 13:00:03 by nosweat */

/* this program was generated by auditgn1.p and should not be modified */
/* modifications should be made to the auto generation program         */

DEFINE INPUT PARAMETER action AS CHARACTER NO-UNDO.

DEFINE VARIABLE i AS INTEGER NO-UNDO.

{methods/defines/audit.i}
FIND FIRST ttbl_header.

DEFINE WORK-TABLE ttbl_gl-jrn1 NO-UNDO LIKE gl-jrn.
DEFINE WORK-TABLE ttbl_gl-jrn2 NO-UNDO LIKE gl-jrn.

CREATE ttbl_gl-jrn1.
IMPORT STREAM s_audit_file ttbl_gl-jrn1.
CREATE ttbl_gl-jrn2.
IF action = "UPDATE" THEN
IMPORT STREAM s_audit_file ttbl_gl-jrn2.

PUT STREAM s-audit-rpt UNFORMATTED
  "Action    User ID/(User Name)            DB.Table/(Database)  Date       Time" SKIP
  "--------- ------------------------------ -------------------- ---------- --------" SKIP
  ttbl_header.v_action AT 1
  ttbl_header.v_userid AT 11
  ttbl_header.v_file AT 42
  ttbl_header.v_date FORMAT "99/99/9999" AT 63
  STRING(ttbl_header.v_time,"HH:MM:SS") AT 74.
FIND users WHERE users.user_id = v_userid NO-LOCK NO-ERROR.
IF AVAILABLE users THEN
PUT STREAM s-audit-rpt UNFORMATTED "(" AT 10 users.user_name ")".
ELSE
PUT STREAM s-audit-rpt UNFORMATTED "(user record does not exist)" AT 10.
PUT STREAM s-audit-rpt UNFORMATTED
  "(" AT 41 ttbl_header.v_db ")" SKIP(1)
  "Label: Value (database field-name)" AT 3.
IF action = "UPDATE" THEN
PUT STREAM s-audit-rpt UNFORMATTED " ( * - denotes field value change )".
PUT STREAM s-audit-rpt UNFORMATTED FILL("-",75) AT 3.
IF NOT v_field_changed OR ttbl_gl-jrn1.company NE ttbl_gl-jrn2.company OR action NE "UPDATE" THEN
DO:
  PUT STREAM s-audit-rpt UNFORMATTED "Company: " AT 3 ttbl_gl-jrn1.company " (company)".
  IF action = "UPDATE" THEN
  DO:
    IF ttbl_gl-jrn1.company NE ttbl_gl-jrn2.company THEN
    PUT STREAM s-audit-rpt UNFORMATTED "*" AT 1.
    PUT STREAM s-audit-rpt UNFORMATTED "Company: " AT 3 ttbl_gl-jrn2.company.
  END.
END.
IF NOT v_field_changed OR ttbl_gl-jrn1.freq-code NE ttbl_gl-jrn2.freq-code OR action NE "UPDATE" THEN
DO:
  PUT STREAM s-audit-rpt UNFORMATTED "?: " AT 3 ttbl_gl-jrn1.freq-code " (freq-code)".
  IF action = "UPDATE" THEN
  DO:
    IF ttbl_gl-jrn1.freq-code NE ttbl_gl-jrn2.freq-code THEN
    PUT STREAM s-audit-rpt UNFORMATTED "*" AT 1.
    PUT STREAM s-audit-rpt UNFORMATTED "?: " AT 3 ttbl_gl-jrn2.freq-code.
  END.
END.
IF NOT v_field_changed OR ttbl_gl-jrn1.j-no NE ttbl_gl-jrn2.j-no OR action NE "UPDATE" THEN
DO:
  PUT STREAM s-audit-rpt UNFORMATTED "Internal Journal#: " AT 3 ttbl_gl-jrn1.j-no FORMAT ">>>>>>9" " (j-no)".
  IF action = "UPDATE" THEN
  DO:
    IF ttbl_gl-jrn1.j-no NE ttbl_gl-jrn2.j-no THEN
    PUT STREAM s-audit-rpt UNFORMATTED "*" AT 1.
    PUT STREAM s-audit-rpt UNFORMATTED "Internal Journal#: " AT 3 ttbl_gl-jrn2.j-no FORMAT ">>>>>>9".
  END.
END.
IF NOT v_field_changed OR ttbl_gl-jrn1.journal NE ttbl_gl-jrn2.journal OR action NE "UPDATE" THEN
DO:
  PUT STREAM s-audit-rpt UNFORMATTED "Journal #: " AT 3 ttbl_gl-jrn1.journal FORMAT ">>>>>>9" " (journal)".
  IF action = "UPDATE" THEN
  DO:
    IF ttbl_gl-jrn1.journal NE ttbl_gl-jrn2.journal THEN
    PUT STREAM s-audit-rpt UNFORMATTED "*" AT 1.
    PUT STREAM s-audit-rpt UNFORMATTED "Journal #: " AT 3 ttbl_gl-jrn2.journal FORMAT ">>>>>>9".
  END.
END.
IF NOT v_field_changed OR ttbl_gl-jrn1.period NE ttbl_gl-jrn2.period OR action NE "UPDATE" THEN
DO:
  PUT STREAM s-audit-rpt UNFORMATTED "?: " AT 3 ttbl_gl-jrn1.period FORMAT "99" " (period)".
  IF action = "UPDATE" THEN
  DO:
    IF ttbl_gl-jrn1.period NE ttbl_gl-jrn2.period THEN
    PUT STREAM s-audit-rpt UNFORMATTED "*" AT 1.
    PUT STREAM s-audit-rpt UNFORMATTED "?: " AT 3 ttbl_gl-jrn2.period FORMAT "99".
  END.
END.
IF NOT v_field_changed OR ttbl_gl-jrn1.posted NE ttbl_gl-jrn2.posted OR action NE "UPDATE" THEN
DO:
  PUT STREAM s-audit-rpt UNFORMATTED "Posted: " AT 3 ttbl_gl-jrn1.posted FORMAT "y/n" " (posted)".
  IF action = "UPDATE" THEN
  DO:
    IF ttbl_gl-jrn1.posted NE ttbl_gl-jrn2.posted THEN
    PUT STREAM s-audit-rpt UNFORMATTED "*" AT 1.
    PUT STREAM s-audit-rpt UNFORMATTED "Posted: " AT 3 ttbl_gl-jrn2.posted FORMAT "y/n".
  END.
END.
IF NOT v_field_changed OR ttbl_gl-jrn1.recur NE ttbl_gl-jrn2.recur OR action NE "UPDATE" THEN
DO:
  PUT STREAM s-audit-rpt UNFORMATTED "Recurring Entry: " AT 3 ttbl_gl-jrn1.recur FORMAT "yes/no" " (recur)".
  IF action = "UPDATE" THEN
  DO:
    IF ttbl_gl-jrn1.recur NE ttbl_gl-jrn2.recur THEN
    PUT STREAM s-audit-rpt UNFORMATTED "*" AT 1.
    PUT STREAM s-audit-rpt UNFORMATTED "Recurring Entry: " AT 3 ttbl_gl-jrn2.recur FORMAT "yes/no".
  END.
END.
IF NOT v_field_changed OR ttbl_gl-jrn1.tcred NE ttbl_gl-jrn2.tcred OR action NE "UPDATE" THEN
DO:
  PUT STREAM s-audit-rpt UNFORMATTED "Credits: " AT 3 ttbl_gl-jrn1.tcred FORMAT "->>,>>>,>>9.99" " (tcred)".
  IF action = "UPDATE" THEN
  DO:
    IF ttbl_gl-jrn1.tcred NE ttbl_gl-jrn2.tcred THEN
    PUT STREAM s-audit-rpt UNFORMATTED "*" AT 1.
    PUT STREAM s-audit-rpt UNFORMATTED "Credits: " AT 3 ttbl_gl-jrn2.tcred FORMAT "->>,>>>,>>9.99".
  END.
END.
IF NOT v_field_changed OR ttbl_gl-jrn1.tdeb NE ttbl_gl-jrn2.tdeb OR action NE "UPDATE" THEN
DO:
  PUT STREAM s-audit-rpt UNFORMATTED "Debits: " AT 3 ttbl_gl-jrn1.tdeb FORMAT "->>,>>>,>>9.99" " (tdeb)".
  IF action = "UPDATE" THEN
  DO:
    IF ttbl_gl-jrn1.tdeb NE ttbl_gl-jrn2.tdeb THEN
    PUT STREAM s-audit-rpt UNFORMATTED "*" AT 1.
    PUT STREAM s-audit-rpt UNFORMATTED "Debits: " AT 3 ttbl_gl-jrn2.tdeb FORMAT "->>,>>>,>>9.99".
  END.
END.
IF NOT v_field_changed OR ttbl_gl-jrn1.tr-amt NE ttbl_gl-jrn2.tr-amt OR action NE "UPDATE" THEN
DO:
  PUT STREAM s-audit-rpt UNFORMATTED "Amount: " AT 3 ttbl_gl-jrn1.tr-amt FORMAT "->>,>>>,>>9.99" " (tr-amt)".
  IF action = "UPDATE" THEN
  DO:
    IF ttbl_gl-jrn1.tr-amt NE ttbl_gl-jrn2.tr-amt THEN
    PUT STREAM s-audit-rpt UNFORMATTED "*" AT 1.
    PUT STREAM s-audit-rpt UNFORMATTED "Amount: " AT 3 ttbl_gl-jrn2.tr-amt FORMAT "->>,>>>,>>9.99".
  END.
END.
IF NOT v_field_changed OR ttbl_gl-jrn1.tr-date NE ttbl_gl-jrn2.tr-date OR action NE "UPDATE" THEN
DO:
  PUT STREAM s-audit-rpt UNFORMATTED "Trx Date: " AT 3 ttbl_gl-jrn1.tr-date FORMAT "99/99/99" " (tr-date)".
  IF action = "UPDATE" THEN
  DO:
    IF ttbl_gl-jrn1.tr-date NE ttbl_gl-jrn2.tr-date THEN
    PUT STREAM s-audit-rpt UNFORMATTED "*" AT 1.
    PUT STREAM s-audit-rpt UNFORMATTED "Trx Date: " AT 3 ttbl_gl-jrn2.tr-date FORMAT "99/99/99".
  END.
END.
IF NOT v_field_changed OR ttbl_gl-jrn1.user-id NE ttbl_gl-jrn2.user-id OR action NE "UPDATE" THEN
DO:
  PUT STREAM s-audit-rpt UNFORMATTED "?: " AT 3 ttbl_gl-jrn1.user-id " (user-id)".
  IF action = "UPDATE" THEN
  DO:
    IF ttbl_gl-jrn1.user-id NE ttbl_gl-jrn2.user-id THEN
    PUT STREAM s-audit-rpt UNFORMATTED "*" AT 1.
    PUT STREAM s-audit-rpt UNFORMATTED "?: " AT 3 ttbl_gl-jrn2.user-id.
  END.
END.
IF v_show_index THEN
DO:
  PUT STREAM s-audit-rpt UNFORMATTED "Index Fields - Label: Value (database field-name)" AT 5.
  PUT STREAM s-audit-rpt UNFORMATTED "Posted: " AT 7.
  IF action = "UPDATE" THEN
  PUT STREAM s-audit-rpt UNFORMATTED ttbl_gl-jrn2.posted FORMAT "y/n" " (posted)".
  ELSE
  PUT STREAM s-audit-rpt UNFORMATTED ttbl_gl-jrn1.posted FORMAT "y/n" " (posted)".
  PUT STREAM s-audit-rpt UNFORMATTED "Company: " AT 7.
  IF action = "UPDATE" THEN
  PUT STREAM s-audit-rpt UNFORMATTED ttbl_gl-jrn2.company " (company)".
  ELSE
  PUT STREAM s-audit-rpt UNFORMATTED ttbl_gl-jrn1.company " (company)".
  PUT STREAM s-audit-rpt UNFORMATTED "Trx Date: " AT 7.
  IF action = "UPDATE" THEN
  PUT STREAM s-audit-rpt UNFORMATTED ttbl_gl-jrn2.tr-date FORMAT "99/99/99" " (tr-date)".
  ELSE
  PUT STREAM s-audit-rpt UNFORMATTED ttbl_gl-jrn1.tr-date FORMAT "99/99/99" " (tr-date)".
  PUT STREAM s-audit-rpt UNFORMATTED "Journal #: " AT 7.
  IF action = "UPDATE" THEN
  PUT STREAM s-audit-rpt UNFORMATTED ttbl_gl-jrn2.journal FORMAT ">>>>>>9" " (journal)".
  ELSE
  PUT STREAM s-audit-rpt UNFORMATTED ttbl_gl-jrn1.journal FORMAT ">>>>>>9" " (journal)".
END.
PUT STREAM s-audit-rpt UNFORMATTED SKIP(1).
