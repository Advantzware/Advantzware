/* emp.p - generated 01/10/2001 - 13:00:02 by nosweat */

/* this program was generated by auditgn1.p and should not be modified */
/* modifications should be made to the auto generation program         */

DEFINE INPUT PARAMETER action AS CHARACTER NO-UNDO.

DEFINE VARIABLE i AS INTEGER NO-UNDO.

{methods/defines/audit.i}
FIND FIRST ttbl_header.

DEFINE WORK-TABLE ttbl_emp1 NO-UNDO LIKE emp.
DEFINE WORK-TABLE ttbl_emp2 NO-UNDO LIKE emp.

CREATE ttbl_emp1.
IMPORT STREAM s_audit_file ttbl_emp1.
CREATE ttbl_emp2.
IF action = "UPDATE" THEN
IMPORT STREAM s_audit_file ttbl_emp2.

PUT STREAM s-audit-rpt UNFORMATTED
  "Action    User ID/(User Name)            DB.Table/(Database)  Date       Time" SKIP
  "--------- ------------------------------ -------------------- ---------- --------" SKIP
  ttbl_header.v_action AT 1
  ttbl_header.v_userid AT 11
  ttbl_header.v_file AT 42
  ttbl_header.v_date FORMAT "99/99/9999" AT 63
  STRING(ttbl_header.v_time,"HH:MM:SS") AT 74.
FIND users WHERE users.user_id = v_userid NO-LOCK NO-ERROR.
IF AVAILABLE users THEN
PUT STREAM s-audit-rpt UNFORMATTED "(" AT 10 users.user_name ")".
ELSE
PUT STREAM s-audit-rpt UNFORMATTED "(user record does not exist)" AT 10.
PUT STREAM s-audit-rpt UNFORMATTED
  "(" AT 41 ttbl_header.v_db ")" SKIP(1)
  "Label: Value (database field-name)" AT 3.
IF action = "UPDATE" THEN
PUT STREAM s-audit-rpt UNFORMATTED " ( * - denotes field value change )".
PUT STREAM s-audit-rpt UNFORMATTED FILL("-",75) AT 3.
IF NOT v_field_changed OR ttbl_emp1.actnum NE ttbl_emp2.actnum OR action NE "UPDATE" THEN
DO:
  PUT STREAM s-audit-rpt UNFORMATTED "GL Acct: " AT 3 ttbl_emp1.actnum " (actnum)".
  IF action = "UPDATE" THEN
  DO:
    IF ttbl_emp1.actnum NE ttbl_emp2.actnum THEN
    PUT STREAM s-audit-rpt UNFORMATTED "*" AT 1.
    PUT STREAM s-audit-rpt UNFORMATTED "GL Acct: " AT 3 ttbl_emp2.actnum.
  END.
END.
IF NOT v_field_changed OR ttbl_emp1.company NE ttbl_emp2.company OR action NE "UPDATE" THEN
DO:
  PUT STREAM s-audit-rpt UNFORMATTED "Company: " AT 3 ttbl_emp1.company " (company)".
  IF action = "UPDATE" THEN
  DO:
    IF ttbl_emp1.company NE ttbl_emp2.company THEN
    PUT STREAM s-audit-rpt UNFORMATTED "*" AT 1.
    PUT STREAM s-audit-rpt UNFORMATTED "Company: " AT 3 ttbl_emp2.company.
  END.
END.
IF NOT v_field_changed OR ttbl_emp1.emp-id NE ttbl_emp2.emp-id OR action NE "UPDATE" THEN
DO:
  PUT STREAM s-audit-rpt UNFORMATTED "Emp ID: " AT 3 ttbl_emp1.emp-id " (emp-id)".
  IF action = "UPDATE" THEN
  DO:
    IF ttbl_emp1.emp-id NE ttbl_emp2.emp-id THEN
    PUT STREAM s-audit-rpt UNFORMATTED "*" AT 1.
    PUT STREAM s-audit-rpt UNFORMATTED "Emp ID: " AT 3 ttbl_emp2.emp-id.
  END.
END.
IF NOT v_field_changed OR ttbl_emp1.emp-no NE ttbl_emp2.emp-no OR action NE "UPDATE" THEN
DO:
  PUT STREAM s-audit-rpt UNFORMATTED "Employee Number: " AT 3 ttbl_emp1.emp-no " (emp-no)".
  IF action = "UPDATE" THEN
  DO:
    IF ttbl_emp1.emp-no NE ttbl_emp2.emp-no THEN
    PUT STREAM s-audit-rpt UNFORMATTED "*" AT 1.
    PUT STREAM s-audit-rpt UNFORMATTED "Employee Number: " AT 3 ttbl_emp2.emp-no.
  END.
END.
IF NOT v_field_changed OR ttbl_emp1.emp-type NE ttbl_emp2.emp-type OR action NE "UPDATE" THEN
DO:
  PUT STREAM s-audit-rpt UNFORMATTED "Employee Type: " AT 3 ttbl_emp1.emp-type " (emp-type)".
  IF action = "UPDATE" THEN
  DO:
    IF ttbl_emp1.emp-type NE ttbl_emp2.emp-type THEN
    PUT STREAM s-audit-rpt UNFORMATTED "*" AT 1.
    PUT STREAM s-audit-rpt UNFORMATTED "Employee Type: " AT 3 ttbl_emp2.emp-type.
  END.
END.
IF NOT v_field_changed OR ttbl_emp1.f-name NE ttbl_emp2.f-name OR action NE "UPDATE" THEN
DO:
  PUT STREAM s-audit-rpt UNFORMATTED "First Name: " AT 3 ttbl_emp1.f-name " (f-name)".
  IF action = "UPDATE" THEN
  DO:
    IF ttbl_emp1.f-name NE ttbl_emp2.f-name THEN
    PUT STREAM s-audit-rpt UNFORMATTED "*" AT 1.
    PUT STREAM s-audit-rpt UNFORMATTED "First Name: " AT 3 ttbl_emp2.f-name.
  END.
END.
IF NOT v_field_changed OR ttbl_emp1.l-name NE ttbl_emp2.l-name OR action NE "UPDATE" THEN
DO:
  PUT STREAM s-audit-rpt UNFORMATTED "Last Name: " AT 3 ttbl_emp1.l-name " (l-name)".
  IF action = "UPDATE" THEN
  DO:
    IF ttbl_emp1.l-name NE ttbl_emp2.l-name THEN
    PUT STREAM s-audit-rpt UNFORMATTED "*" AT 1.
    PUT STREAM s-audit-rpt UNFORMATTED "Last Name: " AT 3 ttbl_emp2.l-name.
  END.
END.
DO i = 1 TO 3:
IF NOT v_field_changed OR ttbl_emp1.rate[i] NE ttbl_emp2.rate[i] OR action NE "UPDATE" THEN
DO:
  PUT STREAM s-audit-rpt UNFORMATTED "Rate[" AT 3 i "]: " ttbl_emp1.rate[i] FORMAT ">>9.99" " (rate[" i "])".
  IF action = "UPDATE" THEN
  DO:
    IF ttbl_emp1.rate[i] NE ttbl_emp2.rate[i] THEN
    PUT STREAM s-audit-rpt UNFORMATTED "*" AT 1.
    PUT STREAM s-audit-rpt UNFORMATTED "Rate[" AT 3 i "]: " ttbl_emp2.rate[i] FORMAT ">>9.99".
  END.
END.
END.
IF NOT v_field_changed OR ttbl_emp1.start-date NE ttbl_emp2.start-date OR action NE "UPDATE" THEN
DO:
  PUT STREAM s-audit-rpt UNFORMATTED "Start Date: " AT 3 ttbl_emp1.start-date FORMAT "99/99/9999" " (start-date)".
  IF action = "UPDATE" THEN
  DO:
    IF ttbl_emp1.start-date NE ttbl_emp2.start-date THEN
    PUT STREAM s-audit-rpt UNFORMATTED "*" AT 1.
    PUT STREAM s-audit-rpt UNFORMATTED "Start Date: " AT 3 ttbl_emp2.start-date FORMAT "99/99/9999".
  END.
END.
IF v_show_index THEN
DO:
  PUT STREAM s-audit-rpt UNFORMATTED "Index Fields - Label: Value (database field-name)" AT 5.
  PUT STREAM s-audit-rpt UNFORMATTED "Company: " AT 7.
  IF action = "UPDATE" THEN
  PUT STREAM s-audit-rpt UNFORMATTED ttbl_emp2.company " (company)".
  ELSE
  PUT STREAM s-audit-rpt UNFORMATTED ttbl_emp1.company " (company)".
  PUT STREAM s-audit-rpt UNFORMATTED "Emp ID: " AT 7.
  IF action = "UPDATE" THEN
  PUT STREAM s-audit-rpt UNFORMATTED ttbl_emp2.emp-id " (emp-id)".
  ELSE
  PUT STREAM s-audit-rpt UNFORMATTED ttbl_emp1.emp-id " (emp-id)".
END.
PUT STREAM s-audit-rpt UNFORMATTED SKIP(1).
