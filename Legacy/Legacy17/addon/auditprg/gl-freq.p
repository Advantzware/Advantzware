/* gl-freq.p - generated 01/10/2001 - 13:00:03 by nosweat */

/* this program was generated by auditgn1.p and should not be modified */
/* modifications should be made to the auto generation program         */

DEFINE INPUT PARAMETER action AS CHARACTER NO-UNDO.

DEFINE VARIABLE i AS INTEGER NO-UNDO.

{methods/defines/audit.i}
FIND FIRST ttbl_header.

DEFINE WORK-TABLE ttbl_gl-freq1 NO-UNDO LIKE gl-freq.
DEFINE WORK-TABLE ttbl_gl-freq2 NO-UNDO LIKE gl-freq.

CREATE ttbl_gl-freq1.
IMPORT STREAM s_audit_file ttbl_gl-freq1.
CREATE ttbl_gl-freq2.
IF action = "UPDATE" THEN
IMPORT STREAM s_audit_file ttbl_gl-freq2.

PUT STREAM s-audit-rpt UNFORMATTED
  "Action    User ID/(User Name)            DB.Table/(Database)  Date       Time" SKIP
  "--------- ------------------------------ -------------------- ---------- --------" SKIP
  ttbl_header.v_action AT 1
  ttbl_header.v_userid AT 11
  ttbl_header.v_file AT 42
  ttbl_header.v_date FORMAT "99/99/9999" AT 63
  STRING(ttbl_header.v_time,"HH:MM:SS") AT 74.
FIND users WHERE users.user_id = v_userid NO-LOCK NO-ERROR.
IF AVAILABLE users THEN
PUT STREAM s-audit-rpt UNFORMATTED "(" AT 10 users.user_name ")".
ELSE
PUT STREAM s-audit-rpt UNFORMATTED "(user record does not exist)" AT 10.
PUT STREAM s-audit-rpt UNFORMATTED
  "(" AT 41 ttbl_header.v_db ")" SKIP(1)
  "Label: Value (database field-name)" AT 3.
IF action = "UPDATE" THEN
PUT STREAM s-audit-rpt UNFORMATTED " ( * - denotes field value change )".
PUT STREAM s-audit-rpt UNFORMATTED FILL("-",75) AT 3.
IF NOT v_field_changed OR ttbl_gl-freq1.company NE ttbl_gl-freq2.company OR action NE "UPDATE" THEN
DO:
  PUT STREAM s-audit-rpt UNFORMATTED "Company: " AT 3 ttbl_gl-freq1.company " (company)".
  IF action = "UPDATE" THEN
  DO:
    IF ttbl_gl-freq1.company NE ttbl_gl-freq2.company THEN
    PUT STREAM s-audit-rpt UNFORMATTED "*" AT 1.
    PUT STREAM s-audit-rpt UNFORMATTED "Company: " AT 3 ttbl_gl-freq2.company.
  END.
END.
IF NOT v_field_changed OR ttbl_gl-freq1.freq-code NE ttbl_gl-freq2.freq-code OR action NE "UPDATE" THEN
DO:
  PUT STREAM s-audit-rpt UNFORMATTED "Frequency Code: " AT 3 ttbl_gl-freq1.freq-code " (freq-code)".
  IF action = "UPDATE" THEN
  DO:
    IF ttbl_gl-freq1.freq-code NE ttbl_gl-freq2.freq-code THEN
    PUT STREAM s-audit-rpt UNFORMATTED "*" AT 1.
    PUT STREAM s-audit-rpt UNFORMATTED "Frequency Code: " AT 3 ttbl_gl-freq2.freq-code.
  END.
END.
IF NOT v_field_changed OR ttbl_gl-freq1.freq-dscr NE ttbl_gl-freq2.freq-dscr OR action NE "UPDATE" THEN
DO:
  PUT STREAM s-audit-rpt UNFORMATTED "Frequency Description: " AT 3 ttbl_gl-freq1.freq-dscr " (freq-dscr)".
  IF action = "UPDATE" THEN
  DO:
    IF ttbl_gl-freq1.freq-dscr NE ttbl_gl-freq2.freq-dscr THEN
    PUT STREAM s-audit-rpt UNFORMATTED "*" AT 1.
    PUT STREAM s-audit-rpt UNFORMATTED "Frequency Description: " AT 3 ttbl_gl-freq2.freq-dscr.
  END.
END.
IF v_show_index THEN
DO:
  PUT STREAM s-audit-rpt UNFORMATTED "Index Fields - Label: Value (database field-name)" AT 5.
  PUT STREAM s-audit-rpt UNFORMATTED "Company: " AT 7.
  IF action = "UPDATE" THEN
  PUT STREAM s-audit-rpt UNFORMATTED ttbl_gl-freq2.company " (company)".
  ELSE
  PUT STREAM s-audit-rpt UNFORMATTED ttbl_gl-freq1.company " (company)".
  PUT STREAM s-audit-rpt UNFORMATTED "Frequency Code: " AT 7.
  IF action = "UPDATE" THEN
  PUT STREAM s-audit-rpt UNFORMATTED ttbl_gl-freq2.freq-code " (freq-code)".
  ELSE
  PUT STREAM s-audit-rpt UNFORMATTED ttbl_gl-freq1.freq-code " (freq-code)".
END.
PUT STREAM s-audit-rpt UNFORMATTED SKIP(1).
