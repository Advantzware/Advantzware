/* usrx.p - generated 01/10/2001 - 13:00:10 by nosweat */

/* this program was generated by auditgn1.p and should not be modified */
/* modifications should be made to the auto generation program         */

DEFINE INPUT PARAMETER action AS CHARACTER NO-UNDO.

DEFINE VARIABLE i AS INTEGER NO-UNDO.

{methods/defines/audit.i}
FIND FIRST ttbl_header.

DEFINE WORK-TABLE ttbl_usrx1 NO-UNDO LIKE usrx.
DEFINE WORK-TABLE ttbl_usrx2 NO-UNDO LIKE usrx.

CREATE ttbl_usrx1.
IMPORT STREAM s_audit_file ttbl_usrx1.
CREATE ttbl_usrx2.
IF action = "UPDATE" THEN
IMPORT STREAM s_audit_file ttbl_usrx2.

PUT STREAM s-audit-rpt UNFORMATTED
  "Action    User ID/(User Name)            DB.Table/(Database)  Date       Time" SKIP
  "--------- ------------------------------ -------------------- ---------- --------" SKIP
  ttbl_header.v_action AT 1
  ttbl_header.v_userid AT 11
  ttbl_header.v_file AT 42
  ttbl_header.v_date FORMAT "99/99/9999" AT 63
  STRING(ttbl_header.v_time,"HH:MM:SS") AT 74.
FIND users WHERE users.user_id = v_userid NO-LOCK NO-ERROR.
IF AVAILABLE users THEN
PUT STREAM s-audit-rpt UNFORMATTED "(" AT 10 users.user_name ")".
ELSE
PUT STREAM s-audit-rpt UNFORMATTED "(user record does not exist)" AT 10.
PUT STREAM s-audit-rpt UNFORMATTED
  "(" AT 41 ttbl_header.v_db ")" SKIP(1)
  "Label: Value (database field-name)" AT 3.
IF action = "UPDATE" THEN
PUT STREAM s-audit-rpt UNFORMATTED " ( * - denotes field value change )".
PUT STREAM s-audit-rpt UNFORMATTED FILL("-",75) AT 3.
IF NOT v_field_changed OR ttbl_usrx1.company NE ttbl_usrx2.company OR action NE "UPDATE" THEN
DO:
  PUT STREAM s-audit-rpt UNFORMATTED "Company: " AT 3 ttbl_usrx1.company " (company)".
  IF action = "UPDATE" THEN
  DO:
    IF ttbl_usrx1.company NE ttbl_usrx2.company THEN
    PUT STREAM s-audit-rpt UNFORMATTED "*" AT 1.
    PUT STREAM s-audit-rpt UNFORMATTED "Company: " AT 3 ttbl_usrx2.company.
  END.
END.
IF NOT v_field_changed OR ttbl_usrx1.gid NE ttbl_usrx2.gid OR action NE "UPDATE" THEN
DO:
  PUT STREAM s-audit-rpt UNFORMATTED "Group ID: " AT 3 ttbl_usrx1.gid " (gid)".
  IF action = "UPDATE" THEN
  DO:
    IF ttbl_usrx1.gid NE ttbl_usrx2.gid THEN
    PUT STREAM s-audit-rpt UNFORMATTED "*" AT 1.
    PUT STREAM s-audit-rpt UNFORMATTED "Group ID: " AT 3 ttbl_usrx2.gid.
  END.
END.
IF NOT v_field_changed OR ttbl_usrx1.loc NE ttbl_usrx2.loc OR action NE "UPDATE" THEN
DO:
  PUT STREAM s-audit-rpt UNFORMATTED "Warehouse: " AT 3 ttbl_usrx1.loc " (loc)".
  IF action = "UPDATE" THEN
  DO:
    IF ttbl_usrx1.loc NE ttbl_usrx2.loc THEN
    PUT STREAM s-audit-rpt UNFORMATTED "*" AT 1.
    PUT STREAM s-audit-rpt UNFORMATTED "Warehouse: " AT 3 ttbl_usrx2.loc.
  END.
END.
IF NOT v_field_changed OR ttbl_usrx1.pr-132 NE ttbl_usrx2.pr-132 OR action NE "UPDATE" THEN
DO:
  PUT STREAM s-audit-rpt UNFORMATTED "Print-132: " AT 3 ttbl_usrx1.pr-132 FORMAT ">9" " (pr-132)".
  IF action = "UPDATE" THEN
  DO:
    IF ttbl_usrx1.pr-132 NE ttbl_usrx2.pr-132 THEN
    PUT STREAM s-audit-rpt UNFORMATTED "*" AT 1.
    PUT STREAM s-audit-rpt UNFORMATTED "Print-132: " AT 3 ttbl_usrx2.pr-132 FORMAT ">9".
  END.
END.
IF NOT v_field_changed OR ttbl_usrx1.pr-80 NE ttbl_usrx2.pr-80 OR action NE "UPDATE" THEN
DO:
  PUT STREAM s-audit-rpt UNFORMATTED "Print-80: " AT 3 ttbl_usrx1.pr-80 FORMAT "z9" " (pr-80)".
  IF action = "UPDATE" THEN
  DO:
    IF ttbl_usrx1.pr-80 NE ttbl_usrx2.pr-80 THEN
    PUT STREAM s-audit-rpt UNFORMATTED "*" AT 1.
    PUT STREAM s-audit-rpt UNFORMATTED "Print-80: " AT 3 ttbl_usrx2.pr-80 FORMAT "z9".
  END.
END.
IF NOT v_field_changed OR ttbl_usrx1.uid NE ttbl_usrx2.uid OR action NE "UPDATE" THEN
DO:
  PUT STREAM s-audit-rpt UNFORMATTED "User ID: " AT 3 ttbl_usrx1.uid " (uid)".
  IF action = "UPDATE" THEN
  DO:
    IF ttbl_usrx1.uid NE ttbl_usrx2.uid THEN
    PUT STREAM s-audit-rpt UNFORMATTED "*" AT 1.
    PUT STREAM s-audit-rpt UNFORMATTED "User ID: " AT 3 ttbl_usrx2.uid.
  END.
END.
IF v_show_index THEN
DO:
  PUT STREAM s-audit-rpt UNFORMATTED "Index Fields - Label: Value (database field-name)" AT 5.
  PUT STREAM s-audit-rpt UNFORMATTED "Company: " AT 7.
  IF action = "UPDATE" THEN
  PUT STREAM s-audit-rpt UNFORMATTED ttbl_usrx2.company " (company)".
  ELSE
  PUT STREAM s-audit-rpt UNFORMATTED ttbl_usrx1.company " (company)".
  PUT STREAM s-audit-rpt UNFORMATTED "Warehouse: " AT 7.
  IF action = "UPDATE" THEN
  PUT STREAM s-audit-rpt UNFORMATTED ttbl_usrx2.loc " (loc)".
  ELSE
  PUT STREAM s-audit-rpt UNFORMATTED ttbl_usrx1.loc " (loc)".
  PUT STREAM s-audit-rpt UNFORMATTED "User ID: " AT 7.
  IF action = "UPDATE" THEN
  PUT STREAM s-audit-rpt UNFORMATTED ttbl_usrx2.uid " (uid)".
  ELSE
  PUT STREAM s-audit-rpt UNFORMATTED ttbl_usrx1.uid " (uid)".
END.
PUT STREAM s-audit-rpt UNFORMATTED SKIP(1).
