/* jc-ctrl.p - generated 01/10/2001 - 13:00:04 by nosweat */

/* this program was generated by auditgn1.p and should not be modified */
/* modifications should be made to the auto generation program         */

DEFINE INPUT PARAMETER action AS CHARACTER NO-UNDO.

DEFINE VARIABLE i AS INTEGER NO-UNDO.

{methods/defines/audit.i}
FIND FIRST ttbl_header.

DEFINE WORK-TABLE ttbl_jc-ctrl1 NO-UNDO LIKE jc-ctrl.
DEFINE WORK-TABLE ttbl_jc-ctrl2 NO-UNDO LIKE jc-ctrl.

CREATE ttbl_jc-ctrl1.
IMPORT STREAM s_audit_file ttbl_jc-ctrl1.
CREATE ttbl_jc-ctrl2.
IF action = "UPDATE" THEN
IMPORT STREAM s_audit_file ttbl_jc-ctrl2.

PUT STREAM s-audit-rpt UNFORMATTED
  "Action    User ID/(User Name)            DB.Table/(Database)  Date       Time" SKIP
  "--------- ------------------------------ -------------------- ---------- --------" SKIP
  ttbl_header.v_action AT 1
  ttbl_header.v_userid AT 11
  ttbl_header.v_file AT 42
  ttbl_header.v_date FORMAT "99/99/9999" AT 63
  STRING(ttbl_header.v_time,"HH:MM:SS") AT 74.
FIND users WHERE users.user_id = v_userid NO-LOCK NO-ERROR.
IF AVAILABLE users THEN
PUT STREAM s-audit-rpt UNFORMATTED "(" AT 10 users.user_name ")".
ELSE
PUT STREAM s-audit-rpt UNFORMATTED "(user record does not exist)" AT 10.
PUT STREAM s-audit-rpt UNFORMATTED
  "(" AT 41 ttbl_header.v_db ")" SKIP(1)
  "Label: Value (database field-name)" AT 3.
IF action = "UPDATE" THEN
PUT STREAM s-audit-rpt UNFORMATTED " ( * - denotes field value change )".
PUT STREAM s-audit-rpt UNFORMATTED FILL("-",75) AT 3.
IF NOT v_field_changed OR ttbl_jc-ctrl1.auto-all NE ttbl_jc-ctrl2.auto-all OR action NE "UPDATE" THEN
DO:
  PUT STREAM s-audit-rpt UNFORMATTED "Auto Allocation: " AT 3 ttbl_jc-ctrl1.auto-all FORMAT "yes/no" " (auto-all)".
  IF action = "UPDATE" THEN
  DO:
    IF ttbl_jc-ctrl1.auto-all NE ttbl_jc-ctrl2.auto-all THEN
    PUT STREAM s-audit-rpt UNFORMATTED "*" AT 1.
    PUT STREAM s-audit-rpt UNFORMATTED "Auto Allocation: " AT 3 ttbl_jc-ctrl2.auto-all FORMAT "yes/no".
  END.
END.
IF NOT v_field_changed OR ttbl_jc-ctrl1.company NE ttbl_jc-ctrl2.company OR action NE "UPDATE" THEN
DO:
  PUT STREAM s-audit-rpt UNFORMATTED "Company: " AT 3 ttbl_jc-ctrl1.company " (company)".
  IF action = "UPDATE" THEN
  DO:
    IF ttbl_jc-ctrl1.company NE ttbl_jc-ctrl2.company THEN
    PUT STREAM s-audit-rpt UNFORMATTED "*" AT 1.
    PUT STREAM s-audit-rpt UNFORMATTED "Company: " AT 3 ttbl_jc-ctrl2.company.
  END.
END.
IF NOT v_field_changed OR ttbl_jc-ctrl1.gl-int NE ttbl_jc-ctrl2.gl-int OR action NE "UPDATE" THEN
DO:
  PUT STREAM s-audit-rpt UNFORMATTED "G/L Interface: " AT 3 ttbl_jc-ctrl1.gl-int FORMAT "yes/no" " (gl-int)".
  IF action = "UPDATE" THEN
  DO:
    IF ttbl_jc-ctrl1.gl-int NE ttbl_jc-ctrl2.gl-int THEN
    PUT STREAM s-audit-rpt UNFORMATTED "*" AT 1.
    PUT STREAM s-audit-rpt UNFORMATTED "G/L Interface: " AT 3 ttbl_jc-ctrl2.gl-int FORMAT "yes/no".
  END.
END.
IF NOT v_field_changed OR ttbl_jc-ctrl1.over-pct NE ttbl_jc-ctrl2.over-pct OR action NE "UPDATE" THEN
DO:
  PUT STREAM s-audit-rpt UNFORMATTED "Overrun %: " AT 3 ttbl_jc-ctrl1.over-pct FORMAT ">>9.99%" " (over-pct)".
  IF action = "UPDATE" THEN
  DO:
    IF ttbl_jc-ctrl1.over-pct NE ttbl_jc-ctrl2.over-pct THEN
    PUT STREAM s-audit-rpt UNFORMATTED "*" AT 1.
    PUT STREAM s-audit-rpt UNFORMATTED "Overrun %: " AT 3 ttbl_jc-ctrl2.over-pct FORMAT ">>9.99%".
  END.
END.
DO i = 1 TO 25:
IF NOT v_field_changed OR ttbl_jc-ctrl1.post[i] NE ttbl_jc-ctrl2.post[i] OR action NE "UPDATE" THEN
DO:
  PUT STREAM s-audit-rpt UNFORMATTED "Post?[" AT 3 i "]: " ttbl_jc-ctrl1.post[i] FORMAT "Y/N" " (post[" i "])".
  IF action = "UPDATE" THEN
  DO:
    IF ttbl_jc-ctrl1.post[i] NE ttbl_jc-ctrl2.post[i] THEN
    PUT STREAM s-audit-rpt UNFORMATTED "*" AT 1.
    PUT STREAM s-audit-rpt UNFORMATTED "Post?[" AT 3 i "]: " ttbl_jc-ctrl2.post[i] FORMAT "Y/N".
  END.
END.
END.
IF NOT v_field_changed OR ttbl_jc-ctrl1.std-cst-flg NE ttbl_jc-ctrl2.std-cst-flg OR action NE "UPDATE" THEN
DO:
  PUT STREAM s-audit-rpt UNFORMATTED "Std Cost Flag: " AT 3 ttbl_jc-ctrl1.std-cst-flg " (std-cst-flg)".
  IF action = "UPDATE" THEN
  DO:
    IF ttbl_jc-ctrl1.std-cst-flg NE ttbl_jc-ctrl2.std-cst-flg THEN
    PUT STREAM s-audit-rpt UNFORMATTED "*" AT 1.
    PUT STREAM s-audit-rpt UNFORMATTED "Std Cost Flag: " AT 3 ttbl_jc-ctrl2.std-cst-flg.
  END.
END.
IF v_show_index THEN
DO:
  PUT STREAM s-audit-rpt UNFORMATTED "Index Fields - Label: Value (database field-name)" AT 5.
  PUT STREAM s-audit-rpt UNFORMATTED "Company: " AT 7.
  IF action = "UPDATE" THEN
  PUT STREAM s-audit-rpt UNFORMATTED ttbl_jc-ctrl2.company " (company)".
  ELSE
  PUT STREAM s-audit-rpt UNFORMATTED ttbl_jc-ctrl1.company " (company)".
END.
PUT STREAM s-audit-rpt UNFORMATTED SKIP(1).
