/* address.p - generated 12/02/2005 - 16:51:16 by NoSweat */

/* this program was generated by auditgn1.p and should not be modified */
/* modifications should be made to the auto generation program         */

DEFINE INPUT PARAMETER action AS CHARACTER NO-UNDO.

DEFINE VARIABLE i AS INTEGER NO-UNDO.

{methods/defines/audit.i}
FIND FIRST ttbl_header.

DEFINE WORK-TABLE ttbl_address1 NO-UNDO LIKE address.
DEFINE WORK-TABLE ttbl_address2 NO-UNDO LIKE address.

CREATE ttbl_address1.
IMPORT STREAM s_audit_file ttbl_address1.
CREATE ttbl_address2.
IF action = "UPDATE" THEN
IMPORT STREAM s_audit_file ttbl_address2.

PUT STREAM s-audit-rpt UNFORMATTED
  "Action    User ID/(User Name)            DB.Table/(Database)  Date       Time" SKIP
  "--------- ------------------------------ -------------------- ---------- --------" SKIP
  ttbl_header.v_action AT 1
  ttbl_header.v_userid AT 11
  ttbl_header.v_file AT 42
  ttbl_header.v_date FORMAT "99/99/9999" AT 63
  STRING(ttbl_header.v_time,"HH:MM:SS") AT 74.
FIND users WHERE users.user_id = v_userid NO-LOCK NO-ERROR.
IF AVAILABLE users THEN
PUT STREAM s-audit-rpt UNFORMATTED "(" AT 10 users.user_name ")".
ELSE
PUT STREAM s-audit-rpt UNFORMATTED "(user record does not exist)" AT 10.
PUT STREAM s-audit-rpt UNFORMATTED
  "(" AT 41 ttbl_header.v_db ")" SKIP(1)
  "Label: Value (database field-name)" AT 3.
IF action = "UPDATE" THEN
PUT STREAM s-audit-rpt UNFORMATTED " ( * - denotes field value change )".
PUT STREAM s-audit-rpt UNFORMATTED FILL("-",75) AT 3.
IF NOT v_field_changed OR ttbl_address1.address1 NE ttbl_address2.address1 OR action NE "UPDATE" THEN
DO:
  PUT STREAM s-audit-rpt UNFORMATTED "Address Line 1: " AT 3 ttbl_address1.address1 " (address1)".
  IF action = "UPDATE" THEN
  DO:
    IF ttbl_address1.address1 NE ttbl_address2.address1 THEN
    PUT STREAM s-audit-rpt UNFORMATTED "*" AT 1.
    PUT STREAM s-audit-rpt UNFORMATTED "Address Line 1: " AT 3 ttbl_address2.address1.
  END.
END.
IF NOT v_field_changed OR ttbl_address1.address2 NE ttbl_address2.address2 OR action NE "UPDATE" THEN
DO:
  PUT STREAM s-audit-rpt UNFORMATTED "Address Line 2: " AT 3 ttbl_address1.address2 " (address2)".
  IF action = "UPDATE" THEN
  DO:
    IF ttbl_address1.address2 NE ttbl_address2.address2 THEN
    PUT STREAM s-audit-rpt UNFORMATTED "*" AT 1.
    PUT STREAM s-audit-rpt UNFORMATTED "Address Line 2: " AT 3 ttbl_address2.address2.
  END.
END.
IF NOT v_field_changed OR ttbl_address1.address_type NE ttbl_address2.address_type OR action NE "UPDATE" THEN
DO:
  PUT STREAM s-audit-rpt UNFORMATTED "Address Type: " AT 3 ttbl_address1.address_type " (address_type)".
  IF action = "UPDATE" THEN
  DO:
    IF ttbl_address1.address_type NE ttbl_address2.address_type THEN
    PUT STREAM s-audit-rpt UNFORMATTED "*" AT 1.
    PUT STREAM s-audit-rpt UNFORMATTED "Address Type: " AT 3 ttbl_address2.address_type.
  END.
END.
IF NOT v_field_changed OR ttbl_address1.name1 NE ttbl_address2.name1 OR action NE "UPDATE" THEN
DO:
  PUT STREAM s-audit-rpt UNFORMATTED "Name 1: " AT 3 ttbl_address1.name1 " (name1)".
  IF action = "UPDATE" THEN
  DO:
    IF ttbl_address1.name1 NE ttbl_address2.name1 THEN
    PUT STREAM s-audit-rpt UNFORMATTED "*" AT 1.
    PUT STREAM s-audit-rpt UNFORMATTED "Name 1: " AT 3 ttbl_address2.name1.
  END.
END.
IF NOT v_field_changed OR ttbl_address1.name2 NE ttbl_address2.name2 OR action NE "UPDATE" THEN
DO:
  PUT STREAM s-audit-rpt UNFORMATTED "Name 2: " AT 3 ttbl_address1.name2 " (name2)".
  IF action = "UPDATE" THEN
  DO:
    IF ttbl_address1.name2 NE ttbl_address2.name2 THEN
    PUT STREAM s-audit-rpt UNFORMATTED "*" AT 1.
    PUT STREAM s-audit-rpt UNFORMATTED "Name 2: " AT 3 ttbl_address2.name2.
  END.
END.
IF NOT v_field_changed OR ttbl_address1.pref# NE ttbl_address2.pref# OR action NE "UPDATE" THEN
DO:
  PUT STREAM s-audit-rpt UNFORMATTED "Preference #: " AT 3 ttbl_address1.pref# FORMAT "99" " (pref#)".
  IF action = "UPDATE" THEN
  DO:
    IF ttbl_address1.pref# NE ttbl_address2.pref# THEN
    PUT STREAM s-audit-rpt UNFORMATTED "*" AT 1.
    PUT STREAM s-audit-rpt UNFORMATTED "Preference #: " AT 3 ttbl_address2.pref# FORMAT "99".
  END.
END.
IF NOT v_field_changed OR ttbl_address1.pref_type NE ttbl_address2.pref_type OR action NE "UPDATE" THEN
DO:
  PUT STREAM s-audit-rpt UNFORMATTED "Preference Type: " AT 3 ttbl_address1.pref_type " (pref_type)".
  IF action = "UPDATE" THEN
  DO:
    IF ttbl_address1.pref_type NE ttbl_address2.pref_type THEN
    PUT STREAM s-audit-rpt UNFORMATTED "*" AT 1.
    PUT STREAM s-audit-rpt UNFORMATTED "Preference Type: " AT 3 ttbl_address2.pref_type.
  END.
END.
IF NOT v_field_changed OR ttbl_address1.table_rec_key NE ttbl_address2.table_rec_key OR action NE "UPDATE" THEN
DO:
  PUT STREAM s-audit-rpt UNFORMATTED "Table Rec Key: " AT 3 ttbl_address1.table_rec_key " (table_rec_key)".
  IF action = "UPDATE" THEN
  DO:
    IF ttbl_address1.table_rec_key NE ttbl_address2.table_rec_key THEN
    PUT STREAM s-audit-rpt UNFORMATTED "*" AT 1.
    PUT STREAM s-audit-rpt UNFORMATTED "Table Rec Key: " AT 3 ttbl_address2.table_rec_key.
  END.
END.
IF NOT v_field_changed OR ttbl_address1.zipcode NE ttbl_address2.zipcode OR action NE "UPDATE" THEN
DO:
  PUT STREAM s-audit-rpt UNFORMATTED "Zip/Postal Code: " AT 3 ttbl_address1.zipcode " (zipcode)".
  IF action = "UPDATE" THEN
  DO:
    IF ttbl_address1.zipcode NE ttbl_address2.zipcode THEN
    PUT STREAM s-audit-rpt UNFORMATTED "*" AT 1.
    PUT STREAM s-audit-rpt UNFORMATTED "Zip/Postal Code: " AT 3 ttbl_address2.zipcode.
  END.
END.
IF NOT v_field_changed OR ttbl_address1.zipcode_4 NE ttbl_address2.zipcode_4 OR action NE "UPDATE" THEN
DO:
  PUT STREAM s-audit-rpt UNFORMATTED "Plus 4: " AT 3 ttbl_address1.zipcode_4 FORMAT "9999" " (zipcode_4)".
  IF action = "UPDATE" THEN
  DO:
    IF ttbl_address1.zipcode_4 NE ttbl_address2.zipcode_4 THEN
    PUT STREAM s-audit-rpt UNFORMATTED "*" AT 1.
    PUT STREAM s-audit-rpt UNFORMATTED "Plus 4: " AT 3 ttbl_address2.zipcode_4 FORMAT "9999".
  END.
END.
IF v_show_index THEN
DO:
  PUT STREAM s-audit-rpt UNFORMATTED "Index Fields - Label: Value (database field-name)" AT 5.
  PUT STREAM s-audit-rpt UNFORMATTED "Table Rec Key: " AT 7.
  IF action = "UPDATE" THEN
  PUT STREAM s-audit-rpt UNFORMATTED ttbl_address2.table_rec_key " (table_rec_key)".
  ELSE
  PUT STREAM s-audit-rpt UNFORMATTED ttbl_address1.table_rec_key " (table_rec_key)".
  PUT STREAM s-audit-rpt UNFORMATTED "Zip/Postal Code: " AT 7.
  IF action = "UPDATE" THEN
  PUT STREAM s-audit-rpt UNFORMATTED ttbl_address2.zipcode " (zipcode)".
  ELSE
  PUT STREAM s-audit-rpt UNFORMATTED ttbl_address1.zipcode " (zipcode)".
  PUT STREAM s-audit-rpt UNFORMATTED "Address Line 1: " AT 7.
  IF action = "UPDATE" THEN
  PUT STREAM s-audit-rpt UNFORMATTED ttbl_address2.address1 " (address1)".
  ELSE
  PUT STREAM s-audit-rpt UNFORMATTED ttbl_address1.address1 " (address1)".
  PUT STREAM s-audit-rpt UNFORMATTED "Address Line 2: " AT 7.
  IF action = "UPDATE" THEN
  PUT STREAM s-audit-rpt UNFORMATTED ttbl_address2.address2 " (address2)".
  ELSE
  PUT STREAM s-audit-rpt UNFORMATTED ttbl_address1.address2 " (address2)".
END.
PUT STREAM s-audit-rpt UNFORMATTED SKIP(1).
