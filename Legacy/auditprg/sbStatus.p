/* sbStatus.p - generated 05/23/2017 - 12:07:22 by NoSweat */

/* this program was generated by auditgn1.p and should not be modified */
/* modifications should be made to the auto generation program         */

DEFINE INPUT PARAMETER action AS CHARACTER NO-UNDO.

DEFINE VARIABLE i AS INTEGER NO-UNDO.

{methods/defines/audit.i}
FIND FIRST ttbl_header.

DEFINE WORK-TABLE ttbl_sbStatus1 NO-UNDO LIKE sbStatus.
DEFINE WORK-TABLE ttbl_sbStatus2 NO-UNDO LIKE sbStatus.

CREATE ttbl_sbStatus1.
IMPORT STREAM s_audit_file ttbl_sbStatus1.
CREATE ttbl_sbStatus2.
IF action = "UPDATE" THEN
IMPORT STREAM s_audit_file ttbl_sbStatus2.

PUT STREAM s-audit-rpt UNFORMATTED
  "Action    User ID/(User Name)            DB.Table/(Database)  Date       Time" SKIP
  "--------- ------------------------------ -------------------- ---------- --------" SKIP
  ttbl_header.v_action AT 1
  ttbl_header.v_userid AT 11
  ttbl_header.v_file AT 42
  ttbl_header.v_date FORMAT "99/99/9999" AT 63
  STRING(ttbl_header.v_time,"HH:MM:SS") AT 74.
FIND users WHERE users.user_id = v_userid NO-LOCK NO-ERROR.
IF AVAILABLE users THEN
PUT STREAM s-audit-rpt UNFORMATTED "(" AT 10 users.user_name ")".
ELSE
PUT STREAM s-audit-rpt UNFORMATTED "(user record does not exist)" AT 10.
PUT STREAM s-audit-rpt UNFORMATTED
  "(" AT 41 ttbl_header.v_db ")" SKIP(1)
  "Label: Value (database field-name)" AT 3.
IF action = "UPDATE" THEN
PUT STREAM s-audit-rpt UNFORMATTED " ( * - denotes field value change )".
PUT STREAM s-audit-rpt UNFORMATTED FILL("-",75) AT 3.
IF NOT v_field_changed OR ttbl_sbStatus1.company NE ttbl_sbStatus2.company OR action NE "UPDATE" THEN
DO:
  PUT STREAM s-audit-rpt UNFORMATTED "Company: " AT 3 ttbl_sbStatus1.company " (company)".
  IF action = "UPDATE" THEN
  DO:
    IF ttbl_sbStatus1.company NE ttbl_sbStatus2.company THEN
    PUT STREAM s-audit-rpt UNFORMATTED "*" AT 1.
    PUT STREAM s-audit-rpt UNFORMATTED "Company: " AT 3 ttbl_sbStatus2.company.
  END.
END.
IF NOT v_field_changed OR ttbl_sbStatus1.frm NE ttbl_sbStatus2.frm OR action NE "UPDATE" THEN
DO:
  PUT STREAM s-audit-rpt UNFORMATTED "Form: " AT 3 ttbl_sbStatus1.frm FORMAT ">>9" " (frm)".
  IF action = "UPDATE" THEN
  DO:
    IF ttbl_sbStatus1.frm NE ttbl_sbStatus2.frm THEN
    PUT STREAM s-audit-rpt UNFORMATTED "*" AT 1.
    PUT STREAM s-audit-rpt UNFORMATTED "Form: " AT 3 ttbl_sbStatus2.frm FORMAT ">>9".
  END.
END.
IF NOT v_field_changed OR ttbl_sbStatus1.job-no NE ttbl_sbStatus2.job-no OR action NE "UPDATE" THEN
DO:
  PUT STREAM s-audit-rpt UNFORMATTED "Job Number: " AT 3 ttbl_sbStatus1.job-no " (job-no)".
  IF action = "UPDATE" THEN
  DO:
    IF ttbl_sbStatus1.job-no NE ttbl_sbStatus2.job-no THEN
    PUT STREAM s-audit-rpt UNFORMATTED "*" AT 1.
    PUT STREAM s-audit-rpt UNFORMATTED "Job Number: " AT 3 ttbl_sbStatus2.job-no.
  END.
END.
IF NOT v_field_changed OR ttbl_sbStatus1.job-no2 NE ttbl_sbStatus2.job-no2 OR action NE "UPDATE" THEN
DO:
  PUT STREAM s-audit-rpt UNFORMATTED "Run: " AT 3 ttbl_sbStatus1.job-no2 FORMAT ">9" " (job-no2)".
  IF action = "UPDATE" THEN
  DO:
    IF ttbl_sbStatus1.job-no2 NE ttbl_sbStatus2.job-no2 THEN
    PUT STREAM s-audit-rpt UNFORMATTED "*" AT 1.
    PUT STREAM s-audit-rpt UNFORMATTED "Run: " AT 3 ttbl_sbStatus2.job-no2 FORMAT ">9".
  END.
END.
IF NOT v_field_changed OR ttbl_sbStatus1.m-code NE ttbl_sbStatus2.m-code OR action NE "UPDATE" THEN
DO:
  PUT STREAM s-audit-rpt UNFORMATTED "Machine: " AT 3 ttbl_sbStatus1.m-code " (m-code)".
  IF action = "UPDATE" THEN
  DO:
    IF ttbl_sbStatus1.m-code NE ttbl_sbStatus2.m-code THEN
    PUT STREAM s-audit-rpt UNFORMATTED "*" AT 1.
    PUT STREAM s-audit-rpt UNFORMATTED "Machine: " AT 3 ttbl_sbStatus2.m-code.
  END.
END.
DO i = 1 TO 14:
IF NOT v_field_changed OR ttbl_sbStatus1.sbStatus[i] NE ttbl_sbStatus2.sbStatus[i] OR action NE "UPDATE" THEN
DO:
  PUT STREAM s-audit-rpt UNFORMATTED "SB Status[" AT 3 i "]: " ttbl_sbStatus1.sbStatus[i] FORMAT "yes/no" " (sbStatus[" i "])".
  IF action = "UPDATE" THEN
  DO:
    IF ttbl_sbStatus1.sbStatus[i] NE ttbl_sbStatus2.sbStatus[i] THEN
    PUT STREAM s-audit-rpt UNFORMATTED "*" AT 1.
    PUT STREAM s-audit-rpt UNFORMATTED "SB Status[" AT 3 i "]: " ttbl_sbStatus2.sbStatus[i] FORMAT "yes/no".
  END.
END.
END.
IF v_show_index THEN
DO:
  PUT STREAM s-audit-rpt UNFORMATTED "Index Fields - Label: Value (database field-name)" AT 5.
  PUT STREAM s-audit-rpt UNFORMATTED "Company: " AT 7.
  IF action = "UPDATE" THEN
  PUT STREAM s-audit-rpt UNFORMATTED ttbl_sbStatus2.company " (company)".
  ELSE
  PUT STREAM s-audit-rpt UNFORMATTED ttbl_sbStatus1.company " (company)".
  PUT STREAM s-audit-rpt UNFORMATTED "Machine: " AT 7.
  IF action = "UPDATE" THEN
  PUT STREAM s-audit-rpt UNFORMATTED ttbl_sbStatus2.m-code " (m-code)".
  ELSE
  PUT STREAM s-audit-rpt UNFORMATTED ttbl_sbStatus1.m-code " (m-code)".
  PUT STREAM s-audit-rpt UNFORMATTED "Job Number: " AT 7.
  IF action = "UPDATE" THEN
  PUT STREAM s-audit-rpt UNFORMATTED ttbl_sbStatus2.job-no " (job-no)".
  ELSE
  PUT STREAM s-audit-rpt UNFORMATTED ttbl_sbStatus1.job-no " (job-no)".
  PUT STREAM s-audit-rpt UNFORMATTED "Run: " AT 7.
  IF action = "UPDATE" THEN
  PUT STREAM s-audit-rpt UNFORMATTED ttbl_sbStatus2.job-no2 FORMAT ">9" " (job-no2)".
  ELSE
  PUT STREAM s-audit-rpt UNFORMATTED ttbl_sbStatus1.job-no2 FORMAT ">9" " (job-no2)".
  PUT STREAM s-audit-rpt UNFORMATTED "Form: " AT 7.
  IF action = "UPDATE" THEN
  PUT STREAM s-audit-rpt UNFORMATTED ttbl_sbStatus2.frm FORMAT ">>9" " (frm)".
  ELSE
  PUT STREAM s-audit-rpt UNFORMATTED ttbl_sbStatus1.frm FORMAT ">>9" " (frm)".
END.
PUT STREAM s-audit-rpt UNFORMATTED SKIP(1).
