/* terms.p - generated 12/09/2005 - 15:42:55 by NoSweat */

/* this program was generated by auditgn1.p and should not be modified */
/* modifications should be made to the auto generation program         */

DEFINE INPUT PARAMETER action AS CHARACTER NO-UNDO.

DEFINE VARIABLE i AS INTEGER NO-UNDO.

{methods/defines/audit.i}
FIND FIRST ttbl_header.

DEFINE WORK-TABLE ttbl_terms1 NO-UNDO LIKE terms.
DEFINE WORK-TABLE ttbl_terms2 NO-UNDO LIKE terms.

CREATE ttbl_terms1.
IMPORT STREAM s_audit_file ttbl_terms1.
CREATE ttbl_terms2.
IF action = "UPDATE" THEN
IMPORT STREAM s_audit_file ttbl_terms2.

PUT STREAM s-audit-rpt UNFORMATTED
  "Action    User ID/(User Name)            DB.Table/(Database)  Date       Time" SKIP
  "--------- ------------------------------ -------------------- ---------- --------" SKIP
  ttbl_header.v_action AT 1
  ttbl_header.v_userid AT 11
  ttbl_header.v_file AT 42
  ttbl_header.v_date FORMAT "99/99/9999" AT 63
  STRING(ttbl_header.v_time,"HH:MM:SS") AT 74.
FIND users WHERE users.user_id = v_userid NO-LOCK NO-ERROR.
IF AVAILABLE users THEN
PUT STREAM s-audit-rpt UNFORMATTED "(" AT 10 users.user_name ")".
ELSE
PUT STREAM s-audit-rpt UNFORMATTED "(user record does not exist)" AT 10.
PUT STREAM s-audit-rpt UNFORMATTED
  "(" AT 41 ttbl_header.v_db ")" SKIP(1)
  "Label: Value (database field-name)" AT 3.
IF action = "UPDATE" THEN
PUT STREAM s-audit-rpt UNFORMATTED " ( * - denotes field value change )".
PUT STREAM s-audit-rpt UNFORMATTED FILL("-",75) AT 3.
IF NOT v_field_changed OR ttbl_terms1.company NE ttbl_terms2.company OR action NE "UPDATE" THEN
DO:
  PUT STREAM s-audit-rpt UNFORMATTED "Company: " AT 3 ttbl_terms1.company " (company)".
  IF action = "UPDATE" THEN
  DO:
    IF ttbl_terms1.company NE ttbl_terms2.company THEN
    PUT STREAM s-audit-rpt UNFORMATTED "*" AT 1.
    PUT STREAM s-audit-rpt UNFORMATTED "Company: " AT 3 ttbl_terms2.company.
  END.
END.
IF NOT v_field_changed OR ttbl_terms1.cut-date NE ttbl_terms2.cut-date OR action NE "UPDATE" THEN
DO:
  PUT STREAM s-audit-rpt UNFORMATTED "Cutoff: " AT 3 ttbl_terms1.cut-date FORMAT "999" " (cut-date)".
  IF action = "UPDATE" THEN
  DO:
    IF ttbl_terms1.cut-date NE ttbl_terms2.cut-date THEN
    PUT STREAM s-audit-rpt UNFORMATTED "*" AT 1.
    PUT STREAM s-audit-rpt UNFORMATTED "Cutoff: " AT 3 ttbl_terms2.cut-date FORMAT "999".
  END.
END.
IF NOT v_field_changed OR ttbl_terms1.disc-days NE ttbl_terms2.disc-days OR action NE "UPDATE" THEN
DO:
  PUT STREAM s-audit-rpt UNFORMATTED "Days: " AT 3 ttbl_terms1.disc-days FORMAT "z9" " (disc-days)".
  IF action = "UPDATE" THEN
  DO:
    IF ttbl_terms1.disc-days NE ttbl_terms2.disc-days THEN
    PUT STREAM s-audit-rpt UNFORMATTED "*" AT 1.
    PUT STREAM s-audit-rpt UNFORMATTED "Days: " AT 3 ttbl_terms2.disc-days FORMAT "z9".
  END.
END.
IF NOT v_field_changed OR ttbl_terms1.disc-rate NE ttbl_terms2.disc-rate OR action NE "UPDATE" THEN
DO:
  PUT STREAM s-audit-rpt UNFORMATTED "Disc %: " AT 3 ttbl_terms1.disc-rate FORMAT "z9.9" " (disc-rate)".
  IF action = "UPDATE" THEN
  DO:
    IF ttbl_terms1.disc-rate NE ttbl_terms2.disc-rate THEN
    PUT STREAM s-audit-rpt UNFORMATTED "*" AT 1.
    PUT STREAM s-audit-rpt UNFORMATTED "Disc %: " AT 3 ttbl_terms2.disc-rate FORMAT "z9.9".
  END.
END.
IF NOT v_field_changed OR ttbl_terms1.dscr NE ttbl_terms2.dscr OR action NE "UPDATE" THEN
DO:
  PUT STREAM s-audit-rpt UNFORMATTED "Description: " AT 3 ttbl_terms1.dscr " (dscr)".
  IF action = "UPDATE" THEN
  DO:
    IF ttbl_terms1.dscr NE ttbl_terms2.dscr THEN
    PUT STREAM s-audit-rpt UNFORMATTED "*" AT 1.
    PUT STREAM s-audit-rpt UNFORMATTED "Description: " AT 3 ttbl_terms2.dscr.
  END.
END.
IF NOT v_field_changed OR ttbl_terms1.net-days NE ttbl_terms2.net-days OR action NE "UPDATE" THEN
DO:
  PUT STREAM s-audit-rpt UNFORMATTED "Net: " AT 3 ttbl_terms1.net-days FORMAT "z9" " (net-days)".
  IF action = "UPDATE" THEN
  DO:
    IF ttbl_terms1.net-days NE ttbl_terms2.net-days THEN
    PUT STREAM s-audit-rpt UNFORMATTED "*" AT 1.
    PUT STREAM s-audit-rpt UNFORMATTED "Net: " AT 3 ttbl_terms2.net-days FORMAT "z9".
  END.
END.
IF NOT v_field_changed OR ttbl_terms1.t-code NE ttbl_terms2.t-code OR action NE "UPDATE" THEN
DO:
  PUT STREAM s-audit-rpt UNFORMATTED "Terms: " AT 3 ttbl_terms1.t-code " (t-code)".
  IF action = "UPDATE" THEN
  DO:
    IF ttbl_terms1.t-code NE ttbl_terms2.t-code THEN
    PUT STREAM s-audit-rpt UNFORMATTED "*" AT 1.
    PUT STREAM s-audit-rpt UNFORMATTED "Terms: " AT 3 ttbl_terms2.t-code.
  END.
END.
IF NOT v_field_changed OR ttbl_terms1.type NE ttbl_terms2.type OR action NE "UPDATE" THEN
DO:
  PUT STREAM s-audit-rpt UNFORMATTED "Type: " AT 3 ttbl_terms1.type " (type)".
  IF action = "UPDATE" THEN
  DO:
    IF ttbl_terms1.type NE ttbl_terms2.type THEN
    PUT STREAM s-audit-rpt UNFORMATTED "*" AT 1.
    PUT STREAM s-audit-rpt UNFORMATTED "Type: " AT 3 ttbl_terms2.type.
  END.
END.
IF v_show_index THEN
DO:
  PUT STREAM s-audit-rpt UNFORMATTED "Index Fields - Label: Value (database field-name)" AT 5.
  PUT STREAM s-audit-rpt UNFORMATTED "Company: " AT 7.
  IF action = "UPDATE" THEN
  PUT STREAM s-audit-rpt UNFORMATTED ttbl_terms2.company " (company)".
  ELSE
  PUT STREAM s-audit-rpt UNFORMATTED ttbl_terms1.company " (company)".
  PUT STREAM s-audit-rpt UNFORMATTED "Terms: " AT 7.
  IF action = "UPDATE" THEN
  PUT STREAM s-audit-rpt UNFORMATTED ttbl_terms2.t-code " (t-code)".
  ELSE
  PUT STREAM s-audit-rpt UNFORMATTED ttbl_terms1.t-code " (t-code)".
END.
PUT STREAM s-audit-rpt UNFORMATTED SKIP(1).
