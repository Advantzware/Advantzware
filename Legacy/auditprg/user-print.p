/* user-print.p - generated 12/09/2005 - 15:42:55 by NoSweat */

/* this program was generated by auditgn1.p and should not be modified */
/* modifications should be made to the auto generation program         */

DEFINE INPUT PARAMETER action AS CHARACTER NO-UNDO.

DEFINE VARIABLE i AS INTEGER NO-UNDO.

{methods/defines/audit.i}
FIND FIRST ttbl_header.

DEFINE WORK-TABLE ttbl_user-print1 NO-UNDO LIKE user-print.
DEFINE WORK-TABLE ttbl_user-print2 NO-UNDO LIKE user-print.

CREATE ttbl_user-print1.
IMPORT STREAM s_audit_file ttbl_user-print1.
CREATE ttbl_user-print2.
IF action = "UPDATE" THEN
IMPORT STREAM s_audit_file ttbl_user-print2.

PUT STREAM s-audit-rpt UNFORMATTED
  "Action    User ID/(User Name)            DB.Table/(Database)  Date       Time" SKIP
  "--------- ------------------------------ -------------------- ---------- --------" SKIP
  ttbl_header.v_action AT 1
  ttbl_header.v_userid AT 11
  ttbl_header.v_file AT 42
  ttbl_header.v_date FORMAT "99/99/9999" AT 63
  STRING(ttbl_header.v_time,"HH:MM:SS") AT 74.
FIND users WHERE users.user_id = v_userid NO-LOCK NO-ERROR.
IF AVAILABLE users THEN
PUT STREAM s-audit-rpt UNFORMATTED "(" AT 10 users.user_name ")".
ELSE
PUT STREAM s-audit-rpt UNFORMATTED "(user record does not exist)" AT 10.
PUT STREAM s-audit-rpt UNFORMATTED
  "(" AT 41 ttbl_header.v_db ")" SKIP(1)
  "Label: Value (database field-name)" AT 3.
IF action = "UPDATE" THEN
PUT STREAM s-audit-rpt UNFORMATTED " ( * - denotes field value change )".
PUT STREAM s-audit-rpt UNFORMATTED FILL("-",75) AT 3.
IF NOT v_field_changed OR ttbl_user-print1.batch NE ttbl_user-print2.batch OR action NE "UPDATE" THEN
DO:
  PUT STREAM s-audit-rpt UNFORMATTED "Batch: " AT 3 ttbl_user-print1.batch " (batch)".
  IF action = "UPDATE" THEN
  DO:
    IF ttbl_user-print1.batch NE ttbl_user-print2.batch THEN
    PUT STREAM s-audit-rpt UNFORMATTED "*" AT 1.
    PUT STREAM s-audit-rpt UNFORMATTED "Batch: " AT 3 ttbl_user-print2.batch.
  END.
END.
IF NOT v_field_changed OR ttbl_user-print1.batch-seq NE ttbl_user-print2.batch-seq OR action NE "UPDATE" THEN
DO:
  PUT STREAM s-audit-rpt UNFORMATTED "?: " AT 3 ttbl_user-print1.batch-seq FORMAT "->,>>>,>>9" " (batch-seq)".
  IF action = "UPDATE" THEN
  DO:
    IF ttbl_user-print1.batch-seq NE ttbl_user-print2.batch-seq THEN
    PUT STREAM s-audit-rpt UNFORMATTED "*" AT 1.
    PUT STREAM s-audit-rpt UNFORMATTED "?: " AT 3 ttbl_user-print2.batch-seq FORMAT "->,>>>,>>9".
  END.
END.
IF NOT v_field_changed OR ttbl_user-print1.company NE ttbl_user-print2.company OR action NE "UPDATE" THEN
DO:
  PUT STREAM s-audit-rpt UNFORMATTED "Company: " AT 3 ttbl_user-print1.company " (company)".
  IF action = "UPDATE" THEN
  DO:
    IF ttbl_user-print1.company NE ttbl_user-print2.company THEN
    PUT STREAM s-audit-rpt UNFORMATTED "*" AT 1.
    PUT STREAM s-audit-rpt UNFORMATTED "Company: " AT 3 ttbl_user-print2.company.
  END.
END.
DO i = 1 TO 100:
IF NOT v_field_changed OR ttbl_user-print1.field-label[i] NE ttbl_user-print2.field-label[i] OR action NE "UPDATE" THEN
DO:
  PUT STREAM s-audit-rpt UNFORMATTED "?[" AT 3 i "]: " ttbl_user-print1.field-label[i] " (field-label[" i "])".
  IF action = "UPDATE" THEN
  DO:
    IF ttbl_user-print1.field-label[i] NE ttbl_user-print2.field-label[i] THEN
    PUT STREAM s-audit-rpt UNFORMATTED "*" AT 1.
    PUT STREAM s-audit-rpt UNFORMATTED "?[" AT 3 i "]: " ttbl_user-print2.field-label[i].
  END.
END.
END.
DO i = 1 TO 100:
IF NOT v_field_changed OR ttbl_user-print1.field-name[i] NE ttbl_user-print2.field-name[i] OR action NE "UPDATE" THEN
DO:
  PUT STREAM s-audit-rpt UNFORMATTED "?[" AT 3 i "]: " ttbl_user-print1.field-name[i] " (field-name[" i "])".
  IF action = "UPDATE" THEN
  DO:
    IF ttbl_user-print1.field-name[i] NE ttbl_user-print2.field-name[i] THEN
    PUT STREAM s-audit-rpt UNFORMATTED "*" AT 1.
    PUT STREAM s-audit-rpt UNFORMATTED "?[" AT 3 i "]: " ttbl_user-print2.field-name[i].
  END.
END.
END.
DO i = 1 TO 100:
IF NOT v_field_changed OR ttbl_user-print1.field-value[i] NE ttbl_user-print2.field-value[i] OR action NE "UPDATE" THEN
DO:
  PUT STREAM s-audit-rpt UNFORMATTED "?[" AT 3 i "]: " ttbl_user-print1.field-value[i] " (field-value[" i "])".
  IF action = "UPDATE" THEN
  DO:
    IF ttbl_user-print1.field-value[i] NE ttbl_user-print2.field-value[i] THEN
    PUT STREAM s-audit-rpt UNFORMATTED "*" AT 1.
    PUT STREAM s-audit-rpt UNFORMATTED "?[" AT 3 i "]: " ttbl_user-print2.field-value[i].
  END.
END.
END.
IF NOT v_field_changed OR ttbl_user-print1.frequency NE ttbl_user-print2.frequency OR action NE "UPDATE" THEN
DO:
  PUT STREAM s-audit-rpt UNFORMATTED "How Often?: " AT 3 ttbl_user-print1.frequency " (frequency)".
  IF action = "UPDATE" THEN
  DO:
    IF ttbl_user-print1.frequency NE ttbl_user-print2.frequency THEN
    PUT STREAM s-audit-rpt UNFORMATTED "*" AT 1.
    PUT STREAM s-audit-rpt UNFORMATTED "How Often?: " AT 3 ttbl_user-print2.frequency.
  END.
END.
IF NOT v_field_changed OR ttbl_user-print1.last-date NE ttbl_user-print2.last-date OR action NE "UPDATE" THEN
DO:
  PUT STREAM s-audit-rpt UNFORMATTED "Last Date: " AT 3 ttbl_user-print1.last-date FORMAT "99/99/9999" " (last-date)".
  IF action = "UPDATE" THEN
  DO:
    IF ttbl_user-print1.last-date NE ttbl_user-print2.last-date THEN
    PUT STREAM s-audit-rpt UNFORMATTED "*" AT 1.
    PUT STREAM s-audit-rpt UNFORMATTED "Last Date: " AT 3 ttbl_user-print2.last-date FORMAT "99/99/9999".
  END.
END.
IF NOT v_field_changed OR ttbl_user-print1.last-time NE ttbl_user-print2.last-time OR action NE "UPDATE" THEN
DO:
  PUT STREAM s-audit-rpt UNFORMATTED "LastTime: " AT 3 ttbl_user-print1.last-time FORMAT "->,>>>,>>9" " (last-time)".
  IF action = "UPDATE" THEN
  DO:
    IF ttbl_user-print1.last-time NE ttbl_user-print2.last-time THEN
    PUT STREAM s-audit-rpt UNFORMATTED "*" AT 1.
    PUT STREAM s-audit-rpt UNFORMATTED "LastTime: " AT 3 ttbl_user-print2.last-time FORMAT "->,>>>,>>9".
  END.
END.
IF NOT v_field_changed OR ttbl_user-print1.next-date NE ttbl_user-print2.next-date OR action NE "UPDATE" THEN
DO:
  PUT STREAM s-audit-rpt UNFORMATTED "Next Date: " AT 3 ttbl_user-print1.next-date FORMAT "99/99/9999" " (next-date)".
  IF action = "UPDATE" THEN
  DO:
    IF ttbl_user-print1.next-date NE ttbl_user-print2.next-date THEN
    PUT STREAM s-audit-rpt UNFORMATTED "*" AT 1.
    PUT STREAM s-audit-rpt UNFORMATTED "Next Date: " AT 3 ttbl_user-print2.next-date FORMAT "99/99/9999".
  END.
END.
IF NOT v_field_changed OR ttbl_user-print1.next-time NE ttbl_user-print2.next-time OR action NE "UPDATE" THEN
DO:
  PUT STREAM s-audit-rpt UNFORMATTED "Next Time: " AT 3 ttbl_user-print1.next-time FORMAT "->,>>>,>>9" " (next-time)".
  IF action = "UPDATE" THEN
  DO:
    IF ttbl_user-print1.next-time NE ttbl_user-print2.next-time THEN
    PUT STREAM s-audit-rpt UNFORMATTED "*" AT 1.
    PUT STREAM s-audit-rpt UNFORMATTED "Next Time: " AT 3 ttbl_user-print2.next-time FORMAT "->,>>>,>>9".
  END.
END.
IF NOT v_field_changed OR ttbl_user-print1.printer-name NE ttbl_user-print2.printer-name OR action NE "UPDATE" THEN
DO:
  PUT STREAM s-audit-rpt UNFORMATTED "?: " AT 3 ttbl_user-print1.printer-name " (printer-name)".
  IF action = "UPDATE" THEN
  DO:
    IF ttbl_user-print1.printer-name NE ttbl_user-print2.printer-name THEN
    PUT STREAM s-audit-rpt UNFORMATTED "*" AT 1.
    PUT STREAM s-audit-rpt UNFORMATTED "?: " AT 3 ttbl_user-print2.printer-name.
  END.
END.
IF NOT v_field_changed OR ttbl_user-print1.prog-seq NE ttbl_user-print2.prog-seq OR action NE "UPDATE" THEN
DO:
  PUT STREAM s-audit-rpt UNFORMATTED "?: " AT 3 ttbl_user-print1.prog-seq FORMAT "->,>>>,>>9" " (prog-seq)".
  IF action = "UPDATE" THEN
  DO:
    IF ttbl_user-print1.prog-seq NE ttbl_user-print2.prog-seq THEN
    PUT STREAM s-audit-rpt UNFORMATTED "*" AT 1.
    PUT STREAM s-audit-rpt UNFORMATTED "?: " AT 3 ttbl_user-print2.prog-seq FORMAT "->,>>>,>>9".
  END.
END.
IF NOT v_field_changed OR ttbl_user-print1.prog-title NE ttbl_user-print2.prog-title OR action NE "UPDATE" THEN
DO:
  PUT STREAM s-audit-rpt UNFORMATTED "Title: " AT 3 ttbl_user-print1.prog-title " (prog-title)".
  IF action = "UPDATE" THEN
  DO:
    IF ttbl_user-print1.prog-title NE ttbl_user-print2.prog-title THEN
    PUT STREAM s-audit-rpt UNFORMATTED "*" AT 1.
    PUT STREAM s-audit-rpt UNFORMATTED "Title: " AT 3 ttbl_user-print2.prog-title.
  END.
END.
IF NOT v_field_changed OR ttbl_user-print1.program-id NE ttbl_user-print2.program-id OR action NE "UPDATE" THEN
DO:
  PUT STREAM s-audit-rpt UNFORMATTED "?: " AT 3 ttbl_user-print1.program-id " (program-id)".
  IF action = "UPDATE" THEN
  DO:
    IF ttbl_user-print1.program-id NE ttbl_user-print2.program-id THEN
    PUT STREAM s-audit-rpt UNFORMATTED "*" AT 1.
    PUT STREAM s-audit-rpt UNFORMATTED "?: " AT 3 ttbl_user-print2.program-id.
  END.
END.
IF NOT v_field_changed OR ttbl_user-print1.user-id NE ttbl_user-print2.user-id OR action NE "UPDATE" THEN
DO:
  PUT STREAM s-audit-rpt UNFORMATTED "User-Id: " AT 3 ttbl_user-print1.user-id " (user-id)".
  IF action = "UPDATE" THEN
  DO:
    IF ttbl_user-print1.user-id NE ttbl_user-print2.user-id THEN
    PUT STREAM s-audit-rpt UNFORMATTED "*" AT 1.
    PUT STREAM s-audit-rpt UNFORMATTED "User-Id: " AT 3 ttbl_user-print2.user-id.
  END.
END.
IF NOT v_field_changed OR ttbl_user-print1.usr-grp NE ttbl_user-print2.usr-grp OR action NE "UPDATE" THEN
DO:
  PUT STREAM s-audit-rpt UNFORMATTED "User Group: " AT 3 ttbl_user-print1.usr-grp " (usr-grp)".
  IF action = "UPDATE" THEN
  DO:
    IF ttbl_user-print1.usr-grp NE ttbl_user-print2.usr-grp THEN
    PUT STREAM s-audit-rpt UNFORMATTED "*" AT 1.
    PUT STREAM s-audit-rpt UNFORMATTED "User Group: " AT 3 ttbl_user-print2.usr-grp.
  END.
END.
IF v_show_index THEN
DO:
  PUT STREAM s-audit-rpt UNFORMATTED "Index Fields - Label: Value (database field-name)" AT 5.
  PUT STREAM s-audit-rpt UNFORMATTED "Company: " AT 7.
  IF action = "UPDATE" THEN
  PUT STREAM s-audit-rpt UNFORMATTED ttbl_user-print2.company " (company)".
  ELSE
  PUT STREAM s-audit-rpt UNFORMATTED ttbl_user-print1.company " (company)".
  PUT STREAM s-audit-rpt UNFORMATTED "?: " AT 7.
  IF action = "UPDATE" THEN
  PUT STREAM s-audit-rpt UNFORMATTED ttbl_user-print2.batch-seq FORMAT "->,>>>,>>9" " (batch-seq)".
  ELSE
  PUT STREAM s-audit-rpt UNFORMATTED ttbl_user-print1.batch-seq FORMAT "->,>>>,>>9" " (batch-seq)".
  PUT STREAM s-audit-rpt UNFORMATTED "?: " AT 7.
  IF action = "UPDATE" THEN
  PUT STREAM s-audit-rpt UNFORMATTED ttbl_user-print2.prog-seq FORMAT "->,>>>,>>9" " (prog-seq)".
  ELSE
  PUT STREAM s-audit-rpt UNFORMATTED ttbl_user-print1.prog-seq FORMAT "->,>>>,>>9" " (prog-seq)".
END.
PUT STREAM s-audit-rpt UNFORMATTED SKIP(1).
