/* surcharge.p - generated 12/09/2005 - 15:42:55 by NoSweat */

/* this program was generated by auditgn1.p and should not be modified */
/* modifications should be made to the auto generation program         */

DEFINE INPUT PARAMETER action AS CHARACTER NO-UNDO.

DEFINE VARIABLE i AS INTEGER NO-UNDO.

{methods/defines/audit.i}
FIND FIRST ttbl_header.

DEFINE WORK-TABLE ttbl_surcharge1 NO-UNDO LIKE surcharge.
DEFINE WORK-TABLE ttbl_surcharge2 NO-UNDO LIKE surcharge.

CREATE ttbl_surcharge1.
IMPORT STREAM s_audit_file ttbl_surcharge1.
CREATE ttbl_surcharge2.
IF action = "UPDATE" THEN
IMPORT STREAM s_audit_file ttbl_surcharge2.

PUT STREAM s-audit-rpt UNFORMATTED
  "Action    User ID/(User Name)            DB.Table/(Database)  Date       Time" SKIP
  "--------- ------------------------------ -------------------- ---------- --------" SKIP
  ttbl_header.v_action AT 1
  ttbl_header.v_userid AT 11
  ttbl_header.v_file AT 42
  ttbl_header.v_date FORMAT "99/99/9999" AT 63
  STRING(ttbl_header.v_time,"HH:MM:SS") AT 74.
FIND users WHERE users.user_id = v_userid NO-LOCK NO-ERROR.
IF AVAILABLE users THEN
PUT STREAM s-audit-rpt UNFORMATTED "(" AT 10 users.user_name ")".
ELSE
PUT STREAM s-audit-rpt UNFORMATTED "(user record does not exist)" AT 10.
PUT STREAM s-audit-rpt UNFORMATTED
  "(" AT 41 ttbl_header.v_db ")" SKIP(1)
  "Label: Value (database field-name)" AT 3.
IF action = "UPDATE" THEN
PUT STREAM s-audit-rpt UNFORMATTED " ( * - denotes field value change )".
PUT STREAM s-audit-rpt UNFORMATTED FILL("-",75) AT 3.
IF NOT v_field_changed OR ttbl_surcharge1.amt NE ttbl_surcharge2.amt OR action NE "UPDATE" THEN
DO:
  PUT STREAM s-audit-rpt UNFORMATTED "Amount: " AT 3 ttbl_surcharge1.amt FORMAT "->>>,>>9.99" " (amt)".
  IF action = "UPDATE" THEN
  DO:
    IF ttbl_surcharge1.amt NE ttbl_surcharge2.amt THEN
    PUT STREAM s-audit-rpt UNFORMATTED "*" AT 1.
    PUT STREAM s-audit-rpt UNFORMATTED "Amount: " AT 3 ttbl_surcharge2.amt FORMAT "->>>,>>9.99".
  END.
END.
IF NOT v_field_changed OR ttbl_surcharge1.calc-on NE ttbl_surcharge2.calc-on OR action NE "UPDATE" THEN
DO:
  PUT STREAM s-audit-rpt UNFORMATTED "Calc-On: " AT 3 ttbl_surcharge1.calc-on " (calc-on)".
  IF action = "UPDATE" THEN
  DO:
    IF ttbl_surcharge1.calc-on NE ttbl_surcharge2.calc-on THEN
    PUT STREAM s-audit-rpt UNFORMATTED "*" AT 1.
    PUT STREAM s-audit-rpt UNFORMATTED "Calc-On: " AT 3 ttbl_surcharge2.calc-on.
  END.
END.
IF NOT v_field_changed OR ttbl_surcharge1.charge NE ttbl_surcharge2.charge OR action NE "UPDATE" THEN
DO:
  PUT STREAM s-audit-rpt UNFORMATTED "Charge: " AT 3 ttbl_surcharge1.charge " (charge)".
  IF action = "UPDATE" THEN
  DO:
    IF ttbl_surcharge1.charge NE ttbl_surcharge2.charge THEN
    PUT STREAM s-audit-rpt UNFORMATTED "*" AT 1.
    PUT STREAM s-audit-rpt UNFORMATTED "Charge: " AT 3 ttbl_surcharge2.charge.
  END.
END.
IF NOT v_field_changed OR ttbl_surcharge1.company NE ttbl_surcharge2.company OR action NE "UPDATE" THEN
DO:
  PUT STREAM s-audit-rpt UNFORMATTED "Company: " AT 3 ttbl_surcharge1.company " (company)".
  IF action = "UPDATE" THEN
  DO:
    IF ttbl_surcharge1.company NE ttbl_surcharge2.company THEN
    PUT STREAM s-audit-rpt UNFORMATTED "*" AT 1.
    PUT STREAM s-audit-rpt UNFORMATTED "Company: " AT 3 ttbl_surcharge2.company.
  END.
END.
IF NOT v_field_changed OR ttbl_surcharge1.cust-no NE ttbl_surcharge2.cust-no OR action NE "UPDATE" THEN
DO:
  PUT STREAM s-audit-rpt UNFORMATTED "Customer: " AT 3 ttbl_surcharge1.cust-no " (cust-no)".
  IF action = "UPDATE" THEN
  DO:
    IF ttbl_surcharge1.cust-no NE ttbl_surcharge2.cust-no THEN
    PUT STREAM s-audit-rpt UNFORMATTED "*" AT 1.
    PUT STREAM s-audit-rpt UNFORMATTED "Customer: " AT 3 ttbl_surcharge2.cust-no.
  END.
END.
IF NOT v_field_changed OR ttbl_surcharge1.dscr NE ttbl_surcharge2.dscr OR action NE "UPDATE" THEN
DO:
  PUT STREAM s-audit-rpt UNFORMATTED "Description: " AT 3 ttbl_surcharge1.dscr " (dscr)".
  IF action = "UPDATE" THEN
  DO:
    IF ttbl_surcharge1.dscr NE ttbl_surcharge2.dscr THEN
    PUT STREAM s-audit-rpt UNFORMATTED "*" AT 1.
    PUT STREAM s-audit-rpt UNFORMATTED "Description: " AT 3 ttbl_surcharge2.dscr.
  END.
END.
IF v_show_index THEN
DO:
  PUT STREAM s-audit-rpt UNFORMATTED "Index Fields - Label: Value (database field-name)" AT 5.
  PUT STREAM s-audit-rpt UNFORMATTED "Company: " AT 7.
  IF action = "UPDATE" THEN
  PUT STREAM s-audit-rpt UNFORMATTED ttbl_surcharge2.company " (company)".
  ELSE
  PUT STREAM s-audit-rpt UNFORMATTED ttbl_surcharge1.company " (company)".
  PUT STREAM s-audit-rpt UNFORMATTED "Charge: " AT 7.
  IF action = "UPDATE" THEN
  PUT STREAM s-audit-rpt UNFORMATTED ttbl_surcharge2.charge " (charge)".
  ELSE
  PUT STREAM s-audit-rpt UNFORMATTED ttbl_surcharge1.charge " (charge)".
  PUT STREAM s-audit-rpt UNFORMATTED "Customer: " AT 7.
  IF action = "UPDATE" THEN
  PUT STREAM s-audit-rpt UNFORMATTED ttbl_surcharge2.cust-no " (cust-no)".
  ELSE
  PUT STREAM s-audit-rpt UNFORMATTED ttbl_surcharge1.cust-no " (cust-no)".
END.
PUT STREAM s-audit-rpt UNFORMATTED SKIP(1).
