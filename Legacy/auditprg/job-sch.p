/* job-sch.p - generated 12/09/2005 - 15:42:48 by NoSweat */

/* this program was generated by auditgn1.p and should not be modified */
/* modifications should be made to the auto generation program         */

DEFINE INPUT PARAMETER action AS CHARACTER NO-UNDO.

DEFINE VARIABLE i AS INTEGER NO-UNDO.

{methods/defines/audit.i}
FIND FIRST ttbl_header.

DEFINE WORK-TABLE ttbl_job-sch1 NO-UNDO LIKE job-sch.
DEFINE WORK-TABLE ttbl_job-sch2 NO-UNDO LIKE job-sch.

CREATE ttbl_job-sch1.
IMPORT STREAM s_audit_file ttbl_job-sch1.
CREATE ttbl_job-sch2.
IF action = "UPDATE" THEN
IMPORT STREAM s_audit_file ttbl_job-sch2.

PUT STREAM s-audit-rpt UNFORMATTED
  "Action    User ID/(User Name)            DB.Table/(Database)  Date       Time" SKIP
  "--------- ------------------------------ -------------------- ---------- --------" SKIP
  ttbl_header.v_action AT 1
  ttbl_header.v_userid AT 11
  ttbl_header.v_file AT 42
  ttbl_header.v_date FORMAT "99/99/9999" AT 63
  STRING(ttbl_header.v_time,"HH:MM:SS") AT 74.
FIND users WHERE users.user_id = v_userid NO-LOCK NO-ERROR.
IF AVAILABLE users THEN
PUT STREAM s-audit-rpt UNFORMATTED "(" AT 10 users.user_name ")".
ELSE
PUT STREAM s-audit-rpt UNFORMATTED "(user record does not exist)" AT 10.
PUT STREAM s-audit-rpt UNFORMATTED
  "(" AT 41 ttbl_header.v_db ")" SKIP(1)
  "Label: Value (database field-name)" AT 3.
IF action = "UPDATE" THEN
PUT STREAM s-audit-rpt UNFORMATTED " ( * - denotes field value change )".
PUT STREAM s-audit-rpt UNFORMATTED FILL("-",75) AT 3.
IF NOT v_field_changed OR ttbl_job-sch1.company NE ttbl_job-sch2.company OR action NE "UPDATE" THEN
DO:
  PUT STREAM s-audit-rpt UNFORMATTED "Company: " AT 3 ttbl_job-sch1.company " (company)".
  IF action = "UPDATE" THEN
  DO:
    IF ttbl_job-sch1.company NE ttbl_job-sch2.company THEN
    PUT STREAM s-audit-rpt UNFORMATTED "*" AT 1.
    PUT STREAM s-audit-rpt UNFORMATTED "Company: " AT 3 ttbl_job-sch2.company.
  END.
END.
IF NOT v_field_changed OR ttbl_job-sch1.job-no NE ttbl_job-sch2.job-no OR action NE "UPDATE" THEN
DO:
  PUT STREAM s-audit-rpt UNFORMATTED "Job Number: " AT 3 ttbl_job-sch1.job-no " (job-no)".
  IF action = "UPDATE" THEN
  DO:
    IF ttbl_job-sch1.job-no NE ttbl_job-sch2.job-no THEN
    PUT STREAM s-audit-rpt UNFORMATTED "*" AT 1.
    PUT STREAM s-audit-rpt UNFORMATTED "Job Number: " AT 3 ttbl_job-sch2.job-no.
  END.
END.
IF NOT v_field_changed OR ttbl_job-sch1.job-no2 NE ttbl_job-sch2.job-no2 OR action NE "UPDATE" THEN
DO:
  PUT STREAM s-audit-rpt UNFORMATTED "Run #: " AT 3 ttbl_job-sch1.job-no2 FORMAT ">9" " (job-no2)".
  IF action = "UPDATE" THEN
  DO:
    IF ttbl_job-sch1.job-no2 NE ttbl_job-sch2.job-no2 THEN
    PUT STREAM s-audit-rpt UNFORMATTED "*" AT 1.
    PUT STREAM s-audit-rpt UNFORMATTED "Run #: " AT 3 ttbl_job-sch2.job-no2 FORMAT ">9".
  END.
END.
IF NOT v_field_changed OR ttbl_job-sch1.m-code NE ttbl_job-sch2.m-code OR action NE "UPDATE" THEN
DO:
  PUT STREAM s-audit-rpt UNFORMATTED "Code: " AT 3 ttbl_job-sch1.m-code " (m-code)".
  IF action = "UPDATE" THEN
  DO:
    IF ttbl_job-sch1.m-code NE ttbl_job-sch2.m-code THEN
    PUT STREAM s-audit-rpt UNFORMATTED "*" AT 1.
    PUT STREAM s-audit-rpt UNFORMATTED "Code: " AT 3 ttbl_job-sch2.m-code.
  END.
END.
IF NOT v_field_changed OR ttbl_job-sch1.m-date NE ttbl_job-sch2.m-date OR action NE "UPDATE" THEN
DO:
  PUT STREAM s-audit-rpt UNFORMATTED "Date: " AT 3 ttbl_job-sch1.m-date FORMAT "99/99/9999" " (m-date)".
  IF action = "UPDATE" THEN
  DO:
    IF ttbl_job-sch1.m-date NE ttbl_job-sch2.m-date THEN
    PUT STREAM s-audit-rpt UNFORMATTED "*" AT 1.
    PUT STREAM s-audit-rpt UNFORMATTED "Date: " AT 3 ttbl_job-sch2.m-date FORMAT "99/99/9999".
  END.
END.
IF NOT v_field_changed OR ttbl_job-sch1.seq NE ttbl_job-sch2.seq OR action NE "UPDATE" THEN
DO:
  PUT STREAM s-audit-rpt UNFORMATTED "Seq#: " AT 3 ttbl_job-sch1.seq FORMAT ">>>,>>9" " (seq)".
  IF action = "UPDATE" THEN
  DO:
    IF ttbl_job-sch1.seq NE ttbl_job-sch2.seq THEN
    PUT STREAM s-audit-rpt UNFORMATTED "*" AT 1.
    PUT STREAM s-audit-rpt UNFORMATTED "Seq#: " AT 3 ttbl_job-sch2.seq FORMAT ">>>,>>9".
  END.
END.
IF v_show_index THEN
DO:
  PUT STREAM s-audit-rpt UNFORMATTED "Index Fields - Label: Value (database field-name)" AT 5.
  PUT STREAM s-audit-rpt UNFORMATTED "Company: " AT 7.
  IF action = "UPDATE" THEN
  PUT STREAM s-audit-rpt UNFORMATTED ttbl_job-sch2.company " (company)".
  ELSE
  PUT STREAM s-audit-rpt UNFORMATTED ttbl_job-sch1.company " (company)".
  PUT STREAM s-audit-rpt UNFORMATTED "Code: " AT 7.
  IF action = "UPDATE" THEN
  PUT STREAM s-audit-rpt UNFORMATTED ttbl_job-sch2.m-code " (m-code)".
  ELSE
  PUT STREAM s-audit-rpt UNFORMATTED ttbl_job-sch1.m-code " (m-code)".
  PUT STREAM s-audit-rpt UNFORMATTED "Date: " AT 7.
  IF action = "UPDATE" THEN
  PUT STREAM s-audit-rpt UNFORMATTED ttbl_job-sch2.m-date FORMAT "99/99/9999" " (m-date)".
  ELSE
  PUT STREAM s-audit-rpt UNFORMATTED ttbl_job-sch1.m-date FORMAT "99/99/9999" " (m-date)".
  PUT STREAM s-audit-rpt UNFORMATTED "Seq#: " AT 7.
  IF action = "UPDATE" THEN
  PUT STREAM s-audit-rpt UNFORMATTED ttbl_job-sch2.seq FORMAT ">>>,>>9" " (seq)".
  ELSE
  PUT STREAM s-audit-rpt UNFORMATTED ttbl_job-sch1.seq FORMAT ">>>,>>9" " (seq)".
END.
PUT STREAM s-audit-rpt UNFORMATTED SKIP(1).
