/* exchange.p - generated 12/09/2005 - 15:42:44 by NoSweat */

/* this program was generated by auditgn1.p and should not be modified */
/* modifications should be made to the auto generation program         */

DEFINE INPUT PARAMETER action AS CHARACTER NO-UNDO.

DEFINE VARIABLE i AS INTEGER NO-UNDO.

{methods/defines/audit.i}
FIND FIRST ttbl_header.

DEFINE WORK-TABLE ttbl_exchange1 NO-UNDO LIKE exchange.
DEFINE WORK-TABLE ttbl_exchange2 NO-UNDO LIKE exchange.

CREATE ttbl_exchange1.
IMPORT STREAM s_audit_file ttbl_exchange1.
CREATE ttbl_exchange2.
IF action = "UPDATE" THEN
IMPORT STREAM s_audit_file ttbl_exchange2.

PUT STREAM s-audit-rpt UNFORMATTED
  "Action    User ID/(User Name)            DB.Table/(Database)  Date       Time" SKIP
  "--------- ------------------------------ -------------------- ---------- --------" SKIP
  ttbl_header.v_action AT 1
  ttbl_header.v_userid AT 11
  ttbl_header.v_file AT 42
  ttbl_header.v_date FORMAT "99/99/9999" AT 63
  STRING(ttbl_header.v_time,"HH:MM:SS") AT 74.
FIND users WHERE users.user_id = v_userid NO-LOCK NO-ERROR.
IF AVAILABLE users THEN
PUT STREAM s-audit-rpt UNFORMATTED "(" AT 10 users.user_name ")".
ELSE
PUT STREAM s-audit-rpt UNFORMATTED "(user record does not exist)" AT 10.
PUT STREAM s-audit-rpt UNFORMATTED
  "(" AT 41 ttbl_header.v_db ")" SKIP(1)
  "Label: Value (database field-name)" AT 3.
IF action = "UPDATE" THEN
PUT STREAM s-audit-rpt UNFORMATTED " ( * - denotes field value change )".
PUT STREAM s-audit-rpt UNFORMATTED FILL("-",75) AT 3.
IF NOT v_field_changed OR ttbl_exchange1.exch-code NE ttbl_exchange2.exch-code OR action NE "UPDATE" THEN
DO:
  PUT STREAM s-audit-rpt UNFORMATTED "Exchange Code: " AT 3 ttbl_exchange1.exch-code " (exch-code)".
  IF action = "UPDATE" THEN
  DO:
    IF ttbl_exchange1.exch-code NE ttbl_exchange2.exch-code THEN
    PUT STREAM s-audit-rpt UNFORMATTED "*" AT 1.
    PUT STREAM s-audit-rpt UNFORMATTED "Exchange Code: " AT 3 ttbl_exchange2.exch-code.
  END.
END.
IF NOT v_field_changed OR ttbl_exchange1.exch-desc NE ttbl_exchange2.exch-desc OR action NE "UPDATE" THEN
DO:
  PUT STREAM s-audit-rpt UNFORMATTED "Exchange Description: " AT 3 ttbl_exchange1.exch-desc " (exch-desc)".
  IF action = "UPDATE" THEN
  DO:
    IF ttbl_exchange1.exch-desc NE ttbl_exchange2.exch-desc THEN
    PUT STREAM s-audit-rpt UNFORMATTED "*" AT 1.
    PUT STREAM s-audit-rpt UNFORMATTED "Exchange Description: " AT 3 ttbl_exchange2.exch-desc.
  END.
END.
IF NOT v_field_changed OR ttbl_exchange1.exch-rate NE ttbl_exchange2.exch-rate OR action NE "UPDATE" THEN
DO:
  PUT STREAM s-audit-rpt UNFORMATTED "Exchange Rate: " AT 3 ttbl_exchange1.exch-rate FORMAT ">,>>>,>>9.9<<<<<<<" " (exch-rate)".
  IF action = "UPDATE" THEN
  DO:
    IF ttbl_exchange1.exch-rate NE ttbl_exchange2.exch-rate THEN
    PUT STREAM s-audit-rpt UNFORMATTED "*" AT 1.
    PUT STREAM s-audit-rpt UNFORMATTED "Exchange Rate: " AT 3 ttbl_exchange2.exch-rate FORMAT ">,>>>,>>9.9<<<<<<<".
  END.
END.
IF v_show_index THEN
DO:
  PUT STREAM s-audit-rpt UNFORMATTED "Index Fields - Label: Value (database field-name)" AT 5.
  PUT STREAM s-audit-rpt UNFORMATTED "?: " AT 7.
  IF action = "UPDATE" THEN
  EXPORT STREAM s-audit-rpt ttbl_exchange2.
  ELSE
  EXPORT STREAM s-audit-rpt ttbl_exchange1.
END.
PUT STREAM s-audit-rpt UNFORMATTED SKIP(1).
