/* est-inst.p - generated 12/09/2005 - 15:42:44 by NoSweat */

/* this program was generated by auditgn1.p and should not be modified */
/* modifications should be made to the auto generation program         */

DEFINE INPUT PARAMETER action AS CHARACTER NO-UNDO.

DEFINE VARIABLE i AS INTEGER NO-UNDO.

{methods/defines/audit.i}
FIND FIRST ttbl_header.

DEFINE WORK-TABLE ttbl_est-inst1 NO-UNDO LIKE est-inst.
DEFINE WORK-TABLE ttbl_est-inst2 NO-UNDO LIKE est-inst.

CREATE ttbl_est-inst1.
IMPORT STREAM s_audit_file ttbl_est-inst1.
CREATE ttbl_est-inst2.
IF action = "UPDATE" THEN
IMPORT STREAM s_audit_file ttbl_est-inst2.

PUT STREAM s-audit-rpt UNFORMATTED
  "Action    User ID/(User Name)            DB.Table/(Database)  Date       Time" SKIP
  "--------- ------------------------------ -------------------- ---------- --------" SKIP
  ttbl_header.v_action AT 1
  ttbl_header.v_userid AT 11
  ttbl_header.v_file AT 42
  ttbl_header.v_date FORMAT "99/99/9999" AT 63
  STRING(ttbl_header.v_time,"HH:MM:SS") AT 74.
FIND users WHERE users.user_id = v_userid NO-LOCK NO-ERROR.
IF AVAILABLE users THEN
PUT STREAM s-audit-rpt UNFORMATTED "(" AT 10 users.user_name ")".
ELSE
PUT STREAM s-audit-rpt UNFORMATTED "(user record does not exist)" AT 10.
PUT STREAM s-audit-rpt UNFORMATTED
  "(" AT 41 ttbl_header.v_db ")" SKIP(1)
  "Label: Value (database field-name)" AT 3.
IF action = "UPDATE" THEN
PUT STREAM s-audit-rpt UNFORMATTED " ( * - denotes field value change )".
PUT STREAM s-audit-rpt UNFORMATTED FILL("-",75) AT 3.
IF NOT v_field_changed OR ttbl_est-inst1.company NE ttbl_est-inst2.company OR action NE "UPDATE" THEN
DO:
  PUT STREAM s-audit-rpt UNFORMATTED "Company: " AT 3 ttbl_est-inst1.company " (company)".
  IF action = "UPDATE" THEN
  DO:
    IF ttbl_est-inst1.company NE ttbl_est-inst2.company THEN
    PUT STREAM s-audit-rpt UNFORMATTED "*" AT 1.
    PUT STREAM s-audit-rpt UNFORMATTED "Company: " AT 3 ttbl_est-inst2.company.
  END.
END.
IF NOT v_field_changed OR ttbl_est-inst1.dept NE ttbl_est-inst2.dept OR action NE "UPDATE" THEN
DO:
  PUT STREAM s-audit-rpt UNFORMATTED "Department: " AT 3 ttbl_est-inst1.dept " (dept)".
  IF action = "UPDATE" THEN
  DO:
    IF ttbl_est-inst1.dept NE ttbl_est-inst2.dept THEN
    PUT STREAM s-audit-rpt UNFORMATTED "*" AT 1.
    PUT STREAM s-audit-rpt UNFORMATTED "Department: " AT 3 ttbl_est-inst2.dept.
  END.
END.
IF NOT v_field_changed OR ttbl_est-inst1.dscr NE ttbl_est-inst2.dscr OR action NE "UPDATE" THEN
DO:
  PUT STREAM s-audit-rpt UNFORMATTED "Description: " AT 3 ttbl_est-inst1.dscr " (dscr)".
  IF action = "UPDATE" THEN
  DO:
    IF ttbl_est-inst1.dscr NE ttbl_est-inst2.dscr THEN
    PUT STREAM s-audit-rpt UNFORMATTED "*" AT 1.
    PUT STREAM s-audit-rpt UNFORMATTED "Description: " AT 3 ttbl_est-inst2.dscr.
  END.
END.
IF NOT v_field_changed OR ttbl_est-inst1.e-num NE ttbl_est-inst2.e-num OR action NE "UPDATE" THEN
DO:
  PUT STREAM s-audit-rpt UNFORMATTED "Est.#: " AT 3 ttbl_est-inst1.e-num FORMAT ">>>>>9" " (e-num)".
  IF action = "UPDATE" THEN
  DO:
    IF ttbl_est-inst1.e-num NE ttbl_est-inst2.e-num THEN
    PUT STREAM s-audit-rpt UNFORMATTED "*" AT 1.
    PUT STREAM s-audit-rpt UNFORMATTED "Est.#: " AT 3 ttbl_est-inst2.e-num FORMAT ">>>>>9".
  END.
END.
IF NOT v_field_changed OR ttbl_est-inst1.eqty NE ttbl_est-inst2.eqty OR action NE "UPDATE" THEN
DO:
  PUT STREAM s-audit-rpt UNFORMATTED "Qty: " AT 3 ttbl_est-inst1.eqty FORMAT "->>,>>9.99" " (eqty)".
  IF action = "UPDATE" THEN
  DO:
    IF ttbl_est-inst1.eqty NE ttbl_est-inst2.eqty THEN
    PUT STREAM s-audit-rpt UNFORMATTED "*" AT 1.
    PUT STREAM s-audit-rpt UNFORMATTED "Qty: " AT 3 ttbl_est-inst2.eqty FORMAT "->>,>>9.99".
  END.
END.
IF NOT v_field_changed OR ttbl_est-inst1.est-no NE ttbl_est-inst2.est-no OR action NE "UPDATE" THEN
DO:
  PUT STREAM s-audit-rpt UNFORMATTED "Estimate #: " AT 3 ttbl_est-inst1.est-no " (est-no)".
  IF action = "UPDATE" THEN
  DO:
    IF ttbl_est-inst1.est-no NE ttbl_est-inst2.est-no THEN
    PUT STREAM s-audit-rpt UNFORMATTED "*" AT 1.
    PUT STREAM s-audit-rpt UNFORMATTED "Estimate #: " AT 3 ttbl_est-inst2.est-no.
  END.
END.
DO i = 1 TO 3:
IF NOT v_field_changed OR ttbl_est-inst1.inst[i] NE ttbl_est-inst2.inst[i] OR action NE "UPDATE" THEN
DO:
  PUT STREAM s-audit-rpt UNFORMATTED "?[" AT 3 i "]: " ttbl_est-inst1.inst[i] " (inst[" i "])".
  IF action = "UPDATE" THEN
  DO:
    IF ttbl_est-inst1.inst[i] NE ttbl_est-inst2.inst[i] THEN
    PUT STREAM s-audit-rpt UNFORMATTED "*" AT 1.
    PUT STREAM s-audit-rpt UNFORMATTED "?[" AT 3 i "]: " ttbl_est-inst2.inst[i].
  END.
END.
END.
IF NOT v_field_changed OR ttbl_est-inst1.line-no NE ttbl_est-inst2.line-no OR action NE "UPDATE" THEN
DO:
  PUT STREAM s-audit-rpt UNFORMATTED "Line Number: " AT 3 ttbl_est-inst1.line-no FORMAT "zz9" " (line-no)".
  IF action = "UPDATE" THEN
  DO:
    IF ttbl_est-inst1.line-no NE ttbl_est-inst2.line-no THEN
    PUT STREAM s-audit-rpt UNFORMATTED "*" AT 1.
    PUT STREAM s-audit-rpt UNFORMATTED "Line Number: " AT 3 ttbl_est-inst2.line-no FORMAT "zz9".
  END.
END.
IF NOT v_field_changed OR ttbl_est-inst1.loc NE ttbl_est-inst2.loc OR action NE "UPDATE" THEN
DO:
  PUT STREAM s-audit-rpt UNFORMATTED "Warehouse: " AT 3 ttbl_est-inst1.loc " (loc)".
  IF action = "UPDATE" THEN
  DO:
    IF ttbl_est-inst1.loc NE ttbl_est-inst2.loc THEN
    PUT STREAM s-audit-rpt UNFORMATTED "*" AT 1.
    PUT STREAM s-audit-rpt UNFORMATTED "Warehouse: " AT 3 ttbl_est-inst2.loc.
  END.
END.
IF NOT v_field_changed OR ttbl_est-inst1.new-inst NE ttbl_est-inst2.new-inst OR action NE "UPDATE" THEN
DO:
  PUT STREAM s-audit-rpt UNFORMATTED "Instruction: " AT 3 ttbl_est-inst1.new-inst " (new-inst)".
  IF action = "UPDATE" THEN
  DO:
    IF ttbl_est-inst1.new-inst NE ttbl_est-inst2.new-inst THEN
    PUT STREAM s-audit-rpt UNFORMATTED "*" AT 1.
    PUT STREAM s-audit-rpt UNFORMATTED "Instruction: " AT 3 ttbl_est-inst2.new-inst.
  END.
END.
IF v_show_index THEN
DO:
  PUT STREAM s-audit-rpt UNFORMATTED "Index Fields - Label: Value (database field-name)" AT 5.
  PUT STREAM s-audit-rpt UNFORMATTED "Company: " AT 7.
  IF action = "UPDATE" THEN
  PUT STREAM s-audit-rpt UNFORMATTED ttbl_est-inst2.company " (company)".
  ELSE
  PUT STREAM s-audit-rpt UNFORMATTED ttbl_est-inst1.company " (company)".
  PUT STREAM s-audit-rpt UNFORMATTED "Estimate #: " AT 7.
  IF action = "UPDATE" THEN
  PUT STREAM s-audit-rpt UNFORMATTED ttbl_est-inst2.est-no " (est-no)".
  ELSE
  PUT STREAM s-audit-rpt UNFORMATTED ttbl_est-inst1.est-no " (est-no)".
  PUT STREAM s-audit-rpt UNFORMATTED "Qty: " AT 7.
  IF action = "UPDATE" THEN
  PUT STREAM s-audit-rpt UNFORMATTED ttbl_est-inst2.eqty FORMAT "->>,>>9.99" " (eqty)".
  ELSE
  PUT STREAM s-audit-rpt UNFORMATTED ttbl_est-inst1.eqty FORMAT "->>,>>9.99" " (eqty)".
  PUT STREAM s-audit-rpt UNFORMATTED "Department: " AT 7.
  IF action = "UPDATE" THEN
  PUT STREAM s-audit-rpt UNFORMATTED ttbl_est-inst2.dept " (dept)".
  ELSE
  PUT STREAM s-audit-rpt UNFORMATTED ttbl_est-inst1.dept " (dept)".
  PUT STREAM s-audit-rpt UNFORMATTED "Line Number: " AT 7.
  IF action = "UPDATE" THEN
  PUT STREAM s-audit-rpt UNFORMATTED ttbl_est-inst2.line-no FORMAT "zz9" " (line-no)".
  ELSE
  PUT STREAM s-audit-rpt UNFORMATTED ttbl_est-inst1.line-no FORMAT "zz9" " (line-no)".
END.
PUT STREAM s-audit-rpt UNFORMATTED SKIP(1).
