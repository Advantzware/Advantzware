/* users.p - generated 12/02/2005 - 16:52:37 by NoSweat */

/* this program was generated by auditgn1.p and should not be modified */
/* modifications should be made to the auto generation program         */

DEFINE INPUT PARAMETER action AS CHARACTER NO-UNDO.

DEFINE VARIABLE i AS INTEGER NO-UNDO.

{methods/defines/audit.i}
FIND FIRST ttbl_header.

DEFINE WORK-TABLE ttbl_users1 NO-UNDO LIKE users.
DEFINE WORK-TABLE ttbl_users2 NO-UNDO LIKE users.

CREATE ttbl_users1.
IMPORT STREAM s_audit_file ttbl_users1.
CREATE ttbl_users2.
IF action = "UPDATE" THEN
IMPORT STREAM s_audit_file ttbl_users2.

PUT STREAM s-audit-rpt UNFORMATTED
  "Action    User ID/(User Name)            DB.Table/(Database)  Date       Time" SKIP
  "--------- ------------------------------ -------------------- ---------- --------" SKIP
  ttbl_header.v_action AT 1
  ttbl_header.v_userid AT 11
  ttbl_header.v_file AT 42
  ttbl_header.v_date FORMAT "99/99/9999" AT 63
  STRING(ttbl_header.v_time,"HH:MM:SS") AT 74.
FIND users WHERE users.user_id = v_userid NO-LOCK NO-ERROR.
IF AVAILABLE users THEN
PUT STREAM s-audit-rpt UNFORMATTED "(" AT 10 users.user_name ")".
ELSE
PUT STREAM s-audit-rpt UNFORMATTED "(user record does not exist)" AT 10.
PUT STREAM s-audit-rpt UNFORMATTED
  "(" AT 41 ttbl_header.v_db ")" SKIP(1)
  "Label: Value (database field-name)" AT 3.
IF action = "UPDATE" THEN
PUT STREAM s-audit-rpt UNFORMATTED " ( * - denotes field value change )".
PUT STREAM s-audit-rpt UNFORMATTED FILL("-",75) AT 3.
IF NOT v_field_changed OR ttbl_users1.developer NE ttbl_users2.developer OR action NE "UPDATE" THEN
DO:
  PUT STREAM s-audit-rpt UNFORMATTED "Developer: " AT 3 ttbl_users1.developer FORMAT "yes/no" " (developer)".
  IF action = "UPDATE" THEN
  DO:
    IF ttbl_users1.developer NE ttbl_users2.developer THEN
    PUT STREAM s-audit-rpt UNFORMATTED "*" AT 1.
    PUT STREAM s-audit-rpt UNFORMATTED "Developer: " AT 3 ttbl_users2.developer FORMAT "yes/no".
  END.
END.
IF NOT v_field_changed OR ttbl_users1.image_filename NE ttbl_users2.image_filename OR action NE "UPDATE" THEN
DO:
  PUT STREAM s-audit-rpt UNFORMATTED "Image File Name: " AT 3 ttbl_users1.image_filename " (image_filename)".
  IF action = "UPDATE" THEN
  DO:
    IF ttbl_users1.image_filename NE ttbl_users2.image_filename THEN
    PUT STREAM s-audit-rpt UNFORMATTED "*" AT 1.
    PUT STREAM s-audit-rpt UNFORMATTED "Image File Name: " AT 3 ttbl_users2.image_filename.
  END.
END.
IF NOT v_field_changed OR ttbl_users1.track_usage NE ttbl_users2.track_usage OR action NE "UPDATE" THEN
DO:
  PUT STREAM s-audit-rpt UNFORMATTED "Track Usage: " AT 3 ttbl_users1.track_usage FORMAT "yes/no" " (track_usage)".
  IF action = "UPDATE" THEN
  DO:
    IF ttbl_users1.track_usage NE ttbl_users2.track_usage THEN
    PUT STREAM s-audit-rpt UNFORMATTED "*" AT 1.
    PUT STREAM s-audit-rpt UNFORMATTED "Track Usage: " AT 3 ttbl_users2.track_usage FORMAT "yes/no".
  END.
END.
IF NOT v_field_changed OR ttbl_users1.user_id NE ttbl_users2.user_id OR action NE "UPDATE" THEN
DO:
  PUT STREAM s-audit-rpt UNFORMATTED "User ID's: " AT 3 ttbl_users1.user_id " (user_id)".
  IF action = "UPDATE" THEN
  DO:
    IF ttbl_users1.user_id NE ttbl_users2.user_id THEN
    PUT STREAM s-audit-rpt UNFORMATTED "*" AT 1.
    PUT STREAM s-audit-rpt UNFORMATTED "User ID's: " AT 3 ttbl_users2.user_id.
  END.
END.
IF NOT v_field_changed OR ttbl_users1.user_language NE ttbl_users2.user_language OR action NE "UPDATE" THEN
DO:
  PUT STREAM s-audit-rpt UNFORMATTED "Language: " AT 3 ttbl_users1.user_language " (user_language)".
  IF action = "UPDATE" THEN
  DO:
    IF ttbl_users1.user_language NE ttbl_users2.user_language THEN
    PUT STREAM s-audit-rpt UNFORMATTED "*" AT 1.
    PUT STREAM s-audit-rpt UNFORMATTED "Language: " AT 3 ttbl_users2.user_language.
  END.
END.
IF NOT v_field_changed OR ttbl_users1.user_name NE ttbl_users2.user_name OR action NE "UPDATE" THEN
DO:
  PUT STREAM s-audit-rpt UNFORMATTED "User Name: " AT 3 ttbl_users1.user_name " (user_name)".
  IF action = "UPDATE" THEN
  DO:
    IF ttbl_users1.user_name NE ttbl_users2.user_name THEN
    PUT STREAM s-audit-rpt UNFORMATTED "*" AT 1.
    PUT STREAM s-audit-rpt UNFORMATTED "User Name: " AT 3 ttbl_users2.user_name.
  END.
END.
DO i = 1 TO 5:
IF NOT v_field_changed OR ttbl_users1.user_program[i] NE ttbl_users2.user_program[i] OR action NE "UPDATE" THEN
DO:
  PUT STREAM s-audit-rpt UNFORMATTED "User Application Program[" AT 3 i "]: " ttbl_users1.user_program[i] " (user_program[" i "])".
  IF action = "UPDATE" THEN
  DO:
    IF ttbl_users1.user_program[i] NE ttbl_users2.user_program[i] THEN
    PUT STREAM s-audit-rpt UNFORMATTED "*" AT 1.
    PUT STREAM s-audit-rpt UNFORMATTED "User Application Program[" AT 3 i "]: " ttbl_users2.user_program[i].
  END.
END.
END.
IF NOT v_field_changed OR ttbl_users1.use_colors NE ttbl_users2.use_colors OR action NE "UPDATE" THEN
DO:
  PUT STREAM s-audit-rpt UNFORMATTED "Use Defined Colors: " AT 3 ttbl_users1.use_colors FORMAT "yes/no" " (use_colors)".
  IF action = "UPDATE" THEN
  DO:
    IF ttbl_users1.use_colors NE ttbl_users2.use_colors THEN
    PUT STREAM s-audit-rpt UNFORMATTED "*" AT 1.
    PUT STREAM s-audit-rpt UNFORMATTED "Use Defined Colors: " AT 3 ttbl_users2.use_colors FORMAT "yes/no".
  END.
END.
IF NOT v_field_changed OR ttbl_users1.use_ctrl_keys NE ttbl_users2.use_ctrl_keys OR action NE "UPDATE" THEN
DO:
  PUT STREAM s-audit-rpt UNFORMATTED "Use Control Key Accelerators: " AT 3 ttbl_users1.use_ctrl_keys FORMAT "yes/no" " (use_ctrl_keys)".
  IF action = "UPDATE" THEN
  DO:
    IF ttbl_users1.use_ctrl_keys NE ttbl_users2.use_ctrl_keys THEN
    PUT STREAM s-audit-rpt UNFORMATTED "*" AT 1.
    PUT STREAM s-audit-rpt UNFORMATTED "Use Control Key Accelerators: " AT 3 ttbl_users2.use_ctrl_keys FORMAT "yes/no".
  END.
END.
IF NOT v_field_changed OR ttbl_users1.use_fonts NE ttbl_users2.use_fonts OR action NE "UPDATE" THEN
DO:
  PUT STREAM s-audit-rpt UNFORMATTED "Use Defined Fonts: " AT 3 ttbl_users1.use_fonts FORMAT "yes/no" " (use_fonts)".
  IF action = "UPDATE" THEN
  DO:
    IF ttbl_users1.use_fonts NE ttbl_users2.use_fonts THEN
    PUT STREAM s-audit-rpt UNFORMATTED "*" AT 1.
    PUT STREAM s-audit-rpt UNFORMATTED "Use Defined Fonts: " AT 3 ttbl_users2.use_fonts FORMAT "yes/no".
  END.
END.
DO i = 1 TO 13:
IF NOT v_field_changed OR ttbl_users1.widget_bgc[i] NE ttbl_users2.widget_bgc[i] OR action NE "UPDATE" THEN
DO:
  PUT STREAM s-audit-rpt UNFORMATTED "Widget Background Color[" AT 3 i "]: " ttbl_users1.widget_bgc[i] FORMAT ">>9" " (widget_bgc[" i "])".
  IF action = "UPDATE" THEN
  DO:
    IF ttbl_users1.widget_bgc[i] NE ttbl_users2.widget_bgc[i] THEN
    PUT STREAM s-audit-rpt UNFORMATTED "*" AT 1.
    PUT STREAM s-audit-rpt UNFORMATTED "Widget Background Color[" AT 3 i "]: " ttbl_users2.widget_bgc[i] FORMAT ">>9".
  END.
END.
END.
DO i = 1 TO 13:
IF NOT v_field_changed OR ttbl_users1.widget_fgc[i] NE ttbl_users2.widget_fgc[i] OR action NE "UPDATE" THEN
DO:
  PUT STREAM s-audit-rpt UNFORMATTED "Widget Foreround Color[" AT 3 i "]: " ttbl_users1.widget_fgc[i] FORMAT ">>9" " (widget_fgc[" i "])".
  IF action = "UPDATE" THEN
  DO:
    IF ttbl_users1.widget_fgc[i] NE ttbl_users2.widget_fgc[i] THEN
    PUT STREAM s-audit-rpt UNFORMATTED "*" AT 1.
    PUT STREAM s-audit-rpt UNFORMATTED "Widget Foreround Color[" AT 3 i "]: " ttbl_users2.widget_fgc[i] FORMAT ">>9".
  END.
END.
END.
DO i = 1 TO 13:
IF NOT v_field_changed OR ttbl_users1.widget_font[i] NE ttbl_users2.widget_font[i] OR action NE "UPDATE" THEN
DO:
  PUT STREAM s-audit-rpt UNFORMATTED "Widget Font[" AT 3 i "]: " ttbl_users1.widget_font[i] FORMAT ">>9" " (widget_font[" i "])".
  IF action = "UPDATE" THEN
  DO:
    IF ttbl_users1.widget_font[i] NE ttbl_users2.widget_font[i] THEN
    PUT STREAM s-audit-rpt UNFORMATTED "*" AT 1.
    PUT STREAM s-audit-rpt UNFORMATTED "Widget Font[" AT 3 i "]: " ttbl_users2.widget_font[i] FORMAT ">>9".
  END.
END.
END.
IF v_show_index THEN
DO:
  PUT STREAM s-audit-rpt UNFORMATTED "Index Fields - Label: Value (database field-name)" AT 5.
  PUT STREAM s-audit-rpt UNFORMATTED "User ID's: " AT 7.
  IF action = "UPDATE" THEN
  PUT STREAM s-audit-rpt UNFORMATTED ttbl_users2.user_id " (user_id)".
  ELSE
  PUT STREAM s-audit-rpt UNFORMATTED ttbl_users1.user_id " (user_id)".
END.
PUT STREAM s-audit-rpt UNFORMATTED SKIP(1).
