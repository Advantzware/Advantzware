/* monthly.p - generated 01/10/2001 - 13:00:05 by nosweat */

/* this program was generated by auditgn1.p and should not be modified */
/* modifications should be made to the auto generation program         */

DEFINE INPUT PARAMETER action AS CHARACTER NO-UNDO.

DEFINE VARIABLE i AS INTEGER NO-UNDO.

{methods/defines/audit.i}
FIND FIRST ttbl_header.

DEFINE WORK-TABLE ttbl_monthly1 NO-UNDO LIKE monthly.
DEFINE WORK-TABLE ttbl_monthly2 NO-UNDO LIKE monthly.

CREATE ttbl_monthly1.
IMPORT STREAM s_audit_file ttbl_monthly1.
CREATE ttbl_monthly2.
IF action = "UPDATE" THEN
IMPORT STREAM s_audit_file ttbl_monthly2.

PUT STREAM s-audit-rpt UNFORMATTED
  "Action    User ID/(User Name)            DB.Table/(Database)  Date       Time" SKIP
  "--------- ------------------------------ -------------------- ---------- --------" SKIP
  ttbl_header.v_action AT 1
  ttbl_header.v_userid AT 11
  ttbl_header.v_file AT 42
  ttbl_header.v_date FORMAT "99/99/9999" AT 63
  STRING(ttbl_header.v_time,"HH:MM:SS") AT 74.
FIND users WHERE users.user_id = v_userid NO-LOCK NO-ERROR.
IF AVAILABLE users THEN
PUT STREAM s-audit-rpt UNFORMATTED "(" AT 10 users.user_name ")".
ELSE
PUT STREAM s-audit-rpt UNFORMATTED "(user record does not exist)" AT 10.
PUT STREAM s-audit-rpt UNFORMATTED
  "(" AT 41 ttbl_header.v_db ")" SKIP(1)
  "Label: Value (database field-name)" AT 3.
IF action = "UPDATE" THEN
PUT STREAM s-audit-rpt UNFORMATTED " ( * - denotes field value change )".
PUT STREAM s-audit-rpt UNFORMATTED FILL("-",75) AT 3.
IF NOT v_field_changed OR ttbl_monthly1.bill-amt NE ttbl_monthly2.bill-amt OR action NE "UPDATE" THEN
DO:
  PUT STREAM s-audit-rpt UNFORMATTED "Amount: " AT 3 ttbl_monthly1.bill-amt FORMAT "->>,>>9.99" " (bill-amt)".
  IF action = "UPDATE" THEN
  DO:
    IF ttbl_monthly1.bill-amt NE ttbl_monthly2.bill-amt THEN
    PUT STREAM s-audit-rpt UNFORMATTED "*" AT 1.
    PUT STREAM s-audit-rpt UNFORMATTED "Amount: " AT 3 ttbl_monthly2.bill-amt FORMAT "->>,>>9.99".
  END.
END.
IF NOT v_field_changed OR ttbl_monthly1.csnum NE ttbl_monthly2.csnum OR action NE "UPDATE" THEN
DO:
  PUT STREAM s-audit-rpt UNFORMATTED "Cust Num: " AT 3 ttbl_monthly1.csnum " (csnum)".
  IF action = "UPDATE" THEN
  DO:
    IF ttbl_monthly1.csnum NE ttbl_monthly2.csnum THEN
    PUT STREAM s-audit-rpt UNFORMATTED "*" AT 1.
    PUT STREAM s-audit-rpt UNFORMATTED "Cust Num: " AT 3 ttbl_monthly2.csnum.
  END.
END.
IF NOT v_field_changed OR ttbl_monthly1.dept NE ttbl_monthly2.dept OR action NE "UPDATE" THEN
DO:
  PUT STREAM s-audit-rpt UNFORMATTED "Billing Dept: " AT 3 ttbl_monthly1.dept " (dept)".
  IF action = "UPDATE" THEN
  DO:
    IF ttbl_monthly1.dept NE ttbl_monthly2.dept THEN
    PUT STREAM s-audit-rpt UNFORMATTED "*" AT 1.
    PUT STREAM s-audit-rpt UNFORMATTED "Billing Dept: " AT 3 ttbl_monthly2.dept.
  END.
END.
IF NOT v_field_changed OR ttbl_monthly1.freight NE ttbl_monthly2.freight OR action NE "UPDATE" THEN
DO:
  PUT STREAM s-audit-rpt UNFORMATTED "Freight: " AT 3 ttbl_monthly1.freight FORMAT "->>,>>9.99" " (freight)".
  IF action = "UPDATE" THEN
  DO:
    IF ttbl_monthly1.freight NE ttbl_monthly2.freight THEN
    PUT STREAM s-audit-rpt UNFORMATTED "*" AT 1.
    PUT STREAM s-audit-rpt UNFORMATTED "Freight: " AT 3 ttbl_monthly2.freight FORMAT "->>,>>9.99".
  END.
END.
IF NOT v_field_changed OR ttbl_monthly1.inv-date NE ttbl_monthly2.inv-date OR action NE "UPDATE" THEN
DO:
  PUT STREAM s-audit-rpt UNFORMATTED "Invoice Date: " AT 3 ttbl_monthly1.inv-date FORMAT "99/99/99" " (inv-date)".
  IF action = "UPDATE" THEN
  DO:
    IF ttbl_monthly1.inv-date NE ttbl_monthly2.inv-date THEN
    PUT STREAM s-audit-rpt UNFORMATTED "*" AT 1.
    PUT STREAM s-audit-rpt UNFORMATTED "Invoice Date: " AT 3 ttbl_monthly2.inv-date FORMAT "99/99/99".
  END.
END.
IF NOT v_field_changed OR ttbl_monthly1.invnum NE ttbl_monthly2.invnum OR action NE "UPDATE" THEN
DO:
  PUT STREAM s-audit-rpt UNFORMATTED "Invoice #: " AT 3 ttbl_monthly1.invnum " (invnum)".
  IF action = "UPDATE" THEN
  DO:
    IF ttbl_monthly1.invnum NE ttbl_monthly2.invnum THEN
    PUT STREAM s-audit-rpt UNFORMATTED "*" AT 1.
    PUT STREAM s-audit-rpt UNFORMATTED "Invoice #: " AT 3 ttbl_monthly2.invnum.
  END.
END.
IF NOT v_field_changed OR ttbl_monthly1.invnumint NE ttbl_monthly2.invnumint OR action NE "UPDATE" THEN
DO:
  PUT STREAM s-audit-rpt UNFORMATTED "Invoice: " AT 3 ttbl_monthly1.invnumint FORMAT "99999" " (invnumint)".
  IF action = "UPDATE" THEN
  DO:
    IF ttbl_monthly1.invnumint NE ttbl_monthly2.invnumint THEN
    PUT STREAM s-audit-rpt UNFORMATTED "*" AT 1.
    PUT STREAM s-audit-rpt UNFORMATTED "Invoice: " AT 3 ttbl_monthly2.invnumint FORMAT "99999".
  END.
END.
IF NOT v_field_changed OR ttbl_monthly1.paid NE ttbl_monthly2.paid OR action NE "UPDATE" THEN
DO:
  PUT STREAM s-audit-rpt UNFORMATTED "Paid: " AT 3 ttbl_monthly1.paid FORMAT "->>,>>9.99" " (paid)".
  IF action = "UPDATE" THEN
  DO:
    IF ttbl_monthly1.paid NE ttbl_monthly2.paid THEN
    PUT STREAM s-audit-rpt UNFORMATTED "*" AT 1.
    PUT STREAM s-audit-rpt UNFORMATTED "Paid: " AT 3 ttbl_monthly2.paid FORMAT "->>,>>9.99".
  END.
END.
IF NOT v_field_changed OR ttbl_monthly1.paid-date NE ttbl_monthly2.paid-date OR action NE "UPDATE" THEN
DO:
  PUT STREAM s-audit-rpt UNFORMATTED "Paid Date: " AT 3 ttbl_monthly1.paid-date FORMAT "99/99/99" " (paid-date)".
  IF action = "UPDATE" THEN
  DO:
    IF ttbl_monthly1.paid-date NE ttbl_monthly2.paid-date THEN
    PUT STREAM s-audit-rpt UNFORMATTED "*" AT 1.
    PUT STREAM s-audit-rpt UNFORMATTED "Paid Date: " AT 3 ttbl_monthly2.paid-date FORMAT "99/99/99".
  END.
END.
IF NOT v_field_changed OR ttbl_monthly1.slnum NE ttbl_monthly2.slnum OR action NE "UPDATE" THEN
DO:
  PUT STREAM s-audit-rpt UNFORMATTED "?: " AT 3 ttbl_monthly1.slnum " (slnum)".
  IF action = "UPDATE" THEN
  DO:
    IF ttbl_monthly1.slnum NE ttbl_monthly2.slnum THEN
    PUT STREAM s-audit-rpt UNFORMATTED "*" AT 1.
    PUT STREAM s-audit-rpt UNFORMATTED "?: " AT 3 ttbl_monthly2.slnum.
  END.
END.
IF NOT v_field_changed OR ttbl_monthly1.tax NE ttbl_monthly2.tax OR action NE "UPDATE" THEN
DO:
  PUT STREAM s-audit-rpt UNFORMATTED "Tax: " AT 3 ttbl_monthly1.tax FORMAT "->,>>9.99" " (tax)".
  IF action = "UPDATE" THEN
  DO:
    IF ttbl_monthly1.tax NE ttbl_monthly2.tax THEN
    PUT STREAM s-audit-rpt UNFORMATTED "*" AT 1.
    PUT STREAM s-audit-rpt UNFORMATTED "Tax: " AT 3 ttbl_monthly2.tax FORMAT "->,>>9.99".
  END.
END.
IF NOT v_field_changed OR ttbl_monthly1.type NE ttbl_monthly2.type OR action NE "UPDATE" THEN
DO:
  PUT STREAM s-audit-rpt UNFORMATTED "Type: " AT 3 ttbl_monthly1.type " (type)".
  IF action = "UPDATE" THEN
  DO:
    IF ttbl_monthly1.type NE ttbl_monthly2.type THEN
    PUT STREAM s-audit-rpt UNFORMATTED "*" AT 1.
    PUT STREAM s-audit-rpt UNFORMATTED "Type: " AT 3 ttbl_monthly2.type.
  END.
END.
IF v_show_index THEN
DO:
  PUT STREAM s-audit-rpt UNFORMATTED "Index Fields - Label: Value (database field-name)" AT 5.
  PUT STREAM s-audit-rpt UNFORMATTED "Invoice #: " AT 7.
  IF action = "UPDATE" THEN
  PUT STREAM s-audit-rpt UNFORMATTED ttbl_monthly2.invnum " (invnum)".
  ELSE
  PUT STREAM s-audit-rpt UNFORMATTED ttbl_monthly1.invnum " (invnum)".
END.
PUT STREAM s-audit-rpt UNFORMATTED SKIP(1).
