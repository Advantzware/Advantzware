/* rm-receipts.p - generated 01/10/2001 - 13:00:08 by nosweat */

/* this program was generated by auditgn1.p and should not be modified */
/* modifications should be made to the auto generation program         */

DEFINE INPUT PARAMETER action AS CHARACTER NO-UNDO.

DEFINE VARIABLE i AS INTEGER NO-UNDO.

{methods/defines/audit.i}
FIND FIRST ttbl_header.

DEFINE WORK-TABLE ttbl_rm-receipts1 NO-UNDO LIKE rm-receipts.
DEFINE WORK-TABLE ttbl_rm-receipts2 NO-UNDO LIKE rm-receipts.

CREATE ttbl_rm-receipts1.
IMPORT STREAM s_audit_file ttbl_rm-receipts1.
CREATE ttbl_rm-receipts2.
IF action = "UPDATE" THEN
IMPORT STREAM s_audit_file ttbl_rm-receipts2.

PUT STREAM s-audit-rpt UNFORMATTED
  "Action    User ID/(User Name)            DB.Table/(Database)  Date       Time" SKIP
  "--------- ------------------------------ -------------------- ---------- --------" SKIP
  ttbl_header.v_action AT 1
  ttbl_header.v_userid AT 11
  ttbl_header.v_file AT 42
  ttbl_header.v_date FORMAT "99/99/9999" AT 63
  STRING(ttbl_header.v_time,"HH:MM:SS") AT 74.
FIND users WHERE users.user_id = v_userid NO-LOCK NO-ERROR.
IF AVAILABLE users THEN
PUT STREAM s-audit-rpt UNFORMATTED "(" AT 10 users.user_name ")".
ELSE
PUT STREAM s-audit-rpt UNFORMATTED "(user record does not exist)" AT 10.
PUT STREAM s-audit-rpt UNFORMATTED
  "(" AT 41 ttbl_header.v_db ")" SKIP(1)
  "Label: Value (database field-name)" AT 3.
IF action = "UPDATE" THEN
PUT STREAM s-audit-rpt UNFORMATTED " ( * - denotes field value change )".
PUT STREAM s-audit-rpt UNFORMATTED FILL("-",75) AT 3.
IF NOT v_field_changed OR ttbl_rm-receipts1.company NE ttbl_rm-receipts2.company OR action NE "UPDATE" THEN
DO:
  PUT STREAM s-audit-rpt UNFORMATTED "Company: " AT 3 ttbl_rm-receipts1.company " (company)".
  IF action = "UPDATE" THEN
  DO:
    IF ttbl_rm-receipts1.company NE ttbl_rm-receipts2.company THEN
    PUT STREAM s-audit-rpt UNFORMATTED "*" AT 1.
    PUT STREAM s-audit-rpt UNFORMATTED "Company: " AT 3 ttbl_rm-receipts2.company.
  END.
END.
IF NOT v_field_changed OR ttbl_rm-receipts1.cost NE ttbl_rm-receipts2.cost OR action NE "UPDATE" THEN
DO:
  PUT STREAM s-audit-rpt UNFORMATTED "Costs: " AT 3 ttbl_rm-receipts1.cost FORMAT ">>>,>>9.9999" " (cost)".
  IF action = "UPDATE" THEN
  DO:
    IF ttbl_rm-receipts1.cost NE ttbl_rm-receipts2.cost THEN
    PUT STREAM s-audit-rpt UNFORMATTED "*" AT 1.
    PUT STREAM s-audit-rpt UNFORMATTED "Costs: " AT 3 ttbl_rm-receipts2.cost FORMAT ">>>,>>9.9999".
  END.
END.
IF NOT v_field_changed OR ttbl_rm-receipts1.i-no NE ttbl_rm-receipts2.i-no OR action NE "UPDATE" THEN
DO:
  PUT STREAM s-audit-rpt UNFORMATTED "Item No: " AT 3 ttbl_rm-receipts1.i-no " (i-no)".
  IF action = "UPDATE" THEN
  DO:
    IF ttbl_rm-receipts1.i-no NE ttbl_rm-receipts2.i-no THEN
    PUT STREAM s-audit-rpt UNFORMATTED "*" AT 1.
    PUT STREAM s-audit-rpt UNFORMATTED "Item No: " AT 3 ttbl_rm-receipts2.i-no.
  END.
END.
IF NOT v_field_changed OR ttbl_rm-receipts1.job-no NE ttbl_rm-receipts2.job-no OR action NE "UPDATE" THEN
DO:
  PUT STREAM s-audit-rpt UNFORMATTED "?: " AT 3 ttbl_rm-receipts1.job-no " (job-no)".
  IF action = "UPDATE" THEN
  DO:
    IF ttbl_rm-receipts1.job-no NE ttbl_rm-receipts2.job-no THEN
    PUT STREAM s-audit-rpt UNFORMATTED "*" AT 1.
    PUT STREAM s-audit-rpt UNFORMATTED "?: " AT 3 ttbl_rm-receipts2.job-no.
  END.
END.
IF NOT v_field_changed OR ttbl_rm-receipts1.loc NE ttbl_rm-receipts2.loc OR action NE "UPDATE" THEN
DO:
  PUT STREAM s-audit-rpt UNFORMATTED "Warehouse: " AT 3 ttbl_rm-receipts1.loc " (loc)".
  IF action = "UPDATE" THEN
  DO:
    IF ttbl_rm-receipts1.loc NE ttbl_rm-receipts2.loc THEN
    PUT STREAM s-audit-rpt UNFORMATTED "*" AT 1.
    PUT STREAM s-audit-rpt UNFORMATTED "Warehouse: " AT 3 ttbl_rm-receipts2.loc.
  END.
END.
IF NOT v_field_changed OR ttbl_rm-receipts1.po-complete NE ttbl_rm-receipts2.po-complete OR action NE "UPDATE" THEN
DO:
  PUT STREAM s-audit-rpt UNFORMATTED "?: " AT 3 ttbl_rm-receipts1.po-complete FORMAT "Y/N" " (po-complete)".
  IF action = "UPDATE" THEN
  DO:
    IF ttbl_rm-receipts1.po-complete NE ttbl_rm-receipts2.po-complete THEN
    PUT STREAM s-audit-rpt UNFORMATTED "*" AT 1.
    PUT STREAM s-audit-rpt UNFORMATTED "?: " AT 3 ttbl_rm-receipts2.po-complete FORMAT "Y/N".
  END.
END.
IF NOT v_field_changed OR ttbl_rm-receipts1.po-no NE ttbl_rm-receipts2.po-no OR action NE "UPDATE" THEN
DO:
  PUT STREAM s-audit-rpt UNFORMATTED "?: " AT 3 ttbl_rm-receipts1.po-no " (po-no)".
  IF action = "UPDATE" THEN
  DO:
    IF ttbl_rm-receipts1.po-no NE ttbl_rm-receipts2.po-no THEN
    PUT STREAM s-audit-rpt UNFORMATTED "*" AT 1.
    PUT STREAM s-audit-rpt UNFORMATTED "?: " AT 3 ttbl_rm-receipts2.po-no.
  END.
END.
IF NOT v_field_changed OR ttbl_rm-receipts1.posted NE ttbl_rm-receipts2.posted OR action NE "UPDATE" THEN
DO:
  PUT STREAM s-audit-rpt UNFORMATTED "?: " AT 3 ttbl_rm-receipts1.posted FORMAT "Y/N" " (posted)".
  IF action = "UPDATE" THEN
  DO:
    IF ttbl_rm-receipts1.posted NE ttbl_rm-receipts2.posted THEN
    PUT STREAM s-audit-rpt UNFORMATTED "*" AT 1.
    PUT STREAM s-audit-rpt UNFORMATTED "?: " AT 3 ttbl_rm-receipts2.posted FORMAT "Y/N".
  END.
END.
IF NOT v_field_changed OR ttbl_rm-receipts1.posted-date NE ttbl_rm-receipts2.posted-date OR action NE "UPDATE" THEN
DO:
  PUT STREAM s-audit-rpt UNFORMATTED "?: " AT 3 ttbl_rm-receipts1.posted-date FORMAT "99/99/99" " (posted-date)".
  IF action = "UPDATE" THEN
  DO:
    IF ttbl_rm-receipts1.posted-date NE ttbl_rm-receipts2.posted-date THEN
    PUT STREAM s-audit-rpt UNFORMATTED "*" AT 1.
    PUT STREAM s-audit-rpt UNFORMATTED "?: " AT 3 ttbl_rm-receipts2.posted-date FORMAT "99/99/99".
  END.
END.
IF NOT v_field_changed OR ttbl_rm-receipts1.pur-uom NE ttbl_rm-receipts2.pur-uom OR action NE "UPDATE" THEN
DO:
  PUT STREAM s-audit-rpt UNFORMATTED "Purchased UOM: " AT 3 ttbl_rm-receipts1.pur-uom " (pur-uom)".
  IF action = "UPDATE" THEN
  DO:
    IF ttbl_rm-receipts1.pur-uom NE ttbl_rm-receipts2.pur-uom THEN
    PUT STREAM s-audit-rpt UNFORMATTED "*" AT 1.
    PUT STREAM s-audit-rpt UNFORMATTED "Purchased UOM: " AT 3 ttbl_rm-receipts2.pur-uom.
  END.
END.
IF NOT v_field_changed OR ttbl_rm-receipts1.qty-recvd NE ttbl_rm-receipts2.qty-recvd OR action NE "UPDATE" THEN
DO:
  PUT STREAM s-audit-rpt UNFORMATTED "?: " AT 3 ttbl_rm-receipts1.qty-recvd FORMAT ">>>,>>>,>>9.9<<" " (qty-recvd)".
  IF action = "UPDATE" THEN
  DO:
    IF ttbl_rm-receipts1.qty-recvd NE ttbl_rm-receipts2.qty-recvd THEN
    PUT STREAM s-audit-rpt UNFORMATTED "*" AT 1.
    PUT STREAM s-audit-rpt UNFORMATTED "?: " AT 3 ttbl_rm-receipts2.qty-recvd FORMAT ">>>,>>>,>>9.9<<".
  END.
END.
IF NOT v_field_changed OR ttbl_rm-receipts1.tag NE ttbl_rm-receipts2.tag OR action NE "UPDATE" THEN
DO:
  PUT STREAM s-audit-rpt UNFORMATTED "?: " AT 3 ttbl_rm-receipts1.tag " (tag)".
  IF action = "UPDATE" THEN
  DO:
    IF ttbl_rm-receipts1.tag NE ttbl_rm-receipts2.tag THEN
    PUT STREAM s-audit-rpt UNFORMATTED "*" AT 1.
    PUT STREAM s-audit-rpt UNFORMATTED "?: " AT 3 ttbl_rm-receipts2.tag.
  END.
END.
IF NOT v_field_changed OR ttbl_rm-receipts1.trans-date NE ttbl_rm-receipts2.trans-date OR action NE "UPDATE" THEN
DO:
  PUT STREAM s-audit-rpt UNFORMATTED "?: " AT 3 ttbl_rm-receipts1.trans-date FORMAT "99/99/99" " (trans-date)".
  IF action = "UPDATE" THEN
  DO:
    IF ttbl_rm-receipts1.trans-date NE ttbl_rm-receipts2.trans-date THEN
    PUT STREAM s-audit-rpt UNFORMATTED "*" AT 1.
    PUT STREAM s-audit-rpt UNFORMATTED "?: " AT 3 ttbl_rm-receipts2.trans-date FORMAT "99/99/99".
  END.
END.
IF v_show_index THEN
DO:
  PUT STREAM s-audit-rpt UNFORMATTED "Index Fields - Label: Value (database field-name)" AT 5.
  PUT STREAM s-audit-rpt UNFORMATTED "Company: " AT 7.
  IF action = "UPDATE" THEN
  PUT STREAM s-audit-rpt UNFORMATTED ttbl_rm-receipts2.company " (company)".
  ELSE
  PUT STREAM s-audit-rpt UNFORMATTED ttbl_rm-receipts1.company " (company)".
  PUT STREAM s-audit-rpt UNFORMATTED "Warehouse: " AT 7.
  IF action = "UPDATE" THEN
  PUT STREAM s-audit-rpt UNFORMATTED ttbl_rm-receipts2.loc " (loc)".
  ELSE
  PUT STREAM s-audit-rpt UNFORMATTED ttbl_rm-receipts1.loc " (loc)".
END.
PUT STREAM s-audit-rpt UNFORMATTED SKIP(1).
