/* stax-group.p - generated 01/10/2001 - 13:00:09 by nosweat */

/* this program was generated by auditgn1.p and should not be modified */
/* modifications should be made to the auto generation program         */

DEFINE INPUT PARAMETER action AS CHARACTER NO-UNDO.

DEFINE VARIABLE i AS INTEGER NO-UNDO.

{methods/defines/audit.i}
FIND FIRST ttbl_header.

DEFINE WORK-TABLE ttbl_stax-group1 NO-UNDO LIKE stax-group.
DEFINE WORK-TABLE ttbl_stax-group2 NO-UNDO LIKE stax-group.

CREATE ttbl_stax-group1.
IMPORT STREAM s_audit_file ttbl_stax-group1.
CREATE ttbl_stax-group2.
IF action = "UPDATE" THEN
IMPORT STREAM s_audit_file ttbl_stax-group2.

PUT STREAM s-audit-rpt UNFORMATTED
  "Action    User ID/(User Name)            DB.Table/(Database)  Date       Time" SKIP
  "--------- ------------------------------ -------------------- ---------- --------" SKIP
  ttbl_header.v_action AT 1
  ttbl_header.v_userid AT 11
  ttbl_header.v_file AT 42
  ttbl_header.v_date FORMAT "99/99/9999" AT 63
  STRING(ttbl_header.v_time,"HH:MM:SS") AT 74.
FIND users WHERE users.user_id = v_userid NO-LOCK NO-ERROR.
IF AVAILABLE users THEN
PUT STREAM s-audit-rpt UNFORMATTED "(" AT 10 users.user_name ")".
ELSE
PUT STREAM s-audit-rpt UNFORMATTED "(user record does not exist)" AT 10.
PUT STREAM s-audit-rpt UNFORMATTED
  "(" AT 41 ttbl_header.v_db ")" SKIP(1)
  "Label: Value (database field-name)" AT 3.
IF action = "UPDATE" THEN
PUT STREAM s-audit-rpt UNFORMATTED " ( * - denotes field value change )".
PUT STREAM s-audit-rpt UNFORMATTED FILL("-",75) AT 3.
IF NOT v_field_changed OR ttbl_stax-group1.company NE ttbl_stax-group2.company OR action NE "UPDATE" THEN
DO:
  PUT STREAM s-audit-rpt UNFORMATTED "?: " AT 3 ttbl_stax-group1.company " (company)".
  IF action = "UPDATE" THEN
  DO:
    IF ttbl_stax-group1.company NE ttbl_stax-group2.company THEN
    PUT STREAM s-audit-rpt UNFORMATTED "*" AT 1.
    PUT STREAM s-audit-rpt UNFORMATTED "?: " AT 3 ttbl_stax-group2.company.
  END.
END.
IF NOT v_field_changed OR ttbl_stax-group1.tax-dscr NE ttbl_stax-group2.tax-dscr OR action NE "UPDATE" THEN
DO:
  PUT STREAM s-audit-rpt UNFORMATTED "Description: " AT 3 ttbl_stax-group1.tax-dscr " (tax-dscr)".
  IF action = "UPDATE" THEN
  DO:
    IF ttbl_stax-group1.tax-dscr NE ttbl_stax-group2.tax-dscr THEN
    PUT STREAM s-audit-rpt UNFORMATTED "*" AT 1.
    PUT STREAM s-audit-rpt UNFORMATTED "Description: " AT 3 ttbl_stax-group2.tax-dscr.
  END.
END.
IF NOT v_field_changed OR ttbl_stax-group1.tax-group NE ttbl_stax-group2.tax-group OR action NE "UPDATE" THEN
DO:
  PUT STREAM s-audit-rpt UNFORMATTED "Tax Group: " AT 3 ttbl_stax-group1.tax-group " (tax-group)".
  IF action = "UPDATE" THEN
  DO:
    IF ttbl_stax-group1.tax-group NE ttbl_stax-group2.tax-group THEN
    PUT STREAM s-audit-rpt UNFORMATTED "*" AT 1.
    PUT STREAM s-audit-rpt UNFORMATTED "Tax Group: " AT 3 ttbl_stax-group2.tax-group.
  END.
END.
IF v_show_index THEN
DO:
  PUT STREAM s-audit-rpt UNFORMATTED "Index Fields - Label: Value (database field-name)" AT 5.
  PUT STREAM s-audit-rpt UNFORMATTED "?: " AT 7.
  IF action = "UPDATE" THEN
  PUT STREAM s-audit-rpt UNFORMATTED ttbl_stax-group2.company " (company)".
  ELSE
  PUT STREAM s-audit-rpt UNFORMATTED ttbl_stax-group1.company " (company)".
  PUT STREAM s-audit-rpt UNFORMATTED "Tax Group: " AT 7.
  IF action = "UPDATE" THEN
  PUT STREAM s-audit-rpt UNFORMATTED ttbl_stax-group2.tax-group " (tax-group)".
  ELSE
  PUT STREAM s-audit-rpt UNFORMATTED ttbl_stax-group1.tax-group " (tax-group)".
END.
PUT STREAM s-audit-rpt UNFORMATTED SKIP(1).
