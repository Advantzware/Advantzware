/* optconfg.p - generated 12/02/2005 - 16:52:40 by NoSweat */

/* this program was generated by auditgn1.p and should not be modified */
/* modifications should be made to the auto generation program         */

DEFINE INPUT PARAMETER action AS CHARACTER NO-UNDO.

DEFINE VARIABLE i AS INTEGER NO-UNDO.

{methods/defines/audit.i}
FIND FIRST ttbl_header.

DEFINE WORK-TABLE ttbl_optconfg1 NO-UNDO LIKE optconfg.
DEFINE WORK-TABLE ttbl_optconfg2 NO-UNDO LIKE optconfg.

CREATE ttbl_optconfg1.
IMPORT STREAM s_audit_file ttbl_optconfg1.
CREATE ttbl_optconfg2.
IF action = "UPDATE" THEN
IMPORT STREAM s_audit_file ttbl_optconfg2.

PUT STREAM s-audit-rpt UNFORMATTED
  "Action    User ID/(User Name)            DB.Table/(Database)  Date       Time" SKIP
  "--------- ------------------------------ -------------------- ---------- --------" SKIP
  ttbl_header.v_action AT 1
  ttbl_header.v_userid AT 11
  ttbl_header.v_file AT 42
  ttbl_header.v_date FORMAT "99/99/9999" AT 63
  STRING(ttbl_header.v_time,"HH:MM:SS") AT 74.
FIND users WHERE users.user_id = v_userid NO-LOCK NO-ERROR.
IF AVAILABLE users THEN
PUT STREAM s-audit-rpt UNFORMATTED "(" AT 10 users.user_name ")".
ELSE
PUT STREAM s-audit-rpt UNFORMATTED "(user record does not exist)" AT 10.
PUT STREAM s-audit-rpt UNFORMATTED
  "(" AT 41 ttbl_header.v_db ")" SKIP(1)
  "Label: Value (database field-name)" AT 3.
IF action = "UPDATE" THEN
PUT STREAM s-audit-rpt UNFORMATTED " ( * - denotes field value change )".
PUT STREAM s-audit-rpt UNFORMATTED FILL("-",75) AT 3.
IF NOT v_field_changed OR ttbl_optconfg1.description NE ttbl_optconfg2.description OR action NE "UPDATE" THEN
DO:
  PUT STREAM s-audit-rpt UNFORMATTED "Description: " AT 3 ttbl_optconfg1.description " (description)".
  IF action = "UPDATE" THEN
  DO:
    IF ttbl_optconfg1.description NE ttbl_optconfg2.description THEN
    PUT STREAM s-audit-rpt UNFORMATTED "*" AT 1.
    PUT STREAM s-audit-rpt UNFORMATTED "Description: " AT 3 ttbl_optconfg2.description.
  END.
END.
IF NOT v_field_changed OR ttbl_optconfg1.export_job NE ttbl_optconfg2.export_job OR action NE "UPDATE" THEN
DO:
  PUT STREAM s-audit-rpt UNFORMATTED "Export Job: " AT 3 ttbl_optconfg1.export_job FORMAT "yes/no" " (export_job)".
  IF action = "UPDATE" THEN
  DO:
    IF ttbl_optconfg1.export_job NE ttbl_optconfg2.export_job THEN
    PUT STREAM s-audit-rpt UNFORMATTED "*" AT 1.
    PUT STREAM s-audit-rpt UNFORMATTED "Export Job: " AT 3 ttbl_optconfg2.export_job FORMAT "yes/no".
  END.
END.
IF NOT v_field_changed OR ttbl_optconfg1.file_format NE ttbl_optconfg2.file_format OR action NE "UPDATE" THEN
DO:
  PUT STREAM s-audit-rpt UNFORMATTED "File Format: " AT 3 ttbl_optconfg1.file_format " (file_format)".
  IF action = "UPDATE" THEN
  DO:
    IF ttbl_optconfg1.file_format NE ttbl_optconfg2.file_format THEN
    PUT STREAM s-audit-rpt UNFORMATTED "*" AT 1.
    PUT STREAM s-audit-rpt UNFORMATTED "File Format: " AT 3 ttbl_optconfg2.file_format.
  END.
END.
IF NOT v_field_changed OR ttbl_optconfg1.file_location NE ttbl_optconfg2.file_location OR action NE "UPDATE" THEN
DO:
  PUT STREAM s-audit-rpt UNFORMATTED "File Location Directory: " AT 3 ttbl_optconfg1.file_location " (file_location)".
  IF action = "UPDATE" THEN
  DO:
    IF ttbl_optconfg1.file_location NE ttbl_optconfg2.file_location THEN
    PUT STREAM s-audit-rpt UNFORMATTED "*" AT 1.
    PUT STREAM s-audit-rpt UNFORMATTED "File Location Directory: " AT 3 ttbl_optconfg2.file_location.
  END.
END.
IF NOT v_field_changed OR ttbl_optconfg1.loadtype NE ttbl_optconfg2.loadtype OR action NE "UPDATE" THEN
DO:
  PUT STREAM s-audit-rpt UNFORMATTED "Load Type: " AT 3 ttbl_optconfg1.loadtype " (loadtype)".
  IF action = "UPDATE" THEN
  DO:
    IF ttbl_optconfg1.loadtype NE ttbl_optconfg2.loadtype THEN
    PUT STREAM s-audit-rpt UNFORMATTED "*" AT 1.
    PUT STREAM s-audit-rpt UNFORMATTED "Load Type: " AT 3 ttbl_optconfg2.loadtype.
  END.
END.
IF NOT v_field_changed OR ttbl_optconfg1.prgmname NE ttbl_optconfg2.prgmname OR action NE "UPDATE" THEN
DO:
  PUT STREAM s-audit-rpt UNFORMATTED "Program: " AT 3 ttbl_optconfg1.prgmname " (prgmname)".
  IF action = "UPDATE" THEN
  DO:
    IF ttbl_optconfg1.prgmname NE ttbl_optconfg2.prgmname THEN
    PUT STREAM s-audit-rpt UNFORMATTED "*" AT 1.
    PUT STREAM s-audit-rpt UNFORMATTED "Program: " AT 3 ttbl_optconfg2.prgmname.
  END.
END.
IF v_show_index THEN
DO:
  PUT STREAM s-audit-rpt UNFORMATTED "Index Fields - Label: Value (database field-name)" AT 5.
  PUT STREAM s-audit-rpt UNFORMATTED "Description: " AT 7.
  IF action = "UPDATE" THEN
  PUT STREAM s-audit-rpt UNFORMATTED ttbl_optconfg2.description " (description)".
  ELSE
  PUT STREAM s-audit-rpt UNFORMATTED ttbl_optconfg1.description " (description)".
END.
PUT STREAM s-audit-rpt UNFORMATTED SKIP(1).
