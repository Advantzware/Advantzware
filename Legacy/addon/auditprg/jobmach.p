/* jobmach.p - generated 12/02/2005 - 16:52:39 by NoSweat */

/* this program was generated by auditgn1.p and should not be modified */
/* modifications should be made to the auto generation program         */

DEFINE INPUT PARAMETER action AS CHARACTER NO-UNDO.

DEFINE VARIABLE i AS INTEGER NO-UNDO.

{methods/defines/audit.i}
FIND FIRST ttbl_header.

DEFINE WORK-TABLE ttbl_jobmach1 NO-UNDO LIKE jobmach.
DEFINE WORK-TABLE ttbl_jobmach2 NO-UNDO LIKE jobmach.

CREATE ttbl_jobmach1.
IMPORT STREAM s_audit_file ttbl_jobmach1.
CREATE ttbl_jobmach2.
IF action = "UPDATE" THEN
IMPORT STREAM s_audit_file ttbl_jobmach2.

PUT STREAM s-audit-rpt UNFORMATTED
  "Action    User ID/(User Name)            DB.Table/(Database)  Date       Time" SKIP
  "--------- ------------------------------ -------------------- ---------- --------" SKIP
  ttbl_header.v_action AT 1
  ttbl_header.v_userid AT 11
  ttbl_header.v_file AT 42
  ttbl_header.v_date FORMAT "99/99/9999" AT 63
  STRING(ttbl_header.v_time,"HH:MM:SS") AT 74.
FIND users WHERE users.user_id = v_userid NO-LOCK NO-ERROR.
IF AVAILABLE users THEN
PUT STREAM s-audit-rpt UNFORMATTED "(" AT 10 users.user_name ")".
ELSE
PUT STREAM s-audit-rpt UNFORMATTED "(user record does not exist)" AT 10.
PUT STREAM s-audit-rpt UNFORMATTED
  "(" AT 41 ttbl_header.v_db ")" SKIP(1)
  "Label: Value (database field-name)" AT 3.
IF action = "UPDATE" THEN
PUT STREAM s-audit-rpt UNFORMATTED " ( * - denotes field value change )".
PUT STREAM s-audit-rpt UNFORMATTED FILL("-",75) AT 3.
IF NOT v_field_changed OR ttbl_jobmach1.blank_number NE ttbl_jobmach2.blank_number OR action NE "UPDATE" THEN
DO:
  PUT STREAM s-audit-rpt UNFORMATTED "Blank Number: " AT 3 ttbl_jobmach1.blank_number FORMAT ">9" " (blank_number)".
  IF action = "UPDATE" THEN
  DO:
    IF ttbl_jobmach1.blank_number NE ttbl_jobmach2.blank_number THEN
    PUT STREAM s-audit-rpt UNFORMATTED "*" AT 1.
    PUT STREAM s-audit-rpt UNFORMATTED "Blank Number: " AT 3 ttbl_jobmach2.blank_number FORMAT ">9".
  END.
END.
IF NOT v_field_changed OR ttbl_jobmach1.department NE ttbl_jobmach2.department OR action NE "UPDATE" THEN
DO:
  PUT STREAM s-audit-rpt UNFORMATTED "Department: " AT 3 ttbl_jobmach1.department " (department)".
  IF action = "UPDATE" THEN
  DO:
    IF ttbl_jobmach1.department NE ttbl_jobmach2.department THEN
    PUT STREAM s-audit-rpt UNFORMATTED "*" AT 1.
    PUT STREAM s-audit-rpt UNFORMATTED "Department: " AT 3 ttbl_jobmach2.department.
  END.
END.
IF NOT v_field_changed OR ttbl_jobmach1.description NE ttbl_jobmach2.description OR action NE "UPDATE" THEN
DO:
  PUT STREAM s-audit-rpt UNFORMATTED "Description: " AT 3 ttbl_jobmach1.description " (description)".
  IF action = "UPDATE" THEN
  DO:
    IF ttbl_jobmach1.description NE ttbl_jobmach2.description THEN
    PUT STREAM s-audit-rpt UNFORMATTED "*" AT 1.
    PUT STREAM s-audit-rpt UNFORMATTED "Description: " AT 3 ttbl_jobmach2.description.
  END.
END.
IF NOT v_field_changed OR ttbl_jobmach1.form_number NE ttbl_jobmach2.form_number OR action NE "UPDATE" THEN
DO:
  PUT STREAM s-audit-rpt UNFORMATTED "Form Number: " AT 3 ttbl_jobmach1.form_number FORMAT ">>9" " (form_number)".
  IF action = "UPDATE" THEN
  DO:
    IF ttbl_jobmach1.form_number NE ttbl_jobmach2.form_number THEN
    PUT STREAM s-audit-rpt UNFORMATTED "*" AT 1.
    PUT STREAM s-audit-rpt UNFORMATTED "Form Number: " AT 3 ttbl_jobmach2.form_number FORMAT ">>9".
  END.
END.
IF NOT v_field_changed OR ttbl_jobmach1.job NE ttbl_jobmach2.job OR action NE "UPDATE" THEN
DO:
  PUT STREAM s-audit-rpt UNFORMATTED "Job Number: " AT 3 ttbl_jobmach1.job " (job)".
  IF action = "UPDATE" THEN
  DO:
    IF ttbl_jobmach1.job NE ttbl_jobmach2.job THEN
    PUT STREAM s-audit-rpt UNFORMATTED "*" AT 1.
    PUT STREAM s-audit-rpt UNFORMATTED "Job Number: " AT 3 ttbl_jobmach2.job.
  END.
END.
IF NOT v_field_changed OR ttbl_jobmach1.machine NE ttbl_jobmach2.machine OR action NE "UPDATE" THEN
DO:
  PUT STREAM s-audit-rpt UNFORMATTED "Machine: " AT 3 ttbl_jobmach1.machine " (machine)".
  IF action = "UPDATE" THEN
  DO:
    IF ttbl_jobmach1.machine NE ttbl_jobmach2.machine THEN
    PUT STREAM s-audit-rpt UNFORMATTED "*" AT 1.
    PUT STREAM s-audit-rpt UNFORMATTED "Machine: " AT 3 ttbl_jobmach2.machine.
  END.
END.
IF NOT v_field_changed OR ttbl_jobmach1.make_ready NE ttbl_jobmach2.make_ready OR action NE "UPDATE" THEN
DO:
  PUT STREAM s-audit-rpt UNFORMATTED "Make Ready: " AT 3 ttbl_jobmach1.make_ready FORMAT ">>9.99" " (make_ready)".
  IF action = "UPDATE" THEN
  DO:
    IF ttbl_jobmach1.make_ready NE ttbl_jobmach2.make_ready THEN
    PUT STREAM s-audit-rpt UNFORMATTED "*" AT 1.
    PUT STREAM s-audit-rpt UNFORMATTED "Make Ready: " AT 3 ttbl_jobmach2.make_ready FORMAT ">>9.99".
  END.
END.
IF NOT v_field_changed OR ttbl_jobmach1.pass NE ttbl_jobmach2.pass OR action NE "UPDATE" THEN
DO:
  PUT STREAM s-audit-rpt UNFORMATTED "Pass: " AT 3 ttbl_jobmach1.pass FORMAT ">>9" " (pass)".
  IF action = "UPDATE" THEN
  DO:
    IF ttbl_jobmach1.pass NE ttbl_jobmach2.pass THEN
    PUT STREAM s-audit-rpt UNFORMATTED "*" AT 1.
    PUT STREAM s-audit-rpt UNFORMATTED "Pass: " AT 3 ttbl_jobmach2.pass FORMAT ">>9".
  END.
END.
IF NOT v_field_changed OR ttbl_jobmach1.run_time NE ttbl_jobmach2.run_time OR action NE "UPDATE" THEN
DO:
  PUT STREAM s-audit-rpt UNFORMATTED "Run Time: " AT 3 ttbl_jobmach1.run_time FORMAT ">>9.99" " (run_time)".
  IF action = "UPDATE" THEN
  DO:
    IF ttbl_jobmach1.run_time NE ttbl_jobmach2.run_time THEN
    PUT STREAM s-audit-rpt UNFORMATTED "*" AT 1.
    PUT STREAM s-audit-rpt UNFORMATTED "Run Time: " AT 3 ttbl_jobmach2.run_time FORMAT ">>9.99".
  END.
END.
IF NOT v_field_changed OR ttbl_jobmach1.sequence NE ttbl_jobmach2.sequence OR action NE "UPDATE" THEN
DO:
  PUT STREAM s-audit-rpt UNFORMATTED "Sequence: " AT 3 ttbl_jobmach1.sequence FORMAT ">>9" " (sequence)".
  IF action = "UPDATE" THEN
  DO:
    IF ttbl_jobmach1.sequence NE ttbl_jobmach2.sequence THEN
    PUT STREAM s-audit-rpt UNFORMATTED "*" AT 1.
    PUT STREAM s-audit-rpt UNFORMATTED "Sequence: " AT 3 ttbl_jobmach2.sequence FORMAT ">>9".
  END.
END.
IF NOT v_field_changed OR ttbl_jobmach1.speed NE ttbl_jobmach2.speed OR action NE "UPDATE" THEN
DO:
  PUT STREAM s-audit-rpt UNFORMATTED "Speed: " AT 3 ttbl_jobmach1.speed FORMAT ">>>,>>9" " (speed)".
  IF action = "UPDATE" THEN
  DO:
    IF ttbl_jobmach1.speed NE ttbl_jobmach2.speed THEN
    PUT STREAM s-audit-rpt UNFORMATTED "*" AT 1.
    PUT STREAM s-audit-rpt UNFORMATTED "Speed: " AT 3 ttbl_jobmach2.speed FORMAT ">>>,>>9".
  END.
END.
IF NOT v_field_changed OR ttbl_jobmach1.units NE ttbl_jobmach2.units OR action NE "UPDATE" THEN
DO:
  PUT STREAM s-audit-rpt UNFORMATTED "Units: " AT 3 ttbl_jobmach1.units FORMAT ">>,>>>,>>9" " (units)".
  IF action = "UPDATE" THEN
  DO:
    IF ttbl_jobmach1.units NE ttbl_jobmach2.units THEN
    PUT STREAM s-audit-rpt UNFORMATTED "*" AT 1.
    PUT STREAM s-audit-rpt UNFORMATTED "Units: " AT 3 ttbl_jobmach2.units FORMAT ">>,>>>,>>9".
  END.
END.
IF NOT v_field_changed OR ttbl_jobmach1.waste NE ttbl_jobmach2.waste OR action NE "UPDATE" THEN
DO:
  PUT STREAM s-audit-rpt UNFORMATTED "Waste: " AT 3 ttbl_jobmach1.waste FORMAT ">>>9" " (waste)".
  IF action = "UPDATE" THEN
  DO:
    IF ttbl_jobmach1.waste NE ttbl_jobmach2.waste THEN
    PUT STREAM s-audit-rpt UNFORMATTED "*" AT 1.
    PUT STREAM s-audit-rpt UNFORMATTED "Waste: " AT 3 ttbl_jobmach2.waste FORMAT ">>>9".
  END.
END.
IF v_show_index THEN
DO:
  PUT STREAM s-audit-rpt UNFORMATTED "Index Fields - Label: Value (database field-name)" AT 5.
  PUT STREAM s-audit-rpt UNFORMATTED "Job Number: " AT 7.
  IF action = "UPDATE" THEN
  PUT STREAM s-audit-rpt UNFORMATTED ttbl_jobmach2.job " (job)".
  ELSE
  PUT STREAM s-audit-rpt UNFORMATTED ttbl_jobmach1.job " (job)".
  PUT STREAM s-audit-rpt UNFORMATTED "Sequence: " AT 7.
  IF action = "UPDATE" THEN
  PUT STREAM s-audit-rpt UNFORMATTED ttbl_jobmach2.sequence FORMAT ">>9" " (sequence)".
  ELSE
  PUT STREAM s-audit-rpt UNFORMATTED ttbl_jobmach1.sequence FORMAT ">>9" " (sequence)".
END.
PUT STREAM s-audit-rpt UNFORMATTED SKIP(1).
