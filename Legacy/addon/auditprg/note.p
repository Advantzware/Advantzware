/* note.p - generated 03/29/2000 - 16:41:43 by nosweat */

/* this program was generated by auditgn1.p and should not be modified */
/* modifications should be made to the auto generation program         */

DEFINE INPUT PARAMETER action AS CHARACTER NO-UNDO.

DEFINE VARIABLE i AS INTEGER NO-UNDO.

{methods/defines/audit.i}
FIND FIRST ttbl_header.

DEFINE WORK-TABLE ttbl_note1 NO-UNDO LIKE notes.
DEFINE WORK-TABLE ttbl_note2 NO-UNDO LIKE notes.

CREATE ttbl_note1.
IMPORT STREAM s_audit_file ttbl_note1.
CREATE ttbl_note2.
IF action = "UPDATE" THEN
IMPORT STREAM s_audit_file ttbl_note2.

PUT STREAM s-audit-rpt UNFORMATTED
  "Action    User ID/(User Name)            DB.Table/(Database)  Date       Time" SKIP
  "--------- ------------------------------ -------------------- ---------- --------" SKIP
  ttbl_header.v_action AT 1
  ttbl_header.v_userid AT 11
  ttbl_header.v_file AT 42
  ttbl_header.v_date FORMAT "99/99/9999" AT 63
  STRING(ttbl_header.v_time,"HH:MM:SS") AT 74.
FIND users WHERE users.user_id = v_userid NO-LOCK NO-ERROR.
IF AVAILABLE users THEN
PUT STREAM s-audit-rpt UNFORMATTED "(" AT 10 users.user_name ")".
ELSE
PUT STREAM s-audit-rpt UNFORMATTED "(user record does not exist)" AT 10.
PUT STREAM s-audit-rpt UNFORMATTED
  "(" AT 41 ttbl_header.v_db ")" SKIP(1)
  "Label: Value (database field-name)" AT 3.
IF action = "UPDATE" THEN
PUT STREAM s-audit-rpt UNFORMATTED " ( * - denotes field value change )".
PUT STREAM s-audit-rpt UNFORMATTED FILL("-",75) AT 3.
IF NOT v_field_changed OR ttbl_note1.note_date NE ttbl_note2.note_date OR action NE "UPDATE" THEN
DO:
  PUT STREAM s-audit-rpt UNFORMATTED "Note Date: " AT 3 ttbl_note1.note_date FORMAT "99/99/9999" " (note_date)".
  IF action = "UPDATE" THEN
  DO:
    IF ttbl_note1.note_date NE ttbl_note2.note_date THEN
    PUT STREAM s-audit-rpt UNFORMATTED "*" AT 1.
    PUT STREAM s-audit-rpt UNFORMATTED "Note Date: " AT 3 ttbl_note2.note_date FORMAT "99/99/9999".
  END.
END.
IF NOT v_field_changed OR ttbl_note1.note_text NE ttbl_note2.note_text OR action NE "UPDATE" THEN
DO:
  PUT STREAM s-audit-rpt UNFORMATTED "Note Text: " AT 3 ttbl_note1.note_text " (note_text)".
  IF action = "UPDATE" THEN
  DO:
    IF ttbl_note1.note_text NE ttbl_note2.note_text THEN
    PUT STREAM s-audit-rpt UNFORMATTED "*" AT 1.
    PUT STREAM s-audit-rpt UNFORMATTED "Note Text: " AT 3 ttbl_note2.note_text.
  END.
END.
IF NOT v_field_changed OR ttbl_note1.note_time NE ttbl_note2.note_time OR action NE "UPDATE" THEN
DO:
  PUT STREAM s-audit-rpt UNFORMATTED "Note Time: " AT 3 ttbl_note1.note_time FORMAT "99999" " (note_time)".
  IF action = "UPDATE" THEN
  DO:
    IF ttbl_note1.note_time NE ttbl_note2.note_time THEN
    PUT STREAM s-audit-rpt UNFORMATTED "*" AT 1.
    PUT STREAM s-audit-rpt UNFORMATTED "Note Time: " AT 3 ttbl_note2.note_time FORMAT "99999".
  END.
END.
IF NOT v_field_changed OR ttbl_note1.note_title NE ttbl_note2.note_title OR action NE "UPDATE" THEN
DO:
  PUT STREAM s-audit-rpt UNFORMATTED "Note Title: " AT 3 ttbl_note1.note_title " (note_title)".
  IF action = "UPDATE" THEN
  DO:
    IF ttbl_note1.note_title NE ttbl_note2.note_title THEN
    PUT STREAM s-audit-rpt UNFORMATTED "*" AT 1.
    PUT STREAM s-audit-rpt UNFORMATTED "Note Title: " AT 3 ttbl_note2.note_title.
  END.
END.
IF NOT v_field_changed OR ttbl_note1.user_id NE ttbl_note2.user_id OR action NE "UPDATE" THEN
DO:
  PUT STREAM s-audit-rpt UNFORMATTED "User ID: " AT 3 ttbl_note1.user_id " (user_id)".
  IF action = "UPDATE" THEN
  DO:
    IF ttbl_note1.user_id NE ttbl_note2.user_id THEN
    PUT STREAM s-audit-rpt UNFORMATTED "*" AT 1.
    PUT STREAM s-audit-rpt UNFORMATTED "User ID: " AT 3 ttbl_note2.user_id.
  END.
END.
IF NOT v_field_changed OR ttbl_note1.viewed NE ttbl_note2.viewed OR action NE "UPDATE" THEN
DO:
  PUT STREAM s-audit-rpt UNFORMATTED "Viewed: " AT 3 ttbl_note1.viewed FORMAT "yes/no" " (viewed)".
  IF action = "UPDATE" THEN
  DO:
    IF ttbl_note1.viewed NE ttbl_note2.viewed THEN
    PUT STREAM s-audit-rpt UNFORMATTED "*" AT 1.
    PUT STREAM s-audit-rpt UNFORMATTED "Viewed: " AT 3 ttbl_note2.viewed FORMAT "yes/no".
  END.
END.
IF v_show_index THEN
DO:
  PUT STREAM s-audit-rpt UNFORMATTED "Index Fields - Label: Value (database field-name)" AT 5.
  PUT STREAM s-audit-rpt UNFORMATTED "Record Key: " AT 7.
  IF action = "UPDATE" THEN
  PUT STREAM s-audit-rpt UNFORMATTED ttbl_note2.rec_key " (rec_key)".
  ELSE
  PUT STREAM s-audit-rpt UNFORMATTED ttbl_note1.rec_key " (rec_key)".
  PUT STREAM s-audit-rpt UNFORMATTED "Note Date: " AT 7.
  IF action = "UPDATE" THEN
  PUT STREAM s-audit-rpt UNFORMATTED ttbl_note2.note_date FORMAT "99/99/9999" " (note_date)".
  ELSE
  PUT STREAM s-audit-rpt UNFORMATTED ttbl_note1.note_date FORMAT "99/99/9999" " (note_date)".
  PUT STREAM s-audit-rpt UNFORMATTED "Note Time: " AT 7.
  IF action = "UPDATE" THEN
  PUT STREAM s-audit-rpt UNFORMATTED ttbl_note2.note_time FORMAT "99999" " (note_time)".
  ELSE
  PUT STREAM s-audit-rpt UNFORMATTED ttbl_note1.note_time FORMAT "99999" " (note_time)".
END.
PUT STREAM s-audit-rpt UNFORMATTED SKIP(1).
