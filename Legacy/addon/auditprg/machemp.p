/* machemp.p - generated 12/02/2005 - 16:52:38 by NoSweat */

/* this program was generated by auditgn1.p and should not be modified */
/* modifications should be made to the auto generation program         */

DEFINE INPUT PARAMETER action AS CHARACTER NO-UNDO.

DEFINE VARIABLE i AS INTEGER NO-UNDO.

{methods/defines/audit.i}
FIND FIRST ttbl_header.

DEFINE WORK-TABLE ttbl_machemp1 NO-UNDO LIKE machemp.
DEFINE WORK-TABLE ttbl_machemp2 NO-UNDO LIKE machemp.

CREATE ttbl_machemp1.
IMPORT STREAM s_audit_file ttbl_machemp1.
CREATE ttbl_machemp2.
IF action = "UPDATE" THEN
IMPORT STREAM s_audit_file ttbl_machemp2.

PUT STREAM s-audit-rpt UNFORMATTED
  "Action    User ID/(User Name)            DB.Table/(Database)  Date       Time" SKIP
  "--------- ------------------------------ -------------------- ---------- --------" SKIP
  ttbl_header.v_action AT 1
  ttbl_header.v_userid AT 11
  ttbl_header.v_file AT 42
  ttbl_header.v_date FORMAT "99/99/9999" AT 63
  STRING(ttbl_header.v_time,"HH:MM:SS") AT 74.
FIND users WHERE users.user_id = v_userid NO-LOCK NO-ERROR.
IF AVAILABLE users THEN
PUT STREAM s-audit-rpt UNFORMATTED "(" AT 10 users.user_name ")".
ELSE
PUT STREAM s-audit-rpt UNFORMATTED "(user record does not exist)" AT 10.
PUT STREAM s-audit-rpt UNFORMATTED
  "(" AT 41 ttbl_header.v_db ")" SKIP(1)
  "Label: Value (database field-name)" AT 3.
IF action = "UPDATE" THEN
PUT STREAM s-audit-rpt UNFORMATTED " ( * - denotes field value change )".
PUT STREAM s-audit-rpt UNFORMATTED FILL("-",75) AT 3.
IF NOT v_field_changed OR ttbl_machemp1.employee NE ttbl_machemp2.employee OR action NE "UPDATE" THEN
DO:
  PUT STREAM s-audit-rpt UNFORMATTED "Emp ID: " AT 3 ttbl_machemp1.employee " (employee)".
  IF action = "UPDATE" THEN
  DO:
    IF ttbl_machemp1.employee NE ttbl_machemp2.employee THEN
    PUT STREAM s-audit-rpt UNFORMATTED "*" AT 1.
    PUT STREAM s-audit-rpt UNFORMATTED "Emp ID: " AT 3 ttbl_machemp2.employee.
  END.
END.
IF NOT v_field_changed OR ttbl_machemp1.end_date NE ttbl_machemp2.end_date OR action NE "UPDATE" THEN
DO:
  PUT STREAM s-audit-rpt UNFORMATTED "End Date: " AT 3 ttbl_machemp1.end_date FORMAT "99/99/9999" " (end_date)".
  IF action = "UPDATE" THEN
  DO:
    IF ttbl_machemp1.end_date NE ttbl_machemp2.end_date THEN
    PUT STREAM s-audit-rpt UNFORMATTED "*" AT 1.
    PUT STREAM s-audit-rpt UNFORMATTED "End Date: " AT 3 ttbl_machemp2.end_date FORMAT "99/99/9999".
  END.
END.
IF NOT v_field_changed OR ttbl_machemp1.end_time NE ttbl_machemp2.end_time OR action NE "UPDATE" THEN
DO:
  PUT STREAM s-audit-rpt UNFORMATTED "End Time: " AT 3 ttbl_machemp1.end_time FORMAT "->,>>>,>>9" " (end_time)".
  IF action = "UPDATE" THEN
  DO:
    IF ttbl_machemp1.end_time NE ttbl_machemp2.end_time THEN
    PUT STREAM s-audit-rpt UNFORMATTED "*" AT 1.
    PUT STREAM s-audit-rpt UNFORMATTED "End Time: " AT 3 ttbl_machemp2.end_time FORMAT "->,>>>,>>9".
  END.
END.
IF NOT v_field_changed OR ttbl_machemp1.login_time NE ttbl_machemp2.login_time OR action NE "UPDATE" THEN
DO:
  PUT STREAM s-audit-rpt UNFORMATTED "Login Time: " AT 3 ttbl_machemp1.login_time FORMAT "->,>>>,>>9" " (login_time)".
  IF action = "UPDATE" THEN
  DO:
    IF ttbl_machemp1.login_time NE ttbl_machemp2.login_time THEN
    PUT STREAM s-audit-rpt UNFORMATTED "*" AT 1.
    PUT STREAM s-audit-rpt UNFORMATTED "Login Time: " AT 3 ttbl_machemp2.login_time FORMAT "->,>>>,>>9".
  END.
END.
IF NOT v_field_changed OR ttbl_machemp1.logout_time NE ttbl_machemp2.logout_time OR action NE "UPDATE" THEN
DO:
  PUT STREAM s-audit-rpt UNFORMATTED "Logout Time: " AT 3 ttbl_machemp1.logout_time FORMAT "->,>>>,>>9" " (logout_time)".
  IF action = "UPDATE" THEN
  DO:
    IF ttbl_machemp1.logout_time NE ttbl_machemp2.logout_time THEN
    PUT STREAM s-audit-rpt UNFORMATTED "*" AT 1.
    PUT STREAM s-audit-rpt UNFORMATTED "Logout Time: " AT 3 ttbl_machemp2.logout_time FORMAT "->,>>>,>>9".
  END.
END.
IF NOT v_field_changed OR ttbl_machemp1.posted NE ttbl_machemp2.posted OR action NE "UPDATE" THEN
DO:
  PUT STREAM s-audit-rpt UNFORMATTED "Posted: " AT 3 ttbl_machemp1.posted FORMAT "yes/no" " (posted)".
  IF action = "UPDATE" THEN
  DO:
    IF ttbl_machemp1.posted NE ttbl_machemp2.posted THEN
    PUT STREAM s-audit-rpt UNFORMATTED "*" AT 1.
    PUT STREAM s-audit-rpt UNFORMATTED "Posted: " AT 3 ttbl_machemp2.posted FORMAT "yes/no".
  END.
END.
IF NOT v_field_changed OR ttbl_machemp1.rate NE ttbl_machemp2.rate OR action NE "UPDATE" THEN
DO:
  PUT STREAM s-audit-rpt UNFORMATTED "Rate: " AT 3 ttbl_machemp1.rate FORMAT ">>>>9.99<<<" " (rate)".
  IF action = "UPDATE" THEN
  DO:
    IF ttbl_machemp1.rate NE ttbl_machemp2.rate THEN
    PUT STREAM s-audit-rpt UNFORMATTED "*" AT 1.
    PUT STREAM s-audit-rpt UNFORMATTED "Rate: " AT 3 ttbl_machemp2.rate FORMAT ">>>>9.99<<<".
  END.
END.
IF NOT v_field_changed OR ttbl_machemp1.ratetype NE ttbl_machemp2.ratetype OR action NE "UPDATE" THEN
DO:
  PUT STREAM s-audit-rpt UNFORMATTED "Type: " AT 3 ttbl_machemp1.ratetype " (ratetype)".
  IF action = "UPDATE" THEN
  DO:
    IF ttbl_machemp1.ratetype NE ttbl_machemp2.ratetype THEN
    PUT STREAM s-audit-rpt UNFORMATTED "*" AT 1.
    PUT STREAM s-audit-rpt UNFORMATTED "Type: " AT 3 ttbl_machemp2.ratetype.
  END.
END.
IF NOT v_field_changed OR ttbl_machemp1.rate_usage NE ttbl_machemp2.rate_usage OR action NE "UPDATE" THEN
DO:
  PUT STREAM s-audit-rpt UNFORMATTED "Rate Use: " AT 3 ttbl_machemp1.rate_usage FORMAT "Shift/Machine" " (rate_usage)".
  IF action = "UPDATE" THEN
  DO:
    IF ttbl_machemp1.rate_usage NE ttbl_machemp2.rate_usage THEN
    PUT STREAM s-audit-rpt UNFORMATTED "*" AT 1.
    PUT STREAM s-audit-rpt UNFORMATTED "Rate Use: " AT 3 ttbl_machemp2.rate_usage FORMAT "Shift/Machine".
  END.
END.
IF NOT v_field_changed OR ttbl_machemp1.shift NE ttbl_machemp2.shift OR action NE "UPDATE" THEN
DO:
  PUT STREAM s-audit-rpt UNFORMATTED "Shift: " AT 3 ttbl_machemp1.shift " (shift)".
  IF action = "UPDATE" THEN
  DO:
    IF ttbl_machemp1.shift NE ttbl_machemp2.shift THEN
    PUT STREAM s-audit-rpt UNFORMATTED "*" AT 1.
    PUT STREAM s-audit-rpt UNFORMATTED "Shift: " AT 3 ttbl_machemp2.shift.
  END.
END.
IF NOT v_field_changed OR ttbl_machemp1.start_date NE ttbl_machemp2.start_date OR action NE "UPDATE" THEN
DO:
  PUT STREAM s-audit-rpt UNFORMATTED "Start Date: " AT 3 ttbl_machemp1.start_date FORMAT "99/99/9999" " (start_date)".
  IF action = "UPDATE" THEN
  DO:
    IF ttbl_machemp1.start_date NE ttbl_machemp2.start_date THEN
    PUT STREAM s-audit-rpt UNFORMATTED "*" AT 1.
    PUT STREAM s-audit-rpt UNFORMATTED "Start Date: " AT 3 ttbl_machemp2.start_date FORMAT "99/99/9999".
  END.
END.
IF NOT v_field_changed OR ttbl_machemp1.start_time NE ttbl_machemp2.start_time OR action NE "UPDATE" THEN
DO:
  PUT STREAM s-audit-rpt UNFORMATTED "Start Time: " AT 3 ttbl_machemp1.start_time FORMAT "->,>>>,>>9" " (start_time)".
  IF action = "UPDATE" THEN
  DO:
    IF ttbl_machemp1.start_time NE ttbl_machemp2.start_time THEN
    PUT STREAM s-audit-rpt UNFORMATTED "*" AT 1.
    PUT STREAM s-audit-rpt UNFORMATTED "Start Time: " AT 3 ttbl_machemp2.start_time FORMAT "->,>>>,>>9".
  END.
END.
IF NOT v_field_changed OR ttbl_machemp1.table_rec_key NE ttbl_machemp2.table_rec_key OR action NE "UPDATE" THEN
DO:
  PUT STREAM s-audit-rpt UNFORMATTED "Table Record Key: " AT 3 ttbl_machemp1.table_rec_key " (table_rec_key)".
  IF action = "UPDATE" THEN
  DO:
    IF ttbl_machemp1.table_rec_key NE ttbl_machemp2.table_rec_key THEN
    PUT STREAM s-audit-rpt UNFORMATTED "*" AT 1.
    PUT STREAM s-audit-rpt UNFORMATTED "Table Record Key: " AT 3 ttbl_machemp2.table_rec_key.
  END.
END.
IF NOT v_field_changed OR ttbl_machemp1.total_time NE ttbl_machemp2.total_time OR action NE "UPDATE" THEN
DO:
  PUT STREAM s-audit-rpt UNFORMATTED "Total Time: " AT 3 ttbl_machemp1.total_time FORMAT "->,>>>,>>9" " (total_time)".
  IF action = "UPDATE" THEN
  DO:
    IF ttbl_machemp1.total_time NE ttbl_machemp2.total_time THEN
    PUT STREAM s-audit-rpt UNFORMATTED "*" AT 1.
    PUT STREAM s-audit-rpt UNFORMATTED "Total Time: " AT 3 ttbl_machemp2.total_time FORMAT "->,>>>,>>9".
  END.
END.
IF v_show_index THEN
DO:
  PUT STREAM s-audit-rpt UNFORMATTED "Index Fields - Label: Value (database field-name)" AT 5.
  PUT STREAM s-audit-rpt UNFORMATTED "Table Record Key: " AT 7.
  IF action = "UPDATE" THEN
  PUT STREAM s-audit-rpt UNFORMATTED ttbl_machemp2.table_rec_key " (table_rec_key)".
  ELSE
  PUT STREAM s-audit-rpt UNFORMATTED ttbl_machemp1.table_rec_key " (table_rec_key)".
  PUT STREAM s-audit-rpt UNFORMATTED "Emp ID: " AT 7.
  IF action = "UPDATE" THEN
  PUT STREAM s-audit-rpt UNFORMATTED ttbl_machemp2.employee " (employee)".
  ELSE
  PUT STREAM s-audit-rpt UNFORMATTED ttbl_machemp1.employee " (employee)".
  PUT STREAM s-audit-rpt UNFORMATTED "Start Date: " AT 7.
  IF action = "UPDATE" THEN
  PUT STREAM s-audit-rpt UNFORMATTED ttbl_machemp2.start_date FORMAT "99/99/9999" " (start_date)".
  ELSE
  PUT STREAM s-audit-rpt UNFORMATTED ttbl_machemp1.start_date FORMAT "99/99/9999" " (start_date)".
  PUT STREAM s-audit-rpt UNFORMATTED "Start Time: " AT 7.
  IF action = "UPDATE" THEN
  PUT STREAM s-audit-rpt UNFORMATTED ttbl_machemp2.start_time FORMAT "->,>>>,>>9" " (start_time)".
  ELSE
  PUT STREAM s-audit-rpt UNFORMATTED ttbl_machemp1.start_time FORMAT "->,>>>,>>9" " (start_time)".
END.
PUT STREAM s-audit-rpt UNFORMATTED SKIP(1).
