/* mailcont.p - generated 12/02/2005 - 16:52:39 by NoSweat */

/* this program was generated by auditgn1.p and should not be modified */
/* modifications should be made to the auto generation program         */

DEFINE INPUT PARAMETER action AS CHARACTER NO-UNDO.

DEFINE VARIABLE i AS INTEGER NO-UNDO.

{methods/defines/audit.i}
FIND FIRST ttbl_header.

DEFINE WORK-TABLE ttbl_mailcont1 NO-UNDO LIKE mailcont.
DEFINE WORK-TABLE ttbl_mailcont2 NO-UNDO LIKE mailcont.

CREATE ttbl_mailcont1.
IMPORT STREAM s_audit_file ttbl_mailcont1.
CREATE ttbl_mailcont2.
IF action = "UPDATE" THEN
IMPORT STREAM s_audit_file ttbl_mailcont2.

PUT STREAM s-audit-rpt UNFORMATTED
  "Action    User ID/(User Name)            DB.Table/(Database)  Date       Time" SKIP
  "--------- ------------------------------ -------------------- ---------- --------" SKIP
  ttbl_header.v_action AT 1
  ttbl_header.v_userid AT 11
  ttbl_header.v_file AT 42
  ttbl_header.v_date FORMAT "99/99/9999" AT 63
  STRING(ttbl_header.v_time,"HH:MM:SS") AT 74.
FIND users WHERE users.user_id = v_userid NO-LOCK NO-ERROR.
IF AVAILABLE users THEN
PUT STREAM s-audit-rpt UNFORMATTED "(" AT 10 users.user_name ")".
ELSE
PUT STREAM s-audit-rpt UNFORMATTED "(user record does not exist)" AT 10.
PUT STREAM s-audit-rpt UNFORMATTED
  "(" AT 41 ttbl_header.v_db ")" SKIP(1)
  "Label: Value (database field-name)" AT 3.
IF action = "UPDATE" THEN
PUT STREAM s-audit-rpt UNFORMATTED " ( * - denotes field value change )".
PUT STREAM s-audit-rpt UNFORMATTED FILL("-",75) AT 3.
IF NOT v_field_changed OR ttbl_mailcont1.contact-rec NE ttbl_mailcont2.contact-rec OR action NE "UPDATE" THEN
DO:
  PUT STREAM s-audit-rpt UNFORMATTED "Contact Record: " AT 3 ttbl_mailcont1.contact-rec FORMAT ">>>>>>9" " (contact-rec)".
  IF action = "UPDATE" THEN
  DO:
    IF ttbl_mailcont1.contact-rec NE ttbl_mailcont2.contact-rec THEN
    PUT STREAM s-audit-rpt UNFORMATTED "*" AT 1.
    PUT STREAM s-audit-rpt UNFORMATTED "Contact Record: " AT 3 ttbl_mailcont2.contact-rec FORMAT ">>>>>>9".
  END.
END.
IF NOT v_field_changed OR ttbl_mailcont1.contact-title NE ttbl_mailcont2.contact-title OR action NE "UPDATE" THEN
DO:
  PUT STREAM s-audit-rpt UNFORMATTED "Title: " AT 3 ttbl_mailcont1.contact-title " (contact-title)".
  IF action = "UPDATE" THEN
  DO:
    IF ttbl_mailcont1.contact-title NE ttbl_mailcont2.contact-title THEN
    PUT STREAM s-audit-rpt UNFORMATTED "*" AT 1.
    PUT STREAM s-audit-rpt UNFORMATTED "Title: " AT 3 ttbl_mailcont2.contact-title.
  END.
END.
IF NOT v_field_changed OR ttbl_mailcont1.cust-name NE ttbl_mailcont2.cust-name OR action NE "UPDATE" THEN
DO:
  PUT STREAM s-audit-rpt UNFORMATTED "Company: " AT 3 ttbl_mailcont1.cust-name " (cust-name)".
  IF action = "UPDATE" THEN
  DO:
    IF ttbl_mailcont1.cust-name NE ttbl_mailcont2.cust-name THEN
    PUT STREAM s-audit-rpt UNFORMATTED "*" AT 1.
    PUT STREAM s-audit-rpt UNFORMATTED "Company: " AT 3 ttbl_mailcont2.cust-name.
  END.
END.
IF NOT v_field_changed OR ttbl_mailcont1.cust-no NE ttbl_mailcont2.cust-no OR action NE "UPDATE" THEN
DO:
  PUT STREAM s-audit-rpt UNFORMATTED "Customer Number: " AT 3 ttbl_mailcont1.cust-no " (cust-no)".
  IF action = "UPDATE" THEN
  DO:
    IF ttbl_mailcont1.cust-no NE ttbl_mailcont2.cust-no THEN
    PUT STREAM s-audit-rpt UNFORMATTED "*" AT 1.
    PUT STREAM s-audit-rpt UNFORMATTED "Customer Number: " AT 3 ttbl_mailcont2.cust-no.
  END.
END.
IF NOT v_field_changed OR ttbl_mailcont1.first-name NE ttbl_mailcont2.first-name OR action NE "UPDATE" THEN
DO:
  PUT STREAM s-audit-rpt UNFORMATTED "First Name: " AT 3 ttbl_mailcont1.first-name " (first-name)".
  IF action = "UPDATE" THEN
  DO:
    IF ttbl_mailcont1.first-name NE ttbl_mailcont2.first-name THEN
    PUT STREAM s-audit-rpt UNFORMATTED "*" AT 1.
    PUT STREAM s-audit-rpt UNFORMATTED "First Name: " AT 3 ttbl_mailcont2.first-name.
  END.
END.
IF NOT v_field_changed OR ttbl_mailcont1.last-name NE ttbl_mailcont2.last-name OR action NE "UPDATE" THEN
DO:
  PUT STREAM s-audit-rpt UNFORMATTED "Last Name: " AT 3 ttbl_mailcont1.last-name " (last-name)".
  IF action = "UPDATE" THEN
  DO:
    IF ttbl_mailcont1.last-name NE ttbl_mailcont2.last-name THEN
    PUT STREAM s-audit-rpt UNFORMATTED "*" AT 1.
    PUT STREAM s-audit-rpt UNFORMATTED "Last Name: " AT 3 ttbl_mailcont2.last-name.
  END.
END.
IF NOT v_field_changed OR ttbl_mailcont1.list-no NE ttbl_mailcont2.list-no OR action NE "UPDATE" THEN
DO:
  PUT STREAM s-audit-rpt UNFORMATTED "List#: " AT 3 ttbl_mailcont1.list-no FORMAT ">>>>>9" " (list-no)".
  IF action = "UPDATE" THEN
  DO:
    IF ttbl_mailcont1.list-no NE ttbl_mailcont2.list-no THEN
    PUT STREAM s-audit-rpt UNFORMATTED "*" AT 1.
    PUT STREAM s-audit-rpt UNFORMATTED "List#: " AT 3 ttbl_mailcont2.list-no FORMAT ">>>>>9".
  END.
END.
IF NOT v_field_changed OR ttbl_mailcont1.maillist NE ttbl_mailcont2.maillist OR action NE "UPDATE" THEN
DO:
  PUT STREAM s-audit-rpt UNFORMATTED "Mail List: " AT 3 ttbl_mailcont1.maillist FORMAT "yes/no" " (maillist)".
  IF action = "UPDATE" THEN
  DO:
    IF ttbl_mailcont1.maillist NE ttbl_mailcont2.maillist THEN
    PUT STREAM s-audit-rpt UNFORMATTED "*" AT 1.
    PUT STREAM s-audit-rpt UNFORMATTED "Mail List: " AT 3 ttbl_mailcont2.maillist FORMAT "yes/no".
  END.
END.
IF NOT v_field_changed OR ttbl_mailcont1.middle-initial NE ttbl_mailcont2.middle-initial OR action NE "UPDATE" THEN
DO:
  PUT STREAM s-audit-rpt UNFORMATTED "Middle Initial: " AT 3 ttbl_mailcont1.middle-initial " (middle-initial)".
  IF action = "UPDATE" THEN
  DO:
    IF ttbl_mailcont1.middle-initial NE ttbl_mailcont2.middle-initial THEN
    PUT STREAM s-audit-rpt UNFORMATTED "*" AT 1.
    PUT STREAM s-audit-rpt UNFORMATTED "Middle Initial: " AT 3 ttbl_mailcont2.middle-initial.
  END.
END.
IF NOT v_field_changed OR ttbl_mailcont1.sman NE ttbl_mailcont2.sman OR action NE "UPDATE" THEN
DO:
  PUT STREAM s-audit-rpt UNFORMATTED "Salesman: " AT 3 ttbl_mailcont1.sman " (sman)".
  IF action = "UPDATE" THEN
  DO:
    IF ttbl_mailcont1.sman NE ttbl_mailcont2.sman THEN
    PUT STREAM s-audit-rpt UNFORMATTED "*" AT 1.
    PUT STREAM s-audit-rpt UNFORMATTED "Salesman: " AT 3 ttbl_mailcont2.sman.
  END.
END.
IF v_show_index THEN
DO:
  PUT STREAM s-audit-rpt UNFORMATTED "Index Fields - Label: Value (database field-name)" AT 5.
  PUT STREAM s-audit-rpt UNFORMATTED "List#: " AT 7.
  IF action = "UPDATE" THEN
  PUT STREAM s-audit-rpt UNFORMATTED ttbl_mailcont2.list-no FORMAT ">>>>>9" " (list-no)".
  ELSE
  PUT STREAM s-audit-rpt UNFORMATTED ttbl_mailcont1.list-no FORMAT ">>>>>9" " (list-no)".
  PUT STREAM s-audit-rpt UNFORMATTED "Contact Record: " AT 7.
  IF action = "UPDATE" THEN
  PUT STREAM s-audit-rpt UNFORMATTED ttbl_mailcont2.contact-rec FORMAT ">>>>>>9" " (contact-rec)".
  ELSE
  PUT STREAM s-audit-rpt UNFORMATTED ttbl_mailcont1.contact-rec FORMAT ">>>>>>9" " (contact-rec)".
END.
PUT STREAM s-audit-rpt UNFORMATTED SKIP(1).
