/* reftable.p - generated 01/10/2001 - 13:00:08 by nosweat */

/* this program was generated by auditgn1.p and should not be modified */
/* modifications should be made to the auto generation program         */

DEFINE INPUT PARAMETER action AS CHARACTER NO-UNDO.

DEFINE VARIABLE i AS INTEGER NO-UNDO.

{methods/defines/audit.i}
FIND FIRST ttbl_header.

DEFINE WORK-TABLE ttbl_reftable1 NO-UNDO LIKE reftable.
DEFINE WORK-TABLE ttbl_reftable2 NO-UNDO LIKE reftable.

CREATE ttbl_reftable1.
IMPORT STREAM s_audit_file ttbl_reftable1.
CREATE ttbl_reftable2.
IF action = "UPDATE" THEN
IMPORT STREAM s_audit_file ttbl_reftable2.

PUT STREAM s-audit-rpt UNFORMATTED
  "Action    User ID/(User Name)            DB.Table/(Database)  Date       Time" SKIP
  "--------- ------------------------------ -------------------- ---------- --------" SKIP
  ttbl_header.v_action AT 1
  ttbl_header.v_userid AT 11
  ttbl_header.v_file AT 42
  ttbl_header.v_date FORMAT "99/99/9999" AT 63
  STRING(ttbl_header.v_time,"HH:MM:SS") AT 74.
FIND users WHERE users.user_id = v_userid NO-LOCK NO-ERROR.
IF AVAILABLE users THEN
PUT STREAM s-audit-rpt UNFORMATTED "(" AT 10 users.user_name ")".
ELSE
PUT STREAM s-audit-rpt UNFORMATTED "(user record does not exist)" AT 10.
PUT STREAM s-audit-rpt UNFORMATTED
  "(" AT 41 ttbl_header.v_db ")" SKIP(1)
  "Label: Value (database field-name)" AT 3.
IF action = "UPDATE" THEN
PUT STREAM s-audit-rpt UNFORMATTED " ( * - denotes field value change )".
PUT STREAM s-audit-rpt UNFORMATTED FILL("-",75) AT 3.
IF NOT v_field_changed OR ttbl_reftable1.code NE ttbl_reftable2.code OR action NE "UPDATE" THEN
DO:
  PUT STREAM s-audit-rpt UNFORMATTED "Code: " AT 3 ttbl_reftable1.code " (code)".
  IF action = "UPDATE" THEN
  DO:
    IF ttbl_reftable1.code NE ttbl_reftable2.code THEN
    PUT STREAM s-audit-rpt UNFORMATTED "*" AT 1.
    PUT STREAM s-audit-rpt UNFORMATTED "Code: " AT 3 ttbl_reftable2.code.
  END.
END.
IF NOT v_field_changed OR ttbl_reftable1.code2 NE ttbl_reftable2.code2 OR action NE "UPDATE" THEN
DO:
  PUT STREAM s-audit-rpt UNFORMATTED "Code 2: " AT 3 ttbl_reftable1.code2 " (code2)".
  IF action = "UPDATE" THEN
  DO:
    IF ttbl_reftable1.code2 NE ttbl_reftable2.code2 THEN
    PUT STREAM s-audit-rpt UNFORMATTED "*" AT 1.
    PUT STREAM s-audit-rpt UNFORMATTED "Code 2: " AT 3 ttbl_reftable2.code2.
  END.
END.
IF NOT v_field_changed OR ttbl_reftable1.company NE ttbl_reftable2.company OR action NE "UPDATE" THEN
DO:
  PUT STREAM s-audit-rpt UNFORMATTED "Company: " AT 3 ttbl_reftable1.company " (company)".
  IF action = "UPDATE" THEN
  DO:
    IF ttbl_reftable1.company NE ttbl_reftable2.company THEN
    PUT STREAM s-audit-rpt UNFORMATTED "*" AT 1.
    PUT STREAM s-audit-rpt UNFORMATTED "Company: " AT 3 ttbl_reftable2.company.
  END.
END.
IF NOT v_field_changed OR ttbl_reftable1.dscr NE ttbl_reftable2.dscr OR action NE "UPDATE" THEN
DO:
  PUT STREAM s-audit-rpt UNFORMATTED "Description: " AT 3 ttbl_reftable1.dscr " (dscr)".
  IF action = "UPDATE" THEN
  DO:
    IF ttbl_reftable1.dscr NE ttbl_reftable2.dscr THEN
    PUT STREAM s-audit-rpt UNFORMATTED "*" AT 1.
    PUT STREAM s-audit-rpt UNFORMATTED "Description: " AT 3 ttbl_reftable2.dscr.
  END.
END.
IF NOT v_field_changed OR ttbl_reftable1.loc NE ttbl_reftable2.loc OR action NE "UPDATE" THEN
DO:
  PUT STREAM s-audit-rpt UNFORMATTED "Warehouse: " AT 3 ttbl_reftable1.loc " (loc)".
  IF action = "UPDATE" THEN
  DO:
    IF ttbl_reftable1.loc NE ttbl_reftable2.loc THEN
    PUT STREAM s-audit-rpt UNFORMATTED "*" AT 1.
    PUT STREAM s-audit-rpt UNFORMATTED "Warehouse: " AT 3 ttbl_reftable2.loc.
  END.
END.
IF NOT v_field_changed OR ttbl_reftable1.reftable NE ttbl_reftable2.reftable OR action NE "UPDATE" THEN
DO:
  PUT STREAM s-audit-rpt UNFORMATTED "Table: " AT 3 ttbl_reftable1.reftable " (reftable)".
  IF action = "UPDATE" THEN
  DO:
    IF ttbl_reftable1.reftable NE ttbl_reftable2.reftable THEN
    PUT STREAM s-audit-rpt UNFORMATTED "*" AT 1.
    PUT STREAM s-audit-rpt UNFORMATTED "Table: " AT 3 ttbl_reftable2.reftable.
  END.
END.
DO i = 1 TO 13:
IF NOT v_field_changed OR ttbl_reftable1.val[i] NE ttbl_reftable2.val[i] OR action NE "UPDATE" THEN
DO:
  PUT STREAM s-audit-rpt UNFORMATTED "Values[" AT 3 i "]: " ttbl_reftable1.val[i] FORMAT "->>,>>9.99" " (val[" i "])".
  IF action = "UPDATE" THEN
  DO:
    IF ttbl_reftable1.val[i] NE ttbl_reftable2.val[i] THEN
    PUT STREAM s-audit-rpt UNFORMATTED "*" AT 1.
    PUT STREAM s-audit-rpt UNFORMATTED "Values[" AT 3 i "]: " ttbl_reftable2.val[i] FORMAT "->>,>>9.99".
  END.
END.
END.
IF v_show_index THEN
DO:
  PUT STREAM s-audit-rpt UNFORMATTED "Index Fields - Label: Value (database field-name)" AT 5.
  PUT STREAM s-audit-rpt UNFORMATTED "Table: " AT 7.
  IF action = "UPDATE" THEN
  PUT STREAM s-audit-rpt UNFORMATTED ttbl_reftable2.reftable " (reftable)".
  ELSE
  PUT STREAM s-audit-rpt UNFORMATTED ttbl_reftable1.reftable " (reftable)".
  PUT STREAM s-audit-rpt UNFORMATTED "Company: " AT 7.
  IF action = "UPDATE" THEN
  PUT STREAM s-audit-rpt UNFORMATTED ttbl_reftable2.company " (company)".
  ELSE
  PUT STREAM s-audit-rpt UNFORMATTED ttbl_reftable1.company " (company)".
  PUT STREAM s-audit-rpt UNFORMATTED "Warehouse: " AT 7.
  IF action = "UPDATE" THEN
  PUT STREAM s-audit-rpt UNFORMATTED ttbl_reftable2.loc " (loc)".
  ELSE
  PUT STREAM s-audit-rpt UNFORMATTED ttbl_reftable1.loc " (loc)".
  PUT STREAM s-audit-rpt UNFORMATTED "Code: " AT 7.
  IF action = "UPDATE" THEN
  PUT STREAM s-audit-rpt UNFORMATTED ttbl_reftable2.code " (code)".
  ELSE
  PUT STREAM s-audit-rpt UNFORMATTED ttbl_reftable1.code " (code)".
  PUT STREAM s-audit-rpt UNFORMATTED "Code 2: " AT 7.
  IF action = "UPDATE" THEN
  PUT STREAM s-audit-rpt UNFORMATTED ttbl_reftable2.code2 " (code2)".
  ELSE
  PUT STREAM s-audit-rpt UNFORMATTED ttbl_reftable1.code2 " (code2)".
END.
PUT STREAM s-audit-rpt UNFORMATTED SKIP(1).
