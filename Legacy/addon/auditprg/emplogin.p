/* emplogin.p - generated 12/02/2005 - 16:52:38 by NoSweat */

/* this program was generated by auditgn1.p and should not be modified */
/* modifications should be made to the auto generation program         */

DEFINE INPUT PARAMETER action AS CHARACTER NO-UNDO.

DEFINE VARIABLE i AS INTEGER NO-UNDO.

{methods/defines/audit.i}
FIND FIRST ttbl_header.

DEFINE WORK-TABLE ttbl_emplogin1 NO-UNDO LIKE emplogin.
DEFINE WORK-TABLE ttbl_emplogin2 NO-UNDO LIKE emplogin.

CREATE ttbl_emplogin1.
IMPORT STREAM s_audit_file ttbl_emplogin1.
CREATE ttbl_emplogin2.
IF action = "UPDATE" THEN
IMPORT STREAM s_audit_file ttbl_emplogin2.

PUT STREAM s-audit-rpt UNFORMATTED
  "Action    User ID/(User Name)            DB.Table/(Database)  Date       Time" SKIP
  "--------- ------------------------------ -------------------- ---------- --------" SKIP
  ttbl_header.v_action AT 1
  ttbl_header.v_userid AT 11
  ttbl_header.v_file AT 42
  ttbl_header.v_date FORMAT "99/99/9999" AT 63
  STRING(ttbl_header.v_time,"HH:MM:SS") AT 74.
FIND users WHERE users.user_id = v_userid NO-LOCK NO-ERROR.
IF AVAILABLE users THEN
PUT STREAM s-audit-rpt UNFORMATTED "(" AT 10 users.user_name ")".
ELSE
PUT STREAM s-audit-rpt UNFORMATTED "(user record does not exist)" AT 10.
PUT STREAM s-audit-rpt UNFORMATTED
  "(" AT 41 ttbl_header.v_db ")" SKIP(1)
  "Label: Value (database field-name)" AT 3.
IF action = "UPDATE" THEN
PUT STREAM s-audit-rpt UNFORMATTED " ( * - denotes field value change )".
PUT STREAM s-audit-rpt UNFORMATTED FILL("-",75) AT 3.
IF NOT v_field_changed OR ttbl_emplogin1.company NE ttbl_emplogin2.company OR action NE "UPDATE" THEN
DO:
  PUT STREAM s-audit-rpt UNFORMATTED "Company: " AT 3 ttbl_emplogin1.company " (company)".
  IF action = "UPDATE" THEN
  DO:
    IF ttbl_emplogin1.company NE ttbl_emplogin2.company THEN
    PUT STREAM s-audit-rpt UNFORMATTED "*" AT 1.
    PUT STREAM s-audit-rpt UNFORMATTED "Company: " AT 3 ttbl_emplogin2.company.
  END.
END.
IF NOT v_field_changed OR ttbl_emplogin1.employee NE ttbl_emplogin2.employee OR action NE "UPDATE" THEN
DO:
  PUT STREAM s-audit-rpt UNFORMATTED "Emp ID: " AT 3 ttbl_emplogin1.employee " (employee)".
  IF action = "UPDATE" THEN
  DO:
    IF ttbl_emplogin1.employee NE ttbl_emplogin2.employee THEN
    PUT STREAM s-audit-rpt UNFORMATTED "*" AT 1.
    PUT STREAM s-audit-rpt UNFORMATTED "Emp ID: " AT 3 ttbl_emplogin2.employee.
  END.
END.
IF NOT v_field_changed OR ttbl_emplogin1.end_date NE ttbl_emplogin2.end_date OR action NE "UPDATE" THEN
DO:
  PUT STREAM s-audit-rpt UNFORMATTED "End Date: " AT 3 ttbl_emplogin1.end_date FORMAT "99/99/9999" " (end_date)".
  IF action = "UPDATE" THEN
  DO:
    IF ttbl_emplogin1.end_date NE ttbl_emplogin2.end_date THEN
    PUT STREAM s-audit-rpt UNFORMATTED "*" AT 1.
    PUT STREAM s-audit-rpt UNFORMATTED "End Date: " AT 3 ttbl_emplogin2.end_date FORMAT "99/99/9999".
  END.
END.
IF NOT v_field_changed OR ttbl_emplogin1.end_time NE ttbl_emplogin2.end_time OR action NE "UPDATE" THEN
DO:
  PUT STREAM s-audit-rpt UNFORMATTED "End Time: " AT 3 ttbl_emplogin1.end_time FORMAT "->,>>>,>>9" " (end_time)".
  IF action = "UPDATE" THEN
  DO:
    IF ttbl_emplogin1.end_time NE ttbl_emplogin2.end_time THEN
    PUT STREAM s-audit-rpt UNFORMATTED "*" AT 1.
    PUT STREAM s-audit-rpt UNFORMATTED "End Time: " AT 3 ttbl_emplogin2.end_time FORMAT "->,>>>,>>9".
  END.
END.
IF NOT v_field_changed OR ttbl_emplogin1.login_time NE ttbl_emplogin2.login_time OR action NE "UPDATE" THEN
DO:
  PUT STREAM s-audit-rpt UNFORMATTED "Login Time: " AT 3 ttbl_emplogin1.login_time FORMAT "->,>>>,>>9" " (login_time)".
  IF action = "UPDATE" THEN
  DO:
    IF ttbl_emplogin1.login_time NE ttbl_emplogin2.login_time THEN
    PUT STREAM s-audit-rpt UNFORMATTED "*" AT 1.
    PUT STREAM s-audit-rpt UNFORMATTED "Login Time: " AT 3 ttbl_emplogin2.login_time FORMAT "->,>>>,>>9".
  END.
END.
IF NOT v_field_changed OR ttbl_emplogin1.logout_time NE ttbl_emplogin2.logout_time OR action NE "UPDATE" THEN
DO:
  PUT STREAM s-audit-rpt UNFORMATTED "Logout Time: " AT 3 ttbl_emplogin1.logout_time FORMAT "->,>>>,>>9" " (logout_time)".
  IF action = "UPDATE" THEN
  DO:
    IF ttbl_emplogin1.logout_time NE ttbl_emplogin2.logout_time THEN
    PUT STREAM s-audit-rpt UNFORMATTED "*" AT 1.
    PUT STREAM s-audit-rpt UNFORMATTED "Logout Time: " AT 3 ttbl_emplogin2.logout_time FORMAT "->,>>>,>>9".
  END.
END.
IF NOT v_field_changed OR ttbl_emplogin1.machine NE ttbl_emplogin2.machine OR action NE "UPDATE" THEN
DO:
  PUT STREAM s-audit-rpt UNFORMATTED "Machine: " AT 3 ttbl_emplogin1.machine " (machine)".
  IF action = "UPDATE" THEN
  DO:
    IF ttbl_emplogin1.machine NE ttbl_emplogin2.machine THEN
    PUT STREAM s-audit-rpt UNFORMATTED "*" AT 1.
    PUT STREAM s-audit-rpt UNFORMATTED "Machine: " AT 3 ttbl_emplogin2.machine.
  END.
END.
IF NOT v_field_changed OR ttbl_emplogin1.shift NE ttbl_emplogin2.shift OR action NE "UPDATE" THEN
DO:
  PUT STREAM s-audit-rpt UNFORMATTED "Shift: " AT 3 ttbl_emplogin1.shift " (shift)".
  IF action = "UPDATE" THEN
  DO:
    IF ttbl_emplogin1.shift NE ttbl_emplogin2.shift THEN
    PUT STREAM s-audit-rpt UNFORMATTED "*" AT 1.
    PUT STREAM s-audit-rpt UNFORMATTED "Shift: " AT 3 ttbl_emplogin2.shift.
  END.
END.
IF NOT v_field_changed OR ttbl_emplogin1.start_date NE ttbl_emplogin2.start_date OR action NE "UPDATE" THEN
DO:
  PUT STREAM s-audit-rpt UNFORMATTED "Start Date: " AT 3 ttbl_emplogin1.start_date FORMAT "99/99/9999" " (start_date)".
  IF action = "UPDATE" THEN
  DO:
    IF ttbl_emplogin1.start_date NE ttbl_emplogin2.start_date THEN
    PUT STREAM s-audit-rpt UNFORMATTED "*" AT 1.
    PUT STREAM s-audit-rpt UNFORMATTED "Start Date: " AT 3 ttbl_emplogin2.start_date FORMAT "99/99/9999".
  END.
END.
IF NOT v_field_changed OR ttbl_emplogin1.start_time NE ttbl_emplogin2.start_time OR action NE "UPDATE" THEN
DO:
  PUT STREAM s-audit-rpt UNFORMATTED "Start Time: " AT 3 ttbl_emplogin1.start_time FORMAT "->,>>>,>>9" " (start_time)".
  IF action = "UPDATE" THEN
  DO:
    IF ttbl_emplogin1.start_time NE ttbl_emplogin2.start_time THEN
    PUT STREAM s-audit-rpt UNFORMATTED "*" AT 1.
    PUT STREAM s-audit-rpt UNFORMATTED "Start Time: " AT 3 ttbl_emplogin2.start_time FORMAT "->,>>>,>>9".
  END.
END.
IF NOT v_field_changed OR ttbl_emplogin1.total_time NE ttbl_emplogin2.total_time OR action NE "UPDATE" THEN
DO:
  PUT STREAM s-audit-rpt UNFORMATTED "Total Time: " AT 3 ttbl_emplogin1.total_time FORMAT "->,>>>,>>9" " (total_time)".
  IF action = "UPDATE" THEN
  DO:
    IF ttbl_emplogin1.total_time NE ttbl_emplogin2.total_time THEN
    PUT STREAM s-audit-rpt UNFORMATTED "*" AT 1.
    PUT STREAM s-audit-rpt UNFORMATTED "Total Time: " AT 3 ttbl_emplogin2.total_time FORMAT "->,>>>,>>9".
  END.
END.
IF v_show_index THEN
DO:
  PUT STREAM s-audit-rpt UNFORMATTED "Index Fields - Label: Value (database field-name)" AT 5.
  PUT STREAM s-audit-rpt UNFORMATTED "Company: " AT 7.
  IF action = "UPDATE" THEN
  PUT STREAM s-audit-rpt UNFORMATTED ttbl_emplogin2.company " (company)".
  ELSE
  PUT STREAM s-audit-rpt UNFORMATTED ttbl_emplogin1.company " (company)".
  PUT STREAM s-audit-rpt UNFORMATTED "Emp ID: " AT 7.
  IF action = "UPDATE" THEN
  PUT STREAM s-audit-rpt UNFORMATTED ttbl_emplogin2.employee " (employee)".
  ELSE
  PUT STREAM s-audit-rpt UNFORMATTED ttbl_emplogin1.employee " (employee)".
  PUT STREAM s-audit-rpt UNFORMATTED "Start Date: " AT 7.
  IF action = "UPDATE" THEN
  PUT STREAM s-audit-rpt UNFORMATTED ttbl_emplogin2.start_date FORMAT "99/99/9999" " (start_date)".
  ELSE
  PUT STREAM s-audit-rpt UNFORMATTED ttbl_emplogin1.start_date FORMAT "99/99/9999" " (start_date)".
  PUT STREAM s-audit-rpt UNFORMATTED "Start Time: " AT 7.
  IF action = "UPDATE" THEN
  PUT STREAM s-audit-rpt UNFORMATTED ttbl_emplogin2.start_time FORMAT "->,>>>,>>9" " (start_time)".
  ELSE
  PUT STREAM s-audit-rpt UNFORMATTED ttbl_emplogin1.start_time FORMAT "->,>>>,>>9" " (start_time)".
  PUT STREAM s-audit-rpt UNFORMATTED "Machine: " AT 7.
  IF action = "UPDATE" THEN
  PUT STREAM s-audit-rpt UNFORMATTED ttbl_emplogin2.machine " (machine)".
  ELSE
  PUT STREAM s-audit-rpt UNFORMATTED ttbl_emplogin1.machine " (machine)".
END.
PUT STREAM s-audit-rpt UNFORMATTED SKIP(1).
