/* zipcode.p - generated 12/02/2005 - 16:52:37 by NoSweat */

/* this program was generated by auditgn1.p and should not be modified */
/* modifications should be made to the auto generation program         */

DEFINE INPUT PARAMETER action AS CHARACTER NO-UNDO.

DEFINE VARIABLE i AS INTEGER NO-UNDO.

{methods/defines/audit.i}
FIND FIRST ttbl_header.

DEFINE WORK-TABLE ttbl_zipcode1 NO-UNDO LIKE zipcode.
DEFINE WORK-TABLE ttbl_zipcode2 NO-UNDO LIKE zipcode.

CREATE ttbl_zipcode1.
IMPORT STREAM s_audit_file ttbl_zipcode1.
CREATE ttbl_zipcode2.
IF action = "UPDATE" THEN
IMPORT STREAM s_audit_file ttbl_zipcode2.

PUT STREAM s-audit-rpt UNFORMATTED
  "Action    User ID/(User Name)            DB.Table/(Database)  Date       Time" SKIP
  "--------- ------------------------------ -------------------- ---------- --------" SKIP
  ttbl_header.v_action AT 1
  ttbl_header.v_userid AT 11
  ttbl_header.v_file AT 42
  ttbl_header.v_date FORMAT "99/99/9999" AT 63
  STRING(ttbl_header.v_time,"HH:MM:SS") AT 74.
FIND users WHERE users.user_id = v_userid NO-LOCK NO-ERROR.
IF AVAILABLE users THEN
PUT STREAM s-audit-rpt UNFORMATTED "(" AT 10 users.user_name ")".
ELSE
PUT STREAM s-audit-rpt UNFORMATTED "(user record does not exist)" AT 10.
PUT STREAM s-audit-rpt UNFORMATTED
  "(" AT 41 ttbl_header.v_db ")" SKIP(1)
  "Label: Value (database field-name)" AT 3.
IF action = "UPDATE" THEN
PUT STREAM s-audit-rpt UNFORMATTED " ( * - denotes field value change )".
PUT STREAM s-audit-rpt UNFORMATTED FILL("-",75) AT 3.
IF NOT v_field_changed OR ttbl_zipcode1.area_code NE ttbl_zipcode2.area_code OR action NE "UPDATE" THEN
DO:
  PUT STREAM s-audit-rpt UNFORMATTED "Area Code: " AT 3 ttbl_zipcode1.area_code FORMAT "999" " (area_code)".
  IF action = "UPDATE" THEN
  DO:
    IF ttbl_zipcode1.area_code NE ttbl_zipcode2.area_code THEN
    PUT STREAM s-audit-rpt UNFORMATTED "*" AT 1.
    PUT STREAM s-audit-rpt UNFORMATTED "Area Code: " AT 3 ttbl_zipcode2.area_code FORMAT "999".
  END.
END.
IF NOT v_field_changed OR ttbl_zipcode1.city NE ttbl_zipcode2.city OR action NE "UPDATE" THEN
DO:
  PUT STREAM s-audit-rpt UNFORMATTED "City: " AT 3 ttbl_zipcode1.city " (city)".
  IF action = "UPDATE" THEN
  DO:
    IF ttbl_zipcode1.city NE ttbl_zipcode2.city THEN
    PUT STREAM s-audit-rpt UNFORMATTED "*" AT 1.
    PUT STREAM s-audit-rpt UNFORMATTED "City: " AT 3 ttbl_zipcode2.city.
  END.
END.
IF NOT v_field_changed OR ttbl_zipcode1.country NE ttbl_zipcode2.country OR action NE "UPDATE" THEN
DO:
  PUT STREAM s-audit-rpt UNFORMATTED "Country: " AT 3 ttbl_zipcode1.country " (country)".
  IF action = "UPDATE" THEN
  DO:
    IF ttbl_zipcode1.country NE ttbl_zipcode2.country THEN
    PUT STREAM s-audit-rpt UNFORMATTED "*" AT 1.
    PUT STREAM s-audit-rpt UNFORMATTED "Country: " AT 3 ttbl_zipcode2.country.
  END.
END.
IF NOT v_field_changed OR ttbl_zipcode1.county NE ttbl_zipcode2.county OR action NE "UPDATE" THEN
DO:
  PUT STREAM s-audit-rpt UNFORMATTED "County: " AT 3 ttbl_zipcode1.county " (county)".
  IF action = "UPDATE" THEN
  DO:
    IF ttbl_zipcode1.county NE ttbl_zipcode2.county THEN
    PUT STREAM s-audit-rpt UNFORMATTED "*" AT 1.
    PUT STREAM s-audit-rpt UNFORMATTED "County: " AT 3 ttbl_zipcode2.county.
  END.
END.
IF NOT v_field_changed OR ttbl_zipcode1.dst NE ttbl_zipcode2.dst OR action NE "UPDATE" THEN
DO:
  PUT STREAM s-audit-rpt UNFORMATTED "Daylight Savings Time: " AT 3 ttbl_zipcode1.dst FORMAT "yes/no" " (dst)".
  IF action = "UPDATE" THEN
  DO:
    IF ttbl_zipcode1.dst NE ttbl_zipcode2.dst THEN
    PUT STREAM s-audit-rpt UNFORMATTED "*" AT 1.
    PUT STREAM s-audit-rpt UNFORMATTED "Daylight Savings Time: " AT 3 ttbl_zipcode2.dst FORMAT "yes/no".
  END.
END.
IF NOT v_field_changed OR ttbl_zipcode1.fips_code NE ttbl_zipcode2.fips_code OR action NE "UPDATE" THEN
DO:
  PUT STREAM s-audit-rpt UNFORMATTED "FIPS Code: " AT 3 ttbl_zipcode1.fips_code FORMAT "99999" " (fips_code)".
  IF action = "UPDATE" THEN
  DO:
    IF ttbl_zipcode1.fips_code NE ttbl_zipcode2.fips_code THEN
    PUT STREAM s-audit-rpt UNFORMATTED "*" AT 1.
    PUT STREAM s-audit-rpt UNFORMATTED "FIPS Code: " AT 3 ttbl_zipcode2.fips_code FORMAT "99999".
  END.
END.
IF NOT v_field_changed OR ttbl_zipcode1.pref# NE ttbl_zipcode2.pref# OR action NE "UPDATE" THEN
DO:
  PUT STREAM s-audit-rpt UNFORMATTED "Preference #: " AT 3 ttbl_zipcode1.pref# FORMAT "99" " (pref#)".
  IF action = "UPDATE" THEN
  DO:
    IF ttbl_zipcode1.pref# NE ttbl_zipcode2.pref# THEN
    PUT STREAM s-audit-rpt UNFORMATTED "*" AT 1.
    PUT STREAM s-audit-rpt UNFORMATTED "Preference #: " AT 3 ttbl_zipcode2.pref# FORMAT "99".
  END.
END.
IF NOT v_field_changed OR ttbl_zipcode1.pref_type NE ttbl_zipcode2.pref_type OR action NE "UPDATE" THEN
DO:
  PUT STREAM s-audit-rpt UNFORMATTED "Preference Type: " AT 3 ttbl_zipcode1.pref_type " (pref_type)".
  IF action = "UPDATE" THEN
  DO:
    IF ttbl_zipcode1.pref_type NE ttbl_zipcode2.pref_type THEN
    PUT STREAM s-audit-rpt UNFORMATTED "*" AT 1.
    PUT STREAM s-audit-rpt UNFORMATTED "Preference Type: " AT 3 ttbl_zipcode2.pref_type.
  END.
END.
IF NOT v_field_changed OR ttbl_zipcode1.state NE ttbl_zipcode2.state OR action NE "UPDATE" THEN
DO:
  PUT STREAM s-audit-rpt UNFORMATTED "Province/State: " AT 3 ttbl_zipcode1.state " (state)".
  IF action = "UPDATE" THEN
  DO:
    IF ttbl_zipcode1.state NE ttbl_zipcode2.state THEN
    PUT STREAM s-audit-rpt UNFORMATTED "*" AT 1.
    PUT STREAM s-audit-rpt UNFORMATTED "Province/State: " AT 3 ttbl_zipcode2.state.
  END.
END.
IF NOT v_field_changed OR ttbl_zipcode1.time_zone NE ttbl_zipcode2.time_zone OR action NE "UPDATE" THEN
DO:
  PUT STREAM s-audit-rpt UNFORMATTED "Time Zone: " AT 3 ttbl_zipcode1.time_zone " (time_zone)".
  IF action = "UPDATE" THEN
  DO:
    IF ttbl_zipcode1.time_zone NE ttbl_zipcode2.time_zone THEN
    PUT STREAM s-audit-rpt UNFORMATTED "*" AT 1.
    PUT STREAM s-audit-rpt UNFORMATTED "Time Zone: " AT 3 ttbl_zipcode2.time_zone.
  END.
END.
IF NOT v_field_changed OR ttbl_zipcode1.zipcode NE ttbl_zipcode2.zipcode OR action NE "UPDATE" THEN
DO:
  PUT STREAM s-audit-rpt UNFORMATTED "Zip/Postal Code: " AT 3 ttbl_zipcode1.zipcode " (zipcode)".
  IF action = "UPDATE" THEN
  DO:
    IF ttbl_zipcode1.zipcode NE ttbl_zipcode2.zipcode THEN
    PUT STREAM s-audit-rpt UNFORMATTED "*" AT 1.
    PUT STREAM s-audit-rpt UNFORMATTED "Zip/Postal Code: " AT 3 ttbl_zipcode2.zipcode.
  END.
END.
IF v_show_index THEN
DO:
  PUT STREAM s-audit-rpt UNFORMATTED "Index Fields - Label: Value (database field-name)" AT 5.
  PUT STREAM s-audit-rpt UNFORMATTED "Zip/Postal Code: " AT 7.
  IF action = "UPDATE" THEN
  PUT STREAM s-audit-rpt UNFORMATTED ttbl_zipcode2.zipcode " (zipcode)".
  ELSE
  PUT STREAM s-audit-rpt UNFORMATTED ttbl_zipcode1.zipcode " (zipcode)".
  PUT STREAM s-audit-rpt UNFORMATTED "Preference Type: " AT 7.
  IF action = "UPDATE" THEN
  PUT STREAM s-audit-rpt UNFORMATTED ttbl_zipcode2.pref_type " (pref_type)".
  ELSE
  PUT STREAM s-audit-rpt UNFORMATTED ttbl_zipcode1.pref_type " (pref_type)".
  PUT STREAM s-audit-rpt UNFORMATTED "Preference #: " AT 7.
  IF action = "UPDATE" THEN
  PUT STREAM s-audit-rpt UNFORMATTED ttbl_zipcode2.pref# FORMAT "99" " (pref#)".
  ELSE
  PUT STREAM s-audit-rpt UNFORMATTED ttbl_zipcode1.pref# FORMAT "99" " (pref#)".
END.
PUT STREAM s-audit-rpt UNFORMATTED SKIP(1).
