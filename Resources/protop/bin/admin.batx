@echo off

rem	examples of commands to be "run as admin" (from Windows-Explorer right-click on .BAT to execute "as admin")
rem

call C:\Users\Administrator.WIN-SIDPMPLNDIB\Downloads\protop\bin\protopenv.bat

cd /d %PROTOP%

set XDB=d:\db\dbname

rem	disconnect a specific user
rem
rem	call proshut %XDB% -C disconnect 16
rem	sleep 2

rem	online increase of a startup parameter
rem
rem	call proutil %XDB% -C increaseto -L 2000000
